#!/bin/sh
# SPDX-FileCopyrightText: 2026 The RISE Project
# SPDX-License-Identifier: MIT
#
# Installed via `git config core.hooksPath ci_scripts/git-hooks` (see
# ci_scripts/check_port_pr_scope.sh for what this actually checks and why).
exec "$(git rev-parse --show-toplevel)/ci_scripts/check_port_pr_scope.sh"
