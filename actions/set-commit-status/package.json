{
  "name": "set-commit-status",
  "version": "1.0.0",
  "private": true,
  "description": "Set a pending commit status before a workflow_dispatch job and the final outcome after it.",
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/pre.ts -o dist/pre && ncc build src/main.ts -o dist/main && ncc build src/post.ts -o dist/post",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.9.2"
  }
}
