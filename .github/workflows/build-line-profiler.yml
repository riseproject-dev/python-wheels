# SPDX-FileCopyrightText: 2026 The RISE Project
# SPDX-License-Identifier: MIT
---
# Based on the `build_binpy_wheels` job of
# https://github.com/pyutils/line_profiler/blob/v5.0.2/.github/workflows/tests.yml
name: Build line-profiler wheels (riscv64)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'line-profiler version to build (git tag without leading v, e.g. 5.0.2)'
        required: true
        default: '5.0.2'
  pull_request:
    paths:
      - '.github/workflows/build-line-profiler.yml'

concurrency:
  group: ${{ github.workflow }}-${{ inputs.version || '5.0.2' }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read  # to fetch code (actions/checkout)

env:
  LINE_PROFILER_VERSION: ${{ inputs.version || '5.0.2' }}
  MANYLINUX_RISCV64_IMAGE: quay.io/pypa/manylinux_2_39_riscv64
  MUSLLINUX_RISCV64_IMAGE: quay.io/pypa/musllinux_1_2_riscv64

jobs:
  setup:
    uses: $/.github/workflows/_setup.yml

  build_wheels:
    needs: [setup]
    name: Build line-profiler ${{ inputs.version || '5.0.2' }} ${{ matrix.python }}-${{ matrix.libc }}_riscv64
    runs-on: ubuntu-24.04-riscv
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        # cp314t is excluded: upstream's own `[tool.cibuildwheel] build` list
        # doesn't build it either.
        python: ["cp312", "cp313", "cp314"]
        libc: [manylinux, musllinux]

    steps:
      - name: Checkout line_profiler v${{ env.LINE_PROFILER_VERSION }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          repository: pyutils/line_profiler
          ref: v${{ env.LINE_PROFILER_VERSION }}
          persist-credentials: false

      - uses: pypa/cibuildwheel@1828c10ab37f080699c7b81cea34097c684a7074  # v4.2.0
        env:
          CIBW_ARCHS: riscv64
          CIBW_BUILD: ${{ matrix.python }}-${{ matrix.libc }}_riscv64
          CIBW_MANYLINUX_RISCV64_IMAGE: ${{ env.MANYLINUX_RISCV64_IMAGE }}
          CIBW_MUSLLINUX_RISCV64_IMAGE: ${{ env.MUSLLINUX_RISCV64_IMAGE }}
          # Cython has no public riscv64 wheel; ours is in the registry.
          CIBW_ENVIRONMENT: PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/
          CIBW_TEST_COMMAND: >-
            python -c "import line_profiler._line_profiler as m;
            assert m.__file__.endswith('.so'), m.__file__" &&
            python {project}/run_tests.py

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: line-profiler-${{ env.LINE_PROFILER_VERSION }}-${{ matrix.python }}-${{ matrix.libc }}_riscv64
          path: ./wheelhouse/*.whl
          if-no-files-found: error

  publish:
    name: Publish line-profiler ${{ inputs.version || '5.0.2' }}
    needs: [setup, build_wheels]
    permissions:
      contents: write
      pull-requests: write
    uses: $/.github/workflows/_publish-wheel.yml
    with:
      artifact-pattern: line-profiler-${{ inputs.version || '5.0.2' }}-*riscv64
