# SPDX-FileCopyrightText: 2026 The RISE Project
# SPDX-License-Identifier: MIT
---
# Based on upstream's own (currently riscv64-disabled) wheel builder:
#   https://github.com/rhasspy/pyspeex-noise/blob/v2.0.1/.github/workflows/wheels.yml
name: Build pyspeex-noise wheels (riscv64)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'pyspeex-noise version to build (git tag without leading v, e.g. 2.0.1)'
        required: true
        default: '2.0.1'
  pull_request:
    paths:
      - '.github/workflows/build-pyspeex-noise.yml'

concurrency:
  group: ${{ github.workflow }}-${{ inputs.version || '2.0.1' }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read  # to fetch code (actions/checkout)

env:
  # `inputs.version` is empty on pull_request events; default to 2.0.1 there.
  PYSPEEX_NOISE_VERSION: ${{ inputs.version || '2.0.1' }}
  MANYLINUX_RISCV64_IMAGE: quay.io/pypa/manylinux_2_39_riscv64

jobs:
  setup:
    uses: $/.github/workflows/_setup.yml

  build_wheels:
    needs: [setup]
    name: Build pyspeex-noise ${{ inputs.version || '2.0.1' }} ${{ matrix.python }}-manylinux_riscv64
    runs-on: ubuntu-24.04-riscv
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        # setup.cfg hardcodes py_limited_api=cp39 (single abi3 wheel for all
        # 3.9+), and upstream's own wheels.yml only builds cp39-* (gotcha 96:
        # build on the floor interpreter its abi3 tag claims). No free-threaded
        # variant is published upstream either.
        python: ["cp39"]

    steps:
      - name: Checkout pyspeex-noise v${{ env.PYSPEEX_NOISE_VERSION }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          repository: rhasspy/pyspeex-noise
          ref: v${{ env.PYSPEEX_NOISE_VERSION }}
          persist-credentials: false

      - name: Build wheels
        uses: pypa/cibuildwheel@1828c10ab37f080699c7b81cea34097c684a7074  # v4.2.0
        with:
          output-dir: wheelhouse/
          only: ${{ matrix.python }}-manylinux_riscv64
        env:
          CIBW_MANYLINUX_RISCV64_IMAGE: ${{ env.MANYLINUX_RISCV64_IMAGE }}
          CIBW_TEST_REQUIRES: pytest syrupy
          CIBW_TEST_COMMAND: python -m pytest -vv {project}/tests

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: pyspeex-noise-${{ env.PYSPEEX_NOISE_VERSION }}-${{ matrix.python }}-manylinux_riscv64
          path: wheelhouse/*.whl
          if-no-files-found: error

  publish:
    name: Publish pyspeex-noise ${{ inputs.version || '2.0.1' }}
    needs: [setup, build_wheels]
    permissions:
      contents: write
      pull-requests: write
    uses: $/.github/workflows/_publish-wheel.yml
    with:
      artifact-pattern: pyspeex-noise-${{ inputs.version || '2.0.1' }}-*-manylinux_riscv64
