# SPDX-FileCopyrightText: 2026 The RISE Project
# SPDX-License-Identifier: MIT
---
# This workflow is based on: https://github.com/ada-url/ada-python/blob/v4.0.0/.github/workflows/build.yml
name: Build ada-url wheels (riscv64)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'ada-url version to build (git tag without the v prefix, e.g. 4.0.0)'
        required: true
        default: '4.0.0'
  pull_request:
    paths:
      - '.github/workflows/build-ada-url.yml'

concurrency:
  group: ${{ github.workflow }}-${{ inputs.version || '4.0.0' }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read  # to fetch code (actions/checkout)

env:
  ADA_URL_VERSION: ${{ inputs.version || '4.0.0' }}
  MANYLINUX_RISCV64_IMAGE: quay.io/pypa/manylinux_2_39_riscv64

jobs:
  setup:
    uses: $/.github/workflows/_setup.yml

  build_wheels:
    needs: [setup]
    name: Build ada-url ${{ inputs.version || '4.0.0' }} ${{ matrix.python }}-manylinux_riscv64
    runs-on: ubuntu-24.04-riscv
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        python: ["cp312", "cp313", "cp314"]

    steps:
      - name: Checkout ada-url ${{ env.ADA_URL_VERSION }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          repository: ada-url/ada-python
          ref: v${{ env.ADA_URL_VERSION }}
          persist-credentials: false

      - name: Build wheels
        uses: pypa/cibuildwheel@1828c10ab37f080699c7b81cea34097c684a7074  # v4.2.0
        with:
          output-dir: wheelhouse/
          only: ${{ matrix.python }}-manylinux_riscv64
        env:
          CIBW_MANYLINUX_RISCV64_IMAGE: ${{ env.MANYLINUX_RISCV64_IMAGE }}
          # cffi is a build requirement and has no riscv64 wheel on public PyPI.
          CIBW_ENVIRONMENT: PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/
          CIBW_TEST_SOURCES: tests
          CIBW_TEST_COMMAND: python -m unittest discover -s tests -v

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ada-url-${{ env.ADA_URL_VERSION }}-${{ matrix.python }}-manylinux_riscv64
          path: ./wheelhouse/*.whl
          if-no-files-found: error

  publish:
    name: Publish ada-url ${{ inputs.version || '4.0.0' }}
    needs: [setup, build_wheels]
    permissions:
      contents: write
      pull-requests: write
    uses: $/.github/workflows/_publish-wheel.yml
    with:
      artifact-pattern: ada-url-${{ inputs.version || '4.0.0' }}-*-manylinux_riscv64
