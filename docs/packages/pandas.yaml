package-name: pandas
source-code: https://github.com/pandas-dev/pandas
license: BSD-3-Clause
versions:
  - version: 2.2.2
    comment: We append the files in the LICENSES directory into the LICENSE file in the wheel, resulting in a dirty tag.  See `issue 58632 <https://github.com/pandas-dev/pandas/issues/58632>`_
    warning: We disable test_rolling_var_numerical_issues.
  - version: 2.2.3
    warning: We disable test_rolling_var_numerical_issues and test_value_counts_nat. These tests are fixed on main but not on the 2.2.3 tag.
  - version: 2.3.1
    warning: We disable test_rolling_var_numerical_issues. This test is fixed on main but not on the 2.3.1 tag.
  - version: 2.3.2
    patched:
    warning: |
      We disable test_rolling_var_numerical_issues. This test is fixed on main
      but not on the 2.3.2 tag. We also patch the build to use Cython 3.1.2
      since 3.2.0 seems to cause issues with pickling data types. See also
      `issue 63078 <https://github.com/pandas-dev/pandas/issues/63078>`_.
  - version: 2.3.3
    patched:
    warning: |
      We disable test_rolling_var_numerical_issues. This test is fixed on main
      but not on the 2.3.3 tag. We also patch the build to use Cython 3.1.2
      since 3.2.0 seems to cause issues with pickling data types. See also
      `issue 63078 <https://github.com/pandas-dev/pandas/issues/63078>`_.
  - version: 3.0.0
    patched:
    warning: |
      We skip certain NaN tests on riscv64 because canonical NaN handling differs from other common architectures.
  - version: 3.0.1
    patched:
    warning: |
      We skip certain NaN tests on riscv64 because canonical NaN handling differs from other common architectures.
  - version: 3.0.2
    patched:
    warning: |
      We skip certain NaN tests on riscv64 because canonical NaN handling differs from other common architectures.
  - version: 3.0.3
    patched:
    warning: |
      We skip certain NaN tests on riscv64 because canonical NaN handling differs from other common architectures.
