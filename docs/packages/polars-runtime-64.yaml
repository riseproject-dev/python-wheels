package-name: polars-runtime-64
source-code: https://github.com/pola-rs/polars
license: MIT
versions:
  - version: 1.39.3
  - version: 1.40.0
  - version: 1.40.1
  - version: 1.41.0
  - version: 1.41.1
  - version: 1.41.2
  - version: 1.42.1
    comment: |
      We add custom CARGO_ and RUSTUP_ settings for optimization to the
      workflow, to avoid exhausting the resources available on the native riscv64
      runners.
  - version: 1.43.2
    comment: |
      We add custom CARGO_ and RUSTUP_ settings for optimization to the
      workflow, to avoid exhausting the resources available on the native riscv64
      runners.
