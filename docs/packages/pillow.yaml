package-name: pillow
source-code: https://github.com/python-pillow/Pillow
license: MIT-CMU License
versions:
  - version: 10.3.0
    patched:
    license: HPND License
  - version: 10.4.0
    patched:
    license: HPND License
  - version: 11.0.0
    patched:
    license: HPND License
  - version: 11.1.0
    patched:
  - version: 11.3.0
    patched:
    comment: test_pyroma is incompatible with the latest version of pyroma and is skipped.
  - version: 12.0.0
    patched:
  - version: 12.1.0
    patched:
  - version: 12.1.1
    patched:
  - version: 12.2.0
    patched:
    comment: |
      - Disable SIMD for libjpeg_turbo which does not provide SIMD riscv support for version < 3.2, and current version embedded in Pillow is JPEGTURBO_VERSION=3.1.4.1

      - Disable arch-specific AOM optimization. See https://aomedia.issues.chromium.org/issues/492439207
  - version: 12.3.0
    patched:
    license: MIT-CMU
    comment: |
      - Rebase libavif lto patch from previous versions
      - Patch scripts to use 'uv pip' instead of 'python3 -m pip'
      - Disable arch-specific AOM optimization. See https://aomedia.issues.chromium.org/issues/492439207
      - Skip grabclipboard_wl_clipboard test via 'pytest' invocation (depends on Wayland and graphical components)
