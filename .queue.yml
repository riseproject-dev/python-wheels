packages:
- pkg: cython
  version: 3.2.9
  home: https://cython.org
  repo: https://github.com/cython/cython
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/1695
  notes: 'HIGH PRIORITY (explicit user request). build-cython.yml (from PR #330, builds 3.3.0) already parameterizes by version via workflow_dispatch, so no workflow changes needed. patches/cython/3.3.0/0001-*.patch does not apply cleanly to 3.2.9 (confirmed via git apply --check): the 3.2.9 pyproject.toml [[tool.cibuildwheel.overrides]] blocks lack the `inherit.environment = "append"` line the 3.3.0 patch context requires. Added patches/cython/3.2.9/0001-*.patch instead (same riscv64 CFLAGS override, matching 3.2.9 override style). CI green on PR #1695: cp312/cp313/cp314 riscv64 wheels built, publish job dry-ran cleanly (not on main, no upload). Awaiting maintainer review/merge; not yet published.'
- pkg: crick
  version: 0.0.8
  home: https://github.com/dask/crick
  repo: https://github.com/dask/crick
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1192
  notes: 'PR #1192 green (4 wheels built cp310-cp313, publish dry-run OK), awaiting review. numpy is a build-time (and undeclared-runtime) dependency, pulled from our registry via PIP_ONLY_BINARY. Backports two small upstream fixes (sequence-point self-assignment in stats_kurt, strict-aliasing violation in space_saving''s asint64) that landed on dask/crick main after the 0.0.8 tag but before any release. cp314/cp314t dropped: neither is upstream-supported and both fail for reasons unrelated to riscv64 (cp314 segfault in test collection, cp314t sys.getrefcount vs free-threaded immortal objects).'
- pkg: sglang
  version: 0.5.18
  home: https://github.com/sgl-project/sglang
  repo: https://github.com/sgl-project/sglang
  status: parked
  pr: https://github.com/riseproject-dev/python-wheels/pull/357
  notes: 'Draft. Maintainer hold, unconditional: hard-requires cuda-python>=13.0 and cuda-tile, neither of which exists for riscv64 anywhere, so the wheel is buildable but not installable. Do not push to the branch - pull_request: paths restarts a ~1.5h-per-interpreter build on the shared runners.'
- pkg: grpcio-tools
  version: 1.83.1
  home: https://grpc.io
  repo: https://github.com/grpc/grpc/tree/master/tools/distrib/python/grpcio_tools
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/843
  notes: 'Merged (#843). Issue #266 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: scikit-learn
  version: 1.9.0
  home: https://scikit-learn.org
  repo: https://github.com/scikit-learn/scikit-learn
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: watchdog
  version: 6.0.0
  home: https://github.com/gorakhargosh/watchdog
  repo: https://github.com/gorakhargosh/watchdog/
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 27, feasibility-and-triage.md): setup.py sets ext_modules=[] unless sys.platform == ''darwin'' (only _watchdog_fsevents.c on macOS); on Linux (all arches, riscv64 included) watchdog drives inotify via pure ctypes. Confirmed from the real 6.0.0 sdist (setup.py) and PyPI JSON: the 7 Linux wheels are all py3-none-manylinux2014_{x86_64,aarch64,armv7l,i686,ppc64,ppc64le,s390x} (Root-Is-Purelib, hand-set --plat-name in upstream''s release loop, zero .so). Upstream deliberately ships no py3-none-any wheel so riscv64 pip install already falls back to the sdist, which builds in seconds with no compiler. Publishing a manylinux_riscv64 wheel would carry zero arch-specific content -- a packaging convenience, not a port. No worktree/branch/PR created (one was opened for research then removed once this was confirmed).'
- pkg: playwright
  version: 1.62.0
  home: https://github.com/Microsoft/playwright-python
  repo: https://github.com/Microsoft/playwright-python
  status: parked
  pr: null
  notes: 'Not feasible (vendored-binary, gotcha 35/183). No sdist on PyPI; all 8 wheels are py3-none-<platform> (~40-48MB) because setup.py extracts a per-platform bundle of a prebuilt Node.js binary + the playwright-core npm package into playwright/driver/ -- nothing is compiled. Verified against the real v1.62.0 sources: NODE_VERSION pins 24.18.1, which nodejs.org''s official SHASUMS256.txt has zero riscv64 builds for (unofficial-builds.nodejs.org does publish one, but that only gets the driver running). The deeper blocker: playwright-core''s hostPlatform.ts degrades any non-x64/arm64 Linux arch to hostPlatform="<unknown>" (no crash, just silent failure), so `playwright install` and any local browser launch fail on riscv64 regardless of the Node binary -- Microsoft ships no riscv64 Chromium/Firefox/WebKit at all, official or unofficial. The wheel would be installable and importable but non-functional for its entire purpose on every interpreter, with no patch available (unlike av, whose vendored ffmpeg libs do build for riscv64). Worktree/branch not pushed; no PR opened.'
- pkg: numba
  version: 0.67.0
  home: https://numba.pydata.org
  repo: https://github.com/numba/numba
  status: parked
  pr: null
  notes: Depends on llvmlite, which pins to conda-distributed LLVM builds; conda riscv64 support is being handled elsewhere. Parked until that lands. No worktree/branch/PR created.
- pkg: llvmlite
  version: 0.49.0
  home: http://llvmlite.readthedocs.io
  repo: https://github.com/numba/llvmlite
  status: parked
  pr: null
  notes: Depends on conda-distributed LLVM builds; conda riscv64 support is being handled elsewhere. Parked until that lands. No worktree/branch/PR created.
- pkg: xgboost
  version: 3.4.1
  home: https://xgboost.readthedocs.io/en/stable/
  repo: https://github.com/dmlc/xgboost
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/408
  notes: 'Merged (#408) and published: main run succeeded, pypi.riseproject.dev/simple/xgboost/ returns 200. Issue #841 linked, project status set.'
- pkg: torchvision
  version: 0.29.0
  home: https://github.com/pytorch/vision
  repo: https://github.com/pytorch/vision
  status: parked
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: av
  version: 18.1.0
  home: https://pyav.basswood.io
  repo: https://github.com/PyAV-Org/PyAV
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/461
  notes: 'Merged (#461), squash commit 2cc3066e82. This PR predated the current workflow architecture (no docs/packages/av.yaml, no push trigger, publish job called the now-deleted actions/publish-wheels GitLab action) and was modernized before merge: setup job on $/.github/workflows/_setup.yml with package: av, matrix.version from needs.setup.outputs.versions, publish on $/.github/workflows/_publish-wheel.yml. ci/av/collect-vendor-sources.py was inlined into the vendor_sources job as a heredoc, since ci/ is not a permitted port-commit path per check_port_pr_scope.sh. Licence corrected to BSD-3-Clause (PyAV''s own, verified against upstream LICENSE.txt); the wheel additionally bundles 33 prebuilt libraries from pyav-ffmpeg - libx264/libx265 are GPL-2.0-or-later even though every libav* reports LGPLv3 (upstream patches configure to move them out of the GPL list) - vendor_sources publishes the 17 pinned source tarballs plus the pyav-ffmpeg tree, licence texts land in dist-info/licenses. PR CI (build + publish dry run) was fully green before merge. Issue #2136 opened and linked. Push-triggered build+publish on main in progress (run 35534726800); move to published once it completes and https://pypi.riseproject.dev/simple/av/ returns 200.'
- pkg: claude-agent-sdk
  version: 0.2.152
  home: https://github.com/anthropics/claude-agent-sdk-python
  repo: https://github.com/anthropics/claude-agent-sdk-python
  status: parked
  pr: null
  notes: 'Not feasible: vendored-binary whose primary function is unreachable on riscv64, not merely degraded (gotcha 35/157/183, feasibility-and-triage.md). The package itself is pure Python (pyproject.toml: hatchling, only-include src/claude_agent_sdk, no build hook); its 5 published Linux/macOS/Windows bdist wheels are tagged py3-none-<platform> only because scripts/build_wheel.py retags the wheel after scripts/download_cli.py fetches Anthropic''s closed-source ''claude'' CLI binary (via bash install.sh) into src/claude_agent_sdk/_bundled/ -- 342.56 MB of a 343 MB wheel per gotcha 157. That binary has zero riscv64 build anywhere upstream, confirmed independently on all three of gotcha 157''s sources: install.sh''s `case "$(uname -m)"` hard-errors ''Unsupported architecture'' for anything but x86_64/aarch64; downloads.claude.ai''s release manifest lists only {darwin,linux,win32}-{x64,arm64}(+musl); and npm''s @anthropic-ai/claude-code optionalDependencies fan out to the same set, no riscv64 entry. Building from the sdist without that step (what `pip install claude-agent-sdk` already falls back to on riscv64 today, since upstream ships no py3-none-any wheel) does produce a working *import* in seconds with no compiler -- confirmed locally, plain hatchling -> claude_agent_sdk-0.2.152-py3-none-any.whl -- but every call fails: subprocess_cli.py._find_cli() checks the bundled path, then shutil.which(''claude'') and a handful of install locations, then raises CLINotFoundError, which on riscv64 is always true since no ''claude'' binary can exist there at all. Per gotcha 157/183, installable-but-non-functional is `vendored-binary`, not `not-feasible`/`not needed`, and the verdict is to park it: the primary reason anyone installs this SDK (driving the claude CLI) has no riscv64 story at any layer, official or unofficial, with no patch available. No worktree/branch/PR created.'
- pkg: tree-sitter-c-sharp
  version: 0.23.5
  home: https://github.com/tree-sitter/tree-sitter-c-sharp
  repo: https://github.com/tree-sitter/tree-sitter-c-sharp
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/844
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opencv-contrib-python
  version: 5.0.0.93
  home: https://github.com/opencv/opencv-python
  repo: https://github.com/opencv/opencv-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/845
  notes: 'Merged. Mirrors merged build-opencv-python.yml (PR #390); adds opencv_contrib submodule checkout + ENABLE_CONTRIB=1. Post-merge done: issue #862 linked, project status set, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: awscrt
  version: 0.36.2
  home: https://awslabs.github.io/aws-crt-python
  repo: https://github.com/awslabs/aws-crt-python
  status: published
  pr: null
  notes: Merged (#523); confirmed live on pypi.riseproject.dev (200).
- pkg: tree-sitter-java
  version: 0.23.5
  home: https://github.com/tree-sitter/tree-sitter-java
  repo: https://github.com/tree-sitter/tree-sitter-java
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/846
  notes: 'Merged and published: main run 33765432508 succeeded. Issue #852 linked, project status set.'
- pkg: prek
  version: 0.5.2
  home: https://prek.j178.dev/
  repo: https://github.com/j178/prek
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/848
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. maturin bindings = "bin" project (gotcha 117, like py-spy): single native build via PyO3/maturin-action, riscv64gc-unknown-linux-gnu, manylinux 2_39, no cibuildwheel/interpreter matrix on the build job. Worktree at .claude/worktrees/prek, branch prek. PR #848 merged (auto-merged after CI went fully green: build + all 4 interpreter test jobs 3.12/3.13/3.14/3.14t + publish dry-run, no patches needed). Post-merge steps done: issue #856 created and linked, added to Projects/Python Wheels with status Available in RISE PyPI, build-prek.yml dispatched on main (run in progress) to actually publish. Learnings folded into gotchas 187/188 (rust-maturin-and-pyo3.md, local-validation-and-rehearsal.md). Confirmed live on the registry (200).'
- pkg: tree-sitter-ruby
  version: 0.23.1
  home: https://github.com/tree-sitter/tree-sitter-ruby
  repo: https://github.com/tree-sitter/tree-sitter-ruby
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/850
  notes: 'Merged. Same shape as merged tree-sitter-java/cpp/rust. Publish dispatched on main just now; issue #854 linked, project status set. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: tree-sitter-php
  version: 0.24.1
  home: https://github.com/tree-sitter/tree-sitter-php
  repo: https://github.com/tree-sitter/tree-sitter-php
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/855
  notes: 'Merged. Nonstandard upstream layout: two grammars (php, php_only) compiled into one tree_sitter_php._binding extension. Same shape as merged tree-sitter-java/c-sharp/ruby. Post-merge done: issue #857 linked, project status set, build-tree-sitter-php.yml dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: tensorflow
  version: 2.21.0
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: parked
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. The original park reason was never recorded here; re-derived 2026-09-20 while triaging the tensorflow-cpu sibling, whose entry carries the full evidence (and gotcha 426). Summary, stated as a re-derivation rather than as the original reasoning: this is the enormous Bazel-driven C++/XLA/MLIR build TensorFlow always is, and it is disproportionate rather than hard-blocked. The wheel is per-interpreter (cp{v}-cp{v} from HERMETIC_PYTHON_VERSION), so cp310-cp313 is 4 full Bazel builds with no abi3/py3-none collapse; PR #2104 (mediapipe) compiles only the framework/TFLite subset of this very TF 2.21.0 source, with no XLA/MLIR/LLVM, and still spent 5h23m in its build step plus 2h24m-3h21m of queue wait per job on the shared riscv64 pool, against a repo record of ~10h (libclang) and a repo-wide job cap of 1440min (build-torch.yml). Upstream maps riscv64 nowhere in its own build: zero riscv occurrences in .bazelrc, release_cpu_linux pulls in -mavx, WORKSPACE registers only linux_x86_64/linux_aarch64 CC toolchains, and the platform_tag select in tensorflow/tools/pip_package/BUILD has no riscv64 entry and no //conditions:default, so the wheel target fails at analysis time. There is also no sdist and no upstream riscv64 CI to narrow (goal 2). Note for anyone reopening this: riscv64 has no CUDA, so a riscv64 tensorflow wheel would be the CPU-only build - which is exactly what upstream already ships under this plain name on aarch64 (268MB, matching x86_64 tensorflow_cpu at 261MB rather than x86_64 tensorflow at 545MB). That makes `tensorflow` the correct distribution name for a riscv64 port, and tensorflow-cpu the wrong one; if this is ever unparked, port this entry and leave tensorflow-cpu parked. Not the jaxlib/PR #526 blocker: at the XLA revision this tag vendors, xla/backends/cpu/codegen/BUILD does wire if_llvm_riscv_available to llvm:RISCVCodeGen, and the bazel 7.7.0 bootstrap is de-risked by gotchas 47/133.'
- pkg: pi-heif
  version: 1.4.0
  home: https://github.com/bigcat88/pillow_heif
  repo: https://github.com/bigcat88/pillow_heif
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/847
  notes: 'Merged. Needed patches/pi-heif/1.4.0/0001-...-enable-group.patch (cibuildwheel 4.2.0 rejects a cpython-freethreading enable token upstream removed in v1.5.0); folded as gotchas 184/185. Publish dispatched on main just now; issue #853 linked, project status set. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: jaxlib
  version: 0.11.1
  home: https://github.com/jax-ml/jax
  repo: https://github.com/jax-ml/jax
  status: parked
  pr: https://github.com/riseproject-dev/python-wheels/pull/526
  notes: 'Parked at maintainer request. XLA genrule fails: embed_bitcode links every LLVM backend except RISC-V. xla/codegen/intrinsic/cpp/BUILD uses if_llvm_aarch32/hexagon/powerpc/system_z/x86_available but never if_llvm_riscv_available, and embed_bitcode.cc has no RISC-V #if arm. Needs two hunks upstream; bazel bootstrap and clang selection already work. Full diagnosis in the PR comment.'
- pkg: imageio-ffmpeg
  version: 0.6.0
  home: https://github.com/imageio/imageio-ffmpeg
  repo: https://github.com/imageio/imageio-ffmpeg
  status: parked
  pr: null
  notes: 'Not feasible (vendored-binary, gotcha 35/157/183). The package itself is pure Python (setup.py: zero ext_modules); its 6 Linux/macOS/Windows bdist wheels are all py3-none-<platform> only because tasks.py''s `build` task copies a per-platform prebuilt `ffmpeg` executable into imageio_ffmpeg/binaries/ before packing (FNAME_PER_PLATFORM in _definitions.py), then hand-retags the wheel Root-Is-Purelib/Tag -- nothing is compiled in this repo''s sense. The binaries come from the imageio/imageio-binaries GitHub repo (tasks.py get_ffmpeg_binary: `github.com/imageio/imageio-binaries/raw/master/ffmpeg/<fname>`), which for Linux are themselves johnvansickle.com static builds per a code comment in _definitions.py. Verified both have zero riscv64 offering: `gh api repos/imageio/imageio-binaries/contents/ffmpeg` lists only linux-{x86_64,aarch64} (+ legacy v4.2.2 linux32/64/aarch64) plus macOS/Windows; johnvansickle.com/ffmpeg/ ships only amd64/i686/arm64/armhf/armel, no riscv64, official or unofficial. This differs from av (PR #461, in-review): av''s own upstream fetch step (scripts/fetch-vendor.py + scripts/ffmpeg-latest.json) pulls prebuilt libraries from PyAV-Org/pyav-ffmpeg, which *does* publish a riscv64 release (ffmpeg-manylinux-riscv64.tar.gz, confirmed by download) -- but inspecting that tarball shows it contains only include/ headers and lib/ shared libraries for linking against, no bin/ffmpeg CLI executable at all, so it does not supply what imageio-ffmpeg needs either. Getting imageio-ffmpeg a real riscv64 binary would mean this repo compiling a full statically-linked ffmpeg CLI (with x264/x265/etc.) from raw source ourselves -- something upstream''s own CI never does on any platform (it only ever copies a prebuilt exe) -- which is out of proportion for a port and would not ''closely mirror upstream''s own CI, narrowed to riscv64'' (goal 2), unlike av''s case where the existing upstream vendor step already had a riscv64 target. No worktree/branch/PR created (a worktree was opened for research then removed once this was confirmed).'
- pkg: shap
  version: 0.52.0
  home: https://shap.readthedocs.io/en/latest/index.html
  repo: http://github.com/shap/shap
  status: parked
  pr: null
  notes: 'Not feasible today: blocked-on-dependency, same family as the numba/llvmlite entries above (gotcha 40/42, feasibility-and-triage.md). shap 0.52.0''s pyproject.toml hard-requires numba unconditionally on every non-(darwin+x86_64) platform (''numba; sys_platform != "darwin" or platform_machine != "x86_64"'' -- true for linux/riscv64), and it is not an optional accelerator: shap/utils/_masked_model.py and shap/explainers/_partition.py both do module-level ''from numba import njit'', and shap/__init__.py unconditionally imports PartitionExplainer at package-import time, so ''import shap'' itself hard-fails without numba installed -- confirmed against the real v0.52.0 source tree, not inferred. numba itself is un-portable today because it pins llvmlite, whose ffi/CMakeLists.txt requires LLVM major 22 exactly and whose build script sources that patched LLVM from the numba/label/llvm_wheel conda channel, which publishes 0 packages for linux-riscv64 (confirmed via https://conda.anaconda.org/numba/label/llvm_wheel/linux-riscv64/repodata.json). Numba/llvmlite must land first; shap cannot be sequenced ahead of them. New finding folded back into the skill as gotcha 187 (feasibility-and-triage.md): conda-forge''s own llvmdev channel (a different channel than the one llvmlite''s build script actually uses) does publish linux-riscv64 llvmdev at major 22 (llvmdev-22.1.8), unlike the numba/label/llvm_wheel channel gotcha 42 checked -- doesn''t unblock llvmlite by itself (the build script is hardcoded to the numba channel and its patches), but is a concrete lead for whoever picks up the llvmlite port. shap''s own build (scikit-build-core + nanobind, small _cext C++ extension) was not otherwise a blocker -- this is purely the numba/llvmlite dependency wall. No worktree/branch/PR created (one was opened for research at .claude/worktrees/shap on branch shap, then removed once this was confirmed).'
- pkg: lancedb
  version: 0.37.1
  home: https://github.com/lancedb/lancedb
  repo: https://github.com/lancedb/lancedb
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/859
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Worktree at .claude/worktrees/lancedb, branch lancedb. Rust/maturin (python/ subdir), builds one cp310-abi3 wheel on cp312 (~750 resolved crates for riscv64gc-unknown-linux-gnu per cargo metadata). Thin LTO + CARGO_BUILD_JOBS=2 override the shipped fat-LTO release profile (same reasoning as build-deltalake.yml); protoc from Rocky 10 CRB (same as build-statsig-python-core.yml). Tests skip 6 files with unconditional import of polars/pylance (no riscv64 wheel, both large Rust projects in their own right). First CI run failed after ~2h: lance-core''s SIMD_SUPPORT static and lance-linalg''s f32x8/f32x16/i32x8/f64x4/f64x8 types compile only for x86_64/aarch64/loongarch64 upstream (no riscv64 fallback, unlike u8x16/u8x32 which already have one). Added patches/lancedb/0.37.1/000{1,2}-*.patch: 0002 gives lance-linalg a portable scalar fallback (verified with cargo check --target riscv64gc-unknown-linux-gnu against an isolated copy of the simd module, plus aarch64/x86_64 regression checks), 0001 redirects lance-core/lance-linalg to that patched sibling checkout via [patch.crates-io] with `exclude = ["lance"]` to keep its own [workspace] independent (verified end-to-end with cargo metadata --filter-platform riscv64gc-unknown-linux-gnu against the exact nested checkout layout CI produces). Pushed in a second commit; that run failed fast (workflow bug, not the port): `git -C lance apply ../python-wheels/...` -- bash expands the glob relative to its own cwd before git''s -C takes effect, so the literal unexpanded path never matched. Fixed with `cd lance &&` in a third commit. That run (33791589510) built the wheel fine (~9.5h) but failed at test time: 13 failures out of 726 passed/39 skipped/45 deselected -- 12 tests in test_db.py/test_hybrid_query.py/test_permutation.py/test_query.py import lance(pylance)/polars lazily inside the test body (not module scope, so the existing --ignore=<file> list didn''t catch them) and 1 (test_package_metadata.py::test_pyo3_abi_matches_minimum_supported_python) reads python/Cargo.toml relative to its own path, which CIBW_TEST_SOURCES never stages. Fixed in commit 4ee59c0a2 with 13 individual --deselect=<nodeid> entries copied verbatim from the FAILED lines (same CIBW_TEST_COMMAND invocation/rootdir, so no gotcha-14-style relative-path mismatch); pushed, re-running CI now. That run (33843581031) failed again ~10h later with the exact same 13 failures -- the deselects never took effect. Root cause: cibuildwheel runs CIBW_TEST_COMMAND from test_cwd/, one level above test_cwd/python/pyproject.toml; pytest''s rootdir/inifile search only walks upward from cwd, never into subdirectories, so no config was ever found and every --deselect silently failed to match (the FAILED nodeids print identically either way) -- confirmed by reproducing the exact cwd/config layout locally. Fixed in commit badb1f67e with `cd python &&` before the pytest invocation (paths in the command drop one `python/` level to match the new cwd); checked lancedb''s python/pyproject.toml `--strict-markers`/--ignore-glob addopts are safe now that config actually loads (the suite only uses slow/asyncio/s3_test, all three registered; the ignore-glob targets the source package, not tests). Pushed, re-running CI now (run 33916807494, queued). Merged (#859). Issue #963 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pyyaml-ft
  version: 8.0.0
  home: https://github.com/Quansight-Labs/pyyaml-ft/
  repo: https://github.com/Quansight-Labs/pyyaml-ft
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/861
  notes: 'Merged. Free-threading fork of PyYAML; matrix cp313 (GIL) + cp314t (free-threaded), no cp313t interpreter on manylinux_2_39_riscv64. Post-merge done: issue #864 linked, project status set, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: nodejs-wheel-binaries
  version: 24.19.0
  home: https://github.com/njzjz/nodejs-wheel
  repo: https://github.com/njzjz/nodejs-wheel
  status: publishing
  pr: 860
  notes: 'Not vendored-binary: builds Node.js from source via CMake ExternalProject (configure && make), same as upstream''s own x86_64/aarch64 wheels. Node 24.19.0 configure.py/V8 BUILD.gn support riscv64 natively; unofficial-builds.nodejs.org confirms a working riscv64 build exists. Full compile (V8+ICU) on riscv64 runner, timeout-minutes: 1440. First CI run failed 30min in: gcc rejected -m64 while compiling OpenSSL, because Node''s deps/openssl/openssl_asm.gypi has no riscv64 branch and its catch-all fallback wrongly includes the linux-x86_64 asm config. Fixed with patches/nodejs-wheel-binaries/24.19.0/0001-riscv64-pass-openssl-no-asm-to-Node-s-configure.patch (adds --openssl-no-asm to configure on riscv64 only), validated locally under linux/riscv64 QEMU (configure command line + absence of -m64 in generated openssl.target.mk) before pushing. Re-running in CI. Worktree at .claude/worktrees/nodejs-wheel-binaries, branch nodejs-wheel-binaries. PR https://github.com/riseproject-dev/python-wheels/pull/860. Build itself then succeeded (V8+ICU compile completed after ~23h), but the publish dry-run job failed: "No wheels found in dist". Root cause: the upload-artifact name (nodejs-wheel-binaries-24.19.0-manylinux_riscv64) had no segment between version and manylinux_riscv64, but the publish job''s artifact-pattern requires one (nodejs-wheel-binaries-24.19.0-*-manylinux_riscv64) - the glob matched 0 artifacts. The built wheel is tagged py2.py3-none (universal, not per-interpreter), so renamed the artifact to nodejs-wheel-binaries-24.19.0-py2.py3-none-manylinux_riscv64 to match. Re-running jobs failed on the same commit SHA would have reused the old (buggy) workflow definition per GitHub''s documented behavior, so this required a fresh full push (new commit touching the workflow file re-triggers pull_request: paths, meaning another full V8+ICU recompile from scratch) rather than a job re-run. Pushed fix commit e6220aeba, new CI run in progress as of 2026-09-04. That re-run then hit a different failure: the build job''s own 24h timeout-minutes killed it mid-compile (started 2026-09-04T20:31:00Z, killed 2026-09-05T20:31:21Z while still compiling V8 objects, e.g. js-collator-tq-csa.o) - this build''s runtime is close to the 24h edge (the first successful run took ~23h16m). Bumped timeout-minutes to 2880 (48h) in commit 8e14cbd56 and re-pushed; new CI run (33990804990) in progress as of 2026-09-05.'
- pkg: aiokafka
  version: 0.14.0
  home: http://aiokafka.readthedocs.org
  repo: https://github.com/aio-libs/aiokafka
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/858
  notes: 'Merged. Compiles the Cython aiokafka/record/_crecords extension. Needed CIBW_TEST_ENVIRONMENT PYTHON_GIL=1 for cp314t (gotcha 127: single-phase-init Cython extensions re-enable the GIL under free-threading, fatal under aiokafka''s filterwarnings=["error"]). Post-merge done: issue #863 linked, project status set, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: libclang
  version: 18.1.1
  home: https://github.com/sighingnow/libclang
  repo: https://github.com/sighingnow/libclang
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/866
  notes: '4 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev. Compiles a full LLVM/Clang 18.1.1 from source (this package''s whole purpose is bundling a prebuilt libclang.so) - the largest build in this repo so far. Worktree at .claude/worktrees/libclang, branch libclang. Not conda-blocked like numba/llvmlite (gotcha 40/42/187): upstream''s own recipe builds vanilla LLVM/Clang via CMake against the manylinux image''s own gcc, no conda-distributed LLVM involved. Not the jaxlib/XLA case either (PR #526, parked): that''s a bazel genrule missing riscv64 in embed_bitcode, unrelated to a plain CMake libclang build. Single docker run job (manylinux image has no Node, so a native `container:` job can''t run checkout/artifact actions - same constraint noted for build-cryptography.yml/build-orjson.yml), no interpreter matrix (wheel is py2.py3-none, one build serves everyone, like build-xgboost.yml''s py3-none). New finding: LLVM''s CheckAtomic.cmake fails ''Host compiler must support std::atomic!'' when configuring with upstream''s own `-static-libgcc -static-libstdc++` CXX flags, because manylinux_2_39_riscv64 ships libstdc++.so but not libstdc++.a - fixed with `dnf -y --enablerepo=crb install libstdc++-static` before cmake configure (libstdc++-static is CRB-only, same repo build-chromadb.yml already enables for protobuf). gpl_sources job added (packages: gcc) since -static-libgcc/-static-libstdc++ embeds the image''s gcc runtime into libclang.so, same shape as build-xgboost.yml''s libgomp job. Smoke test parses a small C snippet via clang.cindex after installing the built wheel (upstream''s own CI only greps GLIBC symbol versions, no functional test). First full run (~10h) compiled LLVM/Clang cleanly but then failed at the wheel-packaging step: upstream''s setup_ext.py does `import distutils`, removed from the stdlib in Python 3.12, and the workflow only `pip install`ed wheel (not setuptools) so the compat shim was never installed - `ModuleNotFoundError: No module named ''distutils''`. Fixed by installing setuptools alongside wheel; validated the root cause directly against the real manylinux_2_39_riscv64 image (no LLVM rebuild needed) and filed as gotcha 211. Pushed fix commit d33f298be. Second full run (~9h51m, 2026-09-04 06:15-16:07Z) again compiled LLVM/Clang cleanly, then failed one line further into the same wheel-packaging step: unpinned `pip install -U wheel` now resolves wheel 0.48.0, which dropped `wheel.bdist_wheel.get_platform` in 0.44.0 in favour of setuptools'' own bundled bdist_wheel - upstream''s setup_ext.py imports that symbol by name, so it dies with `ImportError: cannot import name ''get_platform'' from ''wheel.bdist_wheel''`. Reproduced and fixed against the real setup_ext.py + a stub native/libclang.so in quay.io/pypa/manylinux_2_39_riscv64 (no LLVM rebuild needed): wheel 0.48.0 fails identically, pinning wheel==0.43.0 (last release exporting get_platform) produces a real wheel. Filed as gotcha 225 (committed directly to main, 86cb8f409, following the gotcha-211 precedent). Pushed fix commit bb2851bf1. Third full run (2026-09-04T16:20Z - 2026-09-05T02:35Z, ~10h15m) compiled LLVM/Clang cleanly, packaged the wheel cleanly under wheel==0.43.0, and the smoke test + publish dry-run both passed. All checks green: setup, check_commit_messages, check_patches, build, gpl_sources, publish dry-run. PR #866 open, not yet merged - awaiting maintainer merge. Confirmed via `gh pr view 866` immediately before this update. No further active monitoring; will run post-merge steps (pw-postmerge.py, registry confirmation) once merged. Issue #1156 created and linked to PR #866 (was previously missing). Publish (re-)dispatched on main.'
- pkg: ctranslate2
  version: 4.8.1
  home: https://opennmt.net
  repo: https://github.com/OpenNMT/CTranslate2
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/873
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Worktree at .claude/worktrees/ctranslate2, branch ctranslate2. CPU backend picked via CMake: WITH_MKL=OFF, WITH_RUY=ON, OPENMP_RUNTIME=COMP, BUILD_CLI=OFF - matches upstream''s own aarch64 CIBW_BEFORE_ALL_LINUX branch (prepare_build_environment_linux.sh) minus OpenBLAS, since Ruy alone already covers both float32 and int8 GEMM per src/cpu/backend.cc (OpenBLAS only adds float32, redundant here). Ruy''s own third_party/cpuinfo submodule (pinned pre-riscv64-support commit 082deffc) degrades to a harmless ''unsupported platform'' warning on riscv64 rather than a fatal error - confirmed by reading pytorch/cpuinfo''s CMakeLists.txt at that exact pinned commit. Needed `-DCMAKE_POLICY_VERSION_MINIMUM=3.5` because cpuinfo''s own vendored deps/clog declares a `cmake_minimum_required` below CMake 4''s hard floor of 3.5 (fatal ''Compatibility with CMake < 3.5 has been removed''), and only CMake >=4.1 ships a riscv64 wheel on PyPI - confirmed both by triggering the exact error locally and by checking PyPI''s file list per version. Only third_party/spdlog + third_party/ruy (+ its own cpuinfo submodule) are initialized - third_party/{cxxopts,cpu_features,googletest,thrust,cutlass} are unused for a BUILD_CLI=OFF/BUILD_TESTS=OFF/WITH_CUDA=OFF CPU build and thrust+cutlass alone are gigabytes of NVIDIA sources not worth fetching. gpl_sources job added (packages: gcc): auditwheel repair vendors the image''s libgomp.so.1 into ctranslate2.libs/ because OPENMP_RUNTIME=COMP links GNU OpenMP - confirmed by actually running auditwheel repair locally. Also fixed a pre-existing upstream packaging gap: `cp LICENSE python/` before cibuildwheel, since python/setup.py''s find_packages() only globs inside python/ so upstream''s own PyPI wheel (verified via pip download) ships no LICENSE at all (same shape as gotcha 86/build-thrift.yml). Locally validated the FULL build+test loop (not just cmake configure) end-to-end on a native aarch64 manylinux_2_39 container via podman as a fast proxy for riscv64 (no QEMU riscv64 binfmt available on this Mac host): C++ library builds and installs in under a minute, python wheel builds and imports, `get_supported_compute_types(''cpu'')` returns {float32, int8, int8_float32} confirming Ruy is live, and the real python/tests suite (mirroring upstream''s test-python-wheels job, minus legacy fairseq/opennmt-py/opennmt-tf converter tests) passes 160/168 with 7 skipped and 1 deselected (a wav2vec2 transcription test that diverges by one character under Ruy''s int8 GEMM vs upstream''s OpenBLAS+Ruy aarch64 build - same numeric-divergence class as gotcha 170, not a functional bug). First real riscv64 CI run failed fast (~1min into the C++ compile): cpuinfo''s pinned commit (082deffc, pre-riscv64-support) never defines -D_GNU_SOURCE=1 for riscv64 because CMakeLists.txt''s CPUINFO_SUPPORTED_PLATFORM gate only sets that flag for processors its regex recognizes (riscv wasn''t added until pytorch/cpuinfo commit 4e5be9e, 2023-11-14), yet src/api.c''s cpuinfo_get_current_processor() calls syscall(__NR_getcpu,...) unconditionally on any __linux__ build - without _GNU_SOURCE glibc''s unistd.h hides the syscall() prototype and GCC 14 hard-errors on the implicit declaration. This is exactly the kind of arch-specific runtime failure gotcha 9''s aarch64-proxy configure/build check cannot catch (aarch64 IS in cpuinfo''s recognized-processor regex, so CPUINFO_SUPPORTED_PLATFORM is true there and _GNU_SOURCE gets defined normally - confirmed by trying to repro on aarch64 via -DCMAKE_SYSTEM_PROCESSOR=riscv64, which cmake silently ignores for a non-cross-compile config, so this had to be caught for real on riscv64 CI). Fixed with patches/ctranslate2/4.8.1/0001-cpuinfo-fix-missing-syscall-declaration-on-riscv64.patch (Upstream-Status: Backport, citing pytorch/cpuinfo commit 4e5be9e1c6c5895bc5105a92d587bc9df8d2522b which already fixes this for current cpuinfo releases) adding `#define _GNU_SOURCE 1` directly to third_party/ruy/third_party/cpuinfo/src/api.c, applied via a second `actions/checkout` of this repo (path: python-wheels) + `git apply` step after submodule init. Verified the fix in isolation with a direct `gcc -std=c99` compile of the pristine vs. patched file (matching cpuinfo''s own C_EXTENSIONS NO / C99 target settings) inside the manylinux_2_39_aarch64 image: pristine reproduces the identical `error: implicit declaration of function ''syscall''`, patched compiles cleanly. Pushed as a new commit on the ctranslate2 branch. Second real riscv64 CI run got past cpuinfo and failed later, in ctranslate2''s own src/cpu/primitives.cc: ''CPU_ISA_DISPATCH was not declared in this scope'' at every call site. Root cause in src/cpu/cpu_isa.h: the CPU_ISA_DISPATCH macro is defined inside `#ifdef CT2_WITH_CPU_DISPATCH` (set whenever ENABLE_CPU_DISPATCH=ON, the default) with only `#if defined(CT2_X86_BUILD) #elif defined(CT2_ARM64_BUILD) #endif` branches and no third #else - so on any architecture that is neither x86 nor arm64, the macro is simply never defined while every call site still expands it unconditionally. This is a genuine upstream gap for any third architecture, not riscv64-specific handling gone wrong. Fixed by adding `-D ENABLE_CPU_DISPATCH=OFF` to the CMake configure line, which routes through cpu_isa.h''s OUTER macro-cascade instead (gated on raw compiler macros __AVX512F__/__AVX2__/__AVX__/__ARM_NEON, all false on riscv64) and lands on its own unconditional final `#else` defining CPU_ISA_DISPATCH against CpuIsa::GENERIC - exactly what riscv64 needs since ENABLE_CPU_DISPATCH''s whole purpose (compiling multiple ISA variants and picking one at runtime) is moot when there is only ever one ISA to target. Verified via a from-scratch riscv64 cross-compile sandbox built for this (not just the aarch64 proxy, which can''t reproduce this bug since aarch64 matches CT2_ARM64_BUILD and never hits the missing-macro path): installed gcc-riscv64-linux-gnu from EPEL inside the manylinux_2_39_aarch64 image, extracted a real sysroot (libc/libstdc++/libgomp/headers) from quay.io/pypa/manylinux_2_39_riscv64 via `podman create` + `podman export` (no riscv64 QEMU binfmt needed since nothing is executed, only the image filesystem is unpacked), and confirmed CMake configure now correctly reaches the ''not supported in cpuinfo'' riscv64 branch (proving the cross-config itself is authentic) with ENABLE_CPU_DISPATCH=OFF set. Also did a full manual grep audit of every CT2_X86_BUILD/CT2_ARM64_BUILD/__AVX__/__ARM_NEON/#error site in src/+include/ (7 files total) to rule out further third-architecture gaps in the same pass (gotcha 71/139''s ''grep the vendored tree once'' principle) - all other sites either have a working generic fallback (src/cpu/kernels.cc''s own TARGET_ISA cascade, cpu_isa.cc''s get_cpu_isa()) or are self-contained within their own arch guard with no call from outside it (cpu_info.cc/.h, the one arch-specific log line in utils.cc). Pushed as a third commit. Third real riscv64 CI run got all the way through the build, wheel packaging, and nearly the whole test suite (2h53m total) - genuine progress, not another config failure. Only 3 tests failed, all parametrizations of test_transformers_whisper_align: each sums small per-token float differences into a result ~0.004 outside the test''s `pytest.approx(..., abs=1e-3)` tolerance (e.g. 20.490975484251976 vs 20.48674027621746±0.001) - same numeric-divergence class as the already-deselected wav2vec2 test (gotcha 170), now confirmed for real on riscv64 hardware rather than inferred. Deselected with `--deselect .../test_transformers.py::TestWhisper::test_transformers_whisper_align` (no bracket = all 3 parametrizations) and pushed as a fourth commit. PR #873 open, re-running CI now - not yet confirmed green. Both the cpuinfo _GNU_SOURCE patch and the ENABLE_CPU_DISPATCH=OFF fix are confirmed working on real riscv64 hardware (build+wheel succeed); only test selection remains open. Fourth real riscv64 CI run (33859295079) revealed that fourth-commit deselect was actually a no-op: pytest --deselect requires an exact nodeid match, and the unbracketed function name never matches parametrized nodeids (test_names0/1/2-cpu), so all 3 whisper_align cases ran again and failed identically (~0.004 outside tolerance) - confirmed straight from the CI log (158 passed, 3 failed, 8783s test time). The job''s overall conclusion showed as `cancelled` rather than `failure` because this coincided almost to the second with the 180-minute timeout-minutes wall (job ran 3h0m47s) - a race between the natural pytest failure and the timeout, not a concurrency-cancel or manual cancel (checked via `gh run list`/`gh run view --log`, no superseding push in that window). Fixed with a fifth commit (863644151): three separate --deselect flags with the correct bracketed nodeids (test_names0/1/2-cpu), plus timeout-minutes 180->240 for headroom since 180 left ~0 margin even before this bug (other repo workflows use up to 240-720 for large builds). Pushed; CI re-queued (run picking up now), not yet confirmed green. Fifth real riscv64 CI run (33878204120, headSha 863644151) still failed the same way: the wav2vec2 test that''s supposed to be deselected ran and PASSED, and all 3 whisper_align parametrizations ran and FAILED again (168 items collected/run total, no ''N deselected'' anywhere in the summary) - so bracketing the nodeid wasn''t the real bug. Root cause (matches this skill''s own gotcha 14 and the nested note under gotcha 111/jpype): pytest reports collected nodeids relative to its rootdir (/project/python here, from pyproject.toml), not relative to the invocation cwd or the absolute test path given on the command line - all 4 --deselect args used the {project}/python/tests/... absolute form, which never equals the rootdir-relative tests/... nodeid pytest actually matches against, so every deselect silently matched zero tests regardless of cwd. Verified the mechanism directly with a local pytest 8.4.2 repro (rootdir + pyproject.toml, invoked from an unrelated cwd): the absolute-path form left both parametrized cases failing with 0 deselected, the rootdir-relative tests/... form correctly reported ''2 deselected''. Fixed with a sixth commit (227b8e20f): switched all 4 --deselect nodeids from {project}/python/tests/... to the rootdir-relative tests/... form. Pushed; CI re-queued again, not yet confirmed green. Sixth real riscv64 CI run (33916551244, headSha 227b8e20f) confirmed the gotcha-14 deselect fix works for real ("157 passed, 7 skipped, 4 deselected"), but the job was CANCELLED at exactly the 240-minute timeout-minutes wall (started 20:30:35Z, cancelled 00:30:57Z), mid-way through testing the third of three tested interpreters. cibuildwheel runs the full CIBW_TEST_COMMAND once per tested interpreter (cp312/cp313/cp314; cp314t is build-only) - each a from-scratch HF Hub model download+conversion+inference pass. Measured from the log: cp312''s pass took 1h50m19s with a cold HF cache, cp313''s took 52m49s with a warm one; cp314''s was cancelled 19m37s in at 46% progress. Extrapolating from cp313''s warm-cache pace, total end-to-end need is ~4.5h - only ~30min over the 240m budget it just missed. Fixed with a seventh commit (d4ed03aa5): timeout-minutes 240->480 for real margin against a slow HF Hub or a cold cache, still far below this repo''s own budget for comparably large multi-target builds (torch''s build_wheels job: 1440). Pushed; CI re-queued again, not yet confirmed green. timeout-minutes raised 240->480 fixed the cutoff; gotcha-14 deselect fix confirmed working. All CI checks green (build, gpl_sources, setup, commit-message, patch checks, publish dry-run) as of 2026-09-06. Ready for review. Issue #1155 created and linked to PR #873 (was previously missing). Publish (re-)dispatched on main.'
- pkg: chromadb
  version: 1.5.9
  home: https://github.com/chroma-core/chroma
  repo: https://github.com/chroma-core/chroma
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/554
  notes: 'PR #554 merged (2026-08-27). Publish run 33804881915 (re-dispatched 2026-09-03) finally completed successfully after ~4 days wall-clock (started 2026-09-03T20:53, finished 2026-09-07T00:31) -- NOT hung after all, just an extremely slow/queued build+test cycle on the shared riscv64 runner pool. GitHub release chromadb-v1.5.9-20260907003142 created with wheel + gpl-sources assets. pypi.riseproject.dev/simple/chromadb/ still 404 as of 2026-09-07T00:32 -- registry indexing may lag the release by some time; re-check before assuming broken.'
- pkg: obstore
  version: 0.11.1
  home: https://developmentseed.org/obstore
  repo: https://github.com/developmentseed/obstore
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/865
  notes: 'Merged (#865). Issue #882 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: deptry
  version: 0.25.1
  home: https://deptry.com
  repo: https://github.com/osprey-oss/deptry
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/869
  notes: 'Merged (#869). Issue #883 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: pylsqpack
  version: 0.3.24
  home: https://github.com/aiortc/pylsqpack
  repo: https://github.com/aiortc/pylsqpack
  status: published
  pr: 868
  notes: '6 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aioquic
  version: 1.3.0
  home: https://github.com/aiortc/aioquic
  repo: https://github.com/aiortc/aioquic
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/881
  notes: 'Merged (#881). Issue #887 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: pikepdf
  version: 10.12.0
  home: https://pikepdf.readthedocs.io/
  repo: https://github.com/pikepdf/pikepdf
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/877
  notes: 'Merged (#877). Issue #888 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: cchardet
  version: 2.1.7
  home: https://github.com/PyYoshi/cChardet
  repo: https://github.com/PyYoshi/cChardet
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/894
  notes: 'Merged (#894). Issue #902 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: mitmproxy-rs
  version: 0.12.11
  home: https://mitmproxy.github.io/mitmproxy_rs/
  repo: https://github.com/mitmproxy/mitmproxy_rs
  status: published
  pr: 962
  notes: 'maturin/pyo3, abi3-py312 unconditional in Cargo.toml -- one cp312-abi3-manylinux_riscv64 wheel, tested on cp312/cp313/cp314. Real pyproject.toml lives in mitmproxy-rs/ subdir of the Cargo workspace (package-dir). lib.rs unconditionally imports mitmproxy_linux at module init on Linux (no riscv64 wheel exists) -- built with the crates own --features docs to skip that eager import; local.start_local_redirector() still re-imports it and raises the same error at call time. mitmproxy_linux is also an unconditional Requires-Dist on Linux -- CIBW_TEST_ENVIRONMENT PIP_NO_DEPS=1 (gotcha 122) keeps the wheel install from trying to resolve it. mitmproxy-rs/ ships no LICENSE of its own (only the repo root does) -- staged a copy before build so maturin bundles it. PR #962 green: build+publish-dry-run both succeeded, 9/9 smoke tests passed on cp312 (reused via find_compatible_wheel on cp313/cp314). Merged (#962). Issue #964 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tree-sitter-yaml
  version: 0.7.2
  home: https://github.com/tree-sitter-grammars/tree-sitter-yaml
  repo: https://github.com/tree-sitter-grammars/tree-sitter-yaml
  status: published
  pr: 872
  notes: 'PR #872 merged; post-merge steps done (issue #875, project board, docs PR #871 shared/pending). build-tree-sitter-yaml.yml dispatched on main, release tree-sitter-yaml-v0.7.2 created and verified immutable. pypi.riseproject.dev/simple/tree-sitter-yaml/ still 404 as of merge time -- same propagation lag seen on other same-day publishes (pylsqpack, opencv-contrib-python), expected to clear on its own.'
- pkg: tree-sitter-language-pack
  version: 1.15.8
  home: https://github.com/xberg-io/tree-sitter-language-pack
  repo: https://github.com/xberg-io/tree-sitter-language-pack
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: zstd
  version: 1.5.7.2
  home: https://github.com/sergey-dryabzhinsky/python-zstd
  repo: https://github.com/sergey-dryabzhinsky/python-zstd
  status: published
  pr: 880
  notes: 'Merged (#880). Issue #886 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: dependency-injector
  version: 4.49.1
  home: https://github.com/ets-labs/python-dependency-injector
  repo: https://github.com/ets-labs/python-dependency-injector
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/879
  notes: 'Merged (#879). Issue #899 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: tree-sitter-typescript
  version: 0.23.2
  home: https://github.com/tree-sitter/tree-sitter-typescript
  repo: https://github.com/tree-sitter/tree-sitter-typescript
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/876
  notes: 'Merged (#876). Issue #884 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: tree-sitter-embedded-template
  version: 0.25.0
  home: https://github.com/tree-sitter/tree-sitter-embedded-template
  repo: https://github.com/tree-sitter/tree-sitter-embedded-template
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/890
  notes: 'Merged (#890). Issue #900 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: pyrefly
  version: 1.2.0
  home: https://pyrefly.org
  repo: https://github.com/facebook/pyrefly
  status: published
  pr: 891
  notes: 'Merged (#891). Issue #905 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: pipdeptree
  version: 4.2.2
  home: https://github.com/tox-dev/pipdeptree
  repo: https://github.com/tox-dev/pipdeptree
  status: published
  pr: 893
  notes: 'Merged (#893). Issue #906 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: rasterio
  version: 1.5.1
  home: https://rasterio.readthedocs.io/
  repo: https://github.com/rasterio/rasterio
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/912
  notes: 'Merged (#912). Issue #943 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: bluetooth-data-tools
  version: 1.29.21
  home: https://bluetooth-data-tools.readthedocs.io
  repo: https://github.com/bdraco/bluetooth-data-tools
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: rustworkx
  version: 0.18.1
  home: https://www.rustworkx.org/
  repo: https://github.com/Qiskit/rustworkx
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/878
  notes: 'Merged (#878). Issue #885 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: firecrawl-anydoc
  version: 0.2.4
  home: https://github.com/firecrawl/anydoc#readme
  repo: https://github.com/firecrawl/anydoc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/984
  notes: 'PyO3/maturin abi3-py310 extension; PR #984 opened, CI running. Merged (#984). Issue #1002 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: jq
  version: 1.12.0
  home: https://github.com/mwilliamson/jq.py
  repo: https://github.com/mwilliamson/jq.py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/896
  notes: 'Merged (#896). Issue #904 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: ckzg
  version: 2.1.8
  home: https://github.com/ethereum/c-kzg-4844
  repo: https://github.com/ethereum/c-kzg-4844
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/908
  notes: 'Merged (#908). Issue #915 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: lru-dict
  version: 1.4.1
  home: https://github.com/amitdev/lru-dict
  repo: https://github.com/amitdev/lru-dict
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/892
  notes: 'Merged (#892). Issue #901 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: ortools
  version: 9.15.6755
  home: https://developers.google.com/optimization/
  repo: https://github.com/google/or-tools
  status: parked
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Parked as disproportionate in scope, not Bazel-blocked (unlike jaxlib/PR #526): confirmed the real PyPI release wheel is built by tools/release/build_delivery_linux.sh''s build_python(), plain `cmake -DBUILD_PYTHON=ON` against upstream''s own CMakeLists.txt, matching .github/workflows/amd64_linux_cmake_python.yml -- no Bazel involved despite the repo also carrying a bazel/ tree. But BUILD_DEPS=ON (forced whenever BUILD_PYTHON is on) FetchContent-builds abseil, protobuf, Eigen3, re2, Boost, SoPlex, SCIP, and the 4-library COIN-OR suite (CoinUtils/Osi/Clp/Cgl/Cbc), plus HiGHS and PDLP and BOP, on top of compiling ortools'' own large C++ core (CP-SAT, routing) and its SWIG Python bindings -- over a dozen independently-large C++ projects in one build. abseil/protobuf are proven on riscv64 in this repo (grpcio-tools, chromadb, ctranslate2) and HiGHS is already published standalone as highspy, but SCIP/SoPlex/Boost-as-a-solver-dep/COIN-OR have zero riscv64 precedent here. Weighed against this repo''s own largest build to date (libclang: ~10h from-scratch LLVM/Clang compile, explicitly logged as the largest so far), a build FetchContenting that much additional unproven C++ on top of a comparably-sized core -- each new dependency a plausible source of its own multi-hour fix/rebuild cycle -- reads as a multi-day effort even with zero hard architectural blocker, matching this task''s own parking criterion. Folded into the skill as gotcha 214 (feasibility-and-triage.md). No worktree/branch opened -- the scope call was fully resolvable read-only against the real upstream release script and CMakeLists.txt plus this repo''s own git history, no checkout needed.'
- pkg: osqp
  version: 1.1.3
  home: https://osqp.org/
  repo: https://github.com/osqp/osqp-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/895
  notes: 'Merged (#895). Issue #903 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: toons
  version: 0.7.0
  home: https://github.com/alesanfra/toons
  repo: https://github.com/alesanfra/toons
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/918
  notes: 'Merged (#918). Issue #922 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: autobahn
  version: 26.7.1
  home: https://autobahn.readthedocs.io/
  repo: https://github.com/crossbario/autobahn-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/909
  notes: 'Merged (#909). Issue #921 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: python-calamine
  version: 0.8.2
  home: https://github.com/dimastbk/python-calamine
  repo: https://github.com/dimastbk/python-calamine
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/910
  notes: 'Merged (#910). Issue #916 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: openlineage-sql
  version: 1.53.0
  home: https://github.com/OpenLineage/OpenLineage
  repo: https://github.com/OpenLineage/OpenLineage
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/967
  notes: 'Monorepo subdir integration/sql (tag is the bare version, e.g. 1.53.0). Pure-Rust sqlparser-backed pyo3 crate, maturin, abi3-py310 (pyo3 feature unconditional) so matrix collapses to cp310-abi3 (built once) + cp314t. No native deps; psm/stacker in the dep tree has riscv64 asm. Repo LICENSE/NOTICE.txt not globbed by maturin (pyproject lives in iface-py/) so the workflow copies them in. Adds a cargo-test job on the impl crate mirroring upstream''s run_tests step. PR #967 green (all checks pass, publish dry-ran clean). Merged (#967). Issue #972 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: python-crfsuite
  version: 0.9.12
  home: https://github.com/scrapinghub/python-crfsuite
  repo: https://github.com/scrapinghub/python-crfsuite
  status: published
  pr: 907
  notes: 'Merged (#907). Issue #913 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: timezonefinder
  version: 8.3.0
  home: https://timezonefinder.michelfe.it/gui
  repo: https://github.com/jannikmi/timezonefinder
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/911
  notes: 'Merged (#911). Issue #917 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: cvxpy
  version: 1.9.2
  home: https://github.com/cvxpy/cvxpy
  repo: https://github.com/cvxpy/cvxpy
  status: blocked-on-dependency
  pr: null
  notes: 'Own build is trivial and fully validated locally (docker, aarch64): cvxcore is a plain cibuildwheel C++ extension, Eigen vendored, SWIG wrapper pre-generated in the sdist (no swig/cmake needed), builds clean with just numpy+scipy+setuptools+wheel from `pip wheel . --no-deps --no-build-isolation`, imports and solves a real LP. osqp/clarabel/scs are already on the registry (build-time not actually required, only runtime). Blocked on sparsediffpy>=0.3.0,<0.4.0 (hard runtime dep, imported by cvxpy''s NLP diff-engine but still a hard install_requires): no riscv64 wheel anywhere, and its 0.3.0 sdist cannot build with any installable scikit-build-core (pins >=0.10 but uses the cmake.minimum-version key, removed in scikit-build-core >=0.8 - filed SparseDifferentiation/SparseDiffPy#22, one-line pyproject.toml fix, already patched and open as PR #920 in this repo). Once #920 merges and publishes, resume with: matrix cp312/cp313/cp314 (drop cp314t - scs and highspy, also hard deps, have no riscv64 wheel for cp314t on PyPI or our registry either); plain build-from-checkout cibuildwheel, `CIBW_ENVIRONMENT: PIP_EXTRA_INDEX_URL=https:// pypi.riseproject.dev/simple/` (covers numpy/scipy/osqp/clarabel/scs/highspy/ sparsediffpy), qdldl needs no registry wheel - builds cleanly from sdist via cmake+pybind11, both already resolvable from PyPI for riscv64, no patch needed. Test with upstream''s own `pytest cvxpy/tests` (mirrors their test_script.sh). No GPL sources job needed for cvxpy''s own wheel (no OpenMP/BLAS linkage). Worktree/branch removed per blocked-on-dependency convention; full validated build-cvxpy.yml content is captured in this note and PR #920/queue''s sparsediffpy entry - no re-research needed, just re-author and push once unblocked.'
- pkg: jsonschema-rs
  version: 0.52.0
  home: https://github.com/Stranger6667/jsonschema/tree/master/crates/jsonschema-py
  repo: https://github.com/Stranger6667/jsonschema
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/919
  notes: 'Merged. Issue #929 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pinecone
  version: 9.1.0
  home: https://www.pinecone.io
  repo: https://github.com/pinecone-io/python-sdk
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/939
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Confirmed real Rust/PyO3 core (Cargo.toml/Cargo.lock in sdist, abi3 wheel tag), not a pure-py false positive. maturin build + protoc-via-CRB before-script rehearsed end-to-end on manylinux_2_39_aarch64 (native build, install, pytest tests/unit); deselected 2 pre-existing order/timing-dependent test flakes unrelated to riscv64. First riscv64 CI run failed: test_retry_after_fuzz.py::test_compute_retry_after_delay_respects_bounds hit pytest-timeout''s ini `timeout=5` (upstream''s x86-sized value) building hypothesis''s charmap interval cache -- fixed with `-o timeout=60` (gotcha 176), amended + force-pushed. PR #939 open, CI re-running. Merged (#939). Issue #957 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: daft
  version: 0.7.24
  home: https://www.daft.ai
  repo: https://github.com/Eventual-Inc/Daft
  status: published
  pr: 930
  notes: 'PR #930 merged, issue #1154 linked. Publish run 34052244469 (dispatched 2026-09-06) completed successfully (build + publish jobs both green) after fixing the UV_-vs-PIP_-prefixed env var bug (gotcha 244). GitHub release created. pypi.riseproject.dev/simple/daft/ still 404 as of 2026-09-07T01:20 -- registry indexing may lag the release (same pattern seen with chromadb); re-check before assuming broken.'
- pkg: cassandra-driver
  version: 3.30.1
  home: https://github.com/apache/cassandra-python-driver/
  repo: https://github.com/apache/cassandra-python-driver/
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2116
  notes: 'PR #2116 merged by the maintainer (luhenry) on 2026-09-19; awaiting the riscv64 build+publish workflow to run on main and produce the actual wheel before this can move to published. All five ubuntu-latest checks were green pre-merge (check_commit_messages, check_patches, check_packages_yaml, setup, docs preview); the four riscv64 build jobs were still queued ~1h40m after the push, so the shared runner pool is saturated and CI has produced no wheel yet -- needs watching in a later session. Build-from-checkout cibuildwheel port mirroring datastax/python-driver-wheels'' build_wheels_linux.yml: the driver repo itself ships no GitHub Actions at the 3.30.1 tag (its Jenkinsfile is the CCM/DSE integration matrix, installing the driver --editable), the wheel recipe lives in that separate packaging repo and is named only in README-dev.rst''s release checklist -- new gotcha 391. Twenty extensions: cmurmur3, io/libevwrapper and eighteen Cython modules. libev is NOT EPEL-only on Rocky 10 (contra the gotcha 51/337 reflex): libev 4.33 is in baseos and libev-devel in crb, both enabled in the manylinux image, so upstream''s `yum install -y libev libev-devel` ships unchanged; the header is /usr/include/ev.h, covered by pyproject''s bare /usr/include entry -- new gotcha 390. One patch (Upstream-Status: To upstream) adds LICENSE.libev and extends the explicit PEP 639 license-files list, since auditwheel bundles libev.so.4 into cassandra_driver.libs/ while the wheel otherwise carries only the driver''s Apache-2.0 LICENSE; libev is dual BSD-2-Clause/GPL-2.0-or-later, so the BSD option applies and no gpl_sources job is needed. Tests are upstream''s tox [testenv] command (pytest tests/unit) with VERIFY_CYTHON=True as the Jenkinsfile sets, plus the extension verification from the wheels repo''s config.sh run_tests. test-sources stages tests plus just cassandra/ioutils.pyx and cassandra/cython_marshal.pyx, which tests/unit/cython/types_testhelper.pyx includes by a checkout-root-relative path: a cassandra/ directory with no __init__.py is only a namespace portion, so the wheel still wins the import -- new gotcha 389. Test deps are tox''s [base] list minus kerberos (krb5 headers, integration-only), plus setuptools (pyximport needs a distutils on 3.12+, gotcha 211), minus gevent on cp314t only (no free-threaded build; its reactor tests skip themselves). PIP_PREFER_BINARY=1 is required (gotcha 354): PyPI''s gevent 26.9.0 is newer than the registry''s 26.8.0, so without it pip compiled gevent from source in the test venv. Everything else (greenlet, cryptography, numpy, lz4, twisted, eventlet) resolves as a riscv64 wheel on all four interpreters -- checked with the new ci_scripts/riscv64_resolve.py. test_cloud.py::CloudTests::test_use_default_tempdir deselected: it asserts PermissionError from a mode-0544 dir, which root in the container never gets (fails identically on x86_64). Validated locally, not just trusted: a full QEMU riscv64 cibuildwheel run in quay.io/pypa/manylinux_2_39_riscv64 with upstream''s exact CFLAGS built all twenty extensions (~2h emulated; ~60min of that on cluster.c alone at -O3), auditwheel repaired to manylinux_2_34_riscv64.manylinux_2_39_riscv64 vendoring libev.so.4, and the workflow''s exact test command then ran in that image against the repaired wheel: 619 passed, 24 skipped, 1 deselected, 1 failed. The single failure is tests/unit/io/test_libevreactor.py::LibevTimerTest::test_multi_timer_validation, a hardcoded 0.15s assertAlmostEqual on timer latency that measured 0.188s under QEMU (gotcha 38/316 shape); it passed in the parallel run on the same code, so it is left in rather than deselected -- if the real riscv64 runners flake on it, that is the one node to triage. The 24 skips are upstream''s usual 8 plus the 16 tests/unit/io/test_asyncorereactor.py cases, which skip on 3.12+ since asyncore was removed from the stdlib. Also dry-ran upstream''s released x86_64 wheel through the identical staging and test command (636 passed, 8 skipped, 1 deselected) and built the patched tree on x86_64 with Cython 3.3.0, confirming all eighteen Cython modules cythonize with today''s floating Cython (no pin needed) and that LICENSE + LICENSE.libev both land in dist-info/licenses. cp314t is the strongest evidence here: a complete, unattended cibuildwheel run for cp314t-manylinux_riscv64 went green end to end under QEMU in 2 hours -- before_all, all twenty extensions at -O3 against /opt/python/cp314-cp314t, auditwheel repair to cp314-cp314t-manylinux_2_34_riscv64.manylinux_2_39_riscv64, test phase 620 passed / 24 skipped / 1 deselected / 0 failed, one wheel produced. (That run also compiled gevent from source in its test venv, since the local driver script passed gevent on cp314t where the workflow drops it -- a superset of the shipped test deps, so the green result stands. The same wheel was re-tested with gevent absent, as the workflow does: identical 620/24/1/0.) The timer test that flaked on the cp312 leg passed on every cp314t run, which is further evidence that failure is emulation latency and not a riscv64 bug. The Cython extensions use single-phase init, so 3.14t re-enables the GIL on import and warns; nothing here runs under -W error, so no PYTHON_GIL=1 is needed (gotcha 127) and the suite passes as-is. cp313 and cp314 were not built locally -- they sit between the two legs that were, so CI is the authority there. Port also filed new gotcha 410 (a -O0 rehearsal override can ship a wheel whose extension keeps an undefined symbol from a non-static C99 inline, and pass the suite anyway via the package''s pure-Python fallback -- which is why the workflow imports the extensions instead of only listing them in the zip).'
- pkg: prophet
  version: 1.4.0
  home: https://facebook.github.io/prophet/
  repo: https://github.com/facebook/prophet
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Blocked-on-dependency, same shape as gotcha 341 (feasibility-and-triage.md): prophet''s python/setup.py (STAN_BACKEND=CMDSTANPY is the only backend supported since prophet>=1.1) calls cmdstanpy.install_cmdstan(version="2.37.0") unconditionally at wheel-build time, and CmdStan''s own make/stanc rule downloads bin/stanc -- the Stan-to-C++ transpiler -- as a prebuilt OCaml binary from stan-dev/stanc3''s GitHub Releases, keyed by `uname -m` via a fixed ARCH_TAG table (aarch64/ppc64le/s390x/armv7l only). riscv64 matches none of those branches, so it would fetch the plain x86_64 linux-stanc asset, which cannot execute on riscv64. Verified no riscv64 asset exists on either the pinned v2.37.0 stanc3 release or the current nightly (checked 2026-09-10): assets are linux-stanc/linux-arm64-stanc/linux-armel-stanc/linux-armhf-stanc/linux-ppc64el-stanc/linux-s390x-stanc/mac-*/windows-stanc. Stan Math''s own vendored TBB 2020.3 is NOT the blocker -- its tbb_machine.h takes the portable machine/gcc_generic.h fallback for any GCC>=7 build (unconditional per tbb_config.h), the same path already exercised by the existing x86_64/aarch64 wheels, so the C++ side compiles fine cross-arch. The actual blocker is narrowly stanc3 itself: an OCaml/dune project with zero riscv64 release or CI history of its own -- OCaml''s compiler has supported riscv64 codegen since 4.12 and opam lists a host-arch-riscv64 package, but bootstrapping that toolchain and then building stanc3 (a separate upstream project outside this repo''s rules to patch or file against) would mean standing up a whole second, previously-untested build pipeline as a prerequisite -- out of scope for a patches/<pkg>/<version>/ fix. Possible future path: pre-generate the transpiled prophet_model.hpp via stanc on x86_64 once and vendor it as a patch so riscv64 only ever compiles portable C++, bypassing stanc entirely on-target -- not attempted here since it diverges heavily from upstream''s own build (setup.py) for a package with no explicit priority ask. No worktree/branch opened -- diagnosed read-only via gh api against facebook/prophet (v1.4.0), stan-dev/cmdstan (v2.37.0), stan-dev/stanc3 (v2.37.0 + nightly), and stan-dev/math (58ad15b... via stan-dev/stan''s pin), no checkout required.'
- pkg: catboost
  version: 1.2.10
  home: https://catboost.ai
  repo: https://github.com/catboost/catboost
  status: parked
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Not Arcadia/ya-make-blocked in the way the task brief hypothesized: catboost''s real Linux CI (.github/workflows/build_per_platform.yaml) drives plain ./ci/build_all.py -> cmake/ninja, and python-package/setup.py calls that same CMake build for its _catboost/_hnsw extensions - no ya/Arcadia tooling invoked anywhere in the actual build path, no pure-Python fallback. The real blocker is one layer down and bigger than ortools'' scope (gotcha 214): every CMakeLists.txt with platform-specific content is a generated stub carrying a "generated by the YaTool build system (yandex/yatool) from ya.make files" header, dispatching purely on CMAKE_SYSTEM_NAME/CMAKE_SYSTEM_PROCESSOR with no else() branch - confirmed at both build/CMakeLists.txt and a leaf like catboost/libs/model/CMakeLists.txt. Only Linux x86_64/aarch64/ppc64le get a variant; riscv64 has none, so the endif() falls through silently at every one of those injection points (SIMD boosting kernels, source lists, compile flags) - not a compile error but a systematically incomplete build. `gh api search/code -f ''q=filename:CMakeLists.linux-x86_64.txt repo:catboost/catboost''` counts 350 separate such injection points across the tree. Fixing this needs either running Yandex''s YaTool generator for a riscv64 platform preset it has never targeted (unproven, needs Arcadia-side platform/toolchain definitions this repo has no access to), or hand-writing riscv64 equivalents of all 350 generated files by reverse-engineering the x86_64/aarch64 ones - larger and more mechanically blocked than ortools'' dozen FetchContent''d dependencies. Filed as gotcha 230 (feasibility-and-triage.md). No worktree/branch opened - resolved read-only via gh api against the v1.2.10 tag (workflows, ci/build_all.py, build/build_native.py, python-package/setup.py, and the search/code stub count), no checkout needed.'
- pkg: uamqp
  version: 1.6.11
  home: https://github.com/Azure/azure-uamqp-python
  repo: https://github.com/Azure/azure-uamqp-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/927
  notes: 'Merged (#927). Issue #941 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: fiona
  version: 1.10.1
  home: https://fiona.readthedocs.io/
  repo: https://github.com/Toblerity/Fiona
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/949
  notes: 'Cython/GDAL wheel, same shape as rasterio/pyogrio: shared GDAL/GEOS/PROJ/SpatiaLite image (pyogrio-style, BUILD_APPS=OFF). Matrix cp312/cp313 only: fiona pins cython~=3.0.2, whose last 3.0.x release publishes no cp314 wheel, matching fiona 1.10.1s own PyPI ceiling (cp313). No patch needed - setuptools default license-file glob already covers the generated LICENSE_GDAL/GEOS/PROJ/SPATIALITE files. Hit three real CI issues, all fixed: (1) GDALs cmake build ships no gdal-config and fiona setup.py has no gdalinfo fallback (unlike rasterio) - image now writes a minimal synthetic gdal-config itself; (2)+(3) fiona pytest.ini sets filterwarnings=error and fiona 1.10.1 (Oct 2024) predates Click 8.2.0 (May 2025), so its own CI never saw it: 8.2.0 added a UserWarning for a command declaring the same option twice (fio cats own cat.py declares --where via @click.option twice, verbatim - a latent upstream bug, not touched) and 8.5.0 added a DeprecationWarning on get_text_stream (used internally by CliRunner); pyparsing 3.3.0 also newly warns on the vendored fiona._vendor.snuggs calc/reduce engines pre-PEP8 parseString call. Settled on one CIBW_TEST_REQUIRES ceiling per dep: click<8.2.0, pyparsing<3.3.0. Rebuilt CI in progress on the third fix. Click pinned to <8.2.0 fixed both the get_text_stream deprecation and the duplicate --where UserWarning issues. All CI checks green (GDAL build image, build cp312/cp313, setup, commit-message, patch checks, publish dry-run) as of 2026-09-06. Ready for review. Merged (#949). Issue #996 linked, publish dispatched on main (project-board sync skipped, see gh token scoping note). Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pyahocorasick
  version: 2.3.1
  home: http://github.com/WojciechMula/pyahocorasick
  repo: http://github.com/WojciechMula/pyahocorasick
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/925
  notes: 'Merged. Issue #928 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: sqlite-vec
  version: 0.1.9
  home: https://alexgarcia.xyz/sqlite-vec
  repo: https://github.com/asg017/sqlite-vec
  status: published
  pr: 951
  notes: 'Loadable SQLite C extension, packaged by upstream''s own sqlite-dist tool (no riscv64 in its platform table); workflow hand-builds the py3-none wheel to match. CI green: build + upstream pytest suite (91 passed, 4 skipped) + wheel install, publish dry-run clean. Merged (#951). Issue #952 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pyiceberg-core
  version: 0.10.1
  home: https://rust.iceberg.apache.org/
  repo: https://github.com/apache/iceberg-rust
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/971
  notes: 'bindings/python subdir of the apache/iceberg-rust Cargo workspace (maturin, pyo3 abi3-py310 unconditional -> single cp310-abi3 wheel, matches PyPI). RUSTUP_TOOLCHAIN=stable needed since the checkout''s rust-toolchain.toml pins a nightly the sdist excludes. Tested (cp312, matching pypi.riseproject.dev''s pyarrow/pyiceberg floor) on test_transform.py; test_datafusion_table_provider.py/test_huggingface_and_cdc.py dropped (unconditional `import datafusion`, no riscv64 wheel anywhere); test_manifest.py dropped (imports pyiceberg.io.pyarrow, which unconditionally imports pyarrow._s3fs, and our riscv64 pyarrow ships ARROW_S3=OFF). PR #971 green on the real riscv64 runner: build succeeded, 11 passed, publish dry-run clean. Merged (#971). Issue #982 linked, publish dispatched on main (project-board sync skipped - fine-grained token lacks Projects permission, see pw-postmerge.py fix). Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: highspy
  version: 1.15.1
  home: https://github.com/ERGO-Code/HiGHS
  repo: https://github.com/ERGO-Code/HiGHS
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: nemo-relay
  version: 0.8.4
  home: https://github.com/NVIDIA/NeMo-Relay
  repo: https://github.com/NVIDIA/NeMo-Relay
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/993
  notes: 'Rust/maturin/pyo3 abi3-py311 (agent runtime bindings). Patched crates/worker-proto/build.rs to fall back to prost-build''s own protoc lookup: protoc-bin-vendored ships no riscv64 binary; Rocky 10 CRB supplies protoc 3.19 instead. build-nemo-relay.yml added, validated locally against the released macOS wheel (529 tests pass). PR #993 open, watching CI.'
- pkg: selectolax
  version: 0.4.11
  home: https://github.com/rushter/selectolax
  repo: https://github.com/rushter/selectolax
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/926
  notes: 'Merged (#926). Issue #935 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: bitsandbytes
  version: 0.50.2
  home: https://github.com/bitsandbytes-foundation/bitsandbytes
  repo: https://github.com/bitsandbytes-foundation/bitsandbytes
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2094
  notes: 'PR #2094 open. setup + all three pr-checks jobs green; the two riscv64 jobs (build_wheels, gpl_sources) are dispatched but still queued on the self-hosted pool after ~3h, so no wheel has been built yet -- needs watching/triage in a later session. Feasible despite the CUDA reputation: CMake COMPUTE_BACKEND defaults to cpu, so libbitsandbytes_cpu.so builds from csrc/cpu_ops.cpp + csrc/pythonInterface.cpp with plain C++17 + OpenMP and no CUDA/ROCm/XPU toolkit; every SIMD block in csrc is guarded by __x86_64__/__aarch64__ with scalar fallbacks, and both CPU sources compile clean through the generic path (verified natively via renamed arch macros -- see new gotcha 382; no riscv64 image or apt mirror was reachable from this host for a real QEMU rehearsal). Upstream wheels are py3-none-<platform> (ctypes-loaded .so, no extension module), so one cp312 cibuildwheel build is retagged py3-none, as upstream own build-wheels job does; matrix collapses to a single job. Runtime dep torch is already on the registry (2.13.0+cpu/2.14.0+cpu), as are scipy/numpy/tokenizers/safetensors/regex/pyyaml; transformers/einops/lion-pytorch are pure-Python. Tests mirror upstream test-runner.yml CPU job (BNB_TEST_DEVICE=cpu, [test] extras, pytest --durations=100). Dry run of that exact suite against a locally built CPU-only wheel on x86: 976 passed, 126 skipped, 0 failed through ~31% of collection before the local time budget ran out. auditwheel vendors libgomp, so a gcc gpl_sources job is wired in. PR also adds gotchas 381/382.'
- pkg: backports-zoneinfo
  version: 0.2.1
  home: https://github.com/pganssle/zoneinfo
  repo: https://github.com/pganssle/zoneinfo
  status: parked
  pr: null
  notes: 'Unnecessary for this repo''s cp312+ target: PyPI 0.2.1 ships wheels only for cp36/cp37/cp38 (verified via PyPI JSON, requires_python >=3.6 with no cp39+ builds ever released) because this is CPython''s own backport of stdlib zoneinfo (added in 3.9). Upstream''s own README states support for 3.9+ is minimal and "if you do not support anything earlier than Python 3.9, you do not need this library" -- consumers are meant to depend on it via python_version<"3.9" markers. No worktree/branch/PR created.'
- pkg: tensorflow-io-gcs-filesystem
  version: 0.37.1
  home: https://github.com/tensorflow/io
  repo: https://github.com/tensorflow/io
  status: blocked-on-dependency
  pr: null
  notes: 'Blocked, not Bazel-tree-size: WORKSPACE pulls abseil/protobuf/grpc/BoringSSL/google-cloud-cpp 1.21.0 as plain http_archive source builds, all with riscv64 precedent elsewhere in this repo (gotcha 132). The real blocker is structural: tools/build/configure.py imports the pip-installed tensorflow package and reads tf.sysconfig.get_compile_flags()/get_link_flags(); WORKSPACE''s tf_configure rule then copies that package''s actual libtensorflow_framework.so into @local_config_tf, and tensorflow_io_gcs_filesystem/core/BUILD links the plugin .so directly against it (TF_SetStatus, TF_Filesystem*Ops etc. are real symbols in that library, not header-only declarations). No riscv64 libtensorflow_framework.so exists anywhere to link against: no tensorflow* wheel on PyPI or pypi.riseproject.dev ships riscv64 (this repo''s own tensorflow entry is parked with zero work started), and Google''s official standalone libtensorflow C-library tarball has no riscv64 build either (storage.googleapis.com/tensorflow/versions/2.16.1/libtensorflow-cpu-linux-riscv64.tar.gz -> 404, confirmed against the working linux-x86_64 URL). Producing one means bootstrapping full TensorFlow core via Bazel for riscv64, a much larger effort than jaxlib -- which shares the same XLA/TSL stack at a fraction of TF-core''s size and is itself still parked on an upstream RISC-V codegen gap, PR #526. No sdist on PyPI either (wheels only, would need to build our own from a checkout). Parking rather than hand-rolling a synthetic stub .so with fabricated symbols: that would diverge completely from upstream''s own build and produce an artifact nothing on riscv64 can actually load or test. Revisit once tensorflow itself has a real riscv64 build.'
- pkg: rdkit
  version: 2026.3.5
  home: https://github.com/kuelumbus/rdkit-pypi
  repo: https://github.com/kuelumbus/rdkit-pypi
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: py-rust-stemmers
  version: 0.1.8
  home: https://github.com/qdrant/py-rust-stemmers
  repo: https://github.com/qdrant/py-rust-stemmers
  status: published
  pr: 931
  notes: 'Merged (#931). Issue #936 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: ezdxf
  version: 1.4.4
  home: https://ezdxf.readthedocs.io
  repo: https://github.com/mozman/ezdxf
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: logbook
  version: 1.10.1
  home: https://logbook.readthedocs.io
  repo: https://github.com/getlogbook/logbook
  status: published
  pr: 932
  notes: 'Merged (#932). Issue #937 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: gensim
  version: 4.4.0
  home: https://radimrehurek.com/gensim/
  repo: https://github.com/RaRe-Technologies/gensim
  status: published
  pr: 945
  notes: 'PR #945 open. Build-from-checkout shape mirroring upstream build-wheels.yml; numpy/scipy/Cython resolved from RISE registry. No OpenMP/libgomp involved (no -fopenmp in setup.py; nmf_pgd.pyx/_matutils.pyx prange compiles serially without it). No patches needed. First CI run failed: test_api.py dataset-loading tests need network access unavailable in this sandbox (all interpreters); test_corpora.py TestWikiCorpus/test_scripts.py TestSegmentWiki fail only on cp314/cp314t with "cannot pickle generator object" (multiprocessing pickling a live-generator attribute, a pre-existing gensim bug newly surfaced by Python 3.14 - upstream itself excludes cp314* via CIBW_SKIP). Fixed by deselecting these 23 known-non-riscv64 test failures (commit fb4bf6473); new CI run queued. Merged (#945). Issue #970 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pycurl
  version: 7.47.0
  home: http://pycurl.io/
  repo: https://github.com/pycurl/pycurl
  status: published
  pr: 940
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#940). Issue #953 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tach
  version: 0.35.0
  home: https://github.com/gauge-sh/tach
  repo: https://github.com/gauge-sh/tach
  status: published
  pr: 934
  notes: 'Merged (#934). Issue #944 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: zizmor
  version: 1.29.0
  home: https://docs.zizmor.sh
  repo: https://github.com/zizmorcore/zizmor
  status: published
  pr: 933
  notes: 'Merged (#933). Issue #942 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: statsig-python-core
  version: 0.22.0
  home: https://statsig.com/
  repo: https://github.com/statsig-io/statsig-server-core/tree/main/statsig-pyo3
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: torch-c-dlpack-ext
  version: 0.1.5
  home: https://github.com/apache/tvm-ffi
  repo: https://github.com/apache/tvm-ffi
  status: parked
  pr: null
  notes: 'Not feasible today (gotcha 40 shape, feasibility-and-triage.md: dependency-blocked, not package-blocked). Lives at addons/torch_c_dlpack_ext/ in apache/tvm-ffi; upstream builds it via .github/workflows/torch_c_dlpack.yml + build_aot_wheels.sh. torch itself is NOT the blocker: pypi.riseproject.dev already serves torch 2.13.0+cpu and 2.14.0+cpu for cp312/cp313/cp314/cp314t riscv64. The real blocker is one level down: the wheel bundles one prebuilt .so per supported torch major.minor (named libtorch_c_dlpack_addon_torch{MM}-{cpu,cuda}.so) and its custom build_backend.py, whenever an .so is not already prebuilt, requires apache-tvm-ffi>=0.1.1 and shells out to `python -m tvm_ffi.utils._build_optional_torch_c_dlpack` to compile one via torch.utils.cpp_extension+ninja. That helper cannot even be invoked without a working apache-tvm-ffi install, because tvm_ffi/__init__.py unconditionally ctypes-loads its own compiled native core (libinfo.load_lib_ctypes) on import. apache-tvm-ffi publishes wheels for cp39-cp314(t) on manylinux aarch64/x86_64 + macOS + Windows only -- no riscv64 wheel on PyPI, and none on pypi.riseproject.dev. Its native core is a ~10k-line CMake/C++ build (src/ffi, vendored 3rdparty/dlpack, optional libbacktrace) that has never targeted riscv64; porting it would be a full separate package port, out of scope here. Separately, upstream also builds CUDA variants of each .so via an installed CUDA toolkit, which would not apply to riscv64 (CPU-only) but is moot given the earlier blocker. No worktree/branch/PR created.'
- pkg: tensorflow-text
  version: 2.21.1
  home: http://github.com/tensorflow/text
  repo: http://github.com/tensorflow/text
  status: blocked-on-dependency
  pr: null
  notes: 'Blocked on tensorflow itself (same family as tensorflow-io-gcs-filesystem, gotcha 343; this repo''s tensorflow entry is parked, and there is no docs/packages/tensorflow.yaml). Latest upstream is now v2.21.1 (PyPI 2.21.1, tag v2.21.1) -- version bumped from the queued 2.21.0; 12 wheels, macOS arm64 + manylinux_2_27 {x86_64,aarch64} for cp310-cp313, no sdist, no riscv64. Build-time blocker is structural, not Bazel-tree size: tensorflow-text is a TensorFlow custom-ops package whose every kernel library links against the *prebuilt* libtensorflow_framework.so.2 taken out of a pip-installed tensorflow wheel. Mechanism differs from tensorflow-io''s tf_configure cp -r, so grep for it separately: WORKSPACE''s py_deps_profile(deps_map={"tensorflow": [..., "libtensorflow_framework"]}) plus third_party/tensorflow/tf_configure.bzl''s pip_parse(annotations={"tensorflow": package_annotation(additive_build_content=...)}) declares cc_library(name="libtensorflow_framework", srcs=["site-packages/tensorflow/libtensorflow_framework.so.2"]), and tensorflow_text/tftext.bzl (tf_cc_library/tflite_cc_library, lines ~163 and ~224) plus tensorflow_text/core/kernels/BUILD add @release_or_nightly//:tensorflow_libtensorflow_framework to deps of every op library. That is a target-architecture ELF the linker must actually consume, not a header tree. It fails even earlier than the link: oss_scripts/pip_package/requirements_lock_3_{10,11,12,13}.txt are hash-pinned (tensorflow==2.21.0 with sha256s of only the macOS/x86_64/aarch64 wheels), so pip_parse cannot resolve a tensorflow wheel for linux_riscv64 at all. Two further riscv64 gaps in the same WORKSPACE: register_toolchains lists only @rules_ml_toolchain//cc:linux_x86_64_linux_x86_64 and :linux_aarch64_linux_aarch64, and oss_scripts/ ships build.Dockerfile.{x86_64,aarch64} only. No riscv64 libtensorflow_framework exists anywhere to supply: no tensorflow/tensorflow-cpu wheel on PyPI or pypi.riseproject.dev ships riscv64, and Google''s standalone C-library tarball has no riscv64 build (storage.googleapis.com/tensorflow/versions/{2.16.1,2.18.0}/libtensorflow-cpu-linux-riscv64.tar.gz -> 404 against working linux-x86_64 200s; the 2.21.0 path is gone entirely). Runtime is blocked too: setup.py install_requires=[''tensorflow>=2.21.0, <2.22''] unconditionally, so a wheel we built could be neither installed nor smoke-tested on riscv64. Unblocking requires bootstrapping full TensorFlow core via Bazel for riscv64 first -- a separate, much larger port than jaxlib (PR #526, parked). Not attempted: no workflow, no docs/packages yaml, no patches, no branch, no PR. Revisit only once tensorflow itself has a real riscv64 build on pypi.riseproject.dev.'
- pkg: netcdf4
  version: 1.7.4
  home: https://unidata.github.io/netcdf4-python/
  repo: https://github.com/Unidata/netcdf4-python
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: blosc2
  version: 4.11.0
  home: https://github.com/Blosc/python-blosc2
  repo: https://github.com/Blosc/python-blosc2
  status: published
  pr: 948
  notes: 'PR #948 green: cp311-abi3 (retested cp312-cp314) + cp314t. No cp315t - numpy has no riscv64 wheel for cp315/cp315t anywhere. Merged (#948). Issue #954 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: dm-tree
  version: 0.1.10
  home: https://github.com/deepmind/tree
  repo: https://github.com/deepmind/tree
  status: published
  pr: 950
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. All CI checks green (build cp312/cp313/cp314/cp314t, gpl_sources, setup, commit-message, patch checks) as of 2026-09-04. Ready for review. Merged (#950). Issue #955 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pairmux
  version: 0.5.2
  home: https://github.com/treeleaves30760/pairmux
  repo: https://github.com/treeleaves30760/pairmux
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/947
  notes: 'Real port: wheel bundles a prebuilt Go binary (goreleaser, CGO_ENABLED=0), no riscv64 target upstream. Cross-compiled with go build; patched build_wheels.py for the missing riscv64 platform tag. Merged (#947). Issue #956 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: shellcheck-py
  version: 0.11.0.1
  home: https://github.com/ryanrhee/shellcheck-py
  repo: https://github.com/ryanrhee/shellcheck-py
  status: published
  pr: 946
  notes: 'Feasible: koalaman/shellcheck v0.11.0 ships a linux.riscv64 binary, and shellcheck-py setup.cfg already has a riscv64 download entry for it. PR #946 builds inside manylinux_2_39_riscv64, forcing --plat-name; no compilation involved. Merged (#946). Issue #958 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: torchcodec
  version: 0.16.0
  home: https://pytorch.org/torchcodec/stable/index.html
  repo: https://github.com/pytorch/torchcodec
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2105
  notes: 'PR #2105 open, second CI run in flight with the fix. First run: all four riscv64 jobs built, repaired and installed a wheel and then failed at the test step with `OSError: Could not load this library: .../torchcodec/libtorchcodec_image.so` - and nothing else, because pytest renders a conftest-time ImportError without the exception chain, so ld.so''s message never reached the log (new gotcha 416). Root cause, found by reproducing the whole job under docker+QEMU on quay.io/pypa/manylinux_2_39_riscv64: torchcodec''s `!TORCHCODEC_ENABLE_AVIF` stub for decode_avif() still carries the three-parameter signature from before num_threads was added, while DecodeAvif.h, the real implementation and image_custom_ops.cpp''s `m.impl("decode_avif", TORCH_BOX(&decode_avif))` all use four - so with TORCHCODEC_BUILD_AVIF=0 (which this port needs; libavif only exists in upstream''s S3 bucket, which has no riscv64 build) the four-parameter overload is defined nowhere. An ELF shared object may carry undefined symbols, so the link, the wheel build and auditwheel all stayed green and the first dlopen - which load_image_library() does at `import torchcodec` - took the whole package down (`undefined symbol: _ZN8facebook10torchcodec11decode_avifERKN5torch6stable6TensorElll`). Fixed by patch 0002-DecodeAvif-give-the-no-libavif-stub-the-declared-sign.patch, Upstream-Status: To upstream - a genuine upstream bug that upstream never hits because their wheels always link the S3 libavif, not riscv64-specific. New gotcha 415 generalises it (and the `ldd -r` sweep that proved this was the *only* dangling symbol across all five shipped libraries, rather than the first of several). Local validation, contrary to the original PR''s ''not built locally'' note: the whole job now reproduces on this host under docker + binfmt QEMU - FFmpeg 6.1.6 built --disable-everything (the image library links no FFmpeg and the core libs only need the libav* API surface, which cuts 36 min to a few), torch 2.14.0+cpu from our registry, cp312. Unpatched: the CI traceback exactly. Patched: `IMPORT_OK 0.16.0 6`, `ldd -r` clean on all five .so files, and the workflow''s own wheel-contents check passes (libjpeg/libpng16/libwebp/libwebpdemux/libsharpyuv grafted, no FFmpeg, no libtorch, three third-party licence texts in .dist-info/licenses/). New gotcha 417 records the two rehearsal traps: scikit-build-core''s CMake probe times out under QEMU unless CI=1 is exported, and CIBW_BEFORE_ALL''s licence staging has to be replayed or patch 0001''s PEP 639 ''LICENSE.*'' pattern matches nothing and metadata generation fails. Decode tests themselves left to CI (the rehearsal FFmpeg has no codecs, and 2620 tests under QEMU is not a local proposition). Second run: all four legs built/imported fine but 242 tests failed identically on each - traced to the container''s LGPL-only FFmpeg (no libx264/libx265/libvpx/libaom/libmp3lame, AV1 decode hwaccel-only), so upstream''s H.264/HEVC/VP9/AV1/MP3 encode paths are unreachable here (upstream''s own CI installs a GPL FFmpeg via conda, which this build correctly does not). Fixed by a surgical CIBW_TEST_COMMAND -k deselect of exactly those unreachable tests, verified offline against the literal cibuildwheel command before pushing (gotchas 429/430). Third run: all four legs green plus the publish dry run. PR ready for review/merge.'
- pkg: newrelic
  version: 13.5.0
  home: https://docs.newrelic.com/docs/apm/agents/python-agent/
  repo: https://github.com/newrelic/newrelic-python-agent
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: vllm
  version: 0.29.0+cpu
  home: https://github.com/vllm-project/vllm
  repo: https://github.com/vllm-project/vllm
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2093
  notes: 'CI GREEN on 9f9505239b (run 35545079670): build, the wheel''s own import test, and the publish dry run all pass. Wheel: vllm-0.29.0+cpu-cp312-abi3-manylinux_2_39_riscv64.whl, requires-python <3.15,>=3.10, so one wheel serves cp312/cp313/cp314 - the interpreters this registry''s torch 2.13.0+cpu covers. PR #2093 left as a draft for review. The original triage (CPU backend riscv64 support is first-class upstream: a riscv64 branch in cmake/cpu_extension.cmake, RVV kernels in csrc/cpu, CpuArchEnum.RISCV, riscv64 markers in requirements/cpu.txt pinning torch==2.13.0) held up, but three build-level defects it could not predict each took a CI round. (1) numa.h: csrc/cpu/utils.cpp includes it behind only #ifndef VLLM_NUMA_DISABLED and cpu_extension.cmake sets ENABLE_NUMA TRUE with only an Apple carve-out, so the CPU extension cannot compile without it; upstream''s docker/Dockerfile.cpu installs libnuma-dev, which on the Rocky 10 image is numactl-devel from appstream (no --enablerepo=crb) - verified in a riscv64 rockylinux:10 container to ship /usr/include/numa.h. Gotcha 460. (2) undefined symbol: sbgemm_ at import. cpu_extension.cmake defines VLLM_HAS_OPENBLAS whenever the torch wheel contains a libopenblas*.so*; ours does ship torch/lib/libopenblas.so.0, but built without BUILD_BFLOAT16 - nm finds sgemm_ and no sbgemm_ and zero bf16 symbols, because OpenBLAS only has bf16 kernels for x86_64 Cooper Lake+, ARM64 Neoverse, POWER10 and z14+. _C deliberately does not link OpenBLAS (it expects libtorch.so to have loaded it RTLD_GLOBAL), so the .so links clean and only dlopen reports it: invisible to the build log and to auditwheel, caught solely by the import test. Patch 0002 probes the library for the symbol via CMAKE_NM and falls back to blas_gemm.h''s existing PyTorch reference path (at::native::cpublas::gemm_no_downcast_stub, confirmed exported by our libtorch_cpu.so); arch-agnostic and keeps current behaviour when the probe cannot run, so Upstream-Status: To upstream. Validated locally against the real riscv64 libopenblas.so.0 plus synthetic positive/negative .so files. Gotcha 461. (3) Wheel tag: the first green run''s publish dry run printed cp312-cp312, not the cp38-abi3 upstream publishes - upstream''s Dockerfile.cpu passes bdist_wheel --py-limited-api=cp38 and cibuildwheel passes nothing, so we were serving one interpreter instead of three even though the extensions are already stable-ABI (USE_SABI 3 C extensions, PyO3 half with features=["pyo3/abi3-py38"]). Fixed with CIBW_CONFIG_SETTINGS --build-option=--py-limited-api=cp312; cp312 rather than upstream''s cp38 because torch 2.13.0 has no riscv64 wheel below cp312, so cp312 is the oldest interpreter this can be built on (gotcha 96). Folded into gotcha 11. Branch cleanup along the way: it had carried a skills/ gotcha mixed into a port commit (check_port_pr_scope violation) plus a merge commit; it is now a clean rebase of six port-scoped commits, and that gotcha went to main as 459. PR body rewritten - it had claimed a cp38-abi3 wheel and still said ''not yet built''. Possible follow-up for a reviewer: per gotcha 11 the abi3 build could also select cp313-*/cp314-* so cibuildwheel test-reuses the one wheel on each interpreter; not done because riscv64_resolve.py cannot evaluate platform_machine == "riscv64" markers from an x86 host, so the ~100-package runtime closure is unverified on cp313/cp314 and a dependency gap would fail the run for a non-vllm reason. Merged (#2093). Push-triggered build+publish on main run 35572918748 succeeded. 404 on pypi.riseproject.dev/simple/vllm/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: livekit
  version: 1.1.16
  home: https://docs.livekit.io
  repo: https://github.com/livekit/python-sdks/
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Blocked-on-dependency, same shape as gotcha 335''s deno_core/rusty_v8 case: the PyPI `livekit` package (the `livekit-rtc` dir of the `livekit/python-sdks` monorepo) is a plain-setuptools py3-none-manylinux wheel, not maturin/pyo3 - confirmed via its pyproject.toml `[tool.cibuildwheel] before-build = "pip install requests && python rust-sdks/download_ffi.py --output livekit/rtc/resources"`, which downloads a prebuilt `livekit-ffi` shared library from `livekit/rust-sdks` GitHub Releases instead of compiling anything locally; `download_ffi.py`''s own `--arch` choices are `[x86_64, arm64, armv7]`, no riscv64 option exists. That ffi library itself links `webrtc-sys`, whose `build.rs` downloads a prebuilt `libwebrtc.a` per `{target_os}-{target_arch}-release` triple from the same repo''s Releases (tag `webrtc-89d790b`, via `webrtc_sys_build::download_webrtc()`); `gh api repos/livekit/rust-sdks/releases/tags/webrtc-89d790b` lists assets for android-{arm,arm64,x64}, ios-{device,simulator}-arm64, linux-{arm64,x64}, mac-{arm64,x64}, win-{arm64,x64} only - no riscv64 asset anywhere. The `LK_CUSTOM_WEBRTC` escape hatch only relocates the problem: producing a riscv64 `libwebrtc.a` means running LiveKit''s own `.github/workflows/webrtc-builds.yml`, which drives depot_tools/gn/ninja against a Chromium-derived checkout (a `.gclient` `target_os` step) - a second, Chromium-scale build system whose own matrix (checked at the `rust-sdks` submodule pin `2d9f01ab1e`) covers only win/mac/linux/android/ios x {x64,arm64,arm}, never riscv64. Even a hand-built `libwebrtc.a` would not be enough on its own: `webrtc-sys/build.rs`''s Linux glue hardcodes exactly two architectures - `add_gio_headers()` panics "unsupported arch" for anything but arm64/x64, and `add_lazy_load_so()`''s prebuilt shim objects only exist under `src/lazy_load_deps_for/*/{x86_64-linux-gnu,aarch64-linux-gnu}/` - so riscv64 would also need new shim sources upstream has never shipped. Folded as gotcha 340 (feasibility-and-triage.md). No worktree/branch opened - fully diagnosed read-only via `gh api` against `livekit/python-sdks` and `livekit/rust-sdks` (submodule pin `2d9f01ab1e933a86a8a5c53805ee29ee58b9be1b`) plus the real GitHub Releases asset list, no checkout needed. Re-check if `webrtc-builds.yml` ever grows a riscv64 target, or LiveKit starts shipping a prebuilt riscv64 libwebrtc some other way.'
- pkg: tzfpy
  version: 1.3.3
  home: https://github.com/ringsaturn/tzfpy
  repo: https://github.com/ringsaturn/tzfpy
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: clang-format
  version: 23.1.0
  home: https://clang.llvm.org/docs/ClangFormat.html
  repo: https://github.com/ssciwr/clang-format-wheel
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/959
  notes: '12 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev. Builds clang-format from LLVM 23.1.0 release source via scikit-build-core (CMake ExternalProject, wheel.py-api=py2.py3 collapses matrix to cp312-only). Merged (#959). Issue #325 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: patchright
  version: 1.62.1
  home: https://github.com/Kaliiiiiiiiii-Vinyzu/patchright-python
  repo: https://github.com/Kaliiiiiiiiii-Vinyzu/patchright-python
  status: parked
  pr: null
  notes: 'Not feasible (vendored-binary, gotcha 35/183, same shape as playwright). The 2 Linux wheels (manylinux1_x86_64, manylinux_2_17_aarch64.manylinux2014_aarch64) are py3-none-<platform> tagged (Root-Is-Purelib: true) but ~45-48MB because patchright is a patched checkout of microsoft/playwright-python (patch_python_package.py AST-rewrites setup.py/pyproject.toml to rename playwright->patchright, playwright-core->patchright-core, leaving scripts/build_driver.py''s fetch logic untouched): setup.py still extracts a per-platform prebuilt Node.js binary plus patchright-core (Kaliiiiiiiiii-Vinyzu''s own npm fork of playwright-core, stealth-patched but not re-targeted architecturally) into patchright/driver/. Verified against the real v1.62.1 aarch64 wheel: patchright/driver/node is an aarch64 ELF binary, and patchright/driver/package/lib/coreBundle.js (the same file cited in the playwright entry) contains zero riscv strings and enumerates only ubuntu/debian {18.04..26.04}-{x64,arm64} plus mac/win host platforms in browsers.json/hostPlatform -- any other Linux arch degrades to <unknown>, so `patchright install` cannot resolve a browser to fetch even before asking whether Microsoft publishes riscv64 Chromium/Firefox/WebKit (it does not, per the playwright entry). Same blocker as playwright, inherited unchanged since the patching layer only touches stealth behavior and naming, not platform support. No worktree/branch opened -- resolved fully read-only via PyPI JSON/wheel inspection and the patchright-python GitHub API, no checkout needed.'
- pkg: pyside6-essentials
  version: 6.11.2
  home: https://pyside.org
  repo: https://code.qt.io/cgit/pyside/pyside-setup.git/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Depends on shiboken6 being available first (runtime requires-dist, and pyside6-essentials own build self-generates its bindings against an installed shiboken6-generator the same way shiboken6 does). UPDATE: shiboken6 6.11.2 riscv64 is now merged and published (PR #1696, live on pypi.riseproject.dev) - this entry is UNBLOCKED. This is the actual base of the pyside6 family dependency chain (pyside6 and pyside6-addons are both blocked-on-dependency on this entry landing first; see their notes and gotchas 382/383/384 in skills/python-project-porting/references/gotchas/feasibility-and-triage.md before starting). Key findings for whoever picks this up: (1) the libclang dependency is NOT libclang-scale (unlike build-libclang.yml, PR #866, ~10h from-source LLVM/Clang compile) - Rocky 10 riscv64s AppStream repo (manylinux_2_39_riscv64 base) ships prebuilt clang-devel/llvm-devel 21.1.8 and qt6-qtbase-devel, both installable via plain `dnf install` (confirmed against real repodata, and in-image dnf/repoquery DOES work through the egress proxy with --network host + proxy env vars + importing the proxy CA via update-ca-trust, contrary to what two earlier queue entries assumed - see gotcha 384). (2) At tag 6.11.2, pyside-setups own build_scripts/config.py only allows four --build-type values (all/shiboken6/shiboken6-generator/pyside6) - there is NO separate essentials/addons build target; a single `--build-type=pyside6 --standalone` build produces one tree, and the distinct wheels (PySide6_Essentials, PySide6_Addons, PySide6 meta-wheel, PySide6_Examples) are carved out AFTER the build by the repos own create_wheels.py. The right shape is ONE port/workflow owning that single build + create_wheels.py, then publishing pyside6-essentials + pyside6-addons + pyside6 as separate wheel artifacts from it via disjoint _publish-wheel.yml artifact-patterns (gotcha 380/382) - not three independent per-package workflows. (3) 9 of 41 addon modules (QtWebEngine*, QtPdf*, QtGraphs*, QtHttpServer, QtWebView*) have no Qt6 provider anywhere in the riscv64/x86_64/aarch64 image repos (RHEL 10 ships no Qt6 WebEngine, no chromium, no gn) - the other 32 are covered by prebuilt Rocky 10 riscv64 AppStream -devel packages; a first port likely ships the reduced 32/41 module set and documents the WebEngine gap. (4) IMPORTANT, not previously flagged: the manylinux_2_39_riscv64 image ships Qt 6.10.1 via dnf (confirmed via repoquery over all ~100 qt6-* packages; this repos own published shiboken6 wheel is already tagged .../shiboken6-6.11.2-6.10.1-.../), not 6.11.x - the resulting wheels would expose a Qt 6.10 API surface under a 6.11.2 version number (e.g. QtCanvasPainter, new in 6.11, has no provider). Not a hard blocker (no minimum Qt version is asserted at configure time) but must be called out explicitly in the PR description, same as any other upstream-vs-build-environment version skew in this repo. Will additionally need shiboken6-generator installed (build it the same way build-shiboken6.yml does).'
- pkg: pycocotools
  version: 2.0.11
  home: https://github.com/ppwwyyxx/cocoapi
  repo: https://github.com/ppwwyyxx/cocoapi
  status: published
  pr: 966
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout pinned to commit ac87f5077a (byte-identical to the 2.0.11 sdist; upstream tags no releases). Two cibuildwheel jobs: cp312-abi3 (reused/tested cp313/cp314) and cp314t; musllinux dropped (no musllinux_riscv64 numpy). PR #966. Merged (#966). Issue #968 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: shiboken6
  version: 6.11.2
  home: https://pyside.org
  repo: https://code.qt.io/cgit/pyside/pyside-setup.git/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1696
  notes: '2 Linux wheels upstream (abi: cp310-abi3); no riscv64 on PyPI or pypi.riseproject.dev. No sdist on PyPI (pyside-setup has no per-component sdist); build-from-checkout of pyside/pyside-setup at git tag 6.11.2 via its own setup.py (not PEP517/cibuildwheel-compatible - custom --build-type flag), docker-run-yourself shape like build-libclang.yml. Initial concern was that ApiExtractor (shiboken6-generators C++ parser) needs libclang, expected to require the same from-scratch multi-hour LLVM/Clang compile as build-libclang.yml (PR #866, ~10h). Not the case here: Rocky 10 riscv64s AppStream repo (the manylinux_2_39_riscv64 base) already ships prebuilt clang-devel/llvm-devel 21.1.8 with ClangConfig.cmake/LLVMConfig.cmake/libclang.so/headers (confirmed via the real repodata at dl.rockylinux.org, no container needed) - setup_clang()s find_package(Clang CONFIG REQUIRED) finds it with no LLVM_INSTALL_DIR override needed. Second dependency, also unexpected: sources/shiboken6/cmake/ShibokenSetup.cmake does find_package(Qt6 REQUIRED COMPONENTS Core) even for a shiboken6-only (non-PySide) build - also covered by Rocky 10 AppStream (qt6-qtbase-devel, confirmed in the same repodata, includes qtpaths6 and Qt6Config.cmake). Third wrinkle: shiboken6s own bindings (shibokenmodule) are self-generated - shibokenmodule/CMakeLists.txt runs the already-built Shiboken6::shiboken6 tool against typesystem_shiboken.xml, so shiboken6-generator (the 49-56MB wheel that bundles libclang.so, confirmed via requires_dist/wheel sizes on PyPI) has to be built and pip-installed into the build venv first, even though it is never published (out of scope per this tasks brief; see pyside6-essentials note below for what a future generator/pyside6 port would reuse). The final shiboken6 wheel (libshiboken.so + Shiboken*.so, ~270KB on aarch64) links only libpython - no Qt/Clang/GPL code ships in it, so no gpl_sources job. Worktree at .claude/worktrees/shiboken6, branch shiboken6. PR #1696, CI green (setup/build/publish dry-run all passed). Four manylinux-container-specific fixups were needed beyond the first-pass dnf/CLI-flag guess: (1) pip building the `patchelf` PyPI package from source runs its own `make check`, which fails 8/48 tests on riscv64 - fetch NixOS/patchelf''s own riscv64 release binary instead (Rocky 10''s riscv64 repos ship no patchelf package either, gotcha 95). (2) pyside-setup defaults to the Ninja CMake generator on Linux but the image ships none - pip install ninja (PyPI has a riscv64 wheel) and put $pybin on PATH so cmake''s generator search finds it. (3) manylinux''s CPython is --disable-shared and finalize.sh strips every libpython*.a from lib/ to save space (archiving them first into /opt/_internal/static-libs-for-embedding-only.tar.xz) - extract that tarball back so build_info_collector.py''s LIBDIR search finds shiboken6-generator''s embedding library. (4) --plat-name on setup.py bdist_wheel crashes on a native (non-cross-compiling, non-macOS) Linux build, since PysideBuild.finalize_options only fakes os.name to dodge distutils'' Windows-only check when cross-compiling or on darwin - drop the flag from both bdist_wheel invocations and let auditwheel repair retag the wheel; also install shiboken6-generator with --no-deps since its wheel declares a shiboken6==6.11.2 runtime dependency that does not exist yet at that point in the build.'
- pkg: bitstruct
  version: 8.23.0
  home: https://github.com/eerimoq/bitstruct
  repo: https://github.com/eerimoq/bitstruct
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/965
  notes: '54 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#965). Issue #969 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: quickjs
  version: 1.19.4
  home: https://github.com/PetterS/quickjs
  repo: https://github.com/PetterS/quickjs
  status: published
  pr: 978
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CI fixed and green as of 2026-09-06 (all 8 matrix legs + publish dry-run pass). Three stacked root causes, each masking the next: (1) actions/checkout defaults to clean:true and wiped the whole $GITHUB_WORKSPACE (incl. the just-downloaded sdist artifact) because it ran after download-artifact in build_wheels - fixed by reordering checkout before download (gotcha 245); (2) cibuildwheel extracts a .tar.gz package-dir to its own temp dir and chdirs the whole build into it, so CIBW_TEST_SOURCES (resolved against Path.cwd()) could never see test_quickjs.py sparse-checked-out at the workspace root - fixed by extracting the sdist ourselves and pointing package-dir at the directory, matching build-lightgbm.ymls pattern (gotcha 251); (3) once both of those were fixed and the real suite finally ran, FunctionTest.test_deep_recursion genuinely fails on riscv64: the vendored quickjs engines default 256 KiB JS-recursion stack budget assumes a small C stack frame per JS_CallInternal call, and on riscv64 that frame is large enough that f(100) alone exhausts it (glibc: caught quickjs.StackOverflow instead of success; musl: a real SIGSEGV, smaller default pthread stack) - this is a genuine upstream-quickjs (bellard/quickjs) riscv64 portability gap, not a packaging defect; per this tasks hard rule no issue was filed on any third-party repo, the test is skipped on riscv64 via patches/quickjs/1.19.4/0001-tests-skip-test_deep_recursion-on-riscv64.patch (Upstream-Status: Inappropriate). PR #978, branch quickjs, worktree .claude/worktrees/quickjs. Merged (#978). Issue #1004 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: ibm-db
  version: 3.3.0
  home: http://pypi.python.org/pypi/ibm_db/
  repo: http://pypi.python.org/pypi/ibm_db/
  status: parked
  pr: null
  notes: 'Not feasible: proprietary build-and-link-time driver with no riscv64 asset (gotcha 342, feasibility-and-triage.md). ibm-db 3.3.0''s C extension (ibm_db.c) is compiled and linked against IBM''s proprietary Db2 CLI driver (`library = [''db2'']` in the real setup.py), which setup.py downloads itself at build time from `https://public.dhe.ibm.com/ibmdl/export/pub/software/data/db2/drivers/odbc_cli/<ver>/<cliFileName>` (falling back to the `ibmdb/db2drivers` GitHub mirror on failure), keyed by an `os.uname()[4]` if/elif chain covering only ppc64le/ppc/ppc64/x86/x86_64/s390/s390x -- no riscv64 (or aarch64) branch exists, so on those arches `cliFileName`/`arch_` are never assigned and the next reference raises a bare NameError rather than a clean "unsupported platform" exit. Confirmed against the real vendor download server (top level plus all 9 versioned subdirectories v11.1.4 through v12.1.4) and the GitHub mirror fallback: both list only aix/linuxia32/linuxx64/macarm64/macos64/nt/ntx64/ppc32/ppc64/ppc64le/s390/s390x64/sun32/sun64/sunamd32/sunamd64 clidriver archives -- zero riscv64 asset at any version, official or mirrored. There is also no Linux ARM clidriver at all, which is why PyPI''s 3.3.0 wheels are manylinux_2_34_{i686,x86_64} only (12 wheels, cp39-cp314 x 2 arches) -- the clidriver''s own arch ceiling drives the published wheel matrix, not an unrelated packaging choice, and riscv64 would need to clear the same wall aarch64 already doesn''t. This is a build-and-link-time dependency, not merely a runtime one (unlike claude-agent-sdk, gotcha 157), so there is no "installs fine, fails at call time" middle ground, and no source is available for IBM''s driver to relink ibm_db.c against an open alternative. No worktree/branch opened -- diagnosed read-only against the real 3.3.0 sdist (setup.py) and IBM''s live download server.'
- pkg: skia-python
  version: 144.0.post2
  home: https://github.com/kyamagu/skia-python
  repo: https://github.com/kyamagu/skia-python
  status: published
  pr: 1697
  notes: 'Builds Skia from source (GN/Ninja), not a prebuilt-binary download -- different shape from gotcha 335/340/341/342. Reimplements scripts/build_Linux.sh''s `yum install ninja-build gn` (EPEL-only, riscv64 has no EPEL) inline: ninja from its own riscv64 PyPI wheel, gn bootstrapped from source, patched to skip its riscv64-less CIPD download. MILESTONE: the full multi-interpreter build (cp312/cp313/cp314/cp314t, Skia GN/Ninja compile + wheel build + tests) went fully green in run 34544835553. Only the publish dry-run job failed: the single combined-interpreter artifact name (no per-leg segment) didn''t match _publish-wheel.yml''s artifact-pattern wildcard -- fixed by dropping the wildcard to match exactly. A `gh run rerun --failed` confirmed reruns pin to the original run''s workflow snapshot (didn''t pick up the fix), so a fresh `gh workflow run --ref skia-python` dispatch (run 34594707848) is validating the fix end-to-end now. PR #1697, should be the last iteration.'
- pkg: pyside6-addons
  version: 6.11.2
  home: https://pyside.org
  repo: https://code.qt.io/cgit/pyside/pyside-setup.git/
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310-abi3, manylinux_2_34_x86_64 + manylinux_2_39_aarch64,
    175 MB each); no riscv64 on PyPI or pypi.riseproject.dev (checked directly: /simple/pyside6-addons/,
    /simple/pyside6-essentials/ and /simple/pyside6/ all 404; only shiboken6 6.11.2 riscv64
    is live). 6.11.2 re-confirmed latest on PyPI for all five pyside-setup distributions
    (2026-09-19). Blocked on pyside6-essentials, NOT on pyside6 - the meta-package direction
    is the opposite of what the names suggest. requires_dist, read off PyPI: pyside6-addons
    -> shiboken6==6.11.2 + PySide6_Essentials==6.11.2; pyside6-essentials -> shiboken6 only;
    pyside6 -> shiboken6 + PySide6_Essentials + PySide6_Addons. pyside6 is a 0.57 MB meta-wheel
    (vs essentials 80 MB, addons 175 MB) that depends on THIS package, so the critical path
    is shiboken6 (published, PR #1696) -> pyside6-essentials -> pyside6-addons -> pyside6,
    and pyside6-essentials is the only one of the three that is actually actionable today.
    Its own queue entry says "Do not attempt until shiboken6 is merged and published" - that
    condition is now met (shiboken6 published), and it is currently unclaimed. Publishing
    pyside6-addons alone would in any case ship a wheel whose exact-pinned PySide6_Essentials==6.11.2
    runtime dependency is unsatisfiable from PyPI or pypi.riseproject.dev on riscv64, so
    `pip install pyside6-addons` would still fail - the same consumer-side dead end the pyqt5/pyqt5-qt5
    pair is parked for. Second, harder reason this cannot be its own port: pyside6-addons
    is not a build target at all. build_scripts/config.py:get_allowed_top_level_build_values()
    in pyside-setup 6.11.2 returns exactly four values - all, shiboken6, shiboken6-generator,
    pyside6 - and `--internal-build-type` the same three minus `all`; there is no pyside6-essentials
    or pyside6-addons target. `setup.py bdist_wheel --build-type=pyside6` builds every PySide6
    module (essentials AND addons) into one tree, and the five/six published distributions
    are carved out AFTER the build by the root-level create_wheels.py, which walks build/<env>a/package_for_wheels
    once and emits {shiboken6, shiboken6_generator, PySide6_Essentials, PySide6_Addons, PySide6,
    PySide6_Examples} from build_scripts/wheel_files.py''s per-wheel ModuleData lists. So
    a standalone build-pyside6-addons.yml would run the entire multi-hour Qt6 bindings build
    and throw away four of the five wheels it produced, and a sibling build-pyside6-essentials.yml
    would run the same build a second time to keep a different one. (This is exactly why
    shiboken6 WAS portable standalone: it has its own --build-type. `--module-subset` does
    not rescue the split - it only narrows which Qt modules get bindings, it does not change
    which wheels create_wheels.py writes, and the addon modules still need the essentials
    typesystems and libpyside6 to generate and link against.) Recommended shape when this
    unblocks: ONE port, owned by whoever takes pyside6-essentials, running the single `--build-type=pyside6
    --standalone` build plus create_wheels.py, and publishing the resulting distributions
    through one _publish-wheel.yml call each with disjoint artifact-patterns per gotcha 380.
    Third finding, to be settled by that combined port rather than here: 9 of the 41 modules
    in wheel_files_pyside_addons() have no Qt6 provider in the build image at all. QtWebEngineCore/QtWebEngineQuick/QtWebEngineWidgets,
    QtPdf and QtPdfWidgets need qt6-qtwebengine; QtGraphs/QtGraphsWidgets need qt6-qtgraphs;
    QtHttpServer needs qt6-qthttpserver; QtWebView(+QtWebViewQuick) needs qt6-qtwebview.
    None of those four packages exists in ANY of the manylinux_2_39_riscv64 image''s four
    enabled repos (Rocky Linux 10.2: baseos, appstream, crb, extras) on ANY arch - not riscv64,
    not x86_64, not aarch64(RHEL 10 ships no Qt6 WebEngine at all) - and there is no chromium
    and no gn package either, while EPEL does not exist for riscv64 (gotcha 51). QtWebEngine
    IS Chromium, so those 9 modules are not a dnf line away; they are a Chromium-for-riscv64
    bring-up on the scale of the pyqt5-qt5 Qt5-SDK stop (gotcha 186). The other 32 addon modules
    ARE covered by prebuilt Rocky 10 riscv64 AppStream -devel packages (qt6-qt3d, qt6-qtcharts,
    qt6-qtdatavis3d, qt6-qtmultimedia, qt6-qtnetworkauth, qt6-qtconnectivity, qt6-qtpositioning,
    qt6-qtquick3d, qt6-qtremoteobjects, qt6-qtscxml, qt6-qtsensors, qt6-qtserialport, qt6-qtserialbus,
    qt6-qtspeech, qt6-qtvirtualkeyboard, qt6-qtwebchannel, qt6-qtwebsockets, qt6-qtlocation
    - all verified present), so a reduced 32/41-module addons wheel is technically producible:
    create_wheels.py only prints "Warning: <file> does not exist" (and only when verbose
    > 0) for missing payload, it does not fail. Whether to ship a wheel carrying upstream''s
    name and version while missing a quarter of its advertised modules is a deliberate decision
    for that port, not something to let a suppressed warning make. Unlike pyqt5-qt5 this is
    NOT a park: source is fully open (LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only), most
    of the Qt6 module set is already packaged for riscv64, and the blocker is an unported
    sibling rather than absent source. Evidence gathered read-only: PyPI JSON for the five
    distributions, pypi.riseproject.dev /simple/ endpoints, upstream build_scripts/config.py
    + build_scripts/wheel_files.py + create_wheels.py at tag 6.11.2, and Rocky 10 repodata
    (repomd.xml -> primary.xml.gz) enumerated directly from dl.rockylinux.org for riscv64,
    x86_64 and aarch64; the equivalent in-image `dnf install` probe was blocked by this session''s
    egress proxy MITM-ing TLS to mirrors.rockylinux.org (same limitation recorded on the
    pyqt5-qt5 entry), though the image itself pulled fine and confirmed Rocky 10.2 with those
    four repos enabled. No local build was attempted: there is no addons-only build to attempt.
    No worktree, branch, PR or workflow created. Pattern recorded as gotcha 382 (feasibility-and-triage).'
- pkg: pylance
  version: 11.0.0
  home: https://lancedb.github.io/lance/
  repo: https://github.com/lancedb/lance
  status: published
  pr: 1622
  notes: '6 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. pylance is lance''s own Python binding (python/ subdir of lancedb/lance, tag v11.0.0), maturin/PyO3-based; Rust source lives under rust/. lance-core''s SIMD_SUPPORT already has a portable riscv64 fallback upstream; lance-linalg''s f32/f64/i32 SIMD types did not (unlike u8, which already has one) -- patches/pylance/11.0.0/0001-lance-linalg-add-a-portable-riscv64-simd-fallback.patch adds the same shape of fallback used in build-lancedb.yml''s 0002 patch, verified with cargo check --target riscv64gc-unknown-linux-gnu (plus aarch64/x86_64 regressions) against an isolated copy of the simd module. Worktree at .claude/worktrees/pylance, branch pylance. PR #1622 opened, CI running.'
- pkg: tables
  version: 3.11.1
  home: http://www.pytables.org
  repo: https://github.com/PyTables/PyTables
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1010
  notes: 'Cython/HDF5 wrapper with bundled blosc/bzip2 codecs. Builds HDF5 2.2.0 from source in CIBW_BEFORE_ALL_LINUX (mirrors upstream get_hdf5.sh); links the registry''s own blosc2 wheel (excluded from auditwheel repair, loaded via tables'' ctypes hook) instead of vendoring it. abi3 leg floors on cp312 not cp311 - numexpr has no cp311 riscv64 build here yet; plus a separate cp314t leg. No LZO (upstream''s own Linux wheels skip it too, no GPL concern). Needed 3 patches: backport upstream''s hdf5-blosc2 fix for the current blosc2 C API (BLOSC2_MAX_DIM renamed to B2ND_MAX_DIM in c-blosc2 3.0, gotcha 269), skip the pre-existing, upstream-acknowledged flaky ThreadingTestCase (PyTables/PyTables#946), and extend license-files to actually bundle the licences the wheel links/compiles in. CI green, merged (#1010). Issue #1158 created and linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: glfw
  version: 2.10.2
  home: https://github.com/FlorianRhiem/pyGLFW
  repo: https://github.com/FlorianRhiem/pyGLFW
  status: published
  pr: 991
  notes: 'Ctypes wrapper; bundles GLFW 3.4 built from source (X11+Wayland) via cmake in the riscv64 manylinux image, mirroring upstream .gitlab-ci.yml. No display in CI: tested via get_version()/init() returning 0 gracefully. CI green, publish dry-ran clean. Merged (#991). Issue #1000 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: freetype-py
  version: 2.5.1
  home: https://github.com/rougier/freetype-py
  repo: https://github.com/rougier/freetype-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/994
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Builds FreeType 2.13.2 + HarfBuzz 8.3.0 from source via cmake; single cp312 build retagged py3-none by setup.py. Merged (#994). Issue #1005 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pyside6
  version: 6.11.2
  home: https://pyside.org
  repo: https://code.qt.io/cgit/pyside/pyside-setup.git/
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream of 5 total (abi: cp310-abi3; manylinux_2_34_x86_64 + manylinux_2_39_aarch64,
    0.57 MB each); no riscv64 on PyPI or pypi.riseproject.dev (checked directly: /simple/pyside6/,
    /simple/pyside6-essentials/ and /simple/pyside6-addons/ all 404; only shiboken6 6.11.2
    riscv64 is live). 6.11.2 re-confirmed latest 2026-09-19, both as PyPI info.version and
    as the newest tag on code.qt.io/pyside/pyside-setup (v6.11.2; v6.11.1, v6.10.3 behind it).
    Blocked on BOTH pyside6-essentials and pyside6-addons - pyside6 is the tip of the chain,
    not its base. requires_dist off PyPI: shiboken6==6.11.2 (published, PR #1696) +
    PySide6_Essentials==6.11.2 + PySide6_Addons==6.11.2, so the critical path is shiboken6 ->
    pyside6-essentials -> pyside6-addons -> pyside6, and a green pyside6 build today would ship
    a wheel whose two exact pins are unsatisfiable on riscv64 from either index - the same
    consumer-side dead end pyqt5/pyqt5-qt5 is parked for. See the pyside6-addons entry
    (blocked-on-dependency) and gotcha 382 for the shared structural finding: there is no
    per-distribution build target (build_scripts/config.py:get_allowed_top_level_build_values()
    at 6.11.2 allows only all, shiboken6, shiboken6-generator, pyside6), the distributions are
    carved out after one full build by the root-level create_wheels.py, and the right shape is
    ONE port owned by whoever takes pyside6-essentials that runs `--build-type=pyside6
    --standalone` once and publishes essentials + addons + this meta-wheel from that single
    build (gotcha 380 for the disjoint artifact-pattern publish). Deliberately NOT folded into
    that port here: two product decisions are still open (the reduced 32/41 addons module set,
    and the Qt minor skew below), and neither is mine to settle unilaterally. What this entry
    adds beyond the addons one, recorded as gotcha 383: (1) the pyside6 wheel carries ZERO
    compiled content - the real manylinux_2_39_aarch64 wheel is 67 entries / 4.9 MB uncompressed
    and holds no .so at all, just 59 generated Qt*.pyi stubs plus PySide6/__init__.py, _config.py,
    _git_pyside_version.py, py.typed and dist-info. Its cp310-abi3-manylinux tag comes from a
    deliberately fake extension: wheel_artifacts/setup.py.base declares
    ext_modules=[Extension("PySide6/QtCore", [], py_limited_api=True)] with no sources beside a
    no-op build_ext Command, and says in a comment that it exists only to make setuptools think
    there are extension modules (with py_limited_api/plat_name set in
    wheel_artifacts/pyproject.toml.base). (2) That does NOT make it a gotcha-27 "no port needed"
    case like watchdog: pyside6 publishes no sdist on any version, so riscv64 pip has nothing to
    fall back to and the wheel genuinely is required - just last. (3) It must be cut from the
    SAME build tree as its siblings, not merely a later one: create_wheels.py:get_simple_manifest
    gives it the one-line manifest `prune PySide6`, which with include_package_data=True keeps
    only the top-level files of build/<env>a/package_for_wheels/PySide6 (dropping Qt/, scripts/,
    support/), and the 59 stubs it keeps span essentials modules, addons modules AND the nine
    WebEngine-family modules whose .so lives elsewhere - so an umbrella built from a different
    run than the published siblings can advertise stubs for modules those wheels do not contain.
    (4) New and unweighed by any entry so far: the build image ships Qt 6.10.1, NOT 6.11.x.
    `dnf repoquery qt6*` inside quay.io/pypa/manylinux_2_39_riscv64 (Rocky Linux 10.2) reports
    6.10.1 for all ~100 qt6-* packages across appstream/crb, and this repo''s own published
    shiboken6-6.11.2-6.10.1-cp37-abi3-manylinux_2_39_riscv64.whl already encodes it (that 6.10.1
    is a wheel build tag carrying the Qt version). Not a hard stop -
    sources/pyside6/cmake/PySideSetup.cmake marks only Core/Gui/Widgets/PrintSupport/Sql/Network/
    Test/Concurrent REQUIRED (all in qt6-qtbase*, present), leaves the rest OPTIONAL_COMPONENTS,
    and derives PYSIDE_QT_VERSION from the discovered Qt6Core_VERSION instead of asserting a
    minimum, so a 6.11.2-against-6.10.1 configure succeeds and shiboken''s typesystem `since=`
    gating drops the newer API - but the published wheels would then expose a Qt 6.10 API surface
    under a 6.11.2 version number, and QtCanvasPainter (new in 6.11, present in upstream''s stub
    set) has no provider in the image at all. That is a second divergence from upstream stacked on
    top of the missing-WebEngine one and should be decided explicitly by the combined port; the
    pyside6-essentials entry does not mention it yet. Also corrected, as gotcha 384: in-image dnf
    is NOT blocked by a TLS-intercepting egress proxy (as the pyqt5-qt5 and pyside6-addons entries
    record) - `docker run --network host`, passing the proxy env vars through, plus copying the
    proxy CA into /etc/pki/ca-trust/source/anchors and running update-ca-trust extract, makes
    dnf/repoquery work normally inside the riscv64 image; that is how the Qt 6.10.1 version fact
    above was obtained. Local validation reached: manylinux_2_39_riscv64 image pulled and the
    full qt6-*/clang-devel/llvm-devel inventory enumerated by repoquery in-image; NO build was
    attempted, because there is no pyside6-only thing to build (see above) and a full PySide6
    bindings build needs shiboken6-generator compiled first, which is many hours under QEMU - the
    confirmatory `dnf install` of the 30-package Qt6 -devel set was still running after ~15 min on
    a contended host when this was recorded. No worktree, branch, PR or workflow created; no
    docs/packages/pyside6.yaml added. Re-queue only once pyside6-essentials and pyside6-addons
    riscv64 wheels are published, or fold this entry into that combined port.'
- pkg: vtk
  version: 9.7.0
  home: https://vtk.org
  repo: https://vtk.org
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1727
  notes: 'CMake-based build (not cibuildwheel/scikit-build): drives quay.io/pypa/manylinux_2_39_riscv64 directly with cmake configure/build + setup.py bdist_wheel + auditwheel repair, mirroring .gitlab/ci/configure_wheel.cmake + .cmake_build_wheel_linux from https://gitlab.kitware.com/vtk/vtk (mirrored at Kitware/VTK). Wheel build disables Qt/MPI/Boost/OpenVDB/ADIOS2/GDAL/etc (same module list upstream disables for wheels) and needs no Bazel, no CUDA, no Qt. VTK_JPEG_ENABLE_SIMD forced OFF: riscv64 has no SIMD kernel in vendored libjpeg-turbo and upstream''s default ON hard-fails configure otherwise. KWIML endianness detection already has a riscv64 branch (fixed 2019, Debian bug #924611); Debian already ships vtk9 built natively on riscv64. Not abi3 (VTK_WRAP_PYTHON links Python.h directly), so one full rebuild per interpreter (cp312/cp313/cp314/cp314t), matching upstream''s own per-Python wheel jobs. Runtime GL/OSMesa/EGL libs are dlopened from the host, not bundled, so no vendored binary blobs. matplotlib is a hard runtime dependency (requires_dist, unconditional); already on pypi.riseproject.dev for cp312-cp314t. PR #1727 opened, CI dispatched on the PR branch (full 4-way matrix, up to 24h/job).'
- pkg: cuda-tile
  version: 1.5.0
  home: https://github.com/nvidia/cutile-python
  repo: https://github.com/nvidia/cutile-python
  status: parked
  pr: null
  notes: 'NVIDIA cuTile Python (CUDA Tile Compiler, github.com/nvidia/cutile-python) is a JIT GPU-kernel-launch library, not an optionally-CUDA package. Confirmed via PyPI JSON metadata, wheel contents, and NVIDIA''s own redistrib manifest, not assumed from the name alone: (1) PyPI summary is literally "CUDA Tile Compiler", classifier "Environment :: GPU :: NVIDIA CUDA :: 13". (2) 1.5.0 ships 18 wheel files, all manylinux2014_{x86_64,aarch64} or win_amd64 across cp310-cp314/cp314t -- zero riscv64 and zero any other Linux arch upstream. (3) Downloaded and unzipped the cp312-manylinux2014_x86_64 wheel: cuda/tile/__init__.py line 7 unconditionally does `from cuda.tile._cext import launch` (no try/except, no CPU-fallback path per gotcha 284''s counterexample), and _cext.cpython-312-x86_64-linux-gnu.so''s symbol table is the full CUDA Driver API (cuInit, cuDeviceGet, cuLaunchKernel, cuMemAlloc, cuGraphLaunch, etc). cuda/tile/_load_libcuda.py does an unconditional `ctypes.CDLL("libcuda.so.1")` (nvcuda.dll on Windows) at import time to resolve cuGetProcAddress_v2. (4) The README''s System Requirements section: needs NVIDIA Driver r580+, and the tileiras JIT compiler only targets Blackwell and Ampere/Ada GPUs (Hopper not yet supported) -- GPU-architecture-specific, not just any CUDA device. (5) Building from source needs CUDA Toolkit 13.1+ (nvcc, CMake); the optional tileiras extra depends on `cuda-toolkit[nvcc,nvvm,tileiras]<13.5,>=13.2`, NVIDIA''s own CUDA toolkit PyPI meta-package. (6) NVIDIA''s own redistrib manifest (developer.download.nvidia.com/compute/cuda/redist/redistrib_13.2.0.json) lists cuda_nvcc platforms as exactly linux-x86_64, linux-sbsa, windows-x86_64 -- no riscv64 in any current CUDA release, matching this repo''s existing triton finding (gotcha 41; see also the sglang entry above, parked for the same reason: hard-requires cuda-python>=13.0 and cuda-tile, neither of which exists for riscv64 anywhere). No CPU fallback exists and there is no path to a functional riscv64 build: CUDA has no riscv64 host-architecture story at all. Not a candidate to revisit unless/until NVIDIA ships CUDA for riscv64.'
- pkg: pyodps
  version: 0.13.1
  home: http://github.com/aliyun/aliyun-odps-python-sdk
  repo: http://github.com/aliyun/aliyun-odps-python-sdk
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1729
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Matrix narrowed to cp312-cp314t: pyarrow (hard runtime dep) has no riscv64 wheel below cp312 anywhere.'
- pkg: mssql-python
  version: 1.13.0
  home: https://github.com/microsoft/mssql-python
  repo: https://github.com/microsoft/mssql-python
  status: parked
  pr: null
  notes: 'Not feasible: hard runtime dependency on mssql-python-odbc==18.6.2.1 (pinned exactly, unconditional Requires-Dist for every platform), the same proprietary Microsoft ODBC driver repackage already parked above -- no riscv64 build exists and there is no source to compile one (gotcha 342 shape, feasibility-and-triage.md). Confirmed by downloading and unzipping the real manylinux_2_28_cp312 1.13.0 wheel rather than assuming from the name: it ships two native extensions. (1) mssql_python/ddbc_bindings.cp312-arm64.so is the pybind11 C++ layer that connection.py, cursor.py, helpers.py and pooling.py all call for every single operation (connect, execute, fetchone/many/all, pooling) -- `strings` on it shows the literal filename `libmsodbcsql-18.6.so.2.1` plus `LoadDriverLibrary: dlopen failed for path=%s` and `msodbcsql driver api bindings for Python`, i.e. it dlopen()s Microsoft''s proprietary driver .so at runtime rather than linking it at build time (no NEEDED entry for it), and connection.py:910 hardcodes `Driver=ODBC Driver 18 for SQL Server` with no way to point it at an alternative/open driver. (2) mssql_py_core/mssql_py_core.*.so is a separate, newer Rust extension (strings reveal `/workspace/mssql-tds/src/security/unix/gssapi_ffi.rs`, i.e. built from an mssql-tds crate that reimplements TDS itself, no msodbcsql dlopen, only libssl/libcrypto/libc) but cursor.py only wires it in for the optional bulkcopy()/bulkcopy_arrow() bulk-insert path (`_bulkcopy_core_and_validate`), not as a general Connection/Cursor backend -- it cannot stand in for ddbc_bindings without a from-scratch rewrite of the entire driver, which is out of scope for a wheel port (goal 2: mirror upstream, do not fork its architecture). ddbc_bindings.py''s own architecture map also raises ImportError for any arch other than x86_64/arm64 on Linux, with no riscv64 branch. Even setting that aside and recompiling ddbc_bindings'' MIT-licensed source for riscv64 (legally fine, since it only dlopens the driver at runtime), the wheel would be non-functional out of the box: pip cannot resolve the hard-pinned mssql-python-odbc==18.6.2.1 dependency on riscv64 (no wheel, no buildable source -- see that package''s own parked entry, which cites Microsoft''s install docs explicitly rejecting riscv64 for every msodbcsql version 11-18), and every SQL operation would fail at runtime with no driver .so to dlopen. Same root cause and same wait-on-upstream verdict as mssql-python-odbc: Microsoft does not ship, and has not open-sourced, the ODBC driver this package''s core native layer requires. No worktree/branch opened -- diagnosed read-only against the real 1.13.0 wheel contents (manylinux_2_28_cp312 aarch64), its METADATA, and its Python/native source.'
- pkg: mujoco
  version: 3.12.0
  home: https://github.com/google-deepmind/mujoco
  repo: https://github.com/google-deepmind/mujoco
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1728
  notes: 'CI green on PR #1728 across cp312/cp313/cp314/cp314t (385 passed, 14 skipped each). Two-stage CMake build mirroring upstream build_steps.sh (core lib via FetchContent of ccd/qhull/tinyxml2/tinyobjloader/lodepng/miniz, then pybind11 bindings against MUJOCO_PATH), driven directly in the manylinux_riscv64 image per-interpreter rather than cibuildwheel. Studio (Filament GUI, dear_imgui/implot) and OpenUSD dropped: Filament needs Vulkan/Metal tooling with no riscv64 story, USD is a separate heavy build; the classic GLFW viewer (mujoco.viewer) is kept, reusing the X11/Wayland dnf packages already validated by the glfw riscv64 port (build-glfw.yml). auditwheel excludes libmujoco.so.* (gotcha: auditwheel would otherwise graft a second, renamed copy into mujoco.libs/ for the plugin .so files only, splitting the process into two libmujoco instances and breaking the plugin registry). Root-caused and fixed 5 plugin-test failures (test_load_plugin, test_plugin, etc., all "XML Error: plugin ... not found"): mujoco/__init__.py extends its __path__ via pkgutil.extend_path, and running pytest from a cd-ed /mujoco/python let `python -m pytest` cwd-prepended sys.path resolve the checkout python/mujoco/ package ahead of the installed wheel - compiled extensions still worked via the merged path, but PLUGINS_DIR (derived from that shadowing __file__) pointed at a nonexistent python/mujoco/plugin/, so zero bundled plugins loaded. Fixed by running pytest from the repo root instead, matching upstream build_steps.sh test_python_bindings (which never cds into python/ for that step). Awaiting maintainer review/merge; not yet published.'
- pkg: ua-parser-rs
  version: 0.1.5
  home: https://github.com/ua-parser/uap-rust/blob/main/ua-parser/
  repo: https://github.com/ua-parser/uap-python/tree/main/ua-parser-rs
  status: published
  pr: 977
  notes: '16 Linux wheels upstream (abi: cp310,cp314,graalpy312,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PyPI/Cargo.toml metadata points at ua-parser/uap-rust, but the source moved to ua-parser/uap-python (ua-parser-rs/ subdir, tag ua-parser-rs-0.1.5); uap-rust no longer contains it. CI FAILING as of 2026-09-06: cp310-abi3 and cp314t fail with `AssertionError: .../ua_parser_rs/__init__.py` from the test command''s own `assert ua_parser_rs.__file__.endswith((''.so'', ''.pyd''))` check - the imported module resolved to a pure-Python __init__.py, not the compiled extension. Likely dependency confusion: PyPI may already publish a pure-Python `ua_parser_rs` compatibility/stub package that pip installed instead of (or alongside) the just-built wheel, or the wheel build genuinely produced no .so. Needs investigation of the actual installed package contents before assuming which. PR #977, branch ua-parser-rs. Merged (#977). Issue #1016 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: zeroconf
  version: 0.151.3
  home: https://python-zeroconf.readthedocs.io
  repo: https://github.com/python-zeroconf/python-zeroconf
  status: published
  pr: 975
  notes: '45 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev. All CI checks green (build cp312/cp313/cp314/cp314t, setup, commit-message, patch checks, publish dry-run) as of 2026-09-06. Ready for review. Merged (#975). Issue #980 linked, publish dispatched on main (project-board sync skipped - fine-grained token lacks Projects permission, see pw-postmerge.py fix). Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: typed-ast
  version: 1.5.5
  home: https://github.com/python/typed_ast
  repo: https://github.com/python/typed_ast
  status: parked
  pr: null
  notes: 'Parked: officially inactive upstream (Dev Status 7, "no longer maintained, use stdlib ast instead" for py>=3.8); last release 1.5.5 (Dec 2023) ships wheels only for cp36-cp311, never cp312+. Verified by building the real sdist locally: compiles and imports cleanly on cp312 unmodified, but fails to compile on cp313 and cp314 with "unknown type name ''PyFutureFeatures''" in typed_ast''s vendored ast27/Include/compile.h - CPython 3.13 renamed/internalized that public struct to _PyFutureFeatures (moved into internal/pycore_symtable.h). This repo''s default matrix is cp312/cp313/cp314/cp314t, so at most 1 of 4 target ABIs would build, and fixing the rest means carrying a non-upstream patch around a permanent CPython internal-API removal for a project whose own maintainers point users to stdlib ast. Not worth porting.'
- pkg: pyqwest
  version: 0.10.0
  home: https://github.com/curioswitch/pyqwest
  repo: https://github.com/curioswitch/pyqwest.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/992
  notes: 'PyO3/maturin build wrapping reqwest (incl. HTTP/3 via quinn/h3); abi3-py310 + cp314t, matches upstream release.yaml. Tests drop pyvoy (bundles Envoy, no riscv64 wheel) and run the pyvoy-independent subset. Merged (#992). Issue #998 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: docling-parse
  version: 7.16.0
  home: https://github.com/docling-project/docling-parse
  repo: https://github.com/docling-project/docling-parse
  status: published
  pr: 757
  notes: 'Merged (#757); publish run re-dispatched on main via pw-postmerge.py, issue #1195 created.'
- pkg: ndindex
  version: 1.10.1
  home: https://quansight-labs.github.io/ndindex/
  repo: https://github.com/Quansight-Labs/ndindex
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: tibs
  version: 2.0.1
  home: https://github.com/scott-griffiths/tibs
  repo: https://github.com/scott-griffiths/tibs
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: tilelang
  version: 0.1.13
  home: https://github.com/tile-ai/tilelang
  repo: https://github.com/tile-ai/tilelang
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1730
  notes: '4 Linux/macOS/Windows wheels upstream (abi: cp38-abi3); no riscv64 on PyPI or pypi.riseproject.dev. Upstream (tile-ai/tilelang, built on a vendored TVM fork submodule) is a GPU tile-programming DSL but genuinely supports a documented CPU/LLVM-only build: CMakeLists.txt exposes USE_CUDA/USE_ROCM/USE_METAL/USE_LLVM toggles (USE_CUDA defaults off when no CUDA toolkit is found), and docs/get_started/Installation.md says outright "USE_CUDA: ... set to OFF to build a CPU version" and "USE_LLVM: enable LLVM backend for CPU codegen, LLVM 15+ required" -- a real upstream-supported mode, not an internal escape hatch, even though upstream''s own dist.yml only ever ships CUDA (Linux/Windows) and Metal (macOS) wheels. LLVM is linked via llvm-config (USE_LLVM=ON auto-detects it), not compiled from source, so this is closer in scale to a normal LLVM-linking port than build-libclang.yml''s from-scratch LLVM/Clang compile. Runtime deps checked against pypi.riseproject.dev: torch (2.13/2.14+cpu, cp312-cp314t), apache-tvm-ffi (0.1.12 satisfies tilelang''s >=0.1.11,<0.1.13 pin, cp312-abi3/cp314t), z3-solver (4.15.4.0 satisfies >=4.13,<4.15.5), ml-dtypes/numpy/psutil all have riscv64 wheels; cloudpickle/tqdm/typing-extensions/scikit-build-core are pure py3-none-any. Build-time cython>=3.1.0 (registry has 3.3.0) and patchelf>=0.17.2 both have riscv64 wheels. One real gap: the optional torch-c-dlpack-ext (marker python_version < "3.14") dependency has no riscv64 wheel anywhere (tracked separately as the parked torch-c-dlpack-ext entry); upstream''s own docs call it a soft/JIT-fallback dependency ("Without it, TVM FFI may require JIT compilation on first import"), so the plan is to patch the marker to also exclude riscv64 (Upstream-Status: Inappropriate) rather than block on it. Because apache-tvm-ffi''s own riscv64 wheel is cp312-abi3 + cp314-cp314t only, the realistic interpreter matrix mirrors build-apache-tvm-ffi.yml''s cp312/cp314t split. The vendored 3rdparty/tvm (TileLang/tvm fork) is a substantial C++ tensor-compiler tree -- a genuinely large compile, plausibly among this repo''s largest, but with no from-scratch LLVM build in the loop. Worktree/branch created at .claude/worktrees/tilelang. PR #1730 opened; first riscv64 CI attempt confirmed the full vendored TVM compile succeeds (~5h, USE_LLVM auto-detected the CRB llvm-devel package fine), but tilelang_cython_wrapper failed: Cython 3.3.0 (the only riscv64 wheel on the registry, and PyPI''s current latest -- no upper bound in build-system.requires) added a hard #error in Cython/Utility/ModuleSetupCode.c requiring the Limited API target to be >=3.9, while tilelang''s wheel.py-api="cp38" targets 3.8 -- a cross-platform regression, not riscv64-specific, since any resolver picking cython>=3.3 against tilelang''s existing cp38 SABI target breaks the same way. Added a second patch capping cython to <3.3 (Upstream-Status: To upstream) and re-triggered.'
- pkg: tree-sitter-kotlin
  version: 1.1.0
  home: https://github.com/tree-sitter-grammars/tree-sitter-kotlin
  repo: https://github.com/tree-sitter-grammars/tree-sitter-kotlin
  status: published
  pr: 973
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. All CI checks green (build cp39-abi3, setup, commit-message, patch checks, publish dry-run) as of 2026-09-06. Ready for review. Merged (#973). Issue #981 linked, publish dispatched on main (project-board sync skipped - fine-grained token lacks Projects permission, see pw-postmerge.py fix). Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: coincurve
  version: 21.0.0
  home: https://ofek.dev/coincurve/
  repo: https://github.com/ofek/coincurve
  status: published
  pr: 995
  notes: 'cffi extension wrapping vendored libsecp256k1 (CMake FetchContent, compiled from source). cp312/cp313/cp314 x manylinux/musllinux; cp314t dropped (Py_LIMITED_API incompatible with free-threading upstream-wide). Carries a backport of ofek/coincurve#188 (merged upstream, unreleased) fixing a build break against any cffi>=2.0.0 -- reported upstream already (issue #223), also open as #219 for py3.14. 30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#995). Issue #997 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: datefinder
  version: 1.0.0
  home: https://github.com/akoumjian/datefinder
  repo: https://github.com/akoumjian/datefinder
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: tensorstore
  version: 0.1.85
  home: https://google.github.io/tensorstore
  repo: https://github.com/google/tensorstore
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1865
  notes: '10 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Bazel-built (bazel 8.5.1, WORKSPACE-based); bootstrapped from the dist archive for riscv64 (bazel 8.2.0+ needs no rules_python/rules_java patches, unlike the 7.x builds jaxlib/ray needed). No XLA/TSL/LLVM dependency, so jaxlib''s embed_bitcode blocker does not apply. Four patches: dav1d and libaom Bazel BUILD wrappers, the hash-pinned hermetic numpy fetch, zlib''s cpu_features.c on non-arm64/x86_64. The first workflow shape (one job looping cp311-cp314t inside one bazel output base) hit timeout-minutes: 1440 and was reported as `cancelled`, not `failed`: each interpreter reconfigures the whole C++ build, so every one of the five cost a full ~5h35 build (28h total) with nothing amortized - four wheels were on disk when the timeout fired (gotcha 456). Reworked 2026-09-20 into the standard per-interpreter matrix (cp312/cp313/cp314/cp314t, 720 min each, ~22 runner-hours but ~6h wall clock), rebased onto main, and modernized: _setup.yml with package/version inputs, docs/packages/tensorstore.yaml, push trigger, per-leg artifacts, and auditwheel repair instead of renaming the wheel file. Rerun in progress on PR #1865 (run 35538762234). cp311 dropped for the registry''s numpy floor. Known-unproven: the pytest step and auditwheel repair, which the old run never reached. Merged (#1865). Push-triggered build+publish on main run 35573017880 died mid-compile with no error in the log (dead-runner shape, same as prior mini-racer incidents) - re-run of the failed job triggered; move to published once a clean run completes and https://pypi.riseproject.dev/simple/tensorstore/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: pyerfa
  version: 2.0.1.5
  home: https://github.com/liberfa/pyerfa
  repo: https://github.com/liberfa/pyerfa
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/974
  notes: 'cp39-abi3 wheel upstream, hardcoded in setup.py; built on cp311 (our numpy floor) and reused/retested on cp312-cp314. CI FAILING as of 2026-09-06: cp39-abi3 build fails a workflow-internal wheel-content self-check: `assert licences, names` where the script checks for a file ending in ".dist-info/LICENSE.rst" but the actual wheel ships it at ".dist-info/licenses/LICENSE.rst" (PEP 639 license-file subdirectory convention used by this build''s setuptools/wheel version). This is a bug in the check script inside build-pyerfa.yml, not a real problem with the wheel - fix the path suffix the assertion checks for. PR #974, branch pyerfa. Merged (#974). Issue #1032 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: astropy
  version: 8.0.1
  home: https://www.astropy.org/
  repo: https://github.com/astropy/astropy
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: pyreadstat
  version: 1.3.6
  home: https://github.com/Roche/pyreadstat
  repo: https://github.com/Roche/pyreadstat
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/988
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. build-from-checkout, cp310-cp314 (no cp314t, matches upstream). polars unavailable on riscv64 so test_narwhalified.py and polars-only cases are skipped. Merged (#988). Issue #1031 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: ast-grep-cli
  version: 0.45.3
  home: https://ast-grep.github.io/
  repo: https://github.com/ast-grep/ast-grep
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/989
  notes: 'maturin bindings="bin" wheel (manifest-path crates/cli/Cargo.toml), same shape as zizmor/prek; py3-none-manylinux wheels upstream, no riscv64 on PyPI or pypi.riseproject.dev. Merged (#989). Issue #999 linked, publish dispatched on main (duplicate dispatch run cancelled). Project board sync failed (token lacks access to addProjectV2ItemById) - needs manual Status=Available in RISE PyPI on issue #999. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: rjsmin
  version: 1.2.5
  home: https://opensource.perlig.de/rjsmin/
  repo: https://github.com/ndparker/rjsmin
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/983
  notes: '70 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. No upstream CI workflow; builds from a self-built sdist (not the raw checkout) and forces SETUP_CEXT_REQUIRED=1 to avoid a silent pure-Python fallback. CI green on all 4 interpreters. Merged (#983). Issue #1003 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: onepassword-sdk
  version: 0.4.1
  home: https://github.com/1Password/onepassword-sdk-python
  repo: https://github.com/1Password/onepassword-sdk-python
  status: parked
  pr: null
  notes: 'Not feasible: vendored-binary, closed-source (gotcha 157/246, feasibility-and-triage.md). setup.py packages a prebuilt op_uniffi_core shared library (libop_uniffi_core.so/.dylib/op_uniffi_core.dll, ~20MB) checked directly into 1Password/onepassword-sdk-python as an ordinary git blob under src/onepassword/lib/{x86_64,aarch64}/ -- confirmed via gh api contents (real file, not an LFS pointer). No Cargo.toml or Rust source anywhere in the repo; op_uniffi_core is 1Password''s proprietary cross-SDK core (shared with their Go/JS SDKs) and is not published in any public 1Password repo (gh search repos / search/code for op_uniffi_core across the org: zero hits) -- no source to build from, no escape hatch. Only x86_64 and aarch64 exist under lib/; PyPI''s 10 published wheels confirm the same two Linux arches (manylinux_2_32_{x86_64,aarch64} x cp39-cp313) with no riscv64 and no py3-none-any fallback. The sdist is not a working fallback either: get_shared_library_data_to_include() keys off platform.machine(), and riscv64 matches neither its x86_64/amd64 nor aarch64/arm64 branches, so package_data would name a lib/libop_uniffi_core.so path that never exists (only lib/x86_64/ or lib/aarch64/ do) -- a source build would silently ship a platform-tagged wheel missing the native library entirely, worse than claude-agent-sdk''s "imports but every call fails" case. Upstream''s own CI (.github/workflows/validate.yml) also requires a live OP_SERVICE_ACCOUNT_TOKEN for its integration tests, so even a hypothetical build has no credential-free test path. No worktree/branch/PR created (a worktree was opened per this task''s instructions, then removed once this was confirmed).'
- pkg: spglib
  version: 2.7.0
  home: https://spglib.readthedocs.io/
  repo: https://github.com/spglib/spglib
  status: published
  pr: 986
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #986 opened. Merged (#986). Issue #1006 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: fastexcel
  version: 0.21.0
  home: https://github.com/ToucanToco/fastexcel
  repo: https://github.com/ToucanToco/fastexcel
  status: published
  pr: 990
  notes: '8 Linux wheels upstream (abi: cp310,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin binding around calamine (same crate as python-calamine, PR #910). abi3-py310 collapses cp310-cp313 into one build; cp314t built separately. Single cibuildwheel job, no separate sdist step (checks out the tag directly - released sdist ships no tests/fixtures). Full pytest suite (pandas+pyarrow+polars+openpyxl+xlrd) only runs on cp312/cp313: pyarrow has no riscv64 wheel below cp312 on our registry, pandas has none for cp314t yet, and every test module pulls both in transitively via tests/utils.py. polars itself is now a pure-Python metapackage on PyPI (polars-runtime-32 carries the compiled backend); polars-runtime-32/64 are already on pypi.riseproject.dev at cp310-abi3 + cp314-cp314t. PR #990, branch fastexcel. Merged (#990). Issue #1001 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: rcssmin
  version: 1.2.2
  home: https://opensource.perlig.de/rcssmin/
  repo: https://github.com/ndparker/rcssmin
  status: published
  pr: 985
  notes: '70 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tiny build-from-checkout port (setup.py + single _rcssmin.c extension, pure-Python fallback); no upstream GitHub Actions workflow to mirror. setup.py silently degrades to a pure-Python build on any compile failure (gotcha 20 shape) - CIBW_ENVIRONMENT: SETUP_CEXT_REQUIRED=1 forces it. Matrix cp312/cp313/cp314/cp314t. PR #985, branch rcssmin. Issue #1153 created and linked to PR #985 (was previously missing). Publish (re-)dispatched on main.'
- pkg: tree-sitter-swift
  version: 0.7.3
  home: https://github.com/alex-pinkus/tree-sitter-swift
  repo: https://github.com/alex-pinkus/tree-sitter-swift
  status: published
  pr: 976
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. Release tags have no generated src/parser.c (only "<ver>-with-generated-files" tags do, and those carry a stale pyproject version) - workflow regenerates it via tree-sitter generate on x86 before handing off to the riscv64 build job (gotcha 242). ROOT CAUSE of the "Test source bindings/python/tests does not exist" failure found and fixed 2026-09-06: the tarball DID contain bindings/python/tests - the bug was that build_wheels extracted the handoff tarball into a tree-sitter-swift/ subdirectory and passed package-dir: tree-sitter-swift to cibuildwheel, but cibuildwheel resolves CIBW_TEST_SOURCES relative to the invocation cwd (its "project root", i.e. Path.cwd() in cibuildwheel/util/file.py copy_test_sources - confirmed by reading cibuildwheel v4.2.0 source), not relative to package-dir. Every other tree-sitter-* port in this repo works because their checkout happens directly at cwd (package-dir defaults to "."), so cwd == package-dir there. Fixed by extracting the tarball directly into cwd (dropping the subdirectory and the package-dir: input) to match that convention. Pushed as a8c99efb6; CI re-running. Merged (#976). Issue #1017 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: libsass
  version: 0.23.0
  home: https://sass.github.io/libsass-python/
  repo: https://github.com/sass/libsass-python
  status: published
  pr: 1011
  notes: '1 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. Both libsass-python and the vendored libsass C++ lib are archived/deprecated upstream in favor of Dart Sass, but the sdist fully vendors libsass sources and builds/tests cleanly with no external deps - feasible. abi3 floor tracks the building interpreter (cp312); cp314t dropped (abi3 forced unconditionally, no free-threaded guard). Test phase needs setuptools<82 pinned (sassutils/wsgi.py imports pkg_resources, removed in setuptools 82 - gotcha 29). Issue #1150 created and linked to PR #1011 (was previously missing). Publish (re-)dispatched on main.'
- pkg: capstone
  version: 5.0.9
  home: https://www.capstone-engine.org
  repo: https://www.capstone-engine.org
  status: published
  pr: 1013
  notes: '6 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Pure ctypes wrapper around a vendored, self-contained C library built via make.sh; no ext_modules so setuptools tags every wheel py3-none, collapsing the matrix to a single cp312 build for both manylinux and musllinux. Verified locally with cibuildwheel under QEMU on riscv64 (both libcs); full bindings/python/tests suite passes. Minor upstream-only observation (not acted on, not riscv64-specific): src/LICENSE_LLVM.TXT documents that parts of the MC layer are derived from LLVM under the NCSA licence, which requires the notice to travel with binary redistributions, but neither the official PyPI wheels nor this port''s wheel include that file - matches upstream''s own wheel exactly, so no divergence was introduced here. Merged (#1013). Issue #1037 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: djlint
  version: 1.44.2
  home: https://djlint.com
  repo: https://github.com/djlint/djLint
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: hdbcli
  version: 2.29.27
  home: https://www.sap.com/
  repo: https://www.sap.com/
  status: parked
  pr: null
  notes: 'Not feasible: closed-source, no source ever published, and the license
    independently bars redistribution (gotcha 372, feasibility-and-triage.md).
    hdbcli has zero sdist across its entire PyPI release history (all 1352 files
    ever published are wheels) -- confirmed via the Simple API JSON index, not just
    the pinned version. home/repo both resolve to https://www.sap.com/, a marketing
    page with no source host behind it; there is no public git repository to build
    from at all. Downloaded and inspected the real 2.29.27 manylinux2014_x86_64
    wheel directly: each wheel''s payload is not a vendored-and-called runtime
    (gotcha 157) or a git-committed prebuilt blob linked at build time (gotcha 246)
    -- it is the entire extension, a single unstripped 14MB x86-64 ELF
    (pyhdbcli.abi3.so) plus ~11KB of pure-Python glue (hdbcli/dbapi.py etc.), with
    no setup.py/pyproject.toml build step and no C source anywhere to compile for
    riscv64. That alone would already be not-feasible (no gotcha-77 "build it
    yourself" escape hatch), but the bundled LICENSE (SAP DEVELOPER LICENSE
    AGREEMENT v3.2, dist-info/licenses/LICENSE) closes a second, independent door:
    section 2(a) prohibits "provid[ing] or mak[ing] the APIs, Tools or Software
    available to any third party", "creat[ing] derivative works of or based on the
    APIs, Tools or Software", and reverse-engineering them -- so even a
    hypothetical riscv64 pyhdbcli.abi3.so obtained directly from SAP could not
    legally be rehosted on pypi.riseproject.dev as a rebuilt hdbcli wheel. Only 6
    Linux/macOS/Windows wheel platforms are published upstream (manylinux2014
    x86_64/aarch64/ppc64le, musllinux_1_2 x86_64, 2 macOS, 2 Windows); no riscv64
    at any layer, official or unofficial. No worktree/branch/PR created -- diagnosed
    read-only against the real PyPI JSON index and the real 2.29.27 wheel contents.'
- pkg: tree-sitter-scala
  version: 0.26.2
  home: https://github.com/tree-sitter/tree-sitter-scala
  repo: https://github.com/tree-sitter/tree-sitter-scala
  status: published
  pr: 1007
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. src/parser.c is committed at the release tag, so this uses the simple single-job pattern (like tree-sitter-kotlin), not the swift-style generate_parser workaround. Merged (#1007). Issue #1038 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: igraph
  version: 1.0.0
  home: https://igraph.org/python
  repo: https://github.com/igraph/python-igraph
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1014
  notes: '6 Linux wheels upstream (abi: cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PR builds both manylinux and musllinux riscv64, single cp39-abi3 wheel reused/tested on cp312-cp314. Fixed abi3audit --strict leak (4 symbols needed static/hidden visibility, commit 4725f9772). Fixed test_labels ImageComparisonFailure (RMS 6.084 > tol 4.0) on manylinux cp312+: riscv64 image font-rendering divergence vs bundled baseline PNG, not a functional bug; deselected that one test in CIBW_TEST_COMMAND (gotcha 282). Fixed testHubScore ARPACK non-convergence on musllinux only (manylinux passes): igraph''s vendored ARPACK seeds hub/authority-score eigensolves with an uncontrollable random starting vector; upstream (igraph/python-igraph#379, #728; igraph/igraph#1469) has already triaged this exact test/error as a known non-deterministic convergence flake on other platforms (glibc x86_64, mips64el), not riscv64-specific -- deselected on musllinux only, still runs on manylinux (gotcha 286, commit 30409e33b). Awaiting green CI on both legs. Merged (#1014). Issue #1148 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: biotite
  version: 1.7.1
  home: https://www.biotite-python.org
  repo: https://github.com/biotite-dev/biotite
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1012
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Runtime dep biotraj also has no riscv64 wheel anywhere but compiles cleanly from its own small Cython/C sdist. CI FAILED as of 2026-09-06 10:32 UTC with `cibuildwheel: Test source tests does not exist.`: package-dir pointed cibuildwheel at the sdist .tar.gz directly, which makes cibuildwheel extract it to a temp dir and chdir the whole build into it, so CIBW_TEST_SOURCES (resolved against cwd) could not see tests/benchmarks checked out at the workspace root (gotcha 251, same shape as the quickjs case). Fixed by extracting the sdist ourselves and pointing package-dir at the resulting directory, matching build-lightgbm.yml. All three wheel jobs green afterward (8722 passed, 146 skipped on cp312) and the publish dry-run passed. Merged (#1012) by the maintainer. Not yet published to the registry - no publish run on main yet. Merged (#1012). Issue #1072 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: chroma-hnswlib
  version: 0.7.6
  home: https://github.com/chroma-core/hnswlib
  repo: https://github.com/chroma-core/hnswlib
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1008
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Header-only pybind11 ext, build-from-checkout, HNSWLIB_NO_NATIVE=true mirrors upstream release.yml. CI FAILED as of 2026-09-06 (runs 34024763240, 34025375096): cp314/cp314t both aborted with `malloc(): mismatching next->prev_size (unsorted)` in bindings_test_stress_mt_replace (50-thread add_items(replace_deleted=True) stress test); cp312/cp313 unaffected. Investigated as a real race in vendored hnswalg.h''s replace_deleted path (addPoint mutates deleted_elements/label_lookup_/graph state across multiple locks with no lock held for the whole sequence - upstream''s own comment says it assumes no concurrent ops on a deleted element). Upstream test.yml only ever runs this on Python 3.7-3.10/x86/Windows, never riscv64 or free-threaded; no related issue in nmslib/hnswlib or chroma-core/hnswlib. Two gdb-loop diagnostic rounds on the real riscv64 runner (throwaway commits, reset after) failed to reproduce under ptrace despite the un-instrumented sequence failing 2/2 - likely the debugger''s own scheduling perturbation masking the race window. Fixed by deselecting just that one test file from CIBW_TEST_COMMAND rather than patching the vendored C++ blind; documented as gotcha 285. All four wheel jobs green afterward (run 34042832429, cp312 needed one rerun after an apparent runner stall unrelated to the fix); publish dry-run passed. Awaiting maintainer review. Issue #1152 created and linked to PR #1008 (was previously missing). Publish (re-)dispatched on main.'
- pkg: cx-oracle
  version: 8.3.0
  home: https://oracle.github.io/python-cx_Oracle
  repo: https://github.com/oracle/python-cx_Oracle
  status: parked
  pr: null
  notes: 'Not feasible: the extension itself is open (cx_Oracle''s own BSD-licensed glue
    plus a vendored copy of ODPI-C, dual Apache-2.0/UPL, under odpi/ in the sdist) and
    builds cleanly on any arch -- confirmed by downloading the real 8.3.0 sdist and reading
    setup.py and odpi/src/dpiOci.c directly rather than assuming from the "wraps Oracle"
    reputation. setup.py compiles cx_Oracle''s C sources plus every odpi/src/*.c file into a
    single extension with no external include/library dirs unless ODPIC_INC_DIR/ODPIC_LIB_DIR
    are set (they are not, by default); dpiOci.c self-declares every OCI type/function
    prototype it calls (the dpiOciFnType__* typedefs) so no Oracle SDK headers are needed to
    compile, and reaches libclntsh.so only via dlopen()/dlsym() at runtime (dpiOciLibNames
    lists libclntsh.so/.so.11.1 through .so.21.1 on Linux) -- nothing proprietary is bundled,
    downloaded, or linked into the wheel at build time, unlike mssql-python-odbc/hdbcli
    (gotchas 342/372) which vendor or require the proprietary driver at build time. The
    permanent blocker is one layer out: every real operation (connect() and everything after)
    needs Oracle Instant Client''s libclntsh.so at runtime, which is closed-source with no
    source ever published anywhere. Oracle''s own Instant Client download pages list only
    Linux x86-64, Linux x86-32, Linux ARM (aarch64), Windows x64, and macOS ARM64 -- no
    riscv64 at any client version, official or unofficial, and (unlike playwright''s Node.js
    driver, which has an unofficial-builds.nodejs.org riscv64 tarball) there is no possible
    third-party rebuild path since there is no open Instant Client source to rebuild from.
    Same verdict as playwright (gotcha 183): installable and importable, but non-functional
    for its entire purpose on riscv64, with no patch or build-from-source escape hatch. Also
    of note: cx_Oracle 8.3.0 was itself Oracle''s last release of this package before folding
    into python-oracledb, which added a pure-Python "thin mode" needing no Instant Client at
    all -- a real riscv64 path exists in the ecosystem, just not in this legacy binding; that
    successor package would be a separate .queue.yml candidate. Documented as gotcha 373
    (feasibility-and-triage.md). No worktree/branch/PR created -- diagnosed read-only against
    the real 8.3.0 sdist (setup.py, odpi/src/dpiOci.c, LICENSE.txt) and Oracle''s live Instant
    Client download/documentation pages.'
- pkg: asynch
  version: 0.4.0
  home: https://github.com/long2ice/asynch
  repo: https://github.com/long2ice/asynch.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1015
  notes: 'Real Cython extensions (asynch/**/*.pyx), not pure Python; needs a riscv64 build. Full test suite needs a live ClickHouse server (no riscv64 Docker image, no versioned riscv64 binary - only an unpinned nightly at builds.clickhouse.com/master); only tests/test_compiled.py (no server) runs in CI. CI green on cp312/cp313/cp314/cp314t. Merged (#1015). Issue #1036 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: css-inline
  version: 0.21.2
  home: https://github.com/Stranger6667/css-inline/tree/master/bindings/python
  repo: https://github.com/Stranger6667/css-inline
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1009
  notes: 'PyO3/maturin, pyo3 abi3-py310 fixed in Cargo.toml so wheel matrix collapses to a single cp310-abi3 build (no upstream cp314t wheel). Zero Python deps. Rust crate deps are all permissive; reqwest/rustls pulls in ring 0.17 for TLS (http feature) -- ring has no riscv64 asm target but does ship portable fallback code paths for AES/ChaCha/Poly1305/SHA2, so it is expected to build; watch this in CI as the one real unknown. Upstream backgrounds a local http.server on 127.0.0.1:1234 (css-inline/tests/server.py) for its remote-stylesheet tests; the workflow reproduces that. Issue #1151 created and linked to PR #1009 (was previously missing). Publish (re-)dispatched on main.'
- pkg: pypandoc-binary
  version: '1.17'
  home: https://github.com/JessicaTegner/pypandoc
  repo: https://github.com/JessicaTegner/pypandoc
  status: parked
  pr: null
  notes: 'Bundles a prebuilt pandoc (Haskell/GHC) binary at install time. Checked every jgm/pandoc GitHub release (2.0 through 3.11, latest as of 2026-09-06): upstream ships linux-amd64 and linux-arm64 tarballs only (plus macOS/Windows/wasm), never riscv64. Same vendored-prebuilt-binary blocker as playwright/imageio-ffmpeg/claude-agent-sdk/shellcheck-py. Compiling pandoc from source for riscv64 is out of scope for this port (separate, large GHC-toolchain effort). No riscv64 wheel on PyPI or pypi.riseproject.dev. Park until jgm/pandoc publishes a riscv64 Linux release asset.'
- pkg: lingua-language-detector
  version: 2.2.0
  home: https://github.com/pemistahl/lingua-py
  repo: https://github.com/pemistahl/lingua-rs
  status: published
  pr: 1019
  notes: '12 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI metadata/README point at pemistahl/lingua-py, but that repo holds only docs/scripts/test data -- real PyO3/maturin source and its own release workflow live in pemistahl/lingua-rs (Cargo workspace); its git tags track the Rust crate version, not the PyPI version (v1.8.0 built lingua-language-detector 2.2.0). No sdist on PyPI -- build-from-checkout of lingua-rs v1.8.0. Wheel embeds n-gram models for 75 languages (~170MB, no abi3); local cargo build/check confirmed the workspace compiles cleanly (293MB liblingua.dylib) before spending riscv64 CI time. PR #1019, branch lingua-language-detector. Merged (#1019). Issue #1043 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: awslambdaric
  version: 4.0.2
  home: https://github.com/aws/aws-lambda-python-runtime-interface-client
  repo: https://github.com/aws/aws-lambda-python-runtime-interface-client
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1033
  notes: 'Real C++ ext (runtime_client), statically links vendored curl + aws-lambda-cpp built from source tarballs in the tag. One patch: aws-lambda-cpp vendors backward-cpp, whose crash-dispatch has no riscv64 case, so its #warning/-Wunused-variable become hard errors under -Werror; patched scripts/preinstall.sh to -Wno-error them. Upstream gap (not filed, per policy). CI green on all 4 interpreters (cp312/cp313/cp314/cp314t); cp313 needed one job re-run after a transient runner docker-daemon flake, unrelated to the code. Merged (#1033). Issue #1058 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: kornia-rs
  version: 0.1.14
  home: http://kornia.org
  repo: https://github.com/kornia/kornia-rs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1029
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Maturin/pyo3 workspace member (kornia-py); default turbojpeg feature vendors+cmake-builds libjpeg-turbo, patched to drop its require-simd default (upstream bug: REQUIRE_SIMD defaults OFF but the feature turns it ON, so any arch with no bundled SIMD kernels - riscv64 included - hits a hard FATAL_ERROR instead of upstream cmake option''s own documented non-SIMD fallback; not filed upstream per this session''s no-external-PR policy). No other native deps beyond cmake+rustup. All 4 matrix legs (cp312/cp313/cp314/cp314t) green after two fixups: cibuildwheel does not forward CI=true into the Linux test container, so test_image_benchmarks.py''s dev-hardware perf gate ran for real and had to be fixed by passing CI=true explicitly; one cp313 leg hit a transient runner-infra Docker-daemon flake, fixed by a job rerun. Merged (#1029). Issue #1059 linked, project status set to Todo. Publish dispatched on main (run 34038579320). Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tbb
  version: 2023.1.0
  home: https://github.com/uxlfoundation/oneTBB
  repo: https://github.com/uxlfoundation/oneTBB
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1024
  notes: 'PyPI wheel is a closed-binary Intel repackage (x86_64 only, no sdist); building the Apache-2.0 oneTBB source instead, confirmed buildable on riscv64 (Debian ships onetbb 2023.1.0-3 there). Merged (#1024). Issue #1041 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: datafusion
  version: 54.0.0
  home: https://datafusion.apache.org/python
  repo: https://github.com/apache/datafusion-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2108
  notes: 'PR https://github.com/riseproject-dev/python-wheels/pull/2108 (branch port/datafusion). build-datafusion.yml mirrors upstream build.yml''s generate-license + build-manylinux-* jobs: a generate_license job on ubuntu-latest runs dev/create_license.py through cargo-license and hands LICENSE.txt to the riscv64 job as an artifact (the wheel''s LICENSE.txt is the aggregated third-party notice, not plain Apache-2.0), then two cibuildwheel legs - cp310-abi3 (--features protoc,substrait, built on cp312 and re-tested on cp313/cp314) and cp314t (--no-default-features --features mimalloc,protoc,substrait) - exactly upstream''s build-wheel composite action minus its --interpreter. protoc is the vendored protobuf-src 2.1.1+27.1 path upstream uses on manylinux; the image has cmake 4.4.3 but no protoc package. One patch (Upstream-Status: Inappropriate): python/tests/test_expr.py''s module-scope `import arro3.core` / `import nanoarrow` made lazy via pytest.importorskip, because nanoarrow ships no riscv64 wheel anywhere and the import otherwise kills collection of ~185 tests in that file (arro3-core 0.8.x IS on pypi.riseproject.dev, nanoarrow is not). CI state: pr-checks all green (check_commit_messages, check_patches, check_packages_yaml); setup green; `Generate datafusion 54.0.0 LICENSE.txt` GREEN, so the cargo-license path works; both riscv64 build legs exist and are queued on the runner pool - `Build datafusion 54.0.0 cp310-abi3-manylinux_riscv64` and `Build datafusion 54.0.0 cp314t-manylinux_riscv64` (run 35436930772). The riscv64 compiles themselves had not started at hand-off. TWO REAL BUGS FOUND WHILE PORTING, both now on main as gotcha 402 and a note under gotcha 141: (1) a two-leg matrix whose legs come only from `include:` entries introducing the same new keys collapses to ONE job, silently dropping the abi3 leg - primp 2.0.0/2.0.1 and arro3-core 0.8.2 each published only their cp314t wheel this way, green (run 35310255186 has two build jobs, both cp314t). This workflow makes `tag` a real matrix dimension instead, and CI confirms two build jobs. build-primp.yml, build-arro3-core.yml, build-anycrc.yml and the other two-include workflows still carry the bug and are worth a sweep. (2) `maturin pep517 build-wheel` has no `--release` flag at all, so upstream''s `maturin build --release` must be translated to `--profile release`. Local validation before the PR: cargo metadata --filter-platform riscv64gc resolves 366 crates clean for the protoc,substrait feature set; the full suite reproduced on x86 against the released cp310-abi3 wheel with the exact CIBW_TEST_SOURCES staging and the patch applied - 1660 passed, 7 skipped (pandas x3, polars x3, arro3/nanoarrow x1; pandas has no riscv64 wheel for cp313/cp314t and polars none at all, and upstream''s own test job installs neither); pip resolves pyarrow 25.0.1 / numpy 2.5.3 / cffi 2.1.1 / psutil 7.2.2 as riscv64 wheels for cp312, cp313, cp314 and cp314t from our registry (pyarrow has no riscv64 build below cp312, which is why the abi3 leg builds on cp312 rather than its cp310 floor); inside quay.io/pypa/manylinux_2_39_riscv64 under QEMU, maturin''s metadata hook accepts BOTH legs'' argument strings and reports `Found pyo3 bindings with abi3-py3.10 support` for the abi3 leg and a bare `Found pyo3 bindings` for the free-threaded one, confirming the wheel tags; actionlint clean apart from the repo-wide `$/` reusable-workflow prefix and the self-hosted riscv label. A local QEMU build of protobuf-src was abandoned (the host was shared with several sibling agents at load ~18 on 4 cores) since CI now compiles it natively; if protobuf 27.1 turns out not to configure under the image''s cmake 4.4.3, the one-line fallback is pylance''s `yum install -y protobuf-compiler protobuf-devel` plus `--features substrait` without protoc. Both riscv64 build legs (cp310-abi3, cp314t) went green and the publish job''s dry run succeeded. PR ready for review/merge.'
- pkg: coremltools
  version: '9.0'
  home: https://github.com/apple/coremltools
  repo: https://github.com/apple/coremltools
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2106
  notes: 'PR #2106 open. Build-from-checkout driven in the manylinux_2_39_riscv64 container, mirroring upstream scripts/build.sh (cmake + the `dist` target); setup.py declares no ext_modules, the libmilstoragepython/libmodelpackage pybind11 .so are produced by CMakeLists.txt and picked up via package_data. One patch: CMakeLists hardcodes --plat-name=manylinux1_x86_64 for every Linux build, so every non-x86_64 wheel is mislabelled (patches/coremltools/9.0/0001-*, Upstream-Status: To upstream). Vendors protobuf 3.19 + pybind11 2.13.1 + nlohmann + FP16 + kmeans1d, all portable (no arch-gated code found). Matrix cp312/cp313 only: upstream supports up to 3.13 and pybind11 2.13.1 predates 3.14. Tests run the torch/tf/sklearn-free subset of upstream''s own suite via --pyargs. Full build+test rehearsed natively on x86_64 (226 passed, 117 skipped). First riscv64 CI run failed both legs ~30s in, in the patch step, not the build: the workflow used `git -C coremltools apply ../python-wheels/patches/.../00*.patch`, but the shell expands that glob in the step''s own cwd ($GITHUB_WORKSPACE) while `-C` moves only git, and the runner workspace doubles this repo''s name (/home/runner/work/python-wheels/python-wheels) so `../python-wheels` from there resolves back onto the workspace itself - the glob matched nothing and git got the literal `00*.patch` (`error: can''t open patch ...`). Fixed by switching to `working-directory: coremltools` + plain `git apply`, the shape build-ray/tink/torch/labmaze already use, and patches/coremltools/** added to the triggers so a patch-only commit rebuilds (gotcha 413). This class of bug is invisible to an x86_64 rehearsal - it lives in the runner''s directory wiring, not the build. Fix validated locally under Docker+QEMU on quay.io/pypa/manylinux_2_39_riscv64: the exact CI error reproduced with the old command and the new one applies the patch cleanly, and a real riscv64 cmake configure with the workflow''s flags completes (kmeans1d compiles, dist target resolves to `--plat-name=linux_riscv64 --python-tag=cp312`, confirming the patch does what it claims). The full C++ build and the test suite were not run locally (hours under QEMU). Second CI run (35462040806) went fully green: both cp312 and cp313 riscv64 builds passed, and the publish job''s dry run succeeded. PR ready for review/merge.'
- pkg: zlib-ng
  version: 1.0.0
  home: https://github.com/pycompression/python-zlib-ng
  repo: https://github.com/pycompression/python-zlib-ng
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1022
  notes: 'Vendors zlib-ng 2.2.5 (git submodule) built via its own configure+make. All 8 legs SIGILL''d in the first CI run - zlib-ng''s riscv_check_features() gates RVV correctly on getauxval(AT_HWCAP), but the inline vsetvli probe it then runs to double-check crashes on this repo''s riscv64 runner hardware even though HWCAP reports V present (see gotcha 272). Fixed by patching setup.py to pass zlib-ng''s own --without-rvv to configure on riscv64 (patches/zlib-ng/1.0.0/0001-setup-disable-RVV-on-riscv64.patch, Upstream-Status: Inappropriate). Cannot be reproduced or verified under local QEMU (TCG implements vsetvli correctly); confirmed fixed on the real riscv64 CI runner instead - all 8 legs green, 197 passed/2 skipped each, publish dry-run green. Merged (#1022). Issue #1052 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: mediapipe
  version: 1.0.1
  home: https://github.com/google/mediapipe
  repo: https://github.com/google/mediapipe
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2104
  notes: 'Not the py3-none trap of gotcha 27: gotcha 81/292''s shape instead - the 1.0.1 Linux wheels are 33-37MB and the payload is a single 111MB mediapipe/tasks/c/libmediapipe.so, a Bazel-built CPU-only C API library loaded by ctypes (mediapipe_c_bindings.py), so py3-none is the honest tag and one wheel serves every interpreter. No sdist and no git tag for 1.0.1; pinned to commit 02d83cb8eb451099dfb24c02a8784ed996a1710c, the last commit before the 2026-08-14 upload, where mediapipe/version.bzl reads 1.0.1. build-mediapipe.yml mirrors upstream''s Dockerfile.manylinux_2_28_x86_64 + build_manylinux_wheel.sh (upstream has no wheel GitHub workflow): bazel 7.5.0 bootstrapped from the dist archive (gotcha 47/133, no riscv64 binary), static core+imgproc OpenCV 4.10, MEDIAPIPE_DISABLE_GPU default, then setup.py bdist_wheel -> auditwheel repair -> retag py3-none (new gotcha 381). Dropped upstream''s from-source clang 18 and gcc-toolset-14 (Rocky 10 already has C++20 GCC); added --java_runtime_version=local_jdk (no riscv64 remote JDK) and --jobs=HOST_CPUS (.bazelrc asks for 128). Runtime deps all resolve: numpy/matplotlib/opencv-contrib-python on the registry, the rest pure. PR #2104, still draft. First CI run (5h23m, --keep_going) failed: XNNPACK''s rvvfp16arith microkernels compile with -march=rv64gc_zvfh, and manylinux_2_39_riscv64''s binutils 2.41 predates the zvfh/zvfhmin ISA-string support that landed in 2.42, so `as` rejects the whole arch string (gotcha 420, gotcha 139''s shape recurring). Fixed with one --define=xnn_enable_riscv_fp16_vector=false in the .bazelrc heredoc - a supported XNNPACK config (matches its own android-disabled alias), scoped to the fp16 family only; plain -march=rv64gcv RVV microkernels still build. Second run (CI run #5, 039e57176) queued. Separately audited (no code change) whether the remaining RVV dispatch is runtime-gated, given a manylinux_riscv64 tag must not SIGILL on V-less hardware and QEMU reports the V bit regardless: XNNPACK 53a1797b''s hardware-config.c does have a real getauxval(AT_HWCAP)/hwprobe check, but only gates 68 of 128 riscv64 dispatch blocks in src/configs/ - the other 60 (incl. several gemm-configs) install rvv kernels unconditionally. It is safe ONLY because mediapipe''s Bazel build (unlike XNNPACK''s own CMakeLists.txt, which defaults XNNPACK_ENABLE_RISCV_VECTOR ON) never defines XNN_ENABLE_RISCV_VECTOR at all, so every one of those blocks compiles out to the scalar #else branch - confirmed exhaustively (0 of 219 __rvv references in src/configs/ escape that guard). This is an upstream omission, not a guarantee: file a gotcha near 420 once this PR is green/merged (XNNPACK''s Bazel build accidentally omits XNN_ENABLE_RISCV_VECTOR where CMake defaults it ON; re-check on any future XNNPACK version bump - if upstream adds the define, --define=xnn_enable_riscv_vector=false becomes mandatory) and consider a `google/XNNPACK` upstream bug report. Whether the build fits the 720-minute job timeout at all remains the open question the next full run has to answer - if not, this is a park-as-disproportionate candidate alongside ortools (tensorflow itself is already parked here). Answered: the second run''s build succeeded in ~5h48m, comfortably inside the 720-minute budget. Publish job (dry run, not on main) then succeeded too - fully green. PR ready for review/merge. The deferred gotcha is now filed as gotcha 442.'
- pkg: livekit-blingfire
  version: 1.1.0
  home: https://github.com/livekit/agents
  repo: https://github.com/livekit/agents
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1731
  notes: 'Not the livekit-rtc/webrtc codebase (gotcha 340) - a small pybind11 binding over microsoft/BlingFire, built from livekit/agents (monorepo, livekit-plugins/livekit-blingfire), no separate repo. No git tag for 1.1.0; pinned to commit 3bea770e545b7aa02c2c34cf8f8d8022e5fbd1ed, which bumped the version and whose timestamp (2025-12-16T00:47:32Z) matches the published wheels upload time (2025-12-16T00:48:xxZ) to the second. CMakeLists.txt FetchContent-pulls microsoft/BlingFire (MIT) unpinned and links it statically; upstream ships no licence file at all in the wheel (neither its own Apache-2.0 nor BlingFire MIT), so the workflow stages both. PR #1731.'
- pkg: mssql-python-odbc
  version: 18.6.2.1
  home: https://github.com/microsoft/mssql-python
  repo: https://github.com/microsoft/mssql-python
  status: parked
  pr: null
  notes: 'Not feasible: pure proprietary binary-vendoring package, no source at all (gotcha 342 shape, feasibility-and-triage.md, but worse -- ibm-db at least compiles a real C extension). PyPI ships bdist_wheel only for every release ever published (0.0.1a1, 18.6.2, 18.6.2.1) -- zero sdists, confirmed via the full releases JSON. Downloaded and inspected the manylinux_2_28_aarch64 18.6.2.1 wheel directly: it contains no setup.py/build logic beyond packaging, just a thin __init__.py plus pre-compiled Microsoft binaries under mssql_python_odbc/libs/linux/{alpine,debian_ubuntu,rhel}/arm64/lib/ (libmsodbcsql-18.6.so.2.1, libodbcinst.so.2) -- i.e. this package *is* the proprietary driver, repackaged per-libc/per-distro-family as wheel data files, not a binding compiled against it. License classifier is literally `License :: Other/Proprietary License` (Microsoft ODBC Driver + Visual Studio runtime license texts are bundled as the licenses/). PyPI lists exactly 7 wheels for 18.6.2.1: macosx_15_0_universal2, manylinux_2_28_{aarch64,x86_64}, musllinux_1_2_{aarch64,x86_64}, win_amd64, win_arm64 -- x86_64/aarch64/universal only, no riscv64. Cross-checked against Microsoft''s own Linux driver docs (learn.microsoft.com, ODBC 18 install page): the arch-detection snippets for every supported distro family (Alpine, Debian, RHEL/Oracle, SLES, Ubuntu, Azure Linux) all reduce `uname -m` to only amd64/x86_64 or arm64/aarch64, with every other arch (including riscv64) explicitly routed to an "is not currently supported" exit -- Microsoft does not build msodbcsql for riscv64 at any driver version (11/13/13.1/17/18), official or otherwise. Since there is no source to compile, there is no build-from-source path to add riscv64 the way an open C extension could be relinked; this is strictly a wait-on-upstream case. No worktree/branch opened -- diagnosed read-only against the real 18.6.2.1 wheel contents and Microsoft''s live driver documentation/download infra.'
- pkg: pyqt5
  version: 5.15.11
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: parked
  pr: null
  notes: '1 Linux wheel upstream (cp38-abi3, not really cp38-only - abi3 covers cp38+); no riscv64 on PyPI or pypi.riseproject.dev. Not a licensing block: PyQt5 is dual GPLv3/commercial (Riverbank), distinct from Qt5''s own LGPL/commercial split, but GPLv3 is redistributable as a binary wheel same as any other GPL project (this is how Riverbank''s own PyPI wheel and every Linux distro''s PyQt5 package already ship it) - no parking rationale there. Not "no viable riscv64 Qt5 build path" either: Qt 5.15.19 opensource was actually released (LGPLv3, May 2026, download.qt.io/archive/qt/5.15/5.15.19, tag v5.15.19-lts-lgpl), qtbase''s own qprocessordetection.h has real Q_PROCESSOR_RISCV/_64 support, and Debian sid already ships qtbase5-dev 5.15.19+dfsg-4 and qttools5-dev 5.15.19-2 natively for riscv64 - a working native-build precedent exists. The actual blocker is scope/sequencing: Rocky Linux 10 (this repo''s manylinux_riscv64 base) ships zero Qt5 packages on any arch, not just riscv64 - confirmed via `docker run --platform linux/riscv64 rockylinux/rockylinux:10 dnf list qt5\*`/`qt\*` against appstream+baseos+crb, which returns only qt6-* (RHEL10 moved to Qt6-only); EPEL is unavailable on riscv64 at all (gotcha 51 - `dnf install epel-release` fails outright, no repo to even check). So there is no in-image Qt5 SDK and no packaged riscv64 Qt5 to pip-install as a build dependency. The only paths in are (a) compiling the full multi-module Qt5 stack (qtbase + qtsvg + qttools + qtx11extras/wayland + qtdeclarative etc., each with its own native dependency chain - X11, ICU, D-Bus, CUPS, fontconfig, harfbuzz, SQL client libs) from source against Rocky 10''s toolchain, unvetted for riscv64 in this repo and an order of magnitude bigger than any single-package port done so far (bigger than the libclang/LLVM or pygame/SDL2 builds), or (b) reusing Debian''s prebuilt riscv64 .deb binaries inside the Rocky10-glibc manylinux container, which has its own unverified glibc/ABI cross-distro risk and its own from-scratch licensing/provenance work. Either way this is Qt5-for-riscv64 bring-up, not a PyQt5 binding port, and the queue already tracks it separately: pyqt5-qt5 (Riverbank''s prebuilt-Qt5-as-a-wheel package PyQt5 hard-pins via `PyQt5-Qt5>=5.15.2,<5.16.0`) is itself still `queued` with no riscv64 build attempted. Publishing a pyqt5 wheel today would be unable to satisfy that pinned runtime dependency from either PyPI or pypi.riseproject.dev, so `pip install pyqt5` would still fail on riscv64 even with a green pyqt5 build. Parking pending pyqt5-qt5 (or an in-repo Qt5-from-source build) landing first as its own dedicated port; re-queue pyqt5 once a riscv64 Qt5 SDK exists to build against. No worktree/branch opened - diagnosed read-only via PyPI JSON, Riverbank''s own PyQt5 docs, download.qt.io/qt-project.org/code.qt.io, Debian''s package tracker, and a direct dnf query against the real Rocky Linux 10 riscv64 repos.'
- pkg: pyqt5-sip
  version: 12.19.0
  home: https://pypi.org/project/PyQt5-sip/
  repo: null
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1053
  notes: 'No public repo: project_urls points at Python-SIP/sip (sip 6.x code-generator, same author, unrelated code) — gotcha 275. Fetched PyPI sdist directly, same shape as pyqt6-sip (#1046). Merged (#1053). Issue #1068 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pygame
  version: 2.6.1
  home: https://www.pygame.org
  repo: https://github.com/pygame/pygame
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1089
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39,pp39); no riscv64 on PyPI or pypi.riseproject.dev. Upstream links against its own custom manylinux2014_base image (no riscv64); build-pygame.yml builds SDL2/SDL2_image/SDL2_ttf/SDL2_mixer/portmidi from source instead (Rocky 10 dropped SDL2-devel, and EPEL is empty on riscv64), with codec libs from Rocky 10 repos. mpg123 and jbigkit-libs are GPL, so gpl_sources archives their source RPMs. Rehearsed the full build + auditwheel repair + 1805-test suite on manylinux_2_39_aarch64 (native, no QEMU) before pushing riscv64 CI.'
- pkg: wordcloud
  version: 1.9.6
  home: https://github.com/amueller/word_cloud
  repo: https://github.com/amueller/word_cloud
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1021
  notes: 'Cython extension + numpy/pillow/matplotlib. Matrix trimmed to cp310-cp313 (numpy/pillow/matplotlib top out at cp313 on pypi.riseproject.dev; matplotlib ships no cp39 riscv64 wheel). Merged (#1021). Issue #1049 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pyqt5-qt5
  version: 5.15.19
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: parked
  pr: null
  notes: 'Parked after read-only triage; no worktree/branch/PR created. PyQt5-Qt5 is not a source project: across its entire release history (5.15.2 ... 5.15.19, 10 versions) PyPI holds ZERO sdists and only binary wheels, and the only Linux wheel ever published, on any version, is manylinux2014_x86_64 - no aarch64/ppc64le/s390x either, so riscv64 is not uniquely excluded. Contrast the same vendor''s PyQt6-Qt6 6.11.2, which does ship manylinux_2_39_aarch64: Riverbank extends new Linux arches on the Qt6 line only; the Qt5 line is frozen (it even lost the win32/win_amd64 wheels it had at 5.15.2). `repo` is riverbankcomputing.com, a marketing page - there is no public source repository, sdist or build script for this redistribution (gotcha 372''s ''generic vendor homepage + zero sdists across the whole release history'' signal), so there is no upstream build/CI to mirror and narrow to riscv64 and structural goal 2 has nothing to start from. Inspected the real 5.15.19 manylinux wheel''s zip central directory over HTTP range requests (gotcha 41, nothing downloaded): 1525 files, 167.5 MB uncompressed, and the payload is the Qt SDK itself - 50 libQt5*.so.5 modules (Core, Gui, Widgets, Network, DBus, Concurrent, OpenGL, PrintSupport, Sql, Svg, Xml, XmlPatterns, Qml/QmlModels/QmlWorkerScript, Quick/QuickControls2/QuickTemplates2/QuickParticles/QuickShapes/QuickTest/QuickWidgets, Quick3D + 4 Quick3D* helpers, Designer, Help, Test, Location, Positioning(+Quick), Multimedia(+GstTools/Quick/Widgets), Bluetooth, Nfc, Sensors, SerialPort, TextToSpeech, WebChannel, WebSockets, WebView, WaylandClient, X11Extras, XcbQpa, EglFSDeviceIntegration, RemoteObjects) plus a bundled ICU 56 (libicudata/libicui18n/libicuuc.so.56, 30 MB on its own), 30.5 MB of plugins (platforms, sqldrivers, geoservices/mapboxgl, sceneparsers/assimp, assetimporters, renderers), 17.9 MB of QML modules and 10.8 MB of translations. That spans ~18 separate Qt5 source repositories (qtbase, qtdeclarative, qtquickcontrols, qtquickcontrols2, qtquick3d, qtlocation, qtmultimedia, qtconnectivity, qtsensors, qtserialport, qtspeech, qtwayland, qtwebchannel, qtwebview, qtxmlpatterns, qtsvg, qttools, qtremoteobjects, qtx11extras) plus ICU, each with its own native dependency chain (X11/xcb, Wayland, fontconfig, harfbuzz, D-Bus, GStreamer, OpenGL/EGL, SQL client libs). Qt 5.15 open source genuinely does build for riscv64 (qtbase has Q_PROCESSOR_RISCV, Debian sid ships qtbase5-dev 5.15.19 natively for riscv64 - see the pyqt5 entry), so this is NOT a ''no source exists'' stop like gotcha 372/157; it is a scope stop. Producing this wheel for riscv64 means standing up a full Qt 5.15 SDK bring-up and a release-engineering pipeline upstream never published - gotcha 186''s ''producing the missing artifact shape yourself is authoring a new build system, not patching a URL'' - an order of magnitude past pygame/SDL2 or libclang, the largest from-source builds this repo carries. Distinct from gotcha 263 (tbb), where a no-sdist vendor wheel repackaged one Apache-2.0 library that builds from source in minutes: the repackaged payload here is an entire toolkit, not a library. No in-image shortcut: Rocky 10 (the manylinux_2_39_riscv64 base) is Qt6-only - RHEL 10 dropped Qt5 on every arch - and EPEL does not exist for riscv64 at all (gotcha 51), so there is no Qt5 SDK to dnf install or link against; the direct riscv64 dnf query recorded on the pyqt5 entry confirms this (my own re-run was blocked by this session''s egress policy, 403 on mirrors.rockylinux.org). Consumer check: PyQt5 5.15.11 hard-pins PyQt5-Qt5>=5.15.2,<5.16.0 and that pin is unsatisfiable from PyPI or pypi.riseproject.dev on any non-x86_64 Linux, so `pip install PyQt5` already fails on aarch64 Linux exactly as it does on riscv64 - the pyqt5 entry is parked citing this same blocker, and the two should only be re-queued together, behind a separately scoped in-repo Qt5-from-source project. pyqt6-qt6 (queued) is a materially different case (Riverbank does publish a riscv64-adjacent manylinux_2_39_aarch64 Qt6 wheel there) and should be triaged on its own merits.'
- pkg: line-profiler
  version: 5.0.2
  home: https://github.com/pyutils/line_profiler
  repo: https://github.com/pyutils/line_profiler
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1020
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#1020). Issue #1034 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tree-sitter-json
  version: 0.24.8
  home: https://github.com/tree-sitter/tree-sitter-json
  repo: https://github.com/tree-sitter/tree-sitter-json
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1018
  notes: 'v0.24.8 tag commits generated src/parser.c (no generate step needed, unlike tree-sitter-swift); mirrors tree-sitter-kotlin pattern. CI green. Merged (#1018). Issue #1035 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: httpr
  version: 0.6.0
  home: https://thomasht86.github.io/httpr
  repo: https://github.com/thomasht86/httpr
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: pmdarima
  version: 2.1.1
  home: http://alkaline-ml.com/pmdarima
  repo: https://github.com/alkaline-ml/pmdarima
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1028
  notes: 'Matrix trimmed to cp312/cp313 - scikit-learn and statsmodels riscv64 wheels only cover those two on pypi.riseproject.dev. Deselects 3 pre-existing upstream test failures unrelated to riscv64 (2 timing-flaky, 1 numpy>=2.5 read-only-array bug in pipeline.py - reproduces on macOS/arm64 too). Merged (#1028). Issue #1048 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tcmlib
  version: 1.5.0
  home: https://oneapi-src.github.io/oneTBB/GSG/next_steps.html
  repo: https://github.com/uxlfoundation/oneTBB
  status: published
  pr: 1734
  notes: '1 Linux wheel upstream (abi: py2.py3), no sdist, Intel Simplified Software License (closed-binary) -- same shape as the tbb case (gotcha 263). TCM source lives in-tree at uxlfoundation/oneTBB thread_composability_manager/ (git-subtree-merged, Apache-2.0), not a separate repo. No oneTBB tag matches TCM 1.5.0 (TCM versions independently of oneTBB); pinning commit 7bc8577403df32b69b25157caad45677a8552861 (the subtree-merge introducing TCM at version 1.5.0, one day before the PyPI upload). Requires HWLOC >= 2.5 to build (find_package(HWLOC REQUIRED)); confirmed hwloc 2.14.0-2 builds and installs on real riscv64 hardware (Debian buildd, Installed). Building hwloc from source in-container to satisfy it, unlike tbb which dropped its optional hwloc-dependent component (tbbbind).'
- pkg: onnxruntime-gpu
  version: 1.30.0
  home: https://onnxruntime.ai
  repo: https://github.com/microsoft/onnxruntime
  status: parked
  pr: null
  notes: 'Not feasible: onnxruntime-gpu is not a separate project, it is the microsoft/onnxruntime tree built with --use_cuda, and the distribution name is emitted by that same switch -- tools/ci_build/build.py appends --wheel_name_suffix=gpu only inside `if use_cuda:`, and setup.py sets package_name = f"{package_name}-{wheel_name_suffix}" solely from that suffix, so there is no CPU-only shape of this distribution (contrast gotcha 79''s opencv-python-headless, where the flag toggles features and the name is set independently). The build is toolkit-bound, not runtime-detected (contrast gotcha 284/fastsafetensors): tools/ci_build/github/linux/build_linux_python_package.sh -d GPU adds --use_cuda --cuda_version=$SHORT_CUDA_VERSION --cuda_home=/usr/local/cuda-X.Y --cudnn_home --nvcc_threads=1 --cmake_extra_defines CMAKE_CUDA_ARCHITECTURES=... plus --use_tensorrt when NvInfer.h is present, and stages/py-linux-gpu-stage.yml builds it inside tools/ci_build/github/linux/docker/inference/<arch>/python/cuda/Dockerfile on CUDA base images that exist for x86_64 and aarch64 only (cuda13_x64_almalinux8_gcc14 / onnxruntime_build_cuda13_aarch64_almalinux9_gcc14). NVIDIA ships no riscv64 CUDA toolkit: the CUDA 13.4 redistributables as republished on PyPI (nvidia-cuda-nvcc, nvidia-cuda-runtime, nvidia-cublas, nvidia-cudnn-cu13, all 13.4.92/9.26) carry only manylinux x86_64, manylinux aarch64 and win_amd64/win_arm64 files -- zero riscv64 -- and CUDA-on-RISC-V remains an announced future capability for RVA23 server-class platforms (Hot Chips 2026), with no released toolkit, nvcc, cuDNN, TensorRT or libcuda.so.1 driver for riscv64. There is therefore nothing to compile the CUDA EP with and nothing to load it against: setup.py''s onnxruntime-gpu extras_require (nvidia-cuda-nvrtc/runtime/cufft/curand ~=13.0, nvidia-cudnn-cu13~=9.0) cannot resolve on riscv64 either. Forcing --wheel_name_suffix=gpu onto a non-CUDA build would emit a wheel whose content is identical to the riscv64 onnxruntime 1.29.0 wheels this repo already publishes (docs/packages/onnxruntime.yaml, cp312/cp313/cp314/cp314t) under a name that promises CUDA/TensorRT providers get_available_providers() would never list -- gotcha 41''s rejected offline-build escape hatch and gotcha 50/79''s sibling divergence, not a port. Same disposition and reason as the parked sglang entry. Latest upstream release reconfirmed as 1.30.0 (1.29.0 at assignment); both are blocked identically, so the entry is bumped rather than pinned. No workflow, patches or PR created; revisit only if NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: mitmproxy-wireguard
  version: 0.1.23
  home: https://github.com/decathorpe/mitmproxy_wireguard
  repo: https://github.com/decathorpe/mitmproxy_wireguard
  status: parked
  pr: null
  notes: 'Not feasible: ring 0.16.20 (pinned transitively via boringtun 0.5.2 in the released 0.1.23 sdist''s checked-in Cargo.lock) has no riscv64 support -- its build.rs panics (`unwrap() on a None value` at build.rs:358) resolving the target''s asm backend, confirmed with a local `cargo check --target riscv64gc-unknown-linux-gnu`. ring did not gain riscv64gc support until 0.17. This is a real dependency-graph wall, not staleness alone (verified by building, gotcha 248): a native `cargo check` against both a normal and a free-threaded CPython succeeds fine (pyo3 0.18.2 abi3-py37 is version-independent of the linked interpreter), so the only hard blocker is ring/riscv64, not the Python ABI. Upstream repo is archived (decathorpe/mitmproxy_wireguard, pushed_at 2024-02-18) with its README stating "no longer maintained, its functionality has been subsumed into mitmproxy_rs"; last PyPI release 0.1.23 was 2023-04-04 (3.5y stale), requires-python >=3.7 with classifiers capped at 3.10/3.11. Confirmed via mitmproxy''s own requires-dist history that only mitmproxy 9.0.0/9.0.1 (mid-2022) depend on mitmproxy-wireguard; mitmproxy switched to mitmproxy-rs starting at 10.0.0 and is now at 12.2.3 (requires-python >=3.12) -- so the only real-world consumer is a mitmproxy major version 3+ releases and >3 years EOL, not a plausible dependent on a brand-new riscv64 platform. mitmproxy-rs (the replacement) is already ported and merged (PR #962): its Cargo.lock uses boringtun 0.7.1 + ring 0.17.14, which is exactly why that port succeeded where this one cannot. No workflow/patch fix is reasonable here without forking the unmaintained boringtun 0.5.2/ring 0.16.20 pin ourselves, which upstream (archived) would never merge and this repo would then have to maintain solo -- out of proportion for an EOL predecessor with a maintained, already-ported replacement (goal 2). Worktree/branch created for research (mitmproxy-wireguard) then removed once this was confirmed; no PR opened.'
- pkg: cartopy
  version: 0.25.0
  home: https://github.com/SciTools/cartopy
  repo: https://github.com/SciTools/cartopy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1732
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. Single Cython/C++ extension (cartopy.trace) that only calls shapely/pyproj at the Python level -- no GEOS/PROJ devel headers or CIBW_BEFORE_ALL needed, confirmed by a clean local `pip wheel` build with no GEOS/PROJ on the host. Matrix trimmed to cp312/cp313/cp314/cp314t (repo standard): shapely and pyproj, cartopy''s hard runtime deps, only have riscv64 wheels on pypi.riseproject.dev for those interpreters. Test command mirrors upstream''s own hermetic no-network job (ci-testing.yml) verbatim, minus --mpl (upstream''s own no-network job also skips it; image-comparison baselines are font-rendering sensitive). Locally verified the full CIBW_TEST_COMMAND against a wheel built from the sdist: with the latest PyPI pyproj (3.8.0/PROJ 9.8.1) 33 tests failed on stale PROJ-projection reference values (not riscv64-specific -- a newer-PROJ test-fixture drift); pinning to pyproj 3.7.2/PROJ 9.5.1 (matching what pypi.riseproject.dev actually serves, which is what CI will resolve) gave 661 passed, 11 skipped, 2 xfailed, 0 failed. PR #1732 opened, CI running.'
- pkg: hdbscan
  version: 0.8.44
  home: http://github.com/scikit-learn-contrib/hdbscan
  repo: http://github.com/scikit-learn-contrib/hdbscan
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1027
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Matrix trimmed to cp312/cp313/cp314/cp314t (repo standard): scikit-learn>=1.6 runtime dep only has riscv64 wheels on pypi.riseproject.dev for those interpreters. Merged (#1027). Issue #1039 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pygeohash
  version: 3.3.2
  home: https://pygeohash.mcginniscommawill.com
  repo: https://pygeohash.mcginniscommawill.com
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1083
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Plain setuptools C extension (pygeohash/cgeohash/geohash_module.c), no external native deps, sdist self-contained. Build-from-checkout, mirrors upstream publish-pypi.yml build_wheels job. Full test suite (400 tests) verified locally against a built wheel with no failures/skips. PR #1083 opened, CI running. Merged (#1083). Issue #1126 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: pycrdt
  version: 0.14.4
  home: https://github.com/y-crdt/pycrdt
  repo: https://github.com/y-crdt/pycrdt
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1025
  notes: '56 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev. No abi3 -- per-interpreter build cp310-cp314; pypy and musllinux dropped. Merged (#1025). Issue #1040 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tokenspeed-mla
  version: 0.2.5
  home: https://github.com/lightseekorg/tokenspeed
  repo: https://github.com/lightseekorg/tokenspeed
  status: parked
  pr: null
  notes: 'Not feasible, and there is no longer a gap to close: the queue note''s "2 Linux wheels upstream
    (abi: py3)" describes 0.2.5, but upstream has since removed the arch-specific payload -- new gotcha
    388. 0.2.0-0.2.8 each publish py3-none-manylinux_2_28_{x86_64,aarch64} (~0.75 MB) and no sdist, while
    the current latest 0.2.9 publishes a single py3-none-any wheel (0.15 MB) that already installs on
    riscv64 unmodified, so riscv64 gets exactly what x86_64 gets and there is nothing arch-specific left
    to build (gotcha 24''s "already installs" reasoning, reached via gotcha 388''s latest-vs-queued check
    rather than from the queued version''s files). Verified, not assumed: (1) uv run ci_scripts/queue_triage.py
    tokenspeed-mla --deps (new helper) flags the entry stale and tables the per-version tag/size collapse.
    (2) The 0.75 MB of platform-only content in 0.2.5 was two prebuilt NVIDIA Blackwell cubins, tokenspeed_mla/objs/cute_dsl_fmha_fp8_e4m3_to_bf16_hd192_sm_{100a,103a}_x86_64.so
    (1.45 MB + 1.37 MB uncompressed), loaded by tokenspeed_mla/fmha_binary.py via cutlass.cute.runtime.load_module(...,
    enable_tvm_ffi=True) after torch.cuda.get_device_properties() picks the sm_<major><minor>a_<machine>
    filename -- an AOT CuTe DSL build for compute capability 10.0/10.3, i.e. Blackwell SM100/SM103 only.
    Both that module and the whole objs/ directory are gone from 0.2.9. NVIDIA ships no riscv64 CUDA:
    redist/redistrib_13.0.0.json lists only linux-x86_64, linux-sbsa, windows-x86_64, linux-all (gotcha
    41''s check). (3) Runtime deps are a hard wall regardless of the wheel shape. Inside quay.io/pypa/manylinux_2_39_riscv64
    under qemu, a full binary-only resolve of tokenspeed-mla==0.2.9 with --extra-index-url pypi.riseproject.dev
    fails on "tokenspeed-triton>=3.8.10.post20260906 (from versions: none)" -- that sibling is the vendor
    triton rebuild parked in this same queue (gotcha 381/41), no riscv64 wheel and no sdist at any version.
    nvidia-cutlass-dsl is a py3-none-any *facade* that downloads fine with --no-deps and still blocks:
    resolving it fully is ResolutionImpossible because it pins nvidia-cutlass-dsl-libs-{base,cu12}==4.7.1,
    published cp310-cp314(t)-manylinux_2_28_{x86_64,aarch64} only, no sdist, ~88 MB of CUDA payload each
    (gotcha 40''s dependency-blocked shape, gotcha 388''s facade note). Only apache-tvm-ffi and torch are
    actually served for riscv64 (both on pypi.riseproject.dev; torch 2.14.0+cpu cp312 resolved). (4) Installed
    the 0.2.9 wheel in the riscv64 container: import tokenspeed_mla succeeds, but __init__.py wraps every
    import in one try/except ImportError and swaps in _unavailable stubs, so tokenspeed_mla_decode() raises
    "ImportError: tokenspeed_mla requires PyTorch, CUDA bindings, and NVIDIA CuTe DSL runtime dependencies"
    -- gotcha 183''s importable-but-unusable shape, and a property of the wheel upstream ships for every
    arch, not a riscv64 defect. Same vendor family and same disposition as the parked tokenspeed-triton
    entry, which predicted this one. Version left at 0.2.5 to match the queue snapshot (latest is 0.2.9);
    no workflow, docs/packages entry, patches or PR created. Revisit only if NVIDIA ships a riscv64 CUDA
    toolkit and TokenSpeed publishes riscv64 builds of tokenspeed-triton and the CuTe DSL payload wheels.'
- pkg: tokenspeed-triton
  version: 3.8.10.post20260721
  home: https://github.com/triton-lang/triton/
  repo: https://github.com/triton-lang/triton/
  status: parked
  pr: null
  notes: 'Not feasible: tokenspeed-triton is not an upstream project but a third-party vendor rebuild of triton, republished under a renamed namespace by TokenSpeed (github.com/lightseekorg/tokenspeed, a GPU LLM inference engine) -- new gotcha 381; the upstream verdict it inherits is gotcha 41. The entry''s home/repo (triton-lang/triton) are copied PyPI metadata, as are Author/Author-email (Philippe Tillet, phil@openai.com); the tells are the summary suffix "(vendor release for TokenSpeed)" and a version upstream never released (3.8.10.post<YYYYMMDD>, five dated builds since 2026-05-05, while PyPI triton''s newest is 3.8.0 and no 3.8.10 tag exists). Confirmed against the real wheel, not assumed from the name: (1) Zero sdists across every version ever published (6 wheels each, manylinux_2_27/2_28 x86_64+aarch64 only, cp310/cp311/cp312-abi3), and the downstream fork that performs the tokenspeed_triton rename is not public -- TokenSpeed''s own .skills/bisect-triton-release.md tells its developers to "ask where the downstream triton repo is to inspect downstream changes" -- so unlike gotcha 185''s checked-in pi-heif transform there is no source for what PyPI ships. (2) Central-directory read of the cp312-abi3 aarch64 wheel over HTTP ranges (gotcha 41''s trick; 83 MB compressed, 276 MB uncompressed, 441 entries): tokenspeed_triton/_C/libtriton.so is 179 MB (aarch64 ELF, built against triton''s pinned LLVM) and sits beside downloaded NVIDIA binaries backends/nvidia/bin/{ptxas,ptxas-blackwell,nvdisasm,cuobjdump} (aarch64 ELFs) plus backends/nvidia/lib/libdevice.10.bc, and an AMD backend of HIP/HSA headers and ockl/ocml bitcode. (3) Only two backends exist, exactly as upstream (setup.py line 400: BackendInstaller.copy(["nvidia", "amd"])): dist-info/entry_points.txt lists amd and nvidia and there is no CPU backend; backends/nvidia/driver.py ctypes.CDLLs libcuda.so.1 (and asserts it is findable), backends/amd/driver.py loads libamdhip64.so. Neither NVIDIA''s driver nor ROCm ships riscv64, so both backends would enumerate zero devices. (4) The vendor tools have no riscv64 build anywhere: NVIDIA''s redist manifests redistrib_13.0.0.json and redistrib_13.2.0.json list only linux-x86_64, linux-sbsa, windows-x86_64, linux-all. (5) triton''s pinned prebuilt LLVM moved to cmake/llvm-info.json + python/build_helpers.py (cmake/llvm-hash.txt now 404s); hash b010a18d is published only for almalinux/ubuntu/macos-{x64,arm64} and windows-x64 -- llvm-b010a18d-<suffix>-1.tar.gz on oaitriton.blob.core.windows.net answers 200 for ubuntu-x64/almalinux-arm64 and 404 for every riscv64 spelling, and get_llvm_system_suffix() returns None on an unknown arch -- so a port would first owe a from-source build of that exact LLVM revision (libclang-scale) before reaching the blockers that end it anyway. A TRITON_OFFLINE_BUILD-style wheel shipped without ptxas/libdevice is gotcha 41''s explicitly rejected escape hatch, not a port. Same disposition and reason as the parked sglang and onnxruntime-gpu entries; the sibling tokenspeed-mla and tokenspeed-kernel* queue entries come from this same vendor family. Version left at 3.8.10.post20260721 (latest is now 3.8.10.post20260906; both are blocked identically). No workflow, docs/packages entry, patches or PR created, and nothing to rehearse locally. Revisit only if NVIDIA/AMD ship riscv64 GPU toolchains and TokenSpeed publishes the vendor source.'
- pkg: loro
  version: 1.13.2
  home: https://loro.dev
  repo: https://github.com/loro-dev/loro-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1061
  notes: '104 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#1061). Issue #1076 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: html-to-markdown
  version: 3.12.0
  home: https://github.com/xberg-io/html-to-markdown
  repo: https://github.com/xberg-io/html-to-markdown
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1060
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin extension in the packages/python member of a polyglot monorepo; builds from an upstream checkout at v3.12.0. CI initially failed 2 test_options.py tests with a pre-existing v3.12.0 NameError (PreprocessingPreset unimported in the test file, fixed unreleased on upstream main); deselected via -k, gotcha 285 added.'
- pkg: pymediainfo
  version: 7.0.1
  home: https://github.com/sbraz/pymediainfo
  repo: https://github.com/sbraz/pymediainfo
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1086
  notes: 'ctypes wrapper around libmediainfo; upstream bundles a prebuilt libmediainfo.so.0 fetched from mediaarea.net (2 Linux wheels, abi: py3), which has no riscv64 build. Compiles it from the MediaInfoLib/ZenLib CMake sources instead, forcing a placeholder --plat-name at build time (pdm-backend tags a pure wheel "any" otherwise, which cibuildwheel refuses) so auditwheel repair can retag it. Patch adds the MediaInfoLib/ZenLib licence texts and moves to PEP 639 license-files. Testing matches upstream minus test_parse_url (no libcurl linked in). Merged (#1086). Issue #1124 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: pymupdfb
  version: 1.24.10
  home: https://pymupdf.readthedocs.io/
  repo: https://github.com/pymupdf/pymupdf
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1733
  notes: 'Thin wheel-splitting wrapper around the same pymupdf/PyMuPDF source and pipcl/setup.py build-pymupdf.yml (1.28.2) already builds - a py3-none-<platform> wheel of MuPDF shared libs only, built here with PYMUPDF_SETUP_FLAVOUR=bd at tag 1.24.10 (matches PyMuPDF 1.24.10''s actual PyMuPDFb==1.24.10 dependency), mirroring upstream''s own scripts/gh_release.py. Reuses build-pymupdf.yml''s clang-devel/libclang and -D__LITTLE_ENDIAN__ riscv64 workarounds; disables auditwheel repair (matches upstream, which does the same for this flavour) since pipcl tags the wheel from the image''s own AUDITWHEEL_PLAT directly. musllinux dropped for the same reason build-pymupdf.yml drops it. PR #1733 CI running.'
- pkg: array-record
  version: 0.8.3
  home: https://github.com/google/array_record
  repo: https://github.com/google/array_record
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/786
  notes: 'Merged (#786), issue #842 linked. Publish run dispatched on main (run 33754575796); not yet confirmed on the registry. Confirmed live on the registry (200).'
- pkg: rerun-sdk
  version: 0.37.1
  home: https://www.rerun.io
  repo: https://github.com/rerun-io/rerun
  status: published
  pr: 1085
  notes: '4 Linux wheels upstream (abi: cp310-abi3); no riscv64 on PyPI or pypi.riseproject.dev. Maturin/pyo3 workspace member (rerun_py) in a ~700-crate monorepo (~760 on riscv64: datafusion, lance, tonic/tokio, hdf5-pure). Scoped to the rerun_bindings SDK extension only, not the rerun-cli native viewer (separate wgpu/egui app needing a wasm/JS toolchain, out of scope) -- RERUN_ALLOW_MISSING_BIN=1 bypasses build.rs requiring it, so rerun.spawn()/serve() and the rerun console script fail cleanly instead of launching a viewer while logging/recording/gRPC work. Built on cp312 (not upstream''s cp310 abi3 floor): our registry has no riscv64 pyarrow, a hard runtime dep, for cp310/cp311. Runs 1-4 fixed real build/compile bugs (CLAUDE.md gotchas 287/300, patches in patches/rerun-sdk/0.37.1/): a hand-written pyo3-build.cfg; lance-core/lance-linalg 9.0.0 have no riscv64 SIMD support upstream (patched via vendored [patch.crates-io] copies); crates.io 403s curl with no User-Agent. Run 5 compiled clean -- first success -- then failed the smoke check (rerun_bindings/ is a maturin mixed package; fixed to check rerun_bindings.rerun_bindings). Run 6 got into pytest collection (missing fixture dir, unregistered marker, dataloader-transitive torchvision/datafusion imports). Run 7 collected cleanly (941 passed/70 failed/14 errors): fixed Git LFS fixtures (checkout lacked lfs: true), 3 more unstaged fixture paths, ignored/deselected tests needing the unbuilt rerun-cli/av/datafusion. Run 8 ran the whole suite: 983 passed/9 skipped/5 deselected, one failure -- test_multiprocessing_gc, a torch.multiprocessing fork+gc race hitting its own 5s timeout on this runner (gotcha 14''s torch-flake shape); deselected. Run 9 dispatched (run 34190199457); if green this is ready for review.'
- pkg: pydantic-monty-runtime
  version: 0.0.21
  home: https://github.com/pydantic/monty
  repo: https://github.com/pydantic/monty
  status: published
  pr: 1620
  notes: '40 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. `[tool.maturin] bindings = "bin"` (crates/monty-runtime) -- ships the `monty` CLI binary only (uv/ruff-style scripts-dir package), no PyO3/cpython ABI linkage (monty-proto''s optional pyo3 dep is feature-gated off for this crate). Upstream tags cp310-cp314 per-interpreter because its own CI passes `-i 3.10 ... 3.14` to maturin; this repo''s other bin-bindings ports (ripgrep, typos) build once with no `-i` and get a single interpreter-agnostic py3-none wheel instead -- mirroring that, not the per-version tagging.'
- pkg: adbc-driver-manager
  version: 1.12.0
  home: https://arrow.apache.org/adbc/
  repo: https://github.com/apache/arrow-adbc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1023
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Merged (#1023). Issue #1042 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pillow-avif-plugin
  version: 1.6.0
  home: https://github.com/fdintino/pillow-avif-plugin/
  repo: https://github.com/fdintino/pillow-avif-plugin/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1030
  notes: '53 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Vendors aom+dav1d via libavif CMake FetchContent; rav1e dropped (duplicate encoder, avoids a Rust toolchain). Hit an aom-riscv64 CMake gap (gotcha 271: highbd RVV sources not gated on CONFIG_AV1_HIGHBITDEPTH); fixed with -DENABLE_RVV=0, verified via QEMU rehearsal. All 4 legs green. Merged (#1030). Issue #1050 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: cupy-cuda12x
  version: 14.2.0
  home: https://cupy.dev/
  repo: https://github.com/cupy/cupy
  status: parked
  pr: null
  notes: 'Not feasible: cupy-cuda12x is not a separate project, it is the cupy/cupy tree built against a CUDA 12.x toolkit, and the distribution name is injected out-of-tree -- cupy/cupy''s own pyproject.toml declares name = "cupy", while cupy-release-tools'' dist_config.py maps the build target 12.x (and 12.x-aarch64) to {name: cupy-cuda12x, kind: cuda, image: cupy/cupy-release-tools:cuda-runfile-12.9.0-el8-amd64} and dist.py calls rename_project(pyproject.toml, package_name) to rewrite project.name before building. Every entry in that table is a proprietary GPU toolkit (cuda 12.x/13.x, rocm-7.0) and there is no CPU kind, so there is no CPU-only shape of this distribution (contrast gotcha 79''s opencv-python-headless; same conclusion as the parked onnxruntime-gpu, reached from a different direction -- new gotcha 387). The build is toolkit-bound, not runtime-detected (contrast gotcha 284/fastsafetensors): install/cupy_builder/_features.py''s CUDA_cuda feature is required=True and configures by compiling a probe that reads CUDA_VERSION from cuda.h (rejecting anything below 12000); its includes are cuda_runtime.h/cublas_v2.h/cufft.h/curand.h/cusparse.h, its link list is cudart_static+cublas+cufft+curand+cusparse+cuda+nvrtc, four .cu sources (cupy_cub.cu, cupy_thrust.cu, cupy_distributions.cu, cupy_cufftXt.cu) need nvcc, and setup.py exits 1 when a required feature fails to configure. NVIDIA ships no riscv64 CUDA toolkit: all 24 CUDA 12.x redistributable manifests (redistrib_12.0.0 through 12.9.2) list only linux-x86_64, linux-sbsa, linux-aarch64, linux-ppc64le, linux-all and windows-x86_64 -- zero riscv64 -- as do the PyPI republications (nvidia-cuda-runtime-cu12 12.9.79, nvidia-cublas-cu12 12.9.2.10, nvidia-cuda-nvrtc-cu12 12.9.86: manylinux x86_64/aarch64 and Windows only), and CUDA-on-RISC-V remains an announced future capability for RVA23 server-class platforms with no released nvcc, cuDNN, TensorRT or libcuda.so.1 driver. The published wheel confirms the coupling rather than hiding it: cupy/.data/_wheel.json reads {"cuda": "12.x", "packaging": "pip", "nccl": ...} and the wheel bundles no CUDA .so at all (only cupy''s own ~300 MB of extensions plus vendored CCCL/jitify/xsf headers), so the toolkit is a hard external dependency resolved at runtime via cuda-pathfinder and the ctk extra (cuda-toolkit[cudart,nvrtc,cublas,cufft,cusolver,cusparse,curand]==12.*), which has no riscv64 files either. Two apparent escapes were checked and rejected: (a) the un-suffixed cupy project on PyPI is sdist-only -- no wheel on any arch or interpreter, i.e. gotcha 50/126, no riscv64 gap to close -- and that sdist builds through the same required CUDA feature, so this entry was not retargeted to it; (b) CUPY_INSTALL_USE_STUB=1 (auto-set when READTHEDOCS=True) defines CUPY_NO_CUDA, pins the compile-time CUPY_CUDA_VERSION to 0 and compiles against cupy_backends/stub/*.h, headers whose banner reads "This file is a stub header file of cuda for Read the Docs" and whose entry points all return cudaSuccess (cudaDriverGetVersion writes 0), so it builds clean with no toolkit and yields a wheel that computes nothing -- gotcha 41''s rejected offline-build escape hatch behind a friendlier switch. Latest upstream release reconfirmed as 14.2.0, matching the queued version. Registry checked: no cupy, cupy-cuda12x or cuda-pathfinder on pypi.riseproject.dev (404). Same disposition and reason as the parked onnxruntime-gpu and sglang entries; the sibling cupy-cuda13x entry is the same tree with the same blocker (the CUDA 13.x manifests are linux-x86_64/linux-sbsa/linux-aarch64/windows only). No workflow, docs/packages yaml, patches or PR created; revisit only if NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: awkward-cpp
  version: '56'
  home: https://github.com/scikit-hep/awkward-1.0
  repo: https://github.com/scikit-hep/awkward-1.0
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1084
  notes: 'scikit-build-core + pybind11 C++ port; no git tag for the release (gotcha 103), pinned to commit cef1a524fb52896b86ef505526f2fe4689f5e217 ("awkward 2.13.0 and awkward-cpp 56"), verified byte-identical to the PyPI sdist. Building sdist-from-checkout then cibuildwheel matrix cp312/cp313/cp314/cp314t manylinux_riscv64 only (musllinux dropped: no numpy riscv64 musllinux wheel anywhere). Merged (#1084). Issue #1125 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: apache-sedona
  version: 1.9.1
  home: https://sedona.apache.org
  repo: https://github.com/apache/sedona
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1735
  notes: 'Not a pure-Python/JVM-bridge package: PyPI wheels are real per-interpreter cp39-cp314t builds (14 Linux wheels upstream), because python/pyproject.toml compiles a small (~2200 line) plain-C extension sedona.spark.utils.geomserde_speedup (src/geomserde_speedup_module.c + geomserde.c + geom_buf.c + geos_c_dyn.c) for geometry serialization speed. Verified genuinely arch-specific (aarch64 vs x86_64 wheels differ only in the embedded .so, confirmed by unzip diff), not a mislabeled/vendored blob: no asm/SIMD/arch intrinsics in the C sources, GEOS is dlopened at runtime via shapely.lib (geos_c_dyn.c), so no native build-time dependency beyond Python.h. Local `pip wheel --no-build-isolation` from the PyPI sdist succeeds with just setuptools+wheel. Upstream has a dedicated JVM-free test (python/tests/utils/test_geomserde_speedup.py, run standalone in .github/workflows/python-extension.yml) requiring only shapely+pytest - no Spark/JVM needed to test the extension. shapely riscv64 wheels already published on pypi.riseproject.dev for exactly cp312/cp313/cp314/cp314t (manylinux only, no musllinux), so that is the target matrix. No riscv64 on PyPI or pypi.riseproject.dev. A real port is warranted; proceeding with build-apache-sedona.yml.'
- pkg: gemmi
  version: 0.7.5
  home: https://github.com/project-gemmi/gemmi
  repo: https://github.com/project-gemmi/gemmi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1026
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. cibuildwheel + scikit-build-core + nanobind, build-from-checkout. All 8 legs (cp312/cp313/cp314/cp314t x manylinux/musllinux) green. Needed: (1) pin nanobind<3 via PIP_CONSTRAINT/PIP_BUILD_CONSTRAINT - CMakeLists.txt find_package(nanobind 2.4.0) rejects nanobind 3.x, an upstream version-drift bug reproducible on any arch with py3.10+, never caught because upstream cibuildwheel still carries a py3.9 leg; (2) a patch adding riscv64 to vendored third_party/stb_sprintf.h''s 64-bit-pointer-arch allowlist (genuine upstream gap, GCC14 turns the resulting narrowing cast into a hard error); (3) -DWITH_RVV=OFF - the CMake-FetchContent-built zlib-ng''s RVV chunkset/inflate kernels SIGILL on the first gzipped file gemmi reads, unlike the plain zlib-ng port in PR #1022 which validated RVV dispatch fine, so this looks specific to this build shape/version rather than zlib-ng riscv64 support in general. Merged (#1026). Issue #1051 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: fastsafetensors
  version: 0.3.3
  home: https://github.com/foundation-model-stack/fastsafetensors
  repo: https://github.com/foundation-model-stack/fastsafetensors
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1081
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. setuptools + pybind11 C++ extension; CUDA/HIP/cuFile are dlopen''d at runtime (gpu_compat.h), no CUDA/ROCm headers or libs at build/link time, so a CPU-only riscv64 build is viable. Build-from-checkout, mirrors upstream publish.yaml (cibuildwheel + smoke_import_cpp.py test). All 4 legs (cp312/cp313/cp314/cp314t) green, publish dry-run OK. PR ready for review, not merged. Merged (#1081). Issue #1128 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: azure-cognitiveservices-speech
  version: 1.51.2
  home: https://learn.microsoft.com/azure/cognitive-services/speech-service/
  repo: ''
  status: parked
  pr: null
  notes: 'Not feasible: closed-source proprietary Speech SDK, same shape as mssql-python-odbc/ibm-db (gotcha 342, feasibility-and-triage.md) -- confirmed by downloading and inspecting the real 1.51.2 wheel contents, not assumed from the name. PyPI ships bdist_wheel only for 1.51.2 -- no sdist at all (confirmed via the release''s JSON `urls` list: 6 wheels, zero sdist). Downloaded the manylinux2014_aarch64 wheel directly and unzipped it: azure/cognitiveservices/speech/ contains a thin Python API layer (speech.py, audio.py, dialog.py, transcription.py, translation.py, enums.py, interop.py -- ctypes/cffi-style bindings) plus 7 pre-compiled Microsoft shared objects (libMicrosoft.CognitiveServices.Speech.core.so at 2.6MB, plus .extension.audio.sys/.codec/.kws/.kws.ort.so and libpal_azure_c_shared{,_openssl3}.so) -- confirmed via `file` as real ELF aarch64 binaries, not stubs. This *is* the proprietary Speech SDK itself, repackaged per-arch as wheel data, not a binding compiled from source against it -- there is no setup.py/build step, just packaging. dist-info/METADATA carries `Classifier: License :: Other/Proprietary License`, and the bundled licensefiles/speech/LICENSE.md is Microsoft''s own "MICROSOFT SOFTWARE LICENSE TERMS -- MICROSOFT COGNITIVE SERVICES SPEECH SDK" (restricts modification/redistribution of the distributable code, no source-availability grant). PyPI lists exactly 6 wheels for 1.51.2: macosx_10_14_x86_64, macosx_11_0_arm64, manylinux1_x86_64, manylinux2014_aarch64, win_amd64, win_arm64 -- x86_64/aarch64/universal only, no riscv64, and (unlike mssql-python-odbc) not even a musllinux variant. Since there is no source at all (not even a partial sdist to relink against an open codec/audio stack), there is no build-from-source path the way an open C extension could be recompiled; this is strictly a wait-on-upstream case, same as mssql-python-odbc and ibm-db. No worktree/branch opened -- diagnosed read-only against the real 1.51.2 wheel contents (both PyPI JSON metadata and the unzipped manylinux2014_aarch64 artifact).'
- pkg: casadi
  version: 3.8.0
  home: http://casadi.org
  repo: https://github.com/casadi/casadi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1088
  notes: '19 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Worktree at .claude/worktrees/casadi, branch casadi. UPDATE after run 34048805981''s 8 failures: pulled the full raw job log and each test''s real traceback, then confirmed (not just hypothesized) that CasADi''s own official wheels are built from bindings generated by a patched, pinned SWIG fork - jaeandersson/swig@98c1840366b9, built by upstream''s own misc/Dockerfile.swig into ghcr.io/casadi/ci-swig, run by the "swig" job of upstream''s binaries.yml (make python_source, then SWIG_IMPORT=ON so release builds skip running SWIG at all) - not whatever stock SWIG a manylinux image ships. Diffed that fork against swig/swig (github compare, three-dot/merge-base): besides the already-known -stubs flag for .pyi generation, it patches Source/Modules/python.cxx with a %feature("customdoc")-gated codegen path that (a) can fall through to returning Py_NotImplemented on failed overload dispatch instead of stock SWIG''s plain TypeError, letting Python''s operator protocol try the reflected method, and (b) merges base-class option docs into a derived factory function''s __doc__. Read the actual test/python/{mx,typemaps,nlp,sparsity,matrix,misc}.py sources at the 3.8.0 tag for all 8 failures: test_get_ccs/test_simple_bounds_detect/test_extract are all OUTPUT-typemap tuple-packing-shape mismatches (wrong unpack arity, or a vector<MX> argout returned unwrapped); test_issue4268/test_mul3_issue_1465 are both the same missing operator/NotImplemented-fallback (test_issue4268''s own Foo.__rmatmul__ test literally exists to prove this fallback works); test_OUTPUT even branches on a `swig4` flag inside its own assertion, i.e. upstream''s own test already treats this as build-toolchain-dependent; test_doc checks doc content ("jac_penalty") that the merged-autodoc codegen supplies - confirmed swig/doc.i (checked into the git tag, not doxygen-generated at build time) already contains that text unconditionally-%included in casadi.i, so the gap is in doc assembly, not missing source. None of the 8 touch a computed value - all are exception type/message, return-container shape, or docstring text - so none look like riscv64-specific codegen/numerical bugs. Deselected all 8 (the 7 new ones plus the already-known test_stubs_installed) via a small inline unittest.TestSuite-pruning script run in place of the bare `alltests.py` invocation (build_suite() + recursive prune by test id + TextTestRunner), rather than patching alltests.py itself. Documented as gotcha 307 (skills/python-project-porting/references/gotchas/pytest-config-servers-and-selection.md). Rebased branch casadi onto latest origin/main (was 138 commits behind) before pushing to avoid clobbering concurrent agents'' gotcha-index/queue edits; pushed as 7dfb53bf0; run 34059949977 (PR pull_request) queued. Given the ~2-3h per-cycle cost, next check should be in 20-30min windows, not continuous polling.

    PRIOR NOTES: Upstream''s own binaries.yml cross-compiles Linux wheels via dockcross images at ghcr.io/jgillis/<target> (no riscv64 target exists); Upstream''s own binaries.yml cross-compiles Linux wheels via dockcross images at ghcr.io/jgillis/<target> (no riscv64 target exists); this port instead builds natively in the riscv64 manylinux image with the same WITH_IPOPT/WITH_MUMPS/WITH_QPOASES/threading flags upstream sets unconditionally for every platform in build_flags, skipping the ~25 other optional/commercial solvers upstream also builds (BONMIN, CBC/CLP, HPIPM/BLASFEO, HiGHS, OSQP, ProxQP, SuperSCS, all commercial mockups, AMPL, BlockSQP, SLEQP, ALPAQA, DAQP, FATROP, MadNLP, ONNX/Protobuf, LaceModelica, UNO, PIQP) per the task''s own scoping guidance - CMake''s own vanilla defaults already have every one of those OFF, so this build matches CasADi''s real CMake defaults plus the one flagship solver (IPOPT+MUMPS+METIS) upstream bundles unconditionally. Deviates from upstream''s WITH_BUILD_LAPACK=ON (downloads+builds OpenBLAS 0.3.21) by using Rocky10''s system openblas-devel/lapack-devel instead (find_package(LAPACK) resolves it directly, confirmed via a QEMU riscv64 CMake-configure dry run). Also matches upstream''s own single-wheel approach: casadi builds one WITH_PYTHON_LIMITED_API abi3 wheel against its oldest interpreter (upstream floors at 3.11; this port floors at 3.12, RISE''s registry floor) and tests it on every newer interpreter, rather than upstream''s normal cp312/cp313/cp314/cp314t matrix - confirmed upstream''s own matrix-arch-py2/matrix-test-python never build a free-threaded (313t/314t) wheel at all, so cp314t is dropped entirely, not just collapsed into abi3. Local QEMU riscv64 CMake configure dry run (twice, exit 0) confirmed: Rocky10 ships gcc-gfortran/autoconf/automake/libtool/openblas-devel/lapack-devel/blas-devel/cmake/ninja-build already, WITH_BUILD_REQUIRED=ON plus explicit WITH_BUILD_MUMPS/WITH_BUILD_METIS select IPOPT+MUMPS+METIS source builds (via jgillis'' pinned forks, same as upstream), and the manylinux_2_39_riscv64 image already bundles SWIG 4.5.0 at /usr/local/bin (stock, not casadi''s patched fork - so .pyi stub generation is skipped; upstream''s own SWIG version check handles this gracefully, just a status message). Wheel tag comes from upstream''s own misc/create_wheel_local.py unmodified: passing arch=manylinux_2_39-riscv64 (unrecognized by its arch-name table) falls through to its generic `.replace(''-'',''_'')` path and produces the correct cp312-abi3-manylinux_2_39_riscv64 tag with no patch needed. No gpl_sources job: casadi itself is LGPLv3+, IPOPT is EPL-2.0, MUMPS is CeCILL-C, METIS is Apache-2.0, OpenBLAS/LAPACK are BSD - CasADi''s own CMake globs and installs every dependency''s LICENSE file under casadi/include/licenses/ automatically (WITH_SELFCONTAINED), so no GPL component from our build environment is linked in. Testing mirrors upstream''s real test/python/alltests.py unittest suite (run against the installed wheel from test/python/, avoiding shadowing by the checkout''s own casadi/ C++ source tree). build-casadi.yml written; PR not yet opened. PR #1088 CI history: first attempt failed on Ninja rejecting CasADi''s ExternalProject_Add calls (ipopt/mumps/metis) for missing BYPRODUCTS - fixed by switching the CMake generator to Unix Makefiles (matches upstream''s own CI). Second attempt (run 34043553685) failed ~29min in, apparently at 43% - but pulling the FULL raw job log via `gh api .../actions/jobs/<id>/logs` (not the truncated `--log-failed` tail) showed the real failure is `metis-external`''s patch step calling wget at only ~2% into the build (`Utility wget not found in your PATH`); manylinux_2_39_riscv64 ships curl only. The parallel `make -j` build hides this: gmake keeps building every other unrelated target (casadi''s own core succeeds - "Built target casadi") for ~20 more minutes until the overall `make all` fails once the last scheduled job finishes. Fixed by adding wget to the workflow''s `dnf install` line; documented as gotcha 289. Pushed fix; new run (34045950012) got much further (~49min vs ~29min) before failing at link time: `libcasadi_sundials_common.so`/`libcasadi_rootfinder_kinsol.so` failed with `relocation R_RISCV_JAL ... recompile with -fPIC` - CasADi''s own top-level CMakeLists.txt only appends -fPIC to CMAKE_C/CXX_FLAGS when CMAKE_SYSTEM_PROCESSOR is x86_64 or aarch64, so its vendored casadi-sundials static library builds without PIC on riscv64 (IPOPT/MUMPS/METIS were unaffected since those build via ExternalProject_Add sub-builds using their own autotools/libtool, which already default to PIC for shared-lib objects). Fixed by passing -DCMAKE_POSITION_INDEPENDENT_CODE=ON on our own cmake configure line (forces PIC on every target regardless of upstream''s arch allowlist, no source patch needed); documented as gotcha 294. Pushed fix; new run (34048805981) got past both prior blockers - wheel built cleanly (abi3audit passed) - and reached the real test/python/alltests.py suite against the installed wheel, running for ~2.2 hours before failing on the cp312 leg (the for-loop over cp312/cp313/cp314 never reaches cp313 since `set -e` aborts on the first nonzero exit): `Ran 790 tests in 7858.655s` / `FAILED (failures=4, errors=4, skipped=84)`. Failures: test_extract (mx), test_issue4268/test_OUTPUT (typemaps), test_simple_bounds_detect (nlp), test_get_ccs (sparsity), test_mul3_issue_1465 (matrix), test_doc (misc), test_stubs_installed (pyright_stubs - expected/known: our build already documented as omitting .pyi stubs since stub generation needs casadi''s own patched SWIG fork, not stock SWIG). Several of the others (wrong-overload / typemap-shaped errors like test_mul3_issue_1465''s "Wrong number or type of arguments for overloaded function ''mtimes''") look plausibly related to the same stock-vs-patched-SWIG deviation rather than riscv64 codegen bugs, but not yet confirmed per-test. Not yet investigated further or fixed - next step is triaging each of the 8 failures (deselect confirmed-expected ones like test_stubs_installed via CIBW-style test exclusion, root-cause the rest) before this PR is ready to merge. Gotcha 307 was mistakenly committed to the casadi branch instead of main by the triage agent; recovered onto main directly (no number collision found, content identical).'
- pkg: impit
  version: 0.14.1
  home: https://apify.github.io/impit/python
  repo: https://github.com/apify/impit
  status: published
  pr: 1619
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqt6-qt6
  version: 6.11.2
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev (checked directly: /simple/pyqt6-qt6/ and /simple/pyqt6/ both 404; only pyqt6-sip is live). 6.11.2 re-confirmed latest on PyPI (2026-09-19). Parked after read-only triage; no worktree, branch, PR or workflow created. NOT the same stop as pyqt5-qt5, and the difference is worth keeping: PyQt6-Qt6 also has zero sdists across its entire history (41 releases, 184 files, all py3-none-<platform> bdist_wheel, repo field is a marketing page), but its build recipe IS public. dist-info/WHEEL says `Generator: pyqt-qt-wheel`, which is a console script of the sibling distribution PyQt-builder 1.19.1 (BSD-2-Clause, sdist on PyPI): pyqtbuild/bundle/qt_wheel.py plus the payload manifest pyqtbuild/bundle/packages/pyqt6.py. So there is an upstream recipe to mirror - it just is not a build. qt_wheel() compiles nothing; it copies files out of --qt-dir and writes a dist-info from prototypes, so the port''s input is an official Qt installation tree for the target arch, not a source tree. Running gotcha 35/41''s vendor-artifact-index check one level up settles it: download.qt.io/online/qtsdkrepository/ offers exactly linux_x64, linux_arm64, mac_x64, windows_x86, windows_arm64 - a 1:1 match with the six wheels Riverbank publishes (manylinux_2_34_x86_64, manylinux_2_39_aarch64, macosx_10_14_x86_64, macosx_11_0_arm64, win_amd64, win_arm64). The wheel matrix is the Qt Company''s prebuilt-binary matrix; riscv64 is absent from both, so there is nothing to repackage. The tool is additionally hard-wired to that layout: abstract_package.py takes the Qt version from os.path.basename(os.path.dirname(qt_dir)) and qt_wheel.py maps os.path.basename(qt_dir) through a closed arch table (gcc_64, gcc_arm64, macos/clang_64/x86_64/arm64, msvc*) and otherwise raises UserException("Qt architecture ''<x>'' is unsupported") - so --qt-dir must be <prefix>/6.11.2/gcc_64. That table is the ONLY riscv64 blocker inside the tool (bundle_qt() branches only on the manylinux*/macosx*/win* prefix), i.e. a few-line patch to a third-party build dependency, not a rewrite. The wall is the payload, not the packaging. Inspected the real 6.11.2 aarch64 wheel''s zip central directory over HTTP range requests via ci_scripts/wheel_contents.py (nothing downloaded): 2572 entries, 85.5 MB compressed / 226.7 MB uncompressed, zero .py files - 96 libQt6*.so.6 modules spanning ~22 Qt repos (qtbase incl. XcbQpa/EglFS, qtdeclarative, qtquickcontrols2, qtquick3d, qtquick3dphysics, qtquicktimeline, qtquickeffects, qtshadertools, qtsvg, qttools (Designer/Help), qtmultimedia, qtspeech, qtsensors, qtserialport, qtconnectivity (Bluetooth/Nfc), qtpositioning, qtremoteobjects, qtscxml, qtwebchannel, qtwebsockets, qtwebview, qtwayland) plus QtPdf/QtPdfQuick/QtPdfWidgets, a bundled ICU 73 (libicudata/libicui18n/libicuuc.so.73, 39 MB) and a bundled FFmpeg 7.1 (libavcodec.so.61, libavformat.so.61, libavutil.so.59, libswresample.so.5, libswscale.so.8), plus 82 plugins, 2160 QML files and 217 translations. Producing that for riscv64 means a from-source Qt 6.11.2 SDK bring-up plus ICU, FFmpeg and PDFium - gotcha 186 scale, the same scope stop pyqt5-qt5 reached by a different route, and an order of magnitude past build-libclang.yml (the largest from-source build this repo carries). No in-image shortcut. Rocky 10.2 riscv64 (the manylinux_2_39_riscv64 base) does ship a broad Qt6 - 76 qt6-* packages in AppStream and 28 in CRB, enumerated straight from the repodata at dl.rockylinux.org - but at 6.10.1, not 6.11.2, so it cannot back a wheel carrying upstream''s 6.11.2 version (same minor-version divergence gotcha 383 flags for the pyside6 family), and it has no qt6-qtpdf, qt6-qtwebengine, qt6-qtquick3dphysics or qt6-qtwebview, and no ffmpeg, chromium or gn. QtPdf comes from the qtwebengine repo (PDFium, a Chromium subset), so those three modules are the same Chromium-for-riscv64 wall the pyside6-addons entry records. Pointing pyqt-qt-wheel at a distro Qt is also a silent downgrade rather than a substitute: packages/pyqt6.py hardcodes the vendor''s own libicu*.so.73 and libav*/libsw* sonames as other_lib_deps, and bundle_qt(..., ignore_missing=True) only warns, so the wheel would quietly ship without ICU or FFmpeg and resolve them from the host. Consumer side: PyQt6 6.11.0 pins PyQt6-Qt6>=6.11.0,<6.12.0, and pyqt6 is itself now blocked-on-dependency on this entry (see its note, and gotcha 393 for the consumer-side evidence) and unpublished, so this wheel alone would not make `pip install PyQt6` work on riscv64 - but unlike the frozen x86_64-only Qt5 line, PyQt6 does publish an sdist and Riverbank does extend new Linux arches on the Qt6 line, so the pair is not a dead end, just out of scope until someone stands up a Qt-for-riscv64 SDK build. Re-triage trigger: if such a build lands (in-repo or upstream), this becomes a thin repackaging job - patch pyqt-builder''s arch table, run `pyqt-qt-wheel --qt-dir <qt>/6.11.2/<arch> PyQt6`. Not reusable from the pyside6 line: that family compiles bindings against a Qt6 SDK (and can use the distro''s 6.10.1), it does not produce a Qt6 SDK. No local build attempted - there is nothing to build without the input tree. Pattern recorded as gotcha 385 (feasibility-and-triage).'
- pkg: netifaces
  version: 0.11.0
  home: https://github.com/al45tair/netifaces
  repo: https://github.com/al45tair/netifaces
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1044
  notes: 'Upstream archived, unmaintained since 0.11.0 (2021, cp27-cp39 wheels only); still builds cleanly from the release_0_11_0 tag. No riscv64 on PyPI or pypi.riseproject.dev. Merged (#1044). Issue #1057 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: moyopy
  version: 0.18.0
  home: https://github.com/spglib/moyo
  repo: https://github.com/spglib/moyo
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1062
  notes: Upstream repo is spglib/moyo, a Cargo workspace (moyo/moyoc/moyopy/moyo-wasm members); moyopy is the PyO3 binding member, pyo3 abi3-py310 unconditional feature (build on cp310 per gotcha 96, retest cp311-cp314; separate cp314t build, no cp313t per gotcha 204). Pure-Rust core (nalgebra, no BLAS/C deps). Upstream ships no licence text in its own wheel (maturin globs LICEN[CS]E* relative to moyopy/, licences live at repo root) - staged both into moyopy/ before build, same fix as build-primp.yml. Test-only deps pymatgen/ase are pure-Python (py3-none-any); numpy needed from pypi.riseproject.dev for riscv64. cp310-abi3 leg failed CI - pymatgen (sdist-only, no riscv64 wheel, correctly so) pulls in scipy/numpy/pandas/orjson as runtime deps, all of which our registry ships, but PIP_EXTRA_INDEX_URL alone let pip pick PyPI's newer sdist-only releases over the registry's riscv64 wheels, and scipy's meson build then failed on missing OpenBLAS. Fixed by adding PIP_ONLY_BINARY=numpy,scipy,pandas,orjson to CIBW_ENVIRONMENT_LINUX (gotcha 291). Next cp310-abi3 run failed again the same way one hop further down the tree - pymatgen-core (pymatgen's actual base package) depends directly on matplotlib and spglib, and matplotlib drags in contourpy/kiwisolver/pillow; none of those five has a riscv64 wheel on public PyPI's newest release but all do on our registry, and pillow's sdist build failed on missing libjpeg headers. Read pymatgen-core's requires_dist up front this time and added all five in one pass, extending PIP_ONLY_BINARY to numpy,scipy,pandas,orjson,pillow,matplotlib,contourpy,kiwisolver,spglib (gotcha 291 addendum). That fix regressed cp310-abi3 a third time, differently - build-spglib.yml's own matrix only publishes cp312+ riscv64 wheels, so forcing spglib wheel-only left cp310/cp311 test venvs with zero installable candidates; pip's resolver responded by backtracking through pymatgen's entire release history (~200 sdists, 2025.10.7 down to the pre-CalVer 4.x series) hunting for a version without a spglib dependency, burning the full 85-minute job before hard-crashing on pymatgen 4.4.11's ancient numpy-distutils setup.py (AttributeError on __NUMPY_SETUP__, an unrelated __builtins__-is-a-dict-under-exec() bug in that release, not a numpy-version incompatibility). Fixed by dropping spglib from PIP_ONLY_BINARY entirely rather than pinning pymatgen or narrowing the matrix - our registry's spglib (2.7.0) doesn't lag PyPI's today, so leaving it unpinned still prefers our wheel where one exists (cp312+) and falls back to building the sdist where it doesn't (cp310/cp311); confirmed safe since spglib's build-time tools (cmake/ninja) ship interpreter-agnostic py3-none wheels on the registry and build-spglib.yml needs no before-build step (gotcha 291 second addendum). Pushed fix; re-running both legs.
- pkg: isal
  version: 1.8.0
  home: https://github.com/pycompression/python-isal
  repo: https://github.com/pycompression/python-isal
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1047
  notes: 'ISA-L vendors a full portable-C fallback (Makefile.unx host_cpu=base_aliases) for archs with no dedicated asm, confirmed feasible for riscv64 via a QEMU rehearsal (228 passed, 6 skipped, 0 failures). PR CI green (cp312/cp313/cp314/cp314t + publish dry-run). Merged (#1047). Issue #1069 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: immutables
  version: '0.21'
  home: https://github.com/MagicStack/immutables
  repo: https://github.com/MagicStack/immutables
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1045
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. build-from-checkout, cibuildwheel, cp312/cp313/cp314/cp314t x manylinux/musllinux. Merged (#1045). Issue #1056 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: fasttext-wheel
  version: 0.9.2
  home: https://github.com/facebookresearch/fastText
  repo: https://github.com/facebookresearch/fastText
  status: published
  pr: 1621
  notes: '72 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Real source is facebookresearch/fastText (archived); fasttext-wheel is messense''s wrapper repo that renames the same setup.py-built distribution, confirmed via messense/fasttext-wheel''s manylinux.sh. build-from-checkout, cibuildwheel, cp312/cp313/cp314/cp314t. `-march=native` in setup.py''s Extension() is dead code (BuildExt.build_extensions overwrites extra_compile_args before compiling, confirmed via local build log), so no patch needed for it. Three patches: rename distribution to fasttext-wheel; fix a real numpy>=2.0 regression in predict()/scores() (np.array(..., copy=False) -> np.asarray), verified locally (83/83 unit tests pass after the fix, 14 fail without it); and a riscv64-only missing <cstdint> include in args.cc (uint64_t undeclared), backported from upstream commit 6c2204ba6677 which fixed the same failure under GCC 13 before the repo was archived. setup.py also self-installs pybind11 via `pip install` when missing, which fails in `python -m build`''s isolated venv (no pip module there); fixed with CIBW_BEFORE_BUILD pre-installing pybind11/setuptools/numpy and CIBW_BUILD_FRONTEND: pip --no-build-isolation. CI green on all four legs (cp312/cp313/cp314/cp314t). PR #1621 ready for review.'
- pkg: pyqt6-sip
  version: 13.12.0
  home: https://github.com/Python-SIP/sip
  repo: https://github.com/Python-SIP/sip
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1046
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI project_urls points at Python-SIP/sip (the sip build-tool repo, versioned 6.x) but that repo does not contain PyQt6-sip''s C sources (sip_core.c etc) - it is a different project by the same maintainer. No canonical git repo hosts PyQt6-sip''s actual source; Riverbank publishes it as a PyPI sdist only (gotcha 156 pattern). Building from the fetched sdist. BSD-2-Clause, self-contained C extension, no native deps. Merged (#1046). Issue #1070 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: humming-kernels
  version: 0.1.13
  home: https://github.com/inclusionAI/humming
  repo: https://github.com/inclusionAI/humming
  status: parked
  pr: null
  notes: 'Not feasible: CUDA/NVIDIA-GPU-only at every layer -- new gotcha 386, same disposition as the parked sglang entry. Upstream located despite the missing PyPI project URL: the long_description names it (github.com/inclusionAI/humming, Apache-2.0, tags v0.1.2..v0.1.15 present, upstream .github/workflows/build-wheel.yml shipped inside the sdist), so unlike tokenspeed-triton (gotcha 381) provenance is clean and public -- "Humming", a JIT-compiled quantization GEMM kernel library for NVIDIA GPUs "SM75+ (Turing) and beyond", inspired by DeepGEMM/Marlin/CUTLASS. Nothing about the wheel objects: 338 KB, py3-none-manylinux_2_28_{x86_64,aarch64} at 0.1.13, no vendor blobs, so gotcha 41''s payload tell is absent; it is blocked below that. (1) Native payload (read over HTTP ranges with ci_scripts/wheel_contents.py): humming/_native/<arch>/{libhumming_launcher.so, libcubinpatch.so, nvrtc_compile, manifest.json}, selected by get_native_arch() in humming/utils/jit.py, which maps only x86_64|amd64 and aarch64|arm64 -- riscv64 returns None, so build_native() raises "Unsupported architecture" and every get_precompiled_artifact_path() returns None. 0.1.14/0.1.15 add _device_info.abi3.so and retag cp310-abi3, so this entry''s "abi: py3" is already stale (latest is 0.1.15; identically blocked). (2) Build-time CUDA is unconditional, i.e. gotcha 284''s greps come out the opposite way from fastsafetensors: humming/csrc/launcher/{launcher.cpp,tensor.h,tma.h} #include <cuda.h>, csrc/nvrtc_compile.cpp #includes <nvrtc.h>, and humming/build.py:_find_cuda_include() hard-fails without cuda.h from nvidia-cuda-runtime-cu12 or CUDA_HOME. (3) The decisive wall is torch''s CUDA half, not the toolkit: build_launcher() links _torch_library("libtorch_cuda.so"), which raises "is required; build with a CUDA-enabled torch wheel". pypi.riseproject.dev serves torch 2.13.0+cpu/2.14.0+cpu for riscv64 and PyPI has zero riscv64 torch files, so no libtorch_cuda.so exists for the arch and none can be built without a riscv64 CUDA toolkit first. Stubbing is not the missing idea -- upstream already links against a generated empty libcuda.so (-Wl,-soname,libcuda.so.1) and relies on the real driver at run time, while the torch CUDA libraries are linked as real files. (4) Two hard runtime deps have no riscv64 wheel and no sdist anywhere: triton 3.8.0 (manylinux_2_27/2_28 x86_64+aarch64 only -- the gotcha 41 project this repo already ruled out) and cuda-bindings 13.4.2 (x86_64/aarch64/win only). Both are imported at import time -- import humming -> humming.ops -> ops/input.py "import triton" and "from triton.language.extra.cuda import gdc_wait"; eleven humming/kernel/*.py and jit/{compiler,runtime}.py "import cuda.bindings.driver" -- so per gotcha 40 a wheel we published could be neither installed nor smoke-tested. (5) Runtime needs the NVIDIA driver and NVRTC regardless: jit/compiler.py NVRTC-compiles the shipped .cuh sources at first call (nvrtc_compile --nvrtc-path libnvrtc.so), utils/device.py imports pynvml, libhumming_launcher.so links -lcuda. NVIDIA ships nothing riscv64: redistrib_13.0.0/13.2.0/13.4.2.json list only linux-x86_64, linux-sbsa, windows-x86_64/arm64 with zero "riscv" strings, and nvidia-cuda-nvrtc 13.4.92 / nvidia-cuda-runtime-cu12 12.9.79 publish x86_64/aarch64/win wheels only; CUDA-on-RISC-V was announced as a host-CPU target in July 2025 (RVA23 + RISC-V server SoC/platform specs, restated at Hot Chips 2026) with no release and no shipped artifact. No workflow, docs/packages entry, patches or PR created, and nothing to rehearse under QEMU since the build cannot start. Revisit only when NVIDIA ships a riscv64 CUDA redist (nvrtc + cuda_runtime), a CUDA-enabled riscv64 torch exists, and triton + cuda-bindings publish riscv64 wheels.'
- pkg: nvtx
  version: 0.2.16
  home: https://github.com/NVIDIA/NVTX
  repo: https://github.com/NVIDIA/NVTX
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2113
  notes: 'PR https://github.com/riseproject-dev/python-wheels/pull/2113 (branch port/nvtx). Not a GPU/CUDA package despite the NVIDIA name, so it does not belong with the parked onnxruntime-gpu/cupy-cuda* set: the PyPI `nvtx` distribution is the binding in the `python/` subdir of NVIDIA/NVTX, five Cython extension modules over the header-only nvtx3/nvtxw3 C annotation API. setup.py declares a single Extension(''*'', sources=[''src/nvtx/_lib/*.pyx''], include_dirs=[<repo>/c/include]) with no `libraries=` at all, and the only `cdef extern from` headers across the .pxd files are nvtx3/nvToolsExt{,Counters,Payload}.h, nvtx3/nvToolsExtSemantics{Time,Counters}.h and nvtxw3/nvtxw3{,_counter_helpers,_event_helpers}.h - no CUDA header, no libcuda/libcudart/libnvToolsExt on the link line. nvtx.enabled() is `not os.getenv("NVTX_DISABLE")`, nothing GPU-conditional, so the annotations are plain no-ops until an external profiler injection library is loaded via NVTX_INJECTION64_PATH. auditwheel agrees: the repaired wheel references only libc.so.6 and vendors nothing (manylinux_2_34_riscv64.manylinux_2_39_riscv64). Workflow mirrors upstream cibw.yml - checkout + cibuildwheel with package-dir: python - and deliberately keeps upstream''s own [tool.cibuildwheel] build and test config untouched, including its test-command (pytest python/tests, again with --enable-injection, then both again after `pip uninstall -y numpy`). Only deviations: the riscv64 manylinux image and PIP_EXTRA_INDEX_URL to our registry, because numpy (optional for nvtx but installed by that test-command) has no riscv64 wheel on public PyPI. musllinux is not built; upstream skips it itself. No patches needed. No git tag exists for any Python release (gotcha 103): the repo''s v3.* tags are the C/C++ library and the wheels are cut by a workflow_dispatch-only cibw.yml. Pinned NVTX_REF=b7e56f42f85989f6df2839ec08f1666460f972ec (2026-08-11, the last commit before the 2026-08-12 sdist upload), proved by diffing the 0.2.16 PyPI sdist file-by-file against that tree: python/src, setup.py, MANIFEST.in, LICENSE.txt, README.md and the whole include/ tree (= c/include) are byte-identical; the only differences are setuptools'' sdist rewrite of setup.cfg (whitespace plus an empty [egg_info] tag_build, harmless per gotcha 22) and the [tool.cibuildwheel] block, which NVIDIA evidently maintains out-of-tree - the released sdist''s test-command names python/tests/run_ci_tests.py, a file that has never existed in the public repo on any branch. Building from the checkout rather than the sdist is also what makes the suite runnable at all: the sdist ships tests/test_*.py but omits tests/__init__.py, tests/conftest.py and tests/injection/, so --enable-injection would not even be a recognised option there. Rehearsed end to end locally under QEMU inside quay.io/pypa/manylinux_2_39_riscv64, cp312 and cp314t: both build all five .so, auditwheel-repair, install and import from a clean venv (no GIL-re-enable warning on cp314t - upstream sets Cython''s freethreading_compatible=True), and upstream''s pytest runs pass - 4467 passed with numpy and 3869 passed/147 skipped without it, on both interpreters, each also under --enable-injection, which compiles tests/injection/NvtxTestInjection.cpp on riscv64 and asserts every recorded NVTX event, so the annotation path is verified working rather than merely importable. nvtx.annotate/start_range/end_range/push_range/pop_range/mark were also exercised directly on riscv64 with no GPU and no profiler present. CI on #2113 had not started at hand-off (shared runner pool).'
- pkg: decord
  version: 0.6.0
  home: https://github.com/dmlc/decord
  repo: https://github.com/dmlc/decord
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1736
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. libdecord.so is ctypes-loaded (decord/_ffi), not a CPython extension -- the real PyPI wheel is built with one interpreter then hand-retagged py3-none (pypi.yml), same shape as plotext/xgboost. Upstream''s own manylinux job (tools/build_manylinux2010.sh) compiles FFmpeg 4.1.6 from source with --enable-gpl/--enable-nonfree/libx264/libvpx for encoding it never uses (decord has no avcodec_find_encoder/AVOutputFormat call anywhere in src/) -- so build-decord.yml instead builds a plain LGPL FFmpeg (no GPL bits, no gpl_sources job needed). FFmpeg 4.1.6 predates ARCH_RISCV entirely (added in FFmpeg 5.0); pinned to FFmpeg 6.1.6 instead, the last release before FFmpeg 7.0 removed the channels/channel_layout/av_get_default_channel_layout API src/audio/audio_reader.cc still uses (deprecated-but-present through 6.x, so this avoids a source patch entirely -- confirmed by reading FFmpeg''s own libavcodec/codec_par.h at each tag). PyAV-Org/pyav-ffmpeg (this repo''s riscv64 vendor for the in-review av port, PR #461) was considered and rejected: it already publishes the linkable-library artifact shape decord''s FindFFmpeg.cmake needs, but its build enables GPL x264/x265, which decord has no use for and which would need the same vendor_sources/gpl_sources machinery av''s still-draft PR carries. Wheel carries decord.libs/libavcodec* etc. via auditwheel, same shape as the real PyPI wheel. Worktree/branch at .claude/worktrees/decord, branch decord. PR https://github.com/riseproject-dev/python-wheels/pull/1736 opened, CI running.'
- pkg: thriftpy2
  version: 0.7.1
  home: https://thriftpy2.readthedocs.io/
  repo: https://github.com/Thriftpy/thriftpy2
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1063
  notes: 'Bumped to 0.7.1 (latest release; 0.7.0 was stale). Clean cibuildwheel build, no native deps. test_aio_http.py ignored - aiohttp has no riscv64 wheel for cp314/cp314t or musllinux on the registry. cp314t also ignores several fork-race-prone server tests (ConnectionRefusedError). A follow-up cp314t-only PicklingError in test_apache_json.py::test_client (CPython 3.14''s forkserver default surfacing once test_all_protocols_binary_field.py''s set_start_method(''fork'') call stopped running) is fixed with two targeted --deselects (gotcha 283); needed the rootdir-relative tests/ nodeid prefix, not the bare form pytest''s -v output shows. All 8 matrix legs green, PR not yet merged. Merged (#1063). Issue #142 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: viztracer
  version: 1.1.1
  home: https://github.com/gaogaotiantian/viztracer
  repo: https://github.com/gaogaotiantian/viztracer
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1080
  notes: 'Build-from-checkout cibuildwheel, plain setuptools Extension (two C99 exts, pthread only). objprint dep is py3-none-any, no registry entry needed. All 4 legs green: 231 passed, 14 skipped. Skips test_trace_self and test_use_external_processor (both hang forever on riscv64 - self-trace overhead and missing Perfetto trace_processor_shell) and 4 test_remote.py attach tests (no attach_riscv64.so, vendored blob with no source). CIBW_TEST_SOURCES adds example/json for test_combine. Merged (#1080). Issue #1149 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: pyqt6
  version: 6.11.0
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.
    6.11.0 re-confirmed latest on PyPI (2026-09-19; 6 files - macosx/win_amd64/win_arm64/manylinux_2_34_x86_64/manylinux_2_39_aarch64
    wheels plus an sdist). Blocked on pyqt6-qt6 (parked, see that entry + gotcha 385:
    PyQt6-Qt6 is Riverbank repackaging the Qt Company''s own prebuilt installer tree,
    whose matrix has no riscv64, so shipping it means a from-source Qt6 SDK bring-up).
    Read-only triage; no worktree, branch, PR or workflow created. Not the same stop as
    pyqt5: PyQt6 is genuinely open and buildable - GPL-3.0 sdist on every release, real
    C++/.sip sources for 35 binding sets, builds with two public pure-Python tools (sip
    >=6.15,<7 and PyQt-builder >=1.19,<2), and pyqt6-sip is already live on our registry
    (13.12.0, cp310-cp314, satisfies the >=13.8,<14 pin). The blocker is the runtime pair,
    and it is sharper than metadata: the published wheel''s METADATA requires PyQt6-Qt6
    (>=6.11.0, <6.12.0), and ci_scripts/check_riscv64_deps.py --python 312 -- ''PyQt6-Qt6>=6.11.0,<6.12.0''
    answers UNRESOLVABLE (from versions: none) against PyPI + our registry. Worth recording
    because the sdist disagrees with the wheel: the sdist''s PKG-INFO declares only
    ''Requires-Dist: PyQt6-sip (>=13.11, <14)'' and nothing in project.py or in PyQt-builder
    ever emits the PyQt6-Qt6 line (pyqtbuild''s only Requires-Dist writes are bundle/qt_wheel.py,
    which puts it *in* the Qt wheel, and bundle/bundle.py, which *removes* it when pyqt-bundle
    bundles Qt inside the bindings wheel) - it comes from Riverbank''s release step, so
    judging feasibility from the sdist alone would have said "unblocked". The physical
    coupling is the decisive evidence: pulled PyQt6/QtCore.abi3.so out of the 6.11.0
    manylinux_2_39_aarch64 wheel with ci_scripts/wheel_contents.py --member (nothing
    downloaded beyond that member) and readelf -d shows NEEDED libQt6Core.so.6 with
    RUNPATH $ORIGIN/Qt6/lib, while the wheel itself ships no Qt6/lib (893 entries, 40.6 MB
    uncompressed: .abi3.so modules, .pyi stubs, one Qt6/qsci/api file) - the directory is
    populated by the sibling pyqt6-qt6 wheel at install time. Building against a distro Qt6
    instead was checked and does not rescue it, though it is technically possible: project.py
    only rejects qt_version >> 16 != 6 (no minimum minor), PyQt-builder takes the sip tag
    from the discovered qt_version_tag, sipbuild''s update_buildable_bindings() silently
    drops bindings whose config test fails, and Rocky 10.2 riscv64 does have what such a
    build needs (qmake6 in qt6-qtbase-devel, 76 qt6-* in AppStream + 28 in CRB, 28 module
    -devel packages - enumerated from the repodata, not dnf). But that build yields a
    Qt-6.10.1 API surface under a 6.11.0 version number (gotcha 383''s divergence; same
    shape as this repo''s published shiboken6-6.11.2-6.10.1 build tag), silently drops
    QtPdf/QtPdfWidgets (no qt6-qtpdf on any Rocky 10 arch), and - decisive - auditwheel can
    only bundle the Qt libraries the extensions link, not what Qt dlopens: the platform
    plugins (platforms/libqxcb.so, libqoffscreen.so), imageformat/sqldriver plugins and the
    QML tree all live in pyqt6-qt6''s payload, and the bundled distro libQt6Core keeps its
    compiled-in /usr/lib64/qt6/plugins prefix, so such a wheel would import fine and then
    fail at the first QApplication with "no Qt platform plugin could be initialized".
    Shipping that under the pyqt6 name is a broken product, not a reduced one. Re-triage
    trigger: the moment a riscv64 Qt6 SDK exists (in-repo or upstream) and pyqt6-qt6 lands,
    this becomes a straightforward sdist build - pip install "sip>=6.15,<7" "PyQt-builder>=1.19,<2",
    build from the PyPI sdist in the manylinux_2_39_riscv64 image against that SDK, keep the
    upstream shape (no bundled Qt, RUNPATH $ORIGIN/Qt6/lib), cp310-abi3 single wheel. Consumer
    pattern recorded as gotcha 393 (feasibility-and-triage); no need to redo the pyqt6-qt6
    investigation, which is captured on that entry and in gotcha 385.'
- pkg: ibmmq
  version: 2.1.0
  home: https://ibm.com/software/products/en/ibm-mq
  repo: https://ibm.com/software/products/en/ibm-mq
  status: parked
  pr: null
  notes: 'Not feasible: proprietary build-and-link-time MQ client SDK with no riscv64 asset (gotcha 342 shape, feasibility-and-triage.md). ibmmq 2.1.0''s C extension (code/ibmmq/ibmmqc.c) `#include`s <cmqc.h>/<cmqcfc.h>/<cmqxc.h> and setup.py links it against `libraries=[''mqm_r'']` found under /opt/mqm (or $MQ_FILE_PATH) -- these headers and the shared library come only from IBM''s proprietary "MQ Client and SDK" (Redistributable Client), which setup.py does not download itself but hard-requires: it raises FileNotFoundError before compiling if cmqc.h isn''t found (unless $CI is set, in which case compilation itself would fail without the header). Confirmed by disassembling the real manylinux_2_28_x86_64 wheel from PyPI: ibmmqc.abi3.so carries a DT_NEEDED entry on libmqm_r.so, i.e. the published wheel doesn''t vendor the client either -- it''s a runtime dependency on a system-installed proprietary library, on top of being a build-time one via the headers. IBM''s own docs (ibm.com/docs, Redistributable MQ Clients overview) list only Linux x86-64 and Windows x64 for the redistributable client, plus s390x/ppc64le for the MFT agent -- no RISC-V anywhere. Cross-checked against upstream''s own code/examples/Dockerfile, which enumerates every architecture it knows how to install the client on -- amd64 (curl from IBM''s redist server), arm64 (manual full server tarball dropped into MQINST/), ppc64le/s390x (manual .deb/.rpm dropped into MQINST/) -- and ends with an explicit `else: echo "Unsupported platform $T"; exit 1` for everything else, riscv64 included. No source is available for IBM''s MQI client library to relink ibmmqc.c against an open alternative (unlike an open protocol implementation), so there is no build-from-source escape hatch (gotcha 77 doesn''t apply). PyPI 2.1.0 itself ships only 3 platform wheels (macosx_11_0_arm64, manylinux_2_28_x86_64, win_amd64) plus an sdist -- no aarch64 either, confirming the client''s own arch ceiling (not an unrelated packaging choice) drives the published matrix. No worktree/branch opened -- diagnosed read-only against the real 2.1.0 sdist (setup.py, ibmmqc.c), the manylinux wheel''s ELF NEEDED entries, IBM''s live documentation, and upstream''s own Dockerfile.'
- pkg: pedalboard
  version: 0.9.24
  home: https://spotify.github.io/pedalboard/
  repo: https://github.com/spotify/pedalboard
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/759
  notes: 'Batch 4. PR #759 (opened 2026-08-28, before the current workflow architecture)
    was rebased onto main, squashed and merged 2026-09-20 as 3019963219. The build
    content is unchanged from the run that was already green in August (collect-licenses
    heredoc, CIBW_* env, wheel-content verification, gpl_sources over gcc/alsa-lib/freetype/glib2/graphite2/pcre2,
    the CMake non-ARM-is-not-x86 patch); what was rewritten is the scaffolding: the
    workflow had only workflow_dispatch + pull_request triggers, a hardcoded PEDALBOARD_VERSION,
    no setup job, and a publish job calling the deleted actions/publish-wheels (GitLab).
    It now has all three triggers, a setup job on _setup.yml with package: pedalboard,
    matrix.version everywhere, and _publish-wheel.yml. docs/packages/pedalboard.yaml
    was missing entirely and was added; its licence is GPL-3.0-only, not MIT -- upstream
    LICENSE at v0.9.24 is the GPLv3 text and the trove classifier is GPLv3, which matters
    here because the wheel also bundles copyleft libraries and ships a gpl-sources.tar.
    All four interpreters (cp312/cp313/cp314/cp314t) plus gpl_sources went green on
    the PR and again on the main push run (35528229436). Release pedalboard-v0.9.24-20260920194014
    carries the four wheels and gpl-sources.tar. Its first publish attempt failed in
    update_doc.py -- not a pedalboard bug: the shared github-actions/update-doc branch
    could not rebase onto main because zensical 0.0.63''s pending entry (#2132) collided
    with the branch''s own filled 0.0.58-0.0.62 entries. Resolved that conflict as a
    union and force-pushed the docs branch (PR #2133 asks for exactly that), then re-ran
    the failed publish job, which succeeded; the first attempt left an orphan duplicate
    release pedalboard-v0.9.24-20260920193537 that nothing references and that can be
    deleted. Not yet on pypi.riseproject.dev: docs PR #2133 (19 packages) is still awaiting
    maintainer review, and the index only serves what is on main -- move to published
    once it merges and the simple index returns 200. Issue #2134 (label wheel) created
    and linked to #759 through the Development panel via a closing keyword, then reopened,
    since these issues stay open after the port lands. The Projects v2 "Python Wheels"
    board Status could not be set to "Available in RISE PyPI" from this session: GitHub
    GraphQL is blocked here and Projects v2 has no REST API, so that one step needs
    doing by hand.'
- pkg: tree-sitter-elixir
  version: 0.3.5
  home: https://github.com/elixir-lang/tree-sitter-elixir
  repo: https://github.com/elixir-lang/tree-sitter-elixir
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1054
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.3.5 commits generated src/parser.c, so no generate-then-build workaround needed. Merged (#1054). Issue #1067 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: livekit-local-inference
  version: 0.2.7
  home: https://pypi.org/project/livekit-local-inference/
  repo: ''
  status: parked
  pr: null
  notes: 'Not feasible, closed source (gotcha 392; same double lock as gotcha 372/376). Genuinely a LiveKit Inc. package - livekit-agents core `dependencies` lists `livekit-local-inference>=0.2.7` and `livekit/agents/inference/vad.py`, `inference/eot/transports.py` and `ipc/_preload.py` all `from livekit.local_inference import VAD/EOT` - but livekit/agents is the *consumer*, not the source: none of the 79 public repos in the livekit GitHub org contains it, PyPI records no project URL/home_page/author, and `dist-info/WHEEL` says `Generator: setuptools (84.0.0)` (a stock generator, so gotcha 385''s lead is a dead end). The decisive read was conda-forge: `conda-forge/livekit-local-inference-feedstock`''s recipe/recipe.yaml states verbatim "This package is closed-source and ships only binary wheels on PyPI (no sdist)" and its `source:` is a nest of `if: target_platform`/`if: match(python, ...)` blocks each naming a files.pythonhosted.org **wheel** URL - i.e. conda-forge repackages upstream''s wheels per (platform, interpreter) and compiles nothing, so riscv64 has nothing to repackage either. Zero sdist across the entire release history (120 files over 0.2.2-0.2.7, every one a bdist_wheel; tags only macosx_10_9/10_13/10_15_x86_64, macosx_11_0_arm64, manylinux_2_27/2_28_{x86_64,aarch64}, win_amd64 for cp310-cp314), so gotcha 77''s build-it-yourself escape hatch fails at the first step. Wheel content (real compiled extension, not a gotcha 27/35/81 py3-none facade): 14 entries, 35.0 MB of a 35.1 MB wheel is one stripped ELF `livekit/local_inference/_native.cpython-3XX-<arch>-linux-gnu.so`; `readelf -S` shows `.text` 0x444ad (~280 KB) vs `.rodata` 0x21168f8 (~34.8 MB), so 99.2% is the proprietary model weights baked into the binary, with a ~280 KB hand-written pybind11 v12 C++ engine around them (DT_NEEDED is only libstdc++/libm/libgcc_s/libpthread/libc - no ONNX Runtime; .comment `GCC (GNU) 14.2.1 Red Hat`; the shipped _native.pyi says "Eagerly init the EOT model singleton (~108 MB)"). Second, independent lock: `License: Apache-2.0 AND LicenseRef-LiveKit-Model` with two files under dist-info/licenses/ - the bundled MODEL_LICENSE (LIVEKIT MODEL LICENSE AGREEMENT) forbids using the models "on a standalone basis or with any frameworks other than LiveKit Agents" and bars making them available to third parties except under that agreement, so even lifting the weights out of an existing .so into a self-built riscv64 wheel is foreclosed. Consequence for the queue: livekit-agents core is uninstallable on riscv64 while this leaf has no wheel. Verified read-only (PyPI JSON for all releases, ci_scripts/wheel_contents.py, one wheel downloaded and readelf''d, the conda-forge recipe, GitHub org/code search); no workflow, docs/packages entry, worktree, branch or PR created. Re-check only if LiveKit publishes the engine''s sources or ships riscv64 wheels itself.'
- pkg: manifold3d
  version: 3.5.2
  home: https://github.com/elalish/manifold
  repo: https://github.com/elalish/manifold
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1082
  notes: 'build-from-checkout via cibuildwheel/scikit-build-core/nanobind; MANIFOLD_PAR=ON fetches oneTBB (Apache-2.0) and MANIFOLD_CROSS_SECTION fetches Clipper2 (BSL-1.0) from source and statically links both, so their licences are staged at the root for the default glob (upstream ships neither). Upstream skips musllinux entirely; mirrored. All four interpreters green on PR CI (one cp313 job hit a transient docker-daemon runner flake, reran clean); publish job dry-ran successfully. Merged (#1082). Issue #1127 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tink
  version: 1.16.1
  home: https://github.com/tink-crypto/tink-py
  repo: https://github.com/tink-crypto/tink-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1738
  notes: '10 Linux wheels upstream (abi cp310-cp314, x86_64+aarch64), sdist present; no riscv64 anywhere. tink-py compiles its pybind11 C++ extension (//tink/cc/pybind:tink_bindings) entirely via Bazel/bzlmod (MODULE.bazel, no WORKSPACE) -- bazel_dep on abseil-cpp, protobuf, tink_cc (which pulls in BoringSSL) and pybind11_bazel, all of which already have riscv64 precedent elsewhere in this repo (gotcha 132). No CMake path exists; this is a genuine Bazel build, mirroring build-ray.yml/build-array-record.yml''s bootstrap-bazel-from-dist-archive shape (gotcha 47), since bazel publishes no riscv64 release binary. Unlike those ports, tink-py pins bazel 9.1.0 and rules_python 2.2.0 (confirmed via BCR: rules_python 2.2.0''s versions.bzl already lists a riscv64-unknown-linux-gnu platform, and python-build-standalone now publishes riscv64 CPython 3.10-3.14 builds), so no rules_python/rules_java riscv64 override is needed, unlike the bazel 7.x era ports. No protoc release ships a riscv64 binary (setup.py needs one on PATH to regenerate tink/proto/*_pb2.py), so the workflow builds it from the project''s own pinned protobuf (30.1) via Bazel instead of downloading it. Upstream''s own published wheel ships only its own Apache-2.0 LICENSE despite statically linking BoringSSL/abseil-cpp/protobuf/tink_cc/pybind11 (confirmed against the real PyPI cp312 manylinux wheel); patches/tink/1.16.1/0001-*.patch adds LICENSE.<dep> for each at the checkout root, picked up by setuptools'' default license_files glob with no setup.cfg change. Testing runs tink''s own *_test.py suite against the installed wheel, excluding tink/integration/ (live AWS/GCP/Vault KMS calls, no credentials in CI). PR #1738 opened, CI running.'
- pkg: qdldl
  version: 0.1.9.post1
  home: https://github.com/osqp/qdldl-python/
  repo: https://github.com/osqp/qdldl-python/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1064
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout shape mirroring build-osqp.yml; patches in the vendored AMD (SuiteSparse, BSD-3-Clause) licence missing from upstream wheels. Merged (#1064). Issue #1077 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pylibsrtp
  version: 1.0.0
  home: https://github.com/aiortc/pylibsrtp
  repo: https://github.com/aiortc/pylibsrtp
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1073
  notes: '6 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Builds vendored cisco/libsrtp via cmake; OpenSSL comes from aiortc/aioquic-openssl prebuilt tarballs which have no riscv64 entry, so a patch swaps in the image''s own openssl-devel (rehearsed locally under riscv64 QEMU: build + auditwheel repair + full test suite all pass). Merged (#1073). Issue #1079 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: pygraphviz
  version: 2.0.1
  home: https://pygraphviz.github.io
  repo: https://github.com/pygraphviz/pygraphviz
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: sqlglotc
  version: 30.17.0
  home: https://sqlglot.com/
  repo: https://github.com/tobymao/sqlglot
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1737
  notes: 'Confirmed genuine: mypyc-compiled companion extension for sqlglot (Requires-Dist: sqlglot==30.17.0, Root-Is-Purelib: false). cibuildwheel builds the sqlglotc/ subdirectory of the tobymao/sqlglot monorepo (setup.py runs mypyc.build.mypycify over a subset of sqlglot''s own modules); mirrors package-publish.yml''s build-wheels job, which has no committed [tool.cibuildwheel] table to inherit (unlike build-black.yml), so CIBW_* is set explicitly here. Matrix trimmed to cp312/cp313/cp314 (no cp314t - upstream itself ships none, "mypyc doesn''t have great support for free threaded builds" per build-black.yml). Testing runs upstream''s own make unitc recipe (SKIP_INTEGRATION=1 python -m unittest) against the installed compiled wheel; duckdb and pandas (needed by tests/test_optimizer.py and tests/test_executor.py) both have riscv64 wheels on pypi.riseproject.dev for cp312-cp314, confirmed via the registry simple index. Worktree/branch sqlglotc created.'
- pkg: mutf8
  version: 1.1.0
  home: https://github.com/TkTech/mutf8
  repo: https://github.com/TkTech/mutf8
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1065
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Small optional C extension (cmutf8.c), no native deps; conftest.py unconditionally imports the extension so a failed compile hard-fails tests. Merged (#1065). Issue #1078 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: yggdrasil-engine
  version: 1.3.1
  home: https://github.com/Unleash/yggdrasil-bindings
  repo: https://github.com/Unleash/yggdrasil-bindings
  status: published
  pr: 1867
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; located upstream as Unleash/yggdrasil-bindings (python-engine subdir) - a ctypes binding to their Rust yggdrasilffi cdylib, not Google Yggdrasil Decision Forests despite the name (verified via sdist/PyPI metadata: MIT licence, author Simon Hornby, summary "Engine for evaluating Unleash feature flags"). Upstream downloads prebuilt FFI binaries per platform rather than compiling; none published for riscv64, so the port builds the pure-Rust cdylib natively in-container and retags with upstream''s own repair_wheel.py. Musllinux dropped: no riscv64 musl host Rust toolchain (gotcha 10).'
- pkg: biotraj
  version: 1.2.2
  home: https://www.biotite-python.org
  repo: https://github.com/biotite-dev/biotraj
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1074
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. Runtime dependency of biotite (already merged); own small self-contained Cython/C sdist, builds cleanly with no extra native deps. Merged (#1074). Issue #1087 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: opencv-contrib-python-headless
  version: 5.0.0.93
  home: https://github.com/opencv/opencv-python
  repo: https://github.com/opencv/opencv-python
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2111
  notes: 'PR #2111 opened; pr-checks (check_commit_messages/check_patches/check_packages_yaml) and the setup job are green, the riscv64 build job is queued on the runner pool (1440-minute budget, so the compile outlives this session). Union of the two merged siblings (gotcha 79): build-opencv-contrib-python.yml (PR #845) supplies the opencv_contrib submodule checkout, ENABLE_CONTRIB=1 and the 1440-minute timeout; build-opencv-python-headless.yml supplies ENABLE_HEADLESS=1. find_version.py True True False False stamps both flags into cv2/version.py, which is what actually selects the package name once .git is dropped -- verified locally by stubbing skbuild.setup and re-running setup.py (new gotcha 403): stamped contrib/headless True yields name opencv_contrib_python_headless, license Apache 2.0, with -DOPENCV_EXTRA_MODULES_PATH plus WITH_WIN32UI/QT/GTK/MSMF/OBSENSOR=OFF and OPENCV_FFMPEG_ENABLE_LIBAVDEVICE=OFF, while an unstamped version.py with both ENABLE_* still set yields plain opencv_python. Matches upstream build_wheels_manylinux.yml, whose matrix is with_contrib x without_gui. Local validation reached a full CMake configure of opencv + opencv_contrib inside quay.io/pypa/manylinux_2_39_riscv64 under qemu-riscv64 binfmt on an x86_64 host (new gotcha 404): Configuring done (2365.7s) + Generating done, exit 0, extra modules /src/opencv_contrib/modules at 755e506, 50 modules to be built, GUI NONE and highgui builtin backend NONE, baseline -march=rv64gc with the RVV ASM kernels off, third-party libprotobuf 3.19.1/libjpeg-turbo/libpng/libtiff/libopenjp2/libwebp/libclapack all vendored so no external native dep is needed. Python3 wrappers read Unavailable in that rehearsal only because the bare image has no numpy (cibuildwheel installs it); a full emulated compile of 50 modules was out of reach on a loaded 4-core host, so the PR CI run is what proves the compile. No patches needed. pypi.riseproject.dev/simple/opencv-contrib-python-headless/ still 404; the other three siblings are all live.'
- pkg: hogql-parser
  version: 1.3.84
  home: https://github.com/PostHog/posthog/tree/master/common/hogql_parser
  repo: https://github.com/PostHog/posthog/tree/master/common/hogql_parser
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1876
  notes: 'ANTLR4/C++ parser, independently maintained alongside hogql-parser-rs (PostHog runs both in parallel, prod shadow mode); PyPI releases continue past the rust rewrite (1.3.86 as of 2026-09-04). 8 Linux wheels upstream at 1.3.84 (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. Pinned to commit c30d27369 (same commit hogql-parser-rs #1804 was pinned to), which matches setup.py version 1.3.84. PR #1876 open, CI green (cp310-cp313 x manylinux_riscv64); no test command - hogql_parser needs the posthog Django app (not on PyPI) importable, same as upstream own untested wheel.'
- pkg: apsw
  version: 3.53.4.0
  home: https://github.com/rogerbinns/apsw
  repo: https://github.com/rogerbinns/apsw
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1071
  notes: '48 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout, mirrors upstream build-pypi.yml: CIBW_BEFORE_BUILD copies tools/setup-pypi.cfg to setup.apsw so setup.py fetches the matching SQLite amalgamation and enables all extensions; tested with python -m apsw.tests -v (227 passed locally on cp312/aarch64 rehearsal). Merged (#1071). Issue #1129 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: tree-sitter-powershell
  version: 0.26.4
  home: https://github.com/airbus-cert/tree-sitter-powershell
  repo: https://github.com/airbus-cert/tree-sitter-powershell
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1055
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Release tag v0.26.4 commits generated src/parser.c, so no generate-then-build workaround needed (unlike tree-sitter-swift). Merged (#1055). Issue #1066 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: cadquery-ocp
  version: 7.9.3.1.1
  home: https://pypi.org/project/cadquery-ocp/
  repo: https://github.com/CadQuery/ocp-build-system
  status: blocked-on-dependency
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Upstream located: the cadquery-ocp *distribution* is built by CadQuery/ocp-build-system (tag v7.9.3.1.1: .github/workflows/build-ocp.yml plus .github/actions/{build-sdks,build-ocp-source,build-ocp,test-ocp}), not by CadQuery/OCP, which only publishes conda packages and pre-generated sources - hence the empty project_urls on PyPI. Neither code generation nor build time is the problem. Generation: with PYWRAP=false the recipe simply downloads the CadQuery/OCP release asset 7.9.3.1.1/OCP_src_stubs_Linux.zip (6.3 MB -> 639 .cpp / 30.2 MB of generated pybind11, plus 325 .pyi for the stubs wheel), which is architecture-independent, so no pywrap/libclang/micromamba generation step is needed for riscv64. Cost, measured locally on a 4-core x86 (gcc 13): OCCT V7_9_3 configures clean (5,618 ninja steps) and did 1,103 of them in 600 s at -j4 => ~51 min wall / ~3.4 core-h for the whole kernel, and the generated OCP TUs cost 11.0 s (Adaptor2d), 12.9 s (BRepPrimAPI), 15.1 s (TopoDS) and 89.3 s (gp, 538 KB) each => ~2.7 core-h for all 639; scaling by this repo''s own vtk datapoint (12,192 ninja steps ~= 18 h wall at 4 cores on ubuntu-24.04-riscv) puts riscv64 at roughly 5-7 h for OCCT plus 4-6 h per interpreter for OCP, i.e. 10-15 h jobs - inside the 48 h timeout build-vtk.yml already uses, and OCCT is Python-independent so gotcha 15 lets it be built once per version. THE BLOCKER (recorded as gotcha 399): the wheel needs a riscv64 VTK *SDK* and nothing provides one. OCP''s generated CMakeLists.txt opens with find_package(VTK REQUIRED COMPONENTS WrappingPythonCore RenderingCore RenderingOpenGL2 CommonDataModel CommonExecutionModel freetype), and OCCT''s USE_VTK=ON needs 3RDPARTY_VTK_INCLUDE_DIR/3RDPARTY_VTK_LIBRARY_DIR; upstream satisfies the header half with "micromamba install vtk=9.6.2" plus a hand-written cmake/vtk-config-linux.cmake, and the library half by copying vtkmodules/libvtk*.so out of the PyPI vtk wheel. On riscv64 the library half is fine and the header half has no source at all: conda-forge''s linux-riscv64 subdir carries 1,970 packages with 0 vtk and 0 occt (verified by reading repodata.json per gotcha 42; linux-64 has 3,221 vtk), PyPI publishes no riscv64 vtk for any version (9.6.2 is x86_64/aarch64/macos/win only), and our own vtk-9.7.0-cp313-cp313-manylinux_2_39_riscv64.whl holds 197 libvtk*.so but 0 .h and 0 cmake files, because VTK''s wheel setup.py packages only vtkmodules/ even though build-vtk.yml configures with VTK_INSTALL_SDK=ON. Two further walls stack on that: the wheel metadata pins vtk==9.6.2 exactly while our registry has 9.7.0 only, so the pin would have to be diverged from upstream''s and the SDK matched to 9.7.0, whose IVtk compatibility with OCCT 7.9.3 nobody has tested; and our vtk wheels start at cp312, so cp310/cp311 can never be covered (gotchas 67/84). Do NOT work around this by building cadquery-ocp with USE_VTK=OFF under the cadquery-ocp name: cadquery/occ_impl/shapes.py imports OCP.IVtkOCC and OCP.IVtkVTK for toVtkPolyData, so the four IVtk* TUs are load-bearing for the ecosystem, and the vtk== dependency is part of what the name promises. Resume plan, in order: (1) do the sibling queue entry cadquery-ocp-novtk first - same build with USE_VTK=OFF plus upstream''s sed dropping OCP/IVtk* and the find_package(VTK) block, its only dependency is cadquery-ocp-proxy==7.9.3.1.1 (a 3.3 KB py3-none-any wheel already on PyPI, nothing to port), so it needs no VTK whatsoever and can cover all of cp310-cp314, and it proves OCCT-on-riscv64, the 639-TU OCP compile and the pypi/ packaging path (ocp-tree.py, auditwheel repair, patchelf --add-rpath) at a fraction of the risk (gotcha 382: several distributions, one build); (2) then extend build-vtk.yml to publish a VTK SDK (headers + a vtk-config.cmake) alongside its wheels, and only then return here. Groundwork already done for whoever resumes: Rocky 10 riscv64 has freetype-devel, fontconfig-devel, libXcursor-devel, tbb-devel and ninja-build (crb) and already carries mesa-libGL-devel/libX11-devel/libglvnd-devel, but has NO rapidjson-devel, freeimage-devel or fmt-devel - rapidjson and fmt are header-only (fetch from source; OCP pulls fmt in only via OCP_specific.inc under -DFMT_HEADER_ONLY) and USE_FREEIMAGE has to go OFF; the image is gcc 14.3.1 + cmake 4.4.3 so OCCT needs -DCMAKE_POLICY_VERSION_MINIMUM=3.5 (upstream already passes it, gotchas 257/358); OCP''s CMakeLists find_package(Python REQUIRED COMPONENTS Interpreter Development) must be patched to Development.Module (gotcha 374); and upstream''s _GLIBCXX_USE_CXX11_ABI=0/1 switch is an artifact of the x86_64 PyPI vtk wheel only - our riscv64 vtk wheel is built in the manylinux image, so the default new ABI applies. Local validation: inside quay.io/pypa/manylinux_2_39_riscv64 under QEMU, dnf repoquery settled the package availability above (with the proxy CA appended per gotcha 384) and OCCT V7_9_3''s own cmake configure SUCCEEDED on riscv64 - rc=0, 74.2 s configuring + 18.7 s generating, Xlib and FreeType 2.13.2 both detected, "TKIVtk and TKIVtkDraw toolkits excluded due to VTK usage is disabled", and the same 5,618 ninja steps as on x86; ninja then accepted that graph and began compiling, but no edge finished inside the window (QEMU on a 4-core host also running a dozen other agents'' riscv64 containers), so the emulated run proves configure + graph validity only and every throughput figure above comes from native x86. Still blocked after the cadquery-ocp-novtk port (PR https://github.com/riseproject-dev/python-wheels/pull/2114), but the remaining gap is narrower than recorded above: at v8.0.1.0.0 upstream''s build-vtk-sdk action assembles the SDK from a hand-written cmake/vtk-config-linux.cmake kept in ocp-build-system (only the version numbers are sed''ed into it) plus conda''s include tree and the PyPI wheel''s libvtk*.so, so on riscv64 only VTK''s *include* tree is missing - and build-vtk.yml already configures with VTK_INSTALL_SDK=ON, so publishing that install include/ directory as an artifact (or a second wheel) is the whole fix. The vtk==9.6.2 pin against our 9.7.0 and the cp310/cp311 coverage gap are unchanged.'
- pkg: databento-dbn
  version: 0.68.0
  home: https://databento.com
  repo: https://github.com/databento/dbn
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1801
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Maturin/PyO3 extension-module build (python/Cargo.toml, module databento_dbn._lib), pyo3 0.29.2 with no abi3 feature, so per-interpreter builds are needed matching upstream''s own cp310-cp314 matrix (no cp314t: upstream ships none). No runtime dependencies, no Python test suite upstream (scripts/test.sh is cargo test only); wheel test is a smoke test against real pyo3 classes (TradeMsg round-trip). License Apache-2.0, SPDX expression only, no LICENSE file bundled in the real PyPI wheel. PR #1801 opened, CI dispatched on the PR branch (5-way cp310-cp314 matrix).'
- pkg: stpyv8
  version: 13.1.201.22
  home: https://github.com/cloudflare/stpyv8
  repo: https://github.com/cloudflare/stpyv8
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2121
  notes: 'Builds V8 13.1.201.22 from source with a from-source riscv64-linux-gnu- gcc toolchain, wrapped in the Boost.Python extension stpyv8 ships (mirroring upstream linux.yml, split into build_v8/build_wheel jobs so the ~48h V8 build runs once per version). Two independent patches: DEPS drops two V8 CIPD packages (reclient, siso) with no linux-riscv64 build, and gn is routed at V8''s existing gcc_toolchain("riscv64") since the prebuilt clang has no riscv64 host build. Boost bumped to 1.90.0 (upstream 1.87.0) for cp314 support; matrix narrowed to cp312/cp313/cp314 since Boost.Python links a version-specific CPython ABI and has no free-threaded build. PR #2121 opened, CI running. Round 4 (run 35479941332, job 105995838025) was the first to get past gclient sync into the compiler, and it is the reason this port now has a patch 0003. It ran 00:54-10:49 UTC and failed 9h17m into ninja at 1146/2117 targets -- not a wall of any kind: 48h timeout under a fifth spent, exactly one FAILED edge in the whole 168 KB log, and no Killed / out of memory / ICE / ENOSPC / signal anywhere (checked with the same signatures the paddlepaddle investigation used). The one failure is a real GCC-vs-clang source incompatibility, new gotcha 447: src/codegen/riscv/macro-assembler-riscv.h declares MacroAssembler::{push,pop}_helper''s one-register base case as an explicit specialization inside the class body, which only clang accepts ("explicit specialization in non-namespace scope"), and V8 builds its RISC-V port with clang only. baseline.o is simply the first translation unit that pulls in macro-assembler.h, so the 1145 targets before it prove nothing about the rest. Fixed by backporting V8 13.8''s own fix (an empty zero-register overload; 13.2-13.7 all still carry the bug), dry-run against the real pinned header so the rewrite reproduces 13.8''s text byte for byte, with the old and new forms compiled under both g++ and clang++ to confirm the divergence and that the offsets are unchanged. Confidence that this is the only blocker left: the two further GCC stoppers mini-racer''s V8 build hit both postdate this V8 (no WriteLeadingAscii in this unicode.h, no highway in this BUILD.gn), the warning class is already disarmed (settings.py declares treat_warnings_as_errors=false; no -Werror anywhere in the log), and a grep over all nine riscv source directories at the tag found no other in-class specialization. Also passes ninja -k 1000 on riscv64 (mini-racer''s tactic) so the next run reports every remaining GCC-only failure at once rather than one per ~20h; drop it once green. Expect a full build around 25-30h on these 4-core runners (mini-racer measured 1970/2176 at 23h30m), which the existing 2880-minute timeout covers. Round 5 (run 35509203151) was the first with a green build_v8 -- the monolith compiled in 23h10m (2026-09-20 12:01 to 2026-09-21 11:10 UTC) and uploaded a 47 MB artifact -- and then failed all three build_wheel jobs identically, six minutes into the extension build, at the final link: /usr/bin/ld: cannot find -lboost_system. Not an ABI or artifact problem: all nine stpyv8 translation units compiled, and -lv8_monolith and -lboost_python312 both resolved off the downloaded artifact, so the V8 half and the Boost.Python half are wired up correctly; boost_system is simply the one library that no longer exists. Boost.System has been header-only since Boost 1.69, and 1.89 removed the stub compiled library that had remained for link-line compatibility ("The stub compiled library has been removed ... The easiest fix is to just remove system from the list of components", version_1_89_0.html); boostorg/system carries neither build/Jamfile.v2 nor src/ at boost-1.89.0 or boost-1.90.0, so b2 --with-system builds nothing. settings.py hardcodes "boost_system" in its posix libraries list, and upstream never meets this because linux.yml pins Boost 1.87.0 -- this port is on 1.90.0 only because 1.87.0 predates CPython 3.14. New patch 0004 drops the entry (Upstream-Status: To upstream). Verified past the release note: all four patches apply clean in glob order on a fresh v13.1.201.22 checkout, the patched settings.py yields libraries=[boost_iostreams, boost_filesystem, v8_monolith, boost_python3XX, rt], setup.py still imports and runs, stpyv8''s own sources include no boost/system header and name no boost::system symbol, and nm over the real riscv64 libv8_monolith.a from the green run (198 MB, 128814 defined symbols) leaves exactly 358 external symbols -- every one of them libc/libm/libstdc++/libgcc/pthread, no boost, no __atomic_* libcall needing -latomic, no ICU or zlib externals -- so the link line minus -lboost_system resolves. Two supporting changes rode along: build_wheel had no patch step at all (only build_v8 checked out python-wheels and git-applied), so it now mirrors build_v8 -- 0001-0003 only touch the checkout_v8/build_v8 path that --skip-build-v8 never reaches, making the whole set a no-op there beyond 0004; and the actions/cache around the V8 build that upstream''s linux.yml has and this port had dropped is restored, over exactly the three artifact paths, keyed stpyv8-v8-<version>-riscv64-<hashFiles of patches/stpyv8/**> (upstream keys on hashFiles(''v8/src/**''), a tree that only exists once the build has run). Without it every wheel-side iteration repeats the whole 23h monolith build. Round 6 = run 35608199912 on a894d4b751, started 2026-09-21 13:50 UTC; it is a cache miss by construction, so it pays the V8 build once more and the wheels land roughly 24h later. Left for the pre-merge cleanup: patch 0003 still passes ninja -k 1000, which its own commit message says to drop once green -- doing that now changes the cache key and costs another 23h. Round 7 (commit b6f5f4c2ee) fixed the two remaining items: patch 0005 branches tests/test_Wrapper.py::TestWrapper::testReferenceCount''s sys.getrefcount() baselines on sys.version_info >= (3,14) -- CPython 3.14''s LOAD_FAST_BORROW opcode skips an incref at both call sites here, so the previously-hardcoded 2/4 baselines are one lower on 3.14+, cp312/cp313 unaffected -- and build-stpyv8.yml''s build_v8 cache key now hashes only patches 0001-0003 (the ones that actually touch checkout_v8/build_v8) instead of the whole patches/ tree, so a future wheel-only or test-only patch no longer forces a ~24h V8 rebuild. This round paid one last cache-key-format-change rebuild (2026-09-24 03:49 to 2026-09-25 03:20 UTC, ~23h30m) and all three build_wheel legs (cp312/cp313/cp314) plus publish came back green. PR #2121 is fully green; left open for human review.'
- pkg: jsonnet
  version: 0.22.0
  home: https://jsonnet.org
  repo: https://github.com/google/jsonnet
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1796
  notes: '4 Linux wheels upstream (abi: cp314,cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sherpa-onnx
  version: 1.13.6
  home: https://github.com/k2-fsa/sherpa-onnx
  repo: https://github.com/k2-fsa/sherpa-onnx
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1802
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1802 open, CI running (4-way matrix cp312/cp313/cp314/cp314t). Upstream already publishes a prebuilt riscv64 onnxruntime binary (csukuangfj/onnxruntime-libs) via CMake FetchContent, so no from-source onnxruntime build is needed. Building the non-split (SHERPA_ONNX_SPLIT_PYTHON_PACKAGE unset) self-contained wheel shape upstream''s own cmake_extension.py still supports, since the split shape upstream now publishes to PyPI depends at runtime on sherpa-onnx-core, which has no riscv64 wheel or sdist (separate queued pkg). Vendors espeak-ng (GPL-3.0) for TTS phonemization plus ~12 small permissively-licensed C++ deps fetched by CMake at build time; licences staged at checkout root for setuptools'' default glob.'
- pkg: hogql-parser-rs
  version: 1.3.109
  home: https://github.com/PostHog/posthog
  repo: https://github.com/PostHog/posthog
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1804
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin crate at rust/hogql/parser inside the posthog monorepo Cargo workspace (rust/Cargo.toml, ~80 members); no git tag, so pinned to commit c30d27369 (the merge that bumped Cargo.toml/pyproject.toml to 1.3.109, matching current master). PR #1804 open, CI running (single cp312-abi3 wheel, sparse-checkout of rust/ only).'
- pkg: pystemmer
  version: 3.1.0
  home: https://github.com/snowballstem/pystemmer/
  repo: https://github.com/snowballstem/pystemmer/
  status: published
  pr: 1096
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython extension wrapping libstemmer_c, downloaded and sha256-checked by setup.py at build time; no pyproject.toml, so Cython is preinstalled and built with --no-build-isolation. Worktree/branch pystemmer. PR https://github.com/riseproject-dev/python-wheels/pull/1096, CI running. Merged (#1096). Issue #1147 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: pixelhog
  version: 1.2.0
  home: https://github.com/PostHog/pixelhog
  repo: https://github.com/PostHog/pixelhog
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1877
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev. Standalone PyO3/maturin repo (not the posthog monorepo); pyo3 carries abi3-py312 unconditionally, so one cp312-abi3 wheel matches upstream. Pinned to tag v1.2.0. PR #1877 open, CI dispatched.'
- pkg: deltalite
  version: 0.1.7
  home: https://github.com/PostHog/posthog
  repo: https://github.com/PostHog/posthog
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1805
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin crate at rust/deltalite/python inside the posthog monorepo Cargo workspace (rust/Cargo.toml); no git tag, so pinned to commit f2583328 (the merge that bumped Cargo.toml/pyproject.toml to 0.1.7, verified byte-identical to the PyPI sdist). PR #1805 open, CI green (single cp312-abi3 wheel, sparse-checkout of rust/ + LICENSE only). Test wheel step drops upstreams deltalake/pyarrow/duckdb parity suite: uv (0.12.13, riscv64) rejects deltalake==1.6.3s only wheel (cp310-abi3) as unusable when resolved by name from any index (gotcha 375); runs test_planner.py only (17/18 tests, one deselected for the same reason) plus the wheel-install + license check.'
- pkg: aiocsv
  version: 1.4.1
  home: https://github.com/MKuranowski/aiocsv
  repo: https://github.com/MKuranowski/aiocsv
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1387
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xformers
  version: 0.0.35
  home: https://facebookresearch.github.io/xformers/
  repo: https://github.com/facebookresearch/xformers
  status: parked
  pr: null
  notes: 'Parked: the riscv64 build succeeds and the wheel it produces is functionally empty
    -- new gotcha 415, disposition per gotcha 183 (installable and importable, primary function
    unreachable rather than degraded). 0.0.35 confirmed latest (PyPI info.version; upstream''s
    newest tag is v0.0.35); repo field corrected to github.com/facebookresearch/xformers.
    WHAT THE ONE py39 WHEEL IS: xformers-0.0.35-py39-none-manylinux_2_28_x86_64.whl (3.2 MB)
    is not gotcha 27''s cosmetic tag -- it holds a real 11.6 MB xformers/_C.so built with CUDA
    12.8 against torch 2.10.0+cu128 (xformers/cpp_lib.json), NEEDED libcudart.so.12 +
    libc10_cuda.so + libtorch_cuda.so. The py39-none tag is upstream''s own
    bdist_wheel_abi_none.get_tag() returning ("py39", "none", plat): the .so talks only to
    torch''s stable ABI (STABLE_TORCH_LIBRARY_FRAGMENT, the build_ext get_export_symbols()
    override returning [], no PyInit_), so one wheel covers every CPython 3.9+ including
    free-threaded ("All builds are python-version agnostic" in .github/workflows/wheels.yml).
    NOT CUDA-BLOCKED AT BUILD TIME (unlike humming-kernels / gotcha 386): setup.py
    get_extensions() sets extension = CppExtension and only promotes it to CUDAExtension when
    (torch.cuda.is_available() and CUDA_HOME and torch.version.cuda) or FORCE_CUDA=1 or
    TORCH_CUDA_ARCH_LIST != "", with the HIP branch behind torch.version.hip -- so gotcha
    411''s bitsandbytes test passes. Verified both ways: pip wheel . --no-deps
    --no-build-isolation against torch 2.14.0+cpu built a py39-none-linux_x86_64 wheel in
    seconds, and the same build was rehearsed under QEMU in
    quay.io/pypa/manylinux_2_39_riscv64 (cp312) with torch 2.14.0+cpu from pypi.riseproject.dev,
    producing xformers-0.0.35-py39-none-linux_riscv64.whl with no patches and no toolkit.
    THE BLOCKER IS WHAT THAT BUILD CONTAINS: the non-CUDA source list is
    xformers/csrc/**/*.cpp minus hip_fmha = exactly 2 files / 44 lines
    (attention/attention.cpp, sparse24/sparse24.cpp), all m.def() schema declarations, and
    attention.cpp''s entire body sits inside #if defined(USE_ROCM) so it contributes nothing;
    every m.impl lives in a .cu (or HIP .cpp) that only the CUDA/ROCm branches compile. The
    riscv64 _C.so is 223 KB with 2 defined dynamic symbols (x86 CPU build: 233 KB, 5), none of
    them an operator, against 11.6 MB upstream. Runtime proof, on the riscv64 wheel itself
    installed under QEMU (and identically on the x86 one): import xformers works and
    xformers._has_cpp_library is True (it only checks that the .so
    loaded), but torch.ops.xformers.sparse24_largest_mask_2d raises NotImplementedError "Could
    not run ... with arguments from the ''CPU'' backend" and
    xformers.ops.memory_efficient_attention raises "No operator found ... device=cpu
    (supported: {''cuda''})" -- every fmha op class (cutlass, cutlass_blackwell, flash, flash3,
    triton_splitk, ck, ck_splitk) declares SUPPORTED_DEVICES = {"cuda"}, and
    _is_triton_available() returns False without torch.cuda. Only the pure-Python helpers
    (checkpoint, attn_bias builders, unbind) work, and none of them touch the compiled half.
    UPSTREAM SHIPS NO CPU WHEEL ANYWHERE: wheels.yml''s target determinator emits only
    toolkit_type cuda (cu126/cu128/cu130) and rocm 7.1 and upload_pip filters
    *torch2.10.0+cu128*, so a CPU-only wheel is an artifact upstream builds on no platform
    (gotcha 24''s divergence test) -- the opposite of bitsandbytes, whose CPU wheel already
    exists for macOS/win-arm. Nothing to test with either (gotcha 6): the sdist''s tests/ omits
    tests/utils.py and the xformers.components package, so 12 of 20 test modules fail
    collection even on x86 CPU, and the rest is @cuda_only / compute_capability-gated.
    Dependencies are not the problem (torch>=2.10, numpy; the registry serves torch
    2.13.0+cpu and 2.14.0+cpu for cp312-cp314t). No workflow, docs/packages entry, patches or
    PR created. Revisit only if upstream grows a real CPU backend (a CPU m.impl, or an
    eager/torch-SDPA fallback op whose SUPPORTED_DEVICES includes "cpu"), or if a
    CUDA-enabled riscv64 torch ever exists.'
- pkg: mitmproxy-linux
  version: 0.12.11
  home: https://mitmproxy.github.io/mitmproxy_rs/
  repo: https://github.com/mitmproxy/mitmproxy_rs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2119
  notes: 'Upstream located: mitmproxy/mitmproxy_rs (same Cargo workspace as the already-published mitmproxy-rs), member crate mitmproxy-linux/. Not a closed-binary redistribution -- it is a maturin `bindings = "bin"` project (gotcha 117): the wheel is one compiled executable, mitmproxy-linux-redirector, plus a 40-line mitmproxy_linux/__init__.py that only returns its path, which is why PyPI shows py3-none-<platform> and no project URL. Checkout layout follows gotcha 239: mitmproxy-linux/pyproject.toml exists in the git tree (no manifest-path key) but the PyPI sdist hoists it to the root, so the workflow builds with --manifest-path mitmproxy-linux/Cargo.toml. The interesting part is the eBPF half: build.rs uses aya-build to cross-compile mitmproxy-linux-ebpf to bpfel-unknown-none, which needs a nightly toolchain with rust-src plus the bpf-linker binary -- installed in-container in before-script-linux, mirroring upstream .github/actions/setup. No riscv64 wall there: bpf-linker 0.9.15 defaults to rust-llvm-21/aya-rustc-llvm-proxy (no system LLVM, it dlopens rustc''s own), rustup ships a nightly riscv64gc-unknown-linux-gnu host toolchain, and aya-build explicitly lists riscv64 as a bpf_target_arch (with a riscv64gc -> riscv64 fixup). Validated locally before pushing: the exact recipe (stable+nightly+rust-src, cargo install --locked bpf-linker@0.9.15, maturin build --release --locked) built a correct mitmproxy_linux-0.12.11-py3-none-manylinux_2_34_x86_64.whl natively in manylinux_2_34_x86_64 in ~20 min; the embedded eBPF object and its cgroup/sock_create program were confirmed present and byte-shape-identical to upstream''s published x86_64 wheel. A native riscv64 rehearsal under qemu (manylinux_2_39_riscv64, gotcha 188 LTO override) was also run. LICENSE staged into mitmproxy-linux/ so maturin bundles it -- without it the wheel ships no licence text (upstream''s own aarch64 wheel does not). Test job cannot run the redirector end to end (needs root + tun + cgroup eBPF attach; upstream gates that behind its own root-tests feature), so it asserts the riscv64 ELF carries an EM_BPF object with the cgroup/sock_create section and INTERCEPT_CONF map, then that the binary''s usage path works. Follow-up once published: build-mitmproxy-rs.yml currently passes --features docs to skip lib.rs''s eager `import mitmproxy_linux`; with a riscv64 mitmproxy_linux on the registry that workaround (and the PIP_NO_DEPS=1 test env) can be dropped. PR #2119 opened, CI running. CI disproved the eBPF assumption above: the first riscv64 run failed in build.rs with bpf-linker aborting (SIGABRT) in aya-rustc-llvm-proxy, "unable to find LLVM shared lib" -- it dlopens a libLLVM* shared object found via LD_LIBRARY_PATH or a PATH entry sibling lib/, and the riscv64 rustc dist ships none (LLVM is static inside librustc_driver with its C API hidden), where x86_64 and aarch64 carry libLLVM-<major>-rust-*.so; that is also why the x86_64 rehearsal passed, and the qemu riscv64 rehearsal never reached the link step (its dist/ stayed empty). Rebuilding bpf-linker against a system LLVM does not substitute: the newest LLVM packaged for riscv64 is 21.1.8 (Rocky 10.2 AppStream) while this workspace MSRV of 1.95 is the Rust release that moved to LLVM 22, and an LLVM 21 bpf-linker rejects newer bitcode with "ERROR llvm: Invalid record". Fixed in the PR by cross-compiling the object on ubuntu-latest with bpf_target_arch="riscv64" and embedding it in the riscv64 build through patch 0001 (gotcha 425); the riscv64 job now installs neither a nightly toolchain nor bpf-linker. Rehearsed end to end on x86_64 under Rocky 10 with bpf-linker removed from PATH: patch applied, object staged, maturin wheel built, and every assertion the test job makes passing. Watching CI. CI green on run 35479245158: eBPF cross-compile 91s, riscv64 wheel built and uploaded (mitmproxy_linux-0.12.11-py3-none-manylinux_2_39_riscv64.whl), all four interpreters (3.12/3.13/3.14/3.14t) passing, publish dry-run clean. Wheel verified independently from the artifact: EM_RISCV redirector carrying the embedded EM_BPF object with cgroup/sock_create, BTF and the licence. PR ready for review.'
- pkg: tree-sitter-lua
  version: 0.5.0
  home: https://github.com/tree-sitter-grammars/tree-sitter-lua
  repo: https://github.com/tree-sitter-grammars/tree-sitter-lua
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1091
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.5.0 commits generated src/parser.c (byte-identical to the PyPI sdist), so this uses the simple single-job build-from-checkout pattern (like tree-sitter-kotlin/scala/json), not the swift-style generate_parser workaround. PR opened (#1091), CI running. Merged (#1091). Issue #1122 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: coreforecast
  version: 0.0.18
  home: https://nixtla.github.io/coreforecast
  repo: https://github.com/Nixtla/coreforecast
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1795
  notes: 'scikit-build-core + pybind11 build against vendored header-only C++ deps (eigen, skiplist, stl-cpp) checked out as git submodules; no system dependencies. Per-interpreter ABI (no abi3). cp314t builds but skips tests: pandas, a hard test dependency in most test modules, has no riscv64 wheel for cp314t on pypi.riseproject.dev yet. PR #1795 opened, CI running.'
- pkg: paddlepaddle
  version: 3.3.1
  home: https://www.paddlepaddle.org.cn/
  repo: https://github.com/PaddlePaddle/Paddle
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2120
  notes: 'PR #2120 open (draft). No CI result yet and no local rehearsal was possible, so the build is entirely unvalidated - the first riscv64 run is what has to answer whether it fits. 3.3.1 confirmed latest on PyPI (3.4.0 exists only on paddlepaddle.org.cn/packages); wheel version comes straight from $PADDLE_VERSION, which cmake/version.cmake reads before falling back to version.txt (still 3.3.0 at the v3.3.1 tag). Not architecturally blocked, and the CUDA/xbyak reputation does not survive reading the build: WITH_GPU defaults to ${CUDA_FOUND}, WITH_MKL and WITH_AVX to ${AVX_FOUND}, WITH_ONEDNN needs AVX2, WITH_CINN/WITH_OPENVINO/WITH_DISTRIBUTE are OFF, and the three kernels/fusion/cpu/*avx* sources are removed from the source list unless AVX512F_FOUND - so on riscv64 every x86-specific component switches itself off except WITH_XBYAK, which upstream''s own WITH_ARM/WITH_SW/WITH_MIPS/WITH_LOONGARCH blocks already force OFF and route through the scalar jit/refer kernels. Upstream publishes linux_aarch64 wheels (cp39-cp313, on its own index, not PyPI) built exactly that way, so the non-x86 CPU path is real and maintained. The whole arch-gate surface is five files: cmake/flags.cmake''s unconditional -m64 and four #if blocks that select x86 intrinsics by listing the non-x86 architectures Paddle knows (phi/backends/cpu/cpu_info.{h,cc}, phi/core/platform/denormal.cc, phi/kernels/funcs/search_compute.h); everything else is already correct (phi/common/float16.h and both spin_lock.h test __x86_64__ directly). Zero ''riscv'' strings anywhere in the v3.3.1 tree, so this is entirely new territory upstream - but community members have built Paddle natively on riscv64 before and hit only ordinary problems (Paddle#61770 warnings-as-errors on 2.4.2, Paddle#62037 ''undefined symbol: __atomic_exchange_1'', i.e. missing -latomic for sub-word atomics, which patch 0001 adds up front). Real finding worth the port on its own (new gotcha 418): cmake/external/lapack.cmake downloads ONE prebuilt tarball for all of Linux, lapack_lnx_v3.10.0.20210628.tar.gz, and it is x86-64 only; setup.py then copies LAPACK_LIB/BLAS_LIB/GFORTRAN_LIB/GNU_RT_LIB_1 into paddle/libs/ unconditionally, so upstream''s released paddlepaddle-3.3.1-cp312-cp312-linux_aarch64.whl ships x86-64 liblapack.so.3, libblas.so.3, libgfortran.so.3 and libquadmath.so.0 next to a genuinely aarch64 libopenblas.so.0 (confirmed by downloading it and running file on the extracted libs). Nothing links against them - phi/backends/dynload/lapack.cc dlopens liblapack.so.3 - so the build stays green and only paddle.linalg fails at runtime. Patch 0003 builds the same Reference-LAPACK v3.10.0 from source instead, gated on WITH_RISCV, using the image''s gfortran 14.3.1. Three patches total, all verified to apply cleanly to a pristine v3.3.1 checkout: 0001 adds WITH_RISCV (mirrors the WITH_LOONGARCH block, plus -latomic, no -m64, WITH_SLEEF=OFF since Paddle only reaches SLEEF through AVX paths), 0002 adds PADDLE_WITH_RISCV to the four gates, 0003 the LAPACK source build. Workflow drives the manylinux_2_39_riscv64 container directly like build-vtk.yml (Paddle has no wheel workflow on GitHub; ci/run_setup.sh is the script its own CI drives), exports the CPU WITH_* set plus WITH_SHARED_PHI/WITH_SHARED_IR=ON to match the released wheels'' libphi_core.so/libpir.so split, then setup.py bdist_wheel + auditwheel repair; 10GB swap and timeout-minutes 2880 as build-vtk.yml needs on these 4-core runners. Matrix is cp312/cp313 only - upstream ships cp39-cp313 for 3.3.1, so there is no cp314 counterpart. gpl_sources job (packages: gcc) because setup.py copies the image''s libgfortran.so.5/libquadmath.so.0 into paddle/libs/. Runtime deps all resolve: numpy/Pillow/protobuf on pypi.riseproject.dev, safetensors riscv64 on PyPI, rest pure-Python. Test is paddle.utils.run_check() (upstream''s documented post-install check) plus a QR round-trip to exercise the new LAPACK. Main open risk is cost, not architecture: 1.3M lines of heavily templated C++ plus the third_party world, per interpreter, on 4-core riscv64 runners - VTK at comparable scale needed ~18h+ per leg against a 48h budget, and Paddle''s phi kernels are far more template-heavy, so if the first run does not fit 2880 minutes this becomes a park-as-disproportionate candidate alongside ortools/mediapipe. Secondary unknowns, all discoverable only on real hardware: OpenBLAS 0.3.28''s riscv64 auto-detect (no TARGET= passed, matching upstream), cryptopp/gloo/warpctc on riscv64, CMake 4.4.3 policy floors in third-party projects Paddle does not already pass CMAKE_POLICY_VERSION_MINIMUM=3.5 to, and the final link of libpaddle.so (193MB on aarch64) within the runner''s RAM. Worktree .claude/worktrees/agent-a77323204675e1c48, branch port/paddlepaddle. First CI run (35468812304): the cp312 leg failed after 42min in the build container''s pip step, not in the C++ build at all - PIP_EXTRA_INDEX_URL was wired into the test podman run only, so python/requirements.txt resolved against public PyPI, numpy source-built for 21min and Pillow then died with RequiredDependencyException: jpeg (no libjpeg headers in the manylinux image); the cp313 leg was still inside the same numpy build. Fixed on the branch by passing PIP_EXTRA_INDEX_URL plus PIP_ONLY_BINARY=numpy,pillow to the build run as well (gotcha 422), rehearsed off-target with pip --dry-run --report for cp312 and cp313: every requirement now resolves to a wheel (numpy/protobuf/pillow ours, safetensors upstream''s riscv64, rest pure-Python). Second finding, unrelated to the failure (gotcha 421): the 10GB swap step is a verified no-op on these runners - swapon fails with EINVAL because / is overlayfs, and the action soft-passes - so the link budget is the runner''s 15GB of RAM, in this job and in every other workflow copying that step. Timeout question is still unanswered: the first run never reached cmake. Second CI run (35477018075) with that fix in: pip now resolves every requirement to a wheel in ~13min with no source build, and the cp313 leg instead died 23min in during cmake configure, still before compiling anything - "Could NOT find PythonLibs (missing: PYTHON_LIBRARIES PYTHON_INCLUDE_DIRS) (Required is at least version 3.13)" at cmake/external/python.cmake:21 - because manylinux configures every interpreter it ships with --disable-shared (pypa/manylinux build-cpython.sh), so /opt/python/cp313-cp313 has Python.h and a libpython3.13.a but no libpython3.13.so, and the deprecated FindPythonLibs insists on a linkable library. Patch 0004 requires only the headers on non-Windows, taken from PYTHON_EXECUTABLE''s own sysconfig INCLUDEPY, and leaves the library lookup optional; nothing in this configuration ever linked libpython anyway, because cmake/generic.cmake''s cc_library() already strips `python` off every non-Windows target''s link line and links -Wl,-undefined,dynamic_lookup instead (pybind11''s guidance), leaving PYTHON_LIBRARIES to the cc_test() executables (WITH_TESTING=OFF) and two dead variables. New gotcha 428 records the whole shape, including why the obvious repair - pointing PYTHON_LIBRARY at the static archive - would have configured fine and then failed at the final link hours later, since CPython adds -fPIC only when LIBRARY != LDLIBRARY and so a --disable-shared libpython3.13.a holds no PIC. Rehearsed locally on x86_64 with cmake against the patched file in both branches it can take (distro libpython found, and -DCMAKE_DISABLE_FIND_PACKAGE_PythonLibs=TRUE for the manylinux case): configure and generate both succeed, `python` becomes an INTERFACE target cc_library()''s add_dependencies() edge still accepts, and a .so built that way links with the Py_* symbols undefined and no libpython in DT_NEEDED. The "error: pathspec v0.3.28 did not match any file(s) known to git" line in the same log was read as a red herring; it was the next bug (see run 3). Timeout question remains open after two runs: neither has reached the C++ build. UPDATE after run 35480013120 (round 3, both legs red, 31-40 min each): not the memory wall, two ordinary third_party bugs, both fixed on the branch. No OOM, no disk-full and no timeout evidence anywhere in either log - the only errors are getarch.c''s #error and a cmake configure failure, both in third_party, with the C++ tree still untouched. (1) extern_lapack failed its cmake configure step on both legs (cp313 stopped there first, which is why cp313 never reached openblas). The diagnostic is unrecoverable: Paddle''s EXTERNAL_PROJECT_LOG_ARGS sets LOG_CONFIGURE 1, so the job printed only "Command failed: 1" and the path of a stamp log that died with the runner (new gotcha 434; the workflow now tails those logs on failure). Patch 0003 therefore drops the Reference-LAPACK source build entirely and resolves liblapack.so.3, libblas.so.3, libgfortran.so.5 and libquadmath.so.0 out of the build image with ${CMAKE_C_COMPILER} -print-file-name=, FATAL_ERROR if one is not absolute, after a dnf install -y lapack blas libgfortran libquadmath - Rocky 10 riscv64 already ships exactly the sonames phi/backends/dynload/lapack.cc dlopens, nothing links against them, and build-casadi.yml/build-mlx.yml prove those packages install in this image. That removes a Fortran ExternalProject from a 4-core runner and fails in the first configure minute instead of at minute 33. Gotcha 418''s last bullet corrected accordingly. (2) extern_openblas then stopped at getarch.c:1193 "This arch/CPU is not supported by OpenBLAS" - and the run-2 note above had this backwards. The submodule is NOT at v0.3.28: Paddle records third_party/openblas at 5f36f18, whose Makefile.rule says VERSION = 0.3.7 (openblas.cmake''s default CBLAS_TAG; Linux only gets v0.3.28 through a runtime git checkout), and 0.3.7''s getarch.c has no riscv64 target at all - not even FORCE_RISCV64_GENERIC, which is why the #error sits at line 1193 where v0.3.28 has it at 1853. The pathspec error is the cause, not noise: actions/checkout clones submodules without tags, so openblas.cmake''s unchecked `git checkout v0.3.28` cannot succeed and configure proceeds against the 0.3.7 tree (new gotcha 432). The workflow now moves the submodule onto the tag (git fetch --depth 1 origin tag v0.3.28 && git checkout), and new patch 0005 adds the TARGET=RISCV64_GENERIC twin of upstream''s existing WITH_ARM TARGET=ARMV8, because OpenBLAS reaches its riscv64 definitions only via -DFORCE_RISCV64_* from TARGET= and RISCV64_GENERIC is the rv64gc baseline while the ZVL targets would compile RVV into a statically linked libopenblas.a (new gotcha 433). Five patches now, all verified to apply to a pristine v3.3.1 checkout and all green under ci_scripts/check_patch.py; subjects renumbered to n/5. Round 4 pushed as 2e455a23e. NOT parked: the resource wall the port was flagged for has still not been reached, so parking now would be parking on a guess. UPDATE after run 35482612162 (round 4, both legs red in 30 seconds inside the build container): the two round-3 diagnoses were right - the submodule tag move and all five patches applied on both legs - but the dnf line added with them was wrong. "Error: Unable to find a match: libquadmath": libquadmath is built only where __float128 is a type distinct from long double, so GCC ships none for riscv64 (nor aarch64) and Rocky packages none, while libgfortran-14.3.1-4.4.el10.riscv64 was already installed - both additions to `lapack blas` were speculative hardening and both were the only things wrong with the line (new gotcha 435). lapack and blas themselves resolved fine, confirming gotcha 401 for this image. Patch 3/5 also asked for libquadmath.so.0, whose FATAL_ERROR would have stopped the next configure, so it no longer resolves GNU_RT_LIB_1 at all and instead guards setup.py''s unconditional shutil.copy() of it the way the neighbouring GNU_RT_LIB_2 copy already is - architecture-agnostic, since it is the bug wherever there is no libquadmath. Both hunks of patch 3/5 are now generated from the pristine v3.3.1 files with difflib rather than hand-written, after a hand-written setup.py hunk whose context matched byte-for-byte was still rejected by git apply/patch; the patched setup.py parses. Round 5 pushed as ba04cc171. What run 5 answers is whether Paddle''s own phi kernels compile in 15GiB with no swap (gotcha 421) at MAX_JOBS=4; if that is where it dies, that failure is infra, not this PR, and parking is the call then. UPDATE after run 35483780322 (round 5, both legs red at 45-47 min, the furthest any round has got): still not the memory wall, and every round-3 and round-4 fix held - the OpenBLAS submodule tag move, all five patches and the corrected dnf line all worked, pip resolved every requirement to a wheel with no source build, and the build spent ~40 min inside third_party. No OOM, no Killed, no cc1plus out-of-memory, no ENOSPC and no timeout evidence anywhere in either log; the swap step again logged "swapon: /swapfile: swapon failed: Invalid argument" with Swap 0B before and after, confirming gotcha 421 once more. Both legs died at the same place and for the same reason, extern_gloo''s patch step: "error: pathspec ''v0.0.3'' did not match any file(s) known to git", at 6% with Paddle''s own C++ tree still untouched. This is gotcha 432''s cause with the opposite symptom (new gotcha 437): gloo.cmake makes `git checkout ${GLOO_TAG}` an ExternalProject PATCH_COMMAND rather than an unchecked execute_process, so a tagless submodule clone aborts the whole build instead of silently building the recorded commit. third_party/gloo is recorded at 8b6b61d, which is exactly what refs/tags/v0.0.3 names, so nothing needed moving - only the ref was missing. Swept the rest of cmake/external for the same shape rather than fixing one dependency per round, and found one more that this configuration reaches and run 5 had not got to: protobuf.cmake''s build_protobuf() patch step is an unconditional `cd <src> && git checkout v21.12`, and third_party/protobuf is recorded at f0dc78d, which is refs/tags/v21.12^{} - it would have failed identically a few minutes later. Everything else is safe: eigen, warpctc, warprnnt, cccl, libxsmm and the paddle/fluid/fp8 cutlass switch name commits rather than tags, and the tag-name checkouts in pybind11, pocketfft, gtest, cub, openvino and rocksdb are gated on GCC < 9, APPLE, WITH_TESTING or WITH_DISTRIBUTE, CUDA, WITH_OPENVINO and the parameter-server tree respectively, none of which this build reaches. The workflow now fetches both missing tags and checks nothing out itself. Rehearsed locally for both submodules by reproducing exactly what actions/checkout leaves behind - git init, remote add, fetch --depth 1 <recorded sha>, checkout FETCH_HEAD - which fails with run 5''s exact pathspec message, is fixed by the tag fetch, and then checks out to the commit that was already there, so neither submodule''s tree changes. Round 6 pushed as 228cfe516. Still NOT parked: five rounds in, every failure so far has been an ordinary third_party or environment bug with a real fix and none has been a resource wall, the phi-kernel memory question is still unanswered, and this round clears the last tag-name checkout standing between the build and Paddle''s own C++ compile - so round 6 is the first that can actually reach the question this PR exists to answer. UPDATE after run 35487425503 (round 6, both legs red at 2h33m/2h40m - over 3x any previous round, and the first to reach Paddle''s own C++ tree): round 5''s tag fetches held, all of third_party built, and the build got to 21% of phi_core before dying. Still NOT the memory wall, and the question this PR exists to answer is still open: zero OOM evidence in either log - no Killed, no cc1plus out of memory, no virtual memory exhausted, no oom-killer output, no ENOSPC, no ld fatal, and the pre-build probe logged Mem 15Gi total / 14Gi available with Swap 0B (gotcha 421 again, informational only). Both legs failed identically and deterministically on the same translation unit, paddle/phi/kernels/funcs/gru_compute.cc, with eight copies of "error: ''Sleef_sinf1_u35'' was not declared in this scope; did you mean ''Sleef_sinf_u35''?" (plus Sleef_sind1_u10/Sleef_cosf1_u35/Sleef_cosd1_u10) out of activation_functor.h''s Sine/Cosine specialisations, then gmake *** Error 1/2 - an ordinary compile error, not resource exhaustion, so case (b) and a 7th round is evidence-backed rather than speculative. Root cause is our own patch 1/5 (new gotcha 443): its set(WITH_SLEEF OFF ... FORCE) sits in the WITH_RISCV block at CMakeLists.txt:686, but cmake/third_party.cmake is included at line 591, so cmake/sleef.cmake had already run add_definitions(-DPADDLE_WITH_SLEEF) - the configure log says "-- Compile with Sleef support" in plain sight - and the only consumer still reading the FORCE was paddle/phi/CMakeLists.txt''s `if(WITH_SLEEF) list(APPEND PHI_DEPS sleef)`, i.e. the build compiled the SLEEF path and linked no SLEEF, so even a working header would have failed at the final link instead. activation_functor.h gates purely on PADDLE_WITH_SLEEF, not on AVX, so patch 1/5''s original rationale was wrong on that point too. Turning SLEEF on is not the alternative: SLEEF 3.6.1''s src/libm/CMakeLists.txt omits DSP_SCALAR from SLEEF_ARCH_RISCV64''s header list, alone among its six architectures, and DSP_SCALAR is the only entry with no ISA-name argv, which mkrename.c uses as the name suffix - so the generated riscv64 sleef.h declares Sleef_sinf1_u35purec and never Sleef_sinf1_u35 (worth reporting to SLEEF; no wheel-port patch warranted). Fix moves the decision to upstream''s own WITH_SLEEF_DEFAULT switch at CMakeLists.txt:356, beside WIN32 and WITH_ROCM, which runs before third_party is included and sees WITH_RISCV because setup.py forwards every WITH_* env var as a -D; WITH_SLEEF is then consistently OFF, PADDLE_WITH_SLEEF never defined, Sine/Cosine fall back to libm, and the SLEEF ExternalProject (~25 min/leg) disappears. Swept the block''s other forces for the same leak: WITH_AVX and WITH_MKL default to ${AVX_FOUND} so were already OFF, but WITH_XBYAK defaults ON and cmake/external/xbyak.cmake does add_definitions(-DPADDLE_WITH_XBYAK -DXBYAK64), which leaked identically - benign here only by luck (cpu_info.h uses it to skip defining cpuid(), and jit/gen is add_subdirectory''d after the block so it was correctly dropped; cpu_info.cc.o compiled clean at 19%), so left alone rather than churned. Patch verified against a pristine v3.3.1 checkout with git apply --check as well as patch -F 0, after the first hand-written hunk header silently swallowed the next hunk (new gotcha 444). Round 7 pushed as ed22659c3. Honest read on what round 7 can and cannot settle: it removes the one thing standing at 21% of phi_core, but ~79% of phi_core, libpaddle.so''s own sources and the final link are all still unproven, and the final link of a 193MB-on-aarch64 libpaddle.so in 15GiB with no swap remains the original open risk - if round 7 dies on memory rather than on another ordinary compile error, that is the resource wall and parking under gotcha 421 is the call then. Merged (#2120). Push-triggered build+publish on main run 35573179565 in progress as of 2026-09-21T07:29Z (multi-hour C++ compile per prior CI history); move to published once it completes and https://pypi.riseproject.dev/simple/paddlepaddle/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: sspilib
  version: 0.6.0
  home: https://github.com/jborean93/sspilib
  repo: https://github.com/jborean93/sspilib
  status: published
  pr: 1869
  notes: 'PR #1869 opened. Cross-platform: Linux links Cython bindings against libsspi.so built from Devolutions/sspi-rs + static ICU4C via upstream''s own before-all script, not Windows-only. Watching CI.'
- pkg: sudachipy
  version: 0.6.11
  home: https://github.com/WorksApplications/sudachi.rs/tree/develop/python
  repo: https://github.com/WorksApplications/sudachi.rs/tree/develop/python
  status: published
  pr: 1640
  notes: 'PR #1640 opened. setuptools-rust/pyo3 0.27 bindings (python/ subdir is the crate root, sibling ../sudachi crate); no abi3, per-interpreter build. Matrix cp312/cp313/cp314/cp314t; cp314t drops test_pretokenizers.py (tokenizers has no free-threaded wheel). Skips upstream PGO before-all step (external corpus download plus release build, would run once per matrix leg on scarce riscv64 runners). Watching CI.'
- pkg: pagefind-bin
  version: 1.5.2
  home: https://pagefind.app
  repo: https://github.com/pagefind/pagefind/
  status: published
  pr: 1636
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: frida
  version: 17.17.0
  home: https://frida.re
  repo: https://frida.re
  status: parked
  pr: null
  notes: 'Not feasible: frida-python is a thin binding whose entire function depends on frida-core (C, Meson), and frida-core''s Gum engine has zero riscv64 backend anywhere upstream, at the architecture-codegen level, not merely "no prebuilt devkit". Verified against the frida-gum submodule vendored at frida/frida tag 17.17.0 (commit ddc10c5): its top-level meson.build derives host_arch from cpu_family() and falls through to the raw family name (i.e. "riscv64") for anything not arm/arm64/mips/mips64, but gum/meson.build only ever adds GumInterceptor/GumStalker/GumCpuContext/GumProcess backend sources when host_arch is in {x86, x86_64, arm, arm64, mips, mips64} (gum/backend-x86, backend-arm, backend-arm64, backend-mips are the only backend-* dirs that exist; likewise arch-x86/arch-arm/arch-arm64/arch-mips are the only arch-* writer/relocator dirs) -- there is no backend-riscv64 or arch-riscv64 anywhere in the tree, so a riscv64 build would fail to link GumInterceptor/GumStalker (no fallback/generic backend, no explicit meson error, just missing sources). This is corroborated directly by upstream: frida-gum issue #733 ("RISC-V support", open since 2023) was reopened by maintainer oleavr in 2024 with "this is planned, we just don''t know when somebody will find the time to do the work"; frida-gum PR #1071 ("riscv support") is an open, unmerged, third-party draft ("Initial support, draft stage, some issues with the interceptor need to be resolved", 4081 additions across 43 files, last updated days before this check) -- i.e. real but incomplete, unreviewed, unmerged work, not present in 17.17.0 or any released frida-core. The devkit release assets for 17.17.0 confirm the same gap operationally: linux devkits are published for x86/x86_64/arm/arm64/armhf/mips/mips64(+musl/be variants) but no riscv64. Stalker (code-tracer) and Interceptor (function-hook trampoline codegen) are architecture-specific JIT/codegen subsystems, not portable C -- this is a from-scratch native backend (instruction encoding, relocation, disassembly) upstream itself has not finished, so a from-source riscv64 build in this repo would need to originate that backend, which is out of scope for a wheel port. No worktree/branch/PR created.'
- pkg: statsforecast
  version: 2.1.1
  home: https://github.com/Nixtla/statsforecast/
  repo: https://github.com/Nixtla/statsforecast/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1803
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Hard runtime dep coreforecast (pybind11+Eigen) has no riscv64 wheels yet either; its own port (PR #1795) is ci-running, unmerged, so cp312/cp313/cp314 riscv64 numpy availability sets the interpreter floor. PR #1803 opened, CI running.'
- pkg: tree-sitter-groovy
  version: 0.1.2
  home: https://github.com/amaanq/tree-sitter-groovy
  repo: https://github.com/amaanq/tree-sitter-groovy
  status: published
  pr: 1095
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.1.2 commits generated src/parser.c (byte-identical to the released PyPI sdist), so no generate-then-build workaround needed. Same shape as tree-sitter-json/-kotlin/-scala/-elixir. Worktree/branch tree-sitter-groovy. PR https://github.com/riseproject-dev/python-wheels/pull/1095, CI green (build + publish dry-run passed). Merged (#1095). Issue #1118 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: nlopt
  version: 2.11.0
  home: https://nlopt.readthedocs.io/en/latest/
  repo: https://github.com/DanielBok/nlopt-python
  status: published
  pr: 1635
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Source repo is the Python wrapper DanielBok/nlopt-python (git tag 2.11.0, matches PyPI project_urls), which pulls the actual C++ library via a pinned git submodule (extern/nlopt -> stevengj/nlopt @ 88c424d). Build backend is plain setuptools with a hand-written build_ext (extensions.py) driving CMake + SWIG directly, not scikit-build-core. No sdist on PyPI, so this is a build-from-checkout port. Worktree/branch nlopt.'
- pkg: backports-datetime-fromisoformat
  version: 2.0.3
  home: https://github.com/movermeyer/backports.datetime_fromisoformat
  repo: https://github.com/movermeyer/backports.datetime_fromisoformat
  status: published
  pr: 1385
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: protobuf-py-ext
  version: 0.3.0
  home: https://github.com/bufbuild/protobuf-py
  repo: https://github.com/bufbuild/protobuf-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1872
  notes: 'Standalone pyo3/maturin crate (packages/protobuf-py-ext in the monorepo), no C/C++ deps, unrelated to protocolbuffers/protobuf already built in this repo. 20 Linux wheels upstream (abi: cp310,cp312,cp313,cp314,cp314t); no riscv64 on PyPI or pypi.riseproject.dev. PR opened, CI running.'
- pkg: tree-sitter-objc
  version: 3.0.2
  home: https://github.com/tree-sitter-grammars/tree-sitter-objc
  repo: https://github.com/tree-sitter-grammars/tree-sitter-objc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1094
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tag v3.0.2 commits generated src/parser.c, so no generate-then-build workaround needed. Same shape as tree-sitter-json/-kotlin/-scala/-elixir. PR opened, CI running. Merged (#1094). Issue #1119 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tantivy
  version: 0.26.0
  home: https://github.com/quickwit-oss/tantivy-py
  repo: https://github.com/quickwit-oss/tantivy-py
  status: in-review
  pr: 1651
  notes: 'PR #1639 merged before its build_wheels legs finished; all four (cp312/cp313/cp314/cp314t) failed on a doctest-modules test step that collects 0 items against the installed wheel (tantivy compiled classes report __module__ as tantivy.tantivy, not tantivy, confirmed locally). A stale main-branch publish dispatch from before the fix was cancelled (build_wheels never succeeded, so nothing was uploaded). Follow-up PR #1651 drops that dead test step; the real pytest suite (154 items) already passed on every leg. Awaiting #1651 review/merge, then re-dispatch publish on main.'
- pkg: boost-histogram
  version: 1.8.1
  home: https://github.com/scikit-hep/boost-histogram
  repo: https://github.com/scikit-hep/boost-histogram
  status: published
  pr: 1638
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,graalpy312,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: datasketches
  version: 5.2.0
  home: http://datasketches.apache.org
  repo: https://github.com/apache/datasketches-python
  status: published
  pr: 1642
  notes: '20 Linux wheels upstream (abi: cp39,cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. nanobind/CMake build (ExternalProject_Add fetches apache/datasketches-cpp at the matching tag, not a submodule); build-from-checkout shape. PR #1642 open, CI running.'
- pkg: tree-sitter-zig
  version: 1.1.2
  home: https://github.com/tree-sitter-grammars/tree-sitter-zig
  repo: https://github.com/tree-sitter-grammars/tree-sitter-zig
  status: published
  pr: 1090
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Standard tree-sitter grammar shape (gotcha 114): src/parser.c committed at the tag, cp39-abi3 setup.py, no external scanner, no generation step needed (unlike tree-sitter-swift). Mirrors tree-sitter-json/kotlin/scala/elixir exactly. PR #1090, branch tree-sitter-zig. Merged (#1090). Issue #1123 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: mapbox-earcut
  version: 2.0.0
  home: https://github.com/skogler/mapbox_earcut_python
  repo: https://github.com/skogler/mapbox_earcut_python
  status: published
  pr: 1386
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Nanobind + scikit-build-core, header-only earcut.hpp, no patches needed. PR #1386, branch mapbox-earcut. Dropped musllinux: numpy has no musl riscv64 wheel on our registry. CI green: all 4 matrix jobs (cp312/cp313/cp314/cp314t) passed, publish dry-ran cleanly.'
- pkg: tree-sitter-julia
  version: 0.25.0
  home: https://github.com/tree-sitter/tree-sitter-julia
  repo: https://github.com/tree-sitter/tree-sitter-julia
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1093
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.25.0 commits generated src/parser.c and src/scanner.c, so no generate-then-build workaround needed (unlike tree-sitter-swift). Same shape as tree-sitter-json/-elixir/-kotlin/-scala/-powershell; requires-python >=3.10 so cp310-abi3. PR #1093 green (build + publish dry-run pass); not yet merged. Merged (#1093). Issue #1120 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: plyvel
  version: 1.5.1
  home: https://github.com/wbolster/plyvel
  repo: https://github.com/wbolster/plyvel
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1665
  notes: 'PR #1665 green (4 wheels built cp312-cp314t, publish dry-run OK), awaiting review. LevelDB and Snappy built from source via cmake (no riscv64 manylinux image ships either); both BSD-3-Clause, license files added since upstream''s own wheels carry none. test_open_read_only_dir deselected: the test phase runs as root, which never sees the permission error it asserts.'
- pkg: tree-sitter-verilog
  version: 1.0.3
  home: https://github.com/tree-sitter/tree-sitter-verilog
  repo: https://github.com/tree-sitter/tree-sitter-verilog
  status: published
  pr: 1196
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Same shape as tree-sitter-json/-toml: src/parser.c is committed at the v1.0.3 tag, no generate step needed. PR #1196 open, CI running.'
- pkg: tensordict
  version: 0.14.0
  home: https://github.com/pytorch/tensordict
  repo: https://github.com/pytorch/tensordict
  status: published
  pr: 1868
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. torch is not a blocker: pypi.riseproject.dev already serves torch 2.13.0+cpu/2.14.0+cpu for cp312/cp313/cp314/cp314t riscv64 (build-torch.yml). tensordict''s own C++ extension (tensordict/csrc: pybind.cpp+utils.cpp, ~150 lines) does not link libtorch at all -- grepped the sources and CMakeLists.txt, zero torch references; it only calls find_package(pybind11)/find_package(Python3). torch is a pure unpinned runtime dependency. Matrix narrowed to cp312/cp313/cp314/cp314t since torch (a hard runtime dep) has no riscv64 wheel on pypi.riseproject.dev for cp310/cp311 -- same shape as build-torchaudio.yml. Locally validated the full pip-wheel build + upstream smoke test (test/smoke_test.py: imports numpy/torch/tensordict/tensordict.nn) end-to-end on a native aarch64 manylinux_2_39 container via podman before pushing. One real, non-riscv64-specific bug found and patched (patches/tensordict/0.14.0/0001-csrc-request-only-Development.Module-from-FindPython.patch, Upstream-Status: To upstream): CMakeLists.txt''s `find_package(Python3 REQUIRED COMPONENTS Interpreter Development)` requires Development.Embed, which fails to find Python3_LIBRARIES on manylinux''s statically-built CPython (Py_ENABLE_SHARED=0, libpython3.12.a only, no .so) -- confirmed by reproducing the exact CMake configure failure on quay.io/pypa/manylinux_2_39_aarch64, then confirming Development.Module alone (the only component an extension module actually needs) fixes it. PR #1868 open; all 4 matrix legs (cp312/cp313/cp314/cp314t) passed on real riscv64 CI first try, plus publish dry-run, check_patches, check_commit_messages -- confirmed green via `gh pr checks`.'
- pkg: embreex
  version: 4.4.0
  home: https://github.com/trimesh/embreex
  repo: https://github.com/trimesh/embreex
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 366, feasibility-and-triage.md): embreex is a thin Cython binding around Intel''s Embree ray-tracing kernel library; embreex''s own before-build step (package/fetch-embree.py) downloads Embree''s prebuilt release archive, and RenderKit/embree''s GitHub Releases publish x86_64-linux, x86_64/arm64-macos, and x64-windows only for v4.4.0 -- no riscv64, confirmed against the real release assets. Building Embree from source does not unblock it: Embree''s own CMakeLists.txt platform detection recognizes only two families -- EMBREE_ARM (Apple silicon or CMAKE_SYSTEM_PROCESSOR aarch64/ARM64) or, for everything else, EMBREE_MAX_ISA defaulting through to SSE2, with common/simd/sse.h and friends using __m128/_mm_* intrinsics unconditionally outside the ARM branch. There is no third generic/portable/scalar ISA option -- EMBREE_MAX_ISAs own allowed STRINGS list is only NONE NEON NEON2X (ARM) or NONE SSE2 SSE4.2 AVX AVX2 AVX512 DEFAULT (everything else). riscv64 matches neither family, silently falls into the x86 SSE2 default, and fails to compile (xmmintrin.h does not exist for a riscv64-targeted GCC). Confirmed no RISC-V work exists anywhere upstream: common/simd/ has an arm/ shim (sse2neon.h/avx2neon.h) but no riscv equivalent, a GitHub code search across RenderKit/embree for "riscv" returns zero hits (both the v4.4.0 tag and current master), and Embree''s own project site documents support for x86 (Linux/macOS/Windows) and ARM (macOS) CPUs plus Intel Arc GPUs only. Closing this gap would mean writing a full SSE/AVX-to-RVV translation shim covering the hundreds of intrinsics Embree''s kernels use -- a new engineering project for upstream, not a patches/<pkg>/<version>/ fix -- and would not ''closely mirror upstream''s own CI, narrowed to riscv64'' (goal 2: upstream has no riscv64 CI or code path to narrow). No worktree/branch/PR created -- resolved read-only against the real v4.4.0 pyproject.toml/setup.py/package/fetch-embree.py/package/embree.json (embreex) and the real CMakeLists.txt/common/simd/* (Embree).'
- pkg: ephem
  version: 4.2.1
  home: http://rhodesmill.org/pyephem/
  repo: https://github.com/brandon-rhodes/pyephem
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1388
  notes: '68 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Simple MIT-licensed C extension (ephem._libastro, wraps vendored XEphem libastro, both under one MIT LICENSE at repo root); links only libm, no external native deps. Build-from-checkout mirroring upstream''s build-and-test.yml, cibuildwheel manylinux+musllinux, cp312/cp313/cp314/cp314t. Tests staged from ephem/tests only (leaves ephem/ a namespace portion in the test cwd so import ephem resolves the installed wheel). Verified locally: setup.py build_ext + python -m unittest discover ephem, 177/177 tests pass. Worktree at .claude/worktrees/ephem, branch ephem. PR https://github.com/riseproject-dev/python-wheels/pull/1388, watching CI.'
- pkg: openvino
  version: 2026.3.1
  home: https://docs.openvino.ai/2023.0/index.html
  repo: https://github.com/openvinotoolkit/openvino
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2122
  notes: '12 Linux/macOS/Windows wheels upstream (abi: cp310-cp314t, build tag 22476), no sdist at all; no riscv64 on PyPI or pypi.riseproject.dev. Feasible and NOT a gotcha-214/ortools scope stop: OpenVINO has first-class upstream riscv64 support, so the only open question was build scale. Upstream carries a dedicated .github/workflows/linux_riscv.yml (per-PR plus 00:00 Wed/Sat, 150-min budget, 16-core x86 cross-build with ccache) that builds the CPU plugin for riscv64 and runs ov_cpu_func_tests under `qemu-riscv64 -cpu rv64,v=true,vext_spec=v1.0`, and docs/dev/build_riscv64.md documents the build (validated on Lichee Pi 4A/RVV 0.7.1, Banana Pi BPI-F3 and Orange Pi RV2/RVV 1.0) including `-DENABLE_PYTHON=ON -DENABLE_WHEEL=ON`. The CPU plugin carries real riscv64 code rather than a generic C++ fallback: src/plugins/intel_cpu/CMakeLists.txt links the xbyak_riscv submodule with XBYAK_RISCV_V=1 for RVV JIT, compiles src/{emitters/plugin,emitters/snippets,nodes/kernels,nodes/executors}/riscv64/*, renames the plugin openvino_riscv_cpu_plugin, and excludes mlas/kleidiai/ACL/libxsmm (X86_64/AARCH64-gated); intel_cpu/thirdparty/CMakeLists.txt sets DNNL_TARGET_ARCH=RV64 for the vendored oneDNN, so none of oneDNN''s x64 JIT tree is built. GPU/NPU need no flag work - features.cmake gates ENABLE_INTEL_GPU on "X86_64 OR AARCH64" and ENABLE_INTEL_NPU on "X86_64". Two upstream riscv64 branches make the wheel recipe work unchanged, and both were verified rather than assumed: (1) src/bindings/python/wheel/CMakeLists.txt''s _ov_platform_arch() has a RISCV64 -> riscv64 case and derives the tag from the build glibc, so the build emits manylinux_2_39_riscv64 itself and needs no auditwheel retag (contrast build-onnxruntime.yml, whose setup.py allow-list has no riscv64 entry); (2) cmake/dependencies.cmake''s ov_download_tbb() has a RISCV64 branch fetching oneapi-tbb-2022.3.0-lin-riscv-release.tgz from storage.openvinotoolkit.org - downloaded it and confirmed genuine riscv64 oneTBB (ELF e_machine 243) needing only GLIBC<=2.33, so the default TBB_ADAPTIVE threading resolves and setup.py bundles TBB exactly as in the published x86_64/aarch64 wheels. Deliberately did NOT copy linux_riscv.yml''s -DTHREADING=OMP: that predates the riscv TBB archive, and OMP would link the image''s libgomp into the wheel, forcing a gpl_sources job and diverging from the wheel recipe for no gain (ov_download_tbbbind_2_5 has no riscv64 entry but only warns and returns). Scale is proportionate: with GPU (996 files) and NPU (176) off, the compiled set is ~2400 of openvino''s own .cpp plus oneDNN RV64, protobuf, onnx, flatbuffers, snappy, pugixml and zlib - comparable to onnxruntime, which this repo already publishes for riscv64 (cp312-cp314t), and far under torch 2.13/2.14+cpu and libclang''s ~10h LLVM build, both of which this runner pool has already absorbed. build-openvino.yml mirrors manylinux_2_28.yml (the recipe behind the PyPI wheels): one C++ build, then a per-interpreter cmake over src/bindings/python with -DOpenVINODeveloperPackage_DIR pointing at that build dir and ENABLE_GIL_PYTHON_API=OFF for cp314t, podman-driving quay.io/pypa/manylinux_2_39_riscv64 like build-onnxruntime.yml (single job, no python matrix, so the interpreter-independent C++ world is compiled once). Deviations: -DENABLE_JS=OFF (the npm addon is out of scope and would FetchContent node headers), tests component off, and `dnf install -y cmake` because the image ships CMake 4.4.3 while vendored snappy (cmake_minimum_required 3.1) and xbyak_riscv (2.6...3.0.2) sit below CMake 4''s hard 3.5 floor - confirmed in-image that dnf yields cmake 3.31.8 on Rocky 10 riscv64, above openvino''s own 3.26 requirement, which is also why upstream''s manylinux job calls /usr/bin/cmake. Local rehearsal per gotcha 15/9, run inside the real manylinux_2_39_riscv64 image under QEMU (a full build is impractical there, a configure is not): BOTH halves configure clean with the workflow''s exact flags - the base build ends "Configuring done (135.6s) / Generating done", exit 0, with CMakeCache showing DNNL_TARGET_ARCH=RV64, ENABLE_INTEL_CPU=ON, XBYAK_RISCV_V=ON, ENABLE_INTEL_GPU/NPU=OFF, THREADING=TBB_ADAPTIVE and TBBROOT=temp/Linux_riscv64/tbb (the prebuilt riscv TBB actually downloaded and resolved); the per-interpreter `cmake -S src/bindings/python -DOpenVINODeveloperPackage_DIR=/build` then also exits 0 against a base configured with ENABLE_PYTHON=OFF, which is the one combination upstream never runs itself (its riscv CI uses ENABLE_PYTHON=OFF without the developer package; its wheel CI uses the developer package with python on) and therefore the riskiest part of the design. The generated build files name the wheel exactly: openvino-2026.3.1-1-cp312-cp312-manylinux_2_39_riscv64.whl, i.e. correct version and tag, with PEP 427 build tag 1 because ov_commit_number() counts first-parent commits in actions/checkout''s shallow clone (upstream''s own releases carry 22476); build-tagged filenames are already precedented and published here by cmeel-boost and shiboken6, and update_doc.py reads the version from METADATA rather than the filename. Runtime deps resolve: numpy has riscv64 wheels, openvino-telemetry is py3-none-any, and pillow (needed by one test) is on the registry for cp312-cp314t. Testing mirrors upstream''s job_python_api_tests.yml (pytest over src/bindings/python/tests) narrowed to test_runtime/test_graph/test_transformations with -m ''not template_extension'', since the template plugin/extension only ships in the tests component this build does not produce, plus a real CPU inference check (relu over a 1x4 f32 tensor) and the .so proof on openvino._pyopenvino. No patches needed. PR #2122 open, CI not yet observed - the first real run is the remaining evidence; expect a long build and watch for (a) OOM on the 15GB runner from `--parallel $(nproc)` (cap it if the intel_cpu tail dies), (b) pytest failures from tests that assume the tests component. Candidate gotcha once green (not filed yet, per the skill''s after-green rule): do not copy an arch-specific upstream CI workflow''s build options blindly - check whether the project''s *wheel* recipe already has a branch for the arch, since the CI workflow may predate it (openvino''s riscv CI forces OMP threading while its dependency downloader already ships a riscv64 TBB). UPDATE after the first CI run (35480766550, job 105998045327, 01:12-13:36 UTC, 12h24m): the C++ world and all four wheels BUILD FINE on riscv64 - and the wheel cannot run on this runner fleet. `Build wheels` succeeded after 12h18m (cp312/cp313/cp314/cp314t all produced; artifact openvino-2026.3.1-wheels-manylinux_riscv64, 129MB, retained to 2026-12-19); the job then died 2m12s into `Test wheels` with exit code 132 = 128+4 = SIGILL, ''riscv64-build-and-test.sh: line 32: 41 Illegal instruction (core dumped)''. Neither risk the triage flagged materialised: no OOM (build step green; zero Killed/out of memory/ICE/ENOSPC anywhere) and no pytest failure (the run never reached pytest), and 12h24m against timeout-minutes 1440 is not a timeout either. The crash is in the cp312 smoke check and the boundary is exact: print(ov.get_version()) printed 2026.3.1-1-759c5a6ab8c, print(core.available_devices) never printed, so it dies inside ov.Core()/device enumeration - the moment the CPU plugin is dlopened and constructed; set -e aborted the loop there, so cp313/cp314/cp314t were never tested. Root-caused from the artifact rather than another runner slot (new gotcha 448): of the 20 libraries in the wheel, libopenvino_riscv_cpu_plugin.so is the ONLY one whose ELF Tag_RISCV_arch carries vector - rv64i2p1_m2p0_a2p1_f2p2_d2p2_c2p0_v1p0_zicsr2p0_zifencei2p0_zmmul1p0_zve32f1p0_zve32x1p0_zve64d1p0_zve64f1p0_zve64x1p0_zvl128b1p0_zvl32b1p0_zvl64b1p0 - and it holds 64554 OP-V instructions (34526 of them vsetvl*) in a contiguous ~10.7MB region of its 20.6MB .text, against exactly ZERO across the other 19 libraries'' 52MB of .text, which is the control proving the scan has no false positives. Upstream''s gate is a runtime probe - nodes/kernels/riscv64/cpu_isa_traits.cpp''s `case gv: return mayiuse(g) && cpu.hasExtension(RISCVExtension::V) && can_compile_rvv100();` - and the probe is visible in the binary as the only RVV outside that kernel region: two isolated vsetvli+vmv.v.i pairs at .text+0x10b92 (e8,mf2,ta,ma) and .text+0x10d7a (e64,m1,ta,ma), i.e. can_compile_rvv100()/can_compile_zvfh(), which deliberately execute the instruction under a SIGILL handler. mf2 does not exist in RVV 0.7.1 and the vsetvli encoding differs between 0.7.1 and 1.0, so on this fleet the probe traps exactly as gotcha 272 describes (HWCAP advertises V; the first RVV-1.0 vsetvli is illegal) - and the process core-dumps instead of the probe returning false. Static evidence cannot separate ''the probe''s SIGILL recovery fails'' from ''a static initializer inside one of the RVV-compiled TUs runs at dlopen with no mayiuse in front of it'' (a guard on call sites does not cover a TU''s own .init_array); both land on the same log line, and it should not be guessed at - the artifact is downloadable until 2026-12-19, so the discriminating test (LD_DEBUG=libs to see whether the plugin finished its init, or gdb -batch -ex run -ex bt) runs against the EXISTING wheels in minutes with no rebuild. Second, independent blocker found the same way (new gotcha 449): the prebuilt oneTBB that ov_download_tbb() fetches and setup.py bundles is a T-Head Xuantie build - Tag_RISCV_arch rv64i2p0_m2p0_a2p0_f2p0_d2p0_c2p0_xtheadc2p0, with 906 CUSTOM-0 (opcode 0x0B) instructions in libtbb.so.12 and 892 in libtbbmalloc.so.2, against zero in the 17 libraries built locally. The triage checked e_machine 243 and stopped there - true, and not enough. It runs here only because the runner fleet is itself T-Head (which also explains gotcha 272''s otherwise-odd HWCAP-says-V-but-vsetvli-SIGILLs pairing: a T-Head C9xx core means RVV 0.7.1), and it means this wheel would SIGILL on SiFive U74/P550, JH7110 or plain QEMU rv64gc - so a green run on this fleet would not have proved the wheel portable even if the RVV problem did not exist. Why the triage missed the RVV question: upstream''s linux_riscv.yml runs ov_cpu_func_tests only under qemu-riscv64 -cpu rv64,v=true,vext_spec=v1.0, and docs/dev/build_riscv64.md''s hardware list mixes RVV 0.7.1 (Lichee Pi 4A) with RVV 1.0 (BPI-F3, Orange Pi RV2) boards, so ''upstream carries a real riscv64 CPU plugin with RVV JIT'' is evidence about an emulator with V forced on, not about baseline hardware - gotcha 279''s rule generalises past zlib-ng. NOT re-triggered, deliberately: at ~12.5h per round this is now one of the most expensive builds in the repo and there is no cheap fix to be confident in. There is no off switch - src/plugins/intel_cpu/CMakeLists.txt adds the four riscv64/ source dirs and XBYAK_RISCV_V=1 whenever RISCV64, intel_cpu/thirdparty/CMakeLists.txt sets XBYAK_RISCV_V ON and DNNL_TARGET_ARCH=RV64 unconditionally, no option() governs any of it, and -march=rv64gcv appears nowhere in the build (only in clang_tidy.cmake) - so unlike gotcha 279''s -DWITH_RVV=OFF or gotcha 71''s PNG_RISCV_RVV=off this needs a real patch against a scalar-fallback path upstream''s own CI never exercises without v=true. And even a green RVV-off build could not ship while the T-Head TBB blob is in the wheel: a shippable wheel needs BOTH an RVV patch and a replacement for upstream''s prebuilt TBB (source-built oneTBB, or THREADING=SEQ) - two material divergences from the manylinux_2_28.yml recipe this port exists to mirror, yielding a scalar-only OpenVINO with none of the vector acceleration that motivated it, at >=2-3 further 12.5h rounds to validate. That is a human call on cost vs. value, in the shape of the paddlepaddle decision framework, not an agent''s. The options: (a) park as disproportionate; (b) authorise the minutes-long artifact-only backtrace first and decide with the exact faulting frame in hand; (c) accept an RVV-1.0-only wheel, which the manylinux_riscv64 tag forbids (gotcha 139). Flagged for that decision on PR #2122; status left in-review and draft status untouched. The candidate gotcha the earlier note reserved ''once green'' is superseded by 448/449.'
- pkg: tree-sitter-toml
  version: 0.7.0
  home: https://github.com/tree-sitter-grammars/tree-sitter-toml
  repo: https://github.com/tree-sitter-grammars/tree-sitter-toml
  status: published
  pr: 1092
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. src/parser.c is committed at the release tag, so this uses the simple single-job pattern (like tree-sitter-json/kotlin/scala/elixir/yaml/powershell), not the swift-style generate_parser workaround. Worktree at .claude/worktrees/tree-sitter-toml, branch tree-sitter-toml. PR https://github.com/riseproject-dev/python-wheels/pull/1092, CI in progress. Merged (#1092). Issue #1121 linked, publish dispatched on main. Not yet confirmed on the registry. Confirmed live on pypi.riseproject.dev.'
- pkg: tree-sitter-fortran
  version: 0.6.0
  home: https://github.com/stadelmanma/tree-sitter-fortran
  repo: https://github.com/stadelmanma/tree-sitter-fortran
  status: published
  pr: 1199
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. src/parser.c and scanner.c are committed at the release tag (byte-identical to the PyPI sdist), so this uses the simple single-job pattern. No bindings/python/tests dir upstream, so tests use the tree-sitter-dart-style inline smoke test (parse a snippet, assert no error node). Worktree at .claude/worktrees/tree-sitter-fortran, branch tree-sitter-fortran. PR https://github.com/riseproject-dev/python-wheels/pull/1199, CI in progress.'
- pkg: connectorx
  version: 0.4.5
  home: https://github.com/sfu-db/connector-x
  repo: https://github.com/sfu-db/connector-x
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1797
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Maturin/PyO3 build (connectorx-python/Cargo.toml, path dep on sibling connectorx crate); pyproject.toml lives in connectorx-python/, not repo root. Mirrors upstream release.yml linux job (PyO3/maturin-action, manylinux 2_28 -> 2_39 for riscv64). Matrix cp312/cp313/cp314 (repo floor; upstream also ships cp310/cp311; no cp314t until 0.4.6a2). connectorx-python/Cargo.lock is committed, so no floating-deps risk (gotcha 10). Default features include federation/fed_exec (pulls j4rs) but upstream''s own release workflow never runs `just setup-java`, so the federated-rewriter.jar is absent from real PyPI wheels too -- matching upstream, not a gap we introduce. Enabling --features integrated-auth-gssapi (matches upstream''s Linux job) needs krb5-libs + clang-devel + libffi-devel for libgssapi-sys''s bindgen; perl (full) needed for openssl-sys''s vendored OpenSSL build (gotcha 46). `cargo metadata --filter-platform riscv64gc-unknown-linux-gnu --locked` resolves clean (567 packages). connectorx-python opts out of the workspace `[profile.release]` (debug=true;lto=true) via its own empty `[workspace]` table, so it gets Cargo''s normal release defaults, not the heavy workspace profile. PR #1797 opened, CI running.'
- pkg: tensorflow-cpu
  version: 2.21.0
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: parked
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. Parked for two independent reasons - wrong distribution name for this architecture, and disproportionate scope - and explicitly NOT Bazel-blocked the way jaxlib/PR #526 is. Folded into the skill as gotcha 426 (feasibility-and-triage.md). NAMING: tensorflow-cpu is the same upstream tree as the already-parked tensorflow, selected by one repo_env - tensorflow/tools/pip_package/utils/tf_wheel.bzl reads WHEEL_NAME from @python_version_repo and its own docstring says it "Should be set via --repo_env=WHEEL_NAME=tensorflow_cpu" - and the two distributions PyPI metadata is identical down to the same 12 nvidia-* "extra == and-cuda" requirements. Across its entire release history tensorflow-cpu has published win_amd64 and manylinux*_x86_64 only: zero aarch64, zero ppc64le, zero other Linux arch, and no sdist. It exists solely to give x86_64 a CUDA-less wheel, because on x86_64 the default tensorflow wheel is the GPU one (545MB x86_64 tensorflow vs 261MB x86_64 tensorflow_cpu). On aarch64, where there is no CUDA to ship, upstream ships the CPU-only build under the plain name tensorflow - 268MB, matching the cpu wheel size rather than its own arch GPU wheel. riscv64 sits in exactly the aarch64 position, so the riscv64 deliverable for CPU-only TensorFlow is `tensorflow`, and a manylinux_riscv64 wheel named tensorflow-cpu would invent a name upstream uses on exactly one Linux architecture (gotcha 50, divergence with no gap closed, reached from the sibling side per gotcha 79/426). This also settles the plausible-sounding hypothesis that the CPU-only variant sidesteps whatever made full tensorflow impractical: it is backwards. With no CUDA on riscv64 the tensorflow build is already the CPU build, so tensorflow-cpu is the identical compile under a worse name and is never cheaper than the base. SCOPE: the wheel is per-interpreter (_get_full_wheel_name formats cp{v}-cp{v} from HERMETIC_PYTHON_VERSION), so cp310-cp313 means 4 full Bazel builds with none of the abi3/py3-none collapse. Calibrated against PR #2104 (mediapipe, which compiles this same TF 2.21.0 source vendored as org_tensorflow, on this same runner image): mediapipe builds only the framework/TFLite subset its calculators need plus XNNPACK and OpenCV, with no XLA, MLIR or LLVM, and still took 5h23m in the build step on ubuntu-24.04-riscv, with 2h24m-3h21m of queue wait per job on the shared pool and 5 CI runs to clear two arch-specific fixes. Full tensorflow adds all of the TF kernels/ops plus XLA plus MLIR plus a large slice of LLVM on top of that subset, times 4 interpreters. For reference the largest build logged in this repo is libclang at ~10h (LLVM/Clang from scratch) and the highest job cap anywhere in the repo is build-torch.yml at 1440min. NO riscv64 MAPPING IN UPSTREAM BUILD: .bazelrc has zero riscv occurrences across 1023 lines and the official CPU config release_cpu_linux pulls in --config=avx_linux (-mavx), i.e. x86-only by construction; WORKSPACE registers exactly four rules_ml_toolchain CC toolchains (linux_x86_64, linux_x86_64_cuda, linux_aarch64, linux_aarch64_cuda) so there is no hermetic riscv64 toolchain and --config=clang_local plus a local compiler would be mandatory; tensorflow/tools/pip_package/BUILD platform_tag select lists aarch64/arm64/x86_64/ppc with no riscv64 entry and no //conditions:default, so the wheel target fails at analysis time before a single object compiles; third_party/py/python_init_toolchains.bzl filters hermetic interpreter platforms to aarch64/x86_64 with the comment "Avoid obscure platforms for now just in case" (mitigated, since WORKSPACE passes default_python_version = system); and hermetic pip has to resolve hash-pinned requirements_lock_3_*.txt (numpy 2.1.3, scipy 1.14.1, h5py 3.13.0) on riscv64 - the scipy sdist hash IS in the lock so it resolves, but that means compiling scipy and h5py from source inside the build. Upstream publishes no sdist at all (wheels only) and has no riscv64 CI job, so goal 2 has no upstream recipe to narrow - every arch knob would be newly invented here. COUNTERPOINT RECORDED so a later attempt is not misled: at the XLA revision TF 2.21.0 vendors (the full tree at third_party/xla/), xla/backends/cpu/codegen/BUILD does wire if_llvm_riscv_available to @llvm-project//llvm:RISCVCodeGen for the CPU JIT and xla/tsl/BUILD defines linux_riscv64 and riscv64_or_cross, and the embed_bitcode restructure that blocked jaxlib 0.11.1 is not in this tree yet (the rule is cc_ir_header in cc_to_llvm_ir.bzl, whose ir_to_string tool deps are only llvm:Object and llvm:Support, no per-arch CodeGen). The bazel 7.7.0 bootstrap is likewise de-risked by gotchas 47/133 (no riscv64 bazel binary exists, dist-archive bootstrap works, and 7.7.x pins the same rules_python 0.33.2/rules_java 7.6.5 as the 7.5.0 recipe already proven in build-mediapipe.yml). So the wall here is scope and naming, not architecture. REVISIT only if upstream starts publishing a non-x86 Linux wheel under the -cpu name, or if tensorflow itself is unparked - in which case port `tensorflow`, not this entry. No worktree, branch or workflow opened: the call was fully resolvable read-only against upstream BUILD/bzl files at v2.21.0, the PyPI release history, and this repo own PR #526 and #2104 history.'
- pkg: protovalidate
  version: 2.0.0
  home: https://github.com/bufbuild/protovalidate-py
  repo: https://github.com/bufbuild/protovalidate-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1670
  notes: 'PR #1670 pushed, CI running. PyO3/maturin extension statically linking abseil-cpp, antlr4, cel-cpp, protobuf, re2 and protovalidate-cc (all vendored as git submodules, compiled via crates/protovalidate-rs/build.rs). abi3-py310 wheel plus a separate cp314t free-threaded build, matching upstream''s -i 3.10 -i 3.14t maturin-action matrix. Patch adds the six vendored licence files to [project] license-files (maturin has no default glob for a PEP 639 project).'
- pkg: yara-python
  version: 4.5.4
  home: https://github.com/VirusTotal/yara-python
  repo: https://github.com/VirusTotal/yara-python
  status: published
  pr: 1643
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: davey
  version: 0.1.6
  home: https://github.com/Snazzah/davey
  repo: https://github.com/Snazzah/davey
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1866
  notes: '104 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin monorepo (davey-python/ subdir, Cargo.toml at repo root pins the workspace); pure-Rust crypto stack (openmls + RustCrypto crates: p256, aes, sha2, ghash, scrypt) with no ring/aws-lc-rs, so no riscv64-blocking C dependency. No abi3 feature on pyo3 (0.25.1, per-interpreter tags matching PyPI). Mirrors upstream''s python-test.yml linux job. PR #1866 dispatched.'
- pkg: tree-sitter-sql
  version: 0.3.11
  home: https://github.com/derekstride/tree-sitter-sql.git
  repo: https://github.com/derekstride/tree-sitter-sql.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1197
  notes: PR open (#1197). Grammar tag omits generated src/parser.c; a generate_parser job runs tree-sitter generate on x86 first, mirroring tree-sitter-swift/toml. Matrix cp312-cp314 (manylinux_riscv64 image limit); wheel is cp310-abi3. Watching CI.
- pkg: imagecodecs
  version: 2026.8.16
  home: https://www.cgohlke.com
  repo: https://github.com/cgohlke/imagecodecs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1799
  notes: '6 Linux wheels upstream (abi: cp312,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev. PR #1799 CI confirmed green (build x2 + publish dry-run) after 6 iterations. No public upstream CI; setup.py exposes an `imagecodecs_distributor_setup` extension point (its own customize_build_* presets target cgohlke/pyodide/cibuildwheel/conda-forge/macports/mingw) -- patch 0001 adds a Rocky 10 riscv64 preset directly in setup.py (the import hook is not reachable from cibuildwheel''s isolated build), trimmed to codecs backed by appstream/crb dnf packages plus lzf (jxrlib/jpegxr and wavpack dropped -- unpackaged/too old; libdeflate built from source since unpackaged). Patch 0002 drops a png_set_cICP call needing libpng >= 1.6.45 (Rocky 10 ships 1.6.40). Matrix cp312 (abi3) + cp314t only (py_limited_api collapses cp312/cp313/cp314). Test suite deselects TIFF/lzma (no lzma in system libtiff), two jpeg-in-tiff precision cases, cms transform precision, PixarLog per-strip decode, a libtiff 4.6.0 big-endian float16 predictor bug, and bfloat16 -nan round-tripping; 4613 passed, 10386 skipped, 337 deselected on cp312.'
- pkg: sparsediffpy
  version: 0.3.0
  home: https://github.com/SparseDifferentiation/SparseDiffPy
  repo: https://github.com/SparseDifferentiation/SparseDiffPy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/920
  notes: 'Merged (#920). Issue #923 linked, project status set. Publish dispatched on main. Not yet confirmed on the registry. Ported at 0.3.0 (not the latest 0.6.1) because cvxpy>=1.9 hard-pins sparsediffpy>=0.3.0,<0.4.0 and this is the only release in that range. 0.3.0''s sdist needed a one-line pyproject.toml patch (cmake.minimum-version -> cmake.version; scikit-build-core>=0.8 rejects the old key outright, filed upstream at SparseDifferentiation/SparseDiffPy#22). This unblocks cvxpy (blocked-on-dependency), which hard-depends on it. Confirmed live on pypi.riseproject.dev.'
- pkg: mini-racer
  version: 0.14.1
  home: https://github.com/bpcreech/PyMiniRacer
  repo: https://github.com/bpcreech/PyMiniRacer
  status: ci-running
  pr: 1875
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. v8 has a mature riscv64 codegen backend; build routes around Chromium''s prebuilt clang via v8''s own riscv64-linux-gnu- gcc_toolchain, with eleven patches against builder/v8_build.py (riscv64 as a native v8_target_cpu and its manylinux tag, gclient/DEPS fixes for the reclient+siso CIPD packages that have no linux-riscv64 build, use_sysroot/use_custom_libcxx/enable_rust/temporal off, and the two GCC-vs-clang source stoppers: unicode.h''s in-class explicit specializations and third_party/highway falling back to HWY_SCALAR so json-stringifier.cc asks for a FixedTag<T,16> that does not exist there). Runner cost, measured and accepted: a full V8 compile is ~27h wall on a 4-core riscv64 runner (23h30m of ninja reached 1970/2176 and was still progressing when the old 1440-minute ceiling killed it), so build_dll carries timeout-minutes: 2880 and build_wheel 720, the same values build-nodejs-wheel-binaries.yml and build-vtk.yml already use for V8/VTK-from-source work. The 2026-09-19 run (35433322684) was NOT a build failure and NOT the timeout: build_dll died 3h20m in with every step from ''Build dll with v8'' onwards left in_progress, no job log at all (404 BlobNotFound while the setup job of the same run returns 200), and a single job-level annotation ''Fatal glibc error: pthread_mutex_lock.c:130 (___pthread_mutex_lock): assertion failed: mutex->__data.__owner == 0''. That is a dead runner, not the build: the identical annotation and identical shape appear on two other packages on two other runners in the same window -- tensor-grep at 11 minutes and litellm 7 SECONDS after its job started -- 3 of 749 failed jobs over eight days, which rules out any resource wall or anything mini-racer''s build did. New gotcha 458 records the diagnosis (no log + unfinished steps => read /check-runs/<job id>/annotations, correlate across packages, re-run rather than push). Re-ran the dead job in place via rerun-failed-jobs on 2026-09-20 -- same commit, same merge ref, no push, so the rest of the matrix and the 48h ceiling are untouched. Attempt 2 (job 106152918611) then hit a SECOND, different fleet fault and died in 14 seconds: it landed on machine riscv-runner-11, whose Docker daemon is not running (''Cannot connect to the Docker daemon at unix:///var/run/docker.sock'' in Initialize containers, then ''Value cannot be null. (Parameter ''''network'''')''), so every container job that node picks up fails instantly -- worth draining if it keeps taking work. Attempt 3 (job 106153424056) queued 2026-09-20 21:36Z behind a busy pool (37 riscv64 jobs running, 22 queued), picked up a healthy node (rise-riscv-runner-ouemsff1n) at 21:37Z and cleared the checkpoint: Initialize containers green, checkout + git apply of all eleven patches green, now in the build. Expect ~27h in Build dll with v8, i.e. green around 2026-09-22 00-01Z, then build_wheel (four pytest legs). If it dies in seconds on Initialize containers again that is another sick node, not the port -- just rerun-failed-jobs, it costs nothing. Do NOT push to this branch: every push restarts the ~27h V8 compile. No rebase is needed either -- the branch adds only new files (build-mini-racer.yml, docs/packages/mini-racer.yaml, patches/mini-racer/0.14.1/**), _setup.yml / _publish-wheel.yml / pr-checks.yml have not changed since the branch caught up with them on 2026-09-19, and reusable workflows resolve from the PR merge ref anyway (gotcha 158). All four PR checks green; awaiting build_dll attempt 2. Merge is a maintainer call on the ~27h-per-release runner cost, which the PR body flags explicitly.'
- pkg: brotlipy
  version: 0.7.0
  home: https://github.com/python-hyper/brotlipy/
  repo: https://github.com/python-hyper/brotlipy/
  status: parked
  pr: null
  notes: 'Superseded/unmaintained, no cp310+ support anywhere. `github.com/python-hyper/brotlipy` and `github.com/python-hyper/brotlicffi` are the same GitHub repo (id 43543393, confirmed via `gh api`): the project was renamed from brotlipy to brotlicffi years ago and development continued exclusively under the new name (actively maintained, commits as recent as 2026-08-24, Python 3.13/3.14 and free-threaded support). The PyPI brotlipy package was never updated after the rename -- its last release stayed at 0.7.0 (2017-05-30), whose files list tops out at cp39/pp37 with no cp310+ wheel on any platform (verified against the real PyPI JSON), so this is upstream abandonment, not a riscv64-specific gap. Both modern replacements are already ported to riscv64 in this repo and live on the registry: build-brotli.yml and build-brotlicffi.yml (pypi.riseproject.dev/simple/brotli/ and /brotlicffi/ both return 200). No worktree/branch/PR created -- diagnosed read-only against the GitHub API (repo id/rename, commit history), the real brotlicffi README, and the live PyPI/registry JSON.'
- pkg: livekit-plugins-noise-cancellation
  version: 0.3.0
  home: https://docs.livekit.io/home/cloud/noise-cancellation/
  repo: ''
  status: parked
  pr: null
  notes: 'Not feasible (vendored-binary, closed vendor: gotchas 157/183, new gotcha 431). No source exists to build: 13 releases on PyPI, five platform wheels each (macosx_10_9_x86_64, macosx_11_0_arm64, manylinux_2_28_{x86_64,aarch64}, win_amd64) and ZERO sdists at any version, so there is no setup.py, no download script and no platform table to patch. Upstream ships no source either -- livekit/agents/livekit-plugins/ has ~80 plugin directories and no noise-cancellation among them (only a differently-named livekit-plugins-krisp), a global GitHub code search for liblivekit_nc_plugin returns nothing but other people''s committed site-packages copies, and no Cargo.toml anywhere defines the crate. The 73MB wheel is 4KB of Python plus a 39MB resources/liblivekit_nc_plugin.so and 64MB of three opaque .kef model files. `strings -a` on the .so proves the vendor is closed: /var/lib/jenkins/.conan/data/... paths name nine private Conan packages in a krisp/prod channel (krisp-core 2.0.41, krisp-inference-engine 2.2.23, krisp-nc-processor 4.0.9, krisp-dsp, krisp-blas, krisp-mlops, krisp-common, krisp-audio-stream, fftw 3.3.10_7@krisp/stable); the open-source cargo crates in the same output (ureq/rustls/ring/serde_json) are just the licence-check HTTP shim around that closed DSP core. Krisp''s SDK is a commercial prebuilt-binary product listing Windows/Mac/Linux/Web/iOS/Android with no RISC-V. Licensing blocks it independently: License is "SEE LICENSE IN https://livekit.io/legal/terms-of-service" (proprietary ToS, not OSS), so the .kef models could not be republished on our index. Function is also gated on LiveKit Cloud (gotcha 183: primary function unreachable, not merely degraded). Second-order blocker: the mandatory livekit>=0.21.3 dep is itself py3-none-<platform> over the same five platforms with no riscv64 wheel. No worktree/branch/PR created.'
- pkg: mlx
  version: 0.32.2
  home: https://github.com/ml-explore/mlx
  repo: https://github.com/ml-explore/mlx
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1874
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. CPU backend is portable (generic BLAS/LAPACK via find_package, scalar SIMD fallback, no x86-only intrinsics); builds both the mlx frontend and mlx-cpu backend upstream splits releases into, publishing each as its own package. CI green on PR #1874: mlx (cp312/cp313/cp314/cp314t) and mlx-cpu riscv64 wheels built and tested (873-test suite, cp314t deselects one free-threading cache-timing test per ml-explore/mlx#4377); publish jobs dry-ran cleanly. Awaiting maintainer review/merge; not yet published.'
- pkg: open3d
  version: 0.19.0
  home: https://www.open3d.org
  repo: https://github.com/isl-org/Open3D
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 436 + 366, feasibility-and-triage.md): hard-blocked on Embree, which Open3D 0.19.0 requires unconditionally. cpp/open3d/t/geometry/CMakeLists.txt compiles RaycastingScene.cpp in both the SYCL and the non-SYCL branch, Open3D::3rdparty_embree is appended to Open3D_3RDPARTY_PRIVATE_TARGETS_FROM_CUSTOM with no BUILD_*/WITH_*/USE_* off switch anywhere in the tree, and the class it backs is documented public API (o3d.t.geometry.RaycastingScene), so there is no honest reduced wheel to fall back to. Embree 4.3.3 recognizes exactly two CPU families: EMBREE_ARM (Apple silicon, or CMAKE_SYSTEM_PROCESSOR == aarch64/ARM64) and x86 for everything else. common/simd/ holds sse/avx/avx512 plus an arm/ sse2neon shim and a wasm/ shim -- no generic, portable or scalar backend -- and common/sys/intrinsics.h falls through to `#include <immintrin.h>` whenever __ARM_NEON is undefined. riscv64 matches neither family, so both routes fail: the x86 branch it would take passes -DEMBREE_ISA_AVX=ON -DEMBREE_ISA_AVX2=ON (-mavx/-mavx2, rejected by a riscv64 GCC), and forcing every EMBREE_ISA_* OFF the way the Open3D aarch64 branch does hits `CMake Error at CMakeLists.txt:636 (MESSAGE): You have to enable at least one ISA!` -- reproduced locally against the real v4.3.3 tag on an x86 host, where EMBREE_ARM is OFF exactly as it would be on riscv64. Zero "riscv" occurrences in embree master CMakeLists.txt; the upstream README documents x86 (Linux/macOS/Windows) and ARM (Linux/macOS) CPUs only. Not fixed by a version bump: Open3D main still has zero riscv occurrences, still gates only on aarch64, and moves embree to v4.4.0 -- the same wall gotcha 366 hit from the embreex side. Closing it means writing an SSE/AVX-to-RVV shim for the Embree kernels, an upstream engineering project rather than a patches/<pkg>/<version>/ fix, and goal 2 has nothing to mirror (upstream has no riscv64 code path or CI). Secondary and independent, recorded as gotcha 436: every arch fallback in the tree keys off a single LINUX_AARCH64 boolean (CMakeLists.txt, execute_process(COMMAND uname -m) matched against the literal string aarch64), so riscv64 takes the x86_64 else() branch for five prebuilt x86_64-only downloads -- MKL static (mkl_static-2024.1.0-linux_x86_64.tar.xz), Filament (filament-v1.9.19-linux-20.04.tgz), WebRTC (webrtc_60e6748_cxx-abi-1.tar.gz), the ISPC compiler binary, and prebuilt VTK 9.1. All five have the from-source route the upstream aarch64 path already uses, so they are configuration work rather than blockers, but Filament-from-source additionally hard-requires Clang >= 6.0 (FATAL_ERROR "Detected C compiler ${CMAKE_C_COMPILER_ID} is unsupported") on our GCC-based manylinux images, and BUILD_GUI=OFF would drop open3d.visualization.{gui,rendering,draw}. WITH_IPP self-disables via IPP_SUPPORTED_HW (AMD64 x86_64 x64). Also disproportionate even if Embree were solved: the slimmest official non-x86 config upstream maintains (docker/Dockerfile.openblas, exercised by .github/workflows/ubuntu-openblas.yml on a GCE t2a-standard-4; BUILD_SHARED_LIBS=OFF, CUDA/PyTorch/TensorFlow/SYCL all OFF) builds OpenBLAS + VTK 9.1 + Filament + Embree + assimp/curl/boringssl/TBB/qhull from source and then 753 Open3D TUs (250 kLoC), once per interpreter (cp38-cp312 = 5 cp3X-cp3X builds, no abi3 or py3-none collapse), for a ~450 MB wheel each -- against mediapipe (PR #2104) at 5h23m of build plus 2h24m-3h21m of queue wait per job on the same shared riscv64 pool. And there is no sibling-arch wheel to mirror (gotcha 418): upstream publishes no non-x86 Linux wheel at all for 0.19.0 (5x manylinux_2_31_x86_64, 2 macOS variants, win_amd64), so the openblas config is CI-only and never becomes a published artifact. See the open3d-cpu entry for why that sibling name is unreachable on riscv64 as well. No worktree/branch/PR created; resolved read-only from the real v0.19.0 checkout (CMakeLists.txt, 3rdparty/, cpp/open3d/t/geometry, python/setup.py, docker/, .github/workflows/) plus one local x86 cmake configure of embree v4.3.3.'
- pkg: comfy-angle
  version: 0.1.1
  home: https://github.com/Comfy-Org/comfy-angle
  repo: https://github.com/Comfy-Org/comfy-angle
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2123
  notes: 'Feasible and ported - despite the "Redistributable ANGLE libraries" summary and zero
    sdists across all 9 releases, this is NOT a prebuilt-blob redistribution like
    livekit-plugins-noise-cancellation (gotcha 431). Upstream unpacks an Electron release only for
    Windows/macOS; the Linux wheels are genuinely built from the pinned ANGLE revision by
    scripts/build_linux.py (depot_tools + gclient + gn + autoninja, headless, Vulkan-only, no X11 /
    Wayland / GBM, tests / SwiftShader / dawn / GL / wgpu / validation-layers all off), so riscv64
    needed only the architecture added to that build rather than a Chromium-scale port. Every layer
    below already supports riscv64, which is what makes this cheap: depot_tools detect_host_arch.py
    maps riscv* to riscv64; CIPD publishes linux-riscv64 for the cipd client, cpython3, ninja and gn;
    Chromium build/toolchain/linux/BUILD.gn has gcc_toolchain("riscv64") with toolprefix
    riscv64-linux-gnu, which BUILDCONFIG selects once is_clang=false, and config/compiler/BUILD.gn
    carries riscv64 cflags; ANGLE''s own gni/angle.gni lists riscv64 among the 64-bit CPUs, so gn
    analysis does not hit its "Unknown current CPU" assert. The real gap is CIPD: three DEPS entries
    pull packages with no linux-riscv64 build and abort gclient sync before anything compiles -
    buildtools/reclient (infra/rbe/client), third_party/siso/cipd (build/siso) and tools/luci-go
    (isolate and swarming; only its cas tool is published for riscv64). None is reachable from a
    local build, so the patch deletes the three entries from the checkout''s DEPS before the sync;
    reclient has no linux-arm64 build either, which is why upstream''s DEPS already carves arm64 out
    of that dep. The patch otherwise reuses upstream''s
    existing arm64 recipe (alias the container''s gcc/g++/ar/readelf/nm under the prefixed names
    ANGLE expects, is_clang=false, use_custom_libcxx=false, treat_warnings_as_errors=false),
    generalised to a toolprefix table, and the new tables are all keyed by arch so x86_64/arm64
    render a byte-identical .gclient and byte-identical gn args against an untouched DEPS. The x86-only prebuilt host tools the DEPS hooks fetch
    (clang, llvm_objdump, rust, glslang_validator, flex_bison) are not a blocker: update.py maps
    every Linux host to Linux_x64 with no arch check, and upstream''s green aarch64 job proves those
    binaries are never executed by the libEGL/libGLESv2 targets. Wheel is a single
    py3-none-manylinux_2_39_riscv64 (2_28 is not published for riscv64) with no interpreter matrix,
    but the smoke test still loads both libraries and prints the EGL client extension string on
    cp312/cp313/cp314/cp314t (gotcha 145). Licensing: the riscv64 libraries are compiled rather than
    extracted from Electron, so electron-LICENSE and LICENSES.chromium.html genuinely do not apply -
    the patch stages ANGLE''s LICENSE plus an aggregate of the third_party LICENSE/LICENCE/COPYING
    files (collected by directory, so it over-reports rather than miss a statically linked
    component), and a gpl_sources job collects the image''s gcc since the libs are built with it and
    link its libstdc++. Not rehearsed locally: no docker daemon on this host and only 5 GB free, so
    a gclient sync plus a full ANGLE build does not fit. Validation was static - patch applies with
    patch --dry-run --fuzz=0, py_compile of both patched scripts, a unit test of the new
    stage_licenses() against a synthetic checkout (including non-UTF8 licence bytes), bash -n of the
    container script and compile() of both heredocs, and pending_versions.py resolving 0.1.1. PR #2123 open; four CI rounds so far - the first three never got out of gclient sync, the fourth built everything.
    Round 1 (~2 min): vpython3 could not build its venv, since infra/python/wheels publishes no
    linux-riscv64 crcmod/lxml (gotcha 423) - fixed by setting VPYTHON_BYPASS for riscv64 hosts in
    build_linux.py. Round 2 (~4 min): the bypass worked and moved the wall one layer down - with
    no vpython venv, gclient.py has no httplib2 of its own, and gerrit_util imports httplib2 and
    httplib2.socks at module scope (ModuleNotFoundError: No module named ''httplib2'', ~80 s in) -
    fixed in the workflow with pip install httplib2==0.13.1, the version depot_tools'' own
    .vpython3 pins; unpinned resolves to 0.32.0, which no longer ships the httplib2.socks that
    import needs (new gotcha 441). Round 3 (~7 min): the httplib2 pin worked - gclient fetched
    depot_tools and ANGLE, evaluated the whole DEPS graph and reached the "cipd ensure" that ends
    the sync, which then failed to resolve four packages with no linux-riscv64 build:
    infra/rbe/client, build/siso and the luci isolate and swarming tools. So the custom_deps
    null-out the patch carried for reclient and siso had never done anything: gclient applies
    custom_deps while resolving a "git" dep''s URL, whereas a "cipd" entry is copied out of DEPS
    into a CipdDependency that never consults custom_deps, so a None there is accepted and
    silently ignored (new gotcha 445, verified by reading gclient.py at the revision the run
    pinned). This round was avoidable: gotcha 424 already says to audit a chromium-style DEPS
    with cipd describe before building, and already names these exact four packages - the
    earlier rounds trusted the custom_deps null-out instead of checking it. Round 4 deletes the three DEPS entries outright before the sync and raises if one is
    missing, and adds use_siso=false to the gn args - ANGLE''s .gn sets use_siso=true in
    default_args for a standalone checkout (the earlier siso.gni reading was wrong), and autoninja
    reads use_siso back out of args.gn, so dropping the siso package without that arg would have
    swapped one failure for another one layer later. Validated off-target: drop_deps() run against
    the real pinned DEPS removes exactly those four packages (421 of 426 CIPD packages kept, the
    fifth being luci cas from the same entry), the rewritten DEPS still parses to 495 deps and 23
    hooks, and driving main() for all three arches confirms x86_64/arm64 are byte-identical. That round-2 diagnosis was settled off-
    target on x86 in seconds rather than in CI: the failure reproduces exactly under a bare cp312
    venv against the pinned depot_tools tree (fetched as a gitiles archive, no clone), and with
    httplib2 installed every module the sync path touches imports cleanly and a real
    download_from_google_storage hook (angle-glslang-validator) fetches its object and exits 0 -
    so no cryptography/crcmod chase and no extra index are needed, and the pyo3 PanicException a
    host python with a system cryptography produces there is a host artifact. ANGLE pins
    third_party/depot_tools at the same revision the driver clones, so gotcha 427''s gsutil
    4.68/six split does not apply (both are 5.35, fine on 3.12), and the Linux hooks that follow
    (clang, llvm_objdump, rust, lastchange, configure_siso) import nothing outside the stdlib and
    depot_tools. Still not rehearsed end to end locally (no docker daemon, 4.5 GB free). Round 4
    (~64 min) closed every open question at once: gclient sync completed, all 23 hooks ran, gn gen
    with the prefixed GCC toolchain worked, and ninja built all 493 targets - including
    third_party/vulkan-loader/src/libvulkan.a, so the statically linked Vulkan-Loader does take its
    portable C unknown-ext-chain path on riscv64 and needs no per-arch assembly. Both of gotcha
    438''s watch items are therefore closed. libEGL.so and libGLESv2.so linked, scripts/build.py
    produced the 3.4 MB comfy_angle-0.1.1-py3-none-manylinux_2_39_riscv64.whl with all four
    expected members, and the smoke test loaded both libraries and printed the real EGL client
    extension string (EGL_ANGLE_platform_angle_vulkan among them) on cp312, cp313 and cp314; the
    gpl_sources job passed in parallel. The job then failed on the very last line of the container
    script with exit 127, purely because the smoke-test loop spelled the free-threaded interpreter
    /opt/python/cp314t-cp314t while manylinux names that directory cp314-cp314t, so pip was not
    there and set -e took the step down after the whole build had already succeeded (new gotcha
    446; the only compile diagnostics in the log are warnings, none of them riscv64-specific).
    Round 5 changes that one path and nothing else. The fix needed no CI to validate:
    build-mujoco.yml drives the same manylinux_2_39_riscv64 image with /opt/python/cp314-cp314t and
    docs/packages/mujoco.yaml lists published cp314-cp314t riscv64 wheels, while
    build-onnxruntime.yml, build-labmaze.yml and build-vtk.yml all derive the directory as
    ${TAG%t}-${TAG}. Round 5 confirmed it: build succeeded, smoke test passed on all four
    interpreters, and the publish dry run succeeded. PR ready for review/merge.'
- pkg: solders
  version: 0.29.0
  home: https://github.com/kevinheavey/solders
  repo: https://github.com/kevinheavey/solders
  status: published
  pr: 1650
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zensical
  version: 0.0.57
  home: https://zensical.org/
  repo: https://github.com/zensical/zensical/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1871
  notes: '8 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. PR #1871: maturin/pyo3, abi3-py310, pure Rust workspace. Build once on cp310 (mirrors upstreams --interpreter python3.10 pin), reuse+retest cp312/cp313/cp314; musllinux dropped (no riscv64 musl rustup toolchain); cp310 test skipped (tomli has no cp310 riscv64 wheel on registry yet).'
- pkg: hidapi
  version: 0.15.0
  home: https://github.com/trezor/cython-hidapi
  repo: https://github.com/trezor/cython-hidapi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1097
  notes: '48 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1097 builds hid+hidraw against libusb1-devel/systemd-devel. Merged (#1097). Issue #1146 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: vl-convert-python
  version: 1.9.0.post1
  home: https://github.com/jonmmease/vl-convert
  repo: https://github.com/jonmmease/vl-convert
  status: parked
  pr: null
  notes: 'Not feasible today: vl-convert-python 1.9.0.post1 (source is the vega/vl-convert monorepo; jonmmease/vl-convert is a stale fork, last pushed 2025-01, kept only for the crates.io homepage/repository metadata) builds vl-convert-rs, which pulls in deno_core 0.307.0 -> the v8 (rusty_v8) crate pinned by deno_core''s own Cargo.toml as `v8 = "0.105.0"` (caret-compatible, resolves to 0.105.1 in the checked-in Cargo.lock). Checked the GitHub Releases API for rusty_v8 tag v0.105.1: prebuilt static libs exist only for aarch64-apple-darwin, aarch64-unknown-linux-gnu, x86_64-apple-darwin, x86_64-unknown-linux-gnu and x86_64-pc-windows-msvc -- no riscv64 asset. The V8_FROM_SOURCE fallback in that same build.rs has no riscv64 wiring either: its target_cpu GN-arg mapping only covers aarch64/arm/i686/x86_64, so even a from-source build has nothing to build against (denoland/rusty_v8#1476 tracks exactly this failure). rusty_v8 only started shipping riscv64gc-unknown-linux-gnu prebuilt binaries at v150.1.0 (published 2026-07-10) -- about 150 releases/two years past the 0.105.1 pinned here. Closing that gap would require bumping deno_core/deno_runtime/deno_emit/deno_graph across that whole span and likely adapting vl-convert-rs''s own V8-facing source, which is a fork-scale change to a third-party dependency chain, not something patches/<pkg>/<version>/ can carry -- and even vl-convert''s own unreleased v2.0.0-rc5 is only at deno_core 0.411.0, still short of the riscv64-capable v8 150+ line. No riscv64 gap to close until vl-convert itself bumps its deno_core/v8 pins that far. Worktree/branch created locally then removed, never pushed; no PR opened.'
- pkg: coolprop
  version: 8.0.0
  home: http://www.coolprop.org
  repo: https://github.com/CoolProp/CoolProp
  status: published
  pr: 1798
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ast-grep-py
  version: 0.45.2
  home: https://ast-grep.github.io/
  repo: https://github.com/ast-grep/ast-grep
  status: published
  pr: 1641
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libusb-package
  version: 1.0.30.0
  home: https://github.com/pyocd/libusb-package
  repo: https://github.com/pyocd/libusb-package
  status: published
  pr: 1870
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Builds libusb from the vendored src/libusb submodule (setup.py autoreconf+configure+make), same as upstream does for every platform; PR also patches setup.cfg to ship libusb''s own LGPL-2.1 licence, missing from every upstream wheel.'
- pkg: grain
  version: 0.2.18
  home: https://github.com/google/grain
  repo: https://github.com/google/grain
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2124
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Feasible: the compiled surface is one pybind11 extension (index_shuffle, statically linking abseil-cpp) plus a generated protobuf module, and array-record 0.8.3 - the dependency that would have blocked this - is already published here for cp312/cp313/cp314 (no cp311, which sets the matrix). Bazel bootstrapped from source at 7.5.0 like array-record/ray/labmaze, sharing their actions/cache key. Upstream''s `bazel build ...` is unusable on riscv64: 85 BUILD deps reach the @pypi hub, whose test_requirements lock pins jaxlib==0.8.0 and scipy with hashes, and jaxlib has no riscv64 wheel and no sdist - so only the three targets whose output reaches the wheel are built (verified locally in manylinux_2_34_x86_64: the module graph resolves and all three analyse with zero @pypi/jaxlib/scipy fetches). rules_python 1.6.0 carries riscv64 CPython for 3.11-3.14 inclusive. Tests (gotcha 439): upstream''s default CI mode is `bazel test`, one process per py_test target, and its OSS pytest fallback is not a tested path - `pytest --pyargs grain` in one process makes data_loader_test''s 120 tests fail that pass 120/120 alone, and hangs around 4%. So each of the 45 test files runs as its own process, invoked as a module (`python -m grain...`) because ipc/queue.py shadows the stdlib queue when its own directory is sys.path[0], which killed queue_test and variable_size_queue_test; with the env/args upstream''s rules supply (--test_srcdir on data_loader/data_sources/tfrecord_dataset, EXPECTED_FRAMEWORK=NO_FRAMEWORK, PYTHON_VERSION). 9 files dropped: 8 need jax or tensorflow (batch_test among them - it reads sys.modules["jax"] without importing it and its py_test declares @pypi//jax), and multiprocessing_test, which upstream declares no target for and whose __main__ calls an undefined name. dataset_test goes through pytest so grain''s own RUN_IN_PYTEST expectedFailure applies to its two execution-summary tests, which wait on a summary-logging thread that only reports under upstream''s runner. All of that verified in manylinux_2_34_x86_64 against upstream''s released cp312 wheel: data_loader 120, dataset_test 156, traceback_util 18, queue 6, variable_size_queue 19, and no failures in the shipped configuration. Wheel also gains LICENSE.abseil-cpp/LICENSE.pybind11 (both statically linked, upstream ships neither); confirmed setuptools 82 globs them into dist-info/licenses. Local ceiling: no riscv64 bazel compile (the session proxy 403s github.com/pybind/pybind11/archive/*). PR #2124 open; every check still queued on the org''s Actions backlog, so CI has not been proven green yet. Run 35492217884 then came back fully green while building a single wheel: the three interpreter legs were include-only entries ({tag, python}) on a version-only matrix, so GitHub merged them into one combination and only `Build grain 0.2.18 cp314-manylinux_riscv64` ran - gotcha 402, inherited from build-array-record.yml, which grain was derived from. Fixed by making `tag: [cp312, cp313, cp314]` a real dimension crossed with version, the include entries now only mapping tag -> python; expansion re-checked against GitHub''s documented include semantics (3 jobs per version, 6 for two pending versions) and run 35503063718 is the first to queue three build jobs. The same collapse is latent in 58 other jobs repo-wide (noted on gotcha 402; protobuf-py-ext, primp, arro3-core and rigour 2.5.0 have already published incomplete wheel sets). Run 35503063718 confirmed the fix: all three legs (cp312, cp313, cp314) built and the publish dry run succeeded. PR ready for review/merge.'
- pkg: ruptures
  version: 1.1.10
  home: https://github.com/deepcharles/ruptures/
  repo: https://github.com/deepcharles/ruptures/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1809
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython + C extension (memoryviews, no numpy C API); build-from-checkout cibuildwheel workflow, cp312/cp313 only (Requires-Python excludes 3.14+). PR #1809 open, CI running.'
- pkg: saxonche
  version: 13.0.0
  home: https://www.saxonica.com/saxon-c/index.xml
  repo: https://www.saxonica.com/saxon-c/index.xml
  status: parked
  pr: null
  notes: 'Not feasible: the 87% of the wheel that matters is a GraalVM Native Image, and
    GraalVM Native Image ships no riscv64 build from anyone (new gotcha 450,
    feasibility-and-triage.md; same vendored-payload family as gotcha 35/157/183/376/431).
    Verified against the real artifacts, not inferred. saxonche 13.0.0 publishes 35 wheels,
    all genuinely cpXY-cpXY (a real compiled extension, so gotcha 27 does not apply) and has
    never shipped an sdist on any of its 14 releases, so per gotcha 431 the wheel is the only
    evidence: downloading and unzipping
    saxonche-13.0.0-cp312-cp312-manylinux_2_24_aarch64.whl (41.7 MB) gives three native
    artifacts -- saxonche.cpython-312-aarch64-linux-gnu.so (7.8 MB, a genuine Cython
    extension built from the saxonc/saxonc.cpp the wheel also ships),
    saxonche.libs/libsaxonc-he-*.so.13.0.0 (617 KB, the C++ API glue) and
    saxonche.libs/libsaxonc-core-he-*.so.13.0.0 at 109 MB (87% of the wheel). `strings -a` on
    that last one reports ''GraalVM CE 25.0.1+8.1 (serial gc)'', com/oracle/svm/core/... and a
    .svm_heap section: it is the entire SaxonJ Java engine AOT-compiled by GraalVM Native
    Image, which is exactly what Saxonica''s own release notes say (''SaxonC 13 is built from
    SaxonJ 13 using GraalVM Native Image (version 25.0.1)''). `readelf -d` shows the Cython
    extension carries RPATH $ORIGIN/saxonche.libs and a hard NEEDED on
    libsaxonc-core-he-*.so.13.0.0 -- not a dlopen -- so with no riscv64 core there is no
    importable wheel at all, stricter than gotcha 157''s claude-agent-sdk (whose sdist at
    least imports). Licence/source are NOT the blocker here, which is why this is not simply
    the tableauhyperapi/gotcha 376 shape: SaxonC-HE is MPL-2.0 and Saxonica does publish
    SaxonC-HE source on the Saxon-HE GitHub releases -- but the zip that exists
    (SaxonCHE-source-12-9-0.zip, 242 KB, 91 files: the C/C++ glue under src/main/c/, the
    net.sf.saxon.option.cpp Java bridge under src/main/java/, the Cython python/saxonc/
    saxonc.pyx) contains zero build files (no Makefile/CMakeLists/pom.xml/setup.py, no
    native-image configuration of any kind) and its own README calls it ''the source files
    used to build SaxonC-HE'', not a build; and for the queued version there is no such drop
    at all -- the SaxonHE13-0 release carries only SaxonHE13-0J.zip and saxon13-0source.zip
    (both SaxonJ), and downloads.saxonica.com/SaxonC/HE/13/SaxonCHE-source-13-0-0.zip is 404.
    The toolchain is the wall, confirmed on three independent distributions of GraalVM Native
    Image: download.oracle.com/graalvm/25/latest/graalvm-jdk-25_linux-{x64,aarch64}_bin.tar.gz
    -> 200 but linux-riscv64 -> 404; GraalVM CE jdk-25.0.1 (the exact version this wheel was
    built with) publishes graalvm-community-jdk-25.0.1_linux-{x64,aarch64}_bin.tar.gz but no
    linux-riscv64 -> 404; Mandrel (Red Hat''s native-image-only distribution)
    mandrel-java25-linux-{amd64,aarch64}-25.0.1.0-Final.tar.gz -> 200, riscv64 -> 404. Saxonica''s
    own artifact table agrees one layer up: downloads.saxonica.com/SaxonC/HE/13/ serves
    SaxonCHE-linux-x86_64-13-0-0.zip and SaxonCHE-linux-arm64-13-0-0.zip (200) and 404s every
    riscv64 spelling. The one thing that looks like a green light was checked and is not one:
    Platform.LINUX_RISCV64 has been a Native Image leaf platform since 22.2 and oracle/graal
    master does carry ELFMachine.RISCV64 with a full ELFRISCV64Relocation table (verified by
    reading both files), but riscv64 is reached only through the LLVM backend, whose own doc
    states it ''is not included by default as part of Native Image'' (you build GraalVM from
    source with `mx --dynamicimports /substratevm build` and pass --tool:llvm-backend), and
    the riscv64 port behind the 2023 GraalVM blog post was a GraalVM dev build. So porting
    this would mean bootstrapping a riscv64 GraalVM from source with an unshipped experimental
    backend and then reinventing a native-image build recipe (entry points, reflection and
    resource config) Saxonica has never published, for a version whose SaxonC source is not
    published either -- the opposite of goal 2 (mirror upstream''s own CI, narrowed to
    riscv64). Revisit if GraalVM/Mandrel ever ship a riscv64 native-image distribution AND
    Saxonica publishes a SaxonC build. Note saxonche/saxoncpe/saxoncee are one engine behind
    three licence tiers, so the same verdict applies to the saxoncee and saxoncpe queue
    entries (PE/EE additionally have no published source and need a paid licence key). No
    worktree/branch/PR created -- this was settled before any code was written.'
- pkg: tableauhyperapi
  version: 0.0.26359
  home: https://tableau.github.io/hyper-db
  repo: https://tableau.github.io/hyper-db
  status: parked
  pr: null
  notes: 'Not feasible: closed-source proprietary Hyper database engine, no source ever
    published, no riscv64 build from Tableau at any layer (gotcha 376,
    feasibility-and-triage.md). Confirmed by downloading and unzipping the real 0.0.26359
    manylinux2014_x86_64 wheel directly, not assumed from the name: it is 89MB, and the
    payload is not a compiled Python extension but two prebuilt native artifacts --
    tableauhyperapi/bin/hyper/hyperd (255MB uncompressed, a stripped x86-64 PIE ELF --
    this *is* Tableau''s proprietary in-process Hyper database engine, launched as a
    subprocess by hyperprocess.py) and tableauhyperapi/bin/libtableauhyperapi.so (43MB,
    stripped x86-64 ELF, dlopen()''d directly via cffi ABI mode in impl/dll.py: `lib =
    ffi.dlopen(str(find_hyper_api_library()))`; impl/cdef_compiled.py is only cffi type
    declarations, not C source -- no API-mode fallback exists anywhere). There is no
    setup.py/pyproject.toml build step and zero sdist across the entire PyPI release
    history (152 files across all 46 versions ever published, confirmed via the full
    releases JSON, every one a wheel) -- gotcha 77''s "build it yourself" escape hatch
    does not exist because there is nothing upstream ever published to build *from*.
    Unlike hdbcli (gotcha 372), the wheel''s own LICENSE is a plain, unmodified
    Apache-2.0 grant -- no restrictive EULA blocks redistribution -- but that is moot
    without source: the bundled HYPER_API_OSS_disclosure.txt lists ~40 third-party OSS
    components compiled into hyperd (abseil, Arrow, LLVM 22.1.7, PostgreSQL, protobuf,
    zstd, etc.) but conspicuously never lists the Hyper engine''s own core code, i.e. the
    Apache-2.0 grant covers the wrapper and lets those dependencies'' licenses pass
    through cleanly, it does not make the engine itself open source. Tableau''s own
    installation docs (tableau.github.io/hyper-db/docs/installation/) state the hardware
    requirement explicitly: "Intel Nehalem, Apple Silicon or AMD Bulldozer processor or
    newer" -- x86_64 and Apple-Silicon ARM64 only, no RISC-V, stated as policy rather
    than inferred from an absence. PyPI''s own file list for 0.0.26359 corroborates it
    structurally: macosx_10_11_x86_64, macosx_13_0_arm64, manylinux2014_x86_64,
    win_amd64 -- no manylinux*_aarch64 wheel exists either, so even Linux ARM64 users
    get nothing, let alone riscv64. (hyperd''s strings do contain hundreds of
    riscv/aarch64 symbols such as llvm::RISCVVType and ELFLinkGraphBuilder_riscv, but
    these are LLVM''s own JIT backend code compiled in because Hyper uses LLVM to
    JIT-compile query plans -- not evidence Hyper itself runs on those host
    architectures.) Same shape as the already-parked hdbcli/mssql-python-odbc entries:
    a proprietary vendor binary with zero source and a vendor-stated architecture
    ceiling that excludes riscv64. No worktree/branch/PR created -- diagnosed read-only
    against the real 0.0.26359 wheel contents (unzip -l, file/strings on both native
    binaries) and Tableau''s own installation/hardware-requirements documentation.'
- pkg: numpy-quaternion
  version: 2024.0.13
  home: https://github.com/moble/quaternion
  repo: https://github.com/moble/quaternion
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1207
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastwarc
  version: 1.0.9
  home: https://resiliparse.chatnoir.eu
  repo: https://github.com/chatnoir-eu/chatnoir-resiliparse
  status: published
  pr: 1652
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ai-edge-litert
  version: 2.2.0
  home: https://www.tensorflow.org/lite/
  repo: https://github.com/google-ai-edge/LiteRT
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2135
  notes: 'CI fully green on all four legs (cp311/cp312/cp313/cp314) plus the publish dry run; PR un-drafted, ready for review/merge. 10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Feasible, and explicitly NOT the tensorflow/tensorflow-cpu park (gotcha 426) or the jaxlib/PR #526 XLA-codegen wall: LiteRT really is the lighter build its name promises. Upstream source is google-ai-edge/LiteRT (verified: PyPI lists only www.tensorflow.org/lite as a URL, but tag v2.2.0 = 145c7523 has litert/version.bzl LITERT_EXPERIMENTAL_VERSION = "2.2.0" and ci/tools/python/wheel builds the ai_edge_litert distribution); no sdist on any release, so the wheel is the evidence per gotcha 431. EVIDENCE THE BUILD IS SMALL: unzipping the released cp312 manylinux_2_27_aarch64 wheel (14.0MB) gives 23MB of compiled payload - libpywrap_litert_common.so 17.1MB, libLiteRt.so 5.8MB, and 13 pywrap stubs of ~6.5KB each - i.e. no MLIR/LLVM is linked in. That matches the BUILD: //ci/tools/python/wheel:litert_wheel takes //litert/python:pywrap_litert_binaries, and the MLIR converter (//litert/python/mlir, libLiteRTCompilerMLIR.so) sits behind the build_converter bool_flag which defaults False. Calibrated against PR #2104 (mediapipe), which compiles a much larger subset of the same TF tree plus OpenCV on the same image in ~5h48m. VENDORED BLOB IS OPTIONAL, unlike saxonche/gotcha 450: the third 16.9MB file in the aarch64 wheel, libLiteRtWebGpuAccelerator.so, is a prebuilt from @litert_prebuilts, and litert/build_common/special_rule.bzl''s litert_gpu_accelerator_prebuilts() select has a //conditions:default of [] - so riscv64 simply gets no GPU accelerator. Confirmed harmless by running the released x86_64 wheel locally: gpu_registry.cc dlopens it ("Attempting to load GPU accelerator(...)") and already tolerates the miss for libLiteRtGpuAccelerator.so, falling back to XNNPACK CPU. Same for PACKAGE_DATA, whose vendor compiler plugins are selected on //litert:linux_x86_64 only. FOUR riscv64 GAPS FOUND, all cheap: (1) ci/tools/python/wheel/utils/platforms.bzl maps only x86_64/arm64/macos/windows and falls through to the literal string "none", so the wheel would be named ai_edge_litert-2.2.0-cp312-cp312-none.whl - this is the one patch in the PR, and unlike tensorflow''s platform_tag select it does not fail at analysis, it just produces a useless tag; (2) WORKSPACE register_toolchains() lists only @rules_ml_toolchain//cc:linux_x86_64_linux_x86_64, so riscv64 falls back to bazel''s autodetected @local_config_cc - USE_HERMETIC_CC_TOOLCHAIN was read and only gates CUDA/NCCL configure in rules_ml_toolchain 398d613a, so it does not block this; (3) hermetic pip resolves TF''s hash-pinned requirements_lock_3_*.txt, whose numpy (2.1.3 for <=3.13, 2.4.4 for 3.14) has no riscv64 wheel on PyPI - solved with no patch via rules_ml_toolchain''s own HERMETIC_REQUIREMENTS_LOCK repo_env, handing it the upstream lock with the numpy block rewritten to a file:// direct reference to our registry''s wheel (rewrite validated against all three real lock files); (4) upstream''s build script exits 1 on `uname -m` = riscv64, avoided by invoking the wheel target directly rather than patching a script we do not run. Hermetic Python is NOT a gap: TF pins rules_python 1.8.5 and xla/third_party/py/rules_python_versions.patch carries riscv64-unknown-linux-gnu CPython. XNNPACK is pinned at 53a1797b, byte-identical to the commit mediapipe built, so gotcha 420''s --define=xnn_enable_riscv_fp16_vector=false applies verbatim and gotcha 442''s audit (the Bazel build never defines XNN_ENABLE_RISCV_VECTOR, so the ungated RVV dispatch blocks compile out) carries over without re-auditing. Matrix cp311-cp314: cp310 is out because ml_dtypes ships no riscv64 wheel for it (checked with ci_scripts/check_riscv64_deps.py); cp311 resolves only because protobuf falls back to its py3-none-any wheel. Local validation was static plus an off-target rehearsal: no riscv64 bazel compile was possible (no binfmt_misc for riscv64 and 3.4GB free disk), but the patch applies cleanly to the tag, the TF-commit sed and the version.bzl assertion were run against the real checkout, and the wheel test script was run end to end against the released x86_64 wheel - both the TFLite Interpreter and the LiteRT v2 CompiledModel path return 1/sqrt(768) for simple_l2_norm.tflite. PR #2135 open as a draft. First CI run picked bazel 7.7.0 from upstream''s .bazelversion and the bootstrap job died in //src:bazel_nojdk analysis on "name ''macro'' is not defined"; 7.7.0 is the first 7.x whose MODULE.bazel reaches bazel_features (gotcha 451), so the workflow now bootstraps 7.5.0 like every other bazel port here and the bootstrap job is green. Merged (#2135). Push-triggered build+publish on main run 35573236381 in progress as of 2026-09-21T07:29Z (bazel build, expect ~4h); move to published once it completes and https://pypi.riseproject.dev/simple/ai-edge-litert/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: adbc-driver-postgresql
  version: 1.12.0
  home: https://arrow.apache.org/adbc/
  repo: https://github.com/apache/arrow-adbc
  status: published
  pr: 1654
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. libadbc_driver_postgresql.so built via CMake against the manylinux images own libpq-devel (vcpkg has no riscv64 binary cache); py3-none wheel retagged and auditwheel-repaired the way upstreams own relocate script does. Branch and worktree adbc-driver-postgresql created, build-adbc-driver-postgresql.yml pushed.'
- pkg: py-mini-racer
  version: 0.6.0
  home: https://github.com/sqreen/PyMiniRacer
  repo: https://github.com/sqreen/PyMiniRacer
  status: parked
  pr: null
  notes: 'Superseded/unmaintained: sqreen/PyMiniRacer''s own README (commit d271be9f, 2024-03-20, PR #161 "Deprecate this package") reads "This package is now deprecated. Go to https://github.com/bpcreech/PyMiniRacer for an up-to-date version." Confirmed via `gh api`: bpcreech/PyMiniRacer is a literal GitHub fork of sqreen/PyMiniRacer (fork: true, parent: sqreen/PyMiniRacer), actively maintained (pushed 2026-02-02). py-mini-racer''s last PyPI release stayed at 0.6.0 (2021-04-22, over 5 years stale) -- nothing shipped since, confirmed against the real PyPI JSON. The bpcreech fork publishes to PyPI under the separate name mini-racer, with releases starting right after the deprecation (0.7.0, 2024-03-18) and continuing through 0.14.1 (2026-02-01). This is upstream abandonment, not a riscv64-specific gap -- mirrors the brotlipy/brotlicffi pattern. mini-racer is already being ported to riscv64 in this repo: PR #1875 (branch mini-racer, status ci-running as of 2026-09-13), which required extensive V8/depot_tools/gclient/DEPS patches; duplicating that effort for the deprecated fork is not worth it. No worktree/branch/PR created for py-mini-racer -- diagnosed read-only against the GitHub API (repo fork/parent, commit history, README) and the real PyPI JSON.'
- pkg: pact-python-ffi
  version: 0.5.4.1
  home: https://pact.io
  repo: https://github.com/pact-foundation/pact-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1683
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pydevd
  version: 3.5.0
  home: https://github.com/fabioz/PyDev.Debugger/
  repo: https://github.com/fabioz/PyDev.Debugger/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1666
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: correctionlib
  version: 2.9.0
  home: https://github.com/cms-nanoAOD/correctionlib
  repo: https://github.com/cms-nanoAOD/correctionlib
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1107
  notes: 'scikit-build-core + pybind11 C++ port vendoring pybind11/rapidjson/cpp-peglib/pcg-cpp/xxhash/lwtnn as submodules; sdist built from an upstream checkout then cibuildwheel matrix cp312/cp313/cp314/cp314t manylinux_riscv64 (musllinux dropped: no numpy riscv64 musllinux wheel anywhere). Test group drops awkward/dask-awkward (their awkward-cpp dep has no riscv64 wheel yet, PR #1084); cp314t builds but skips tests (pandas/scipy publish no cp314t wheel on any platform yet). cp312/cp313/cp314 legs failed test_lwtnn.py::test_lwtnn_example (assert sf == 0.95186825355646787, got 0.9518682535564676 - ULP-level float divergence in the Eigen/lwtnn matmul, not a bug: upstream hit the identical failure on manylinux_aarch64 and worked around it too, cms-nanoAOD/correctionlib#348). Fixed by deselecting just that one test in CIBW_TEST_COMMAND (gotcha 304); re-run in progress.'
- pkg: zipfile-deflate64
  version: 0.2.0
  home: https://github.com/brianhelba/zipfile-deflate64
  repo: https://github.com/brianhelba/zipfile-deflate64
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1105
  notes: '5 Linux wheels upstream (abi: cp310,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1105 green (cp312/cp313/cp314/cp314t build+test pass, publish dry-run clean), awaiting review. Merged (#1105). Issue #1138 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: zxing-cpp
  version: 3.1.1
  home: https://github.com/zxing-cpp/zxing-cpp
  repo: https://github.com/zxing-cpp/zxing-cpp.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1653
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. nanobind + scikit-build-core extension; wheel is cp312-abi3 (covers cp312/cp313/cp314) plus a separate cp314t free-threaded build, mirrored as cp312/cp314t on riscv64. PR #1653 open, CI running.'
- pkg: xgboost-cpu
  version: 3.4.1
  home: https://xgboost.readthedocs.io/en/stable/
  repo: https://github.com/dmlc/xgboost
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1873
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. CMake+OpenMP C++ core built as the CPU-only variant via upstream''s own pypi_variants.py rename; patches ParallelFor() off dynamic/guided OpenMP schedules (gotcha 166). PR #1873 open, CI green (33 passed).'
- pkg: sherpa-onnx-core
  version: 1.13.6
  home: https://github.com/k2-fsa/sherpa-onnx
  repo: https://github.com/k2-fsa/sherpa-onnx
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2137
  notes: '3 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. The split-package half of sherpa-onnx that carries the native payload: py3-none-<plat> wheels holding libsherpa-onnx-c-api.so, libsherpa-onnx-cxx-api.so, libonnxruntime.so and the C/C++ API headers, no extension module (gotcha 81/xgboost shape - real per-platform content, honest py3-none tag, sizes 9-34 MB per platform). Not redundant with the already-published non-split sherpa-onnx riscv64 wheel: that one ships no sherpa_onnx/_info.py or __main__.py, so `python3 -m sherpa_onnx --cflags`/`--c-api-libs` - upstream''s documented way for C/C++/Rust/Go/Dart consumers to locate the headers and libs - does not work on riscv64 today. Built from upstream''s own build-wheels-linux.yaml `core` job (cmake install, then scripts/wheel/sherpa-onnx-core/setup.py bdist_wheel --plat-name), driving the manylinux_2_39_riscv64 container directly; one wheel serves every interpreter. Uses our published riscv64 onnxruntime 1.29.0 wheel, re-sonamed from libonnxruntime.so.1 to libonnxruntime.so so _info.py''s -lonnxruntime resolves, since upstream''s prebuilt riscv64 onnxruntime is T-Head XuanTie-only (gotcha 449). Rehearsed off-target under Docker+QEMU riscv64 before pushing; CI then went green first try (build 1h55m): sherpa_onnx_core-1.13.6-py3-none-manylinux_2_39_riscv64.whl, 10.8 MB, 4 .so + 2 headers + _info.py/__main__.py, RPATH $ORIGIN. Tested on cp312/cp313/cp314/cp314t plus upstream''s version-cxx-api (asserts 1.13.6 / onnxruntime 1.29.0) and offline-tts-piper-cxx-api synthesizing real audio through the wheel''s C++ API. Publish job dry-ran clean. PR #2137 open as draft awaiting review. Learnings recorded as gotchas 462 (split -core sibling still worth porting after a non-split main package) and 463 (re-soname a substituted dep library instead of shipping a symlink farm). Merged (#2137). Push-triggered build+publish on main run 35573260283 succeeded. 404 on pypi.riseproject.dev/simple/sherpa-onnx-core/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: tree-sitter-xml
  version: 0.7.0
  home: https://github.com/tree-sitter-grammars/tree-sitter-xml
  repo: https://github.com/tree-sitter-grammars/tree-sitter-xml
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1098
  notes: 'home/repo were tree-sitter/tree-sitter-xml (404, does not exist); actual upstream is tree-sitter-grammars/tree-sitter-xml, same org as tree-sitter-yaml. XML+DTD grammars in one cp39-abi3 extension, cibuildwheel, tested via bindings/python/tests -- mirrors the merged tree-sitter-yaml/tree-sitter-json workflows exactly. PR #1098 open, CI running. Merged (#1098). Issue #1145 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: tree-sitter-dart
  version: 0.1.0
  home: https://github.com/efrenbl/tree-sitter-dart
  repo: https://github.com/efrenbl/tree-sitter-dart
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1103
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.1.0 commits generated src/parser.c, so no generate-then-build workaround needed (unlike tree-sitter-swift). Not a tree-sitter-grammars-org repo, so no bindings/python/tests dir; workflow mirrors upstreams own release.yml smoke test (parse a snippet via tree_sitter.Language/Parser) instead of unittest discover. Worktree/branch tree-sitter-dart. PR #1103, CI green (build + publish dry-run passed). Not yet merged. Merged (#1103). Issue #1140 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: gurobipy
  version: 13.0.3
  home: https://www.gurobi.com
  repo: https://www.gurobi.com
  status: parked
  pr: null
  notes: 'Not feasible: gurobipy is Gurobi Optimization LLC''s proprietary solver engine, so there is
    no source to build from, no riscv64 build from the vendor at any layer, and the EULA the
    wheel ships independently bars us from redistributing one even if there were (new gotcha
    453, feasibility-and-triage.md; same closed-payload family as gotchas 372/376/431/450).
    Verified against the real artifacts, not inferred. PyPI 13.0.3 publishes 24 files, all
    wheels (12 Linux: cp310-cp314 + cp314t, manylinux_2_17_x86_64 and manylinux_2_26_aarch64),
    and the project has never shipped an sdist on any of its 28 releases, so per gotcha 431 the
    wheel is the only evidence; home and repo are both the marketing page https://www.gurobi.com
    and the metadata says License: Proprietary (gotcha 372''s hdbcli tell, exactly). WHAT IS IN
    THE WHEEL: downloading and unzipping gurobipy-13.0.3-cp312-cp312-manylinux2014_x86_64.whl
    (15.0 MB) gives 11 genuine Cython extensions (_core, _model, _matrixapi, _lowlevel, ...)
    plus one payload, gurobipy/.libs/libgurobi130.so at 49.6 MB = 88% of the unpacked wheel; the
    aarch64 twin (87.2 MB) is the same file list with that payload at 168.5 MB. No .pyx/.c/.h
    anywhere in either, so even the binding layer ships no source. readelf -d on
    gurobipy/_core.cpython-312-*.so shows NEEDED libgurobi130.so plus RPATH $ORIGIN/.libs -- a
    hard link, not a dlopen -- and gurobipy/__init__.py imports ._batch/._core/._matrixapi at
    import time, so with no riscv64 engine there is no importable wheel at all (the
    saxonche/gotcha 450 shape, stricter than gotcha 157''s claude-agent-sdk, whose sdist at
    least imports). WHY A VENDOR REBUILD IS NOT A RECOMPILE (the new gotcha): strings -a names
    the 49.6 vs 168.5 MB gap -- the x86_64 engine statically links Intel MKL
    (mkl_avx_d_opt_gemm_ker, ''Intel(R) Math Kernel Library Version'', ''Intel MKL FATAL ERROR:
    This system does not meet the minimum requirements''), the aarch64 engine links Arm
    Performance Libraries (ARMPL_NEOVERSE_N1, ARMPL_KUNPENG_920, ARMPL_APPLE_M1), and
    LICENSE.txt says the same thing without tooling by carrying a ''SIMPLIFIED END USER LICENSE
    AGREEMENT FOR FREE OF CHARGE ARM REDISTRIBUTABLES'' section. Each architecture is its own
    port onto a closed, arch-specific BLAS that its own vendor publishes for that arch only;
    neither MKL nor ArmPL exists for riscv64, and neither engine contains a single riscv/rv64
    string (0 hits in 712k). Both are built on a closed vendor Jenkins
    (/home/jenkins/workspace/gurobi-thirdparty-
    updates-v130/gurobi/thirdparty/lib/{linux64,armlinux64}/openssl-3.5.7/..., GCC 8.5.0 on RHEL
    8), gotcha 431''s builder-path tell. THREE INDEPENDENT VENDOR PLATFORM TABLES, all
    riscv64-negative: the Supported Platforms table for Gurobi 13.0 in docs.gurobi.com lists
    exactly win64, linux64 (x86-64), macos_universal2 and armlinux64 (arm64), no RISC-V
    anywhere; packages.gurobi.com/13.0/gurobi13.0.3_linux64.tar.gz, _armlinux64.tar.gz and the
    macOS pkg all answer 206 to a 2-byte range request while gurobi13.0.3_riscvlinux64.tar.gz,
    _riscv64.tar.gz and _linux_riscv64.tar.gz all 404; and Gurobi''s own conda channel has
    124/88/ 122/124 packages in linux-64/linux-aarch64/osx-64/win-64 against 0 in linux-riscv64,
    which still returns 200 with a synthesised empty index (gotcha 42 -- count the packages, do
    not trust the status). NO COMMUNITY/OSS EDITION: Gurobi''s own open-source-packages article
    lists gurobipy-pandas, gurobi-machinelearning, gurobi-optimods, gurobi-modelanalyzer and
    gurobi-logtools under Apache-2.0 and states that the calls those make into ''the proprietary
    Gurobi library'' remain a support matter; neither gurobipy nor the Optimizer engine is among
    them. THE LICENCE CLOSES A SECOND, INDEPENDENT DOOR: dist-info/licenses/LICENSE.txt is
    Gurobi''s Evaluation EULA, whose 2.1 grants only a ''limited, non-exclusive, non-
    transferrable, non-sublicensable, revocable license ... solely in a non-production
    environment'' and whose 2.2 states ''You will not use, copy, modify, or distribute the
    Product'' -- so publishing any gurobipy wheel on pypi.riseproject.dev would not be permitted
    even if a riscv64 engine existed. And there is no swap-in escape hatch of the gotcha 35
    kind: the wheel ships gurobipy/.libs/gurobi.lic (TYPE=PIP, VERSION=13,
    EXPIRATION=2027-11-29, KEY=...) and the engine carries the whole enforcement path (''Invalid
    PIP license'', ''HostID mismatch (licensed to %x, hostid is %x)'', ''Model too large for
    size-limited license; visit https://gurobi.com/unrestricted''), so any substitute payload
    would have to be one the vendor signed. Revisit only if Gurobi itself ships a riscv64
    Optimizer (a gurobi<ver>_riscvlinux64-style tarball on packages.gurobi.com, or a riscv64
    wheel on PyPI) AND grants redistribution; nothing in a build-gurobipy.yml can close either
    gap. No worktree/branch/PR created -- this was settled before any YAML was written.'
- pkg: xrootd
  version: 6.1.1
  home: https://xrootd.org
  repo: https://xrootd.org
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1811
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CMake+setuptools build-from-checkout (root setup.py drives a CMake configure with -DXRDCL_LIB_ONLY=1 -DPYPI_BUILD=1, building only the XrdCl/XrdUtils client libs + pyxrootd C extension, not the server daemon). Mirrors upstream python.yml manylinux job (dnf install krb5-devel libuuid-devel openssl-devel; pip wheel --use-pep517; auditwheel repair), plus licence collection for the openssl/krb5/libuuid/pcre2/selinux/libxcrypt shared libs auditwheel vendors. PR #1811 open, CI running (cp312/cp313/cp314/cp314t riscv64 wheels).'
- pkg: warp-lang
  version: 1.16.0
  home: https://developer.nvidia.com/warp-python
  repo: https://github.com/NVIDIA/warp
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2138
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. FEASIBLE and ported - not the triton/gotcha 41 case and not the saxonche/gotcha 450 case. Warp''s CPU backend is a first-class upstream configuration, not an escape hatch: build_lib.py has --no-cuda (with --cuda-path/--clang-build-toolchain/--use-dynamic-cuda all rejected alongside it), and it is exactly what upstream ships macOS with. The wheel-content diff proves the split: the macOS wheel''s libwarp.dylib is 0.76MB against warp/bin/warp.so at 398MB uncompressed in the manylinux_2_34_aarch64 wheel (167MB compressed), i.e. essentially all of the Linux payload is statically linked CUDA, while warp-clang.so (54MB, 52MB as the .dylib) is the Clang/LLVM JIT that is present on every platform including the CUDA-free one. Read via HTTP range requests over the zip central directory (gotcha 41), nothing downloaded. Wheels are py3-none-<platform> with real compiled content (gotcha 81''s xgboost shape - ctypes, no PyInit), so one build serves every interpreter. FOUR riscv64 GAPS, all small: (1) machine_architecture() in warp/_src/build_dll.py and in setup.py raise ''Unrecognized machine architecture riscv64'' and abort before anything compiles; (2) setup.py''s `platforms` table feeds WarpBDistWheel.get_tag(), so riscv64 needs an entry (manylinux_2_39_riscv64) or no platform tag can be stamped; (3) build_llvm.py maps arch->LLVM_TARGETS_TO_BUILD with an X86 fallback, so riscv64 would get an LLVM that cannot emit code for its own host - RISCV is the backend name; (4) the real find, and the only one that is a correctness bug rather than a missing table row: warp/native/clang/clang.cpp drives Clang at the cc1 level (CompilerInvocation, not the driver), and on RV64 cc1 defaults to a bare rv64i ISA with an empty -target-abi, which clang''s createRISCVTargetCodeGenInfo turns into ABIFLen 0, i.e. soft-float lp64. warp.so and crt.cpp are gcc-built rv64gc/lp64d, so every JIT-compiled kernel calling _wp_isfinite(double) or the other crt.cpp shims would read the argument from the wrong register class - patched by naming -target-abi lp64d and +m/+a/+f/+d/+c, in the same place and style as the existing x86_64 (+f16c) and aarch64 (+reserve-x28) blocks. THE LLVM SOURCE BUILD IS THE COST, NOT A WALL: the distro route was checked first and is closed - Rocky 10 riscv64 does ship llvm/clang 21.1.8 (matching upstream''s own pinned 21.1.0) and llvm-static has 304 libLLVM*.a, but clang-devel ships ZERO .a files (RHEL/Fedora package only libclang-cpp.so), and warp''s build_warp_clang_for_arch links every .a in the LLVM lib dir, so there is no static clang to link; a shared-libclang-cpp variant would also need -D_GLIBCXX_USE_CXX11_ABI=0 dropped and ~180MB of distro .so vendored into a wheel upstream keeps self-contained. So the workflow uses upstream''s own --build-llvm path (clones llvmorg-21.1.0, MinSizeRel, LLVM_TARGETS_TO_BUILD=RISCV;NVPTX, clang project only, tools off) - the same from-source compile upstream does in build-llvm-sdk.yml for x86_64/aarch64, and the same scale as build-libclang.yml, which takes ~10h on these runners; timeout-minutes 1440, and the test suite is a second job so it gets its own budget. LOCAL VALIDATION (Docker+QEMU, manylinux_2_39_riscv64): all 21 warp/native/*.cpp compiled clean with -Werror for riscv64 - no SIMD intrinsics anywhere in warp/native, flags are a generic -O3 -fPIC --std=c++17 - then the link failed with ''cannot find -lstdc++'' because build_dll.py always passes -static-libstdc++/-static-libgcc and the image ships no libstdc++.a; fixed with libstdc++-static from CRB, exactly as build-libclang.yml does, which is also why this port carries a gpl_sources job (gcc). With that installed the local run went green end to end: warp/bin/warp.so links and `file` reports ELF 64-bit LSB shared object, UCB RISC-V, RVC, double-float ABI - i.e. the gcc-built half really is rv64gc/lp64d, which is the premise patch 0002 rests on - and `python -m build --wheel -C--build-option=-Plinux-riscv64 -C--build-option=-Mmanylinux_2_39` produced warp_lang-1.16.0-py3-none-manylinux_2_39_riscv64.whl with Root-Is-Purelib: false and Tag: py3-none-manylinux_2_39_riscv64, carrying dist-info/licenses/licenses/llvm-LICENSE.txt, so upstream''s own license_files already covers the statically linked Clang/LLVM and no licence patch is needed. A full riscv64 LLVM build could not be rehearsed locally (2.5GB free disk) and the local run therefore used --no-standalone, so the LLVM compile, the warp-clang.so link and the JIT itself are first proven on CI. numpy (the only runtime dep) is on the registry for cp312-cp314t. FIRST FULL CI RUN (run 35537623978) BUILT THE WHEEL: build_wheels succeeded after ~11h (LLVM from source), so patches 0001/0002 are proven - CodeModel::Large on the PIC path was NOT the problem, the JIT emits and runs riscv64 code. test_wheels failed, and its 1552 errors split into exactly two causes, both now fixed on the branch. (1) 649 errors, one bug: rv64gc has no half-precision hardware, so clang lowers builtin.h''s _Float16 conversions to the compiler-rt libcalls __extendhfsf2/__truncsfhf2, and wp_load_obj() resolves a JIT module''s externals from a curated CRT table with no compiler builtins - "JIT session error: Symbols not found: [ __extendhfsf2, __truncsfhf2 ]", so every module holding an fp16 kernel failed to materialize and took its non-fp16 kernels down with it (test_codegen, test_print, test_spatial, test_transform, test_fabricarray, 40 more). x86_64 dodges it with +f16c and aarch64 with armv8-a fcvt; RISC-V has no baseline equivalent (Zfh is optional, not in rv64gc, and forcing it would emit fcvt.h.s on hardware that need not implement it). Patch 0003 converts in software, bit-for-bit identical to (_Float16) - RTNE, sNaNs quieted, payloads kept - verified exhaustively over all 2^32 floats and all 2^16 halves, and proven at the compiler level by cross-compiling builtin.h with clang --target=riscv64 -march=rv64gc: the unpatched header leaves both symbols undefined, the patched one leaves none. (2) 903 errors: unittest_parallel caps the WHOLE parallel run at a hard-coded 3600s, and four riscv64 cores compiling every kernel with clang got through 137 of 180 suites in the hour, the other 43 marked crashed. Patch 0004 reads the cap from WARP_TEST_SUITE_TIMEOUT (default unchanged); the workflow sets 43200. Plus a packaging bug the run exposed: setup.py packages examples/assets and tests/assets, both git-lfs, and actions/checkout does not fetch lfs, so the wheel shipped 14 pointer stubs and example_nvdb died on "NanoVDB signature not found"; the workflow now curls the blobs from media.githubusercontent.com (gotcha 296) - verified locally, every byte count matches the pointer''s size: line. Docker+QEMU against the CI wheel itself also settled the one remaining failure, test_bsr_mm_max_new_nnz (assertRegex(output, "exceeded") on an empty string): wp.printf from a JIT kernel works fine on riscv64, its output is simply still sitting in glibc''s stdout buffer. Second CI run 35588248540 in flight on 8f7fb01b13.'
- pkg: pysimdjson
  version: 7.0.2
  home: https://github.com/TkTech/pysimdjson
  repo: https://github.com/TkTech/pysimdjson
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1213
  notes: 'Cython/C++ binding for simdjson (vendored simdjson 3.12.3, amalgamated). Investigated riscv64 SIMD support carefully per gotcha concern: no riscv64 SIMD kernel exists in vendored simdjson, but on riscv64 none of its x86/arm64/ppc64/loongarch arch macros match, so SIMDJSON_IMPLEMENTATION_FALLBACK auto-selects and only the portable fallback implementation compiles -- confirmed locally by building with every SIMD backend explicitly disabled (fallback-only), which passed the full test suite (22 passed, 4 skipped) on cp312/cp313/cp314/cp314t. Patches setup.cfg to ship the vendored Apache-2.0 simdjson licence alongside pysimdjson''s own MIT LICENSE (upstream''s wheel ships only the latter). PR #1213 open, CI running.'
- pkg: nccl4py
  version: 0.4.1
  home: https://developer.nvidia.com/nccl
  repo: https://github.com/NVIDIA/nccl
  status: parked
  pr: null
  notes: 'Parked after read-only triage; no worktree/branch/PR created. nccl4py is NVIDIA''s
    first-party Python binding for NCCL and its source is public (NVIDIA/nccl, bindings/nccl4py,
    Apache-2.0), so this is not a no-source park like pyqt5-qt5/saxonche -- it is a hard
    NVIDIA-GPU requirement, the same class as the parked sglang, cuda-tile and onnxruntime-gpu
    entries (gotchas 386/387; gotcha 284''s "runtime dlopen or build-time headers?" test comes
    out on the headers side). Three independent walls, each verified rather than assumed:
    (1) BUILD. bindings/nccl4py/setup.py''s BuildExt.build_extensions() calls _cuda_include_dir(),
    which raises PlatformError("CUDA_HOME is not set") and then requires $CUDA_HOME/include to
    exist; the four Cython extensions (nccl.bindings.{nccl,cynccl,_internal.nccl,_internal.utils})
    compile against CUDA toolkit headers. Upstream''s own README says it outright: "Installing a
    published wheel does not require CUDA_HOME or a local CUDA Toolkit. Compiling nccl4py from
    source does." NVIDIA ships no riscv64 toolkit -- redistrib_13.3.0/13.2.0 list exactly
    linux-x86_64, linux-sbsa, windows-x86_64 (12.9.0 adds linux-aarch64/linux-all), zero riscv
    strings -- matching the triton (gotcha 41), cupy-cuda12x and cuda-tile findings.
    (2) INSTALL. The unconditional, non-extra dependencies are cuda-core~=1.0 and
    cuda-pathfinder>=1.5.4; cuda-core 1.2.0 publishes only manylinux_2_28_{x86_64,aarch64} and
    win_amd64 wheels and NO sdist, so `pip install nccl4py` cannot resolve on riscv64 at all
    (gotcha 40). The trap is that the CUDA-named requirements (cuda-bindings, nvidia-nccl-cu12/cu13,
    nvidia-cutlass-dsl) sit behind the [cu12]/[cu13] extras while the actual blocker is in the
    base dependency list.
    (3) RUNTIME. Unzipped nccl4py-0.4.1-cp312-cp312-manylinux_2_28_x86_64.whl: 50 files, 11.4 MB
    uncompressed, and no vendored vendor blob at all (contrast gotchas 41/157) -- it is Cython
    bindings plus pure Python. nccl/bindings/_internal/nccl.*.so dlopens libcuda.so.1 (the NVIDIA
    driver, which has no riscv64 build anywhere), libnccl.so.2 is supplied by nvidia-nccl-cu12/cu13
    (py3-none-manylinux_2_18_{x86_64,aarch64} only), nccl/core/cuda.py does an unconditional
    `from cuda.core import Device, Stream` at import time, and nccl/core/device/cute/_bindings.py
    resolves libnccl_device.bc via cuda.pathfinder. There is no CPU-only mode to fall back to:
    NCCL exists to move data between NVIDIA GPUs over NVLink/NVSwitch/PCIe P2P/IB, and its own
    build is nvcc device code (makefiles/common.mk: NVCC ?= $(CUDA_HOME)/bin/nvcc, NVCC_GENCODE
    -gencode arch=compute_60..120), so "build libnccl from source for riscv64 first" is not an
    escape hatch either.
    Latest release re-checked per gotcha 388: 0.5.0 ships the same 12 Linux wheels for
    x86_64/aarch64 only (cp310-cp314 plus cp314t), so the gap is unchanged. No new gotcha added --
    the shape is fully covered by 386 (GPU-only, blob-free, source-open, still unportable), 387
    (ask NVIDIA''s redist index for our arch) and 284 (build-time CUDA headers vs runtime dlopen).
    Not a candidate to revisit unless/until NVIDIA ships a CUDA toolkit and driver for riscv64.'
- pkg: pynvvideocodec
  version: 2.2.3
  home: https://docs.nvidia.com/video-technologies/pynvvideocodec/index.html
  repo: https://catalog.ngc.nvidia.com/orgs/nvidia/resources/pynvvideocodec
  status: parked
  pr: null
  notes: 'Not feasible: NVIDIA-GPU-hardware-only at every layer (the NVENC/NVDEC video engines plus CUDA), same disposition as the parked onnxruntime-gpu / cupy-cuda12x / cuda-tile / humming-kernels entries; folded as gotcha 452 (feasibility-and-triage.md). Version bumped 2.2.2 -> 2.2.3 per gotcha 388: 2.2.3 is the current release and has the identical shape (19 wheels, cp310-cp314, manylinux_2_28/2_34 x86_64+aarch64 and win_amd64/win_arm64, NO sdist on any version ever, zero requires_dist), so both are blocked the same way. The queue premise was checked and is partly wrong, which is worth recording: pynvvideocodec IS an ordinary PyPI project (pip install pynvvideocodec is the documented path; no NGC API key is involved anywhere), and the NGC resource is a SEPARATE, fully public source-code zip -- api.ngc.nvidia.com/v2/resources/nvidia/pynvvideocodec reports isPublic: true / canGuestDownload: true, .../versions/v2.2.3/files names PyNvVideoCodec_2.2.3.zip and .../versions/v2.2.3/files/PyNvVideoCodec_2.2.3.zip fetched it anonymously (the .../versions/<v>/zip form 404s). So NGC gating is not a blocker here, and every finding below is read off the real source tree and the real cp312 manylinux_2_28_x86_64 wheel rather than inferred. (1) Import is unconditionally gated on the proprietary NVIDIA driver. PyNvVideoCodec/__init__.py (byte-identical between the NGC source and the shipped wheel) calls _get_driver_version() at module top level; that constructs VersionCheck.DriverWrapper, whose C++ (src/NvEncVersionCheck/VersionCheck/NvEncVersionCheck.cpp:64) does dlopen("libnvidia-encode.so.1", RTLD_LAZY) and throws when it is absent. There is no lazy path and no fallback -- the enclosing except clause re-raises -- so import PyNvVideoCodec cannot succeed without an NVIDIA driver (stricter than gotcha 284 and than gotcha 183/playwright, which at least imports). The NVENC version the driver reports then selects which of two prebuilt extensions to load: >= 13*16 -> _130, >= 12*16+1 -> _121, otherwise RuntimeError("Driver version is too old"). Note the trap: VersionCheck.cpython-312-x86_64-linux-gnu.so has NO CUDA linkage of its own (DT_NEEDED is only libstdc++/libm/libgcc_s/libc), so a first readelf points the wrong way -- the gate lives in pure Python plus one dlopen string. A second arch tell sits in the same file: _get_module_path() enumerates exactly two Linux spellings, aarch64 and an else-branch that assumes x86_64, so on riscv64 it would hunt for a ...-x86_64-linux-gnu.so (same shape as gotcha 386 humming get_native_arch()). (2) The real extensions are CUDA-linked. readelf -d PyNvVideoCodec_130.cpython-312-x86_64-linux-gnu.so gives DT_NEEDED libcuda.so.1 plus a vendored libcudart-c64a25f3.so.12.3.101; its undefined symbols are the CUDA Driver API (cuInit, cuCtxCreate_v2, cuMemAlloc_v2, cuMemcpy2DAsync_v2, cuStreamCreate, cuEventRecord, ...) plus cudaLaunchKernel@libcudart.so.12 and cudaMemcpyToSymbol@libcudart.so.12; readelf -S shows .nv_fatbin and .nvFatBinSegment, i.e. compiled device code; and the module dlopens libnvcuvid.so.1 for the whole NVDEC cuvid* API (error string: "Could not dynamically load libnvcuvid.so.1. Please ensure Nvidia Graphics drivers are correctly installed!", plus cuvidCreateDecoder/cuvidDecodePicture/cuvidMapVideoFrame64/... resolution errors). (3) The build hard-requires the CUDA toolkit. setup.py is a bare skbuild.setup() (matching the wheel''s Generator: skbuild 0.19.1), src/CMakeLists.txt does find_package(CUDAToolkit 11.2 REQUIRED), and src/VideoCodecSDKUtils/CMakeLists.txt is project(VideoCodecSDKUtils LANGUAGES CXX CUDA) with a CMAKE_CUDA_ARCHITECTURES list of sm_50..90 and four .cu sources (ColorSpace.cu, Resize.cu, BitDepth.cu, crc.cu) that need nvcc. (4) DEMUX_ONLY was checked as an escape hatch and rejected. It is a genuinely CUDA-free mode: src/CMakeLists.txt option(DEMUX_ONLY ...) skips find_package(CUDAToolkit) and the entire VideoCodecSDKUtils subdirectory, and src/PyNvVideoCodec/CMakeLists.txt then builds only PyNvDemuxer.cpp + NvDemuxer.cpp against ffmpeg. It still yields nothing: setup.py never sets it and the released wheels are the full build (both _121 and _130, with .nv_fatbin), so it ships nowhere upstream and publishing it would be gotcha 41 rejected-offline-build / gotcha 387 stub-build under a name that promises hardware codecs; the same __init__.py ships either way and would still dlopen libnvidia-encode.so.1 and raise, so the artifact would not even import on riscv64; and what survives the #ifdefs is an ffmpeg demuxer with every encode, decode and transcode API compiled out -- gotcha 419 dead stub, reached by a build flag instead of a torch branch. (5) Nothing exists on riscv64 to satisfy any of it. NVIDIA ships no riscv64 CUDA toolkit (redistrib_13.0.0.json, redistrib_13.2.0.json and redistrib_13.4.2.json list only linux-x86_64, linux-sbsa, linux-all and windows-x86_64/arm64) and no riscv64 driver, so libcuda.so.1, libnvidia-encode.so.1 and libnvcuvid.so.1 do not exist for the architecture. This one is additionally harder than the compute-only CUDA parks: it needs a GPU carrying the dedicated NVENC/NVDEC silicon, so even a future riscv64-hosted CUDA toolchain would not by itself make the package functional. (6) Secondary, fixable-but-moot arch wall, named so it is not mistaken for the blocker: the bundled prebuilt ffmpeg is external/ffmpeg/lib/{x86_64,aarch64,x64,arm64} only, and src/cmake/ffmpeg.cmake maps CMAKE_SYSTEM_PROCESSOR to just those four, leaving the library dir unset on anything else so link_av_component raises FATAL_ERROR "Required FFmpeg library avformat not found" -- that alone would be portable, since the wheel even ships the ffmpeg 9.0.1 LGPL source tarball under .data/data/external/ffmpeg/src/. Registry checked: pypi.riseproject.dev/simple/pynvvideocodec/ -> 404. No worktree, branch, workflow, docs/packages yaml, patches or PR created; nothing to rehearse locally since the build cannot start. Revisit only if NVIDIA ships a riscv64 CUDA toolkit AND a riscv64-hosted driver exposing the NVENC/NVDEC engines.'
- pkg: guppy3
  version: 3.1.7
  home: https://github.com/zhuyifei1999/guppy3/
  repo: https://github.com/zhuyifei1999/guppy3/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1491
  notes: 'build-guppy3.yml mirrors upstream''s cibuildwheel build_wheels_native job (build-from-checkout, no external deps). Matrix cp310-cp314 x manylinux/musllinux, matching upstream''s own published PyPI wheels. cp313t/cp314t excluded: rootstate.c''s `.ob_refcnt = 1` designated initializer does not compile against the free-threaded PyObject header (confirmed locally), which is also why upstream ships no cp313t/cp314t wheel. Local sanity builds + upstream''s own hpy().test() self-test passed on cp310-cp314 (native aarch64, not riscv64). Worktree at .claude/worktrees/guppy3, branch guppy3. PR #1491, CI running.'
- pkg: lap
  version: 0.5.13
  home: https://github.com/gatagat/lap
  repo: https://github.com/gatagat/lap
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1489
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Single Cython/C++ extension (lap._lapjv, LAPJV/LAPMOD linear assignment solver); no scipy dependency despite upstream CI installing it (unused by lap.tests). numpy is a build-time and runtime dependency, pulled from our registry via PIP_ONLY_BINARY. Mirrors upstream''s own test_full.yaml Linux cibuildwheel job and test invocation (pytest -v --pyargs lap.tests, which resolves via the installed wheel regardless of cwd). Verified locally (macOS, cp39 due to no cp312 available locally): pip wheel + install, 58/58 tests passed. PR #1489 green (all 4 matrix jobs cp312/cp313/cp314/cp314t built and tested successfully, publish dry-run OK), awaiting review.'
- pkg: vispy
  version: 0.17.0
  home: http://vispy.org
  repo: https://github.com/vispy/vispy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1668
  notes: 'Verified upstream latest release is 0.17.0 (queue previously listed 0.16.2, stale). hatchling + hatch-vcs + hatch-cython build; single Cython extension (vispy.visuals.text._sdf_cpu, the 8SSEDT signed-distance-field algorithm used for glyph rendering), version-specific per-interpreter ABI (not abi3). numpy is a build- and runtime dependency, kiwisolver/pillow are test dependencies; all three plus Cython are hosted on our registry for cp312/cp313/cp314/cp314t. Mirrors upstream''s own wheels.yml Linux cibuildwheel job verbatim, relying entirely on vispy''s own [tool.cibuildwheel] table (skip/before-all/test-command/test-extras already exclude musllinux and run vispy.test(''nobackend''), a headless test mode needing no display). No patches needed. Verified locally (macOS, cp312): pip wheel + install, 498 passed, 64 skipped (all GUI/OpenGL-backend related), 1 xfailed. PR #1668 open, awaiting CI.'
- pkg: asammdf
  version: 8.8.25
  home: https://asammdf.readthedocs.io/en/master
  repo: https://github.com/danielhrisca/asammdf
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1667
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: uharfbuzz
  version: 0.56.0
  home: https://github.com/trufont/uharfbuzz
  repo: https://github.com/trufont/uharfbuzz
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1492
  notes: '6 Linux wheels upstream (abi: cp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: plotext
  version: 6.0.0
  home: https://github.com/piccolomo/plotext
  repo: https://github.com/piccolomo/plotext
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1102
  notes: 'py3-none-<platform> wheels carry a real per-arch ctypes-loaded kernel.so (not pure Python); PR #1102 green, builds it for riscv64. Merged (#1102). Issue #1141 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: mrml
  version: 0.2.4
  home: https://github.com/jdrouet/mrml
  repo: https://github.com/jdrouet/mrml
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1204
  notes: '60 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PR #1204 green: cibuildwheel build-from-checkout on packages/mrml-python, cp312/cp313/cp314/cp314t (gil_used(false) declared), publish dry-run OK. Awaiting review.'
- pkg: json-stream-rs-tokenizer
  version: 0.5.3
  home: https://github.com/smheidrich/py-json-stream-rs-tokenizer
  repo: https://github.com/smheidrich/py-json-stream-rs-tokenizer
  status: published
  pr: 1202
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tree-sitter-html
  version: 0.23.2
  home: https://github.com/tree-sitter/tree-sitter-html
  repo: https://github.com/tree-sitter/tree-sitter-html
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1099
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Tag v0.23.2 commits generated src/parser.c and src/scanner.c, so no generate-then-build workaround needed (unlike tree-sitter-swift). Same shape as tree-sitter-json/-kotlin/-scala/-elixir; cp39-abi3. PR #1099, branch/worktree tree-sitter-html. CI green (build + publish dry-run pass); not yet merged. Merged (#1099). Issue #1144 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: pulsar-client
  version: 3.13.0
  home: https://pulsar.apache.org/
  repo: https://github.com/apache/pulsar-client-python
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2140
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. pybind11 bindings over the Apache Pulsar C++ client; portable C++17, no GPU and no x86-only code path (the crc32c SSE4.2 file already falls back to the software implementation outside x86_64, and the -msse4.2/-mpclmul flags are gated on CMAKE_SYSTEM_PROCESSOR). Upstream''s manylinux job builds libpulsar from the apache-pulsar-client-cpp 4.2.0 release tarball pinned in dependencies.yaml with vcpkg supplying boost/openssl/protobuf/curl/snappy/zstd; vcpkg has no tested riscv64 triplet, so the same tarball is built against the image''s own rpms instead - the path LegacyFindPackages.cmake keeps for distro packagers. One riscv64-image quirk: Rocky''s zlib-ng ships a ZLIB CMake config naming a libz.a it never installs, so find_package(zlib) is disabled in favour of the project''s find_library fallback (gotcha 455). Tests need a live Pulsar cluster (apachepulsar/pulsar has no riscv64 image), so CI runs the suite''s one broker-free class plus client-lifecycle and connect-failure checks. Rehearsed end to end off-target on Rocky 10 x86_64 (same distro family as the image): libpulsar built, wheel repaired by auditwheel with 18 vendored libs and 20 rpm licence texts, 10 tests passed; on the riscv64 image itself the cmake configure and the licence sweep were verified under QEMU. PR #2140 open as draft; CI green - cp312/cp313/cp314 manylinux_2_39_riscv64 wheels built in 25-29 min each, 10 broker-free tests passed per interpreter, 33 vendored-rpm licence texts in each wheel, gpl_sources and the publish dry run both clean. Merged (#2140). Push-triggered build+publish on main run 35573303026 succeeded. 404 on pypi.riseproject.dev/simple/pulsar-client/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: aioesphomeapi
  version: 46.2.1
  home: https://esphome.io/
  repo: https://esphome.io/
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2139
  notes: 'Source is github.com/esphome/aioesphomeapi (the queue home https://esphome.io/ is the ESPHome docs site); tags are v<version>. Small cibuildwheel build-from-checkout port, no patches: setup.py cythonizes nine modules (connection, client_base, _sanitize, and the six _frame_helper ones incl. the pyx pack), plain C, no third-party native deps. Every runtime dependency already has riscv64 wheels for cp312/cp313/cp314/cp314t (cryptography 50.0.1, protobuf 7.36.2, zeroconf 0.151.3, cffi 2.1.1 from our registry; aiohappyeyeballs/async-interrupt/chacha20poly1305-reuseable/noiseprotocol/tzdata/tzlocal pure), confirmed with ci_scripts/riscv64_resolve.py. Deviations from upstream release.yml: manylinux only (cryptography has no musl riscv64 wheel), REQUIRE_CYTHON promoted into CIBW_ENVIRONMENT because upstream sets it as a step env: that never reaches the Linux container (gotcha 129), and PIP_ONLY_BINARY for cffi/cryptography/protobuf/zeroconf so pip cannot fall back to a PyPI sdist. The .so assertion in CIBW_TEST_COMMAND is load-bearing: OptionalBuildExt wraps build_extensions() in contextlib.suppress(Exception), so a failed compile would silently ship a pure-Python wheel under a riscv64 tag (gotcha 295). Test command is upstream ci.yml pytest minus -n auto/--cov/--timeout=4. Local validation: full cibuildwheel cp312-manylinux_riscv64 run under docker+QEMU on quay.io/pypa/manylinux_2_39_riscv64 - wheel built (729 kB, 9 .so, licence text present), auditwheel tagged it manylinux_2_31/2_39, deps resolved from the registry, 1261 passed / 1 skipped; the cp314t rehearsal matched it. CI on PR #2139: all four legs (cp312/cp313/cp314/cp314t) green in 8-13 min each, 1261 passed / 1 skipped per leg, and the publish job dry-ran cleanly (would add the four wheels and register the package). Draft PR awaiting maintainer review/merge; nothing published yet. One side finding, recorded as gotcha 457: on cp314t the registry serves protobuf 7.36.2 as a compiled riscv64 wheel while PyPI serves py3-none-any to free-threaded interpreters, and upb has no Py_mod_gil slot, so importing it re-enables the GIL (RuntimeWarning in every cp314t log, tests still green). That is protobuf''s port to fix, not this one. Merged (#2139). Push-triggered build+publish on main run 35573284813 succeeded. 404 on pypi.riseproject.dev/simple/aioesphomeapi/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: kernels-data
  version: 0.16.1
  home: https://github.com/huggingface/kernels
  repo: https://github.com/huggingface/kernels
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2141
  notes: 'Ordinary Rust/maturin port -- the queue premise reads like a GPU payload and is not one, so the triage is worth recording. kernels-data is NOT the GPU half of HuggingFace''s Kernel Hub: it is the pyo3 binding for the kernels-data crate (kernel.toml/kernel config parsers v1-v4, the variant digest hasher, the Version/KernelName/Backend/Metadata types). requires_dist is empty -- no torch, no CUDA, nothing. The wheel is kernels_data/__init__.py (a 131-byte maturin re-export shim, gotcha 314) + kernels_data/kernels_data.abi3.so (3.7 MB) + a cyclonedx SBOM; zero vendored blobs, zero .nv_fatbin, no dlopen of any driver, so none of gotcha 452/386/387''s walls apply and the nccl4py/pynvvideocodec precedent does not transfer. The odd ''cp314,cp38'' abi split across 20 wheels is gotcha 181, not per-platform native payloads: kernels-data/bindings/python/Cargo.toml declares pyo3 = { version = "0.26", features = ["abi3", "abi3-py38"] } unconditionally, so every GIL-ful interpreter gets one cp38-abi3 wheel while pyo3 disables abi3 under Py_GIL_DISABLED, giving cp314-cp314t its own -- 10 platforms x 2 tags = 20. Wheel sizes do diverge per platform (x86_64 1.28 MB, ppc64le 1.53 MB), i.e. real compiled content, and upstream ships an sdist too. Validated locally before pushing: cargo check --target riscv64gc-unknown-linux-gnu over the whole binding graph (~70 crates, all pure Rust, no C, no SIMD gate) finished clean in 18 s; a host maturin build produced exactly PyPI''s layout and the cp38-abi3 tag with no -i flag; upstream''s own tests pass against that wheel (27 passed) and so does its mypy.stubtest stub check. Licence gap fixed: maturin globs LICEN[CS]E* relative to kernels-data/bindings/python/ and the Apache-2.0 LICENSE is at the repo root, which is why upstream''s own wheels ship no licence file at all (its MANIFEST.in ''include ../../../LICENSE'' is not read by maturin) -- the workflow stages it, same as jsoncompat/semantic-text-splitter/primp/moyopy. Matrix is two entries (cp38-abi3 built on the cp312 floor since 3.8 is absent from the manylinux image, plus cp314t); musllinux dropped, rustup.rs has no riscv64 musl toolchain. A QEMU cibuildwheel rehearsal against quay.io/pypa/manylinux_2_39_riscv64 got rustup+maturin installed in-container and through most of the cargo build before native CI overtook it. Note for later: PyPI''s latest is 0.16.2 (same shape), and the sibling ''kernels'' distribution has become the same kind of maturin package (30 files, cp38-abi3 + cp314t, no riscv64) -- gotcha 150 family, this workflow is its recipe. PR #2141 open as draft, branch/worktree kernels-data. First run hit gotcha 402 and is worth flagging: the two-leg matrix was copied from build-rfc3161-client.yml, one of the 58 workflows whose legs are expressed only through include: over a version-only base, so GitHub merged both includes into the single {version} combination, the last one won, and the run was silently green with ONE cp314t job and no abi3 wheel at all. Fixed by declaring tag: [cp38-abi3, cp314t] as a real matrix dimension (second commit); the bad run was cancelled rather than left to burn a contended runner. Collateral confirmation for the pending fleet sweep: docs/packages/rfc3161-client.yaml shows the damage in situ -- 1.0.8 (built before commit e7b2b20a74 introduced the version vector) carries both cp39-abi3 and cp314t wheels, 1.0.9 (built after) carries only cp314t. Run 2 green first time: both legs built, abi3 wheel compiled once on cp312 and re-tested on cp313/cp314 via find_compatible_wheel (27 passed + stubtest clean on each of cp312/cp313/cp314/cp314t), wheels are kernels_data-0.16.1-{cp38-abi3,cp314-cp314t}-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl at ~1.24 MB, publish job dry-ran clean. Left as a draft for human review. Merged (#2141). Push-triggered build+publish on main run 35573334506 succeeded. 404 on pypi.riseproject.dev/simple/kernels-data/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: minify-html
  version: 0.18.1
  home: https://github.com/wilsonzlin/minify-html
  repo: https://github.com/wilsonzlin/minify-html.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1106
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin, minify-html-python subdir, no abi3 feature so per-interpreter wheels; matrix cp312/cp313/cp314 (no cp314t, upstream ships none). Cargo.toml package renamed minify-html-python -> minify_html to match the #[pymodule] fn name, mirroring upstream python.yml own Patches step. LICENSE copied next to pyproject.toml (maturin license glob gotcha 146; upstream''s own PyPI wheels ship no licence file at all). PR #1106, branch/worktree minify-html. CI running. Merged (#1106). Issue #1168 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: ct3
  version: 3.4.0.post5
  home: https://cheetahtemplate.org/
  repo: https://github.com/CheetahTemplate3/cheetah3
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1208
  notes: 'ct3 is the PyPI name for Cheetah3, the CheetahTemplate3/cheetah3 template engine (ct3.egg-info: "Cheetah3 has already been taken at PyPI"). 21 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Single C extension (Cheetah/c/_namemapper.c), plain setup.py, no pyproject.toml. Mirrors upstream test-publish.yaml narrowed to cibuildwheel; test command runs Cheetah.Tests.Test (with and without --namemapper-pure) plus test_abs_import, matching upstream''s tox.ini. Validated end-to-end with a local cibuildwheel run under riscv64 QEMU (cp312-manylinux): 2186 tests passed. PR #1208, branch/worktree ct3. CI green on all 8 matrix jobs (cp312/cp313/cp314/cp314t x manylinux/musllinux) after a rerun of one cp314t-manylinux job that hit the known transient "Cannot connect to the Docker daemon" runner flake; publish dry-run succeeded. Open for review.'
- pkg: fasttext-predict
  version: 0.9.2.4
  home: https://github.com/dalf/fasttext-predict
  repo: https://github.com/dalf/fasttext-predict
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1679
  notes: 'PR #1679. PyPI metadata URL (searxng/fasttext-predict) is stale; the repo moved to dalf/fasttext-predict, no org page left at searxng. No git tags upstream; pins commit 93501d1 ("Prepapre version 0.9.2.4"), verified byte-identical source against the released sdist. Self-contained pybind11/C++ extension (fastText predict-only fork), proper pyproject.toml build-system, builds clean with no patches needed off-riscv64. Matrix cp312/cp313/cp314/cp314t. Test smoke-checks the extension actually compiled and runs the README language-ID example against lid.176.ftz (upstream ships no test suite). CI green on all 4 matrix jobs; publish dry-run succeeded. Open for review.'
- pkg: vhacdx
  version: 0.0.10
  home: https://github.com/trimesh/vhacdx
  repo: https://github.com/trimesh/vhacdx
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1209
  notes: CI green on manylinux+musllinux riscv64 for cp312/cp313/cp314/cp314t; PR open for review.
- pkg: pycapnp
  version: 2.2.4
  home: https://github.com/capnproto/pycapnp
  repo: https://github.com/capnproto/pycapnp
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1493
  notes: Cython binding statically linking a from-source Cap'n Proto C++ build; PR open, CI running on riscv64 manylinux+musllinux for cp312/cp313/cp314/cp314t.
- pkg: artifacts-keyring
  version: 1.0.0
  home: https://github.com/Microsoft/artifacts-keyring
  repo: https://github.com/Microsoft/artifacts-keyring
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 464, plus gotcha 27''s watchdog clause and gotcha 183). Nothing is compiled on any platform: setup.py declares no Extension and forces the platform tag with a Distribution subclass whose has_ext_modules() hardcodes True plus bdist_wheel root_is_pure=False, so the 16 cp39-cp313/pp39-pp311 Linux wheels are fabricated tags (the aarch64 wheels are byte-size-identical across every ABI, and the only .so files in them are the vendored .NET runtime''s libcoreclr.so/libclrjit.so). The payload is Microsoft''s .NET credential provider downloaded at build time from Microsoft/artifacts-credprovider v1.4.1; ARTIFACTS_CREDENTIAL_PROVIDER_RID selects a self-contained build per RID (upstream ships win-x64/osx-x64/osx-arm64/linux-x64/linux-arm64 only, cibuildwheel archs = x86_64+aarch64), and for an unknown arch get_runtime_identifier() returns "" so the build silently falls back to the framework-dependent, arch-neutral provider. Two independent stops: (1) upstream ships no py3-none-any wheel, so riscv64 pip install already falls back to the sdist, which needs no compiler and finishes in seconds - verified locally with ARTIFACTS_CREDENTIAL_PROVIDER_NON_SC=true pip wheel <sdist>, which built a 5.8MB cp311-cp311-linux_x86_64 wheel with zero .so and 49 entries, exactly the bytes a riscv64 wheel could carry, so publishing one would be a packaging convenience, not a port; (2) that payload has runtimes/ present, so plugin.py requires dotnet on PATH (it runs `dotnet --list-runtimes` and raises "Unable to find dependency dotnet", confirmed locally) and Microsoft ships no linux-riscv64 .NET runtime - only community builds (dkurt/dotnet_riscv, filipnavara; RISE''s own LR_04_001 is porting it) - so the package''s entire purpose is unreachable, not merely degraded. runtimeconfig.json has rollForward: Major, so a future riscv64 .NET >= 8 would run the net8.0 provider unchanged: revisit then, since no patch or wheel on our side is what is missing. No port branch, workflow or PR created.'
- pkg: tree-sitter-markdown
  version: 0.5.1
  home: https://github.com/tree-sitter-grammars/tree-sitter-markdown
  repo: https://github.com/tree-sitter-grammars/tree-sitter-markdown
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1104
  notes: 'Nonstandard upstream layout: markdown (block) + markdown_inline grammars compiled into one tree_sitter_markdown._binding extension, cp39-abi3. Same shape as merged tree-sitter-typescript/php/java/c-sharp/ruby. Targets v0.5.1: latest git tags v0.5.2/v0.5.3 fail upstream''s own pypi build job (in-progress split_parser migration breaks their setup.py), and PyPI itself stops at 0.5.1. PR #1104 green (build + publish dry-run pass); not yet merged. Merged (#1104). Issue #1139 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: scikit-network
  version: 0.33.5
  home: https://scikit-network.readthedocs.io/
  repo: https://github.com/sknetwork-team/scikit-network
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1673
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysam
  version: 0.24.1
  home: https://github.com/pysam-developers/pysam
  repo: https://github.com/pysam-developers/pysam
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1669
  notes: '29 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cvxopt
  version: 1.3.3
  home: https://cvxopt.org
  repo: https://github.com/cvxopt/cvxopt
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2110
  notes: 'PR #2110 open (build-cvxopt.yml + docs/packages/cvxopt.yaml, no patches). 1.3.3 confirmed latest on PyPI; nothing on pypi.riseproject.dev (404). Zero install_requires and pytest is the only test dep, so no registry/dependency wall. Builds the seven mandatory extensions (base, blas, lapack, umfpack, cholmod, amd, misc_solvers) plus the optional fftw one, on cp312/cp313/cp314/cp314t, modelled on build-scs.yml. BLAS/LAPACK come from Rocky 10 openblas-devel (crb; its libopenblas.so.0 exports the LAPACK entry points, so -lopenblas covers both) and fftw-devel (appstream). Rocky 10 riscv64 packages NO suitesparse/gsl/glpk and there is no riscv64 EPEL, so umfpack/cholmod/amd are compiled from the SuiteSparse 7.11.0 tarball upstream''s own linux_build.yml pins (same sha256), via cvxopt''s CVXOPT_SUITESPARSE_SRC_DIR route which upstream tests in CI and uses on Windows; the gsl/glpk/dsdp extensions are left off (all three skip cleanly in upstream''s suite and no example uses them). CVXOPT_SUITESPARSE_SRC_DIR must be RELATIVE to the checkout -- distutils rejects absolute Extension(sources=) paths -- so the tarball is extracted into {project}; the untracked tree does not poison setuptools_scm (wheel came out plain 1.3.3). Licensing needs no patch: setuptools'' default LICEN[CS]E* glob still fires despite the legacy license={file=...} table, so LICENSE.openblas/.suitesparse/.fftw staged at the checkout root land in dist-info/licenses (verified in a built wheel); fftw''s COPYING lives under /usr/share/licenses/fftw-libs-double/, not /usr/share/licenses/fftw/. auditwheel vendors libopenblas, libfftw3, libgfortran and libquadmath, so a gpl_sources job collects the gcc and fftw srpms plus the pinned SuiteSparse tarball (UMFPACK and CHOLMOD/Supernodal are GPL-2.0-or-later). Local validation: full cibuildwheel runs on x86_64/manylinux_2_34 for cp312 AND cp314t both green (8 extensions, 4 licence files, 28 passed / 12 skipped each; cp314t emits the expected GIL-re-enable RuntimeWarning, harmless since upstream sets no -W error). On the real manylinux_2_39_riscv64 image under qemu the dnf installs, licence paths, SuiteSparse staging, metadata and the fftw/base/blas/lapack extensions all validated (linked with -L/usr/lib64 -lfftw3 -lopenblas) before the shared 4-core host made the remaining ~250 SuiteSparse translation units too slow to finish locally -- the umfpack/cholmod/amd compile is the one piece only CI will confirm. musllinux left out although cvxopt is exactly gotcha 221''s zero-dependency shape: Alpine 3.22 riscv64 actually has openblas-dev, fftw-dev, suitesparse-dev, gsl-dev AND glpk-dev, but it ships no /usr/share/licenses at all so there is no licence text to bundle, and enabling it would give the musl wheel more extensions than the glibc one -- worth revisiting as a follow-up. Gotchas 400 (relative Extension(sources=) paths) and 401 (Rocky 10 riscv64 numeric-library availability, incl. the Alpine contrast) added on main separately. CI: all three pr-checks jobs and setup are green (so the workflow registered), but the four build_wheels legs and gpl_sources were still queued ~50 min after the push -- the self-hosted riscv64 pool is saturated, so no wheel has been built in CI yet and this needs watching/triage in a later session.'
- pkg: typos
  version: 1.49.1
  home: https://github.com/crate-ci/typos#typos
  repo: https://github.com/crate-ci/typos
  status: published
  pr: 1101
  notes: 'Maturin `bindings = "bin"` project (module-name = "typos", manifest-path = crates/typos-cli/Cargo.toml): wheel bundles the compiled typos-cli Rust binary, py3-none-<platform> tag, no importable module. Mirrors upstream''s maturin.yml linux job with PyO3/maturin-action targeting riscv64gc-unknown-linux-gnu, manylinux 2_39. Pure-Rust dependency graph (clap/ahash/ignore/toml, no ring/openssl-sys/aws-lc-sys); cargo metadata --filter-platform riscv64gc-unknown-linux-gnu resolved all 174 crates cleanly. Verified exit codes/output shape locally against the released macOS wheel (recieve/receive fixture, exit 2 then 0 after --write-changes). Worktree at .claude/worktrees/typos, branch typos. PR https://github.com/riseproject-dev/python-wheels/pull/1101, CI running. Merged (#1101). Issue #1142 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: oai-statsig-python-core
  version: 0.29.0
  home: https://statsig.com/
  repo: https://github.com/statsig-io/statsig-server-core/tree/main/statsig-pyo3
  status: publishing
  pr: 2142
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Genuinely a separate distribution, not an alias of the statsig-python-core entry: same statsig-pyo3 crate but renamed (statsig-rust -> oai-statsig-rust), own version line (0.29.0 uploaded 2026-08-04, ten days before statsig-python-core 0.22.0), authors "Statsig, Daniel Loomb", and an extra [profile.python-release] the public tree does not have. Source is sdist-only: statsig-io/statsig-server-core stops at 0.23.0 across all 164 tags, all 68 releases/* branches and main''s workspace version, so 0.24.0+ are cut from a non-public tree -- gotcha 156 shape (fetch the released sdist, extract it, point package-dir at the extracted dir so CIBW_TEST_SOURCES still resolves against the workspace cwd). Content is genuinely divergent, not a version-string rename: statsig_base_py.rs 770 -> 984 lines, interned_store_py.rs 71 -> 217 vs public 0.23.0, so pinning a public commit was not an option. Workflow mirrors build-statsig-python-core.yml (same crate): one cp310-abi3 wheel, protoc from Rocky 10 CRB, rustup in-container. Two deviations: CARGO_PROFILE_RELEASE_DEBUG=1 + CARGO_PROFILE_RELEASE_STRIP=none reproduce the python-release profile rather than naming it in MATURIN_PEP517_ARGS (gotcha 141 -- the name exists only in this Cargo.toml and would break unrelated maturin sdists in the same container); inferred from the artifact, since upstream''s published .so is 39.6 MB with debug_info and not stripped while [profile.release] sets strip = true. The sdist ships no licence file at all (gotcha 146), so the build stages the project''s ISC text from a run: heredoc and the test command asserts dist-info/licenses/LICENSE. The CI.yml bundled in the sdist is maturin boilerplate (--release, builds a cp313t wheel that does not exist on PyPI), not the real pipeline. Patches 0001/0002 carry forward the 0.22.0/0.23.0 ones: specs_sync_interval_ms=1 is now rejected (< MIN_SYNC_INTERVAL 1000) so six test_data_store.py polling tests fail on upstream''s own published x86_64 wheel, and fork_runner.py''s 50 SDK inits need more than 10s on riscv64. Validated locally on x86_64: cargo metadata --filter-platform riscv64gc-unknown-linux-gnu resolves all 335 crates with the lock a pure prune (no version drift) and the same native pins the 0.23.0 riscv64 build already compiled (ring 0.17.14, zstd-sys 2.0.13, prost 0.13.4, tonic 0.12.3); protoc 3.19.6 + /usr/include/google/protobuf confirmed inside quay.io/pypa/manylinux_2_39_riscv64 under QEMU, and the .proto is byte-identical to 0.23.0''s. Full wheel built from the patched sdist in manylinux (needed protoc 3.19.6; the x86_64 image''s 3.14 predates stable proto3 optional): cp310-abi3 tag, dist-info/licenses/LICENSE present, .so unstripped with debug_info, and 229 tests pass against that wheel in a replica of the CIBW_TEST_SOURCES staging. Worktree at .claude/worktrees/oai-statsig-python-core, branch oai-statsig-python-core. PR https://github.com/riseproject-dev/python-wheels/pull/2142 (draft), lint checks green, riscv64 build running. CI green on the second run (35548712809): 229 passed, no reruns, publish dry-run clean, wheel oai_statsig_python_core-0.29.0-cp310-abi3-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl. The first run failed 2/229 in test_exposure_logging.py: _find_exposure_callsite reads frame.f_code.co_qualname, which CPython only has from 3.11, while the package declares requires-python >= 3.10 and this abi3 build is compiled AND tested under cp310 -- the error boundary swallows the AttributeError, so callsite metadata silently falls back to "unknown". Not riscv64 and not the Rust crate: pure Python in py_src/, and it reproduces on x86_64 under 3.10 against upstream''s own published cp310-abi3-manylinux_2_17_x86_64 wheel (passes under 3.11). The earlier 229-passed rehearsal ran cp312, which has the attribute. Patch 0003 falls back to co_name (new gotcha 469: an abi3 build tests on its floor interpreter -- rehearse there). Merged (#2142). Push-triggered build+publish on main run 35573357950 succeeded. 404 on pypi.riseproject.dev/simple/oai-statsig-python-core/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: resiliparse
  version: 1.0.9
  home: https://resiliparse.chatnoir.eu
  repo: https://github.com/chatnoir-eu/chatnoir-resiliparse
  status: published
  pr: 1672
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libtpu
  version: 0.0.46
  home: https://cloud.google.com/tpu
  repo: ''
  status: parked
  pr: null
  notes: 'Not feasible, three independent times over: libtpu is Google''s closed-source Cloud TPU
    runtime, it has never been published for any non-x86_64 host, and its licence bars
    redistribution. Folded as new gotcha 465 (feasibility-and-triage.md); same vendored-payload
    family as gotchas 35/157/376/431/450/453 and the same hardware-wall disposition as the parked
    nccl4py / pynvvideocodec / cupy-cuda12x / humming-kernels entries. The queue note''s "locate
    upstream during research" resolved to: there is no upstream source repo at all -- PyPI records
    no project URL because the project has none; the vendor''s own distribution points are
    pypi.org/project/libtpu and the GCS buckets libtpu-releases / libtpu-lts-releases /
    libtpu-nightly-releases (the index Google links from JAX''s install docs,
    storage.googleapis.com/jax-releases/libtpu_releases.html). Findings, all read off the real
    cp312 manylinux_2_31_x86_64 wheel (216 MB compressed, 709 MB unpacked, pulled member-wise over
    HTTP range requests per gotcha 41/157) rather than inferred. (1) No source, ever: all 220
    files across all 60 releases (0.0.4 .. 0.0.48) are bdist_wheel, zero sdists; the wheel is 12
    entries of which libtpu/libtpu.so is 685,727,648 bytes and libtpu/sdk.so 22,998,888, beside a
    1,131-byte __init__.py that only sets TPU_LIBRARY_PATH to the blob''s path, a 298-byte LICENSE
    and 755 KB + 83 KB of THIRD_PARTY_NOTICES for the bundled OSS. Generator: setuptools (84.0.0),
    Root-Is-Purelib: false. (2) x86_64-only at every layer and in every index: readelf -h on both
    payloads says Machine: Advanced Micro Devices X86-64, DT_NEEDED includes ld-linux-x86-64.so.2;
    PyPI has only manylinux_2_27_x86_64 / manylinux_2_31_x86_64 / linux_x86_64 tags in project
    history; Google''s own bucket index storage.googleapis.com/libtpu-releases/index.html lists
    1283 wheel links with zero riscv/aarch64/arm64 matches; and JAX''s installation matrix marks
    Cloud TPU as Linux x86_64 with n/a for every other platform. sdk.so even carries the internal
    builder path bazel-out/k8-fastbuild/bin/sdk/client/python/sdk.so -- Bazel''s k8 is its x86_64
    CPU name, i.e. the release is configured for one host arch inside a tree only Google has. (3)
    The riscv false positive that makes this worth a gotcha: strings -a libtpu.so | grep -ci riscv
    returns 1190 (1188 unique) and the blob contains a whole LLVM RISC-V code generator
    (_GLOBAL__sub_I_RISCVTargetMachine / RISCVISelLowering / RISCVInsertVSETVLI /
    RISCVZilsdOptimizer, the riscv-br-merging-base-cost cl::opt text, the full R_RISCV_* reloc
    table). It is not host support: the registered targets are exactly
    LLVMInitialize{AArch64,ARM,PowerPC,TPU,X86}Target* with no LLVMInitializeRISCVTarget, and the
    RISC-V material sits beside device-side fingerprints --
    platforms/asic_sw/lib/common/riscv/results.proto, .asic_sw.riscv.ExceptionInfo,
    .asic_sw.riscv.StackFrameWithException -- i.e. firmware/exception plumbing on the TPU ASIC
    itself, compiled by a custom in-tree LLVM ''TPU'' backend (llvm::TPUInstrInfo::isMxuInstr,
    llvm::TPUAAResult). Gotcha 41 with the ISA inverted so it reads as good news. (4) Hardware
    wall: libtpu.so exports the PJRT/TPU C API (GetPjrtApi, TfTpu_Initialize, TpuTopology_*,
    TpuEmbeddingEngine_*, GetLibtpuSdkApi) and reaches the silicon through Google''s own kernel
    driver -- /dev/accel0, /dev/vfio, /dev/vfio/vfio, VFIO PF/VF enumeration, IOMMU checks -- plus
    chip codenames (jellyfish, pufferfish, viperfish, TPU v2..TPUv7). There is no CPU or simulator
    mode anywhere in the blob (no tpu_* flag or string offers one), so unlike gotcha
    183/playwright even a hypothetical build would import (the __init__.py only sets an env var)
    and then have no device: TPUs exist only in Google Cloud TPU VMs, whose hosts are x86_64.
    sdk.so is a real extension module (PyInit_sdk plus 159 undefined Py* symbols), so the
    cpXY-cpXY tag is honest here -- not gotcha 464''s fabricated tag -- and cp314t/cp315t
    free-threaded wheels ship too. (5) Redistribution is independently barred, gurobipy/gotcha
    453''s point in its cheapest form: the 298-byte LICENSE makes the payload ''"Software" under
    the agreement governing your use of Google Cloud Platform'' (cloud.google.com/terms), whose
    Sec. 3.3 forbids copying, derivative works and distribution -- no open-source grant of any
    kind, so even a vendor riscv64 build could not be republished to pypi.riseproject.dev.
    Version: triaged at the claimed 0.0.46; per gotcha 388 the newest releases were checked and
    0.0.47 and 0.0.48 (current) have the identical shape -- 7 x86_64 wheels each, cp311-cp315
    incl. cp314t/cp315t, no sdist -- so this park covers them. Registry checked:
    pypi.riseproject.dev/simple/libtpu/ -> 404. No worktree, branch, workflow, docs/packages yaml,
    patches or PR created; nothing to rehearse locally since there is no source to build. Revisit
    only if Google both publishes libtpu for a riscv64 host and grants redistribution rights, i.e.
    effectively never.'
- pkg: piper-tts
  version: 1.7.0
  home: http://github.com/OHF-voice/piper1-gpl
  repo: http://github.com/OHF-voice/piper1-gpl
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2143
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.
    piper1-gpl, the GPL-3.0-or-later successor to the original piper under the Open Home
    Foundation. One small limited-API C extension (src/piper/espeakbridge.c, 104 lines,
    Py_LIMITED_API 0x03090000) that CMake links against a static eSpeak NG built from
    source by ExternalProject at the commit upstream pins (724808c5), plus the
    espeak-ng-data eSpeak NG generates; the rest of the 34 MB wheel is pure Python and
    ONNX model data (nakdimon 21 MB, libtashkeel 4.8 MB). onnxruntime is a plain
    install_requires, not vendored - no from-source onnxruntime build needed, unlike
    sherpa-onnx-core (#2137): our published riscv64 onnxruntime resolves straight from
    the registry (1.30.0 picked for cp312/cp313/cp314). No x86 SIMD anywhere - espeak-ng
    has no intrinsics and the bridge is plain C; espeakng-loader''s -fPIC-for-sonic fix
    does not apply either since piper configures USE_LIBSONIC=OFF.
    The PyPI sdist is NOT buildable: it ships neither CMakeLists.txt, espeakbridge.c nor
    espeak-ng-data, so the build is from the upstream v1.7.0 checkout (build-from-checkout
    shape) and that checkout, with eSpeak NG''s, is also what the gpl_sources job archives
    as Corresponding Source.
    Built on cp39, upstream''s own CIBW_BUILD, which is also the abi3 floor the wheel
    claims (setup.cfg py_limited_api = cp39); cp312/cp313/cp314 are listed only so the
    tests can run, since riscv64 onnxruntime starts at cp312, and cibuildwheel reuses the
    one cp39-abi3 wheel for them (CIBW_TEST_SKIP drops the cp39 test leg). No cp314t:
    py_limited_api rules out a free-threaded build and upstream publishes none.
    One patch widens setup.py''s explicit license_files to licenses/* so the four staged
    eSpeak NG licence files reach dist-info/licenses/ - every piper_tts wheel on PyPI is
    missing them today, on every architecture (Upstream-Status: To upstream).
    Rehearsed twice before pushing: cibuildwheel verbatim on manylinux_2_28_x86_64
    (wheel byte-size 34125914 vs upstream''s released 34125913, identical file set), then
    the full riscv64 job under Docker+QEMU - 21 minutes, one
    piper_tts-1.7.0-cp39-abi3-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl (34.2 MB),
    abi3audit --strict clean (baseline 3.9, computed 3.9, no non-abi3 symbols), and
    27 passed / 5 skipped on each of cp312, cp313 and cp314 - the same counts as x86.
    CI then went green first try (build 11m43s on the riscv64 runner): piper_tts-1.7.0-cp39-abi3-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl (sha256 4af87af6...), gpl-sources.tar 88 MB, publish job dry-ran clean with the gpl-sources entry wired in. Learning recorded as gotcha 466 (a large sdist can omit CMakeLists/the C sources entirely and still `pip wheel` to exit 0, yielding a py3-none-any wheel that imports fine and dies on first use). PR #2143 open as draft awaiting review. Merged (#2143). Push-triggered build+publish on main run 35573378068 succeeded. 404 on pypi.riseproject.dev/simple/piper-tts/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: tree-sitter-css
  version: 0.25.0
  home: https://github.com/tree-sitter/tree-sitter-css
  repo: https://github.com/tree-sitter/tree-sitter-css
  status: published
  pr: 1100
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. Standard tree-sitter grammar family shape (gotcha 114): setup.py compiles src/parser.c + src/scanner.c + bindings/python/tree_sitter_css/binding.c, cp310-abi3 tag via bdist_wheel get_tag() override, MIT license. Modeled on build-tree-sitter-yaml.yml. Worktree at .claude/worktrees/tree-sitter-css, branch tree-sitter-css. PR https://github.com/riseproject-dev/python-wheels/pull/1100. Merged (#1100). Issue #1143 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: resvg-py
  version: 0.5.0
  home: https://resvg-py.readthedocs.io/
  repo: https://github.com/baseplate-admin/resvg-py
  status: published
  pr: 1671
  notes: '40 Linux wheels upstream (abi: cp310,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymongocrypt
  version: 1.19.0
  home: https://github.com/mongodb/libmongocrypt/tree/master/bindings/python
  repo: https://github.com/mongodb/libmongocrypt/tree/master/bindings/python
  status: published
  pr: 1693
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: qiskit
  version: 2.5.2
  home: https://www.ibm.com/quantum/qiskit
  repo: https://github.com/Qiskit/qiskit
  status: publishing
  pr: 2144
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. setuptools-rust + PyO3 0.29 with abi3-py310, NOT maturin: build-backend is setuptools.build_meta and setup.py wires crates/pyext through RustExtension with features cache_pygates (+ mimalloc when QISKIT_BUILD_WITH_MIMALLOC=1, which upstream sets in [tool.cibuildwheel]). PR #2144, CI running (run 35549492620). Triage came out clean on every axis, all of it checked locally: `cargo metadata --locked --filter-platform riscv64gc-unknown-linux-gnu --features mimalloc,cache_pygates` resolves 302 packages with no `*-sys` crate other than libmimalloc-sys; a cross `cargo check --target riscv64gc-unknown-linux-gnu --features pyo3/extension-module,cache_pygates` type-checked all 265 crates for riscv64 in 1m51s with zero warnings (faer 0.24 / pulp 0.22 reach riscv64 through their scalar arms - pulp''s `compile_error!("unsupported arch")` lives in a macro body the riscv64 build never expands, so gotcha 78''s two-arm-cfg signature does not apply here); and mimalloc v2.3.2, vendored by libmimalloc-sys 0.1.49, compiles clean for riscv64 with the manylinux_2_39_riscv64 image''s GCC 14.3.1 under QEMU - it carries a generic `sleep(0)` mi_atomic_yield fallback for arches with no pause instruction and its build.rs only special-cases arm32. symengine is NOT a blocker: it moved to the `qpy-compat` extra, and core Requires-Dist is only numpy<3/>=2.0, scipy>=1.14, rustworkx>=0.15, dill, stevedore and typing-extensions, every one of which resolves on riscv64 for cp310 through cp314t (registry: numpy 2.2.2 + scipy 1.15.2 at cp310, numpy 2.4.3 at cp311, numpy 2.5.3 + scipy 1.18.1 at cp312+, rustworkx 0.18.1 cp310-abi3). The matrix is a single cp310-abi3 leg, not the repo default four, for two independent reasons: setup.py sets `options={"bdist_wheel": {"py_limited_api": "cp310"}}` unconditionally (gotcha 34), so cp310 is both the wheel''s tag and the only interpreter it may be compiled on (gotcha 96); and upstream''s `[tool.cibuildwheel] build = ["cp3??-*"]` cannot match the six-character cp314t, so upstream ships no free-threaded wheel at all and the unguarded py_limited_api would mis-tag one if we forced a cp314t leg (new gotcha 468 - `cibuildwheel --platform linux --archs riscv64 --print-build-identifiers` against upstream''s unmodified config prints cp310..cp315-manylinux_riscv64 and no `t` tag). Upstream''s extra cp311-cp315 entries are test-only re-runs of the whole stestr suite and were dropped for riscv64 runner cost; upstream itself sets `test-skip = ["*ppc64le", "*s390x"]`, i.e. it does not run the suite on its own slower platforms. Local rehearsal (gotcha 101 adapted - manylinux_2_39 exists only for aarch64 and riscv64 and this host is x86_64, so manylinux_2_34_x86_64 natively): the whole recipe ran for real, including upstream''s before-all (dnf wget + tools/install_rust.sh), the replaced CIBW_ENVIRONMENT_LINUX, the pipx-free repair command, auditwheel, and cibuildwheel''s own default `abi3audit --strict`, which reported is_abi3 true, baseline 3.10, computed 3.10, non_abi3_symbols []. Rust build 5m47s; upstream''s full stestr suite 62952 tests in 260s -> 62759 passed, 193 skipped, 0 failed, 0 unexpected success; whole cibuildwheel run 13 minutes. Wheel is qiskit-2.5.2-cp310-abi3-manylinux_2_34_x86_64.whl with one qiskit/_accelerate.abi3.so (22 MB) plus dist-info/licenses/LICENSE.txt, the same shape as upstream''s published wheels. No patches. Licensing needs nothing added: upstream declares license-files = ["LICENSE.txt"], the wheel carries it, and the checkout has no THIRDPARTY/NOTICE inventory to forward (gotcha 153). rust-toolchain.toml pins channel 1.87 in the git checkout, but upstream''s own RUSTUP_TOOLCHAIN=stable overrides it, so gotcha 238 is already handled upstream. timeout-minutes: 1440 per gotcha 365, generous for the real cost. CI GREEN FIRST TRY on PR #2144 (run 35549492620): build_wheels 1h51m on rise-riscv-runner-90bl0h0wx - Rust compile ~1h07m, then upstream''s unmodified stestr suite 62964 tests in 2132s -> 62771 passed, 193 skipped, 0 failed, 0 unexpected success (the x86 rehearsal''s 62952/62759/193/0 reproduced almost test-for-test). abi3audit --strict clean on qiskit-2.5.2-cp310-abi3-manylinux_2_39_riscv64.whl (one qiskit/_accelerate.abi3.so, 18 MB). Publish job dry-ran cleanly off the PR branch (would create qiskit-v2.5.2, add qiskit to packages.txt, open the docs PR). All four pr-checks green. Awaiting maintainer review/merge; not yet published. Merged (#2144). Push-triggered build+publish on main run 35573401257 succeeded. 404 on pypi.riseproject.dev/simple/qiskit/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: pyhmmer
  version: 0.12.3
  home: https://pyhmmer.readthedocs.io/en/stable/
  repo: https://github.com/althonos/pyhmmer
  status: parked
  pr: https://github.com/riseproject-dev/python-wheels/pull/1497
  notes: 'p7_MSVFilter produces incorrect scores on riscv64/GCC 14.3.1 specifically (not arm64, not other SSE-emulated filters in the same file) - root cause not isolated despite ruling out optimization level, SSVFilter fast-path interaction, and every individual SIMD primitive in isolation; likely a subtle full-loop state-accumulation effect under SIMDe''s SSE emulation. Needs a real riscv64 GCC debugger session or upstream HMMER/SIMDe maintainer input to resolve further. See PR #1497 for the full investigation.'
- pkg: httpstan
  version: 4.17.0
  home: https://mc-stan.org
  repo: https://github.com/stan-dev/httpstan
  status: blocked-on-dependency
  pr: null
  notes: '3 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Blocked-on-dependency: the same stan-dev/stanc3 wall as prophet (gotcha 341), in its hardest form (gotcha 467). httpstan bundles stanc, the Stan-to-C++ transpiler, as a prebuilt OCaml binary and runs it on every request. Its Makefile pins STANC_VERSION := 2.39.0 and fetches it with one unconditional rule (curl .../stanc3/releases/download/v2.39.0/linux-stanc), and every published wheel''s httpstan/stanc is an x86_64 ELF (readelf -h: Machine: Advanced Micro Devices X86-64, e_machine 62). Probed by URL: linux-riscv64-stanc is 404 on v2.39.0, v2.40.0, v2.41.0 and nightly, while linux-stanc, linux-arm64-stanc, linux-armhf-stanc, linux-ppc64el-stanc, linux-s390x-stanc and mac-stanc all answer 206 - no riscv64 stanc has ever been released. stanc3''s own Jenkinsfile shows the gap is a missing matrix entry, not a missing mechanism: an axis of values ''arm64'', ''ppc64el'', ''s390x'', ''armhf'', ''armel'' fed to qemuArchFlag() -> --platform=linux/<arch> over scripts/docker/static-builder/Dockerfile, riscv64 absent. Unlike prophet - one fixed prophet.stan, so a pre-transpiled .hpp could in principle be vendored as a patch - httpstan compiles arbitrary caller-supplied Stan programs: httpstan/compile.py is the only path to C++ and runs subprocess.run([stanc, ''--name'', ..., ''--print-cpp'', file], timeout=1) per POST /v1/models, with no endpoint, flag or env var accepting pre-generated code, so ahead-of-time transpilation cannot substitute and there is no patch-shaped fix. Everything else is portable and was verified locally in quay.io/pypa/manylinux_2_39_riscv64 (GCC 14.3.1): Stan Math 5.3.0''s make/compiler_flags branches only on __aarch64__ and Windows (no -march/-msse/-mavx anywhere), the vendored classic TBB 2020.3 takes the portable machine/gcc_generic.h atomics path at tbb_machine.h''s __linux__ branch before any architecture #elif and its build/linux.inc falls through to arch := $(uname_m) with def_prefix = lin64 via findstring 64, and both lib/tbb/libtbb.so.2 (a clean riscv64 ELF shared object) and the sundials 6.1.1 static libs built green via httpstan''s own Makefile.libraries. Needing a host C++ compiler at runtime is not a riscv64 gap - upstream''s x86_64 and macOS wheels need one too. Building stanc3 ourselves was spiked and rejected: Rocky 10 riscv64 crb has ocaml 5.2.0-4.el10 and ocaml-dune 3.16.0-4.el10, but stanc.opam pins ocaml {= "4.14.1"} and scripts/install_build_deps.sh pins core.v0.16.0 menhir.20230608 ppx_deriving.5.2.1 fmt.0.11.0 yojson.2.1.0 cmdliner.2.1.0, and dnf finds no opam, ocaml-menhir*, ocaml-core*, ocaml-ppx*, ocaml-yojson*, ocaml-fmt* or ocaml-cmdliner* for riscv64 in any enabled repo (no EPEL 10 riscv64 either); upstream''s recipe is apk add opam && opam switch create 4.14.1 && install_build_deps.sh, i.e. bootstrapping the pinned compiler plus the Jane Street core ppx graph from source for another project''s release artifact that we would then ship as our own compiler binary - gotcha 341''s out-of-scope cross-ecosystem bootstrap, and the exact-version pin is what stops the distro''s own ocaml from short-circuiting it. OCaml itself is not the blocker (4.14.1''s configure.ac maps riscv64-*-linux* to arch=riscv; model=riscv64 with natdynlink=true, and asmcomp/riscv exists) - the pinned dependency graph''s zero riscv64 history is. Re-check when stan-dev/stanc3 adds riscv64 to that Jenkinsfile axis: it is the single upstream ask, it is cheap for them because the QEMU-Docker cross-build already exists, and it also unblocks prophet and any future riscv64 pystan 3.x. No worktree, branch or PR opened - diagnosed from the published wheel plus shallow checkouts of stan-dev/httpstan 4.17.0, stan-dev/stanc3 v2.39.0 and stan-dev/math v5.3.0.'
- pkg: usd-core
  version: '26.8'
  home: https://openusd.org
  repo: https://github.com/PixarAnimationStudios/OpenUSD
  status: publishing
  pr: 2145
  notes: 'PR #2145 green (cp312/cp313/cp314 wheels + publish dry-run), awaiting review. Core (non-imaging) OpenUSD build, so no OpenGL/Vulkan/Metal/X11 - every GPU find_package is inside if (PXR_BUILD_IMAGING). Bindings use the vendored pxr_boost::python, no external Boost since 24.11. Only dependency is TBB 2020.3.1, which builds unpatched on riscv64. One patch adds riscv64 to pxr/base/arch (defines.h #error, math.h gate, stackTrace.cpp execve asm). Merged (#2145). Push-triggered build+publish on main run 35573428656 in progress as of 2026-09-21T07:32Z (three-leg C++ compile, expect hours); move to published once it completes and https://pypi.riseproject.dev/simple/usd-core/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: asyncmy
  version: 0.2.14
  home: https://github.com/long2ice/asyncmy
  repo: https://github.com/long2ice/asyncmy.git
  status: published
  pr: 1674
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pin
  version: 4.1.0
  home: https://github.com/cmake-wheel/pinocchio
  repo: https://github.com/cmake-wheel/pinocchio
  status: blocked-on-dependency
  pr: null
  notes: '10 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The note''s "abi: 0" is a wheel BUILD TAG, not an ABI tag: the real filenames are
    pin-4.1.0-0-cp312-cp312-manylinux_2_28_x86_64.whl etc, where the 0 is PEP 427''s
    optional build number (dist-info/WHEEL says Build: 0, Generator: cmeel 0.60.1).
    These are ordinary per-interpreter compiled wheels for cp310-cp314, not py3-none
    and not abi3 -- every cmake-wheel/cmeel distribution reads "abi: 0" for this reason.
    pin is the PyPI name of the Python bindings of the Pinocchio rigid-body-dynamics
    C++ library; cmake-wheel/pinocchio is a packaging fork of stack-of-tasks/pinocchio
    (same shape as the eigenpy case in gotcha 338) whose pyproject.toml sets
    build-backend = "cmeel" (CMake driven by the cmeel PEP 517 backend). Bindings are
    Boost.Python via eigenpy, not pybind11 (the built .so NEEDs libboost_python312.so
    and libeigenpy.so). PORTABILITY IS FINE: [tool.cmeel] configure-args is only
    -DBUILD_PYTHON_INTERFACE=ON -DBUILD_STANDALONE_PYTHON_INTERFACE=ON
    -DBUILD_WITH_COLLISION_SUPPORT=ON -DBUILD_WITH_LIBPYTHON=OFF, so the default build
    excludes the CppAD/CasADi autodiff backends; the sdist has zero CUDA/nvcc references
    and zero -march/-mavx/SSE strings (Eigen vectorization falls back to scalar); Eigen
    itself is header-only. BLOCKER IS THE DEPENDENCY CHAIN, not the code. The wheel
    ships exactly one file of substance, a 47MB pinocchio_pywrap_default.cpython-312-*.so
    under cmeel.prefix/, with RUNPATH $ORIGIN/../../../../lib into the cmeel prefix
    SHARED with its sibling wheels -- nothing is bundled, so every readelf -d NEEDED
    line is another distribution: libpinocchio_{default,parsers,collision}.so
    (libpinocchio), libcoal.so (libcoal), libeigenpy.so (eigenpy),
    liburdfdom_{model,world,sensor}.so.6 (cmeel-urdfdom), libconsole_bridge.so.1.0
    (cmeel-console-bridge), liboctomap/liboctomath.so.1.10 (cmeel-octomap),
    libboost_*.so.1.90.0 (cmeel-boost). Transitive closure over requires_dist (extras
    dropped) checked against PyPI and pypi.riseproject.dev: SATISFIED are cmeel and
    example-robot-data (py3-none-any, installable from public PyPI as-is) plus
    cmeel-boost 1.90.0 and numpy (riscv64 on our registry). MISSING on both, 11
    packages, each already its own .queue.yml entry: cmeel-zlib, cmeel-tinyxml2,
    cmeel-console-bridge, cmeel-octomap, cmeel-qhull (leaves, need only cmeel) ->
    cmeel-assimp, cmeel-urdfdom, eigenpy -> libcoal -> coal, libpinocchio -> pin.
    Confirmed with the repo''s own oracle: ci_scripts/check_riscv64_deps.py --python 312
    -- ''cmeel-boost~=1.90.0'' ''cmeel-urdfdom>=6'' ''coal>=3.0.3,<4''
    ''libpinocchio==4.1.0'' reports cp312 UNRESOLVABLE (no versions at all for
    cmeel-urdfdom), i.e. the wheel could be neither installed nor smoke-tested.
    Per gotcha 40 this is the "the dependency needs its own port" answer, not a hard
    workflow problem: pin''s own [build-system] requires pins cmeel-urdfdom[build],
    coal[build] and libpinocchio == 4.1.0, so there is not even a build to attempt
    until they exist as riscv64 wheels, and a monolithic build-pin.yml that compiled
    the siblings in-container would rewrite cmeel''s shared-prefix contract, publish
    none of them (outside the port''s commit scope) and still emit a wheel whose
    Requires-Dist (cmeel-urdfdom >= 6, coal >= 3.0.3 < 4, libpinocchio == 4.1.0) the
    index cannot satisfy. Interpreter ceiling for the whole chain is cmeel-boost''s
    riscv64 coverage, today cp312/313/314, although upstream pin ships cp310-cp314.
    Note eigenpy (status blocked-on-dependency, "revisit once cmeel-boost lands") is
    now actionable -- cmeel-boost 1.90.0 riscv64 is on the registry -- but that
    unblocks eigenpy only, not pin. Filed as gotcha 470 (feasibility-and-triage.md).
    No branch/worktree opened: resolved read-only via PyPI JSON, the published aarch64
    wheel (readelf -d) and the sdist, no CI cycle burned. Revisit once libpinocchio,
    coal and cmeel-urdfdom are on the registry.
    UPDATE 2026-09-21 (libpinocchio triage, see its own entry): libpinocchio is NOT a
    cleared blocker -- it was triaged today and parked blocked-on-dependency, because its
    own [build-system] requires pins cmeel-urdfdom[build] >= 6 and libcoal[build] >= 3.0.3
    < 4 and its released libs carry NEEDED libcoal.so / liburdfdom_{model,world,sensor}.so.6,
    neither of which exists for riscv64 on PyPI or our registry. Progress on the 11:
    LANDED are cmeel-zlib, cmeel-tinyxml2, cmeel-console-bridge and cmeel-qhull (all on
    https://pypi.riseproject.dev/simple/) plus cmeel-octomap, merged as PR #2159 with its
    publish still in flight (simple/cmeel-octomap/ 404 as of this note). The ACTIONABLE
    FRONTIER is now cmeel-urdfdom (deps cmeel-console-bridge + cmeel-tinyxml2, both
    published), cmeel-assimp (dep cmeel-zlib, published; no sdist on PyPI, build from the
    checkout) and eigenpy (dep cmeel-boost, published). STILL BLOCKED above them: libcoal
    (needs cmeel-assimp + cmeel-octomap), then coal and libpinocchio, then pin. Filed as
    gotcha 523: a campaign clearing the leaf tier does not make the next-named entry
    actionable, so recompute the frontier per candidate instead of assuming it advanced.
    UPDATE 2026-09-21T15:39Z (libcoal triage, see its own entry): cmeel-octomap''s publish
    has landed, so https://pypi.riseproject.dev/simple/ now serves SIX of the 11 --
    cmeel-boost, cmeel-console-bridge, cmeel-octomap, cmeel-qhull, cmeel-tinyxml2,
    cmeel-zlib (octomap and qhull both 200, both 404 a few hours earlier). libcoal 3.0.3
    was triaged and parked blocked-on-dependency on a SINGLE remaining package,
    cmeel-assimp 6.0.5: the oracle run over its full [build-system] requires fails only on
    cmeel-assimp ("from versions: none"), and the same set with cmeel-assimp dropped is ok
    on cp312/cp313/cp314. pin''s MISSING set is therefore down from 11 to five --
    cmeel-assimp, cmeel-urdfdom, eigenpy, libcoal, coal, libpinocchio (six entries, of
    which three are now one tier from actionable). ACTIONABLE FRONTIER, recomputed:
    cmeel-assimp (deps cmeel + cmeel-zlib, both available; no sdist on PyPI, build from
    the checkout), cmeel-urdfdom (deps cmeel-console-bridge + cmeel-tinyxml2, published)
    and eigenpy (dep cmeel-boost, published). cmeel-assimp is the highest-leverage of the
    three: it is the only thing standing between libcoal and a port, and libcoal in turn
    is a blocker of both coal and libpinocchio. STILL BLOCKED above them: libcoal (needs
    cmeel-assimp), then coal (needs cmeel-assimp + eigenpy + libcoal) and libpinocchio
    (needs cmeel-urdfdom + libcoal), then pin.'
- pkg: s5cmd
  version: 0.3.3
  home: https://github.com/jcfr/s5cmd-python-distributions
  repo: https://github.com/jcfr/s5cmd-python-distributions
  status: published
  pr: 1675
  notes: '6 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: valkey-glide
  version: 2.5.1
  home: https://github.com/valkey-io/valkey-glide
  repo: https://github.com/valkey-io/valkey-glide
  status: published
  pr: 1678
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: eigenpy
  version: 3.13.0
  home: https://github.com/cmake-wheel/eigenpy
  repo: https://github.com/cmake-wheel/eigenpy
  status: blocked-on-dependency
  pr: null
  notes: '10 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev. home/repo correctly point at cmake-wheel/eigenpy, not stack-of-tasks/eigenpy: confirmed via `gh api repos/cmake-wheel/eigenpy` that it is a fork whose parent is stack-of-tasks/eigenpy, and upstream''s own checked-in pyproject.toml (v3.13.0 tag) carries no [build-system] at all -- a plain CMake project (Boost.Python bindings between Eigen and NumPy, BSD-2-Clause) consumed via conda/robotpkg/apt, not buildable with pip. The fork''s own pyproject.toml is what actually produces the wheels PyPI publishes as eigenpy: build-backend = "cmeel", and project.dependencies is ["cmeel-boost ~= 1.90.0"] -- confirmed against the live eigenpy PyPI JSON''s info.requires_dist, not just the fork''s source. cmeel-boost is a runtime dependency (eigenpy''s compiled .so links its Boost.Python shared libraries), not just a build input, so pip install eigenpy cannot resolve on riscv64 until cmeel-boost itself has a riscv64 wheel. cmeel (build dep) and cmeel-eigen (build dep, Eigen is header-only) are both py3-none-any -- zero riscv64 concern, already installable from public PyPI as-is. cmeel-boost is the opposite: its sdist is a 4KB CMakeLists.txt whose ExternalProject_Add downloads the entire upstream boost_1_90_0.tar.bz2 and runs ./bootstrap.sh && ./b2 link=shared python=3.X with no --with-libraries filter, i.e. it compiles Boost''s whole default library set once per interpreter -- a build on the order of this repo''s largest to date (libclang''s ~10h from-scratch LLVM/Clang compile), plausibly portable (no architectural riscv64 blocker) but a full dedicated port of its own, which is why cmeel-boost already carries its own separate .queue.yml entry (status: queued) rather than being something to inline into eigenpy''s build. Per gotcha 125 a pip-resolvable sdist isn''t automatically a reasonable one either: cmeel-boost would likely build from sdist inside a cibuildwheel container (a green CI run), but that same full-Boost compile would then re-run for every future end user''s pip install eigenpy on riscv64 (no riscv64 wheel to resolve to), and most end-user environments won''t have the toolchain for it (CMake >= 4.0, a C++ compiler, numpy headers) -- defeating the "simple index to install riscv64 wheels from" goal even on a green run. Sequenced behind cmeel-boost''s own port, same shape as the k-means-constrained/ortools and cvxpy/sparsediffpy cases; filed as gotcha 338 (feasibility-and-triage.md). No worktree/branch opened -- resolved fully read-only via PyPI JSON, gh api, and one cmeel-boost sdist download, no checkout needed. Revisit once cmeel-boost lands on the registry.'
- pkg: torch-memory-saver
  version: 0.0.9.post1
  home: https://github.com/fzyzcjy/torch_memory_saver
  repo: https://github.com/fzyzcjy/torch_memory_saver
  status: parked
  pr: null
  notes: 'Not feasible: hard NVIDIA-CUDA (or AMD-HIP / Intel-XPU) requirement at build time and at import time, with no CPU backend anywhere in the tree; same disposition as the parked nccl4py / pynvvideocodec / sglang / cuda-tile / onnxruntime-gpu / humming-kernels entries (gotchas 284/386/452). Folded as gotcha 471 (feasibility-and-triage.md). Upstream located (PyPI records no project URL): github.com/fzyzcjy/torch_memory_saver, Apache-2.0, referenced from sgl-project/sglang#2542; there is no .github/workflows at all, the release recipe is the Makefile plus scripts/{build,build_in_docker,build_multi_cuda,rename_wheels}.sh. A conda-forge/torch_memory_saver-feedstock also exists. Everything below is read off the real 0.0.9.post1 sdist, the real manylinux2014_aarch64 wheel and an upstream clone, not inferred. (1) WHAT IT DOES. It pauses/resumes CUDA *device* allocations: the tensor''s virtual address is kept while its physical pages are released, via the CUDA virtual-memory-management driver API. readelf -Ws on torch_memory_saver_hook_mode_preload.abi3.so lists cuMemCreate, cuMemMap, cuMemUnmap, cuMemRelease, cuMemSetAccess, cuMemAddressReserve, cuCtxGetDevice, cuDeviceGetAttribute undefined plus cudaMallocHost/cudaMemcpy/cudaFreeHost@libcudart.so.12, and readelf -d gives DT_NEEDED libcuda.so.1 + libcudart.so.{12,13}. The queue prior that it might hook generic malloc/mmap under LD_PRELOAD and be useful GPU-less is wrong on both halves: csrc/entrypoint.cpp under TMS_HOOK_MODE_PRELOAD exports cudaMalloc/cudaFree (not malloc/free) and forwards via dlsym(RTLD_NEXT, "cudaMalloc"), and the pause/resume primitive has no host-RAM analogue in that API. The second mode (TMS_HOOK_MODE_TORCH) is a torch.cuda.memory.CUDAPluggableAllocator, i.e. CUDA-only by construction. (2) BUILD WALL, proven locally on a CUDA-less host. setup.py _detect_platform() returns only hip or cuda (master adds xpu, still no CPU member); the CUDA arm sets libraries=[''cuda'',''cudart''] with -I$CUDA_HOME/include and -L$CUDA_HOME/lib64{,/stubs} and requires TMS_CUDA_MAJOR. Stage one: python3 setup.py --version -> RuntimeError: TMS_CUDA_MAJOR env var must be set for CUDA builds. Stage two: TMS_CUDA_MAJOR=12 pip wheel . --no-deps --no-build-isolation -> csrc/macro.h:46:10: fatal error: cuda_runtime_api.h: No such file or directory. csrc/macro.h, core.h and core.cpp each end their platform switch with #error "USE_PLATFORM is not set", so there is no third branch to compile. Note the sdist ships hardware_amd_support.h but NOT hardware_amd_support.cpp, so even the ROCm arm cannot be built from the released sdist. (3) IMPORT WALL, independent of the build (gotcha 452 shape). torch_memory_saver/utils.py _detect_cuda_major() reads torch.version.cuda, then probes ctypes.CDLL(libcudart.so.{13,12}), else raises RuntimeError: could not detect CUDA runtime -- reproduced by executing that module on this GPU-less host. Our riscv64 torch is CPU-only (torch.version.cuda is None), so the resolver that picks between the shipped <stem>_cu12/<stem>_cu13 .so variants can never succeed; and entrypoint.py imports torch at module top level although requires_dist is empty. (4) THE aarch64 WHEEL IS NOT A PRECEDENT FOR US. 0.0.9.post1 is the release that added manylinux2014_aarch64 (0.0.9 and earlier are x86_64-only; 0.0.10 keeps the same pair), which invites "the arch axis is open". scripts/build.sh''s whole arch switch is aarch64 -> LIBCUDA_ARCH=sbsa + BUILDER_NAME=pytorch/manylinuxaarch64-builder, else LIBCUDA_ARCH=$ARCH + pytorch/manylinux2_28-builder, image ${BUILDER_NAME}:cuda${CUDA_VERSION}; build_in_docker.sh then symlinks /usr/local/cuda-$CUDA_VERSION/targets/$LIBCUDA_ARCH-linux/lib/stubs/libcuda.so. aarch64 works only because NVIDIA ships an sbsa CUDA toolkit and PyTorch a CUDA aarch64 builder image (gotcha 418 sharpened). (5) NOTHING EXISTS ON riscv64 FOR ANY OF THE THREE BACKENDS. NVIDIA CUDA redist: redistrib_13.0.0/13.2.0/13.4.2.json list only linux-x86_64, linux-sbsa, linux-all, windows-x86_64/arm64 -- no riscv64, and no riscv64 driver, so libcuda.so.1 does not exist. AMD ROCm: repo.radeon.com/rocm/apt/latest/dists/noble/main publishes binary-amd64 only. Intel oneAPI/XPU: no riscv64 either. (6) NO STUB ESCAPE HATCH. There is no CUDA-free build mode to abuse (unlike gotcha 387/452, where one exists and is still rejected) -- the #error is unconditional -- and upstream''s own test suite (test/test_examples.py) is all device=''cuda''/torch.cuda, so a hypothetical artifact could not be smoke-tested either (gotcha 6). Version re-checked per gotcha 388: 0.0.10 is the current release and has the identical shape (x86_64 + aarch64 manylinux2014 cp39-abi3 wheels + sdist, zero requires_dist), and master only adds an Intel XPU backend, so both are blocked identically. Registry checked: pypi.riseproject.dev/simple/torch-memory-saver/ -> 404 (torch itself -> 200, CPU-only). No worktree/branch/workflow/docs-yaml/patches/PR created for the port; nothing to rehearse locally since the compile cannot start. Revisit only if NVIDIA ships a riscv64 CUDA toolkit and driver (or AMD/Intel a riscv64 GPU stack).'
- pkg: cmsis-pack-manager
  version: 0.6.0
  home: https://github.com/pyocd/cmsis-pack-manager/
  repo: https://github.com/pyocd/cmsis-pack-manager/
  status: publishing
  pr: 2149
  notes: 'The py3 abi tag is honest, not a hand-set plat-name: maturin builds the Rust core as a cdylib and cffi ABI mode dlopens it, so one wheel serves every interpreter (gotcha 479). 133-crate graph, rustls not openssl, nothing arch-specific; rustup has a riscv64 host build of the 1.71.1 upstream pins. CI green on PR #2149 - built once on cp312 in 15 minutes, 11 tests pass on each of cp312/cp313/cp314/cp314t, publish dry-run clean. cp314t needed PIP_ONLY_BINARY=hypothesis (gotcha 210). Merged (#2149). Push-triggered build+publish on main run 35573490402 succeeded. 404 on pypi.riseproject.dev/simple/cmsis-pack-manager/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: ulid-transform
  version: 2.2.9
  home: https://github.com/bluetooth-devices/ulid-transform
  repo: https://github.com/bluetooth-devices/ulid-transform
  status: published
  pr: 1201
  notes: '31 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: espeakng-loader
  version: 0.2.4
  home: https://github.com/thewh1teagle/espeakng-loader
  repo: https://github.com/thewh1teagle/espeakng-loader
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1395
  notes: 'PR #1395 green (wheel built, license/content checks, gpl_sources, smoke test and publish dry-run all pass), awaiting review. Upstream compiles eSpeak NG 1.52.0 from source per platform (build.sh/build.yml) and repackages the .so + data as a py3-none-<platform> wheel via hatch_build.py''s WHEEL_TAG hook (no riscv64 wheel today) - a genuine compile-from-source port, not a vendored prebuilt binary. riscv64 needs -DCMAKE_POSITION_INDEPENDENT_CODE=ON (espeak-ng''s vendored sonic subproject skips -fPIC, which only riscv64''s ld rejects). No git tags upstream; pinned release commit 146599e29be3 (tree matches published 0.2.4). gpl_sources archives the eSpeak NG (GPLv3) source it compiles from, since the wheel ships the compiled library directly.'
- pkg: text2num
  version: 3.1.0
  home: https://github.com/allo-media/text2num
  repo: https://github.com/allo-media/text2num
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1808
  notes: 'PyO3/maturin extension (not pure Python despite the name), non-abi3 - upstream ships cp3x-cp3x wheels per interpreter (cp38-cp315 incl. free-threaded cp313t/cp314t, plus pp311). Cargo.lock is tracked upstream. PR #1808 open, CI running (cp312/cp313/cp314/cp314t riscv64 wheels).'
- pkg: mmcif
  version: 1.1.1
  home: https://mmcif.wwpdb.org
  repo: https://github.com/rcsb/py-mmcif
  status: published
  pr: 1682
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyxirr
  version: 0.10.8
  home: https://github.com/Anexen/pyxirr
  repo: https://github.com/Anexen/pyxirr
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1203
  notes: '90 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pydantic-monty-client
  version: 0.0.21
  home: https://github.com/pydantic/monty
  repo: https://github.com/pydantic/monty
  status: published
  pr: 1676
  notes: '40 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rtoml
  version: 0.13.0
  home: https://github.com/samuelcolvin/rtoml
  repo: https://github.com/samuelcolvin/rtoml
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1200
  notes: '40 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: delta-kernel-rust-sharing-wrapper
  version: 0.3.3
  home: https://github.com/delta-io/delta-sharing
  repo: https://github.com/delta-io/delta-sharing
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1677
  notes: 'Subdirectory of the delta-sharing monorepo (python/delta-kernel-rust-sharing-wrapper), no dedicated git tags; pinned to release commit d0712f61b728670896fdd1fec5d4ed2967073c79 (verified byte-identical to the 0.3.3 PyPI sdist). 1 Linux wheel upstream (abi: cp310-abi3, manylinux_2_28); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tree-sitter-regex
  version: 0.25.0
  home: https://github.com/tree-sitter/tree-sitter-regex
  repo: https://github.com/tree-sitter/tree-sitter-regex
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1198
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ecos
  version: 2.0.14
  home: http://github.com/embotech/ecos
  repo: https://github.com/embotech/ecos-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1110
  notes: 'PR open (#1110), CI fully green (cp312/cp313/cp314/cp314t, 13 passed each; publish dry-ran cleanly). Self-contained C solver (bundles AMD/LDL/SuiteSparse_config), built from embotech/ecos-python (the actual Python wrapper repo, not embotech/ecos) at tag v2.0.14, manylinux only (upstream skips musllinux itself). numpy/scipy come from pypi.riseproject.dev (no riscv64 wheel on public PyPI). Tests staged via CIBW_TEST_SOURCES (src/test_interface.py, src/test_interface_bb.py only) to avoid shadowing the installed wheel with src/ecos. Merged (#1110). Issue #1135 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: eccodeslib
  version: 2.48.0.26
  home: https://confluence.ecmwf.int/display/ECC/ecCodes+Home
  repo: https://github.com/ecmwf/eccodes
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1694
  notes: 'Binary wrapper wheel (ECMWF python_wrapper convention): compiled ecCodes C library only, no Python bindings of its own. Upstream builds it via a private wheelmaker tool (ecmwf/eccodes python_wrapper/), not a public workflow; content is Python-ABI-independent (cp310..cp314 wheels are byte-identical), so porting as a single py3-none wheel. Upstream links eckit (ENABLE_ECKIT_GEO=1, hard Requires-Dist: eckitlib) which has no riscv64 build yet (eckitlib itself queued, unported); building with ENABLE_ECKIT_GEO=0 (eccodes CMake default) drops that dependency, keeping core GRIB/BUFR support. PR #1694 open, CI running.'
- pkg: mecab-python3
  version: 1.0.12
  home: https://github.com/SamuraiT/mecab-python3
  repo: https://github.com/SamuraiT/mecab-python3
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1117
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Sdist ships only the SWIG wrapper, not MeCab''s C++ core; CIBW_BEFORE_ALL_LINUX builds libmecab from taku910/mecab (config.guess/config.sub swapped for automake''s, mecab''s predate riscv64), setup.py links it via mecab-config. Patch adds a LICENSE matching setuptools'' default glob so the BSD text mecab-python3 declares itself under actually ships. PR #1117 open, CI running. Merged (#1117). Issue #1130 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: cmeel-boost
  version: 1.90.0
  home: https://github.com/cmake-wheel/cmeel-boost
  repo: https://github.com/cmake-wheel/cmeel-boost.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1810
  notes: '10 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev. cmeel build backend wraps a CMake ExternalProject_Add that bootstraps and b2-builds the full upstream Boost 1.90.0 default library set per interpreter (no --with-libraries filter); manylinux_riscv64 image already carries a pipx cmake>=4 ahead of PATH (gotcha 358), satisfying CMakeLists.txt''s cmake_minimum_required(VERSION 4.0) floor with no image changes. No static libgcc/libstdc++ linking, so no gpl_sources job needed; upstream itself ships no LICENSE file in the wheel (PEP 639 License-Expression: BSL-1.0 only), so no licensing patch needed either. PR #1810 open, CI running (cp312/cp313/cp314 riscv64 wheels).'
- pkg: chdb-core
  version: 26.7.0
  home: https://clickhouse.com/chdb
  repo: https://github.com/chdb-io/chdb-core
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. Parked on disproportionate scale, not on an architectural or vendored-dependency blocker; folded as gotcha 475 (feasibility-and-triage.md). No branch, worktree, workflow, docs/packages yaml, patches or PR created, and no runner slot used - settled read-only from a depth-1 clone of chdb-io/chdb-core, the PyPI JSON and this repo''s own measured build history. (1) WHAT IT IS. chdb-core is not a binding over a library: the repository *is* a ClickHouse fork (src/ holds 4,559 .cpp, plus base/, programs/ and a 285-entry contrib/ of which .github/scripts/update-submodules.sh documents 139 as submodules to clone), and the wheel''s payload is chdb/_chdb.abi3.so - the whole engine linked as one abi3 module - beside the pybind11 nonlimitedapi shim libraries. Apache-2.0. Four wheels per release, all cp39-abi3: macosx x86_64/arm64 and manylinux_2_17 x86_64 (167MB) / aarch64 (130MB). No sdist since the 0.0.1 placeholder, so a port would build from the v26.7.0 tag, mirroring .github/workflows/build_linux_arm64_wheels-gh.yml (self-hosted arm64, persistent ccache, timeout-minutes: 600 per build step, and its own comments call it "a three-hour wheel build"). (2) NOT ARCHITECTURALLY BLOCKED - every usual check comes out green, which is why this needed real reading rather than a scale reflex: cmake/arch.cmake sets ARCH_RISCV64, cmake/linux/toolchain-riscv64.cmake exists, ci/defs/job_configs.py carries a BuildTypes.RISCV64 job producing CH_RISCV64 (i.e. ClickHouse itself builds riscv64 in CI), contrib/jemalloc-cmake/include_linux_riscv64 and contrib/llvm-project-cmake''s ARCH_RISCV64 branches are already written (so the ENABLE_EMBEDDED_COMPILER LLVM has a RISCV target), contrib/corrosion-cmake maps riscv64gc-unknown-linux-gnu, the vendored Rust is wasmtime 45.0.1 / cranelift 0.132 (riscv64 backend long upstream), and cmake/tools.cmake''s CLANG_MINIMUM_VERSION 21 - with GCC rejected outright - is satisfied off the shelf by the manylinux_2_39_riscv64 image''s own clang/lld 21.1.8 (gotcha 454). No vendored blob, no closed dependency, no GPU. (3) WHY PARKED ANYWAY - the arithmetic. Using this repo''s two measured per-edge costs on the same 4-core T-Head runners (build-vtk.yml: 5,095 of 12,192 ninja edges in 7.5h, ~21 core-s/edge; stpyv8''s V8: 1,146 of 2,117 targets in 9h17m, ~117 core-s/edge), ClickHouse TUs sit in the second class - upstream''s own heavy-build guard (cmake/heavy_build_check_scripts/prlimit_generic.sh, wired by ENABLE_CHECK_HEAVY_BUILDS in its riscv64 CI job) budgets 1000 CPU-seconds and 5GB *per translation unit* on a fast builder. ~4,900 in-tree TUs at V8-class cost is already ~160 core-hours (~40h wall at 4 cores) before contrib''s own thousands (embedded-compiler LLVM, arrow/parquet/thrift, aws-sdk-cpp, azure, icu, rocksdb, protobuf, librdkafka, mongo, libpqxx) and before the Rust workspace (270 crates plus delta-kernel-rs; deltalake''s 840-crate graph took 8.1h here). And chdb/build.sh builds the tree *twice*: once with -DENABLE_PYTHON=0 (relinking the clickhouse link line into libchdb.so), then reconfigured with -DENABLE_PYTHON=1 for _chdb.abi3.so - not incremental, because ENABLE_PYTHON sets USE_PYTHON in src/configure_config.cmake which is #cmakedefine01''d into src/Common/config.h.in, a header nearly every TU includes. Skipping the standalone libchdb.so (the wheel does not ship it; CHDB_LITE=1 already skips it) is the one real lever and still leaves one full pass. Against 48h - the largest timeout-minutes this repo has ever granted (build-vtk.yml, build-nodejs-wheel-binaries.yml, build-cadquery-ocp-novtk.yml) and ~5x the ~10h libclang record - that is at or past the ceiling on a *green* first try, with no margin for the iterations a first-ever port always needs, on a pool that had 8 runs in flight when this was triaged (two of them V8 builds measured in days). RAM is tight but not the wall: upstream''s cmake/limit_jobs.cmake with MAX_COMPILER_MEMORY 2500 / MAX_LINKER_MEMORY 5000 resolves to 4 compile jobs on the runners'' 15GB (gotcha 421: no swap), so -j2 would likely be needed for the heavy TUs, doubling the wall time again. (4) THE PORT WOULD ALSO BE NEW GROUND, NOT A NARROWING OF UPSTREAM''S riscv64 JOB (goal 2). Upstream''s riscv64 build is a cross-compile from ARM runners against contrib/sysroot/linux-riscv64, and every riscv64 concession in cmake/target.cmake (GLIBC_COMPATIBILITY OFF, ENABLE_PARQUET OFF, ENABLE_RUST OFF - "it might be ok, but we need to update ''sysroot''" - ENABLE_MYSQL/HDFS/GRPC/LDAP OFF, OPENSSL_NO_ASM ON) sits inside if (CMAKE_CROSSCOMPILING). A native container build has CMAKE_CROSSCOMPILING 0, so it gets none of them and instead configures exactly the libraries upstream has never built for riscv64. chdb/build.sh additionally forces ON several things with no riscv64 code path: -DENABLE_VECTORSCAN=1, -DENABLE_USEARCH=1 -DENABLE_SIMSIMD=1 (all three contrib CMakes list AMD64/AARCH64 sources only) and -DGLIBC_COMPATIBILITY=1, whose base/glibc-compatibility/CMakeLists.txt is a bare message(FATAL_ERROR "glibc_compatibility can only be used on x86_64 or aarch64"). Each is a one-line override (and GLIBC_COMPATIBILITY=0 is harmless here since riscv64 manylinux is _2_39, not _2_17), but they are divergences to justify, not inherited support. Also note build.sh hard-requires the build interpreter to be exactly Python 3.9 for the abi3 wheel. (5) NO LOCAL REHEARSAL WAS POSSIBLE, which is what makes the first CI run an unvalidated multi-day shot: gotcha 9''s local configure needs clang >= 21 (host had 18.1.3), ~10GB for the 139-submodule checkout (8GB free) and tens of GB of build tree, so even the ninja edge count cannot be measured cheaply. (6) Version re-checked per gotcha 388: 26.7.3 is now the latest release (26.7.0 is what this entry claimed) with the identical four-wheel cp39-abi3 shape, so both are the same job; chdb-core-lite (CHDB_LITE=1, ~50MB, ~30 libraries off, MinSizeRel, no libchdb.so) is a separate distribution and still compiles the same engine src/, so it does not change the verdict. Same disposition as the parked ortools and tensorflow entries. Revisit if the riscv64 pool gains materially more cores per runner or a persistent ccache across jobs, or if upstream starts publishing riscv64 wheels itself.'
- pkg: austin-dist
  version: 4.0.0
  home: https://github.com/P403n1x87/austin
  repo: https://github.com/P403n1x87/austin
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2148
  notes: 'Real per-platform artifact despite the py3 tag: a compiled C binary packed
    by upstream''s own scripts/build-wheel.py (--files mode), so the port is a
    from-source build in the manylinux riscv64 image plus a one-entry platform-table
    patch. The wheel ships austin alone, the shape upstream''s musllinux wheels already
    have: austinp, the native-unwinding variant, segfaults on riscv64 both in austin''s
    own MOJO emitter (events.c:83 dereferences the UNKNOWN_SCOPE sentinel - a latent
    upstream bug) and inside libunwind''s riscv64 ELF symbol lookup. PR #2148 green on
    cp312/cp313/cp314 (34 tests unprivileged, 42 as root, per interpreter) with the
    publish dry-run clean; 3 patches, gotchas 498-502 recorded. Merged (#2148). Push-triggered build+publish on main run 35593976247 in progress as of 2026-09-21T11:25Z; move to published once https://pypi.riseproject.dev/simple/austin-dist/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: jsonpath-rust-bindings
  version: 1.1.1
  home: https://github.com/night-crawler/jsonpath-rust-bindings
  repo: https://github.com/night-crawler/jsonpath-rust-bindings
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1210
  notes: 'Located upstream via PyPI owner "night.crawler" -> night-crawler/jsonpath-rust-bindings. pyo3/maturin binding over jsonpath-rust; no abi3 feature, matrix is cp312/cp313/cp314/cp314t. mimalloc dep is cfg-gated off for linux+glibc+riscv64. PR #1210 green (all 4 interpreters, 22 tests each) and publish dry-run passed; awaiting review.'
- pkg: dlib-bin
  version: 20.0.1
  home: https://github.com/alesanfra/dlib-wheels
  repo: https://github.com/alesanfra/dlib-wheels
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: eckitlib
  version: 2.1.1.26
  home: https://github.com/ecmwf/eckit
  repo: https://github.com/ecmwf/eckit
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2147
  notes: 'Binary wrapper wheel (ECMWF python_wrapper convention, sibling of eccodeslib): the compiled eckit C++ toolkit only, no Python bindings of its own (those are the separate `eckit` distribution). Upstream is https://github.com/ecmwf/eckit at tag 2.1.1 (PyPI records no project URL; the released wheel''s __commit_hash__ resolves to that tag). The wheel job calls ecmwf/reusable-workflows'' python-wrapper-wheel.yml plus a private wheelmaker image, but the recipe is public in python/eckitlib/{buildconfig,pre-compile.sh,post-build.sh}, so the port replays its CMAKE_PARAMS and its auditwheel repair directly. No MPI, Fortran, Eigen, LAPACK, PROJ or Qhull (all off in upstream''s own config); only libcurl, lz4 and flex are needed, all in Rocky 10 riscv64. No arch-specific code: a QEMU configure in manylinux_2_39_riscv64 generated an eckit_config.h byte-identical to the released x86_64 wheel''s. Payload is Python-ABI-independent (cp310..cp314 wheels byte-identical), so one py3-none wheel. auditwheel grafts libcurl''s closure (22 RPMs), whose licences are collected from its CycloneDX SBOM and whose copyleft members get a gpl_sources job. The eckit-version binary the wheel ships segfaults on start - on upstream''s own released x86_64 wheel too - so the smoke test calls libeckit''s eckit_version_str/eckit_git_sha1 through ctypes instead. PR #2147 open, CI green. Merged (#2147). Push-triggered build+publish on main run 35573470237 succeeded. 404 on pypi.riseproject.dev/simple/eckitlib/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: simpleitk
  version: 2.5.6
  home: http://simpleitk.org/
  repo: https://github.com/SimpleITK/SimpleITK
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2146
  notes: 'PR #2146 opened (draft); pr-checks and the setup job green, both riscv64 build legs queued on the pool (timeout 1440m, so the compile outlives this session; run 35559886520). 8 Linux wheels upstream (abi: cp310,cp311,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Triaged for scale first and it is NOT the paddlepaddle/chdb/tensorstore class the brief feared: upstream''s own CDash dashboard (open.cdash.org, project SimpleITK, Package/Experimental rows at the v2.5.6 tag, 2026-07-29/30) prices the complete SuperBuild - Lua + PCRE2 + SWIG 4.4.1 + ITK 5.4.7 (default modules plus ITKReview, SimpleITKFilters, IOTransformMINC, GenericLabelInterpolator, LabelErodeDilate) + the static SimpleITK core - at 1h03m-1h50m of compile on 4-core GitHub-hosted x86 Linux and 2h30m-3h30m on 4-core macOS, with every per-language row (build-py39/310/311/314t, build-java) at only 2-8m because Wrapping/<lang> compiles just the SWIG wrap TU and links the prebuilt core (new gotcha 476). Upstream''s Linux wheels, aarch64 included, come out of one Package.yml package-docker job on ubuntu-latest/ubuntu-22.04-arm (4 vCPU) that builds the core once plus Java plus four Python wrappings plus ctest inside GitHub''s 6h hosted cap, and the manylinux2014_aarch64 wheel on PyPI proves it finishes. TU census: 2409 compiled sources in ITK (1990 of them cheap vendored C - HDF5/GDCM/OpenJPEG/TIFF/PNG/zlib-ng/MINC/NIFTI/MetaIO/VXL) + 81 hand-written SimpleITK + 297 generated filter .cxx (one per Code/BasicFilters/json entry) ~ 2800 edges against VTK''s 12192 and chdb''s ~4900, and abi3 halves the legs - so ~8-15h per leg here (VTK measured 5095/12192 edges in 7.5h on this fleet), inside the budget and cheaper than vtk/openvino/mini-racer, all of which this repo built. No architectural blocker: every x86 intrinsic in the vendored trees is ISA-gated (Eigen ConfigureVectorization, vnl_math __SSE2__, gdcmopenjpeg, zlib-ng arch/x86), KWIML/zlib-ng already detect riscv64, no vendored blobs, no CUDA/Bazel/conda, and no ITK dependency wheel is needed because the wheel statically links its own ITK into one 272MB _SimpleITK.so (upstream''s x86 wheel has no auditwheel .libs/ dir, hence no libgomp and no gpl_sources job, and carries LICENSE+NOTICE in dist-info/licenses). One patch: SuperBuild/External_ITK.cmake forwards ITK_*/Module_*/GDCM_*/NIFTI_*/FFTW/TBB_DIR into the ITK sub-build''s cache but nothing WITH_*, so -DWITH_RVV=OFF silently reached nothing - and it is required, since ITK vendors zlib-ng, which defaults WITH_RVV ON on riscv and probes it with the vsetvli that SIGILLs on this hardware (gotchas 272/279). Patch adds ^WITH_ to the forwarded set; verified locally on x86 that the SuperBuild configures clean with the workflow''s exact -D set (Configuring done 1.5s) and that ITK-build/CMakeCacheInit.txt then gains set( WITH_RVV "OFF" ... FORCE ) (new gotcha 477). Other deviations from upstream''s cmd.sh, each one line in the workflow: BUILD_TESTING=OFF in both CMake stages (skips the 30 C++ test TUs and the ExternalData downloads; upstream''s Testing/Unit/Python suite runs against the installed wheel instead of ctest in the build tree), /usr/bin/cmake 3.31.8 rather than the image''s cmake 4.4 (ITK''s vendored trees declare minimums below 3.5, gotcha 358), and SimpleITK_PYTHON_USE_VIRTUALENV=OFF plus VIRTUAL_PYTHON_EXECUTABLE=<image python> because the dist target''s venv pip-installs numpy!=1.24.*,<2.5, which has no riscv64 wheel and would be built from source mid-job. Matrix is cp311 + cp314t, not the default four: Wrapping/Python/dist/CMakeLists.txt hardcodes --py-limited-api=cp311, so the limited-API wheel is always tagged cp311-abi3 (upstream''s own wheel name) and is built on cp311 to match the tag it claims (gotcha 96), covering cp312-cp314; free-threaded cannot use the stable ABI, so cp314t is the second leg. numpy for the tests comes from the registry with PIP_ONLY_BINARY=numpy (2.4.3 is the newest cp311 riscv64 wheel there). Worktree .claude/worktrees/simpleitk, branch simpleitk. Next step: read the two build jobs of run 35559886520.'
- pkg: fnv-hash-fast
  version: 2.0.3
  home: https://github.com/bluetooth-devices/fnv-hash-fast
  repo: https://github.com/bluetooth-devices/fnv-hash-fast
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1114
  notes: 'PR #1114 open, CI fully green (cp312/cp313/cp314/cp314t build+test, publish dry-run). 27 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. One-file C extension (fnv_hash_fast._fnv_impl, plain Python.h, no arch-specific code), poetry-core build backend, pure-Python fnvhash fallback. REQUIRE_EXTENSION=1 does not actually gate a broken compile - build_ext.py''s custom build_extensions() swallows the exception unconditionally - so CIBW_TEST_COMMAND asserts the .so directly (verified locally by deliberately breaking _fnv_impl.c: build still succeeds, silently drops the .so); added as gotcha 295. CIBW_TEST_SOURCES: tests avoids pyproject.toml''s pythonpath = ["src"], which would otherwise shadow the compiled wheel with the raw source (verified locally both ways). Benchmarks excluded (need pytest-codspeed). Verified locally end-to-end on macOS/arm64 from a fresh checkout of tag v2.0.3: extension compiles, all 4 non-benchmark tests pass. One cp312 job hit a transient self-hosted-runner "Cannot connect to the Docker daemon" failure, unrelated to the workflow; reran clean. Worktree at .claude/worktrees/fnv-hash-fast, branch fnv-hash-fast. Merged (#1114). Issue #1131 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: tensorflow-addons
  version: 0.23.0
  home: https://github.com/tensorflow/addons
  repo: https://github.com/tensorflow/addons
  status: blocked-on-dependency
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp39), all manylinux_2_17_x86_64; no riscv64 on PyPI or pypi.riseproject.dev. Upstream located: github.com/tensorflow/addons (Apache-2.0), which PyPI omits (no project URL, Home-page UNKNOWN). Blocked on tensorflow -- same family and same wall as tensorflow-text and tensorflow-io-gcs-filesystem (gotcha 343), but PERMANENTLY, which is the part those two entries do not cover, so folded into the skill as gotcha 478. ARCHIVED, SO THE PIN CAN NEVER MOVE: the GitHub repo is archived: true (read-only, pushed_at 2025-09-04), 0.23.0 is the last of 35 releases (uploaded 2023-11-28) and PyPI''s current version, and tensorflow_addons/version.py pins INCLUSIVE_MIN_TF_VERSION = 2.13.0 / EXCLUSIVE_MAX_TF_VERSION = 2.16.0, with utils/resource_loader.py narrowing the compiled-op ABI window further to [2.15.0, 2.16.0) and WORKSPACE pinning org_tensorflow to the v2.15.0 tarball. So this does not need a riscv64 tensorflow, it needs a riscv64 tensorflow 2.15 -- a 2023 release. This repo''s tensorflow entry is 2.21.0 and parked (and tensorflow-cpu 2.21.0, parked, gotcha 426); there is no docs/packages/tensorflow.yaml and pypi.riseproject.dev/simple/tensorflow/ -> 404 (control: .../numpy/ -> 200). Unparking tensorflow at 2.21.0 would therefore NOT unblock this entry. BUILD-TIME BLOCKER, CONFIRMED FROM UPSTREAM SOURCE AND FROM THE SHIPPED WHEEL: every custom op links against the libtensorflow_framework.so of a pip-installed tensorflow, i.e. a target-architecture ELF the linker must consume. The mechanism is a third variant of gotcha 343''s, so grep for it separately: configure.py does a module-level import tensorflow, reads tf.sysconfig.get_compile_flags() / get_link_flags(), and writes TF_HEADER_DIR / TF_SHARED_LIBRARY_DIR / TF_SHARED_LIBRARY_NAME as build --action_env lines into a generated .bazelrc; WORKSPACE''s tf_configure (build_deps/tf_dependency/tf_configure.bzl, _symlink_genrule_for_dir with cmd = cp -f) copies that directory''s real .so into @local_config_tf, whose BUILD.tpl declares cc_library(name = libtensorflow_framework, srcs = [%{TF_SHARED_LIBRARY_NAME}]); and custom_op_library() in tensorflow_addons/tensorflow_addons.bzl appends @local_config_tf//:libtensorflow_framework plus :tf_header_lib to the deps of EVERY op library unconditionally. Verified end to end against the released x86_64 wheel: all 8 op libraries (image/_image_ops.so, image/_distort_image_ops.so, image/_resampler_ops.so, layers/_correlation_cost_ops.so, layers/_embedding_bag_ops.so, seq2seq/_beam_search_ops.so, text/_parse_time_op.so, text/_skip_gram_ops.so) carry readelf -d NEEDED libtensorflow_framework.so.2. No riscv64 candidate exists at any layer: no tensorflow* wheel anywhere ships riscv64, and Google''s standalone libtensorflow C-library tarball has none at the pinned version -- storage.googleapis.com/tensorflow/libtensorflow/libtensorflow-cpu-linux-riscv64-2.15.0.tar.gz -> 404 against the working ...-x86_64-2.15.0.tar.gz -> 206, and versions/2.16.1/libtensorflow-cpu-linux-{x86_64,riscv64}.tar.gz -> 206/404. MATRIX HAS ZERO VALID ROW EVEN WITH THAT WHEEL: tensorflow 2.13.1/2.14.0/2.15.0 publish cp38-cp311 only (checked on PyPI), while this repo builds cp312/cp313/cp314/cp314t -- empty intersection, so no default matrix row could ever resolve a compatible TF, and building cp39-cp311 instead would diverge from the repo matrix while still needing the missing wheel. ci_scripts/check_riscv64_deps.py and riscv64_resolve.py both report cp312/cp313/cp314/cp314t UNRESOLVABLE, but only because the package itself is absent from the registry, so that run is not the evidence -- the PyPI interpreter-tag intersection is. NO UPSTREAM RECIPE TO NARROW (goal 2): tensorflow itself ships manylinux_2_17_aarch64 wheels for 2.13-2.15, yet tensorflow-addons published ZERO aarch64 (or any non-x86) Linux wheels across all 35 releases -- macOS arm64 only -- and no sdist, ever. .github/workflows/release.yml builds ubuntu-20.04 + macos-12 only; the Linux leg is tools/docker/build_wheel.Dockerfile, FROM tensorflow/build:2.15-python$PY_VERSION with TF_NEED_CUDA=1 and a hardcoded --crosstool_top=@ubuntu20.04-gcc9_manylinux2014-cuda11.8-cudnn8.6-tensorrt8.4_config_cuda//crosstool:toolchain, then auditwheel repair --plat manylinux2014_x86_64; tools/install_deps/install_bazelisk.sh hardcodes bazelisk-linux-amd64 and there is no .bazelversion (404), so the bazel version comes from that x86_64 image. CUDA IS NOT THE BLOCKER, checked as asked: all cuda_srcs in custom_op_library sit behind if_cuda / if_cuda_is_configured and configure.py only calls configure_cuda() when TF_NEED_CUDA=1, so CPU-only is a first-class upstream configuration -- it just needs the same CPU link against libtensorflow_framework.so. Separately, configure.py''s Linux branch writes build --copt=-mavx for every machine that is not ppc64le/arm/aarch64 (there is no is_linux_riscv64 predicate beside is_linux_x86_64 / _s390x / _ppc64le / _arm / _aarch64), so riscv64 falls into the x86 default and a riscv64 GCC rejects the flag -- the same negated-allowlist shape as gotcha 426''s release_cpu_linux finding in TF core, and a patch this port would need if it were ever unblocked. UNTESTABLE EVEN IF BUILT: setup.py takes install_requires from requirements.txt (typeguard>=2.7,<3.0.0, packaging) and keeps tensorflow in extras_require only, so a wheel would install on riscv64 -- but tensorflow_addons/__init__.py imports utils/ensure_tf_install.py, which does import tensorflow, so there is no importable smoke test and upstream''s own test_wheel_in_fresh_environment stage (python -c import tensorflow_addons as tfa; print(tfa.register_all())) cannot run. Note for anyone re-reading the wheel: the top-level _foo.cpython-310-x86_64-linux-gnu.so is not a packaging accident -- setup.py get_ext_modules() returns [Extension(_foo, [stub.cc])] on Linux purely so BinaryDistribution.has_ext_modules() forces a platform wheel tag. REJECTED ALTERNATIVE: a Python-ops-only wheel (TF_ADDONS_PY_OPS=1 / tfa.options.disable_custom_kernel()) would ship an artifact upstream never publishes, still carry a platform tag via the _foo stub, and still be unimportable without a riscv64 tensorflow -- divergence with no gap closed (gotcha 50). NOT ATTEMPTED and nothing to revert: no worktree, branch, workflow, docs/packages yaml, patches or PR. The verdict was fully resolvable read-only from the PyPI JSON (all 35 releases), one GitHub repo-metadata read, raw.githubusercontent.com reads of setup.py / configure.py / WORKSPACE / tf_configure.bzl / BUILD.tpl / tensorflow_addons.bzl / release.yml / build_wheel.Dockerfile at tag v0.23.0, and readelf on the released cp310 x86_64 wheel. REVISIT only if a riscv64 tensorflow 2.15.x specifically ever exists here for cp39-cp311 -- which, given the archived upstream and this repo''s cp312+ matrix, means effectively never.'
- pkg: arch
  version: 8.0.0
  home: https://github.com/bashtage/arch
  repo: https://github.com/bashtage/arch
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1680
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-simplification
  version: 0.2.0
  home: https://github.com/pyvista/fast-simplification
  repo: https://github.com/pyvista/fast-simplification
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1216
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gilknocker
  version: 0.4.2
  home: https://github.com/milesgranger/gilknocker
  repo: https://github.com/milesgranger/gilknocker
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1205
  notes: '65 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev. cp312/cp313/cp314 only: pyo3 pinned to a pre-PEP-703 git rev, no cp314t upstream either.'
- pkg: curated-tokenizers
  version: 2.0.1
  home: https://github.com/explosion/curated-tokenizers
  repo: https://github.com/explosion/curated-tokenizers
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1212
  notes: 'PR #1212 green (4 wheels built cp312/cp313/cp314/cp314t, publish dry-run OK), awaiting review. Compiles vendored sentencepiece C++ core plus wordpiece/bbpe Cython extensions; matches upstream matrix, no patches needed. Validated locally via a manylinux_2_39_aarch64 cibuildwheel rehearsal (29 passed, 1 skipped) before pushing; wheel ships only the MIT LICENSE for the vendored Apache-2.0/BSD-3-Clause sentencepiece/absl/protobuf-lite sources, matching upstream''s own wheel; 28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sglang-kernel
  version: 0.4.6.post1
  home: https://github.com/sgl-project/sglang/tree/main/python/sglang/kernels/aot
  repo: https://github.com/sgl-project/sglang/tree/main/python/sglang/kernels/aot
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310), both cp310-abi3 manylinux2014 (x86_64 + aarch64), no sdist in any release; no riscv64 on PyPI or pypi.riseproject.dev. Not feasible: NVIDIA-CUDA-only AOT kernel library -- the build dies before it reads a single option, and the shipped wheel needs libcuda.so.1 plus a CUDA-enabled torch. Same disposition and class as the parked sglang, nccl4py, cuda-tile, onnxruntime-gpu, humming-kernels and torch-memory-saver entries (gotchas 41/284/386/387/411/452/459/471); no new gotcha number needed. PROVENANCE, since the distribution has no git tag of its own (gotcha 3 shape): the kernel version is a separate axis from sglang''s, so python/sglang/kernels/aot/python/sgl_kernel/version.py was probed across sglang tags over raw.githubusercontent.com -- it reads 0.4.6.post1 at tag v0.5.18 (exactly this entry''s version) and 0.4.7 at v0.5.20, while every v0.5.0..v0.5.9 tag 404s because the directory was still called sgl-kernel/ then. All evidence below is read off the v0.5.18 tree (sparse shallow checkout) and off the real 0.4.6.post1 wheels, not inferred from the name. Zero sdists in all 12 releases; every release is exactly 2 files, cp310-abi3 manylinux2014_x86_64 + manylinux2014_aarch64. (1) BUILD WALL, unconditional and reproduced locally on this CUDA-less x86_64 host. CMakeLists.txt line 2 is project(sgl-kernel LANGUAGES CXX CUDA), followed by enable_language(CUDA), find_package(CUDAToolkit REQUIRED) and find_package(Torch REQUIRED) -- no if(), no backend selector, i.e. the opposite of gotcha 411''s bitsandbytes, whose enable_language(CUDA) sits behind COMPUTE_BACKEND=cpu. cmake -S . -B build -> "CMake Error at Modules/Internal/CMakeCUDAFindToolkit.cmake:104 (message): Failed to find nvcc. Compiler requires the CUDA toolkit." with the call stack ending at CMakeLists.txt:2 (project). 47 of the 49 entries in the single SOURCES list are .cu (49 .cu and 32 .cuh in csrc/ overall); FetchContent pulls NVIDIA CUTLASS, flashinfer and sgl-attn at configure time; every target is compiled with -gencode arch=compute_{80,86,89,90,90a,100a|100f,101a,110a,120a,121a} and linked target_link_libraries(... ${TORCH_LIBRARIES} c10 cuda cublas cublasLt). The whole arch dispatch is x86_64 vs aarch64 (ENABLE_BELOW_SM90 and DEFAULT_SGL_KERNEL_ENABLE_FA3 off on aarch64); grep -ri riscv over the entire aot tree returns zero hits. (2) UPSTREAM RELEASE RECIPE IS A GPU-VENDOR MATRIX (gotcha 471''s shape, sharpening gotcha 418): build.sh takes CUDA_VERSION as a mandatory positional argument and its arch switch is aarch64 -> pytorch/manylinuxaarch64-builder, else pytorch/manylinux2_28-builder, always tagged :cuda${CUDA_VERSION} (Dockerfile: ARG BASE_IMG=pytorch/manylinux2_28-builder, ARG CUDA_VERSION=13.0, ENV FLASHINFER_CUDA_ARCH_LIST="8.0 8.9 9.0a 10.0a 12.0a", CPLUS_INCLUDE_PATH=/usr/local/cuda/include/cccl). aarch64 exists only because NVIDIA ships an sbsa toolkit and PyTorch a CUDA aarch64 builder image. The publisher, .github/workflows/release-whl-kernel.yml (push on that version.py), offers targets cu129, cu130, rocm700, rocm720, musa43 -- five accelerator toolkits and no CPU member, and the string cpu does not appear in it. (3) IMPORT WALL, independent of the build. readelf -d on sgl_kernel/spatial_ops.abi3.so, extracted from the released 0.4.6.post1 aarch64 wheel over HTTP ranges (ci_scripts/wheel_contents.py) gives DT_NEEDED libcuda.so.1, libcudart.so.13, libnvrtc.so.13, libtorch_cuda.so, libc10_cuda.so, with undefined cuGreenCtxCreate, cuDevSmResourceSplitByCount, cuGetProcAddress, cuStreamCreate, cudaRegisterFatBinary; the other four extensions are sm90/common_ops.abi3.so, sm100/common_ops.abi3.so, flashmla_ops, infllm_ops. sgl_kernel/__init__.py takes the Metal branch only on darwin/arm64 and otherwise unconditionally imports torch, then _load_architecture_specific_ops(), which picks sm90 when the CUDA compute capability is 90 and sm100 otherwise -- including its explicit "CPU/No GPU detected" case, so even the no-GPU path dlopens a CUDA extension -- then _preload_cuda_library() for libcudart. pypi.riseproject.dev serves torch 2.13.0+cpu / 2.14.0+cpu for riscv64 (CPU-only, no libtorch_cuda.so) and 0.4.7 hard-pins torch==2.13.0, so gotcha 40 would fail even for a hypothetical artifact. (4) THE ONE APPARENT ESCAPE HATCH, CHECKED AND REJECTED. The directory carries pyproject_cpu.toml beside pyproject_rocm.toml and pyproject_musa.toml (all four listed in the Makefile''s FILES_TO_UPDATE version-bump list, which is where such siblings are found). It is a distribution RENAME, not a build flag -- gotcha 387''s cupy rename_project mechanism, here in-tree: name = "sglang-kernel-cpu", cmake.source-dir = "csrc/cpu", torch==2.12.0. So it cannot be published under this entry''s name (gotcha 50/79 sibling divergence), and pypi.org/pypi/sglang-kernel-cpu/json -> 404, i.e. that distribution is not published on any arch, so there is no riscv64 gap to retarget this entry to and it was not retargeted. It would not build on riscv64 either: csrc/cpu/CMakeLists.txt dispatches on x86_64|AMD64, aarch64|arm64 and ppc64le|ppc64 only, and for an unrecognised processor MY_ARCH_DIR stays empty while the x86-only source list (gemm_int4, gemm_int8, moe, moe_fp8, moe_int4, moe_int8, qkv_proj, mamba/conv) is removed ONLY for aarch64|arm64 -- so riscv64 would compile exactly the AMX/AVX-512 files it cannot; vec.h and vec_pack.h plus ~15 further non-arch-dir sources include <immintrin.h> behind #if defined(__AVX512F__) && defined(__AVX512BF16__) && defined(__AMX_BF16__) or CPU_CAPABILITY_AVX512 with no third arm; the non-x86 branch compiles with -march=native and libnuma is a FATAL_ERROR if missing. Untestable too (gotcha 6): upstream''s own CPU smoke test, .github/workflows/pr-test-xeon.yml, asserts torch.cpu._is_amx_tile_supported(). (5) THE WHEEL-SIZE SPLIT IS NOT GOTCHA 411''S "SMALL END" TELL, which is the trap worth recording here: 377 MB x86_64 vs 30 MB aarch64 looks like bitsandbytes'' 43 MB/123 KB CPU-backend signal, but the entire difference is one file, sgl_kernel/flash_ops.abi3.so, 1.12 GB uncompressed of FA3 gencodes that DEFAULT_SGL_KERNEL_ENABLE_FA3 switches off on aarch64. Both wheels are equally CUDA; the small end is a narrowed gencode list, not a CPU backend. (6) VENDOR AXIS HAS NO riscv64 AT ALL: developer.download.nvidia.com/compute/cuda/redist/redistrib_13.0.0/13.2.0/13.4.2.json list only linux-x86_64, linux-sbsa, linux-all and windows-x86_64/arm64 -- no riscv64, hence no nvcc and no libcuda.so.1 for the arch; ROCm publishes binary-amd64 only and MUSA is Moore Threads'' x86_64 stack. VERSION (gotcha 388): the queue snapshot 0.4.6.post1 is kept rather than bumped, since the evidence above is pinned to the tag that carries exactly it; latest is 0.4.7 (2026-09-13) and is identically blocked -- main''s pyproject.toml still carries classifier Environment :: GPU :: NVIDIA CUDA and dependencies ["torch==2.13.0"], its CMakeLists.txt line 2 is still project(... CUDA), and it still ships 2 cp310-abi3 wheels and no sdist. Registry checked: pypi.riseproject.dev/simple/sglang-kernel/ -> 404 (control: .../torch/ -> 200, CPU-only riscv64 wheels). No workflow, docs/packages yaml, patches, branch or PR created, and nothing to rehearse locally since cmake configure cannot start. Note for whoever takes the sibling sgl-kernel queue entry: that is the predecessor distribution name of this same tree (its PyPI homepage points at sglang/tree/main/sgl-kernel, a directory that 404s at both v0.5.18 and main because it was moved to python/sglang/kernels/aot), so it hits the identical wall. Revisit only if NVIDIA ships a riscv64 CUDA toolkit and driver and a CUDA-enabled riscv64 torch exists.'
- pkg: libvalkey
  version: 4.1.0
  home: https://github.com/valkey-io/libvalkey-py
  repo: https://github.com/valkey-io/libvalkey-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1687
  notes: '56 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. hiredis-style setup.py + vendored submodule (vendor/libvalkey, BSD-3-Clause); carries a LICENSE.libvalkey patch mirroring the hiredis one since setuptools only globs licence files at the project root.'
- pkg: phik
  version: 0.12.5
  home: https://phik.readthedocs.io
  repo: https://github.com/KaveIO/PhiK
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1215
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Upstream located: KaveIO/PhiK.'
- pkg: lazrs
  version: 0.8.2
  home: https://github.com/laz-rs/laz-rs-python
  repo: https://github.com/laz-rs/laz-rs-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1112
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; located upstream at laz-rs/laz-rs-python (verified via sdist sha256 match against the PyPI release). Small PyO3/maturin extension (pyo3 0.29, no abi3 feature -> per-interpreter wheels, matches build-fastuuid.yml shape); Cargo.toml pins laz=0.12.2, whose caret range excludes the newer laz 0.13.0 release, so a freshly-built sdist resolves the same dependency tree as upstream''s released one (only patch-level bumps: pyo3 0.29.0->0.29.2, crossbeam-*, either) - verified locally by diffing Cargo.lock from the extracted PyPI sdist against one built fresh from the git tag, and by building a wheel from each and running the same smoke test. cargo metadata --filter-platform riscv64gc-unknown-linux-gnu shows a 24-crate tree with no target_arch guards anywhere (checked libc/crossbeam-epoch/pyo3-ffi/laz sources directly), so no arch-specific build risk. Upstream ships no test suite at all (noxfile.py only does `import lazrs`); CIBW_TEST_COMMAND instead builds a real point-format-0 LazVlr via LazVlr.new_for_compression(0, 0) (upstream''s own laz crate unit test exercises the same call) and round-trips compress/decompress through io.BytesIO, asserting the data comes back unchanged - validated locally end to end (compressed 1655 of 2000 bytes, round trip exact) before pushing. Matrix cp312/cp313/cp314, no cp314t (upstream''s own build.yml doesn''t build free-threaded wheels either). Worktree at .claude/worktrees/lazrs, branch lazrs. PR #1112 open, CI triggered on push; not yet confirmed green. Merged (#1112). Issue #1133 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: zipcodes
  version: 3.0.0
  home: https://github.com/seanpianka/zipcodes
  repo: https://github.com/seanpianka/zipcodes
  status: published
  pr: 1111
  notes: '3 Linux wheels upstream (abi: cp39-abi3); no riscv64 on PyPI or pypi.riseproject.dev. Not pure Python despite the small dataset-lookup shape - PyPI sdist since 2.0.1 is a pyo3/maturin Cargo workspace (crates/zipcodes + crates/zipcodes-py) embedding the zip database at compile time via bzip2, replacing the 1.x pure-Python implementation; confirmed by inspecting the real sdist tarball (Cargo.toml/Cargo.lock present, no setup.py). bzip2 crate resolves to libbz2-rs-sys (pure Rust reimplementation), so no system libbz2 or C compiler needed - a clean maturin build with no native-dep gotchas. Verified locally end-to-end on macOS/aarch64 as a proxy (built via maturin, installed the wheel, ran the real tests/ suite from a checkout of tag v3.0.0: 36 passed). Matrix built at cp39 (the abi3 floor the pyo3 abi3-py39 feature claims) through cp314 per gotcha 96, not this repo''s usual cp312 floor; upstream ships no free-threaded wheel so there is no cp314t job (css-inline precedent). musllinux dropped (no riscv64 rust musl toolchain). PR #1111, branch zipcodes, worktree at .claude/worktrees/zipcodes. Merged (#1111). Issue #1134 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: memfabric-hybrid
  version: 1.2.0
  home: https://gitcode.com/Ascend/memfabric_hybrid
  repo: https://gitcode.com/Ascend/memfabric_hybrid
  status: parked
  pr: null
  notes: 'Parked: accelerator-vendor-only (Huawei Ascend NPU / CANN), gotcha 480 (feasibility-and-triage.md) -- the Ascend analogue of the CUDA family at gotcha 452/471. Confirmed against the real source at tag v1.2.0, not inferred from the org name: gitcode.com is an ordinary git host and IS reachable from this environment (git clone --depth 1 --branch release/1.2 https://gitcode.com/Ascend/memfabric_hybrid.git and git fetch --depth 1 origin tag v1.2.0 both succeed through the agent proxy; note release/1.2''s VERSION already reads 1.2.1, so the tag is what was read), plus the released cp311 manylinux_2_27_x86_64 wheel. Upstream publishes 8 wheels for 1.2.0 and 76 files across all 14 releases, every one manylinux aarch64 or x86_64, and no sdist in any release. The port is NOT blocked by a build-time CANN requirement, which is what the triage hypothesis expected: the core C++ (hybm/smem/acc_offload) reaches CANN only through dlopen wrappers in src/hybm/csrc/under_api/dl_hybrid_api.h, readelf -d on all five shipped .so shows nothing but libstdc++/libm/libgcc_s/libc/librt/libpthread/libdl plus each other, and libascendcl.so, libascend_hal.so, libhccl.so, libruntime.so and libtsdclient.so appear only as dlopen strings inside libmf_hybm_core.so; the AscendC kernel sources (src/acc_offload/csrc/operators, src/hybm/csrc/copy_extend) are explicitly filtered out of the main CMake build and compiled at install time. Three separate walls sit behind that. (1) The published artifact is an NPU artifact and would be a dead stub on riscv64: the wheel ships _ops/hybm_kernel/ plus libcann_hybm_kernel.ini/.json and the .hybm_aicpu_provision_wheel_only marker, and memfabric_hybrid/__init__.py unconditionally calls _provision.provision(), which builds and installs an AICPU kernel into CANN''s opp/vendors/cust/op_impl/aicpu using ${ASCEND_HOME_PATH}/toolkit/toolchain/hcc/bin/aarch64-target-linux-gnu-g++ -- an aarch64-only cross compiler inside a toolkit whose own layout is ${ASCEND_HOME_PATH}/aarch64-linux/{include,lib64}; provision() silently returns when ASCEND_HOME_PATH is unset, so the wheel would import on a riscv64 host and have no device, no driver and no transport. Upstream''s README states the axis outright -- hardware Atlas 200T A2 Box16 / Atlas 800I,800T A2,A3 / Atlas 900 A3 SuperPoD, platform line ''aarch64/x86'', CANN 8.1.RC1 or later, Ascend HDK npu-driver + npu-firmware -- and grep -ri riscv over the entire tree returns zero hits. (2) The vendor-agnostic build mode exists but is a different distribution version, which is the decisive finding: cmake/config_xpu.cmake really does accept XPU_TYPE=NONE (-DNO_XPU) with live #if defined(NO_XPU) code paths (so this is not a fake flag), but src/smem/python/memfabric_hybrid/setup.py does current_version += "+cpu" for XPU_TYPE=NONE (and "+gpu" for GPU), and no +cpu or +gpu local version has ever been published in any of the 14 releases -- all 76 files are plain-versioned NPU builds. A CPU-only build therefore cannot be published as memfabric-hybrid 1.2.0 at all; it would be a distinct version of a configuration upstream ships nowhere (gotcha 452''s ''does upstream ship this flag?'' test, at the packaging level). (3) Even that CPU mode is Huawei-interconnect-only: the only host transport is HcomTransportManager over libhcom.so, FetchContent''d from https://atomgit.com/openeuler/ubs-comm @ br_BeiMing_MF_Poc (also reachable; cloned and read), whose src/hcom/umq/CMakeLists.txt is a two-arch switch rather than an allowlist -- aarch64 gets -march=armv8-a+crc -DUB_ARCH_ARM64 and everything else, riscv64 included, gets -msse4.2 -DUB_ARCH_X86_64, so a riscv64 build dies on the first compiler flag -- while its Bazel copts.bzl selects on @platforms//cpu:x86_64 and @platforms//cpu:aarch64 with no default, hardware CRC is aarch64 crc32cx inline asm against SSE4.2, urpc_get_cpu_cycles is rdtsc/cntvct_el0, and the NO_XPU key path carries URMA_EID_LENGTH with urma headers required from Huawei''s UMDK (/usr/include/ub/umdk/urma). Revisit only if Huawei ships CANN and Ascend HDK for riscv64 and ubs-comm grows a riscv64 arm. The same verdict applies unchanged to the sibling queue entries memfabric-zbal and memcache-hybrid (gitcode.com/Ascend/memcache), and is the likely shape for triton-ascend and torch-npu; those are left untouched for whoever claims them. No worktree, branch, workflow, docs/packages entry or patch was created; the scratch trees under .git/pw-scratch/memfabric-hybrid/ were removed.'
- pkg: jsonschema2md
  version: 1.7.0
  home: https://github.com/sbrunner/jsonschema2md
  repo: https://github.com/sbrunner/jsonschema2md
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 481, plus gotcha 27''s watchdog clause and gotcha 383''s sdist check): nothing is compiled on any platform and riscv64 pip install already works today. The single upstream wheel jsonschema2md-1.7.0-cp313-cp313-manylinux_2_39_x86_64.whl has 11 entries -- jsonschema2md/__init__.py, 4 locales/*/LC_MESSAGES/messages.po, locales/README.md and dist-info -- zero .so and no PyInit_*, despite Root-Is-Purelib: false. The cpXY-cpXY-manylinux tag is fabricated by poetry-core, which marks any project declaring [tool.poetry.build] script as non-pure; jsonschema2md''s scripts/build.py only runs babel''s CompileCatalog over jsonschema2md/locales/*, i.e. it compiles gettext .po into arch-independent .mo. Dated from the release history (ci_scripts/queue_triage.py): 0.1.0-1.5.2 are all py3-none-any and the tag flips at exactly 1.6.0, the release that added i18n -- 1.5.2''s pyproject.toml has no [tool.poetry.build] and no babel build requirement. The tag''s halves follow the publishing runner, not the content: building the real 1.7.0 sdist locally (pip wheel <sdist> --no-deps, no compiler, ~1s) yields jsonschema2md-1.7.0-cp311-cp311-linux_x86_64.whl on a 3.11 x86_64 host. Upstream''s own .github/workflows/main.yaml at tag 1.7.0 publishes with a single tag-publish step on ubuntu-24.04 -- no cibuildwheel, no arch matrix, no macOS/Windows wheels at all -- so every non-x86_64-Linux user already installs from the sdist, and there is no upstream wheel CI to mirror and narrow to riscv64 (structural goal 2 has nothing to start from). An sdist exists for every release, and the whole install path resolves to riscv64 wheels: runtime deps PyYAML<7,>=6 / markdown<4,>=3 / babel<3,>=2 and the poetry-core + poetry-dynamic-versioning[plugin] + babel build closure all resolved ok for cp312 and cp313 with ci_scripts/check_riscv64_deps.py (pyyaml-6.0.3-cp3XX-manylinux_2_31_riscv64.manylinux_2_39_riscv64.whl comes from our own registry; queue_triage.py''s ''not on RISE'' for PyYAML is a name-casing false negative, recorded as gotcha 482). So a manylinux_2_39_riscv64 wheel would carry byte-identical content to the x86_64 one -- a packaging convenience, not a port. It would in fact be worse than nothing to depend on: the published 1.7.0 wheel ships the .po sources and none of the .mo files its build script compiles (poetry-core 2.1.3), while a from-sdist install (poetry-core 2.5.0) does include them, and __init__.py''s get_locales() globs locales/*/LC_MESSAGES/messages.mo -- so the released wheel silently has no translations where the riscv64 sdist install has them. Upstream-side fix if anyone wants one: make the wheel py3-none-any again (the build script''s output is arch- and interpreter-independent), which would also erase this queue entry''s premise. Verified read-only plus one local sdist build in .git/pw-scratch/jsonschema2md (removed): PyPI JSON for all 18 releases, both 1.7.0 artifacts unzipped, the 1.5.2 sdist''s pyproject.toml, upstream main.yaml over raw.githubusercontent.com, and the two dependency checkers. No worktree left, no branch, no workflow, no docs/packages entry, no PR.'
- pkg: faust-cchardet
  version: 3.2.0
  home: https://github.com/faust-streaming/cChardet
  repo: https://github.com/faust-streaming/cChardet
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1681
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: moocore
  version: 0.3.2
  home: https://multi-objective.github.io/moocore/python/
  repo: https://github.com/multi-objective/moocore/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1206
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymatgen-core
  version: 2026.8.13
  home: https://pymatgen.org
  repo: https://github.com/materialsproject/pymatgen-core
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2150
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI
    or pypi.riseproject.dev. The compiled half of pymatgen, split out of the main distribution:
    three Cython extensions over the NumPy C API (pymatgen.optimization.neighbors,
    pymatgen.optimization.fast_parser, pymatgen.util.coord_cython) built by plain setuptools,
    plus the Python core they serve. No SIMD intrinsics, no BLAS/LAPACK linkage of its
    own, no vendored native library, no GPU dependency - spglib and scipy are ordinary
    runtime dependencies (both already on our registry for cp312-cp314), not something
    this package links. Build-from-checkout with upstream''s own cibuildwheel config
    (release.yml), manylinux riscv64 image substituted; upstream already skips musllinux
    itself. Build requirements (cython, numpy, setuptools, setuptools-scm) all resolve
    to riscv64 wheels on our registry. The checkout is sparse (non-cone, /* plus !/test-files)
    to drop upstream''s 514 MB test-files tree of reference calculation outputs, which
    neither the build nor the selected tests read - that also switches actions/checkout
    to a blob:none clone, so the checkout step takes 9s (gotcha 489). setuptools-scm
    gets the version through SETUPTOOLS_SCM_PRETEND_VERSION_FOR_PYMATGEN_CORE, since
    cibuildwheel gives the container no git. Matrix is cp312/cp313/cp314 - cp314t dropped
    because orjson and pandas are hard runtime requirements and publish no free-threaded
    wheel on any platform (checked orjson 3.12.0 and pandas 3.0.6 on PyPI directly,
    not just our registry), and upstream ships no cp314t wheel of pymatgen-core either,
    so gotcha 149''s build-and-skip-tests shape does not apply. Tests run the three
    extensions'' own suites plus the lattice/site/composition/periodic-table/operation
    tests that reach them through pymatgen.util.coord; the rest of upstream''s suite
    reads test-files, and the split was found by grepping for TEST_FILES_DIR (gotcha
    489). Rehearsed on x86_64 before the first CI push (gotcha 52): wheel built from
    the tag with the pretend-version env var on cp311 and cp314, installed, tests staged
    exactly as CIBW_TEST_SOURCES would - 291 passed on each. First CI run built all
    three wheels but failed 4/291 tests on every leg - PIP_ONLY_BINARY=:all: in CIBW_TEST_ENVIRONMENT
    pinned bibtexparser to 1.3.0 (1.4.0-1.4.4 are sdist-only), and that release fails
    pymatgen''s own is_valid_bibtex; reproduced and fixed off-target, replacing :all:
    with the twelve genuinely-compiled distributions our registry supplies riscv64 wheels
    for (new gotcha 488). Second run green: cp312/cp313/cp314 each 291 passed, wheel-contents
    check (3 .so + LICENSE) passed, publish job dry-ran clean (3 wheels, "Not on main
    branch - no branch, commit, or PR will be created"). PR #2150 left as a draft, not
    merged. Merged (#2150). Push-triggered build+publish on main run 35573514370 succeeded. 404 on pypi.riseproject.dev/simple/pymatgen-core/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: unicorn
  version: 2.1.4
  home: http://www.unicorn-engine.org
  repo: https://github.com/unicorn-engine/unicorn
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1685
  notes: '12 Linux wheels upstream (abi: cp37,py2); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: stream-inflate
  version: 0.0.43
  home: https://github.com/michalc/stream-inflate
  repo: https://github.com/michalc/stream-inflate
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1115
  notes: '54 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Not pure-Python despite the "pure Python" README claim: setup.py cythonizes stream_inflate.py into a real per-interpreter C extension (confirmed by unzipping a released wheel: both stream_inflate.cpython-3XX-*.so and stream_inflate.py ship together, .so wins on import), so a genuine riscv64 gap exists. Build-from-checkout, cp312/cp313/cp314/cp314t manylinux only - musllinux dropped, see gotcha 317 (musl malloc vs pymalloc on an allocation-heavy fuzz suite; not fixable in the workflow). Cython (a build-system.requires dep, unconditional) has no riscv64 wheel on public PyPI but is already published on our own registry for cp312/cp313/cp314 manylinux, so CIBW_ENVIRONMENT routes PIP_EXTRA_INDEX_URL there. Locally validated per gotcha 52 before the first CI push: installed the real PyPI wheel, staged test_stream_inflate.py + fixtures/ into an empty dir exactly as CIBW_TEST_SOURCES would, ran the real upstream suite against the installed .so - 1956 passed. All 4 manylinux riscv64 CI jobs passed (full real test suite, 1956 cases each) and the publish dry-run was clean. Merged as #1115. Issue #1243 filed and linked, project item added with status Available in RISE PyPI. Publish run 34097648004 on main succeeded: GitHub Release stream-inflate-v0.0.43-20260907095404 created with all 4 wheels, and auto-opened docs PR #1323 (docs/packages/stream-inflate.yaml) awaiting maintainer merge - pypi.riseproject.dev/simple/stream-inflate/ still 404s until that lands.'
- pkg: pytrec-eval-terrier
  version: 0.5.10
  home: https://github.com/terrierteam/pytrec_eval
  repo: https://github.com/terrierteam/pytrec_eval
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1684
  notes: 'PR #1684 green (4 wheels built cp312-cp314t, 5 tests passed each, publish dry-run OK). Build-from-checkout: checks out terrierteam/pytrec_eval with the trec_eval submodule (pinned v9.0.8) so cibuildwheel compiles it directly, avoiding the network fetch setup.py otherwise does for the same sources. A patch adds LICENSE.trec_eval: the vendored v9.0.8 .c files carry a stale 2008 "research, non-commercial" per-file header, but usnistgov/trec_eval''s current repo-wide LICENSE.md (2024) explicitly relicenses that same pre-v10 code as permissively-redistributable NIST public-service software, and the wheel now ships that current text (the released sdist otherwise carries only the wrapper''s MIT LICENSE). cp314t passes with the expected GIL-re-enabled warning (extension uses single-phase init, no Py_mod_gil slot). musllinux skipped, matching upstream (no musllinux wheels published either). Awaiting review.'
- pkg: stream-unzip
  version: 0.0.101
  home: https://stream-unzip.docs.trade.gov.uk/
  repo: https://github.com/uktrade/stream-unzip
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1214
  notes: '54 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Not pure-Python despite the per-CPython wheel tags: pyproject.toml build-backend is maturin, and the sdist carries Cargo.toml + src/lib.rs; unzipping a released wheel confirms a real stream_unzip._zipcrypto.cpython-3XX-*.so PyO3 extension (ZipCrypto decryption) ships alongside the pure-Python stream_unzip/__init__.py. Worktree at .claude/worktrees/stream-unzip, branch stream-unzip. Mirrors upstream build-source-package-and-wheels.yml: maturin sdist with the 0.0.0.dev0 version placeholder sed-substituted (same as build-fastuuid.yml), then cibuildwheel cp312/cp313/cp314 (no abi3, no cp314t - upstream does not build one yet) with rustup installed in-container; musllinux skipped, rustup ships no riscv64 musl toolchain. Runtime deps: pycryptodome already has a riscv64 wheel on our registry; stream-inflate does not yet (its own port is PR #1115, in-review) but its build deps (setuptools + Cython>=3.0.0, and Cython is on our registry for cp312/cp313/cp314) let pip build it from its public sdist during the cibuildwheel test-install step, so this port does not need to block on #1115 merging first. PR #1214 open, CI running.'
- pkg: cmeel-urdfdom
  version: 6.0.0
  home: https://github.com/cmake-wheel/cmeel-urdfdom
  repo: https://github.com/cmake-wheel/cmeel-urdfdom
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 0" is PEP 427''s optional build number, not an ABI tag (gotcha 470):
    the real filenames are cmeel_urdfdom-6.0.0-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl,
    i.e. ONE interpreter-independent wheel per platform -- [tool.cmeel] has-sitelib =
    false, so the wheel holds no Python extension at all, just cmeel.prefix/lib/liburdfdom_{model,world,sensor}.so.6
    plus headers and the check_urdf/urdf_to_graphviz/urdf_mem_test binaries (gotcha
    35 shape, not gotcha 27). PORTABILITY IS FINE: urdfdom is ~10 .cpp of XML parsing
    over tinyxml2; the sdist vendors the whole urdfdom tree (git submodule archived
    in) plus a 2-hunk cmeel.patch that only sets INSTALL_RPATH to $ORIGIN/@loader_path;
    no -march/SSE/NEON/RVV string, no CUDA, no assembly, cmake_minimum_required(VERSION
    3.10) so CMake 4 is fine. Built the whole chain locally on x86 from the real sdist
    in about a minute (gotcha 9/125 rehearsal): cmeel_urdfdom-6.0.0-0-py3-none-manylinux_2_39_x86_64.whl.
    BLOCKED ON EXACTLY TWO SIBLINGS, both already their own queued .queue.yml entries
    and both leaves of gotcha 470''s pin closure (deps = cmeel only): cmeel-console-bridge
    1.0.2.3 and cmeel-tinyxml2 11.0.0. Evidence rather than inference: (a) pyproject.toml
    names both twice -- [project] dependencies = ["cmeel-console-bridge", "cmeel-tinyxml2"]
    and [build-system] requires = the same two plus cmeel-urdfdom-headers and cmeel[build]
    -- so they are link targets and header sources, not just runtime metadata; (b)
    readelf -d on the released 6.0.0 aarch64 wheel shows all three liburdfdom_*.so.6
    NEEDing libconsole_bridge.so.1.0 and libtinyxml2.so.11 with RUNPATH $ORIGIN,
    i.e. cmeel''s shared-prefix contract with nothing bundled; (c) https://pypi.riseproject.dev/simple/
    has no cmeel-console-bridge and no cmeel-tinyxml2 at all (cmeel-boost is the
    only cmeel package on the registry so far, cp312/313/314), and neither publishes
    a riscv64 wheel on public PyPI; ci_scripts/riscv64_resolve.py ''cmeel-urdfdom==6.0.0''
    reports UNRESOLVABLE on cp312/cp313/cp314/cp314t. Nothing else in the closure
    is missing: cmeel 0.61.0 and cmeel-urdfdom-headers 3.0.0 are py3-none-any on
    public PyPI, and cmake (cmeel[build]''s only heavy build input) has riscv64 wheels
    on public PyPI at 4.4.3. WHY GOTCHA 125 DOES NOT RESCUE THIS (new gotcha 483):
    both sibling sdists vendor their upstream source and require only cmeel[build],
    which is exactly gotcha 125''s "cheap from sdist" profile -- but running it disproves
    it. pip wheel cmeel_tinyxml2-11.0.0.tar.gz --no-deps builds clean; pip wheel
    cmeel_console_bridge-1.0.2.3.tar.gz --no-deps FAILS with "CMake Error at CMakeLists.txt:1
    (cmake_minimum_required): Compatibility with CMake < 3.5 has been removed from
    CMake", because console_bridge 1.0.2 declares cmake_minimum_required(VERSION
    3.0.2) and cmeel[build] resolves cmake >= 4 (gotchas 207/257/358). CMAKE_POLICY_VERSION_MINIMUM=3.5
    in the environment does fix it (verified: that is how the full local chain build
    above succeeded), so a build-cmeel-urdfdom.yml with it in CIBW_ENVIRONMENT could
    be made green -- but the variable does not ship with the wheel, so pip install
    cmeel-urdfdom on riscv64 would still fail in the user''s own cmeel-console-bridge
    sdist build, and publishing it would put an entry on the index that the index
    cannot install. The fix belongs in cmeel-console-bridge''s own port (which needs
    -DCMAKE_POLICY_VERSION_MINIMUM=3.5 or an equivalent patch), and that package is
    outside this port''s commit scope anyway. FOR WHOEVER TAKES THE SIBLINGS: both
    are single-.cpp builds of seconds, cmeel-tinyxml2 needs no workaround, and build-cmeel-boost.yml
    is the working template -- cibuildwheel with only: cp312-manylinux_riscv64, CIBW_REPAIR_WHEEL_COMMAND:
    "" (cmeel sets INSTALL_RPATH itself), CIBW_ENVIRONMENT carrying PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/,
    and the upstream tag pattern v<version>.c<n> (cmeel-urdfdom''s is v6.0.0.c0, confirmed
    via git ls-remote). Because these wheels are py3-none-<plat>, one cibuildwheel
    job covers every interpreter -- there is no per-interpreter ceiling to inherit
    from cmeel-boost, unlike pin. Landing these two unblocks cmeel-urdfdom, which
    is itself one of pin''s 11. No branch/worktree or PR opened, no CI cycle burned;
    resolved via PyPI JSON, readelf -d on the published wheel, the sdists, and local
    sdist builds. Revisit once cmeel-console-bridge and cmeel-tinyxml2 are on the
    registry.'
- pkg: mpi4py
  version: 4.1.2
  home: https://mpi4py.github.io/mpi4py/
  repo: https://github.com/mpi4py/mpi4py
  status: published
  pr: 1686
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lameenc
  version: 1.8.4
  home: https://github.com/chrisstaite/lameenc
  repo: https://github.com/chrisstaite/lameenc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1116
  notes: '51 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev. Wraps LAME 3.100 (LGPL-2.0), downloaded and statically linked at build time via upstream''s own manylinux build.sh recipe (--disable-frontend/--disable-decoder excludes the GPL frontend/mpglib decoder), reproduced via cibuildwheel + CIBW_BEFORE_ALL_LINUX. Patch widens license-files to ship LAME''s COPYING as LICENSE.lame; gpl_sources job archives the pinned LAME tarball as corresponding source. PR #1116 open, CI running. Merged (#1116). Issue #1167 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: mecab
  version: 0.996.13
  home: https://shogo82148.github.io/mecab/
  repo: https://github.com/shogo82148/mecab
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1390
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; upstream is shogo82148/mecab, a monorepo with the MeCab C++ fork and the Python (SWIG) binding under mecab/python, tagged together (not the distinct, already-ported mecab-python3/SamuraiT package). setup.py links libmecab via mecab-config, same shape as mecab-python3''s PR #1117; CIBW_BEFORE_ALL_LINUX builds it from {project}/mecab (the checkout root''s top-level mecab dir -- not doubly nested like mecab-python3''s /tmp clone of taku910/mecab), needing gettext-devel for AM_ICONV''s m4 macros. Patch adds a LICENSE matching setuptools'' default glob, same gap as mecab-python3. Tests via unidic-lite like mecab-python3. PR #1390 green (4 wheels built cp312-cp314t, publish dry-run OK), awaiting review.'
- pkg: nvshmem4py-cu13
  version: 0.3.1
  home: https://developer.nvidia.com/nvshmem
  repo: https://github.com/NVIDIA/nvshmem
  status: parked
  pr: null
  notes: 'Parked after read-only triage; no worktree/branch/PR/workflow/docs-yaml/patches created.
    The queue entry''s "PyPI records no project URL" was resolved: nvshmem4py is NVIDIA''s
    first-party Python binding for NVSHMEM and its source is public at github.com/NVIDIA/nvshmem
    (Apache-2.0), in the nvshmem4py/ subdirectory, tagged per release (v3.7.2-0 matches the
    nvidia-nvshmem-cu13 3.7.2 runtime). So, like nccl4py, this is not a no-source park
    (contrast pyqt5-qt5/saxonche) -- it is a hard NVIDIA-GPU/CUDA requirement, the same class as
    the parked nccl4py, pynvvideocodec, cupy-cuda12x, onnxruntime-gpu and humming-kernels entries
    (gotchas 386/387/452/471; gotcha 284''s "runtime dlopen or build-time headers?" test comes out
    on the headers side). Five walls, each read off the real source tree and the real cp312
    manylinux_2_34 x86_64 wheel rather than inferred:
    (1) UPSTREAM''S OWN ARCH TABLE SAYS NO (gotcha 386''s first check). nvshmem4py has no
    standalone pip build: it is a CMake subproject of the NVSHMEM C library, and
    nvshmem4py/cmake/buildWheel.cmake opens with a CMAKE_SYSTEM_PROCESSOR switch matching only
    aarch64|ARM64|arm64 and x86_64|amd64|AMD64, else message(FATAL_ERROR "Unsupported
    architecture: ..."). The same two files hold the whole -cu12/-cu13 axis, i.e. gotcha 387''s
    selector table with two GPU-toolkit members and no CPU member: set(CUDA_VERSIONS "12" "13")
    in nvshmem4py/CMakeLists.txt, WHEEL_NAME "nvshmem4py-cu${CUDA_VER}" in buildWheel.cmake, a
    scripts/generate_pyproject_toml.py that synthesizes pyproject.toml at build time, then
    sed -i of project.name from "nvshmem4py" to "nvshmem4py-cu13" and of setup.py''s
    install_requires source from requirements.txt to requirements_cuda13.txt. PyPI agrees: only
    nvshmem4py-cu12 and nvshmem4py-cu13 exist and plain nvshmem4py is 404, so there is no
    CPU-shaped sibling of this distribution.
    (2) BUILD hard-requires CUDA toolkit headers. nvshmem/bindings/cynvshmem.pxd has
    `cdef extern from "cuda_runtime_api.h"` plus "cuda_fp16.h" and "cuda_bf16.h"; the shipped
    generated nvshmem.cpp #includes all three; buildWheel.cmake builds with
    CPPFLAGS=-I${CUDA_HOME}/include/. Reproduced on a CUDA-less x86_64 host:
    g++ -fsyntax-only nvshmem/bindings/nvshmem.cpp -> "fatal error: cuda_runtime_api.h: No such
    file or directory". NVIDIA ships no riscv64 CUDA toolkit -- redistrib_13.0.0.json,
    redistrib_13.2.0.json and redistrib_13.4.2.json list exactly linux-x86_64, linux-sbsa,
    linux-all, windows-x86_64/arm64 and contain zero riscv strings (gotchas 387/471, same finding
    as triton/cupy-cuda12x/nccl4py). Building libnvshmem itself first is not an escape hatch: the
    top-level CMakeLists sets CMAKE_CUDA_COMPILER=${CUDA_HOME}/bin/nvcc with
    CMAKE_CUDA_ARCHITECTURES 75/80/89/90/100/120 for CUDA 13, and src/CMakeLists.txt links
    nvshmem_host and every transport against CUDA::cudart_static.
    (3) INSTALL cannot resolve on riscv64 (gotcha 40). requirements_cuda13.txt is headed "These
    are the run-time dependencies of nvshmem4py / They are non-negotiables", and the wheel''s
    Requires-Dist matches it with no markers and no extras: nvidia-nvshmem-cu13,
    cuda-python>=13.0,<14.0, cuda.core>=0.5.0, cuda.pathfinder>=1.2.3, numpy, Cython>=0.29.24.
    nvidia-nvshmem-cu13 3.7.2 publishes only two 135 MB py3-none-manylinux_2_17_{x86_64,aarch64}
    wheels and NO sdist; cuda-core 1.2.0 (pulled in by cuda-python 13.4.1) publishes only
    manylinux_2_28_{x86_64,aarch64} + win_amd64 and NO sdist. Neither is on the registry
    (pypi.riseproject.dev/simple/{nvidia-nvshmem-cu13,cuda-core}/ -> 404), and nvshmem4py-cu13 has
    never published an sdist on any of its six releases (0.1.2 .. 0.3.1), so nothing could be
    built from PyPI either.
    (4) RUNTIME needs the proprietary driver and real GPUs, and the readelf check points the wrong
    way (gotcha 452). The wheel is 53 files / 23 MB: three Cython extensions plus pure Python and
    no vendored CUDA blob, and readelf -d on all three lists only
    libstdc++/libm/libgcc_s/libpthread/libc with zero undefined cu*/nvshmem* symbols. The gate is
    one dlopen string plus pure Python: nvshmem/bindings/_internal/nvshmem_linux.pyx''s
    load_library() does dlopen("libnvshmem_host.so.3", RTLD_NOW|RTLD_GLOBAL) and raises
    RuntimeError("Failed to dlopen libnvshmem ...") otherwise, and it is called from
    _check_or_init_nvshmem() for every one of the hundreds of dlsym''d entry points; separately
    `import nvshmem.core` unconditionally runs `from cuda.pathfinder import
    load_nvidia_dynamic_lib`, `from cuda.core import Buffer, Device, Stream, MemoryResource,
    ObjectCode` and `import cuda.bindings.driver` across init_fini.py, memory.py,
    nvshmem_types.py, collective.py, rma.py and interop/{torch,cupy,cute}.py, and
    init()/get_version()/get_unique_id() each call load_nvidia_dynamic_lib("nvshmem_host") before
    touching a binding. libnvshmem_host.so.3 ships only in nvidia-nvshmem-cu13 (x86_64/aarch64)
    and needs libcuda from the NVIDIA driver, which has no riscv64 build. There is no CPU-only
    mode even in principle: NVSHMEM is a PGAS/one-sided-RMA layer for GPU-initiated communication
    over NVLink/NVSwitch/PCIe P2P/IB between NVIDIA GPUs, and its device-side API is emitted as
    CUDA kernels through numba-cuda and the CuTe DSL (nvshmem/bindings/device/{numba,cute}).
    (5) Untestable even if something built (gotcha 6): nvshmem4py/test/run_tests.sh is a Slurm job
    script requiring NVSHMEM_HOME, CUDA_HOME, MPI, rdma-core/libibverbs, NVSHMEM_HCA_LIST=mlx5_*
    and >=2 GPU ranks, and the tests import mpi4py plus numba.cuda and launch @cuda.jit kernels.
    Upstream also has no CI workflow to mirror (.github holds only issue templates), so the repo''s
    goal-2 "narrow upstream''s own CI" has nothing to start from.
    Latest release re-checked per gotcha 388: 0.3.1 is current on PyPI (8 wheels, cp310-cp313,
    manylinux_2_34 x86_64+aarch64 only, no sdist); the repo has a release/v3.8.0 branch whose
    nvshmem4py would carry the identical shape. Registry checked:
    pypi.riseproject.dev/simple/nvshmem4py-cu13/ -> 404. No new gotcha number added -- the shape is
    fully covered by 386, 387, 452, 471, 284 and 40. Revisit only if NVIDIA ships both a riscv64
    CUDA toolkit and a riscv64 driver, and even then only once riscv64 hosts carry NVIDIA GPUs on
    an NVLink/IB fabric.'
- pkg: rfc3161-client
  version: 1.0.8
  home: https://pypi.org/project/rfc3161-client
  repo: https://github.com/trailofbits/rfc3161-client
  status: published
  pr: 1211
  notes: PyO3/maturin abi3-py39; runtime dep on cryptography, already riscv64 on pypi.riseproject.dev. PR open, CI running.
- pkg: cmeel-assimp
  version: 6.0.5
  home: https://github.com/cmake-wheel/cmeel-assimp
  repo: https://github.com/cmake-wheel/cmeel-assimp.git
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 0" is PEP 427''s optional build number, not an ABI tag (gotcha 470): the
    released files are cmeel_assimp-6.0.5-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl
    plus two macOS wheels, and there is no sdist on PyPI at all -- [tool.cmeel]
    has-sitelib = false, so per gotcha 487 ONE interpreter-independent py3-none-<plat>
    wheel covers every interpreter (103 entries: cmeel.prefix/lib/libassimp.so.6.0.5 +
    soname links, cmeel.prefix/bin/assimp, headers, assimp-6.0 CMake config and
    assimp.pc -- gotcha 35 shape, not gotcha 27). PORTABILITY IS FINE, and was settled
    rather than assumed. assimp 6.0.5 is architecture-neutral C++:
    CMakeLists.txt/code/CMakeLists.txt contain no CMAKE_SYSTEM_PROCESSOR branch and no
    -march/-msse/-mavx/march=native string anywhere, cmake_minimum_required(VERSION
    3.22) so CMake 4 needs no CMAKE_POLICY_VERSION_MINIMUM (unlike cmeel-console-bridge,
    gotcha 483), and the only optional-dependency risk in the queue prompt does not
    arise: ASSIMP_BUILD_DRACO defaults OFF and draco is the only add_subdirectory''d
    contrib besides contrib/zlib, which is itself skipped because ASSIMP_BUILD_ZLIB
    defaults OFF on non-Windows. The contribs actually compiled into libassimp
    (Open3DGC, clipper, meshlab, openddlparser, poly2tri, pugixml, rapidjson, stb,
    tinyusdz, unzip, utf8cpp, zip) are portable C++; the only SIMD in the whole enabled
    set is stb_image.h (gated on STBI__X86_TARGET) and rapidjson (gated on opt-in
    RAPIDJSON_SIMD, which assimp never defines), so nothing __riscv-hostile.
    [tool.cmeel] configure-args is only -DASSIMP_BUILD_ASSIMP_TOOLS=ON
    -DASSIMP_WARNINGS_AS_ERRORS=OFF, and cmeel.patch is 2 hunks that set INSTALL_RPATH
    to $ORIGIN/@loader_path and replace ${ZLIB_LIBRARIES} with a plain -lz. Local
    rehearsal per gotcha 9/125, from the real v6.0.5.c0 checkout (tag confirmed via git
    ls-remote, not guessed) with submodules: pip wheel . --no-deps produced
    cmeel_assimp-6.0.5-0-py3-none-manylinux_2_39_x86_64.whl in about 4 minutes on 4 x86
    cores, structurally identical to the released wheel (same 103 entries, Build: 0,
    Generator: cmeel). BLOCKED ON EXACTLY ONE SIBLING, cmeel-zlib 1.3.2, which is
    already its own queued .queue.yml entry and a leaf of gotcha 470''s pin closure (its
    deps are cmeel alone). Evidence: pyproject.toml names it twice -- [build-system]
    requires = ["cmeel-zlib", "cmeel[build]"] and [project] dependencies =
    ["cmeel-zlib"] -- the live PyPI JSON for cmeel-assimp 6.0.5 confirms requires_dist
    == ["cmeel", "cmeel-zlib"], cmeel-zlib 1.3.2 publishes only manylinux/musllinux
    x86_64+aarch64 and macOS wheels (no riscv64), pypi.riseproject.dev/simple/ still
    lists cmeel-boost as its only cmeel package, and ci_scripts/riscv64_resolve.py
    ''cmeel-zlib==1.3.2'' reports UNRESOLVABLE on cp312/cp313/cp314/cp314t. Nothing else
    is missing: cmeel is py3-none-any and resolves for all four, and cmake
    (cmeel[build]''s only heavy build input) has riscv64 wheels at 4.4.3. NUANCE WORTH
    RECORDING, because it is the one thing that could tempt a shortcut: cmeel-zlib is a
    *nominal* dependency on Linux, not a link-time one. The cmeel.patch links -lz by
    name, so FIND_PACKAGE(ZLIB) picking the image''s zlib is enough to build -- verified
    in the rehearsal, whose assimp.pc records Libs.private:
    /usr/lib/x86_64-linux-gnu/libz.so, i.e. the host zlib, not cmeel-zlib''s -- and
    readelf -d on both the released x86_64 wheel and the locally built one shows
    libassimp.so.6.0.5 NEEDing libz.so.1 with RUNPATH $ORIGIN while cmeel-zlib ships its
    libz in cmeel.prefix/lib64 (not on that RUNPATH), so libz.so.1 resolves from the
    system at runtime under the manylinux allowlist either way. That does NOT unblock
    the port, for gotcha 483''s reason: pip still enforces the metadata. On riscv64 the
    build env cannot resolve a cmeel-zlib wheel, and a published cmeel_assimp riscv64
    wheel would carry Requires-Dist: cmeel-zlib that the index cannot satisfy
    binary-only, so every user''s pip install cmeel-assimp would fall back to
    cmeel-zlib''s sdist -- whose CMakeLists.txt is an ExternalProject_Add that DOWNLOADS
    https://github.com/madler/zlib/archive/refs/tags/v1.3.2.tar.gz and compiles it, i.e.
    it needs a toolchain and network on the user''s machine (a strictly worse profile
    than cmeel-tinyxml2/cmeel-console-bridge, which vendor their sources). Getting CI
    green is the wrong question (gotcha 483); publishing that entry would break
    structural goal 1. Dropping the dependency by patching pyproject.toml was considered
    and rejected: it would diverge the wheel''s metadata from upstream''s for every
    other cmeel consumer and break the shared-prefix contract (goal 2), and cmeel-zlib
    is outside this port''s commit scope anyway. FOR WHOEVER TAKES cmeel-zlib: it looks
    like a one-cycle port -- tag v1.3.2.c0 (git ls-remote), [build-system] requires =
    ["cmeel[build]"] only, has-sitelib = false so one cp312-manylinux_riscv64
    cibuildwheel job yields the py3-none wheel, cmake_minimum_required(VERSION 4.0) so
    no policy floor workaround, and upstream zlib 1.3.2 has no arch-specific code; the
    ExternalProject download means the job needs outbound github.com access (could not
    be exercised here: this sandbox''s proxy answers 403 to github.com tarballs, which
    is an environment limit, not an upstream failure). FOR WHOEVER RETURNS TO
    cmeel-assimp once cmeel-zlib is on the registry: build-cmeel-tinyxml2.yml /
    build-cmeel-console-bridge.yml are the right template (single job, no python matrix,
    only: cp312-manylinux_riscv64, CIBW_REPAIR_WHEEL_COMMAND: "" since cmeel sets
    INSTALL_RPATH itself, CIBW_ENVIRONMENT with CMEEL_LOG_LEVEL=DEBUG and
    PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/), checkout
    cmake-wheel/cmeel-assimp at v6.0.5.c0 with submodules: true, and assert
    cmeel.prefix/lib/libassimp.so.6.0.5 plus a ctypes.CDLL smoke test on
    cmeel.prefix/lib/libassimp.so -- note a CDLL test on cmeel-zlib''s own libz must use
    lib64/, not lib/. Expect a noticeably longer build than the other cmeel leaves (~4
    min on 4 fast x86 cores here, so budget hours on the riscv64 runner) but nothing
    near cmeel-boost. No branch/worktree or PR opened and no CI cycle burned; resolved
    with PyPI JSON, git ls-remote, the upstream checkout, readelf -d on the released and
    locally built wheels, ci_scripts/riscv64_resolve.py, and one full local
    sdist-equivalent build. Revisit once cmeel-zlib is on the registry.'
- pkg: webrtcvad-wheels
  version: 2.0.14
  home: https://github.com/daanzu/py-webrtcvad-wheels
  repo: https://github.com/daanzu/py-webrtcvad-wheels
  status: published
  pr: null
  notes: '76 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39,pp310,pp37,pp38,); no riscv64 on PyPI or pypi.riseproject.dev. Small isolated Google WebRTC VAD C sources vendored directly in cbits/ (setup.py Extension build, no external deps); cbits/webrtc/typedefs.h already has native __riscv branch as of the v2.0.14 tag (upstream commit fb0e232, "Add RISC-V support"), so no patch needed. Build-from-checkout shape mirroring upstream own cibuildwheel workflow (manylinux+musllinux, cp312/cp313/cp314/cp314t), tests via CIBW_TEST_EXTRAS=dev + upstream test_webrtcvad.py. Verified locally: pip wheel . --no-deps --no-build-isolation builds and imports cleanly on macOS/cp39. Merged (#1109). Issue #1136 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: tflite-runtime
  version: 2.14.0
  home: https://www.tensorflow.org/lite/
  repo: https://github.com/tensorflow/tensorflow
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2154
  notes: 'PR #2154 open as a draft and FULLY GREEN on the first CI cycle - both legs built and tested, publish dry run clean (cp310 53m, cp311 52m; bazel build 991 actions / 2739s, wheels 1.8MB, tflite_runtime/_pywrap_tensorflow_interpreter_wrapper.so 3.9MB with ELF e_machine 0xF3, tag cp3XX-cp3XX-manylinux_2_39_riscv64 after auditwheel retagged linux_riscv64, test installs numpy 2.4.3 from the registry and runs testdata/add.bin to 3x its input). 12 Linux wheels upstream (abi: cp310,cp311,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Feasible and much smaller than tensorflow itself (gotcha 426): 2.14.0 is the last tflite-runtime release and predates the LiteRT split, so upstream is tensorflow/tensorflow at tag v2.14.0 (verified: PyPI lists only www.tensorflow.org/lite, and tensorflow/tools/pip_package/setup.py at that tag reads _VERSION = 2.14.0 while tensorflow/lite/tools/pip_package/build_pip_package_with_bazel.sh builds the tflite_runtime distribution). The recipe builds ONE bazel target, //tensorflow/lite/python/interpreter_wrapper:_pywrap_tensorflow_interpreter_wrapper, with -c opt --config=monolithic --config=noaws --config=nogcp --config=nohdfs --config=nonccl, then copies it next to interpreter.py/metrics_portable.py and runs setup_with_binary.py - no TF core, no MLIR, and flex is behind a define we do not set, so this is comparable in scope to ai-edge-litert (PR #2135) rather than to a full TF build. FOUR riscv64 questions checked statically before the first cycle: (1) bazel: .bazelversion says 6.1.0 and no riscv64 binary exists for any bazel release, so 7.5.0 is bootstrapped as elsewhere in this repo - TF 2.14 has no MODULE.bazel, its only version gate is workspace2.bzl versions.check("1.0.0"), and every flag its .bazelrc sets (--experimental_cc_shared_library, --experimental_link_static_libraries_once, --incompatible_enforce_config_setting_visibility, --experimental_repo_remote_exec, configs monolithic/noaws/nogcp/nohdfs/nonccl) was verified to parse under a real bazel 7.5.0 locally; build:linux is auto-applied via --enable_platform_specific_config and already carries --cxxopt=-std=c++17. (2) XNNPACK at this TF commit is b9d4073a, where riscv64 PRODUCTION microkernels are scalar only (scalar-riscv amalgam / PROD_SCALAR_RISCV_MICROKERNEL_SRCS); ALL_RVV_MICROKERNEL_SRCS and the XNN_ENABLE_RISCV_VECTOR define appear only in bench_microkernels/test_microkernels, and the tree has no rvvfp16arith at all - so neither gotcha 420 (zvfh vs binutils 2.41) nor an RVV-intrinsics-vs-GCC-14 problem can arise, and no xnn_enable_* define is needed. cpuinfo 87d82345 has a linux_riscv64 srcs branch. (3) hermetic python IS the one real gap: TF 2.14 pins rules_python 0.23.1, whose PLATFORMS/TOOL_VERSIONS have no riscv64 CPython, and BOTH //third_party/python_runtime:headers (via @local_config_python -> @python//:python_headers) and //third_party/py/numpy:headers (@pypi_numpy//:numpy_headers) come from it; WORKSPACE also load()s @python//:defs.bzl, so the toolchain_aliases repo is fetched eagerly and get_host_platform() fails on riscv64. Solved with no patch by --override_repository=python=... and --override_repository=pypi_numpy=..., two repos written at run time from the container CPython include dir and numpy.get_include(); VALIDATED off-target: a real bazel 7.5.0 on x86_64 evaluated the whole WORKSPACE python/pip_parse block with those overrides and got as far as workspace3.bzl fetching tf_runtime, i.e. past every hermetic-python line, and pip_parse never ran pip. (4) numpy: nothing needs patching either - the interpreter_wrapper C++ uses only APIs numpy 2 still exports (PyArray_SimpleNewFromData/EMPTY/FromAny/Return/SetBaseObject/ENABLEFLAGS/GETITEM/Iter*, NPY_ARRAY_*, no descr->elsize), so it builds against the registry numpy (2.2.2 cp310 / 2.4.3 cp311) and that keeps the wheel loadable under numpy 1.x and 2.x alike. Matrix is cp310+cp311 by upstream constraint, not by choice: upstream ships cp38-cp311 only, pybind11 is pinned at 2.10.4 which predates CPython 3.12, TF_PYTHON_VERSION accepts 3.9-3.11, and the manylinux riscv64 image has no interpreter below 3.10; registry numpy covers both. Local validation could not go further than WORKSPACE evaluation: this sandbox egress blocks codeload.github.com, so every tf_http_archive 403s. Merged (#2154). Push-triggered build+publish on main run 35594102898 in progress as of 2026-09-21T11:26Z; move to published once https://pypi.riseproject.dev/simple/tflite-runtime/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: cmeel-zlib
  version: 1.3.2
  home: https://github.com/cmake-wheel/cmeel-zlib
  repo: https://github.com/cmake-wheel/cmeel-zlib.git
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2153
  notes: '4 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 0" is PEP 427''s build number, not an ABI tag (gotcha 470); the real
    filenames are cmeel_zlib-1.3.2-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl plus
    two macOS. [tool.cmeel] has-sitelib = false, so one py3-none wheel covers every
    interpreter and the workflow is a single cibuildwheel job (gotcha 487). No patch and
    no build flag needed: the wrapper declares cmake_minimum_required(VERSION 4.0) and
    stock zlib 1.3.2 declares 3.12...3.31, both at or above CMake 4''s floor, so
    cmeel-console-bridge''s CMAKE_POLICY_VERSION_MINIMUM trap (gotcha 257) does not
    apply; this is madler''s stock pure-C zlib, not zlib-ng, and its CMakeLists has no
    SIMD/assembly or arch-conditional path at all (gotcha 477''s WITH_RVV concern is
    zlib-ng-only). The build is one ExternalProject_Add of madler/zlib
    archive/refs/tags/v1.3.2.tar.gz, and because cmeel''s own -DCMAKE_INSTALL_LIBDIR=lib
    never reaches that child the payload installs to cmeel.prefix/lib64/ exactly as in
    upstream''s released x86_64 wheel - new gotcha 493, also the mechanism behind gotcha
    492. Full QEMU riscv64 rehearsal in manylinux_2_39_riscv64 built
    cmeel_zlib-1.3.2-0-py3-none-manylinux_2_39_riscv64.whl with a namelist identical to
    upstream''s, libz.so.1.3.2 a real riscv64 ELF (SONAME libz.so.1, libc the only
    NEEDED) and a compress2/uncompress round-trip through ctypes; CI reproduced it on the
    runner and the publish job dry-ran clean. Unblocks cmeel-assimp and is one of
    pin/pinocchio''s 11 blocking siblings. Merged (#2153). Push-triggered build+publish on main run 35573800141 succeeded. 404 on pypi.riseproject.dev/simple/cmeel-zlib/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: hmmlearn
  version: 0.3.3
  home: https://github.com/hmmlearn/hmmlearn
  repo: https://github.com/hmmlearn/hmmlearn
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1494
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout of the pybind11/setuptools _hmmc extension, mirrors upstream build.yml. Matrix cp312/cp313/cp314/cp314t only: scikit-learn (hard runtime dep) has riscv64 wheels on pypi.riseproject.dev only for those interpreters. Pins SETUPTOOLS_SCM_PRETEND_VERSION_FOR_HMMLEARN since the tag checkout has no git history. Local rehearsal (macOS/cp39, built wheel installed): 295 passed, 21 xfailed, 5 xpassed.'
- pkg: sqlean-py
  version: 3.50.4.5
  home: https://github.com/nalgeon/sqlean.py
  repo: https://github.com/nalgeon/sqlean.py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1217
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout, mirrors upstream publish.yaml: CIBW_BEFORE_BUILD runs upstreams own make prepare-src download-sqlite download-sqlean (sources not checked in); tested with python -m tests (361 passed, 2 skipped locally on cp39/macOS rehearsal). Patch adds LICENSE.sqlean/LICENSE.pcre2 for the vendored MIT/BSD sources compiled into the extension, neither shipped by upstream.'
- pkg: cmeel-octomap
  version: 1.10.0
  home: https://github.com/cmake-wheel/cmeel-octomap
  repo: https://github.com/cmake-wheel/cmeel-octomap.git
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2159
  notes: '9 Linux wheels upstream (abi: 4,5); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 4,5" is PEP 427''s build number, not an ABI tag (gotcha 470), and unlike
    every cmeel package ported so far it is not 0: [tool.cmeel] build-number was bumped
    4 -> 5, so 1.10.0 has two packaging passes on PyPI and the tag to build is the
    highest, v1.10.0.c5 (new gotcha 505). [tool.cmeel] has-sitelib = false, so one
    py3-none wheel covers every interpreter and the workflow is a single cibuildwheel
    job (gotcha 487). A true leaf: pyproject''s [build-system] requires is cmeel[build]
    alone and the released METADATA''s only Requires-Dist is cmeel (py3-none-any on
    PyPI), so no unported sibling is involved - unlike cmeel-assimp/cmeel-zlib (gotcha
    492). [tool.cmeel] source = "octomap/octomap" builds only the core library, so the
    submodule''s top-level CMakeLists (cmake_minimum_required 3.0.2) is never read; the
    one that is declares 3.5, exactly CMake 4''s floor, and the build needs no
    CMAKE_POLICY_VERSION_MINIMUM (gotcha 257/207). No SIMD, assembly or arch-conditional
    code anywhere in octomap. CMEEL_RUN_TESTS=false is copied from upstream''s
    release.yml and is load-bearing: with tests on, octomap''s test_color_tree fails on
    riscv64 with 3431 != 1034 nodes, which is GCC''s default -ffp-contract=fast fusing
    (float)x*0.05f+0.01f into an FMA and moving points across voxel boundaries - x86
    reproduces it exactly under -march=haswell -ffp-contract=fast, so it is not
    riscv64-specific (new gotcha 504). Full QEMU riscv64 rehearsal in
    manylinux_2_39_riscv64 built cmeel_octomap-1.10.0-5-py3-none-manylinux_2_39_riscv64.whl
    with a namelist identical to upstream''s x86_64 wheel, liboctomap.so.1.10.0 a real
    riscv64 ELF (RPATH $ORIGIN/../lib, libc/libstdc++/libm/libgcc_s and its own
    liboctomath the only NEEDED), and the wheel''s own log2graph/graph2tree built a
    14929-node octree from a generated scan; CI reproduced all of it on the runner and
    the publish job dry-ran clean. Is one of pin/pinocchio''s 11 blocking siblings. Merged (#2159). Push-triggered build+publish on main run 35594075140 in progress as of 2026-09-21T11:26Z; move to published once https://pypi.riseproject.dev/simple/cmeel-octomap/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: angr
  version: 9.3.3
  home: https://angr.io/
  repo: https://github.com/angr/angr
  status: blocked-on-dependency
  pr: null
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev. BLOCKED-ON-DEPENDENCY (evidence gathered 2026-09-21 from PyPI JSON, https://pypi.riseproject.dev/simple/<dep>/ and ci_scripts/riscv64_resolve.py; see gotcha 503). angr itself IS genuinely compiled and the queue''s ''abi: cp312'' is real, not misattributed from a dependency: angr 9.3.3 builds angr.rustylib via setuptools-rust (pyo3 0.29 abi3-py312, icicle-emu + libafl 0.16.1 as git deps), plus a C/C++ native/unicornlib/ shared library built with make, and generates angr/protos/*_pb2.py with grpcio-tools. Hard blockers - native code, no riscv64 wheel on PyPI, 404 on our registry, and all five already queued here: (1) pyvex 9.3.3 - the deepest, because it is BOTH a runtime pin (pyvex==9.3.3, also pulled in by cle) and a [build-system] requires entry: native/unicornlib/Makefile compiles against PYVEX_INCLUDE_PATH/PYVEX_LIB_PATH resolved from the *installed* pyvex package and links -lpyvex, so angr cannot even be BUILT on riscv64 until a riscv64 pyvex wheel exists (gotcha 249 shape with the build-time dep being the same package as the runtime one). That port looks feasible: pyvex 9.3.3 vendors VEX with both guest_riscv64_*.c and host_riscv64_*.c present in vex/priv/, builds with scikit-build-core + cffi, and its own deps resolve for riscv64 (bitstring backtracks to 4.3.1, bitarray 3.11.0 is on the registry). (2) pypcode 4.0.0 (angr: pypcode~=4.0) - Ghidra SLEIGH C++ via nanobind+cmake, 15 wheels upstream, none riscv64. (3) pydemumble 0.1.3 (angr: pydemumble~=0.1.3) - C++ demumble via scikit-build-core+nanobind, 7 wheels, none riscv64. (4) pyxdia 0.1.1 (via cle: ''pyxdia~=0.1; sys_platform != "emscripten"'') - the hardest: its sdist has NO native sources (21 pure-Python files), setup.py''s custom build command downloads prebuilt binaries at build time (xdia.zip + xdialdr.tar.xz from mborgerson/xdia releases) and, on any non-x86_64 Linux, a prebuilt blink x86-64 emulator to run the bundled Windows DIA binary under; _install_blink''s table covers only darwin-x86_64/darwin-arm64/linux-aarch64 and hits `assert blink_platform is not None` on riscv64, so the build aborts outright. Needs an upstream linux-riscv64 blink release asset or a from-source blink build. (5) uefi-firmware 1.16 (via cle: ''uefi-firmware~=1.16; sys_platform != "emscripten"'') - C extension (uefi_firmware/compression/EfiCompressor.c), 24 wheels, none riscv64. Version gaps on packages we already publish (each needs a new ''- version:'' entry in the OTHER package''s docs/packages/*.yaml, out of this port''s commit scope): (6) lmdb==2.1.1 - an exact pin in angr''s dependencies; our registry has lmdb 2.3.0 only (plus 1.7.x) and lmdb 2.1.1 has no riscv64 wheel on PyPI either - `riscv64_resolve.py --abi cp312 -- lmdb==2.1.1` reports ''from versions: 1.7.1, 1.7.2, 1.7.3, 1.7.5, 2.3.0''. (7) z3-solver==4.13.0.0 - pinned by claripy 9.3.3; our registry has 4.12.2.0/4.12.6.0/4.14.1.0/4.15.1.0/4.15.4.0/4.15.8.0/4.16.0.0, and upstream PyPI publishes riscv64 only for 5.0.0.0/5.1.0.0. Secondary, resolvable inside the eventual angr port rather than a hard block: grpcio-tools~=1.80.0 in [build-system] requires - our registry has grpcio_tools 1.83.1 only (cp312/313/314) and PyPI has no riscv64 grpcio-tools at all, so the port needs either a 1.80.x build or a pin relaxation (upstream pins it deliberately to keep the generated protobuf gencode stamp on the 6.x major - see the comment at the top of angr''s pyproject.toml). Explicitly NOT blockers, checked so the next agent does not re-litigate them: archinfo 9.3.3, claripy 9.3.3, cle 9.3.3 and angr-data 0.1.1 are all py3-none-any on PyPI (pure Python) and install on riscv64 as-is - of the same-org sibling stack only pyvex needs a port; arpy 1.1.1 and mulpyplexer 0.09 are sdist-only pure Python with no wheel for any platform, so riscv64_resolve.py''s --only-binary flags them unresolvable, a false positive; capstone 5.0.9 is on the registry and unicorn 2.1.4 / keystone-engine 0.9.2 are extras-only and already published here. A combined `riscv64_resolve.py --abi cp312` over the 17 non-blocked runtime deps resolves cleanly (23 wheels: cffi 2.1.1, msgspec 0.21.1, protobuf 7.36.2, psutil 7.2.2, capstone 5.0.9 plus the pure-Python ones). Revisit once pyvex, pypcode, pydemumble, pyxdia and uefi-firmware are published; the lmdb 2.1.1 and z3-solver 4.13.0.0 entries are cheap and can land first. No workflow, docs/packages/angr.yaml or patches written - nothing to build yet.'
- pkg: ta-lib
  version: 0.7.1
  home: http://github.com/ta-lib/ta-lib-python
  repo: http://github.com/ta-lib/ta-lib-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1219
  notes: CI green on cp312/cp313/cp314-manylinux_riscv64; PR open for review.
- pkg: jsoncompat
  version: 0.4.2
  home: https://jsoncompat.com
  repo: https://github.com/ostrowr/jsoncompat
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1814
  notes: 'PyPI records no project URL; found via the sdist-less wheel metadata description (links to ostrowr/jsoncompat). Rust/PyO3 extension built via maturin from the pybindings/ crate in a Cargo workspace (tag v0.4.2); no abi3 feature on pyo3, so upstream ships one wheel per interpreter (cp312, cp313, cp314, cp314t, cp315) via `-i ''3.12 3.13 3.14 3.14t 3.15''` in its own ci.yml. Depends on a pinned git fork (ostrowr/jsonschema) as a Cargo dependency. PR #1814 open, CI running.'
- pkg: cmeel-qhull
  version: 8.0.2.1
  home: https://github.com/cmake-wheel/cmeel-qhull
  repo: https://github.com/cmake-wheel/cmeel-qhull
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2160
  notes: '9 Linux wheels upstream (abi: 0,1); no riscv64 on PyPI or pypi.riseproject.dev. The "abi: 0,1" is PEP 427''s build number, not an ABI tag (gotcha 470), and it lists two values because 8.0.2.1 was packaged twice: [tool.cmeel] build-number = 1 at the tag, `git ls-remote --tags` shows v8.0.2.1.c0 and v8.0.2.1.c1, and the released cmeel_qhull-8.0.2.1-1-py3-none-manylinux_2_28_x86_64.whl carries Build: 1 in dist-info/WHEEL - so v8.0.2.1.c1 is the tag that reproduces the release (gotcha 505). [tool.cmeel] has-sitelib = false, so one py3-none wheel covers every interpreter and the workflow is a single cibuildwheel job (gotcha 487); the payload is cmeel.prefix/{lib/libqhull_r.so.8.0.2 + libqhull{cpp,static,static_r}.a, include/libqhull*, bin/{qhull,qconvex,qdelaunay,qvoronoi,qhalf,rbox}, lib/{cmake,pkgconfig}}. A true leaf: [build-system] requires is cmeel[build] alone and the released METADATA''s only Requires-Dist is cmeel (py3-none-any on PyPI), so no unported cmeel sibling is involved - and it is one of the leaves of the pin/pinocchio closure (gotcha 470''s dependency map). cmeel drives the one `cmake -S qhull` itself, so its -DCMAKE_INSTALL_LIBDIR=lib does reach the build and the payload lands in cmeel.prefix/lib/, not lib64/ (gotcha 493 is ExternalProject-only and does not apply). ONE REAL BLOCKER, verified locally both ways: qhull''s CMakeLists.txt declares cmake_minimum_required(VERSION 3.0) while cmeel[build] resolves CMake >= 4, which fails at the metadata stage with "Compatibility with CMake < 3.5 has been removed"; CMAKE_POLICY_VERSION_MINIMUM=3.5 in CIBW_ENVIRONMENT fixes it with no patch (gotcha 257''s env-var form, as for cmeel-console-bridge - the cmeel backend owns the configure line). No portability risk otherwise: qhull 2020.2 is portable C/C++ with no -march/SSE/AVX/NEON/RVV string, no assembly and no CMAKE_SYSTEM_PROCESSOR branch anywhere; the only arch/OS preprocessor test in the tree is a Windows check in user_r.h, and the historical x86 FPU-precision worry does not appear in this release. Rehearsed twice (gotcha 9/125): locally on x86 and in a QEMU manylinux_2_39_riscv64 container, which built cmeel_qhull-8.0.2.1-1-py3-none-manylinux_2_39_riscv64.whl whose namelist matches upstream''s x86_64 release wheel entry for entry (123/123), loaded libqhull_r.so through ctypes and ran rbox|qconvex/qdelaunay/qvoronoi on it. PR #2160 open. Merged (#2160). Push-triggered build+publish on main run 35594049601 in progress as of 2026-09-21T11:26Z; move to published once https://pypi.riseproject.dev/simple/cmeel-qhull/ returns 200 (docs catalog PR #2133 also pending merge).'
- pkg: coacd
  version: 1.0.14
  home: https://colin97.github.io/CoACD/
  repo: https://github.com/SarahWeiii/CoACD
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1692
  notes: build-from-checkout via cibuildwheel/setuptools+CMake; setup.py tags a single abi3 wheel at the building interpreter (cp39, matching upstream), reused/tested on cp312-cp314; cp314t dropped since the override never guards Py_GIL_DISABLED. Statically links FetchContent-pulled zlib/Boost/OpenVDB/oneTBB/spdlog plus the CDT submodule; their licences (none shipped upstream) are staged at the wheel root. gpl_sources job added for -static-libgcc/-static-libstdc++.
- pkg: fugashi
  version: 1.5.2
  home: https://github.com/polm/fugashi
  repo: https://github.com/polm/fugashi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1490
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1490 open, mirrors build-mecab-python3.yml (system libmecab built from taku910/mecab source).'
- pkg: cmeel-console-bridge
  version: 1.0.2.3
  home: https://github.com/cmake-wheel/cmeel-console-bridge
  repo: https://github.com/cmake-wheel/cmeel-console-bridge
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2152
  notes: '6 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 0" is PEP 427''s build number, not an ABI tag (gotcha 470): the real
    filenames are cmeel_console_bridge-1.0.2.3-0-py3-none-<plat>.whl, and [tool.cmeel]
    has-sitelib = false means there is no Python extension at all -- the wheel is
    just cmeel.prefix/{lib/libconsole_bridge.so.1.0,include/console_bridge/*.h} plus
    the CMake/pkg-config files -- so ONE cibuildwheel job (only: cp312-manylinux_riscv64)
    covers every interpreter. Source is the console_bridge submodule (one console.cpp
    of logging shim, no SIMD/asm/-march anywhere), so portability was never in question.
    Only real blocker, verified locally on x86 both ways: console_bridge''s CMakeLists.txt
    declares cmake_minimum_required(VERSION 3.0.2) while cmeel[build] resolves CMake
    >= 4, which fails with "Compatibility with CMake < 3.5 has been removed" already
    at the metadata stage; CMAKE_POLICY_VERSION_MINIMUM=3.5 in CIBW_ENVIRONMENT fixes
    it (gotcha 257''s env-var form, not a -D flag, because the cmeel backend owns
    the configure line -- no patch needed, and gotcha 358''s PATH shadowing does not
    apply since no older cmake is wanted). build-cmeel-boost.yml was the template:
    CIBW_REPAIR_WHEEL_COMMAND: "" (cmeel sets its own $ORIGIN RPATH into the shared
    prefix), upstream tag v1.0.2.3.c0, submodules: true. No PIP_EXTRA_INDEX_URL needed
    -- cmeel[build]''s whole closure (cmeel, cmake 4.4.3, wheel, packaging, git-archive-all)
    is py3-none-any or has riscv64 wheels on public PyPI. No LICENSE file ships in
    upstream''s own wheels (PEP 639 License-Expression: Zlib, although console_bridge''s
    sources are BSD-3-Clause), same as cmeel-boost, so no licensing patch; no GPL
    linkage, so no gpl_sources job. Testing mirrors upstream''s install-the-wheel
    job plus a ctypes.CDLL load of libconsole_bridge.so on the riscv64 runner. Local
    rehearsal per gotcha 9: pip wheel on the v1.0.2.3.c0 checkout builds cmeel_console_bridge-1.0.2.3-0-py3-none-manylinux_2_39_x86_64.whl
    in under a minute with the same contents as upstream''s published wheel, fails
    with the CMake error without the variable, and the installed wheel''s library
    loads. Landing this plus cmeel-tinyxml2 unblocks cmeel-urdfdom, itself one of
    gotcha 470''s 11 packages under pin. PR #2152 GREEN on the first run (35566532569,
    6 minutes end to end): cmeel_console_bridge-1.0.2.3-0-py3-none-manylinux_2_39_riscv64.whl
    built, contents assertion and the riscv64 ctypes load both passed, publish dry-ran
    clean (the dry-run diff adds only tag/files, no per-version license override,
    confirming license: Zlib in the package YAML matches the wheel''s License-Expression).
    Awaiting review; draft status untouched. Merged (#2152). Push-triggered build+publish on main run 35573768633 succeeded. 404 on pypi.riseproject.dev/simple/cmeel-console-bridge/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: intel-openmp
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  status: parked
  pr: null
  notes: 'Not feasible: intel-openmp is Intel''s own closed, precompiled OpenMP runtime from the
    oneAPI DPC++/C++ Compiler -- the PyPI package is a redistribution of Intel''s x86_64 binaries,
    there is no source to build, no non-x86 build from Intel at any layer, and the Intel EULA the
    wheel ships bars republishing one even if there were (new gotcha 506,
    feasibility-and-triage.md; same closed-vendor-blob family as gotchas 453/465, and the explicit
    counter-case to gotcha 263''s tbb rescue). Verified against the real artifacts, not inferred.
    PyPI: 158 files across 50 releases and 0 sdists ever (gotcha 431 -- the wheel is the only
    evidence); tags are only manylinux1_i686, manylinux1_x86_64, manylinux_2_28_x86_64, win32,
    win_amd64 and macosx_10_{12,15}_x86_64 -- never aarch64, never riscv64; home and repo are both
    Intel''s DPC++ product page; metadata says License: Intel End User License Agreement for
    Developer Tools plus classifier License :: Other/Proprietary License; and the description
    itself reads "Intel OpenMP* Runtime Library x86_64 dynamic libraries for Linux*". WHAT IS IN
    THE WHEEL: intel_openmp-2026.1.1-py2.py3-none-manylinux_2_28_x86_64.whl (52.5 MB) has an EMPTY
    top_level.txt and zero Python modules -- every file is under .data/data/{lib,opt/compiler}:
    prebuilt .so/.a/.o/.bc plus omp.h and a pkgconfig file. No build system, no sources, nothing to
    compile; readelf -h reports Machine: Advanced Micro Devices X86-64 for every library. WHY
    GOTCHA 263''S tbb RESCUE DOES NOT APPLY: for tbb the PyPI version 2023.1.0 matched Apache-2.0
    oneTBB tag v2023.1.0 exactly, so the open project could be built instead. Here 2026.1.1 is an
    Intel compiler release number with no open tag, and the payload is mostly not open at all --
    libomptarget.so is 131.8 MB of the 141 MB unpacked (93%) and its DT_NEEDED list is libimf.so,
    libsvml.so, libirng.so, libintlc.so.5 (Intel''s closed compiler runtimes, shipped only by the
    equally x86_64-only intel-cmplr-lib-rt) plus libur_loader.so.0 from the hard-pinned
    Requires-Dist intel-cmplr-lib-ur==2026.1.1 (also closed, also x86_64/win_amd64 only, also 0
    sdists); SVML is an x86 SIMD vector-math library by construction and none of the four has
    public source. Only libiomp5.so (2.8 MB, 2%) has an open ancestor, LLVM''s openmp runtime,
    which does support riscv64 (LibompGetArchitecture.cmake has a __riscv && __riscv_xlen == 64
    arm) -- but building that yields LLVM libomp under a different name, not this distribution,
    and riscv64 manylinux already ships GCC''s libgomp. libiomp5''s own banner names the closed
    toolchain ("build compiler: Intel(R) oneAPI DPC++/C++ Compiler 2024.2.0", "build time
    2026-02-13") and its topology layer is x86-only (hwloc topology-x86.c, cpuid leaf 11, AVX512*
    feature strings). GOTCHA 465''S INVERTED TELL REPEATS: strings -a libomptarget.so | grep -ci
    riscv is 1106, but those are LLVM BinaryFormat/TargetParser tables (R_RISCV_*, EF_RISCV_*,
    RISCVISAInfo.cpp) from a bundled JIT -- the ELF is x86-64, the only registered LLVM target is
    LLVMInitializeNVPTX*, libiomp5.so itself has 0 riscv strings, and the offload backends are
    Intel GPU ones (level_zero, OpenCL, spir64). SECOND VENDOR INDEX AGREES: apt.repos.intel.com
    /oneapi dists/all/Release says Architectures: all amd64 i386 i686 -- Intel builds oneAPI for no
    non-x86 architecture at all, not even aarch64. LICENCE: the Intel EULA for Developer Tools
    (August 2024) section 3.1 forbids (i) distributing or publicly displaying the Materials, (iv)
    modifying/adapting/translating them and (v) reverse engineering them; the only distribution
    grant (2.1.D) covers "Redistributables" in Executable Code "only as part of Your Product" under
    a licence that itself prohibits reverse engineering -- rehosting the wheel on
    pypi.riseproject.dev is none of those. NO CONSUMER IMPACT: nothing in this repo or on our index
    needs it (intel-openmp, mkl and intel-cmplr-lib-ur all 404 on pypi.riseproject.dev); its only
    PyPI consumer is mkl, itself an x86_64-only Intel blob (queued, same park applies), and this
    repo''s OpenMP/MKL consumers already take the non-Intel path on riscv64 -- ctranslate2 builds
    WITH_MKL=OFF with OPENMP_RUNTIME=COMP (GNU libgomp), clarabel swaps the mkl-backed pardiso, scs
    links OpenBLAS. The queued siblings intel-cmplr-lib-{ur,rt}, intel-cmplr-lic-rt, intel-sycl-rt,
    intel-opencl-rt, dpcpp-cpp-rt, mkl, mkl-static and mkl-include are the same family on the same
    evidence. No worktree, branch or PR created.'
- pkg: quantlib
  version: '1.43'
  home: https://www.quantlib.org
  repo: https://github.com/lballabio/QuantLib-SWIG
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2161
  notes: 'SWIG-generated Python binding for the QuantLib C++ library (PyPI name QuantLib, sources at lballabio/QuantLib-SWIG). No sdist is published and the git tree carries no generated wrapper, so the workflow mirrors upstream wheels.yml make-tarballs job: SWIG 4.4.1 built from source on x86 makes both QuantLib and QuantLib-SWIG dist tarballs, and cibuildwheel on riscv64 runs upstream own .ci/wheels/before_all_linux{,_nogil}.sh to install Boost 1.90 headers and build libQuantLib. No SIMD, intrinsics or arch guards anywhere in ql/ (974 .cpp files, portable C++17). Pre-flight on x86: the 791k-line quantlib_wrap.cpp compiles clean against QuantLib 1.43 + Boost 1.90 headers with Py_LIMITED_API=0x03090000 in 588s at -O3, so upstream -O3 flags are kept rather than dropping to the -O0 it uses for i686. setup.py writes py_limited_api=cp39, so the matrix is cp39-abi3 (built on cp39, re-tested on cp312/cp313/cp314) plus cp314t against a thread-safe-observer libQuantLib. manylinux only; PyPy and musllinux dropped. CI green on PR #2161: the abi3 wheel built on cp39 in 2h (quantlib-1.43-cp39-abi3-manylinux_2_39_riscv64.whl, 19.9MB) and was re-tested in ~55s each on cp312/cp313/cp314 via find_compatible_wheel; cp314t built in 1h; 211 passed, 1 skipped on every leg; abi3audit --strict clean; publish dry-run resolves both wheels.'
- pkg: openexr
  version: 3.4.15
  home: https://openexr.com
  repo: https://github.com/AcademySoftwareFoundation/OpenEXR
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1691
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zope-hookable
  version: '8.3'
  home: https://zopehookable.readthedocs.io
  repo: https://github.com/zopefoundation/zope.hookable
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1108
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #1108 open, CI running. Merged (#1108). Issue #1137 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: openai-codex-cli-bin
  version: 0.149.0
  home: https://github.com/openai/codex
  repo: https://github.com/openai/codex
  status: parked
  pr: null
  notes: 'Parked (gotcha 509, feasibility-and-triage.md; gotcha 35/157 vendored-binary shape). openai-codex-cli-bin is wheel-only by construction -- sdk/python-runtime/hatch_build.py raises "openai-codex-cli-bin is wheel-only" on any sdist target -- and the wheel compiles nothing: hatchling just stamps py3-none-<plat> onto a tree of prebuilt binaries staged by sdk/python/scripts/update_sdk_artifacts.py stage-runtime from a codex-package-<target>.tar.gz release asset. Read the 114 MB manylinux_2_17_aarch64 wheel''s zip central directory with a curl range request instead of downloading it: 5 payloads -- bin/codex (222 MB), bin/codex-code-mode-host (53 MB), codex-path/rg (ripgrep), codex-resources/bwrap, codex-resources/zsh/bin/zsh -- plus a 1 KB __init__.py. All four vendor artifacts 404 for riscv64 (verified by HTTP): codex-package-riscv64gc-unknown-linux-gnu.tar.gz at openai/codex rust-v0.149.0, ripgrep-15.2.0-riscv64gc-unknown-linux-gnu.tar.gz (the dotslash manifest scripts/codex_package/rg pins linux-x86_64/linux-aarch64 only), codex-zsh-riscv64gc-unknown-linux-gnu.tar.gz at the sha256-pinned rust-v0.134.0-alpha.3 zsh release, and librusty_v8_ptrcomp_sandbox_release_riscv64gc-unknown-linux-gnu.a.gz at openai/codex rusty-v8-v150.4.0. rust-release.yml''s Linux matrix is x86_64/aarch64 musl only. The wall is bin/codex-code-mode-host, which cannot be dropped: update_sdk_artifacts.py::_validate_codex_package_layout hard-fails unless bin/codex, bin/codex-code-mode-host, codex-package.json, bin/, codex-resources/ and codex-path/ are all present. That binary is codex-code-mode-host -> codex-code-mode-runtime -> v8 = "=150.4.0" with features = ["v8_enable_sandbox"]. Gotcha 335 noted rusty_v8 gained riscv64 prebuilts from v150.1.0, and 150.4.0 is inside that range -- but the feature set picks the asset name: denoland/rusty_v8 v150.4.0 serves librusty_v8_release_riscv64gc-unknown-linux-gnu.a.gz (302) yet has no _ptrcomp_sandbox asset for ANY target (x86_64 is a 404 too); that profile only exists in openai''s own rusty-v8-v* tags, x86_64/aarch64. Dropping the sandbox feature to use the plain riscv64 prebuilt would ship codex''s model-driven JS code-mode runtime with V8''s sandbox off on riscv64 alone -- a security-posture divergence, not a patch. Building that V8 ourselves has no riscv64-host path: rusty_v8''s V8_FROM_SOURCE gn branch for riscv64 is cross-only (use_sysroot=true + Chromium trixie_riscv64 AND amd64 sysroots) and its unconditional bootstrap downloads are x86_64-only (tools/clang/scripts/update.py _HOST_OS_URL_MAP linux -> Linux_x64; third_party/rust-toolchain in tools/v8_deps.py has one linux object, Linux_x64/...), while gn/ninja are NOT the gap (gn/gn/linux-riscv64 and infra/3pp/tools/ninja/linux-riscv64 both resolve in CIPD). Upstream''s own V8 build (rusty-v8-release.yml) is Bazel with --platforms=@llvm//platforms:<p> from the hermetic llvm BCR module 0.8.11, whose platforms/common.bzl lists (linux, riscv64) in SUPPORTED_TARGETS but NOT in SUPPORTED_EXECS -- riscv64 is a cross target, never an exec platform -- and V8''s own BUILD.bazel annotates its riscv64 sources "NOTE: Bazel rules for riscv64 weren''t tested on a real system". Revisit path if it ever becomes worth the cost: an x86_64-hosted cross pipeline that (1) Bazel-builds rusty_v8 ptrcomp+sandbox for @llvm//platforms:linux_riscv64, (2) cross-builds the ~1000-crate codex-rs workspace to riscv64gc-unknown-linux-gnu (upstream needs zig + AWS_LC_SYS_NO_JITTER_ENTROPY even for musl aarch64), (3) builds ripgrep 15.2.0 and zsh from source and patches scripts/codex_package/targets.py TARGET_SPECS plus the sha256-pinned zsh manifest check, then (4) stages with update_sdk_artifacts.py and hatchling -- i.e. every payload built by a toolchain upstream never uses, the opposite of this repo''s mirror-upstream''s-CI goal. Diagnosed read-only: shallow blobless checkout of openai/codex at rust-v0.149.0 plus the v8 150.4.0 crate under .git/pw-scratch (removed); no worktree/branch/PR created.'
- pkg: pyfarmhash
  version: 0.5.1
  home: https://github.com/veelion/python-farmhash
  repo: https://github.com/veelion/python-farmhash
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1113
  notes: 'PR #1113 open, CI green (8/8 manylinux+musllinux jobs, publish dry-run OK). Upstream cuts releases with no git tags and never commits its version bump; pinned release commit c83451a9 (byte-identical sources to the 0.5.1 sdist) and patched setup.py VERSION from (0,4,0) to (0,5,1) - see patches/pyfarmhash/0.5.1/. No upstream test suite; CIBW_TEST_COMMAND exercises every bound hash/fingerprint function. Worktree at .claude/worktrees/pyfarmhash, branch pyfarmhash. Merged (#1113). Issue #1132 linked, publish dispatched on main. Not yet confirmed on the registry.'
- pkg: sgl-deep-gemm
  version: 0.1.5.post3
  home: https://github.com/sgl-project/DeepGEMM/tree/release
  repo: https://github.com/sgl-project/DeepGEMM/tree/release
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Not feasible: NVIDIA-CUDA-only JIT GEMM kernel library (SGLang''s fork of DeepSeek''s DeepGEMM) -- the shipped wheel''s compiled core links libcudart/libnvrtc/libcublasLt plus the CUDA half of torch, its kernels are sm90/sm100/sm120 CUDA headers that NVRTC compiles on the user''s GPU at first call, and importing the package asserts on CUDA_HOME. Same disposition and class as the parked humming-kernels, sglang-kernel, nvshmem4py-cu13, nccl4py, torch-memory-saver, cupy-cuda12x and onnxruntime-gpu entries (gotchas 41/284/386/387/452/459/471); gotcha 386''s JIT-kernel-library shape is the exact match here, so no new gotcha number is minted. All evidence below is read off the real tag and the real 0.1.5.post3 wheels, not inferred from the name. PROVENANCE. The queue''s home/repo .../tree/release is not a branch (git ls-remote has no refs/heads/release); the fork branches per minor release (release/v0.1.0 .. release/v0.2.0) and tags each wheel version, and refs/tags/v0.1.5.post3 -> fa3a5ca0 is exactly this entry''s version (gotcha 3 is therefore a non-issue). Evidence below comes from a shallow checkout of that tag, from sgl-project/sglang''s release workflow and build scripts (the wheel is published from sglang, not from DeepGEMM''s own CI), and from the two released wheels. (1) BUILD WALL, unconditional. CMakeLists.txt line 3 is project(deep_gemm LANGUAGES CXX CUDA), followed by find_package(CUDAToolkit REQUIRED), find_package(pybind11 REQUIRED) and find_package(Torch REQUIRED) with no if(), no backend selector and no CPU member -- the opposite of gotcha 411''s bitsandbytes. It is only the IDE indexing file, and the file it exists for says why: ''the real compilation is done via JIT''. grep -ril riscv over the whole checkout returns zero hits. The wheel''s actual recipe is build_sgl_deep_gemm.sh, which compiles csrc/tvm_ffi_api.cpp through tvm_ffi.cpp.build with extra_ldflags = -lcudart -lnvrtc -lcublasLt -lcublas -ltorch -ltorch_cpu -lc10 -lc10_cuda -ltorch_cuda and extra_include_paths starting at ${CUDA_HOME}/include, after calling cuda_helpers.find_cuda_home(). (2) UPSTREAM''S RELEASE RECIPE IS A GPU-VENDOR MATRIX (gotcha 471''s shape). sglang''s .github/workflows/release-whl-deepgemm.yml has one build matrix, cuda-version [13.0] x arch [x86_64, aarch64], and its only target choices are ''all'' and ''cu130''. scripts/build_sgl_deep_gemm.sh rejects any CUDA_VERSION but 13.0 and its entire arch switch is aarch64 -> BASE_IMG=pytorch/manylinuxaarch64-builder, else pytorch/manylinux2_28-builder, always FROM ${BASE_IMG}:cuda${CUDA_VERSION}; docker/sgl-deep-gemm.Dockerfile then symlinks /usr/local/cuda-13.0/targets/${sbsa|x86_64}-linux/lib/stubs/libcuda.so and pip installs torch==2.13.0 from download.pytorch.org/whl/cu130. aarch64 exists only because NVIDIA ships an sbsa toolkit and PyTorch a CUDA aarch64 builder image; neither exists for riscv64. NVIDIA''s CUDA 13 redist manifest (redistrib_13.0.0.json) lists exactly linux-x86_64, linux-sbsa, linux-all and windows-x86_64 and contains zero riscv strings, so there is no nvcc, no nvrtc and no libcuda.so.1 for the architecture. (3) THE ''abi: py3'' IN THE QUEUE NOTE IS NOT PORTABILITY (gotchas 81/145). Both wheels are py3-none-manylinux2014_{x86_64,aarch64} with Root-Is-Purelib: true, yet 883 entries in and the third-largest file is deep_gemm/_C.so (2.65 MB x86_64, 2.26 MB aarch64) -- a real ELF shared object. The rename step is explicit about it: sglang''s scripts/rename_sgl_deep_gemm_whl.sh takes the build''s sgl_deep_gemm-<v>-py3-none-any.whl and rewrites filename, METADATA Version and WHEEL tag to +cu130 / manylinux2014_<arch>. The two oldest releases (0.0.1rc0, 0.0.1rc1) predate that step and are still published as py3-none-any -- and 0.0.1rc1 carries the same deep_gemm/_C.so, so ''installs anywhere'' would mean ''installs and then fails at import''. The py3 tag is tvm-ffi''s ABI-stable FFI, not architecture independence (the README sells exactly that: ''a single wheel can run on different python versions''). (4) LINKAGE, read with readelf on both released wheels. deep_gemm/_C.so has DT_NEEDED libtvm_ffi.so, libcudart.so.13, libnvrtc.so.13, libcublasLt.so.13, libcublas.so.13, libtorch.so, libtorch_cpu.so, libc10.so, libc10_cuda.so and libtorch_cuda.so, with undefined nvrtcCreateProgram/nvrtcCompileProgram/nvrtcGetCUBIN, cublasLtCreate/cublasLtMatmul/cublasLtMatmulAlgoGetHeuristic, cudaGetDeviceProperties/cudaMemsetAsync, at::cuda::getCurrentCUDABlasLtHandle and c10::cuda::getCurrentCUDAStream. Identical on both arches, i.e. there is no small CPU end to the wheel-size split (gotcha 411''s tell is absent by construction). (5) THE PAYLOAD IS CUDA SOURCE FOR RUNTIME JIT (gotcha 386''s shape). The bulk of the wheel is NVIDIA CUTLASS/CuTe headers (deep_gemm/include/cute, deep_gemm/include/cutlass; third-party/cutlass is a submodule of github.com/NVIDIA/cutlass) plus 57 .cuh kernels under deep_gemm/include/deep_gemm/impls, every one named sm90_*, sm100_* or sm120_* (Hopper/Blackwell), 11 of them carrying inline PTX asm volatile. csrc/jit/compiler.hpp compiles those at runtime through an NVRTCCompiler (asserting NVRTC >= 12.3, emitting cubin/PTX for the probed compute capability) or an NVCCCompiler that shells out to ${CUDA_HOME}/bin/nvcc and ${CUDA_HOME}/bin/cuobjdump. csrc/jit/device_runtime.hpp includes cublasLt.h and ATen/cuda/CUDAContext.h and allocates its 32 MB workspace as torch::empty(..., at::kCUDA). (6) PURE-PYTHON IMPORT GATE, reproduced on this CUDA-less x86_64 host (gotcha 452). deep_gemm/__init__.py imports torch and tvm_ffi at module level and ends with _C.init(<pkg dir>, find_cuda_home()); deep_gemm/cuda_helpers.py::find_cuda_home() reads CUDA_HOME/CUDA_PATH, else shutil.which(''nvcc''), else /usr/local/cuda, then assert cuda_home is not None -- executing that one module file gives AssertionError at cuda_helpers.py:20 with nvcc absent and /usr/local/cuda missing. get_cuda_arch() shells out to nvidia-smi --query-gpu=compute_cap and falls back to ''9.0'' (Hopper). There is no lazy path and no except: the same file''s _load_module() *rebuilds* _C.so via tvm_ffi.cpp.build against ${CUDA_HOME} when it is missing, so even a hypothetical stripped wheel would try to compile CUDA at import time rather than degrade. (7) OUR riscv64 TORCH CAN NEVER SATISFY IT (gotcha 386''s libtorch_cuda wall, gotcha 40). pypi.riseproject.dev serves torch 2.13.0+cpu and 2.14.0+cpu for riscv64; listing torch/lib inside torch-2.14.0+cpu-cp312-cp312-manylinux_2_39_riscv64.whl gives libtorch_cpu.so, libtorch.so, libtorch_python.so, libc10.so, libtorch_global_deps.so and libtorchbind_test.so -- no libtorch_cuda.so and no libc10_cuda.so, so neither the -lc10_cuda -ltorch_cuda link line nor the DT_NEEDED entries above can resolve. sglang''s own test job force-reinstalls torch==2.13.0 from the cu130 index for exactly this reason. Note the declared dependency is NOT the blocker: apache-tvm-ffi==0.1.11 is the wheel''s only Requires-Dist and pypi.riseproject.dev already serves apache-tvm-ffi riscv64 wheels (0.1.12 .. 0.1.14.post0) -- torch''s CUDA half and the CUDA toolkit are. (8) UNTESTABLE ON riscv64 (gotcha 6). sgl_deep_gemm/run_tests.sh, the pre-release gate, does NUM_GPUS=$(nvidia-smi -L | wc -l) and exits 1 with ''No GPUs visible to nvidia-smi -- DeepGEMM tests require a GPU'', then branches on compute capability 9/10/12; the tests themselves reference CUDA devices 267 times. Upstream''s test matrix runners are 8-gpu-h200, 8-gpu-b200 and 4-gpu-gb300. NO SDIST, EVER: all 29 releases (0.0.1 .. 0.2.0) are wheels only -- 27 of them exactly 2 files, the 2 oldest rc''s one py3-none-any file -- so there is nothing to build from PyPI either. VERSION (gotcha 388): the queue snapshot 0.1.5.post3 is kept rather than bumped; latest is 0.2.0 (2026-09-14) and is identically blocked -- at tag v0.2.0 the CMakeLists is still project(deep_gemm LANGUAGES CXX CUDA) with find_package(CUDAToolkit REQUIRED), sgl_deep_gemm/cuda_helpers.py still ends find_cuda_home() with assert cuda_home is not None, sgl_deep_gemm/pyproject.toml still carries classifier Environment :: GPU :: NVIDIA CUDA, and it still ships 2 manylinux2014 wheels and no sdist. Registry checked: pypi.riseproject.dev/simple/sgl-deep-gemm/ -> 404 (controls: .../torch/ and .../apache-tvm-ffi/ -> 200). No workflow, docs/packages yaml, patches, worktree, branch or PR created, and nothing to rehearse locally since the build cannot start without a CUDA toolkit. Revisit only if NVIDIA ships a riscv64 CUDA toolkit and driver and a CUDA-enabled riscv64 torch exists.'
- pkg: pytype
  version: 2024.10.11
  home: https://google.github.io/pytype
  repo: https://github.com/google/pytype
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2168
  notes: 'PR #2168 open (branch pytype), CI green. Build-from-checkout with cibuildwheel, mirroring the manylinux_wheels job of upstream build.yml (which is just `pip wheel . --no-deps` + `auditwheel repair` inside the manylinux image); setup.py builds one pybind11 extension, pytype.typegraph.cfg, from six .cc files at cxx_std=20, no arch-specific code anywhere and no CMake/ninja involvement in the wheel build (ninja is only a runtime dep and has riscv64 wheels on PyPI). No patches needed. Matrix is cp312 only: pytype refuses to run under an interpreter past 3.12 (validate_version: "Python versions > 3.12 are not yet supported"), which is why upstream ships cp310-cp312; the extension itself compiles and imports fine on cp313, so this only shows up when the tool actually runs (new gotcha 514). Dependency closure resolves binary-only for riscv64 at cp312 with our registry in play - libcst 1.8.6 and msgspec 0.21.1 come from pypi.riseproject.dev, the rest from PyPI. First CI run failed in the test phase only: pip picked libcst 1.9.0, whose riscv64 form is an sdist needing Rust, over our registry''s 1.8.6 wheel; fixed with CIBW_TEST_ENVIRONMENT PIP_ONLY_BINARY=:all: (gotchas 12/67). Testing runs the wheel''s own 96 *_test.py modules via `pytest --pyargs pytype -n auto` plus a one-file `pytype` CLI run; the pytype.tests helpers those modules import are excluded from the wheel, so three of them are staged with CIBW_TEST_SOURCES and copied into the installed package (new gotcha 513), and nine nodeids are deselected - source-tree-only tests (main_test.py, test_error_doc, two typeshed ones), a root-only one (test_die), two serialize_ast tests that fail identically on a pristine x86 checkout of the same tag, and the two staged helpers themselves. --ignore-glob turned out to be a no-op under --pyargs, which cost the first CI cycle''s triage (new gotcha 512). Rehearsed end to end on x86_64 before pushing (same 1746 passed / 22 skipped as CI). riscv64 CI: wheel built and auditwheel-repaired to manylinux_2_39_riscv64 in ~7 min, 1746 passed / 22 skipped in 8 min, publish job dry-ran cleanly. PR is open (not a draft - the create call''s draft flag did not take, and flipping draft status is not ours to do); not merged, not published.'
- pkg: pytorch-tokenizers
  version: 1.4.1
  home: https://pytorch.org/executorch/
  repo: https://github.com/pytorch/executorch
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2164
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Sources are meta-pytorch/tokenizers (the standalone split-out of executorch extension/llm/tokenizers), not the executorch monorepo; PyPI publishes no sdist, so the workflow builds from the v1.4.1 tag with submodules. setuptools + CMake + pybind11: one extension, pytorch_tokenizers_cpp, statically linking the abseil-cpp, re2, sentencepiece (with its own absl/darts-clone/esaxx/protobuf-lite), PCRE2 (SUPPORT_REGEX_LOOKAHEAD=ON) and nlohmann/json submodules plus the vendored llama.cpp unicode tables; pybind11 v2.13.6 comes from CMake FetchContent at configure time. No arch-specific code anywhere in the tree and no ExecuTorch runtime; a local x86 pip wheel build from the tag succeeded unchanged (build isolation pulls cmake 4.4.3, and every CMake floor in the tree is >= 3.5 with CMAKE_POLICY_VERSION_MINIMUM 3.5 already set by upstream), and the full 17-test suite passed against that wheel. Matrix is cp312/cp313/cp314: the wheel requires tokenizers, which publishes cp39-abi3 wheels only (packaging excludes abi3 from free-threaded builds), and upstream ships no cp314t wheel either. Runtime deps tiktoken/tokenizers/sentencepiece and test deps transformers -> safetensors/regex/numpy all resolve from pypi.riseproject.dev. Test phase mirrors upstream .ci/scripts/wheel/test_wheel.py via CIBW_TEST_SOURCES: test. Vendored licences are staged at the project root so the wheel carries them (upstream ships only its own LICENSE); a post-build step asserts the exact licence set and the .so. CI green on PR #2164 (draft): 3/3 riscv64 wheels built and tested (manylinux_2_39_riscv64, ~24 min each, 17 passed 0 skipped on every leg, including the transformers tests that fetch tokenizer files from huggingface.co), licence/.so assertion green, publish dry-run clean. Worktree .claude/worktrees/pytorch-tokenizers, branch pytorch-tokenizers. Awaiting review.'
- pkg: test-results-parser
  version: 0.6.1
  home: https://github.com/codecov/test-results-parser
  repo: https://github.com/codecov/test-results-parser
  status: published
  pr: 1218
  notes: pyo3/maturin, no abi3, one wheel per interpreter (cp312/cp313/cp314/cp314t). Upstream licence is FSL-1.1-Apache-2.0 (Sentry Functional Source License); wheel bundles it via maturin default LICENSE glob. CI green (4/4 riscv64 wheels built and tested), publish dry-run clean; awaiting review.
- pkg: pysqlite3-binary
  version: 0.5.4.post2
  home: https://github.com/coleifer/pysqlite3
  repo: https://github.com/coleifer/pysqlite3
  status: published
  pr: 1498
  notes: No sdist and no matching git tag for 0.5.4.post2 (rebuild of the 0.5.4 tag's source under the pysqlite3-binary name); build-pysqlite3-binary.yml checks out 0.5.4 and patches setup.py to restore the rename and force the amalgamation (self-contained, FTS5/JSON1) build_ext, mirroring upstream's now-removed build-scripts/_build_wheels.sh. CI green (4/4 riscv64 wheels built and tested), publish dry-run clean; awaiting review.
- pkg: systemrdl-compiler
  version: 1.32.2
  home: http://systemrdl-compiler.readthedocs.io
  repo: https://github.com/SystemRDL/systemrdl-compiler
  status: ci-green
  pr: 2162
  notes: 'Real compiled package despite the pure-Python look: setup.py builds the bundled ANTLR4 C++ runtime into systemrdl/parser/sa_systemrdl_cpp_parser, Py_LIMITED_API 0x03070000, so every upstream wheel is cp37-abi3. setup.py swallows a compile failure and falls back to a pure-Python wheel unless SYSTEMRDL_REQUIRE_BINARY_BUILD is set; upstream sets it as a step env on the cibuildwheel action, which never reaches a Linux container, so it is promoted to CIBW_ENVIRONMENT. Single abi3 build job (cp312 builds in 22min, cp313/cp314 reuse+retest); upstream ships no free-threaded wheel so no cp314t leg. All runtime/test deps (antlr4-python3-runtime, colorama, markdown, typing-extensions, pytest, parameterized) are py3-none-any. First CI run failed 14 tests with "Can''t locate Safe.pm in @INC": the RDL Perl preprocessor shells out to ppp_runner.pl which does "use Safe", and the image ships only a minimal perl - fixed with CIBW_BEFORE_TEST: dnf -y install perl-Safe (gotcha 515). CI green: 243 passed on each of cp312/cp313/cp314, publish dry-run lists systemrdl_compiler-1.32.2-cp37-abi3-manylinux_2_39_riscv64.whl. PR https://github.com/riseproject-dev/python-wheels/pull/2162 open, awaiting review/merge. CI concluded all-green (9/9 checks, cp37-abi3 build + publish dry-run success, run 35598844640); PR #2162 marked ready for review.'
- pkg: mgrs
  version: 1.5.4
  home: https://github.com/hobuinc/mgrs/
  repo: https://github.com/hobuinc/mgrs/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1389
  notes: 'PR #1389 open. Small self-contained C extension (libmgrs, ports GeoTrans 2.4.2), loaded via ctypes by the pure-Python mgrs wrapper; no native deps. build-from-checkout shape mirroring deploy.yml. Enabled the test-command upstream ships commented out: tests/test.txt is a doctest that pytest''s default --doctest-glob (test*.txt) already collects.'
- pkg: python-olm
  version: 3.2.16
  home: https://gitlab.matrix.org/matrix-org/olm/-/tree/master/python
  repo: https://gitlab.matrix.org/matrix-org/olm/-/tree/master/python
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2169
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Ordinary sdist->bdist port, no blockers. gitlab.matrix.org is reachable from CI, but actions/checkout cannot use it, so both jobs git clone --depth 1 --branch <version> instead. The sdist comes from upstream own python/make_sdist.sh (preprocesses the public headers, vendors the libolm tree into libolm/, applies packaging.diff); the file list of the sdist it produces is identical to the released PyPI sdist. cibuildwheel then builds that extracted sdist on manylinux_2_39_riscv64. libolm crypto is portable C/C++11 - curve25519-donna (the 32-bit generic .c, not the __int128 c64 one), Brad Conte crypto-algorithms aes/sha256, ref10-derived ed25519 - with zero x86 intrinsics or asm; the only arch-specific code in the tree is doctest DOCTEST_BREAK_INTO_DEBUGGER, which falls back to raise(SIGTRAP) on non-x86 Linux. Two non-obvious points. (1) libolm CMakeLists declares cmake_minimum_required(VERSION 3.4), below CMake 4 floor, and olm_build.py shells out to a fixed cmake command line, so the fix is CMAKE_POLICY_VERSION_MINIMUM=3.5 in the environment (gotcha 257); reproduced and fixed locally against cmake 4.4.3. (2) setuptools cffi_modules hook executes olm_build.py - and so the whole libolm cmake build - even for python -m build --sdist, so the sdist job on ubuntu-latest needs the same variable. Tests are not in the sdist (MANIFEST.in ships libolm/tests, never python/tests), so the wheel job clones the tag and stages python/tests via CIBW_TEST_SOURCES (gotcha 104), with package-dir pointed at the extracted directory rather than the tarball (gotchas 251/281). Test deps pytest pytest-benchmark aspectlib, run with --benchmark-disable like upstream tox; 62 tests. musllinux dropped: upstream publishes none and the registry has no musl riscv64 cffi. One patch (licensing): make_sdist.sh never copies the repo LICENSE into the sdist, so the wheel shipped no licence text at all - patch copies it and names both it and libolm/lib/curve25519-donna/LICENSE.md in setup.cfg license_files, landing them in dist-info/licenses/. crypto-algorithms and ed25519 are public domain and need no notice. No gpl_sources job: the extension NEEDED set is only libstdc++.so.6 and libc.so.6, both manylinux-whitelisted, so auditwheel vendors nothing. Rehearsed end to end on x86: sdist from a fresh patched clone, wheel built on cp312/cp313/cp314/cp314t, 62/62 tests pass on cp311 and cp314t, licences present in the wheel. Note libolm is deprecated upstream (last commit 2024-08, successor vodozemac), which is why the patch is To upstream rather than Submitted. Worktree .claude/worktrees/python-olm, branch python-olm, PR #2169 (draft). Run 35597465076: setup, the sdist job (63s on ubuntu-latest) and all three pr-checks jobs green; the four riscv64 build legs have been sitting in status queued on the self-hosted pool for 4h50m with no runner pickup (pool saturated by other packages multi-hour builds), so they have not started, let alone failed. Next step is simply to read run 35597465076 again - no push is needed and none should be made, since a push restarts the run and loses the queue position. CI concluded all-green (13/13 checks, four riscv64 legs + sdist + publish dry-run all success, run 35597465076); PR #2169 marked ready for review.'
- pkg: cmeel-tinyxml2
  version: 11.0.0
  home: https://github.com/cmake-wheel/cmeel-tinyxml2
  repo: https://github.com/cmake-wheel/cmeel-tinyxml2.git
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2151
  notes: '2 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    The "abi: 0" is PEP 427''s build number, not an ABI tag (gotcha 470): the real
    filenames are cmeel_tinyxml2-11.0.0-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl.
    [tool.cmeel] has-sitelib = false, so the wheel carries no Python extension --
    just cmeel.prefix/{include/tinyxml2.h, lib/libtinyxml2.so.11.0.0, lib/cmake,
    lib/pkgconfig} -- and one py3-none wheel covers every interpreter, hence a single
    cibuildwheel job (only: cp312-manylinux_riscv64) rather than an interpreter matrix.
    No patch needed and none expected: tinyxml2 is two portable C++ files with no
    arch-specific code, and the vendored submodule''s CMakeLists.txt declares
    cmake_minimum_required(VERSION 3.15), above CMake 4''s 3.5 floor, so the
    CMAKE_POLICY_VERSION_MINIMUM workaround its sibling cmeel-console-bridge needs
    does not apply here. Build deps all resolve for riscv64 on public PyPI (cmeel and
    git-archive-all/packaging/wheel are pure; cmake ships riscv64 wheels from 4.1.0
    on), so the extra index is only a fallback. Rehearsed locally per gotcha 9 from
    the v11.0.0.c0 checkout with submodules: pip wheel builds clean in under a minute
    (cmeel_tinyxml2-11.0.0-0-py3-none-manylinux_2_39_x86_64.whl) and the wheel''s
    libtinyxml2.so loads via ctypes, which is what the workflow runs as its
    cibuildwheel test command. Licence is Zlib, permissive; like cmeel-boost the
    wheel ships no LICENSE file (PEP 639 License-Expression only), so no licensing
    patch. Unblocks cmeel-urdfdom together with cmeel-console-bridge. Filed gotcha 487
    (cibuildwheel-matrix-and-abi3.md): has-sitelib is what tells the one-wheel cmeel shape
    from cmeel-boost''s per-interpreter one, since the queue''s "abi: 0" cannot. PR #2151
    open and GREEN on the first real run (run 35566329600 attempt 2, 5m24s): single
    cp312-manylinux_riscv64 job produced
    cmeel_tinyxml2-11.0.0-0-py3-none-manylinux_2_39_riscv64.whl (150.2 kB), the ctypes
    load of libtinyxml2.so passed in the test venv, and the publish job dry-ran clean.
    Left in draft for the maintainer, no patches. (Attempt 1 was cancelled by mistake,
    not a build failure.) Merged (#2151). Push-triggered build+publish on main run 35573543464 succeeded. 404 on pypi.riseproject.dev/simple/cmeel-tinyxml2/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: scylla-driver
  version: 3.29.11
  home: https://github.com/scylladb/python-driver
  repo: https://github.com/scylladb/python-driver/
  status: ci-green
  pr: 2165
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev. Four patches. 0001 ships the bundled libev licence, 0002 restarts the libev prepare watcher that test_watchers_are_finished leaves stopped (without it every later timer test waits forever and the job hits its timeout). 0003/0004 backport 26c201a and 03c4c96: the 3.29.11 tag ships two tests its own cassandra/cluster.py does not satisfy - _set_keyspace_for_all_pools() calls back with the last pool''s error list instead of the accumulated dict, and wait_for_schema_agreement() never validates its scope argument despite documenting ValueError for an unknown one. Both reproduce identically on x86_64 (verified locally), so neither is riscv64-specific; upstream fixed both on master 3 days and 2 weeks after the release was cut. Also listed patches/scylla-driver/** in the workflow path triggers, without which a patch-only commit produces no run (gotcha 413). CI green on run 35634589516: all four legs (cp312/cp313/cp314/cp314t) built and passed the full unit suite plus the gevent reactor test, publish dry-run clean. Learning recorded as gotcha 535.'
- pkg: adbc-driver-sqlite
  version: 1.12.0
  home: https://arrow.apache.org/adbc/
  repo: https://github.com/apache/arrow-adbc
  status: published
  pr: 1740
  notes: 'Compiles the SQLite-based ADBC driver (c/driver/sqlite, C++ over libsqlite3) into libadbc_driver_sqlite.so, loaded via ctypes as a py3-none wheel. libsqlite3 comes from the manylinux images sqlite-devel instead of vcpkg (no riscv64 binary cache); py3-none-any wheel retagged and auditwheel-repaired the way upstreams own relocate script does, same shape as adbc-driver-postgresql. No cibuildwheel - image driven directly. CI green: build, 14/14 tests passed (SQLite is embedded, no external service needed), publish dry-run clean. PR #1740.'
- pkg: pyswisseph
  version: 2.10.3.2
  home: https://astrorigin.com/pyswisseph
  repo: https://astrorigin.com/pyswisseph
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1391
  notes: 'AGPL-3.0 (vendored libswe dual AGPL/commercial, swephelp GPL-2.0-or-later); two patches (buffer overflow fix, license_files). CI green: all 8 manylinux/musllinux x cp312/cp313/cp314/cp314t jobs passed, publish dry-run clean.'
- pkg: pycld2
  version: '0.42'
  home: https://github.com/aboSamoor/pycld2
  repo: https://github.com/aboSamoor/pycld2
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1495
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout cibuildwheel, both manylinux and musllinux; no tag exists for the 0.42 release so the workflow pins commit bc9d269 (tagged v0.3.0), proven byte-identical to the sdist.'
- pkg: xatlas
  version: 0.0.11
  home: https://github.com/mworchel/xatlas-python
  repo: https://github.com/mworchel/xatlas-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1743
  notes: 'PR #1743 green: cp312/cp313/cp314/cp314t manylinux_riscv64 wheels built (4 tests passed each), publish dry-ran cleanly. pybind11 bindings for jpcy/xatlas (mesh UV-unwrapping); build-from-checkout mirrors upstream''s wheels.yml almost exactly (scikit-build-core + cibuildwheel, no patches needed). Dropped musllinux: numpy/scipy (test-only deps) have no musl riscv64 wheel on our registry. Upstream wheel ships only its own LICENSE despite compiling in jpcy/xatlas (MIT) and pybind11 (BSD-3-Clause); workflow stages both at the root so scikit-build-core bundles them too.'
- pkg: rumdl
  version: 0.2.62
  home: https://github.com/rvben/rumdl
  repo: https://github.com/rvben/rumdl.git
  status: published
  pr: 1741
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: complexipy
  version: 8.0.0
  home: https://github.com/rohaquinlop/complexipy
  repo: https://github.com/rohaquinlop/complexipy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1164
  notes: 'maturin/PyO3 extension (Cargo workspace, complexipy-core/-cli/-python, pulls ruff_python_parser/ruff_python_ast from astral-sh/ruff.git@0.12.9); per-interpreter build (no abi3 feature on pyo3), cibuildwheel matrix cp312/cp313/cp314/cp314t manylinux_riscv64 (musllinux dropped: no riscv64 musl host toolchain from rustup.rs). Cargo.lock is committed upstream, so no floating-deps trap. Built/tested clean locally (uv build + pytest, 114 passed) before push. Tests mirror upstream''s quick-tests job: pytest suite plus CLI exclude/ignore-pattern validation against tests/src. PR open, CI running.'
- pkg: psd-tools
  version: 1.19.0
  home: https://psd-tools.readthedocs.io/
  repo: https://github.com/psd-tools/psd-tools
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1815
  notes: 'Cython extension (psd_tools.compression._rle, RLE codec) built via cibuildwheel from pyproject.toml [tool.cibuildwheel] config; setup.py forces the limited API (Py_LIMITED_API 0x030D0000) for cp313+ non-free-threaded non-Windows, and tools/configure_abi_tag.sh (a cibuildwheel override for cp3{13,14}-{manylinux,musllinux,macos}*) tags bdist_wheel py_limited_api=cp313, so PyPI ships cp310/cp311/cp312 per-interpreter plus one cp313-abi3 wheel (covers cp313/cp314 GIL-ful) plus a separate cp314t. Matrix: cp312 (real), cp313-abi3 (build: cp313+cp314, reused via find_compatible_wheel), cp314t. musllinux dropped: numpy (core runtime dep) has no musllinux riscv64 wheel on either index. Upstream cibuildwheel config sets test-skip="*" (its own tests run separately via uv+pytest against checkout); override CIBW_TEST_SKIP to run tests/psd_tools/compression (exercises the compiled _rle extension against the pure-Python fallback, no composite/fixture deps needed).'
- pkg: coal
  version: 3.0.3
  home: https://github.com/cmake-wheel/coal
  repo: https://github.com/cmake-wheel/coal
  status: blocked-on-dependency
  pr: null
  notes: '10 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    coal is the renamed successor of hpp-fcl (collision detection / distance computation)
    and cmake-wheel/coal is the cmeel packaging fork of coal-library/coal, same shape as
    the rest of this family: build-backend = "cmeel", and the note''s "abi: 0" is PEP 427''s
    build number, not an ABI tag (gotcha 470) -- the real filenames are
    coal-3.0.3-0-cp312-cp312-manylinux_2_28_x86_64.whl, ordinary per-interpreter compiled
    wheels for cp310-cp314. UNLIKE the cmeel-<lib> leaves, this one has REAL Python
    bindings: pyproject does not set [tool.cmeel] has-sitelib explicitly, but configure-args
    is -DCOAL_HAS_QHULL=ON -DCOAL_BACKWARD_COMPATIBILITY_WITH_HPP_FCL=ON
    -DBUILD_PYTHON_INTERFACE=ON -DBUILD_STANDALONE_PYTHON_INTERFACE=ON and the released
    wheel carries cmeel.prefix/lib/python3.12/site-packages/coal/coal_pywrap.cpython-312-*.so
    (13MB) plus a hppfcl/ backward-compat package, so a port needs the per-interpreter
    matrix shape of build-cmeel-boost.yml, not the single-job shape of cmeel-qhull/octomap.
    Bindings are Boost.Python via eigenpy, not pybind11. BLOCKER IS THE DEPENDENCY CHAIN,
    not the code. readelf -d on that .so (gotcha 470''s one-shot dependency map; nothing is
    bundled, RUNPATH $ORIGIN/../../../../lib into the shared cmeel prefix) gives NEEDED
    libcoal.so (libcoal), libeigenpy.so (eigenpy), liboctomap/liboctomath.so.1.10
    (cmeel-octomap) and libboost_{python312,serialization,filesystem,atomic,container,graph}.so.1.90.0
    (cmeel-boost). requires_dist with extras dropped is cmeel, cmeel-assimp >= 6.0.2 < 7,
    cmeel-boost ~= 1.90.0, cmeel-octomap >= 1.9.8.2 < 2, cmeel-qhull >= 8.0.2.1 < 8.0.3,
    eigenpy >= 3.13 < 4, libcoal == 3.0.3; [build-system] requires adds only cmeel[build],
    cmeel-eigen and jrl-cmakemodules (all three py3-none-any off public PyPI) on top of the
    same set, so the build-time list adds no blocker the runtime list did not already have.
    SATISFIED: cmeel/cmeel-eigen/jrl-cmakemodules (py3-none-any) and cmeel-boost 1.90.0
    (registry, cp312/313/314). MISSING on both indexes: libcoal 3.0.3 (queued, py3-none-<plat>
    C++ payload), eigenpy 3.13.0 (blocked-on-dependency, now actionable since cmeel-boost
    landed) and cmeel-assimp 6.0.5 (blocked-on-dependency, itself needs cmeel-zlib);
    cmeel-octomap 1.10.0 (#2159) and cmeel-qhull 8.0.2.1 (#2160) are merged but their
    publish has not reached the registry yet (both still 404). Repo oracle agrees:
    ci_scripts/check_riscv64_deps.py --python 312 313 314 over the seven runtime
    requirements reports cp312/cp313/cp314 all UNRESOLVABLE (first failure cmeel-assimp,
    "from versions: none"), i.e. the wheel could be neither installed nor smoke-tested.
    Blocking set for coal, a subset of gotcha 470''s 11-package pin chain: cmeel-zlib ->
    cmeel-assimp, cmeel-octomap, cmeel-qhull, eigenpy -> libcoal -> coal. coal does NOT
    need the cmeel-tinyxml2 / cmeel-console-bridge / cmeel-urdfdom branch, which pin does.
    PORTABILITY IS OTHERWISE FINE: upstream tag is v3.0.3.c0 (the only .cN for 3.0.3,
    checked per gotcha 505); CMakeLists cmake_minimum_required(VERSION 3.22), above CMake
    4''s 3.5 floor, so no CMAKE_POLICY_VERSION_MINIMUM=3.5 as cmeel-console-bridge needed;
    the sdist has no -march/-mavx/SSE/AVX/NEON string (only three "TODO: vectorize"
    comments) and no CUDA/nvcc reference, Eigen arrives header-only via cmeel-eigen and
    degrades to scalar on riscv64, so no -DEIGEN_DONT_VECTORIZE is called for. Interpreter
    ceiling for the whole chain stays cmeel-boost''s riscv64 coverage: cp312/313/314,
    although upstream coal ships cp310-cp314. 3.0.3 is still the latest release (gotcha 388).
    No branch/worktree/PR: resolved read-only via PyPI JSON, the published aarch64 wheel
    and the sdist, no CI cycle burned. Revisit once libcoal, eigenpy and cmeel-assimp are
    on the registry.
    UPDATE 2026-09-21T15:39Z (libcoal triage, see its own entry -- frontier recomputed per
    gotcha 523). Two of the five blockers listed above have cleared: cmeel-octomap 1.10.0
    and cmeel-qhull 8.0.2.1 now return 200 on https://pypi.riseproject.dev/simple/ (both
    were 404 when this entry was written), so the registry lists six cmeel packages
    (cmeel-boost, cmeel-console-bridge, cmeel-octomap, cmeel-qhull, cmeel-tinyxml2,
    cmeel-zlib). coal''s REMAINING blockers are exactly three: cmeel-assimp 6.0.5, eigenpy
    3.13.0 and libcoal 3.0.3. libcoal was triaged today and parked blocked-on-dependency
    on cmeel-assimp alone -- its [build-system] requires pins cmeel-assimp >= 6.0.5 < 7,
    its released libcoal.so carries NEEDED libassimp.so.6, and its CMakeLists.txt:197 is
    find_package(assimp REQUIRED) with no option to drop it. So the chain below coal is
    now one tier deep, not two: cmeel-assimp (ACTIONABLE -- deps are only cmeel and
    cmeel-zlib, both installable; no sdist on PyPI, build from the checkout) -> libcoal,
    with eigenpy (ACTIONABLE -- its only dep cmeel-boost is published) resolvable in
    parallel. Revisit once cmeel-assimp, eigenpy and libcoal are on the registry; the
    check is one run of ci_scripts/check_riscv64_deps.py over coal''s seven runtime
    requirements.'
- pkg: pymavlink
  version: 2.4.49
  home: https://github.com/ArduPilot/pymavlink/
  repo: https://github.com/ArduPilot/pymavlink/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1744
  notes: 'PR #1744 green: cp312/cp313/cp314/cp314t built and tested (45 passed, 3 deselected each - two fixture-file gaps in the sdist, plus test_wp.py::test_save which needs to be the first file to import pymavlink.mavutil in-process). Publish job dry-ran cleanly. Awaiting review.'
- pkg: pymoo
  version: 0.6.2
  home: https://pymoo.org
  repo: https://github.com/anyoptimization/pymoo
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1813
  notes: 'PR #1813 opened. Mirrors upstream build.yml: cp310-cp314 (no cp314t, matches upstream CIBW_SKIP), PIP_ONLY_BINARY scoped to numpy,scipy,matplotlib,moocore (registry drift vs. real PyPI confirmed for all four). Local rehearsal (macOS/cp312): sdist builds cleanly with no special toolchain, all 7 compiled/*.pyx extensions produced, is_compiled() check (same as upstream wheel test) passes. Apache-2.0, no vendored third-party binaries. Awaiting riscv64 CI.'
- pkg: pybullet
  version: 3.2.7
  home: https://github.com/bulletphysics/bullet3
  repo: https://github.com/bulletphysics/bullet3
  status: in-review
  pr: 2170
  notes: 'CI green on PR #2170 (cp312/cp313/cp314/cp314t, 18 unittests each). setup.py compiles the Bullet3 C++ sources directly; SSE/x86 asm paths all gated on __i386__/__x86_64__, no patch needed. 3.2.7 has no git tag - pinned release commit 6c888f4a, byte-identical to the PyPI sdist. Needs CFLAGS=-Wno-error=incompatible-pointer-types for GCC 14 (gotchas 226/517).'
- pkg: python-gdcm
  version: 3.2.6
  home: https://github.com/tfmoraes/python-gdcm/
  repo: https://github.com/tfmoraes/python-gdcm/
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2179
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #2179 opened (draft) after a container restart lost the original agent; the port commit was already complete and local, just unpushed. SWIG bindings + GDCM C++ libs built via cibuildwheel-equivalent manylinux_riscv64 image; swig from Rocky 10, cmake/ninja from pypi.riseproject.dev. Two licensing patches (GDCM bundled libs, OpenSSL). CI just triggered, not yet confirmed green.'
- pkg: vosk
  version: 0.3.45
  home: https://github.com/alphacep/vosk-api
  repo: https://github.com/alphacep/vosk-api
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2166
  notes: 'PR #2166 open (draft); pr-checks and setup green. Run 1 (35596175494) FAILED after 10m34s of build on a GCC 14 issue in OpenBLAS, fixed in dc33eb03a7 and re-running as run 35600068390. THE FAILURE: OpenBLAS 0.3.20''s exports/gensymbol generates linktest.c as a bare list of calls to every exported symbol with no prototype in scope; GCC 14 diagnoses an implicit function declaration as an *error* by default, and the exports/Makefile rule''s own -w only inhibits warnings, so `make shared` dies with `error: implicit declaration of function ''cblas_ztrmv''` and `[Makefile:190: ../libopenblas_riscv64_generic-r0.3.20.so] Error 1`. Not riscv64-specific and not fixed by a version bump - 0.3.29''s exports/Makefile still has the identical prototype-less linktest line and neither release carries any implicit-function-declaration handling. The shared target cannot be skipped either: kaldi''s configure hard-requires $OPENBLASROOT/lib/libopenblas.so to exist on the OPENBLAS_CLAPACK path even though the actual link uses -l:libopenblas.a. FIX: COMMON_OPT="-O2 -Wno-error=implicit-function-declaration" on both OpenBLAS make lines - COMMON_OPT is the supported knob (Makefile.system:1521 ifndef, folded into CFLAGS at :1529), and -O2 restores the default it replaces. Reproduced and the fix verified against the image''s own gcc 14.3.1 under QEMU riscv64 on a 4-line stand-in for the generated linktest.c. Also pre-flighted the next suspect: CLAPACK is CLEAN under GCC 14 diagnostics (gcc -fsyntax-only -Werror=implicit-function-declaration,-implicit-int,-int-conversion,-incompatible-pointer-types over all 1896 .c files: 0 failures in BLAS/SRC and LAPACK SRC; the 6 libf2c hits are just the build-generated arith.h missing at syntax-check time), so no flags are warranted there. Folded as gotcha 510 (compiled-vs-pure-detection.md). 2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Triaged for the Kaldi-scale fear first and it is NOT the chdb/tensorflow class. (1) SHAPE. Gotcha 81/398: every upstream wheel is py3-none-<platform> and holds a real 26MB libvosk.so, opened by cffi in ABI mode (vosk_builder.py does ffibuilder.set_source(..., None), __init__.py does _ffi.dlopen(.../libvosk.so)); no PyInit_*, no extension module, so one build serves every interpreter - a single job, no cp3XX matrix. Sizes diverge per platform (7.2MB x86_64 / 2.4MB aarch64 / 14MB win), which is gotcha 81''s tell for real per-platform content. (2) NO ARCHITECTURAL BLOCKER. src/Makefile''s defaults are already the CPU-only config: HAVE_OPENBLAS_CLAPACK=1, HAVE_MKL=0, HAVE_CUDA=0, HAVE_ACCELERATE=0, and the CUDA batch_recognizer/batch_model TUs are only added under HAVE_CUDA=1. Upstream already names riscv64: travis/build-wheels-dockcross.sh has a *riscv64-*) case setting VOSK_MACHINE=riscv64, and alphacep/kaldi branch vosk ships src/makefiles/linux_openblas_riscv64.mk wired into configure at two call sites (OPENBLAS and OPENBLAS_CLAPACK), selected off uname -m on a native build. OpenBLAS 0.3.20 (the version upstream pins) lists RISCV64_GENERIC in TargetList.txt - plain C kernels, no RVV, so gotchas 272/279 do not bite; C910V is the RVV-0.7.1 one and is not used. No vendored binary blob anywhere: kaldi, OpenFST (alphacep fork), CLAPACK and OpenBLAS are all built from source in-container. No x86 intrinsics either - grep for immintrin/xmmintrin/__SSE/_mm_/__x86_64__ across the 19 kaldi dirs the build touches returns nothing; the x86 Dockerfile''s only ISA line is a no-op sed ''s:-msse -msse2:-msse -msse2:''. (3) SCALE IS ORDINARY, NOT VTK-CLASS. TU census for make online2 lm rnnlm: 212 non-test .cc across base/matrix/util/feat/tree/gmm/transform/hmm/lat/lm/rnnlm/decoder/ivector/nnet3/chain/cudamatrix/fstext/online2, ~150 OpenFST library TUs (--disable-bin drops the binaries), ~2600 tiny f2c''d CLAPACK C files and OpenBLAS''s ONLY_CBLAS=1 set - so ~4000 edges of which the large majority are trivial C, against VTK''s 12192 (measured 5095 edges in 7.5h on this fleet) and chdb''s ~4900 V8-class C++. Upstream has no GitHub Actions CI to time, but their whole manylinux image + wheel is one Dockerfile they run by hand. (4) LOCAL VALIDATION. Wheel-tag path rehearsed on x86 with setuptools 84 / wheel 0.48: setup.py''s bdist_wheel_tag_name still imports and produces vosk-0.3.45-py3-none-linux_riscv64.whl (VOSK_MACHINE=riscv64), containing vosk/libvosk.so + vosk/vosk_cffi.py, and auditwheel repair of that py3-none wheel works and keeps the py3-none halves. Also confirmed upstream''s released x86_64 wheel has no .libs/ dir: everything but libstdc++ is static and libstdc++/libgcc_s are manylinux-whitelisted, so nothing is vendored and no gpl_sources job is right (the build step asserts no vosk.libs/ appears). Deviations from upstream, all one-liners: native manylinux riscv64 image instead of dockcross (no riscv64 dockcross image), TARGET=RISCV64_GENERIC, the three branch-head clones pinned to commits, -DCMAKE_POLICY_VERSION_MINIMUM=3.5 for CLAPACK''s cmake_minimum_required(VERSION 2.6) against the image''s CMake 4.4, and staging LICENSE/LICENSE.kaldi/.openfst/.openblas/.clapack into python/ so setuptools'' default glob ships them (upstream''s wheels carry no licence text at all). Test is upstream''s own example: decode python/example/test.wav with vosk-model-small-en-us-0.15 on cp312/cp313/cp314/cp314t (x86 reference transcript: "one zero zero zero one nine oh two i no zero one eight zero three", 15 timed words). Worktree .claude/worktrees/vosk, branch vosk. SECOND BUG, caught by that local rehearsal rather than by a ~3.5h CI cycle: kaldi died at `../matrix/kaldi-blas.h:100:12: fatal error: lapacke.h: No such file or directory` / `make: *** [Makefile:164: matrix] Error 2`. This one was a self-inflicted deviation, not an upstream problem - ONLY_CBLAS=1 implies NO_LAPACKE and Makefile.install:75 gates the lapacke.h copy behind `ifndef NO_LAPACKE`, while kaldi-blas.h includes it unconditionally on the HAVE_OPENBLAS path. Upstream''s Dockerfile.manylinux AND Dockerfile.dockcross-manylinux both pass ONLY_CBLAS=1 to `all` only and omit it from `install`; that asymmetry is load-bearing and had been normalised away. Restored in 44636bf587, which also makes the workflow a truer mirror of upstream. Lesson: an apparent inconsistency between two upstream invocations is a fact to preserve until proven cosmetic. FULL LOCAL REHEARSAL NOW GREEN end to end in quay.io/rockylinux/rockylinux:10 (same distro + gcc 14.3.1 as the manylinux image, x86, -j3 on a loaded host): OpenBLAS 45s, CLAPACK 30s, OpenFST 153s, kaldi `make online2 lm rnnlm` at -O3 693s, libvosk.so link 67s, total 988s ~ 2964 core-s. The riscv64 runner ran its OpenBLAS leg at about 12x this box per core, which puts the whole build near 2.5-3.5h wall on 4 cores - comfortably inside the 720m timeout and confirming the scale call with measured numbers rather than a TU census. Critically, `ldd libvosk.so` on the rehearsal output is exactly linux-vdso, libstdc++, libm, libgcc_s, libc, ld - all manylinux-whitelisted - so auditwheel vendors nothing, no vosk.libs/ is created and no gpl_sources job is warranted; that was previously inferred from upstream''s released wheel and is now demonstrated from our own link line. Run 2 (35600068390) was cancelled by the concurrency group as intended. RUN 3 (35600928499) ON 44636bf587 IS GREEN: the build_wheels job took 1h58m38s on the riscv64 runner (inside the 2.5-3.5h projected from the local rehearsal, and well inside the 720m timeout), producing vosk-0.3.45-py3-none-manylinux_2_39_riscv64.whl - a single platform tag, no compressed tag set. Every post-build assertion passed on the real artifact: vosk/libvosk.so present, no .cpython-*.so, NO vosk.libs/ (so auditwheel vendored nothing and no gpl_sources job is needed - now proven on the riscv64 wheel itself, not just inferred), and all five licence files shipped. The test decoded upstream''s python/example/test.wav with vosk-model-small-en-us-0.15 on cp312, cp313, cp314 AND cp314t, and every one returned `one zero zero zero one nine oh two i no zero one eight zero three` - byte-identical to the x86 reference transcript, so RISCV64_GENERIC''s plain-C kernels plus CLAPACK reproduce the x86 ASR result exactly and the py3-none tag is justified by four real interpreters loading the same wheel. Publish dry-run finishing. Not merged, not undrafted - maintainer''s call.'
- pkg: leidenalg
  version: 0.12.0
  home: https://leidenalg.readthedocs.io/en/latest/
  repo: https://github.com/vtraag/leidenalg
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2115
  notes: 'PR #2115 open (branch port/leidenalg); 0.12.0 confirmed still latest on PyPI. Build-from-checkout mirroring upstream build.yml: upstream''s own scripts/build_igraph.sh + scripts/build_libleidenalg.sh clone and CMake-build the igraph C core (pinned 1.0.0) and libleidenalg (pinned 0.13.0) inside the container, then setup.py links leidenalg._c_leiden against them; auditwheel vendors both into leidenalg.libs. setup.py tags the wheel cp38-abi3 regardless of the building interpreter (gotcha 34), so the matrix is cp39 (upstream''s CIBW_PROJECT_REQUIRES_PYTHON floor) x libc [manylinux, musllinux], reused and retested on cp312-cp314; no cp314t, matching upstream''s CIBW_SKIP cp3??t-*. Only deviations from upstream: the riscv64 images, zlib-devel in the yum list (upstream''s aarch64 job already has it), and staging the two C cores'' GPL notices at the project root as LICENSE.igraph/LICENSE.libleidenalg so setuptools'' default licence glob ships them (gotcha 53) -- upstream''s wheels carry neither. gpl_sources job collects gcc like build-igraph.yml, since the musllinux wheel vendors the image''s libstdc++/libgcc_s. No patches needed: abi3audit is clean on upstream''s released wheel, and setuptools'' bundled wheel shim keeps setup.py''s bdist_wheel_abi3 path alive. Full local QEMU riscv64 rehearsal of both legs with cibuildwheel 4.2.0 (3h each): leidenalg-0.12.0-cp38-abi3-manylinux_2_39_riscv64.whl and ...-musllinux_1_2_riscv64.whl, each 688 tests passed / 28 skipped via upstream''s `python -m unittest -v`; runtime dep igraph 1.0.0 resolved from the registry for both libcs. CI on PR #2115: manylinux leg and gpl_sources green, musllinux leg red on a single test (test_VertexPartition.CPMVertexPartitionTest.test_Bipartite, `AssertionError: 2 != 1`, 688 ran / 28 skipped). Investigated, not a musl/riscv64 bug and not infra: libleidenalg 0.13.0''s `Optimiser::Optimiser()` does `igraph_rng_seed(&rng, time(NULL))`, so every optimisation is seeded by the wall-clock second and the test seeds nothing. Replaying 1000 consecutive epoch seconds via `Optimiser.set_rng_seed()` fails 7 times (0.7%) on the CI-built musllinux_1_2_riscv64 wheel under QEMU, and on the *upstream released* manylinux_2_28_x86_64 wheel the identical 7 seeds fail -- so it is an upstream, platform-independent flake (~3% per job across the four interpreters retesting the one abi3 wheel). 20 back-to-back full musl suite runs under QEMU were green. No workflow change: deselecting a test upstream ships green would diverge from upstream CI for no defect. Re-ran the failed musllinux job only; recorded as gotcha 414. Awaiting the rerun and maintainer review; not yet published.'
- pkg: lib3mf
  version: 2.5.0
  home: https://github.com/3MFConsortium/lib3mf
  repo: https://github.com/3MFConsortium/lib3mf_python
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2173
  notes: 'PR #2173 open (branch lib3mf, draft), CI green. Upstream located by probing repo names, since PyPI records no project URL: the library is 3MFConsortium/lib3mf (BSD-3-Clause C++) but the PyPI distribution is cut from a separate packaging repo, 3MFConsortium/lib3mf_python, which the source tree never references (gotcha 391 shape). Wheel shape is gotcha 81 (xgboost), not gotcha 27 (watchdog): the single py3-none-manylinux2014_x86_64 wheel has no ABI tag because there is no extension module at all - the whole API is Autogenerated/Bindings/Python/Lib3MF.py, a 725 KB ACT-generated ctypes wrapper, driving lib3mf/lib3mf.so (5.3 MB, SONAME lib3mf.so.2, DT_NEEDED only libstdc++/libm/libgcc_s/libc). The platform half is load-bearing, so riscv64 needs a real build. That .so is committed to the packaging repo as an x86_64 binary (BuildID byte-identical to the released wheel''s), lifted by prepare_pypi_release.py out of the lib3mf_sdk_v2.5.0.zip release asset, which 3MFConsortium publishes for x86_64 Linux only - but the sources are fully open at the same tag, so gotcha 77 applies and the port builds the library instead of vendoring a blob. Recorded as gotcha 522. build-lib3mf.yml mirrors lib3mf_python''s python-package.yml: checkout lib3mf_python v2.5.0 at the root, lib3mf v2.5.0 with submodules at lib3mf-src, then one docker run into quay.io/pypa/manylinux_2_39_riscv64 that runs upstream''s own cmake/GenerateMake.sh + cmake --build (473 TUs, plain Makefiles - the image has no ninja), installs the result over lib3mf/lib3mf.so and runs upstream''s build_wheels.py unchanged. No cibuildwheel: the wheel is py3-none and its platform tag comes from build_wheels.py''s own setup.py bdist_wheel --plat-name, which a PEP 517 frontend would discard, and the MANIFEST.in that script writes is the only reason the binary is packaged. One wheel, no interpreter matrix. LIB3MF_TESTS=OFF is passed to the configure step because cmake --build''s default target also builds the C++ suite and the vendored Libraries/libressl it links (thousands of TUs that never reach the wheel) - EXCLUDE_FROM_ALL on that add_subdirectory does not keep it out of all, since the test targets are in all and link it. patches/lib3mf/2.5.0/0001-*.patch (Upstream-Status: To upstream) is the only patch and touches only the two hardcoded x86_64 tables in the packaging repo: setup.py''s SUPPORTED_PLATFORMS gate, which raises RuntimeError before setuptools runs on any non-x86_64 Linux, and get_platform_tag(), which returns the glibc 2.17 manylinux2014 baseline for every Linux host where riscv64''s oldest manylinux profile is glibc 2.39. No CMake or C++ change was needed: the tree has zero architecture gates, no SIMD, no ACT/Go bootstrap (Autogenerated/ is committed), and its vendored code - zlib 1.3.1, libzip 1.10.1, cpp-base64, fast_float (header-only) - compiles as plain C/C++ from Libraries/. Licensing: upstream''s wheel ships only the binding''s BSD LICENSE although the .so statically links those four, so the workflow stages LICENSE.zlib/.libzip/.cpp-base64/.fast_float at the project root for setuptools'' default glob (gotcha 53); nothing GPL is vendored (libstdc++/libgcc_s stay dynamic, same as the released x86_64 binary), so no gpl_sources job. Local x86_64 pre-flight: full CMake build clean (473/473 objects), patched build_wheels.py produced a wheel carrying lib3mf/lib3mf.so and all five licence files, and the installed wheel passed import + wrapper version (2, 5, 0), the packaging repo''s create_cube example (valid 1,619 B cube.3mf), a read-back of that file (8 vertices / 12 triangles) and its lib3mf_py313_exception.py shutdown regression test. CI run 35609356978 on PR #2173: build 18 min (configure 14:08:00 -> link 14:26:06), all four wheel tests green, lib3mf-2.5.0-py3-none-manylinux_2_39_riscv64.whl (1,344,579 B) with Machine: RISC-V and the same four DT_NEEDED entries as the released x86_64 binary; publish dry-ran cleanly (sha256 db7dcac545300eda09830da42c1a99c919a687bb1b0eb6f1d329957b55d42f10). Left as a draft for the maintainer; not yet published.'
- pkg: chalkpy-rs
  version: 0.1.1
  home: https://chalk.ai
  repo: https://chalk.ai
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2171
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. FEASIBLE and ported. No public source repository: PyPI records only chalk.ai/docs.chalk.ai project URLs, and github.com/chalk-ai holds 30 public repos (chalk-ty, chalk-go, chalk-java, forks of arrow-rs/reqwest/...) but none containing the chalk-ast/chalk-lsp/chalk-proto/chalk-sql-lsp/chalk-ty-preprocess/chalk-utils/chalk-ignore crates - so the released sdist is the upstream tree (gotcha 156 shape, modelled on build-lmnr-claude-code-proxy.yml). The sdist is complete and self-contained: root Cargo.toml + 8 workspace member crates, 185 locked packages, all from crates.io except ruff_python_parser/ruff_python_ast/ruff_python_trivia/ruff_source_file/ruff_text_size pinned at git astral-sh/ruff tag 0.15.2 (public). No build.rs anywhere (chalk-proto ships pre-generated prost output, so no protoc), no vendored binaries, no C/C++ or native deps - the cross-linked .so needs only libc.so.6 and libgcc_s.so.1. Build backend is setuptools.build_meta + setuptools-rust (setup.py RustExtension(''chalk_rs'', features=[''python'']), setuptools pinned <81), pyo3 0.28 with no abi3 feature, so per-interpreter wheels; matrix cp312/cp313/cp314 (no cp314t - upstream ships none). Distribution name chalkpy-rs, import name chalk_rs (a single top-level .so plus _chalkpy_rs_version.py). Local pre-flight on x86_64, all green: cargo check --locked --target riscv64gc-unknown-linux-gnu over the full graph in 69s; a real cross cargo build --release producing an ELF 64-bit RISC-V libchalk_rs.so in 47s; pip wheel from the sdist producing a wheel whose contents match upstream''s published one file for file; and a 10-test smoke suite (duration/ISO-8601 helpers, to_snake_case/build_namespaced_name, StdAstFileSystem, AstFileParserCache imports, AstProjectIndex feature-class and resolver ASTs over a Chalk features module) passing against both upstream''s released cp311 x86_64 wheel and our own build. LICENSING CAVEAT, flagged in the PR and unresolved: chalkpy-rs declares no license at all - no license/license_expression/classifier in PyPI metadata, no LICENSE/COPYING/NOTICE in the sdist or in any published wheel, and the parent chalkpy 2.157.24 wheel has none either - so docs/packages/chalkpy-rs.yaml records license: Unknown (the existing convention, cf. google-crc32c, vhacdx) and a maintainer should settle redistribution before merge. Unlike the hdbcli/mssql-python-odbc parks (gotchas 342/372) there is no proprietary blob and no missing source - the blocker, if any, is metadata-only. PR #2171 (draft), branch chalkpy-rs, commit b1130c4131. CI fully green on run 35608023739: cp312/cp313/cp314 each built chalkpy_rs-0.1.1-cp3xx-cp3xx-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl (~1.9 MB, ~14 min per leg, one chalk_rs.cpython-3xx-riscv64-linux-gnu.so) with 10 passed in the smoke suite, and the publish job dry-ran cleanly over all three artifacts. Left as a draft for the licensing decision above.'
- pkg: miniaudio
  version: '1.71'
  home: https://github.com/irmen/pyminiaudio
  repo: https://github.com/irmen/pyminiaudio
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1812
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. cffi_modules build (build_ffi_module.py compiles miniaudio.c + vendored miniaudio.h/stb_vorbis.c in one go via cffi API mode, setuptools.build_meta backend); cffi is both a build and runtime dependency and is registry-only on riscv64 (cffi 2.1.1 covers cp312-cp314t). Dropped musllinux (cffi has no musllinux riscv64 wheel on our registry). Verified locally on macOS: pip wheel + pytest tests/ -> 50 passed. PR #1812 opened.'
- pkg: murmurhash2
  version: 0.2.10
  home: https://github.com/messense/murmurhash2-py
  repo: https://github.com/messense/murmurhash2-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1162
  notes: pyo3/maturin abi3-py36 extension, pure-Rust hash (no vendored C); pyo3 0.15 predates free-threading so cp314t is dropped, matrix is cp312/cp313/cp314. Built/tested clean locally on cp312 before push (gotcha 306 added). First CI run failed the compiled-extension check - the maturin __init__.py shim's star-import shadows the murmurhash2 submodule attribute with the same-named compiled function, so the probe has to go through sys.modules instead (gotcha 308 added). Fixed and pushed; CI fully green (build + publish dry-run) on 2026-09-06.
- pkg: torch-npu
  version: 2.12.0
  home: https://gitcode.com/ascend/pytorch
  repo: https://gitcode.com/ascend/pytorch
  status: parked
  pr: null
  notes: 'Parked: Huawei Ascend NPU bridge for PyTorch - the accelerator-vendor family of gotcha 480 (memfabric-hybrid) and gotcha 471 (NVIDIA), with the new mechanism recorded as gotcha 516. Verified against the real source and the real released wheel, not the name. Upstream is confirmed: PyPI home_page/Download both point at gitcode.com/ascend/pytorch, the clone works through the agent proxy, github.com/Ascend/pytorch is only the mirror the README links for issues and CI badges, and ascend/torchair is not a separate publisher but a submodule here (third_party/torchair/torchair), its .so shipping inside torch_npu/dynamo/torchair/. The 2.12.0 release is tag v26.1.0-pytorch2.12.0 (version.txt 2.12.0, dated 2026-07-25, matching the wheel member mtimes); v2.12.0 is a moving BRANCH whose version.txt already reads 2.12.0.post3, so the tag is what was read. THE TRIAGE HYPOTHESIS (build hard-requires the CANN toolkit) IS WRONG and was checked rather than assumed: third_party/acl/libs/build_stub.sh compiles nine empty stub .so (libascendcl, libhccl, libge_runner, libgraph, libacl_op_compiler, libacl_tdt_channel, libascend_ml) from in-tree .cpp, setup.py calls build_stub(BASE_DIR) unconditionally, the CANN headers are vendored (third_party/acl/inc, third_party/hccl/inc), and CMakeLists.txt links those stub paths - so torch_npu compiles with no CANN installed anywhere. Three separate walls sit behind that. (1) The published artifact is dead without CANN, and readelf proves it: setup.py''s BdistWheelBuild runs auditwheel repair --exclude over nine cann_dependencies plus libatb.so, stripping the stubs back out, so torch_npu.libs/ in the released wheel is an EMPTY directory while libtorch_npu.so and _C.cpython-312-aarch64-linux-gnu.so keep DT_NEEDED on libascendcl.so, libhccl.so, libacl_op_compiler.so, libge_runner.so, libgraph.so, libacl_tdt_channel.so, libfmk_parser.so and libascend_protobuf.so with RPATH $ORIGIN/lib - resolvable only from the vendor install the README requires (source /usr/local/Ascend/ascend-toolkit/set_env.sh). import torch_npu therefore fails in the dynamic loader on any host without CANN (torch_npu/__init__.py runs _initialize() -> _load_core_modules() -> import torch_npu._C unconditionally), a harder gate than gotcha 452/480''s dlopen probe. libtorch_npu.so carries 1783 aclnn* strings and 62 undefined acl/ge/Hccl symbols: every operator dispatches into CANN, none is compiled in this tree. (2) CANN itself has no riscv64: all 706 tags of the official ascendai/cann image are linux/arm64 + linux/amd64 and nothing else (full pagination of hub.docker.com/v2/repositories/ascendai/cann/tags; the registry-1.docker.io manifest route rate-limits, the hub v2 API does not). The README pins CANN 8.5.0 for torch_npu 2.12.0, lists Atlas 800/900/300T/800I A2 hardware, and upstream ships exactly two build images - ci/docker/X86/Dockerfile and ci/docker/ARM/Dockerfile, both quay.io/pypa/manylinux_2_28_<arch>. PyPI has 426 files over 58 releases, every one manylinux x86_64 or aarch64, and no sdist in any release. (3) There is no CPU-only or generic build mode - unlike gotcha 480''s XPU_TYPE=NONE there is not even a fake one to disprove. Every os.getenv/os.environ.get in setup.py is DISABLE_INSTALL_TORCHAIR, DISABLE_RPC_FRAMEWORK, ENABLE_LTO, PGO_MODE, _GLIBCXX_USE_CXX11_ABI, DEBUG, REL_WITH_DEB_INFO, TORCH_VERSION, AUDITWHEEL_PLAT, TORCH_NPU_PACKAGE_NAME, MAX_JOBS, _ABI_VERSION; CMakeLists.txt declares no option() at all; and setup.py''s manylinux allowlist (the auditwheel gate) names 19 x86_64/aarch64 tags and no others. A stub-linked riscv64 wheel would be a package that cannot even import, i.e. gotcha 41''s rejected offline build in its purest form. Independently blocking: install_requires is an exact torch==<version of the torch installed at build time> pin (torch==2.12.0 in the released metadata), upstream builds against torch==2.12.0+cpu from download.pytorch.org/whl/cpu which has no riscv64 leg, and pypi.riseproject.dev carries riscv64 torch only as 2.13.0+cpu and 2.14.0+cpu - so the pinned base framework does not exist for riscv64 at this version either. The only ''riscv'' string in the entire tree is march=rv64gc in torch_npu/_inductor/cpp_builder.py, a monkeypatch of torch._inductor.cpp_builder''s CPU codegen flags inherited verbatim from PyTorch, not NPU support. Revisit only if Huawei ships CANN and the Ascend HDK driver/firmware for riscv64. Same verdict as the sibling entries memfabric-hybrid, memfabric-zbal and memcache-hybrid, and by inspection triton-ascend, which is left untouched for whoever claims it. Verified read-only (PyPI JSON, the cp312 aarch64 wheel unzipped and readelf''d, the tag and branch of gitcode.com/ascend/pytorch, the Docker Hub tag listing, both package indexes); scratch under .git/pw-scratch/torch-npu/ removed. No worktree, branch, workflow, docs/packages entry, patch or PR.'
- pkg: fuzzysearch
  version: 0.8.1
  home: https://github.com/taleinat/fuzzysearch
  repo: https://github.com/taleinat/fuzzysearch
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1163
  notes: 'Real per-interpreter compiled extensions (_common, _substitutions_only), Root-Is-Purelib false, verified against the real 0.8.1 PyPI wheel. cp314t dropped: single-phase PyModule_Create with no Py_mod_gil slot re-enables the GIL on import, and upstream ships no free-threaded wheel either. Matrix cp312/cp313/cp314. Built/tested clean locally (uv build + unittest discover, 522 passed/62 skipped) before push. PR open, CI running.'
- pkg: zope-i18nmessageid
  version: '8.3'
  home: https://zopei18nmessageid.readthedocs.io
  repo: https://github.com/zopefoundation/zope.i18nmessageid
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1160
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Small C extension (optional_build_ext, same shape as zope-hookable/zope-interface); cibuildwheel build-from-checkout, matrix cp312/cp313/cp314/cp314t to match zope.interface runtime dependency availability on our registry. Unlike its siblings, PURE_PYTHON does not gate the C import here, so tests run once. CI fully green: 46/46 tests passed on all four interpreters, publish dry-ran cleanly. PR open, in-review.'
- pkg: fasttext-numpy2
  version: 0.10.4
  home: https://github.com/simon-ging/fasttext-numpy2
  repo: https://github.com/simon-ging/fasttext-numpy2
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2172
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39), all x86_64; no riscv64 on PyPI or pypi.riseproject.dev. simon-ging/fasttext-numpy2 is facebookresearch/fastText master (1142dc4) plus two numpy-2 commits; pybind11 extension compiling the vendored fastText C++ sources, no external native deps. Unlike fasttext-wheel (#1621) it has a pyproject.toml declaring pybind11, so plain PEP 517 isolation works and no CIBW_BEFORE_BUILD/--no-build-isolation is needed. No git tags upstream; pins commit 11468883b68f ("update build instructions to include source dist"), verified byte-identical to the released 0.10.4 sdist (only diff is the generated [egg_info] block setuptools stamps into setup.cfg). No patches needed: args.cc already includes <cstdint> (upstream 6c2204ba is in this tree), FastText.py already has the numpy-2 predict fix, and setup.py''s Extension(extra_compile_args=[... -march=native]) is dead code because BuildExt.build_extensions overwrites extra_compile_args before compiling (same as fasttext-wheel). The only x86 SIMD is src/densematrix.cc averageRowsFast, guarded by __SSE__/__AVX__/__AVX512F__ with a scalar fallback; verified locally that a scalar-only build returns a bit-identical get_sentence_vector to the SSE build, so riscv64 takes the fallback with no numeric divergence. Upstream''s own unit suite (runtests.py -u, 83 tests) is broken and cannot be used: train_supervised diverges to "Encountered NaN." at the default lr=0.1, reproduced on x86_64 with the *released* fasttext_numpy2-0.10.4 cp311 PyPI wheel (41 errors / 1 failure, and a 3-line hand-written training on trivially separable data fails too), while fasttext-wheel 0.9.2 trains the same data fine - an upstream regression somewhere in fastText master, not arch- or build-specific. CI therefore runs a lid.176.ftz predict + get_sentence_vector smoke test like fasttext-predict (#1679). manylinux only (numpy has no musllinux riscv64 wheel on the registry); CIBW_ENVIRONMENT adds PIP_EXTRA_INDEX_URL so the test venv gets numpy from pypi.riseproject.dev. PR #2172 (draft), branch fasttext-numpy2. CI green: all four riscv64 legs (cp312/cp313/cp314/cp314t) built and passed the smoke test on the self-hosted pool, ~7 min each, and the publish job dry-ran cleanly over all four wheels. numpy 2.5.3 resolved as a riscv64 wheel from the registry in the test venv. The riscv64 get_sentence_vector matched the x86_64 SSE value (norm 4.16875) to <1e-4, confirming the scalar fallback is numerically equivalent. Left as a draft for the orchestrator to undraft.'
- pkg: unicodedata2
  version: 17.0.1
  home: http://github.com/fonttools/unicodedata2
  repo: http://github.com/fonttools/unicodedata2
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1161
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Backport of CPython''s unicodedata C module with generated tables, no native deps; build-from-checkout cibuildwheel matching upstream''s ci.yml build_wheels job, manylinux+musllinux, cp312/cp313/cp314/cp314t. Built/tested clean locally (25 passed) before push. Worktree at .claude/worktrees/unicodedata2, branch unicodedata2. All 8 wheel jobs green, publish dry-run OK (first cp314-musllinux attempt hit a transient "docker daemon not running" runner flake, unrelated to the package; re-run succeeded). PR open, awaiting review.'
- pkg: baml-py
  version: 0.226.1
  home: https://www.boundaryml.com
  repo: https://github.com/BoundaryML/baml
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2175
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.
    Feasible: public Apache-2.0 source, maturin+pyo3 (abi3-py38 unconditional, so one
    cp38-abi3 wheel, no free-threaded variant), no sdist on PyPI so built from the git
    tag. baml-python-ffi is a member of the engine/ Cargo workspace (784-crate lock);
    only native-C deps are ring 0.17.14 and blake3, both of which take portable paths on
    riscv64 (ring ships no riscv asm and no compile_error gate; tombi already ships a
    riscv64 wheel off reqwest+rustls+ring). rust-toolchain.toml pins stable 1.93.0, whose
    channel manifest has riscv64gc-unknown-linux-gnu host tools, so no RUSTUP_TOOLCHAIN
    override is needed (unlike gotcha 238). Built with PyO3/maturin-action out of engine/
    (upstream does the same so engine/.cargo/config.toml supplies the load-bearing
    --cfg tracing_unstable rustflag); release profile fat LTO relaxed to thin +
    codegen-units 16 + CARGO_BUILD_JOBS=2 for the 4-core runners. PR #2175 open as draft.'
- pkg: lapx
  version: 0.9.4
  home: https://github.com/rathaROG/lapx
  repo: https://github.com/rathaROG/lapx
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1394
  notes: '48 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1394 open, cp312/cp313/cp314/cp314t glibc only (musllinux dropped: numpy has no riscv64 musllinux wheel on the registry).'
- pkg: libpinocchio
  version: 4.1.0
  home: https://github.com/cmake-wheel/libpinocchio
  repo: https://github.com/cmake-wheel/libpinocchio
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    BLOCKED, not infeasible -- libpinocchio is a MID-TIER member of gotcha 470''s cmeel
    chain (it is one of pin''s 11 blockers, and is itself blocked on two more), so it is
    not yet portable. The cmeel shape was confirmed first, and the port would have been
    easy: cmake-wheel/libpinocchio is a packaging fork of stack-of-tasks/pinocchio, tag
    v4.1.0.c0 (git ls-remote shows exactly one .cN for 4.1.0, so gotcha 505''s
    highest-tag rule is trivial here); the sdist''s pyproject.toml sets build-backend =
    "cmeel" with [tool.cmeel] has-sitelib = false and run-tests = false, i.e. gotcha
    487''s SINGLE-JOB shape -- one interpreter-independent py3-none wheel, matching the
    released libpinocchio-4.1.0-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl whose
    dist-info/WHEEL reads Build: 0, Generator: cmeel 0.60.1, Tag:
    py3-none-manylinux_2_28_aarch64; and configure-args build the C++ library only
    (-DBUILD_PYTHON_INTERFACE=OFF -DBUILD_WITH_COLLISION_SUPPORT=ON -DBUILD_TESTING=OFF
    -DBUILD_BENCHMARK=OFF -DBUILD_EXAMPLES=OFF -DBUILD_WITH_LIBPYTHON=OFF
    -DSUFFIX_SO_VERSION=OFF), so build-libpinocchio.yml would have been a near-copy of
    build-cmeel-qhull.yml (only cp312-manylinux_riscv64, CIBW_REPAIR_WHEEL_COMMAND "",
    CMAKE_POLICY_VERSION_MINIMUM=3.5 for cmeel[build]''s CMake>=4 floor). THE BLOCKER IS
    THE DEPENDENCY CHAIN, and it bites at build time and link time, not merely in
    metadata. [build-system] requires = cmeel-boost ~= 1.90.0, cmeel-urdfdom[build] >= 6,
    cmeel[build], libcoal[build] >= 3.0.3 < 4, scipy; [project] dependencies repeats
    cmeel-urdfdom >= 6 and libcoal >= 3.0.3 < 4 (identical to the released wheel''s
    Requires-Dist). Neither cmeel-urdfdom nor libcoal has a riscv64 wheel on public PyPI
    (both publish only macosx/manylinux_2_28_{x86_64,aarch64}) or on
    https://pypi.riseproject.dev/simple/, which today lists exactly five cmeel packages:
    cmeel-boost, cmeel-console-bridge, cmeel-qhull, cmeel-tinyxml2, cmeel-zlib. The
    repo''s own oracle agrees: ci_scripts/check_riscv64_deps.py --python 312 --
    ''cmeel-boost~=1.90.0'' ''cmeel-urdfdom[build]>=6'' ''cmeel[build]''
    ''libcoal[build]>=3.0.3,<4'' ''scipy'' reports cp312 UNRESOLVABLE ("Could not find a
    version that satisfies the requirement cmeel-urdfdom>=6 (from versions: none)").
    Because that oracle stops at the FIRST failure it names only one blocker, so it was
    re-run with the two missing requirements dropped (new gotcha 523): that run is cp312
    ok, resolving scipy-1.18.1-cp312-cp312-manylinux_2_39_riscv64.whl,
    cmeel_boost-1.90.0-0-cp312-cp312-manylinux_2_39_riscv64.whl,
    cmake-4.4.3-py3-none-manylinux_2_31_riscv64.whl, numpy-2.5.3 riscv64 and the
    py3-none-any cmeel 0.61.0 / cmeel-eigen 3.4.1 / cmeel-urdfdom-headers 3.0.0 /
    jrl-cmakemodules 2.1.0 -- proving cmeel-urdfdom and libcoal are the ONLY two blockers
    and that nothing else (scipy in particular) needs a port first. Gotcha 470''s readelf
    method was applied to libpinocchio''s OWN released wheel rather than inferred from
    pin''s: the aarch64 wheel bundles nothing (four .so under cmeel.prefix/lib/, RUNPATH
    $ORIGIN), and libpinocchio_{collision,parsers,visualizers}.so each carry NEEDED
    libcoal.so, libpinocchio_parsers.so additionally NEEDs
    liburdfdom_{model,world,sensor}.so.6, and all three NEED liboctomap.so.1.10 /
    liboctomath.so.1.10 (cmeel-octomap) and libboost_{serialization,filesystem,atomic}.so.1.90.0
    (cmeel-boost, satisfied). So the missing siblings are LINK INPUTS: a build attempted
    today dies when pip installs the build requirements, and would die again at link even
    if it got past that. Deliberately rejected: building with collision/parsers support
    off -- it drops libpinocchio_collision.so and libpinocchio_parsers.so, which pin''s
    bindings NEED, cmeel would still emit the unchanged Requires-Dist from [project]
    dependencies so the wheel stays uninstallable, and it diverges from the upstream
    recipe this port exists to mirror. Also rejected: an in-container monolithic build of
    the siblings, for the reasons already recorded in pin''s entry. No branch, worktree or
    PR opened and no CI cycle burned -- resolved read-only via PyPI JSON, the 4.4MB sdist''s
    pyproject.toml, git ls-remote, readelf on the released aarch64 wheel, the registry
    index and two oracle runs. REMAINING FRONTIER, recomputed rather than assumed:
    cmeel-urdfdom 6.0.0 is NOW ACTIONABLE (needs only cmeel-console-bridge and
    cmeel-tinyxml2, both merged and on the registry, plus py3-none-any
    cmeel-urdfdom-headers); cmeel-assimp 6.0.5 is NOW ACTIONABLE (needs only cmeel-zlib,
    on the registry -- note it publishes NO sdist on PyPI, only wheels, so it must be
    built from the checkout); libcoal 3.0.3 then needs cmeel-assimp, cmeel-octomap,
    cmeel-qhull and cmeel-boost, of which cmeel-octomap merged as PR #2159 but
    https://pypi.riseproject.dev/simple/cmeel-octomap/ still returns 404, i.e. its publish
    is still in flight. Revisit once cmeel-urdfdom AND libcoal are both on the registry;
    at that point re-running the single oracle command above is the whole check.
    UPDATE 2026-09-21T15:39Z (libcoal triage): cmeel-octomap''s and cmeel-qhull''s publishes
    have landed (both simple/ URLs now 200), so libcoal''s only remaining blocker is
    cmeel-assimp 6.0.5 -- libcoal is parked blocked-on-dependency with that single name,
    and cmeel-assimp is actionable (deps cmeel + cmeel-zlib only, no sdist on PyPI).
    libpinocchio''s own two blockers are unchanged: cmeel-urdfdom (actionable) and libcoal.'
- pkg: adbutils
  version: 2.12.0
  home: https://github.com/openatx/adbutils
  repo: https://github.com/openatx/adbutils
  status: parked
  pr: null
  notes: 'Not feasible (vendored-binary, gotcha 27/35/157/183; new gotcha 524). Same disposition and reasoning as the parked imageio-ffmpeg and pypandoc-binary entries. The package is pure Python (setup.py: pbr, zero ext_modules; the sdist builds a 45 KB py3-none-any wheel locally in seconds with no compiler -- verified, and it imports clean). Its 4 published wheels are py3-none-<platform> (manylinux1_x86_64, a macOS pair, win32/win_amd64) only because build_wheel.py builds one pure wheel, unpacks it, drops that platform''s prebuilt Android platform-tools binary into adbutils/binaries/, rewrites the WHEEL Tag: line and repacks -- so the manylinux1_x86_64 wheel still says Root-Is-Purelib: true while carrying 8.7 MB of x86-64 ELF adb (confirmed with file/readelf on the real 2.12.0 wheel; sdist is 191 KB). PLATFORM TABLE: build_wheel.py''s BINARIES_URL has exactly three entries, all dl.google.com/android/repository/platform-tools-latest-{linux,darwin,windows}.zip -- no arch segment at all, and latest-linux-{riscv64,aarch64,arm64}.zip all 404. The authoritative read is Google''s own SDK manifest, repository2-1.xml and repository2-3.xml, whose platform-tools remotePackage lists exactly three <archive> entries (host-os linux/macosx/windows) with no host-arch element whatsoever -- Google ships no riscv64 (and no aarch64) platform-tools. NOT playwright-shaped: adb is Apache-2.0 AOSP source and Debian builds it for riscv64 (adb binary package, riscv64 in stable/testing/unstable/experimental), so the primary function is reachable on this arch. It is imageio-ffmpeg-shaped instead: getting a riscv64 adb means THIS repo compiling it from a different tree than upstream uses -- AOSP''s build system cannot build the CLI tools alone, which is why nmeum/android-tools exists (CMake plus vendored libusb/PCRE/googletest/protobuf/brotli/zstd/lz4) -- something upstream''s CI never does on any platform, so there is no upstream recipe to mirror narrowed to riscv64 (goal 2) and nothing cheap to hand back to the maintainer (goal 4). The killer detail is that the fetch is unpinned (-latest-): every adbutils wheel carries whatever adb Google shipped on build day (r37.0.1 today), so a self-built riscv64 adb could never be version-matched to the x86_64 wheel of the same adbutils release, and the mismatch would re-open on every bump. NO GAP TODAY, and riscv64 is not uniquely excluded: upstream has never published a non-x86_64 Linux wheel in 131 releases (no aarch64 either) and no py3-none-any wheel since 0.7.3 in 2020, so riscv64 falls back to the sdist exactly as aarch64/ppc64el do, and adbutils._utils.adb_path() resolves ADBUTILS_ADB_PATH -> shutil.which(''adb'') -> the bundled binaries/ dir, i.e. the distro''s own riscv64 adb works. Shipping a py3-none-manylinux_2_39_riscv64 wheel with an empty binaries/ dir would ship zero arch-specific content (gotcha 27''s watchdog clause), and repackaging a Debian-built adb is not something this repo''s CI does. DEPS ARE NOT THE BLOCKER: check_riscv64_deps.py on 2.12.0''s own requirements (requests, deprecation, retry2, Pillow) resolves clean on cp312/cp313/cp314/cp314t against PyPI + our registry (pillow 12.3.0 riscv64 wheels are ours; the other three are py3-none-any). Note check_riscv64_deps.py adbutils bare reports UNRESOLVABLE -- gotcha 503''s false positive: --only-binary can only see the 0.7.3 py3-none-any wheel, whose dead apkutils2 dep has no wheel. 2.12.0 re-confirmed latest on PyPI (gotcha 388). Settled read-only: no worktree, branch, workflow, docs/packages yaml, patches or PR created, and no runner slot used. Revisit if Google ever publishes a riscv64 platform-tools archive -- that would make this a shellcheck-py-shaped one-URL port.'
- pkg: rocksdict
  version: 0.3.29
  home: https://github.com/rocksdict/RocksDict
  repo: https://github.com/rocksdict/RocksDict
  status: published
  pr: 1224
  notes: '54 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin binding compiling RocksDB (vendored rust-rocksdb fork) + snappy from source; drops the lto Cargo feature (needs clang) for bindgen-runtime only, matching upstream own Windows job shape.'
- pkg: pyvrl
  version: 0.0.2
  home: https://github.com/crowdalert/pyvrl
  repo: https://github.com/crowdalert/pyvrl
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2176
  notes: 'PR https://github.com/riseproject-dev/python-wheels/pull/2176 open (draft), CI green on run 35615588367: pyvrl-0.0.2-cp312-cp312-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl built in 26 min on the riscv64 runner (sha256 625a80e7...), the wheel test printed pyvrl OK on the real runner, the extension/licence check passed and the publish job dry-ran clean (would tag pyvrl-v0.0.2-<ts>, add pyvrl to packages.txt and open the docs PR). Left as a draft for the maintainer. maturin/pyo3 extension over the crates.io vrl 0.15 crate (Vector Remap Language compiler + stdlib): nothing vendored, no git dependencies, no downloads, 292-crate graph whose only C is onig_sys (oniguruma) and zstd-sys, both portable. Built from the v0.0.2 git tag with cibuildwheel (build-from-checkout, rustup in CIBW_BEFORE_ALL_LINUX, musllinux dropped per gotcha 10); upstream commits no Cargo.lock, but a fresh resolution today still lands on pyo3 0.20.3 + vrl 0.15.0, the same pair the released PyPI sdist''s lock pins, so gotcha 10''s floating-dep risk is empty here. Matrix is cp312 alone. pyo3 0.20.3''s pyo3-ffi build script hard-errors on any interpreter above 3.12, and gotcha 371''s PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1 escape hatch does not rescue it: is_abi3() returns true for that variable with no version condition, so Py_LIMITED_API is set and pyo3''s src/conversions/chrono.rs (#![cfg(all(feature = "chrono", not(Py_LIMITED_API)))]) is cfg-removed, at which point src/value.rs''s Value::Timestamp(ts) => Ok(ts.into_py(py)) fails to compile on every interpreter including cp312 - recorded as gotcha 527. UNSAFE_PYO3_SKIP_VERSION_CHECK=1 skips the ceiling without abi3 but is pyo3''s own undocumented test knob against a CPython the crate predates (gotcha 322), so it was rejected rather than shipped. cp314t is separately out (pyo3 0.20 predates PEP 703); pp310 is out of scope for this repo. Upstream''s own wheels stop at cp312 for the same pyo3 reason, so the narrow matrix matches upstream rather than diverging from it. Local validation, all on x86_64 with no riscv64 hardware: in quay.io/pypa/manylinux_2_28_x86_64, maturin 1.9 built pyvrl-0.0.2-cp312-cp312-manylinux_2_28_x86_64.whl (5.5 MB) containing pyvrl/pyvrl.cpython-312-x86_64-linux-gnu.so, the maturin-generated pyvrl/__init__.py re-export shim (gotcha 314) and pyvrl-0.0.2.dist-info/licenses/LICENSE, and the exact CI smoke test passed against the installed wheel - README remap example, to_int!/push!, parse_json! round trip, unicode upcase!, parse_timestamp! returning a real datetime.datetime with tzinfo (the chrono path), and both the compile-error and runtime-error paths. The same tree with PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1 failed to compile on all of cp312/cp313/cp314/cp314t with the identical chrono error, which is the evidence behind the matrix. A cross cargo check --release --target riscv64gc-unknown-linux-gnu --features pyo3/extension-module (debian:trixie, gcc-riscv64-linux-gnu, PYO3_CROSS_PYTHON_VERSION=3.12, gotcha 182) type-checked the whole tree clean in 56 s and produced real riscv64 objects for the two C deps (libonig.a / libzstd.a, readelf Machine: RISC-V). The riscv64 wheel build and wheel test are unvalidated locally; CI is the first run of both.'
- pkg: cadquery-ocp-novtk
  version: 8.0.1.0.0
  home: https://pypi.org/project/cadquery-ocp-novtk/
  repo: https://github.com/CadQuery/ocp-build-system
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2114
  notes: 'PR https://github.com/riseproject-dev/python-wheels/pull/2114 open. Ported 8.0.1.0.0, not the queued 7.9.3.1.1: 8.0.1.0.0 is the latest upstream wheel version, and CadQuery/ocp-build-system''s Linux recipe only became conda-free at v8.0.x - v7.9.3.1.1 built the OCCT SDK inside micromamba with conda freeimage/freetype/fontconfig, and conda-forge publishes no linux-riscv64 freeimage (recorded as gotcha 407). build-cadquery-ocp-novtk.yml mirrors upstream''s build-occt-sdk/build-ocp-source/build-ocp actions: an occt job builds OCCT 8.0.1 once per version (USE_VTK=OFF, USE_FREEIMAGE=OFF since riscv64 has no freeimage-devel, rapidjson at upstream''s pinned master rev and fmt 11.2.0 from source since neither is packaged, CMAKE_POLICY_VERSION_MINIMUM=3.5 for cmake 4.4.3) and hands it to the per-interpreter jobs as an artifact (gotcha 15); each of cp312/cp313/cp314 then downloads the prebuilt OCP_src_stubs_Linux.zip, applies upstream''s VTK/IVtk seds (647 translation units left of 655) plus its Development.Module fix (gotcha 374, already in upstream''s own recipe at this tag), compiles at -j(nproc-2) per upstream''s OOM note and packages/auditwheels/patchelfs the wheel exactly as upstream does. cp314t left out: pybind11 is pinned to 2.13 and the generated module declares no free-threaded support. Only runtime dependency is cadquery-ocp-proxy==8.0.1.0.0, a 3.2 KB py3-none-any wheel on PyPI, so nothing to port. Testing diverges from upstream: its suite is build123d, which requires lib3mf (py3-none x86_64/macOS/Windows wheels only, no riscv64 and no aarch64), so the wheel test is an OCP geometry smoke test (box volume and face count, boolean fuse; every API verified against the 8.0.1 OCP-stubs). Licensing: upstream''s published wheels carry no licence file at all (checked cadquery_ocp_novtk-8.0.1.0.0-cp312-manylinux_2_28_aarch64.whl: 415 entries, zero matching licen*) although they bundle the OCCT runtime under LGPL-2.1-with-exception, because pypi/pyproject.toml''s package-data names a package the built tree does not contain and the legacy license table suppresses setuptools'' glob; patches/cadquery-ocp-novtk/8.0.1.0.0/0001-*.patch switches it to PEP 639 license/license-files and the build stages LICENSE.<dep> for the whole vendored closure (rpm -qL sweep). Local validation: in quay.io/pypa/manylinux_2_39_riscv64 under QEMU, OCCT V8_0_1 with exactly these flags configured clean (rc=0, 78.1 s configuring + 17.3 s generating; Xlib and FreeType 2.13.2 detected, rapidjson picked up from /usr/include, "TKIVtk and TKIVtkDraw toolkits excluded due to VTK usage is disabled"), produced a 5,667-step ninja graph and compiled its first 10 TKernel objects with zero errors before the run was stopped - a full emulated build is impractical on a 4-core host at load 17, and INSTALL_DIR_LIB=lib for OCCT while fmt installs to lib64, which is why the workflow passes CMAKE_PREFIX_PATH and both lib paths instead of hardcoding one. Separately on x86_64: upstream''s VTK/IVtk/Development.Module seds applied to the real 8.0.1.0 generated sources leave 647 TUs and zero IVtk references, the patched pypi/pyproject.toml builds a wheel whose dist-info/licenses carries LICENSE plus every LICENSE.<dep> with correct cp312-manylinux_2_39_riscv64 tags and Root-Is-Purelib false, and OCP_specific.inc''s fmt::format usage compiles against fmt 11.2.0. The OCP compile itself and the wheel test are unvalidated locally; CI is the first run of both.'
- pkg: intel-cmplr-lib-ur
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  status: parked
  pr: null
  notes: 'Not feasible (park): intel-cmplr-lib-ur is Intel''s redistribution of the oneAPI Unified Runtime
    binaries built by the closed oneAPI DPC++/C++ Compiler. Unlike the rest of the oneAPI family the
    payload''s SOURCE IS OPEN -- Unified Runtime is Apache-2.0 WITH LLVM-exception,
    project(unified-runtime VERSION 0.12.0) in intel/llvm''s unified-runtime/
    (oneapi-src/unified-runtime is now only a mirror: "the source code has been moved to
    intel/llvm"), and 0.12.0 is exactly the SONAME of the wheel''s libur_loader.so.0.12.0 -- so this
    was checked concretely instead of parked by analogy with intel-openmp (gotcha 506). It parks
    anyway, on three independent locks, all verified against the artifacts. Filed as new gotcha 528
    (feasibility-and-triage.md). DISTRIBUTION: 22 releases, 44 files, 0 sdists ever (gotcha 431 --
    the wheel is the only evidence); tags only py2.py3-none-{manylinux1_x86_64,
    manylinux_2_28_x86_64, win_amd64}, never aarch64, never riscv64; License: Intel End User License
    Agreement for Developer Tools plus classifier License :: Other/Proprietary License; home and
    repo are Intel''s oneAPI Base Toolkit marketing page.
    intel_cmplr_lib_ur-2026.1.1-py2.py3-none-manylinux_2_28_x86_64.whl (31.6 MB, 17 files, 102 MB
    unpacked) has an EMPTY top_level.txt and zero Python modules: everything is under .data/data/lib
    as four unstripped libraries in three identical copies each (.so/.so.0/.so.0.12.0) --
    libur_loader.so 25.0 MB, libur_adapter_level_zero.so 4.2 MB, libur_adapter_level_zero_v2.so 4.0
    MB, libur_adapter_opencl.so 2.2 MB. No build system, nothing to compile, Root-Is-Purelib: true
    beside a platform tag. readelf -h says Machine: Advanced Micro Devices X86-64 for all four. The
    bundled LICENSE.txt (Intel EULA for Developer Tools, August 2024) 3.1 forbids (i) distributing
    or publicly displaying the Materials, (iv) modifying/adapting them and (v) reverse engineering
    them; the only distribution grant, 2.1.D, covers "Redistributables" in Executable Code "only as
    part of Your Product" under a licence that itself bars reverse engineering -- rehosting on
    pypi.riseproject.dev is none of those. THE SHIPPED TREE IS INTERNAL, SO GOTCHA 263''S tbb RESCUE
    DOES NOT REACH IT: the libraries ship with debug info, and their source paths read
    /netbatch/donb73197_00/runDir/dir/workspace/NIT/xmain-rel/LX/xmainefi2linux_release/ws/icsws/llvm/unified-runtime/source/{loader,loader/layers/{sanitizer,validation,tracing},adapters/level_zero,common}
    -- Intel''s netbatch build farm and Intel''s internal xmain-rel branch of intel/llvm, not a public
    ref. The distribution version 2026.1.1 is a oneAPI release-train number that matches no public
    tag (intel/llvm publishes nightly-YYYY-MM-DD and v7.1.1), where tbb''s 2023.1.0 matched oneTBB
    v2023.1.0 exactly. All three libraries carry the banner "Intel(R) oneAPI DPC++/C++ Compiler
    2026.1.1 (2026.1.1.20260724)" and DT_NEEDED libimf.so, libsvml.so, libirng.so, libintlc.so.5 --
    Intel''s closed compiler runtimes, published only by the equally x86_64-only intel-cmplr-lib-rt
    -- so the published bytes are not reproducible outside Intel even from the open source. FUNCTION
    IS NOT REACHABLE ON riscv64: the loader dlopens eight adapter names
    (libur_adapter_{level_zero,level_zero_v2,opencl,cuda,hip,native_cpu,offload,mock}.so.0) and the
    wheel ships exactly the three device-bound ones; the arch-neutral backends the open project has
    (native_cpu, offload) are the ones Intel does not ship. The Level Zero adapters dlopen
    libze_loader.so.1, whose only implementation is Intel''s GPU/NPU stack: intel/compute-runtime
    publishes amd64-only .debs, Debian''s libze1 and intel-opencl-icd are amd64-only, and Debian has
    no unified-runtime source package at any architecture. Every adapter also DT_NEEDs libumf.so.1
    from the hard pin Requires-Dist: umf==1.1.*; PyPI''s umf (open Apache-2.0-with-LLVM-exceptions
    upstream, oneapi-src/unified-memory-framework) has 7 releases, 14 files, 0 sdists, only
    manylinux_2_28_x86_64/win_amd64, and 404s on our index -- the dependency is itself unported and
    the same shape (gotcha 40). GOTCHA 465''S INVERTED TELL REPEATS: strings -a libur_loader.so |
    grep -ci riscv is 1015 (R_RISCV_*, RISCVISAInfo.cpp, RISCVAttributeParser.cpp,
    DW_CC_LLVM_RISCVVectorCall) but those are bundled LLVM BinaryFormat/TargetParser tables; the ELF
    is x86-64, there is no LLVMInitialize*Target symbol at all, and both adapters have 0 riscv
    strings. NO CONSUMER COULD EXIST: nothing in this repo''s workflows or package YAMLs references
    it, pypi.riseproject.dev 404s for both intel-cmplr-lib-ur and umf, and its only PyPI consumers
    are intel-openmp (Requires-Dist intel-cmplr-lib-ur==<same version>, already parked) and
    intel-sycl-rt -- both 0-sdist Intel binary wheels tagged manylinux*_x86_64/win_amd64 only, so on
    riscv64 no package can ever pull this in. Intel ships oneAPI for no non-x86 architecture at all:
    apt.repos.intel.com/oneapi dists/all/Release says Architectures: all amd64 i386 i686. QUEUE
    ENTRY WAS STALE (gotcha 388): the latest release is 2026.1.2, with the same two wheels, the same
    31.6 MB payload and the same shape, so the park covers it too. The queued siblings
    intel-cmplr-lib-rt, intel-cmplr-lic-rt, intel-sycl-rt, intel-opencl-rt, dpcpp-cpp-rt, mkl,
    mkl-static and mkl-include are the same family on the same evidence. No worktree, branch or PR
    created.'
- pkg: subprocess32
  version: 3.5.4
  home: https://github.com/google/python-subprocess32
  repo: https://github.com/google/python-subprocess32
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 303, feasibility-and-triage.md): PyPI classifiers are explicit -- Programming Language :: Python :: 2 :: Only -- and the only Linux artifact is a cp27 wheel plus an sdist; nothing this repo''s cp312/cp313/cp314/cp314t matrix could build as a real port. Confirmed from the real 3.5.4 sdist: setup.py only declares the _posixsubprocess32 C extension when sys.version_info[0] == 2; for every Python 3 it installs zero extensions and instead packages python3_redirect/__init__.py (sys.modules[''subprocess32''] = subprocess), a pure-Python compatibility stub. pip install subprocess32 on riscv64 today already builds that redirect from the sdist in under a second with no compiler, so a manylinux_riscv64 cp312+ wheel would ship zero arch-specific content for an interpreter range the C extension never targets -- a packaging convenience, not a port. No worktree/branch/PR created.'
- pkg: libcoal
  version: 3.0.3
  home: https://github.com/cmake-wheel/libcoal
  repo: https://github.com/cmake-wheel/libcoal
  status: blocked-on-dependency
  pr: null
  notes: '2 Linux wheels upstream (abi: 0); no riscv64 on PyPI or pypi.riseproject.dev.
    BLOCKED ON EXACTLY ONE DEPENDENCY: cmeel-assimp 6.0.5. Triaged read-only per gotcha
    523 (recompute the frontier per candidate) -- no branch, worktree, workflow,
    docs/packages yaml, patch or PR created and no CI cycle burned.
    SHAPE (confirmed; the port is otherwise ready to write). cmake-wheel/libcoal is the
    cmeel packaging fork of coal-library/coal, the renamed hpp-fcl; the 3.0.3 sdist''s
    pyproject.toml sets build-backend = "cmeel" with [tool.cmeel] has-sitelib = false and
    configure-args -DCOAL_HAS_QHULL=ON -DCOAL_BACKWARD_COMPATIBILITY_WITH_HPP_FCL=ON
    -DBUILD_PYTHON_INTERFACE=OFF, i.e. gotcha 487''s SINGLE-JOB shape -- the C++ library
    only, one interpreter-independent py3-none wheel, matching the released
    libcoal-3.0.3-0-py3-none-manylinux_2_28_{x86_64,aarch64}.whl whose dist-info/WHEEL
    reads Build: 0, Generator: cmeel 0.60.1, Root-Is-Purelib: false, Tag:
    py3-none-manylinux_2_28_aarch64 (the note''s "abi: 0" is PEP 427''s build number, not
    an ABI tag -- gotcha 470). build-libcoal.yml would therefore be a near-copy of
    build-cmeel-qhull.yml (single cp312 manylinux_riscv64 job, CIBW_REPAIR_WHEEL_COMMAND
    ""), the same sketch libpinocchio''s entry describes. Upstream tag is v3.0.3.c0, the
    only .cN for 3.0.3 per git ls-remote, so gotcha 505''s highest-tag rule is trivial
    here; a v3.1.0.c0 tag exists but PyPI has released only 3.0.1/3.0.2/3.0.3, so 3.0.3 is
    still the version to build (gotcha 388). CMakeLists.txt''s
    cmake_minimum_required(VERSION 3.22) is above CMake 4''s 3.5 floor, so unlike
    cmeel-qhull/cmeel-console-bridge no CMAKE_POLICY_VERSION_MINIMUM=3.5 is needed.
    BSD-3-Clause; cmeel already ships dist-info/licenses/LICENSE and the wheel bundles
    nothing, so no licensing patch and no gpl_sources job.
    THE BLOCKER, proven three ways. (1) METADATA: [build-system] requires = cmeel-assimp
    >= 6.0.5 < 7, cmeel-boost ~= 1.90.0, cmeel-octomap >= 1.9.8.2 < 2, cmeel-qhull >=
    8.0.2.1 < 8.0.3, cmeel[build], cmeel-eigen, jrl-cmakemodules; [project] dependencies
    repeats the first four with cmeel-assimp relaxed to >= 6.0.2 (identical to the
    released wheel''s Requires-Dist, checked against the live PyPI JSON). cmeel-assimp is
    thus a BUILD requirement, not merely a runtime one, so pip fails before any compiler
    runs -- gotcha 523''s build-vs-runtime distinction, the "no CI cycle worth spending"
    side. (2) LINK INPUT: readelf -d on cmeel.prefix/lib/libcoal.so inside the released
    manylinux_2_28_aarch64 wheel (266 entries, exactly one .so, nothing bundled, SONAME
    libcoal.so, RUNPATH $ORIGIN/../lib into the shared cmeel prefix) gives NEEDED
    libassimp.so.6 (cmeel-assimp), libqhull_r.so.8.0 (cmeel-qhull),
    liboctomap.so.1.10/liboctomath.so.1.10 (cmeel-octomap),
    libboost_{serialization,filesystem,atomic}.so.1.90.0 (cmeel-boost) and libz.so.1
    (cmeel-zlib, pulled in through assimp) -- so the missing sibling would also break the
    link, not just the metadata. (3) NO REDUCED-BUILD ESCAPE HATCH: CMakeLists.txt:197 is
    a bare find_package(assimp REQUIRED) with no option() guarding it (assimp is coal''s
    mesh loader, include/coal/mesh_loader/assimp.h), so there is no configure-arg that
    drops the dependency the way BUILD_PYTHON_INTERFACE drops the bindings; and even if
    there were, cmeel would still emit the unchanged Requires-Dist from [project]
    dependencies and the wheel would stay uninstallable. An in-container monolithic build
    of cmeel-assimp alongside libcoal is rejected for the reasons already recorded in
    pin''s and libpinocchio''s entries (it rewrites cmeel''s shared-prefix contract,
    publishes nothing, is outside the port''s commit scope and still leaves the
    Requires-Dist unsatisfiable).
    ORACLE, run twice because it stops at the first failure (gotcha 523).
    ci_scripts/check_riscv64_deps.py --python 312 -- ''cmeel-assimp>=6.0.5,<7''
    ''cmeel-boost~=1.90.0'' ''cmeel-octomap>=1.9.8.2,<2'' ''cmeel-qhull>=8.0.2.1,<8.0.3''
    ''cmeel[build]'' ''cmeel-eigen'' ''jrl-cmakemodules'' reports cp312 UNRESOLVABLE
    ("Could not find a version that satisfies the requirement cmeel-assimp<7,>=6.0.5 (from
    versions: none)"). The same command with cmeel-assimp dropped is ok on cp312, cp313
    AND cp314, resolving cmeel_boost-1.90.0-0-cp3XX-cp3XX-manylinux_2_39_riscv64.whl,
    cmeel_octomap-1.10.0-5-py3-none-manylinux_2_39_riscv64.whl,
    cmeel_qhull-8.0.2.1-1-py3-none-manylinux_2_39_riscv64.whl,
    cmake-4.4.3-py3-none-manylinux_2_31_riscv64.whl, numpy-2.5.3 riscv64 and the
    py3-none-any cmeel 0.61.0 / cmeel-eigen 3.4.1 / jrl-cmakemodules 2.1.0 -- proving
    cmeel-assimp is the ONLY blocker and that nothing else in the closure needs a port
    first.
    FRONTIER MOVED SINCE THE LIBPINOCCHIO TRIAGE EARLIER TODAY, which listed libcoal as
    needing cmeel-assimp + cmeel-octomap + cmeel-qhull with the latter two''s publishes
    still in flight: as of 2026-09-21T15:39Z https://pypi.riseproject.dev/simple/ serves
    SIX cmeel packages -- cmeel-boost, cmeel-console-bridge, cmeel-octomap, cmeel-qhull,
    cmeel-tinyxml2, cmeel-zlib (simple/cmeel-octomap/ and simple/cmeel-qhull/ both 200 now,
    both 404 at that triage) -- so libcoal went from four missing deps to one in a few
    hours. cmeel-assimp 6.0.5 is itself ACTIONABLE, not blocked: its requires_dist is just
    cmeel (py3-none-any) and cmeel-zlib (on the registry). It is now the single package
    whose port unblocks this whole upper chain (libcoal -> coal / libpinocchio -> pin),
    and its own entry is still marked blocked-on-dependency from before cmeel-zlib landed.
    Note it publishes NO sdist on PyPI (four wheels only), so it must be built from the
    checkout. No cmeel-family PR is open in this repo as of this triage.
    REVISIT once https://pypi.riseproject.dev/simple/cmeel-assimp/ returns 200; at that
    point re-running the single oracle command above is the whole check, and the workflow
    sketched above can be written directly.'
- pkg: jax-cuda12-plugin
  version: 0.11.1
  home: https://github.com/jax-ml/jax
  repo: https://github.com/jax-ml/jax
  status: parked
  pr: null
  notes: 'Parked: JAX''s CUDA 12 backend plugin is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: jax-cuda12-pjrt
  version: 0.11.1
  home: https://github.com/jax-ml/jax
  repo: https://github.com/jax-ml/jax
  status: parked
  pr: null
  notes: 'Parked: JAX''s CUDA 12 PJRT runtime is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: certbot-dns-multi
  version: 5.4.0
  home: https://pypi.org/project/certbot-dns-multi/
  repo: https://github.com/alexzorin/certbot-dns-multi
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2178
  notes: 'PR #2178 open (draft, branch certbot-dns-multi). Upstream located by search despite the empty PyPI project_urls: github.com/alexzorin/certbot-dns-multi, MIT, tags are bare version numbers (no `v` prefix); tag 5.4.0 = 3f8d156d97b3e1a243dc1de0c6b30f9c686067b3, which matches the sdist _version.py''s __commit_id__ g3f8d156d9. The cp311-cp314 tags are NOT cosmetic and this is not gotcha 27/464''s fabricated-tag shape: setup.py declares a real Extension("lego_bridge", ["certbot_dns_multi/_internal/bridge/main.go"]) with build_golang, so setuptools-golang builds a genuine CPython extension module out of cgo - certbot_dns_multi/_internal/bridge/main.c holds PyInit_lego_bridge/PyModuleDef and main.go holds the //export lego_bridge_cmd entry that go build -buildmode=c-shared links into it. The 33-36 MB per-wheel payload is the embedded go-acme/lego v5.4.0 provider set plus the Go runtime, and the per-interpreter tags are honest (the .so links against the interpreter''s C API). Feasibility is clean: internal/platform/supported.go lists linux/riscv64 in the c-shared buildmode table, go1.26.0.linux-riscv64.tar.gz exists, setuptools-golang 2.9.0 has no architecture table at all (it only shells out to `go build`), and a GOOS=linux GOARCH=riscv64 CGO_ENABLED=0 cross build of github.com/go-acme/lego/v5/providers/dns - the whole dependency closure the bridge imports - succeeded on x86 in under two minutes, so no Go dependency is arch-gated. build-certbot-dns-multi.yml is build-from-checkout at the tag with cibuildwheel over cp312/cp313/cp314; upstream''s own .github/build-manylinux.py has an arch->image map with only amd64/arm64, so cibuildwheel replaces it while keeping upstream''s recipe (the same go.dev tarball curl that .github/build-wheel.sh does, for GOARCH=riscv64, in CIBW_BEFORE_ALL_LINUX). No cp314t: the Go bridge declares no Py_mod_gil slot, so a free-threaded wheel would silently re-enable the GIL at import, and upstream ships cp311-cp314 only. SETUPTOOLS_SCM_PRETEND_VERSION_FOR_CERTBOT_DNS_MULTI is set because the pyproject reads its version from the setuptools_scm-written _version.py and the git tag is not reliably readable inside the container; the _FOR_ name normalisation was verified locally against the real checkout. CIBW_TEST_ENVIRONMENT pins PIP_ONLY_BINARY=cryptography so the test venv takes our registry''s riscv64 cryptography (50.0.1 cp312-abi3 covers the whole matrix) instead of source-building PyPI''s Rust sdist; certbot itself and the rest of its closure are py3-none-any. No patches needed. Upstream ships no test suite (tests/ holds only an empty __init__.py), so the workflow stages one from a run: heredoc and runs it via CIBW_TEST_SOURCES: it drives the compiled bridge over its JSON protocol - unknown action, unknown provider, a provider whose credentials are missing, and a full configure/perform/cleanup round trip through lego''s `exec` provider in EXEC_MODE=RAW, which takes the branch that skips dns01.GetChallengeInfo and therefore needs no network or DNS at all - then loads the certbot.plugins entry point and asserts it is a dns_common.DNSAuthenticator subclass. Local validation before pushing: the wheel builds on this x86_64 host from the tag (pip wheel, ~4 min) and all 6 staged tests pass against the installed wheel after extracting the heredoc byte-for-byte out of the YAML; a riscv64 rehearsal of the same build ran under docker+QEMU on quay.io/pypa/manylinux_2_39_riscv64 (go1.26.0 linux/riscv64 installs and runs; the cgo compile itself is slow under emulation). Registry check: nothing for certbot, acme or josepy on pypi.riseproject.dev, but all three are py3-none-any on PyPI, so only cryptography/cffi need riscv64 wheels and both are hosted. CI concluded all-green (cp312/cp313/cp314 riscv64 legs + publish dry-run all success, run 35621980869); PR #2178 marked ready for review.'
- pkg: iterfzf
  version: 1.9.0.67.0
  home: https://github.com/dahlia/iterfzf
  repo: https://github.com/dahlia/iterfzf
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2177
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PORTED - PR https://github.com/riseproject-dev/python-wheels/pull/2177 open (draft), CI green on run 35621970585: iterfzf-1.9.0.67.0-py3-none-manylinux_2_39_riscv64.whl built on the riscv64 runner in 37 s (sha256 952a1bda1d8bb0b567f3b566bbfc9b365bab1f95fb4e3de1594205b6612a3802), all four test legs (3.12/3.13/3.14/3.14t) green with the bundled binary printing 0.67.0 (2ab923f3) on the real runner and 11 passed / 3 deselected each, and the publish job dry-ran clean (would tag iterfzf-v1.9.0.67.0-20260921162444, add iterfzf to packages.txt and open the docs PR). Left as a draft for the maintainer. An earlier head (0652c1c) failed check_commit_messages only because the PR body carried a tool-injected attribution footer at creation time; the body was cleaned and the branch re-pushed (the pull_request payload is not refreshed by editing the body, so a new head commit is the only way to re-run that check). Vendored-prebuilt-binary shape (gotcha 35/500 family) with the opposite verdict to adbutils (gotcha 524): the vendor already publishes our architecture. iterfzf is pure Python around the fzf binary; its PEP 517 backend build_dist.py downloads junegunn/fzf''s own release asset for a (GOOS, GOARCH) pair and renames the wheel from a goos_goarch_platform_tag_map entry, with a parallel platform_machine_goos_goarch_map for native detection. The version string names the release to check - 1.9.0.67.0 is wrapper 1.9 + fzf 0.67.0 - and fzf-0.67.0-linux_riscv64.tar.gz exists; 0.67.0 is the first fzf release carrying it (0.66.0, 0.65.0, 0.60.0, 0.50.0, 0.46.0, 0.44.0, 0.40.0, 0.30.0 all 404 for that asset name), i.e. exactly the release this wrapper pins. Nothing is compiled, so no Go toolchain, no manylinux container and no gpl_sources job (GPL-3.0-or-later covers the pure-Python wrapper, whose source ships in the wheel; the bundled fzf is MIT). Port is one patch adding the riscv64 row to both tables (missing = KeyError: (''linux'', ''riscv64'') with explicit GOOS/GOARCH, RuntimeError: unsupported platform natively, which is also what a user''s pip install from the sdist hits), plus upstream''s own release.yaml line narrowed to riscv64: GOOS=linux GOARCH=riscv64 python -m build --wheel. Tag chosen is manylinux_2_39_riscv64, matching austin-dist''s hand-set tag precedent. Local rehearsal on x86_64 (the build is arch-independent - a download plus a zip): the patched 1.9.0.67.0 checkout built iterfzf-1.9.0.67.0-py3-none-manylinux_2_39_riscv64.whl (1,693,756 bytes) containing iterfzf/fzf, the release json, test_iterfzf.py and dist-info/licenses/LICENSE, no fzf.exe; the bundled binary is byte-identical (sha256 5693efd07c5b3a327f2a0401d4f38c6904e0b9034066a27b4ba4f495c909cc53) to the official asset, whose tarball sha256 9e578fe8ee88f71498a606551b75d70f1bfadae7602e39860e6e2b88367ecbb4 matches fzf_0.67.0_checksums.txt, and it is a statically linked rv64gc double-float Go binary (ELF e_machine 243, no libc, no vendor ISA - gotcha 449 clear). The same tree with GOOS/GOARCH=linux/amd64 reproduces upstream''s own released wheel filename. Testing mirrors upstream''s tox.ini, minus two of its four commands: iterfzf([]) raises AttributeError at this tag on every platform (the subprocess is spawned only on the first item, so stdin stays None), reproduced against upstream''s own published manylinux x86_64 wheel - recorded as gotcha 529. The wheel ships iterfzf/test_iterfzf.py, so the job runs pytest --pyargs iterfzf.test_iterfzf against the installed artifact: 11 passed, 3 deselected (they open the full-screen picker and need an interactive terminal; running them under a pty hangs rather than passes, which is why upstream''s CI runs tox and not this file). Note the backend renames only the wheel filename - dist-info/WHEEL still says Tag: py3-none-any - and both pip and uv install it regardless. Interpreter matrix is the default four; the wheel is interpreter-agnostic, so every leg exercises the same binary. Unvalidated locally: the api.github.com release lookup (this session''s proxy blocks that repo''s API, so the rehearsal seeded the sdist''s cached release json) and the riscv64 runner itself.'
- pkg: reuse
  version: 6.2.0
  home: https://reuse.software/
  repo: https://github.com/fsfe/reuse-tool
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 532, refining gotcha 481; plus gotcha 27''s watchdog clause and gotcha 383''s sdist check): nothing is compiled on any platform and riscv64 pip install already works today. The single upstream wheel reuse-6.2.0-cp310-cp310-manylinux_2_41_x86_64.whl has 63 entries, all .py / .mo / .json / .txt / .rst / .jinja2 plus dist-info -- `file` finds no ELF anywhere, nothing greps PyInit_, and no .so/.pyd is present, despite Root-Is-Purelib: false. The cpXY-cpXY-manylinux tag is fabricated by poetry-core, which marks any project declaring [tool.poetry.build] script as non-pure; reuse''s _build.py only shells out to msgfmt over po/*.po, i.e. it compiles gettext .po into arch-independent .mo. Dated from the release history (ci_scripts/queue_triage.py + PyPI JSON for all 36 releases): 0.7.0-1.0.0 are all py3-none-any and the tag flips at exactly 1.1.0, the release that added [tool.poetry.build] generate-setup-file=false / script="build.py" (1.0.0''s pyproject.toml has neither), with no C source added. Both halves of the tag then wander with the publishing runner and not the content: cp311/glibc 2.36 (1.1.x), cp310/2.35 (2.x), cp311/2.36 (3.x-4.x), cp313/2.40-2.41 (5.x), cp310/2.41 (6.x). Confirmed by building the real 6.2.0 sdist: pip wheel <sdist> --no-deps takes 2.0 s with no compiler and yields reuse-6.2.0-cp311-cp311-linux_x86_64.whl on a 3.11 x86_64 host, and reuse-6.2.0-cp312-cp312-linux_riscv64.whl inside quay.io/pypa/manylinux_2_39_riscv64. Every release ships an sdist and the whole install path resolves to riscv64 wheels: the 7 hard runtime deps (Jinja2, license-expression, python-debian, tomlkit, attrs, click, python-magic) resolved ok for cp312/cp313/cp314/cp314t with ci_scripts/check_riscv64_deps.py -- every one py3-none-any except markupsafe, which our own registry already serves as markupsafe-3.0.3-cp3XX-manylinux_2_31_riscv64.manylinux_2_39_riscv64.whl -- and poetry-core, the only build requirement, is py3-none-any. Proved end to end on real riscv64 rather than inferred: in a linux/riscv64 QEMU container of the manylinux_2_39_riscv64 image, `pip install ./reuse-6.2.0.tar.gz` completed in 66 s (emulated; far less natively), `import reuse` gave 6.2.0, `reuse --version` ran, and `reuse lint` did real SPDX parsing over the extracted sdist tree. One genuine wrinkle that jsonschema2md did not have, recorded as gotcha 532: _build.py resolves msgfmt with shutil.which and silently does nothing when it is absent, so the catalogs are conditional on a host tool. The manylinux riscv64 image ships no msgfmt, so a from-sdist install there has 0 .mo where upstream''s published x86_64 wheel has 17; `dnf -y install gettext` before the build restores all 17 (both measured in the container). That is the only thing a riscv64 wheel would add, and it is not a reason to port: the data is arch-neutral, so the fixes are a build-time gettext or -- better -- an upstream py3-none-any wheel, which would also erase this queue entry''s premise, not a fabricated cpXY-cpXY-manylinux_2_39_riscv64 tag. Structural goal 2 has nothing to start from either: upstream''s .github/workflows publishes the one wheel from a single ubuntu runner with no cibuildwheel and no arch matrix, so every non-x86_64-Linux user already installs from the sdist. Verified read-only plus local builds in .git/pw-scratch/reuse (removed): PyPI JSON for all releases, both 6.2.0 artifacts unzipped and file(1)-scanned, the 1.0.0 and 1.1.0 sdists'' pyproject.toml, _build.py, and the two dependency checkers. No worktree, no branch, no workflow, no docs/packages entry, no PR.'
- pkg: memcache-hybrid
  version: 1.2.0
  home: https://gitcode.com/Ascend/memcache
  repo: https://gitcode.com/Ascend/memcache
  status: parked
  pr: null
  notes: 'Parked: second-order member of the parked Huawei Ascend NPU family -- blocked by its dependency before its own code, recorded as gotcha 531 (feasibility-and-triage.md), alongside gotcha 480 (memfabric-hybrid) and gotcha 516 (torch-npu). Verified against the real source at tag v1.2.0 and the real released wheel, not inferred from the vendor name: tag v1.2.0 = 4310d68fa8964a3032b71c09296b0ffa80330033, exactly the commit stamped in the released wheel''s memcache_hybrid/VERSION, while the default branch already reads VERSION 1.3.0, so the tag is what was read (gitcode.com/Ascend/memcache clones fine through the agent proxy). What it is: MemCache, a distributed KV cache / large memory pool giving H2D, D2H, D2RH and RH2D access for Ascend inference PrefixCache workloads, exposed as the pybind11 module _pymmc over libmf_memcache. PyPI has 70 files across 12 releases, every one manylinux_2_26/2_28 aarch64 or manylinux_2_27/2_28 x86_64, and no sdist in any release; 1.2.0 is 8 wheels, cp310-cp313. THE DECISIVE WALL IS THE DEPENDENCY, NOT THE VENDOR TOOLKIT, and it is two commands deep. install_requires is exactly one entry, memfabric_hybrid>=1.2.0 -- the sibling queue entry already parked under gotcha 480 -- and ci_scripts/check_riscv64_deps.py ''memfabric_hybrid>=1.2.0'' reports UNRESOLVABLE on cp312/cp313/cp314/cp314t, with pypi.riseproject.dev/simple/memfabric-hybrid/ and .../memcache-hybrid/ both 404. The import is unconditional, so the wheel would be unimportable rather than merely degraded: the released memcache_hybrid/__init__.py opens with `from memfabric_hybrid import bm` and then binds bm.BmCopyType.L2G/G2L/G2H/H2G/AUTO at module level, so `import memcache_hybrid` raises ImportError on any host without the parked sibling -- gotcha 516''s dead-on-arrival one layer up, in Python rather than the dynamic loader. The sibling is a BUILD input too, so vendoring it is no escape: .gitmodules pins 3rdparty/memfabric_hybrid -> gitcode.com/Ascend/memfabric_hybrid at 9a9cfbc8a1e026e7c73a558ff27d75372cb2e803, v1.2.0''s CMakeLists.txt glob-includes every header under 3rdparty/memfabric_hybrid/src (filtering only hybm/csrc/driver/npu_direct_rdma), and script/build.sh puts that submodule''s output/smem/lib64 and output/hybm/lib64 on LD_LIBRARY_PATH so auditwheel repair can resolve. Porting memcache therefore requires porting memfabric first, whose ubs-comm transport dies on its first compiler flag on riscv64 (gotcha 480). Three corroborating walls in memcache''s own tree. (1) CANN is reached by dlopen, gotcha 480''s shape exactly: readelf -d over all 20 shipped .so shows only libstdc++/libm/libgcc_s/libc/librt/libpthread/libdl plus each other, but strings finds libascendcl.so inside both libmf_memcache-38af6187.so and libubsio_kvc.so.1, and the source names it outright -- src/memcache/csrc/client/dl_acl_api.cpp:20 `const char *DlAclApi::gAscendAclLibName = "libascendcl.so";`, with ock::mmc::DlAclApi wrapping aclrtMalloc/Free/Memcpy/Memcpy2d/MemcpyBatch/MemcpyAsync/SetDevice/GetDevice/GetSocName/CreateStream/SynchronizeStream and ock::ubsio::ACLApi repeating it in the SSD backend; the env keys are ASCEND_HOME_PATH and ASCEND_RT_VISIBLE_DEVICES, and this is the cache''s H2D/D2H data path, not an optional accelerator. (2) There is no CPU-only or generic build mode, not even a fake one to disprove (gotcha 516''s cleaner stop): v1.2.0''s CMakeLists declares 12 option()s -- ENABLE_TRACE_LOG, ENABLE_ASAN, BUILD_TESTS, BUILD_OPEN_ABI, ENABLE_CPU_MONOTONIC, BUILD_GIT_COMMIT, BUILD_GIT_COMMIT_GEN_FILE, BUILD_PYTHON, ENABLE_PTRACER, BUILD_CPP_EXAMPLE, MMC_ENABLE_KV_EVENTS, BUILD_UBSIO -- none of them a backend/XPU selector, and setup.py reads only MEMCACHE_VERSION and IS_MANYLINUX, so there is no XPU_TYPE=NONE and no ''+cpu'' version stamping to argue about as in gotcha 480. (3) The SSD backend that ships inside the wheel does not compile on riscv64: cmake/config_ubsio.cmake FetchContent''s https://gitcode.com/openeuler/ubs-io.git @ develop and runs ubsio-boostio/build.sh, script/build.sh defaults BUILD_UBSIO=ON (7th arg), and the released wheel does carry libubsio_kvc.so.1.0.0, libbio_*.so, libock_*.so, libboundscheck.so, libhtracer.so, libcli_agent.so and liburing.so.2.6 under memcache_hybrid/lib/; inside ubs-io (cloned and read) ubsio-boostio/src/disk/CMakeLists.txt is a negated-x86 else-switch -- `if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(arm|aarch64)$")` takes arm/, `else()` takes x86/, whose x86/cpu_vendor_checker.h:40 is `__asm__ __volatile__("cpuid" ...)` -- so riscv64 falls into the x86 arm and dies on the instruction, the same shape gotcha 480 found in ubs-comm, which ubs-io itself then FetchContent''s from gitcode.com/openeuler/ubs-comm @ tag_BeiMing-I26.0.RC1.B013; ubsio-boostio/CMakeLists.txt and ubsio-memstore/CMakeLists.txt add -march=armv8-a+crc for aarch64 only. Supporting axis evidence: upstream''s README states its hardware axis by delegation -- the whole compatibility section is one line saying MemCache depends on MemFabric and its supporting matrix is the same as MemFabric''s, i.e. the parked sibling''s Atlas hardware / aarch64,x86 / CANN 8.1.RC1+ / Ascend HDK axis; script/run_pkg_maker/make_run.sh and install.sh are explicit uname -m allowlists (x86_64 | aarch64 | else "it is not system of x86_64 or aarch64"; exit 1); test/ut/CMakeLists.txt includes and links ${ASCEND_CANN_PACKAGE_PATH}/aarch64-linux/{include,lib64}; example/cpp/CMakeLists.txt FATAL_ERRORs without ASCEND_HOME_PATH; the k8s deploy manifests and benchmark scripts source /usr/local/Ascend/ascend-toolkit/set_env.sh and /usr/local/memfabric_hybrid/set_env.sh; and grep -rin riscv returns zero hits over both the memcache tree and the ubs-io tree. NOT blocking, recorded for honesty: src/memcache/csrc/common/mmc_montotonic.h''s __x86_64__ __rdtsc and __aarch64__ cntvct paths sit under USE_PROCESS_MONOTONIC, which the default ENABLE_CPU_MONOTONIC=OFF never defines, and the #else branch is portable clock_gettime(CLOCK_MONOTONIC). Revisit only if Huawei ships CANN and the Ascend HDK for riscv64 and memfabric-hybrid is unparked; this entry is strictly downstream of that one. Verified read-only plus two clones in .git/pw-scratch/memcache-hybrid/ (removed): PyPI JSON for all 12 releases, the cp311 manylinux_2_27_x86_64 wheel unzipped with readelf -d and strings over all 20 .so, gitcode.com/Ascend/memcache at tag v1.2.0, gitcode.com/openeuler/ubs-io at develop, both package indexes and ci_scripts/check_riscv64_deps.py. No worktree, branch, workflow, docs/packages entry, patch or PR was created.'
- pkg: pyslang
  version: 11.0.0
  home: https://sv-lang.com/
  repo: https://github.com/MikePopoloski/slang
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2182
  notes: 'pybind11 bindings for slang, a SystemVerilog compiler front end (~95k lines of C++20). scikit-build-core + CMake; fully portable - the only x86 intrinsic block (source/numeric/SVIntHelpers.h ``_addcarry_u64``/``_subborrow_u64``) has a portable C fallback on the ``#else`` branch, and the vendored boost_unordered/boost_concurrent headers carry a non-SIMD path. mimalloc is forced OFF for pylib builds and cpptrace defaults OFF, so the only fetched dep is fmt 12.1.0 (static); pybind11 comes from the pip build requirement, boost_unordered/expected-lite/BS_thread_pool are vendored headers. No runtime deps at all (``pytest`` only as a test extra), so the closure is trivially riscv64-clean. build-pyslang.yml mirrors upstream''s python-dist.yml build_wheels job (bare cibuildwheel, upstream''s own [tool.cibuildwheel] table, cp312/cp313/cp314/cp314t manylinux; upstream skips musllinux itself). Tag maps v11.0 -> wheel 11.0.0. Only divergence: SKBUILD_WHEEL_LICENSE_FILES widens upstream''s ``wheel.license-files = ["LICENSE"]`` so fmt/pybind11/BS_thread_pool/BSL-1.0 notices ship in the wheel. Local x86 pre-flight: wheel builds in 8 min on 4 cores (LTO from pybind11_add_module), CMake 4 configure clean, stubs generated by pybind11-stubgen, 109/109 upstream tests pass. PR #2182 green (run 35636833236): all four riscv64 legs built in 1h17-1h46 and ran 109/109 upstream tests, publish dry-ran cleanly. cp314t works but pyslang.cpp uses a plain PYBIND11_MODULE, so CPython re-enables the GIL at import with a RuntimeWarning - same as upstream''s own cp314t wheel, not a riscv64 issue. New gotcha 538: scikit-build-core reads every setting from SKBUILD_<SECTION>_<KEY> (lists split on ``;``), so an explicit wheel.license-files list is widened from CIBW_ENVIRONMENT with no patch.'
- pkg: decord2
  version: 3.4.0
  home: https://github.com/johnnynunez/decord2
  repo: https://github.com/johnnynunez/decord2
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2181
  notes: 'Fork/successor of decord (already published here). Portable: zero SIMD intrinsics in src/ or include/, and the whole CUDA/NVDEC tree is behind USE_CUDA, which upstream''s own CPU wheels set OFF - no GPU blocker. FFmpeg is not built from source: scripts/fetch-vendor.py downloads the prebuilt pyav-ffmpeg 8.1.1-1 tarball, and PyAV-Org/pyav-ffmpeg already publishes ffmpeg-manylinux-riscv64.tar.gz - a generic rv64gc build (ELF ''UCB RISC-V, RVC, double-float ABI''), not a vendor-ISA one (gotcha 449), configured --arch=riscv64 --enable-cross-compile with alsa/gnutls/dav1d/svtav1/vpx/webp/x264/x265/libvpl. So build-decord2.yml mirrors upstream''s pypi.yml CPU job almost verbatim and reuses build-av.yml''s shape (same vendor tarball, same GPL obligation): a vendor_sources job on ubuntu-latest collects the 17 pinned source tarballs + licence texts from pyav-ffmpeg''s scripts/pkg.py, then cibuildwheel (package-dir python, cp312/cp313/cp314/cp314t) runs fetch-vendor + cmake -DUSE_CUDA=OFF and auditwheel bundles the FFmpeg stack into decord2.libs/. Deps resolve: only numpy (2.5.x riscv64 on the registry for all four interpreters); cmake/ninja from [build-system] requires have riscv64 wheels on PyPI. One patch: upstream''s pyproject sets enable = ["cpython-freethreading"], which cibuildwheel 4.2.0 refuses to parse for EVERY interpreter (gotcha 184; upstream pins 3.4.1). New gotcha 533: the gitlab code.videolan.org ''-/archive/'' tarballs (dav1d, x264) are regenerated per request and hash differently from the pin - three different digests seen before one matched - so the collector now retries on a sha256 mismatch; build-av.yml carries the same latent flake. x86 pre-flight against the same 8.1.1 tarball reproduced the full result before pushing. CI green: 4 wheels (manylinux_2_39_riscv64, ~19.8 MB, 33 bundled libraries, 18 licence files each), 36 passed / 1 skipped on every interpreter, publish dry-run clean with gpl-sources.tar (80 MB). PR #2181 left as draft.'
- pkg: moderngl
  version: 5.12.0
  home: https://github.com/moderngl/moderngl
  repo: https://github.com/moderngl/moderngl/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1742
  notes: 'Hand-written C++ extension (src/moderngl.cpp), no build-time native deps - GL is called through function pointers loaded by glcontext (already on the registry) at runtime, same shape as the glcontext port itself (PR #1396). build-moderngl.yml mirrors upstream''s release.yml (bare cibuildwheel, cp312/cp313/cp314/cp314t manylinux+musllinux). manylinux runs the real upstream pytest suite via a standalone EGL context (tests/conftest.py), deselecting test_local (asserts an editable install, never true for a wheel); 359 passed. musllinux runs a context-creation smoke test instead - numpy/scipy have no musllinux riscv64 wheel. PR #1742 green (build+publish dry-run), awaiting review.'
- pkg: typst
  version: 0.15.0
  home: https://github.com/messense/typst-py/
  repo: https://github.com/messense/typst-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1166
  notes: 'PyO3/maturin extension (bindings to the typst compiler: PDF/SVG/PNG/HTML rendering). pyo3 carries abi3-py38 unconditionally (Cargo.toml); the riscv64 manylinux image has no cp38-cp311, so built/tested on our own cp312 floor instead (cp312-cp314 reuse-tested, plus a separate cp314t build). typst-kit''s vendor-openssl feature needs perl (Rocky 10 ships no FindBin.pm, CLAUDE.md gotcha 46). Upstream''s newest git tag v0.15.1 is unreleased on PyPI; ported v0.15.0, the latest actual release. Rehearsed the full pytest suite (80 passed) locally against the real published PyPI wheel before pushing. build-typst.yml added; PR #1166 open, watching CI.'
- pkg: squawk-cli
  version: 2.63.0
  home: https://squawkhq.com
  repo: https://github.com/sbdchd/squawk
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2180
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. FEASIBLE and ported - PR https://github.com/riseproject-dev/python-wheels/pull/2180 open (draft, branch squawk-cli). Not a vendored-prebuilt-binary case (gotcha 35/524/529): this is gotcha 117''s third shape, a maturin `bindings = "bin"` project compiled from source. The released manylinux_2_28_x86_64 wheel holds exactly four entries - METADATA, WHEEL (Generator: maturin (1.7.1)), RECORD and a 21.6 MB squawk_cli-2.63.0.data/scripts/squawk - no .so, no importable module, so the py3-none-<platform> tag is real and there is no interpreter matrix on the build job. Upstream publishes a complete sdist (1,105 files, root Cargo.toml + Cargo.lock + 13 workspace crates), and in the git checkout the pyproject.toml lives at crates/squawk/pyproject.toml (maturin sdist hoists it to the root and adds manifest-path), so the build uses maturin-action''s working-directory: crates/squawk rather than taplo''s write-a-pyproject step. Feasibility evidence on this x86_64 host: `cargo metadata --filter-platform riscv64gc-unknown-linux-gnu --locked` resolves 306 packages; only 34 have build scripts and openssl-sys is the single one with a `links` key - no bindgen, no clang-sys, no ring/aws-lc, and no build.rs anywhere in the squawk workspace itself; `cargo check --locked --target riscv64gc-unknown-linux-gnu` over squawk-parser/-syntax/-linter/-ide/-fmt/-lexer/-line-index/-thread/-server (everything except the reqwest-using squawk-github) is clean in 24 s. The one native piece is the vendored OpenSSL that reqwest''s native-tls-vendored feature pulls in: openssl-src 300.5.2+3.5.2 maps riscv64gc-unknown-linux-gnu to linux64-riscv64 in its own target table, and a cross attempt got as far as ''Configuring OpenSSL version 3.5.2 for target linux64-riscv64'' before dying only on this host''s missing riscv64-linux-gnu-gcc, which the native riscv64 runner supplies. Rust side is fine too: rust-toolchain.toml pins stable 1.94 (not nightly, so gotcha 238 does not bite) and rust-1.94.0-riscv64gc-unknown-linux-gnu.tar.gz exists. Workflow mirrors the `linux` job of upstream''s python.yml with three deliberate deviations. (1) maturin-version deliberately left unset: upstream pins v1.7.1 and maturin-riscv64gc-unknown-linux-gnu.tar.gz 404s for that tag (1.9.5 is 200), i.e. exactly gotcha 344''s gzip: stdin: not in gzip format failure; the pyproject''s own requires is the range maturin>=1.7,<2.0, which findReleaseFromManifest resolves against the live release list, so no explicit override is needed. (2) before-script-linux installs perl-core instead of upstream''s yum perl-IPC-Cmd, per gotcha 46 - Rocky 10 also lacks FindBin, which OpenSSL''s Configure needs - alongside the usual git safe.directory line. (3) upstream''s clang/llvm/llvm-devel and `python3 -m ensurepip` are dropped, since nothing in the riscv64 graph binds C headers and a bin wheel needs no pip in the container. timeout-minutes 1440 per gotcha 365 (tokio + hyper + h2 + vendored OpenSSL + an LSP server is the exact shape that stalled there). No patches; license is Apache-2.0 OR MIT with LICENSE-APACHE/LICENSE-MIT at the upstream root and nothing third-party vendored into the binary beyond statically-linked OpenSSL. Testing: upstream packages no wheel-level suite (its Rust/insta tests are not in the wheel) and the wheel carries no importable module, so there is no `python -m squawk` (gotchas 187/224); the test job drives the installed console script instead - --version, --help, upload-to-github --help (the subcommand behind the OpenSSL dependency), an ELF check, a clean file exiting 0, an ALTER TABLE ... ADD COLUMN ... NOT NULL migration exiting 1 with 5 rules, and --exclude removing exactly prefer-bigint-over-int from the JSON reporter. Worth knowing: squawk exits 1 on violations under EVERY reporter including --reporter json, so each lint call needs `|| rc=$?` under set -e. That whole test step was extracted from the YAML and run byte-for-byte against upstream''s own published 2.63.0 x86_64 binary on this host before pushing (exit 0). No docker daemon in this session, so no QEMU rehearsal of the riscv64 build itself - the real runner is the first end-to-end proof. CI fully green on run 35635450968: the riscv64 build step ran 18:00:35 -> 18:47:42 UTC (47 min, of which the vendored OpenSSL is ~4 min, 18:08:57 -> 18:12:59) and produced squawk_cli-2.63.0-py3-none-manylinux_2_39_riscv64.whl (sha256 81e360801eabdfddb84e98fd828dd9e5f8536ed5ab9600f32d648353c63eeead); all four test legs (3.12/3.13/3.14/3.14t) green in about a minute each, with the real riscv64 binary reporting the expected 5 issues on the fixture migration; publish dry-ran clean (would tag squawk-cli-v2.63.0-20260921185000, add squawk-cli to packages.txt and open the docs PR). The dropped maturin-version pin resolved as predicted - the log reads `Found maturin release from manifest: v1.15.0`, i.e. the >=1.7,<2.0 range picked a release that does ship riscv64 assets - and perl-core pulled in the full el10 perl distribution. maturin also notes the wheel is eligible for manylinux_2_38, which is informational only. The dropped-pin inversion of gotcha 344 is recorded as gotcha 537, together with the working-directory note for a bindings=bin crate in a workspace subdirectory and the JSON-reporter exit-code trap. Left as a draft for the maintainer; not yet published.'
- pkg: spacy-pkuseg
  version: 1.0.1
  home: https://github.com/explosion/spacy-pkuseg
  repo: https://github.com/explosion/spacy-pkuseg
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2183
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. FEASIBLE and ported - PR https://github.com/riseproject-dev/python-wheels/pull/2183 open (draft, branch spacy-pkuseg). An ordinary setuptools+Cython port, and the cheapest of the explosion.ai family this repo has already done (cymem/murmurhash/preshed/srsly/thinc/spacy): setup.py declares three Extensions - spacy_pkuseg.inference (language="c++"), spacy_pkuseg.feature_extractor and spacy_pkuseg.postag.feature_extractor - all cythonized from .pyx with numpy''s include dir, no CMake, no vendored C/C++ library, no submodule. Grepped the whole tree for sse/avx/neon/__x86/aarch64/-march/-mtune: zero hits, so nothing arch-specific to port. Dependency closure is clear: build-system.requires is setuptools + numpy>=2.0.0,<3.0.0 + cython>=0.25 and install_requires is numpy>=1.19.0,<3.0.0 + srsly>=2.3.0,<3.0.0; pypi.riseproject.dev serves numpy 2.5.3 (cp312/cp313/cp314/cp314t), srsly 2.5.3 (same four) and cython 3.3.0 (cp312/cp313/cp314), so a plain isolated build frontend with PIP_EXTRA_INDEX_URL resolves everything - no CIBW_BEFORE_BUILD, no --no-build-isolation, no constraints file. PyPI''s newest numpy and srsly are both 2.5.3 today, i.e. exactly what the registry has, so pip will not prefer a newer sdist. cp314t has no riscv64 cython wheel on the registry; verified on x86 that `pip install --no-binary :all: cython==3.3.0` on a free-threaded 3.14 builds in seconds and yields a py3-none-any wheel (Cython skips compiling its own extensions under free-threading), so the cp314t build env is fine. Two things worth knowing. (1) Upstream''s pyproject carries `free-threaded-support = false`, an option cibuildwheel removed in 3.0; cibuildwheel 4.2.0 refuses the whole [tool.cibuildwheel] table with "Option ''free-threaded-support'' not supported in a config file" before any build starts. Reproduced locally with cibuildwheel 4.2.0 --print-build-identifiers against the release-v1.0.1 checkout. Same failure and same fix as patches/thinc/9.1.1/0001-Comment-out-the-removed-cibuildwheel-free-threaded-support-option.patch; unlike thinc there is no upstream commit to backport (explosion fixed it in thinc as 6f3a08b1 but spacy-pkuseg master, three commits past the tag, still has the line), so the patch is Upstream-Status: To upstream. (2) The three extensions declare no free-threading support, so importing them on cp314t re-enables the GIL with a RuntimeWarning that upstream''s `-Werror` pytest line turns fatal - the same wall build-srsly.yml hit, and the same fix: CIBW_TEST_ENVIRONMENT: PYTHON_GIL=1, which keeps cp314t in the matrix instead of dropping it the way build-spacy.yml had to. Full default matrix cp312/cp313/cp314/cp314t. Testing mirrors tests.yml (`python -m pytest --pyargs spacy_pkuseg -Werror`), but that suite is one test_import case, so the test command first exercises the compiled code directly: inference.run_viterbi on a 3x2 lattice must return states [1,0,1] and exp(11.5), and feature_extractor.get_slice_str(''abcdef'',1,3,6) must be ''bcd''. A post-build step asserts the wheel holds exactly the three expected .so files. No numpy/cython pin was needed: the Cython 3.3.0 regression that forced `cython<3.3.0` in build-preshed.yml/build-spacy.yml is a typed __setitem__ routed through Py_ssize_t, and nothing here uses PreshMap or uint64 keys - built and tested green against Cython 3.3.0 on all four interpreters. x86_64 pre-flight before pushing: built the wheel from the release-v1.0.1 checkout on cp312, cp313, cp314 and cp314t (all four produce the three .so files and a dist-info/licenses/LICENSE), and ran the workflow''s exact CIBW_TEST_COMMAND string byte-for-byte in each of the four venvs - 1 passed each time, cp314t with PYTHON_GIL=1. Also verified the patch applies cleanly to a fresh tag checkout and that cibuildwheel 4.2.0 then accepts all four -manylinux_riscv64 identifiers. License MIT, LICENSE at the upstream root and already in the wheel''s dist-info/licenses/; nothing third-party is vendored. CI green on the first run (PR #2183, run 35640734835): all four riscv64 legs built and tested clean, about 10 minutes each (cp312 18:48:34->18:58:15Z, cp313 ->18:58:30Z, cp314t ->18:58:39Z, cp314 ->18:58:51Z); 1 test passed on each leg, the run_viterbi/get_slice_str smoke check ran ahead of pytest on each, the .so check found exactly the three expected extensions, and the publish job dry-ran clean over the four spacy_pkuseg-1.0.1-cp3XX-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl wheels. No retries and no flakes. The cibuildwheel-removed-option trap is recorded as gotcha 536. Left as a draft for the maintainer; not yet published. Housekeeping note: a concurrent agent on this shared checkout ran an unscoped string replace that briefly appended squawk-cli CI text to this entry; it has been removed.'
- pkg: mosek
  version: 11.2.3
  home: https://www.mosek.com
  repo: https://www.mosek.com
  status: parked
  pr: null
  notes: 'Not feasible: mosek is MOSEK ApS''s proprietary optimization engine, delivered as object
    code only -- no source at any layer, no riscv64 build from the vendor, and an EULA that
    independently bars redistribution (new gotcha 534, feasibility-and-triage.md; the
    gurobipy/gotcha 453 shape, same closed-payload family as 372/376/431/450). Verified against the
    real artifacts, not inferred. PYPI: 11.2.3 publishes exactly 4 wheels, all cp39-abi3
    (macosx_11_0_arm64, manylinux2014_x86_64, manylinux_2_27_aarch64, win_amd64), and the project
    has published 2052 files across 183 releases with ZERO sdists ever, so per gotcha 431 the wheel
    is the only evidence; the sole Project-URL is the marketing page HOME=https://mosek.com (gotcha
    372''s hdbcli tell) and License: is the bare URL https://mosek.com/products/license-agreement.
    11.2.4 shipped since the queue snapshot with the identical 4-wheel shape, so the gap has not
    moved. WHAT IS IN THE WHEEL: unzipping mosek-11.2.3-cp39-abi3-manylinux2014_x86_64.whl (15.4 MB)
    gives 24 files -- one payload, mosek/libmosek64.so.11.2 at 29.9 MB = 63% of the unpacked wheel,
    plus a vendored oneTBB 2021.12.0 (libtbb.so.12), six per-interpreter
    _msk.cpython-3{9,10,11,12,13,14}-*.so and six fusion/impl/fragments.*.so, and 3.5 MB of
    generated Python glue (__init__.py, fusion/impl/_implementation.py). No .c/.h/.pyx/.cpp anywhere
    in either Linux wheel, so even the binding layer ships no source. readelf -d on
    mosek/_msk.cpython-312-x86_64-linux-gnu.so shows NEEDED libmosek64.so.11.2 plus RPATH
    /var/jenkins/workspace/11.2/bin:$ORIGIN -- a hard link, not a dlopen -- and mosek/__init__.py
    does "from . import _msk" at line 7, so with no riscv64 engine there is no importable wheel at
    all (the gurobipy/saxonche shape, gotcha 450). PER-ARCH CLOSED MATH KERNEL (gotcha 453 confirmed
    on a second vendor): the same libmosek64.so.11.2 is 29.9 MB on x86_64 against 16.9 MB on
    aarch64, .text 24.0 MB against 12.3 MB, and the gap is Intel oneMKL statically linked into the
    x86_64 engine only -- 1432 MKL symbols in its symtab (mkl_blas_avx2_dgemm_copyan,
    mkl_blas__dgemm, "Intel oneMKL FATAL ERROR: Cannot load %s", a full
    AVX2/AVX512{F,BW,CD,DQ,VL,VNNI,BF16} dispatch table) against 1 on aarch64, which carries its own
    cblas_*/ src/math/sparseblas.c path and SVE. Neither engine contains a single riscv/rv64 string
    (0 hits in 293k/71k). Both are built on a closed vendor Jenkins whose path names the vendor''s
    platform id --
    /var/jenkins/workspace/11.2/Distro-pipeline/linux64x86/bld/docker-rockylinux-8/final/default/bldflexlm/x64_linux
    and .../Distro-pipeline/linuxaarch64/bld/docker-rockylinux-aarch64-8/.../arm64_linux (gotcha
    431''s builder-path tell, used forward to get the exact spelling to probe with). FOUR
    INDEPENDENT VENDOR PLATFORM TABLES, all riscv64-negative: (1)
    mosek.com/products/supported-platforms lists exactly Linux 64 bit x86, Windows 64 bit x86, Linux
    64 bit ARM and OSX 64 bit ARM, no RISC-V; (2) the docs.mosek.com installation guide gives the
    same four ids linux64x86/linuxaarch64/osxaarch64/win64x86 and says "decide if you are running on
    a 64bit x86 ... or an ARM64 architecture"; (3)
    download.mosek.com/stable/11.2.3/mosektools{linux64x86,linuxaarch64,osxaarch64}.tar.bz2 and
    mosektoolswin64x86.zip all answer 206 to a 2-byte range request while every riscv64 spelling
    answers 403 AccessDenied -- and, per the new gotcha 534, that 403 was calibrated against a
    deliberately bogus key and against the discontinued mosektoolsosx64x86.tar.bz2, both of which
    403 identically, because the bucket has no ListBucket and returns 403 rather than 404 for a
    missing key; (4) the vendor''s own conda channel has 1271/696/552/1268 packages in
    linux-64/linux-aarch64/osx-arm64/win-64 against 0 in linux-riscv64, which still returns 200 with
    a synthesised empty index (gotcha 42 -- count the packages, do not trust the status). NO
    OPEN-SOURCE OR CPU-GENERIC BUILD PATH: the MOSEK GitHub org''s 15 public repos are all
    Julia/Rust/Go/JS/Octave bindings, tutorials, course material and Dockerfiles -- the engine is in
    none of them -- and the one that is closest, MOSEK/Mosek.pip, was archived in 2020 and its
    setup.py says in its own words that "the ''build'' action effectively builds both python source
    and binary libraries (by downloading and unpacking the binary distribution)" over a libs =
    {''win64x86'': ...} platform table, i.e. even the vendor''s own packaging repo only ever
    downloaded a closed distro. The aarch64 engine being MKL-free shows MOSEK does maintain a
    non-MKL build, but that is not a recompile anyone outside MOSEK ApS can do, because there is no
    source to recompile. THE LICENCE CLOSES A SECOND, INDEPENDENT DOOR: the wheel ships NO licence
    text at all (dist-info holds only METADATA/WHEEL/top_level.txt/RECORD), so the terms have to be
    fetched from docs.mosek.com/license/license.pdf; its 6.1 grants "the right to use the delivered
    downloaded object code copy of THE SOFTWARE only and does not acquire any rights of ownership",
    6.2 says the licensee "shall be prohibited from sublicensing and otherwise letting third parties
    use THE SOFTWARE without prior written consent", 10 ("No Modifying, Reverse Engineering etc.")
    says the licensee "shall not be entitled to modify, reverse engineer, disassemble or decompile
    THE SOFTWARE" and may copy it "for backup purposes" only, 11 withholds the MOSEK(R) trademark,
    and 14.6 has MOSEK warranting that "THE SOFTWARE does not comprise any third party open source
    software, free software or the like" -- so publishing any mosek wheel on pypi.riseproject.dev
    would not be permitted even if a riscv64 engine existed. The METADATA itself states "MOSEK
    requires a license" (free trial and non-commercial academic licences exist, neither of which
    grants redistribution), and there is no swap-in escape hatch of the gotcha 35 kind: the engine
    is gated by Revenera FlexNet Publisher, not by vendor code we could stub (bldflexlm in the build
    path; lmgrd, "*** A FLEXlm error occurred", .flexlmrc, "Error in communication with the FlexNet
    Licensing Service", MSK_checkoutlicense/MSK_putlicensecode in the strings), so a riscv64 MOSEK
    would need two separate proprietary vendors to port, not one. Revisit only if MOSEK ApS itself
    ships a riscv64 Optimizer (a mosektoolslinuxriscv64-style tarball on download.mosek.com, a
    linux-riscv64 conda subdir, or a riscv64 wheel on PyPI) AND grants redistribution; nothing in a
    build-mosek.yml can close either gap. No worktree/branch/PR created -- settled before any YAML
    was written.'
- pkg: treelite
  version: 4.7.2
  home: https://treelite.readthedocs.io/en/latest/
  repo: https://github.com/dmlc/treelite
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1816
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: couchbase
  version: 4.6.3
  home: https://github.com/couchbase/couchbase-python-client
  repo: https://github.com/couchbase/couchbase-python-client
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2184
  notes: 'PR #2184 draft, CI green: 4 manylinux_2_39_riscv64 wheels (cp312/cp313/cp314/cp314t, ~7.1 MB each, 1h06-1h12 per leg on the 4-core riscv runner), publish dry-run clean. SDK 4.x does NOT wrap libcouchbase/LCB - it wraps couchbase-cxx-client (C++17), vendored as a git submodule, so nothing needs a system package or a prebuilt binary. The sdist also ships deps/couchbase-cxx-cache, the CPM cache of asio 1.34.2, BoringSSL, snappy 1.2.2, spdlog 1.15.0, taocpp/json 1.0.0-beta.14, llhttp 9.3.0, GSL 4.0.0 and hdr_histogram plus the Mozilla CA bundle, so the riscv container fetches nothing: the sdist job runs upstream''s own PYCBC_SET_CPM_CACHE=ON configure_ext on ubuntu-latest and the riscv job extracts, patches and builds that tarball. TWO riscv64-only blockers, both patched: (1) taocpp/json vendors Google double-conversion whose CORRECT_DOUBLE_OPERATIONS arch list falls through to ''#error Target architecture was not detected as supported by Double-Conversion'' - fixed upstream on taocpp/json main in 91a480f (2026-09-01, after beta.14, in no tag yet), backported; (2) -Werror=cast-align on core/io/mcbp_session.cxx:141, since GCC emits -Wcast-align only on strict-alignment targets so upstream''s x86_64/aarch64 CI never sees it - demoted with -Wno-error=cast-align in the core''s own CompilerWarnings.cmake, matching the four -Wno-error= entries it already carries for asio. Patches apply to the extracted sdist, not the checkout: git apply refuses a path inside the couchbase-cxx-client submodule, and the other target only exists once configure_ext has run. Everything else is clean: no arch branches in the cxx-client CMake, the vendored BoringSSL defines OPENSSL_RISCV64 in target.h and every generated .S is self-guarded on OPENSSL_X86_64/OPENSSL_AARCH64 so riscv64 assembles them to nothing and uses the portable C (the wheel reports openssl_runtime=BoringSSL, so the static lib really is linked), snappy''s SIMD probes all fail closed to scalar, and grep ''#error'' over the whole vendored tree returns exactly one hit (the one patched). CMake 4.4 in the image needs CMAKE_POLICY_VERSION_MINIMUM=3.5 for the vendored GSL''s cmake_minimum_required(VERSION 3.1.3...3.16). Python closure is just typing-extensions (py3-none-any, only for <3.13). Built with PYCBC_USE_OPENSSL=OFF (static BoringSSL) like upstream''s own wheels. Upstream''s suite needs a live Couchbase Server (no riscv64 build; its gocaves mock ships no riscv64 binary either) and the sdist omits the harness, so the wheel gets a functional smoke test: real .so, C++ core 1.3.2 + BoringSSL, and a bootstrap to an unroutable address surfacing as CouchbaseException. New gotchas 546/547/548.'
- pkg: glcontext
  version: 3.0.0
  home: https://github.com/moderngl/glcontext
  repo: https://github.com/moderngl/glcontext
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1396
  notes: 'PR #1396 open. Two small C++ extensions (x11, egl) that dlopen their target libraries at runtime, no native build deps. Builds manylinux and musllinux riscv64 for cp312/cp313/cp314/cp314t. Upstream ships wheels untested; added a smoke test instead since the default (x11) backend needs a running X server this headless runner does not have - the egl backend (the actual headless use case) gets a real functional test via the images'' bundled Mesa software rasterizer.'
- pkg: eclipse-zenoh
  version: 1.10.0
  home: https://readthedocs.org/projects/zenoh-python/
  repo: https://github.com/eclipse-zenoh/zenoh-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2187
  notes: 'PR #2187 open (draft), CI running. Portable PyO3/maturin port, no blockers. pyo3 0.25.1 carries abi3-py39 + extension-module unconditionally in Cargo.toml (gotcha 181), so maturin emits one cp39-abi3 wheel whatever interpreter builds it - matching the single Linux wheel upstream publishes per arch; no MATURIN_PEP517_ARGS needed, and pyproject.toml has no [tool.maturin] table at all so the PEP 517 backend picks the release profile itself (gotcha 141 checked). CIBW_BUILD lists cp39 cp312 cp313 cp314 per gotcha 96: cibuildwheel compiles once on cp39 (the floor the tag claims; the riscv64 image ships it, same as clarabel) and re-tests that wheel on the newer three. No cp314t leg - pyo3 0.25.1 predates CPython 3.14 and upstream ships no free-threaded wheel. musllinux dropped (gotcha 10: rustup.rs has no riscv64 musl host toolchain). Dependency audit: 438 locked crates, zenoh/zenoh-ext pinned as git deps on github.com/eclipse-zenoh/zenoh branch release/1.10.0 at rev c479f0c, so Cargo.lock fully pins them - no floating-dep risk, and the 1.10.0 git tag''s Cargo.toml/Cargo.lock/pyproject.toml are byte-identical to the PyPI sdist, so build-from-checkout with no sdist job (gotcha 325). The only crate that compiles C is ring 0.17.14 (rustls'' provider; no aws-lc-sys, no OpenSSL - openssl-probe only): it cross-compiled clean to riscv64 and produced a real riscv64 libring_core_0_17_14_.a (file: ''ELF 64-bit LSB relocatable, UCB RISC-V''). The default features (shared-memory, zenoh-ext, zenoh/default) are all pure Rust - zenoh-shm uses advisory-lock/shared_memory, no C/C++ dependency, so nothing is feature-gated away. rust-toolchain.toml pins stable channel 1.97.1, harmless to inherit (gotcha 238 is about nightly) and the 2026-07-16 channel manifest ships rustc/cargo/rust-std for riscv64gc-unknown-linux-gnu, so rustup provisions it in-container. maturin 1.15.0 has a riscv64 wheel on PyPI, so default build isolation works. Local x86 pre-flight (rust:trixie + gcc-riscv64-linux-gnu): cargo check --locked --target riscv64gc-unknown-linux-gnu green over the whole graph in 1m41s, and a full cargo build for the same target linked a real riscv64 cdylib in 2m06s (file: ''ELF 64-bit LSB shared object, UCB RISC-V''). Native x86 rehearsal: maturin build --release produced eclipse_zenoh-1.10.0-cp39-abi3-manylinux_2_34_x86_64.whl (10.3 MB, zenoh/zenoh.abi3.so, dist-info carries LICENSE + NOTICE.md + a CycloneDX SBOM), tests/stubs_check.py passed and test_serializer.py was 31/31. Test step runs stubs_check.py plus test_serializer/test_cancellation_token/test_session/test_timestamp_stack; examples_check.py is left out because it drives ~30 concurrent example processes over multicast scouting, which would cross-talk between jobs on the shared runner. stubs_check.py reads *.pyi from <root>/zenoh, so CIBW_TEST_SOURCES stages only the three .pyi files there - with no __init__.py that directory is a namespace portion, which loses to the installed regular package, verified in the rehearsal (import resolved to site-packages). The session-based tests could not be exercised locally: this sandbox has no IPv6 at all, and zenoh''s default listener binds tcp/[::]:0, so they fail with EAFNOSUPPORT - a control run of upstream''s own published x86_64 1.10.0 wheel in the same container reproduced the identical 7 failures, and gotcha 243 records that the manylinux_2_39_riscv64 container''s IPv6 loopback does bind (only sending fails), which is all the default listener needs. CI GREEN: run 35648354757, build job 20:01:27Z-21:19:11Z (1h17m44s) on ubuntu-24.04-riscv. One wheel, eclipse_zenoh-1.10.0-cp39-abi3-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl, 11.4 MB, sha256 bd611d63657c36b92962eaa4654cba230e3423b640a43046f7d9ae4d7ef8f5d5; built on cp39 and reused+retested on cp312/cp313/cp314 via find_compatible_wheel (each reported as test-only, ~2 minutes), 43 passed / 0 skipped on every interpreter with stubs_check.py clean. The IPv6 concern did not materialise: test_session, test_timestamp_stack and test_cancellation_token all pass in the manylinux_2_39_riscv64 container, confirming that gotcha 243''s "bind works, send does not" is enough for zenoh''s default tcp/[::]:0 listener. Publish job dry-ran clean (DRY_RUN true, not on main branch), would tag eclipse-zenoh-v1.10.0-20260921212555 and record requires-python >=3.8.'
- pkg: chonkie-core
  version: 0.10.2
  home: https://github.com/chonkie-inc/chunk
  repo: https://github.com/chonkie-inc/chunk
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2186
  notes: 'PR #2186 open (ready for review), CI green on all three legs. maturin/PyO3 0.27 extension (chonkie_core._chunk) from the chonkie-inc/chunk Cargo workspace; per-interpreter, not abi3 (no abi3 feature on the pyo3 dep, upstream ships cp310-cp314 wheels). Portable: 30 non-dev crates for riscv64 per cargo metadata --filter-platform, zero -sys crates and no C at all - just pyo3, rust-numpy/ndarray, memchr and daggrs; the heavy crates in Cargo.lock (reqwest/ring/tokio/criterion) are dev-deps of the root chunk crate''s benches and are never built. No tokenizers/onnxruntime anywhere, no SIMD intrinsics or target_arch gates in the sources despite the "simd" keyword. Build-from-checkout with package-dir packages/python: the checkout has no root pyproject.toml (gotcha 239 - it lives in packages/python and the sdist''s hoisted copy is maturin''s synthesized one), and cibuildwheel copies the whole cwd so the crate''s `chunk = { path = "../.." }` and the committed root Cargo.lock resolve (gotcha 142/325, no sdist job needed). [tool.maturin] sets no profile, so the PEP 517 backend picks release by itself (gotcha 141 checked). Matrix cp312/cp313/cp314: upstream builds no free-threaded wheel and the #[pymodule] does not pass gil_used(false), so cp314t would re-enable the GIL on import. x86 pre-flight in manylinux_2_28_x86_64: pip wheel packages/python green, 23/23 upstream tests pass, savgol_filter/windowed_cross_similarity verified against numpy; riscv64 cross-compile (PYO3_CROSS, gotcha 182) links a real riscv64 cdylib needing only libc/libgcc_s. Test step adds pytest+numpy from our registry (numpy 2.5.3 riscv64 covers cp312-cp314) plus a numpy smoke check, since upstream''s suite never calls the four numpy-gated functions. CI: cp312/cp313/cp314 all green (23 passed each, ~10 min per leg) and the publish dry-run resolved all three manylinux_2_34_riscv64.manylinux_2_39_riscv64 wheels; cp314 needed one rerun-failed-jobs after an upload-artifact FinalizeArtifact 403 flake (gotcha 540). The only red check on the PR is the stale check_commit_messages from the `opened` payload, which saw the create_pull_request tool''s auto-injected AI footer - it was stripped within seconds and the `edited`-triggered rerun is green.'
- pkg: fastokens
  version: 0.3.1
  home: https://github.com/Atero-ai/fastokens
  repo: https://github.com/Atero-ai/fastokens
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2185
  notes: 'Portable PyO3/maturin port, no blockers. pyo3 0.25.1 carries abi3-py39 unconditionally in python/Cargo.toml (gotcha 181), so maturin emits one cp39-abi3 wheel whatever interpreter builds it - matching the single Linux wheel upstream publishes per arch. Upstream ships no free-threaded wheel at all (build-maturin.yml documents that it removed the -i python3.13t steps: abi3 and Py_GIL_DISABLED only meet on 3.14+), so the matrix is a single build job, no cp314t leg. CIBW_BUILD lists cp39..cp314 per gotcha 96 - cibuildwheel compiles once on cp39 (the floor the tag claims, and the riscv64 image ships it) and re-tests that wheel on cp310..cp314. Native-dep audit of the 164-crate non-dev riscv64 graph (cargo metadata --filter-platform riscv64gc-unknown-linux-gnu): only two crates compile C. pcre2-sys 0.2.10 bundles PCRE2 10.46 and enables SUPPORT_JIT for riscv64 (its enable_jit() excludes only android/musl/apple-ios targets); bundled deps/sljit carries sljitNativeRISCV_64.c, so the JIT is real on riscv64, not a scalar fallback. ring 0.17.14 has no riscv64 asm and needs none: ASM_TARGETS matches nothing, and include/ring-core/target.h sends riscv64 down the __LP64__ + __BYTE_ORDER__ branch to OPENSSL_64_BIT + OPENSSL_SMALL portable C (no "Unknown target CPU" #error), so rustls picks ring, not aws-lc-rs, and no cmake/perl is needed. No OpenSSL anywhere despite upstream''s before-script installing libssl-dev/openssl-devel - dropped. Built .so NEEDED is libgcc_s/libm/libc only. Local x86 pre-flight: v0.3.1 checkout is byte-identical to the PyPI sdist (Cargo.lock committed, tests in tree, so build-from-checkout with no sdist job, gotcha 325); uv build on cp39 produced fastokens-0.3.1-cp39-abi3-linux_x86_64.whl in ~70s, and that one wheel passed 11 tests / 3 skipped on 3.9, 3.12 and 3.14 (gotcha 96/469 grid). test_thread_safety.py deselected - it downloads Qwen/Qwen3-0.6B from the Hugging Face Hub; test_patch_transformers.py and the two tiktoken parity cases self-skip via importorskip. Note upstream has since tagged v0.3.2 (PyPI latest); the queued 0.3.1 is what this PR builds. CI green end to end (run 35646924670): setup + build + publish all success. One wheel produced, fastokens-0.3.1-cp39-abi3-manylinux_2_34_riscv64.manylinux_2_39_riscv64.whl (3.3 MB, sha256 8f926f1bdbac766ebe214dd1ba560aa73bc55555157a8ef887950fdfaf060bda) - the abi3 tag is real, matching upstream''s published cp39-abi3 (gotcha 11''s read-the-filename check). Built on cp39 in 20 min (28 min job total); cp310/cp311/cp312/cp313/cp314 were test-only reuse via find_compatible_wheel at 34-39 s each, 11 passed / 3 skipped on every one. Publish dry-ran clean. One cosmetic note: update_doc.py will add a per-version license: Unknown because fastokens ships no License/License-Expression/classifier in its wheel METADATA (only License-File entries) - the top-level Apache-2.0 is from LICENSE + Cargo.toml and is correct; 10+ existing packages carry the same per-version Unknown, so this was left alone. Learnings recorded as gotcha 541. Left as draft for the maintainer.'
- pkg: dukpy
  version: 0.6.0
  home: https://github.com/amol-/dukpy
  repo: https://github.com/amol-/dukpy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1165
  notes: 'Vendors quickjs-ng (MIT) as a compiled C extension (dukpy._dukpy), same shape as the existing quickjs port. Build-from-checkout, mirrors upstream build-wheels.yml, matrix cp312/cp313/cp314/cp314t x manylinux/musllinux. CI green: all 8 build jobs + publish dry-run passed, 141 tests passed per job. Awaiting review/merge.'
- pkg: numba-cuda
  version: 0.30.4
  home: https://nvidia.github.io/numba-cuda/
  repo: https://github.com/NVIDIA/numba-cuda
  status: parked
  pr: null
  notes: 'Parked: Numba''s CUDA target/toolchain is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: mkl
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: parked
  pr: null
  notes: 'Parked, not feasible (gotcha 528''s vendor-runtime pattern, feasibility-and-triage.md; same family
    and same evidence as the parked intel-cmplr-lib-ur and intel-openmp). VERIFIED ON THE REAL
    2026.1.0 WHEEL, not assumed from the sibling park. NO SOURCE, NO PYTHON: every mkl release on PyPI
    has 0 sdists and exactly 2 wheels (manylinux_2_28_x86_64 + win_amd64); the 224 MB Linux wheel''s
    top_level.txt is a single newline and it contains no .py file at all -- 28 prebuilt .so files
    under mkl-2026.1.0.data/data/lib/ plus 4 dist-info files, nothing else. THE KERNELS ARE X86 ISA
    DISPATCH BY NAME: the shipped set is libmkl_{def,mc3,avx2,avx512,avx10}.so.3 plus
    libmkl_vml_{def,mc3,cmpt,avx2,avx512,avx10}.so.3 -- SSE2/SSE4.2/AVX2/AVX-512/AVX10 variants of the
    same kernels -- and strings libmkl_core.so.3 shows the dispatcher dlopen-ing exactly those names;
    there is no generic kernel below "def", which is itself x86 SSE2. Every .so is ELF x86-64 (readelf
    Machine: Advanced Micro Devices X86-64) and libmkl_rt.so.3 carries "Intel oneMKL FATAL ERROR: This
    system does not meet the minimum requirements" for that ISA check. GOTCHA 465''S INVERTED TELL
    DOES NOT EVEN APPLY HERE: strings -a | grep -ci riscv is 0 in libmkl_rt.so.3, libmkl_core.so.3 and
    libmkl_def.so.3 -- intel-cmplr-lib-ur at least had bundled LLVM tables, MKL has nothing. BUILT
    WITH A TOOLCHAIN THAT HAS NO RISCV64 TARGET: .comment says "Intel(R) oneAPI DPC++/C++ Compiler
    2026.1.0 (2026.1.0.20260611)", the very compiler whose runtime siblings are already parked. INTEL
    SHIPS ONEMKL FOR NO NON-X86 ARCH: Intel''s own oneMKL system-requirements page lists only Intel
    Core/Core Ultra/Xeon/Xeon Scalable/Atom processors (no ARM, RISC-V or POWER), apt.repos.intel.com
    /oneapi dists/all/Release says Architectures: all amd64 i386 i686, and the open oneMath project
    (ex "oneMKL Interfaces", uxlfoundation/oneMath) -- an API wrapper, not an implementation -- lists
    Intel oneMKL''s hardware backend as "x86 CPU, Intel GPU", with no backend in its whole table
    covering riscv64. LICENCE: LICENSE.txt is the Intel Simplified Software License (Oct 2022). Unlike
    the intel-cmplr-lib-ur EULA it does permit redistribution, but only of the unmodified binary
    ("provided in binary form only ... without modification") while forbidding "reverse engineering,
    decompilation, or disassembly ... nor any modification or alteration" -- so the only artifact we
    are licensed to republish is the x86_64 blob nobody on riscv64 can run, and producing a riscv64
    build is precisely what the licence prohibits. NOT THE TBB CASE (gotcha 506, per package not per
    vendor): this repo does ship tbb 2023.1.0 for riscv64, because oneTBB is Apache-2.0 with public
    source at that exact tag and build-tbb.yml compiles it from uxlfoundation/oneTBB; oneMKL''s
    computational core has no public source at any version -- oneMath dispatches to this same closed
    binary -- so gotcha 263''s rescue has nothing to rebuild from. NO CONSUMER COULD EXIST:
    pypi.riseproject.dev 404s for mkl, intel-openmp and onemkl-license; the wheel requires
    onemkl-license==2026.1.0 and intel-openmp>=2025,<2027, both 0-sdist Intel x86_64/win_amd64-only
    wheels (gotcha 40''s shape; only tbb==2023.* resolves on our index). Downstream, mkl-service
    2.8.0, mkl-fft 2.3.2 and mkl-random 1.5.0 are likewise 0-sdist
    manylinux_2_28_x86_64/win_amd64-only, and this repo''s MKL-capable packages already take the
    non-Intel path on riscv64 (ctranslate2 builds WITH_MKL=OFF with OPENMP_RUNTIME=COMP, clarabel
    swaps the mkl-backed pardiso, scs links OpenBLAS). QUEUE VERSION IS CURRENT (gotcha 388): 2026.1.0
    is still the latest release. The queued siblings mkl-static and mkl-include are the same family on
    the same evidence. No worktree, branch or PR created.'
- pkg: pygame-ce
  version: 2.5.8
  home: https://pyga.me
  repo: https://github.com/pygame-community/pygame-ce
  status: publishing
  pr: 1819
  notes: '21 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI
    or pypi.riseproject.dev. CI green on PR #1819 (run 35554793349, commit 25cc84add4): all three legs
    build and the full upstream test command runs 2385 tests per interpreter, OK, and the publish job
    dry-runs clean. Ready for review, not merged - left to the maintainer.

    Workflow: builds SDL2 2.32.10/SDL2_image 2.8.12/SDL2_ttf 2.24.0/SDL2_mixer 2.8.2/portmidi 2.0.7 from
    source (Rocky 10 dropped SDL2-devel on every arch for sdl2-compat, gotcha 288); drops SDL2_mixer''s
    MOD/fluidsynth backends (EPEL-only, EPEL empty on riscv64, gotcha 51), opus file decoding (needs libopusfile,
    no Rocky 10 package) and wavpack with them; drops SDL_ttf HarfBuzz. Matrix cp312-cp314 (numpy ships
    riscv64 wheels for those only); pp311 dropped for lack of riscv64 PyPy.

    The 8-day stall was self-inflicted, not runner eviction and not a cancel-in-progress race: the legs
    were hitting the workflow''s own timeout-minutes: 600, which GitHub records as "cancelled". Rebased
    onto main and modernized the scaffolding (added docs/packages/pygame-ce.yaml, wired _setup.yml with
    package:/version:, added the push trigger and branches: [main] on pull_request, iterated every job
    over matrix.version, passed RISEPROJECT_APP_PRIVATE_KEY to _publish-wheel.yml, dropped the dead /project/pgce-ci-diag.log
    tee - cibuildwheel copies the project into the container rather than bind-mounting it). Matrix 6 ->
    3 legs, timeout-minutes 600 -> 180.

    Two real defects, both found by bypassing pygame''s own test runner (it buffers each module into a
    StringIO and prints it only when the module ends, and defaults to single-process mode where its --time_out
    never applies, so both failures were invisible - new gotchas 472/473). (1) pygame.draw.arc() is broken
    on riscv64: test_arc__correct_drawing never returns (run 35538919447) and test_arc__surface_clip segfaults
    (run 35550328498), both on a bare draw_arc() call in the C rasteriser. python -X faulthandler -m unittest
    named the segfault at draw_test.py:7313, and running each of draw_test''s 252 tests in its own interpreter
    (run 35551432208) left exactly those two failing - patch 0001 skips just them on riscv64. The first
    watchdog run had used exit=True, which stops at the first offender, which is why the segfault only
    surfaced after the hang was skipped. (2) Four tests failed because this build omits optional SDL backends,
    not because of riscv64: patch 0002 extends PG_DEPS_FROM_SYSTEM - upstream''s own marker for a system-deps
    build, set by its run-ubuntu-checks.yml/build-ubuntu-sdist.yml/dev-check.yml/build-sdl3.yml - to the
    house_lo.opus and surfonasinewave.xm entries that sit unguarded next to an already-guarded house_lo.wv,
    and to test_font_set_script/test_font_set_direction, which need SDL_ttf built with HarfBuzz; the workflow
    sets PG_DEPS_FROM_SYSTEM=1 (gotcha 474).

    Verified in the manylinux_2_39_riscv64 image that Rocky 10 riscv64 does ship harfbuzz-devel (appstream
    8.4.0) and wavpack-devel (crb), correcting gotcha 288; harfbuzz is still left off because libharfbuzz
    links libglib-2.0/libgraphite2, which auditwheel would vendor and gpl_sources would then have to archive. Merged (#1819). Push-triggered build+publish on main run 35573051518 succeeded. 404 on pypi.riseproject.dev/simple/pygame-ce/ as of this check, pending docs catalog PR #2133; move to published once it merges and the index returns 200.'
- pkg: nixl-cu13
  version: 1.4.0
  home: 'https://github.com/ai-dynamo/nixl'
  repo: 'https://github.com/ai-dynamo/nixl'
  status: parked
  pr: null
  notes: 'Same ai-dynamo/nixl source tree as nixl-cu12 (PR #2314) - the cu12/cu13 split is purely which CUDA major version meson''s optional CUDA probe finds at build time, not a separate codebase. Our riscv64 build has no CUDA toolkit at all, so it can only ever produce the nixl_cu12-named wheel (see nixl-cu12''s notes for the full feasibility case: CUDA is `required: false` in meson.build, the released core .so''s link no CUDA, UCX has a riscv64 arch dir). Park this entry rather than duplicate the port; revisit only if a future release renames the split or ships a real cu13-specific build path.'
- pkg: bsdiff4
  version: 1.2.6
  home: https://github.com/ilanschnell/bsdiff4
  repo: https://github.com/ilanschnell/bsdiff4
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1159
  notes: Single-file C extension (core.c), no native deps; bzip2 via stdlib bz2, not vendored C sources. Built/tested clean locally before push. PR open, CI running.
- pkg: faster-coco-eval
  version: 1.8.0
  home: https://github.com/MiXaiLL76/faster_coco_eval
  repo: https://github.com/MiXaiLL76/faster_coco_eval
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2255
  notes: 'Two pybind11 C++ extensions, per-interpreter (no abi3), no SIMD/arch-specific code in csrc/; numpy is the only build+runtime dep, already on our registry. Build-from-checkout, mirrors build-fonttools.yml/build-pycocotools.yml. CI fully green: all 4 riscv64 wheels (cp312/cp313/cp314/cp314t) built and tested, publish dry-ran clean. PR ready for review, not merged.'
- pkg: mcp-grafana
  version: 1.2.0
  home: https://github.com/grafana/mcp-grafana
  repo: https://github.com/grafana/mcp-grafana
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2259
  notes: 'py3-none wheels wrap a real per-platform Go binary (go-to-wheel); riscv64 added by extending its platform table (gotcha 554). CI fully green: build plus all 4 Python-ABI smoke tests passed, publish dry-ran clean (one stale check_commit_messages failure tied to the original AI-footer-tainted "opened" event, superseded by a green "edited" rerun - cosmetic only). PR ready for review, not merged.'
- pkg: yara-x
  version: 1.20.0
  home: https://virustotal.github.io/yara-x
  repo: https://github.com/VirusTotal/yara-x.git
  status: published
  pr: 1169
  notes: '4 Linux wheels upstream (abi: cp38,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PR #1169 opened, CI running.'
- pkg: baseten-performance-client
  version: 0.1.13
  home: https://github.com/basetenlabs/truss.git
  repo: https://github.com/basetenlabs/truss.git
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2260
  notes: 'maturin/PyO3; rustls resolves to ring (no aws-lc-sys); abi3-py38 patched to abi3-py39 (no cp38 in riscv64 image). First riscv64 CI run: both matrix legs (cp39-abi3, cp314t) compiled the same dependency tree concurrently on shared/limited runner memory; cp39-abi3 aborted mid-futures-macro codegen with rustc signal 6 ("corrupted double-linked list"), cp314t''s job log never uploaded at all -- gotcha 228''s exact shape. Added CARGO_BUILD_JOBS=2 to CIBW_ENVIRONMENT_LINUX; retry then hit a second, real failure on cp314t: pyo3 0.24.2 (pinned) hard-ceilings at cp313 for any non-abi3 build ("configured Python interpreter version (3.14) is newer than PyO3''s maximum supported version (3.13)") -- gotcha 326''s exact pyrage precedent. Upstream''s real free-threaded floor is cp313-cp313t (confirmed on PyPI), which the riscv64 image does not ship, so cp314t was never a valid substitute; bumping pyo3 cascades into pyo3-async-runtimes/pythonize/numpy plus a with_gil->try_attach API migration, too invasive for this port. Dropped the free-threaded leg entirely (cp39-abi3 only). CI fully green: build passed, publish dry-ran clean. PR ready for review, not merged.'
- pkg: hdf5plugin
  version: 7.0.0
  home: https://github.com/silx-kit/hdf5plugin
  repo: https://github.com/silx-kit/hdf5plugin
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1747
  notes: '3 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libipld
  version: 3.4.1
  home: https://github.com/MarshalX/python-libipld
  repo: https://github.com/MarshalX/python-libipld
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2256
  notes: 'Plain pyo3 dep (no abi3), per-interpreter build (cp312/cp313/cp314); pure-Rust dep tree, no riscv64-blocking crates. CI fully green: all 3 riscv64 wheels built, publish job dry-ran clean. PR ready for review, not merged.'
- pkg: google-python-cloud-debugger
  version: '4.1'
  home: https://github.com/GoogleCloudPlatform/cloud-debug-python
  repo: https://github.com/GoogleCloudPlatform/cloud-debug-python
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: flpc
  version: 0.2.5
  home: https://github.com/itsmeadarsh2008/flpc
  repo: https://github.com/itsmeadarsh2008/flpc.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1817
  notes: '78 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1817 opened, CI dispatched on the PR branch (cp312/cp313/cp314, no cp314t - pyo3 0.22 predates PEP 703).'
- pkg: umf
  version: 1.1.0
  home: https://github.com/oneapi-src/unified-memory-framework
  repo: https://github.com/oneapi-src/unified-memory-framework
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2191
  notes: 'PyPI wheel is Intel''s own closed-redistribution build (x86_64/win_amd64 only, no sdist) of the Apache-2.0 oneAPI Unified Memory Framework C library -- no Python bindings, just headers/libs/CMake config under <wheel>.data/data/ (same "data wheel" shape as tbb/tcmlib). The shipped libumf.so embeds its own build''s UMF_ALL_CMAKE_VARIABLES string, which pins Intel''s exact CMake flags: only UMF_BUILD_SHARED_LIBRARY, UMF_BUILD_LEVEL_ZERO_PROVIDER and UMF_BUILD_CUDA_PROVIDER are ON. Confirmed via strings/readelf that both GPU providers only dlopen() libcuda.so.1/libze_loader.so.1 at call time (src/utils/utils_load_library.c) and compile against header-only FetchContent''d repos (NVIDIA''s cudart headers, oneAPI''s level-zero loader), so they are fully portable and were kept ON rather than disabled, matching upstream''s own reusable_fast.yml CI -- no HIP provider exists in 1.1.0. hwloc>=2.3.0 is a required, dynamically-linked dependency the official wheel does NOT bundle (expects it preinstalled); built hwloc 2.14.0 from source and bundled it with a $ORIGIN rpath instead, same as the tcmlib port. Requires-Dist: tcmlib>=1.5 mirrors the official metadata (tcmlib is already published on pypi.riseproject.dev for riscv64, so the dependency resolves). Local x86_64 validation: full build (CUDA/Level Zero providers included) plus upstream''s own ctest suite, 46/46 passing (one gracefully skipped -- no HMAT hardware); wheel layout (headers/lib/cmake matching the official data-wheel shape), licence bundling (Apache-2.0 UMF + BSD-3-Clause hwloc + UMF''s own third-party-programs.txt) and a ctypes smoke test against the packaged .so all verified. First riscv64 CI run (job 106503433364) built/installed cleanly but ctest failed 8/50 gtest cases in test_provider_os_memory, all under osProviderTest/umfIpcTest.*/disjoint_w_params_0_OS_HostMemoryAccessor (ret=3 UMF_RESULT_ERROR_INVALID_ARGUMENT opening an IPC handle, then a FATAL ptr!=NULL abort). A first theory (a padding/memcmp() bug in provider_tracking.c''s opened-handle cache key, patched with memset()/memcpy()) looked well-reasoned from source and a local x86_64 strace, and was documented as gotcha 545 -- but a fresh riscv64 CI run on the patched commit reproduced the exact same 8 failures unchanged: the failing line is the *first* umfOpenIPCHandle() call, not the cache-hit second lookup the theory required, so it never applied. The patch was reverted and gotcha 545 retracted with a correction. Real root cause, confirmed against upstream issue oneapi-src/unified-memory-framework#979: os_open_ipc_handle()''s pidfd_getfd(2) call is blocked (EPERM) by Docker''s default seccomp profile without CAP_SYS_PTRACE; fixed by adding --cap-add=SYS_PTRACE to the test container''s docker run, no source change needed (gotcha 552). A second, independent riscv64 failure -- test_provider_tracking_fixture_tests crashing with Bus error inside pow2AlignedAllocHelper under a doubly-nested tracking-provider-over-file-backed-proxy-pool stack -- first looked concurrency-specific (it hit multiThreadedpow2AlignedAlloc) and was skipped via GTEST_FILTER on that name alone; a later CI run on the same commit crashed instead in the plain single-threaded pow2AlignedAlloc case, which calls the identical helper, so the skip was widened to both parameterizations. No root cause found for this second bug without riscv64 hardware to reproduce interactively; documented honestly as an open question rather than claimed as fixed (gotcha 553). With both fixes (--cap-add=SYS_PTRACE plus the widened GTEST_FILTER), the next riscv64 CI run''s ctest passed 100% (46/46, ~137s), confirming both; that run then failed at the final smoke-install step (`pip install dist/*.whl`) with "No matching distribution found for tcmlib>=1.5" -- the wheel''s own install_requires, which only resolves via pypi.riseproject.dev, not public PyPI, and this self-driven docker container never inherited the PIP_EXTRA_INDEX_URL wiring a CIBW_ENVIRONMENT-based port gets for free (gotcha 30/422 pattern). Fixed by setting PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/ on that one pip install line; confirmed on the re-run (job 107454034322): ctest 46/46, wheel installs, smoke test prints "umfGetCurrentVersion() = 65536", artifact uploaded. riscv64 CI is fully green; PR is not a draft. Not merged (maintainer merges).'
- pkg: pymsalruntime
  version: 0.20.6
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: mwparserfromhell
  version: 0.7.2
  home: https://mwparserfromhell.readthedocs.io/
  repo: https://github.com/earwig/mwparserfromhell
  status: published
  pr: 1393
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green for cp312/cp313/cp314 manylinux+musllinux riscv64; cp314t dropped (C tokenizer segfaults under free-threading, earwig/mwparserfromhell#343 open upstream).'
- pkg: klayout
  version: 0.30.12
  home: https://github.com/klayout/klayout
  repo: https://github.com/klayout/klayout
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: usearch
  version: 2.26.1
  home: https://github.com/unum-cloud/USearch
  repo: https://github.com/unum-cloud/USearch
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1499
  notes: 'PR #1499 open, CI running. Builds from checkout with cibuildwheel (cp312-cp314t); SIMD dispatch is delegated to the separate NumKong package (setup.py USEARCH_USE_NUMKONG, on by default), which already publishes riscv64 wheels on PyPI, so the default config is unchanged. Only numpy needs our registry.'
- pkg: clang-tidy
  version: 22.1.8
  home: https://clang.llvm.org/extra/clang-tidy/index.html#clang-tidy
  repo: https://github.com/ssciwr/clang-tidy-wheel
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2261
  notes: 'Builds clang-tidy + clang-apply-replacements from LLVM source (CMake ExternalProject downloading llvm-project-22.1.8.src.tar.xz, confirmed a real release asset), same shape as build-clang-format.yml/build-libclang.yml which already ship in this repo on the riscv64 self-hosted runner. No v22.1.8 tag exists yet in ssciwr/clang-tidy-wheel, so the workflow pins commit 0dc6a6e and overrides clang-tidy_version.txt the same way upstream''s own workflow_dispatch(llvm_version) does. Unlike clang-format-wheel, this CMakeLists.txt does not restrict LLVM_TARGETS_TO_BUILD (defaults to all backends), so timeout-minutes is 1440 to match. PR #2261 opened, CI running (multi-hour LLVM/clang build); not yet green.'
- pkg: qiskit-aer
  version: 0.17.2
  home: https://github.com/Qiskit/qiskit-aer
  repo: https://github.com/Qiskit/qiskit-aer
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2262
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CPU-only build is genuinely portable: CUDA/ROCm/MPI are opt-in only (separate cibuildwheel jobs / undefined CMake option by default), and the sole x86 SIMD path (AVX2, src/simulators/statevector/qv_avx2.cpp) is excluded from compilation entirely on non-x86_64 hosts (CMAKE_HOST_SYSTEM_PROCESSOR gate in qiskit_aer/backends/wrappers/CMakeLists.txt) rather than emulated, so none of the gotcha 328/332 SIMDe-correctness risk applies. BLAS is OpenBLAS via `dnf install openblas-devel` on the Rocky10 riscv64 image, the same pattern already proven by cvxopt/scs/casadi. cp314t dropped from the matrix: qiskit (a hard runtime + test dependency) is published on the registry as a single cp310-abi3 wheel, not installable on a free-threaded interpreter. Conan 1.x (used to build the vendored fmt/8.0.1) misdetects riscv64 as x86_64 via its `"64" in machine` fallback and injects an x86-only -m64 flag; fixed by seeding a riscv64-aware Conan settings.yml (all three separate arch/arch_build/arch_target enums) and default profile before Conan runs (gotcha 552-adjacent finding, not yet numbered separately). CI fully green: all 3 legs (cp312/cp313/cp314) built, GPL sources collected, publish dry-ran clean. PR ready for review, not merged.'
- pkg: iregexp-check
  version: 0.1.4
  home: https://github.com/jg-rp/rust-iregexp/tree/main/crates/python
  repo: https://github.com/jg-rp/rust-iregexp/tree/main/crates/python
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2263
  notes: '10 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. Pure-Rust PyO3/maturin extension (pest/pest_derive + pyo3 0.22, no native deps); cargo metadata --filter-platform riscv64gc-unknown-linux-gnu shows no arch-blocking crates, confirmed by a clean riscv64 cross cargo check. Cargo.toml carries an unconditional pyo3 abi3-py38 feature (gotcha 181); riscv64 manylinux floor is cp39, so patches/ bumps it to abi3-py39 (gotcha 555). Upstream ships no cp314t wheel for 0.1.4 (pyo3 0.22 predates Py_GIL_DISABLED), so the matrix is a single cp39-abi3 leg. CI fully green: build passed, publish dry-ran clean. PR ready for review, not merged.'
- pkg: lzfse
  version: 0.4.2
  home: https://github.com/m1stadev/lzfse
  repo: https://github.com/m1stadev/lzfse
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1392
  notes: 'PR #1392 green (4/4 wheels built cp312-cp314t, publish dry-run OK), awaiting review. Pure-C setuptools Extension binding Apple''s LZFSE reference implementation (vendored as a git submodule); no external deps. Mirrors upstream''s pypi-build-publish.yml cibuildwheel workflow narrowed to riscv64. Bundled AUTHORS, COPYING (MIT) and LICENSE.lzfse (BSD-3-Clause, the Apple reference impl) land in the wheel via setuptools'' default license-file discovery, verified by the CI test command.'
- pkg: ifcopenshell
  version: 0.8.5
  home: http://ifcopenshell.org
  repo: http://ifcopenshell.org
  status: parked
  pr: null
  notes: 'Not feasible within practical CI time (park): SWIG-bound (not pybind11) Python extension over IfcOpenShell''s own C++ core (IfcParse/IfcGeom), which upstream''s nix/build-all.py builds against a non-optional native stack - boost 1.86.0 (full source build), libxml2 2.13.8, hdf5 1.13.1, rocksdb (all under IfcParse); OCCT 7.8.1, CGAL v5.6.3 (with GMP 6.3.0 + MPFR 3.1.6 from source), nlohmann-json 3.11.3, Eigen 3.4.0, OpenCOLLADA v1.6.68 (all under IfcGeom) - none swappable for the Python wheel target (build-all.py''s DEPENDENCIES table: IfcGeom needs occ+cgal+json+eigen+OpenCOLLADA+IfcParse; IfcParse needs boost+libxml2+hdf5+rocksdb), before even compiling IfcOpenShell''s own much larger multi-schema codebase (default schemas IFC2X3;IFC4;IFC4X3_ADD2) via SWIG for 5 CPython versions (py310-py314). Upstream itself never builds this cold: ci-ifcopenshell-python-pypi.yml''s `make dist` just downloads a pre-built zip from s3://ifcopenshell-builds keyed to a frozen BUILD_COMMIT; the real compile lives in build_rocky.yml, which runs nix/build-all.py against a persistent, incrementally-updated binary cache (IfcOpenShell/build-outputs, git-lfs+S3) specifically so a full rebuild is never repeated - this repo has no equivalent standing riscv64 cache, so a first port here would be exactly the cold-build case upstream structurally avoids. Concrete scale evidence from this repo''s own infrastructure: cadquery-ocp-novtk (PR #2114) builds only ONE of these deps (OCCT, pinned to 8.0.1.0 - the same scale as IfcOpenShell''s pinned 7.8.1) from scratch on the real riscv64 self-hosted runner, and that alone took 6h41m for the OCCT SDK (run 35438360375 job 105884808675: 2026-09-19T14:53:17Z-21:34:57Z) plus ~9.5-10h per interpreter to compile pybind11 bindings against it (cp312 job 105971393535: 22:47:07Z-08:31:19Z, 9h44m) - about 17h wall-clock for one OCCT-only dependency on one wheel version. IfcOpenShell needs that same OCCT-scale build plus five more substantial from-source native libraries (boost, cgal+gmp+mpfr, opencollada, hdf5, libxml2) plus its own larger codebase, which scales to multiple days of self-hosted riscv64 runner time per version - impractical on shared infrastructure already tight on disk (5.0G free of 252G at triage time) with many concurrent ports in flight. Version note, not the blocker: upstream cuts no per-patch-version git tag for 0.8.5 (branch-based releases); the confirmed source pin, if this is ever revisited, is the `ifcopenshell-python-0.8.5` tag (commit 16723d1). Filed as gotcha 558 (feasibility-and-triage.md). No worktree/branch/PR created.'
- pkg: gmpy2
  version: 2.3.1
  home: https://github.com/gmpy2/gmpy2
  repo: https://github.com/gmpy2/gmpy2
  status: published
  pr: 1225
  notes: '38 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout, mirrors upstream wheels.yml which already builds GMP/MPFR/MPC from source per-wheel and vendors them via auditwheel/delocate. Default cp312/cp313/cp314/cp314t matrix; cp314t builds but skips tests (Cython, a test-only dep, ships no free-threaded wheel anywhere). musllinux dropped (numpy/cython test-extras have no musllinux riscv64 wheel). Patch adds LICENSE.gmp/mpfr/mpc (LGPL-3.0-or-later) next to gmpy2''s own. PR #1225 green: cp312 367 passed/7 skipped, cp313 373 passed/1 skipped, cp314 374 passed, cp314t built (tests skipped); publish dry-run OK. Awaiting review.'
- pkg: python-geohash
  version: 0.9.2
  home: https://github.com/hkwi/python-geohash
  repo: https://github.com/hkwi/python-geohash
  status: published
  pr: 1175
  notes: '1 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. Distinct from already-ported pygeohash (setuptools C-extension); python-geohash is a PyO3/Rust extension built via setuptools-rust, abi3 cp38 floor. Upstream cuts releases without git tags; pinned to commit 1159f4f7 (fix packaging, bumps pyproject.toml to 0.9.2), verified byte-identical to the released sdist. Build-from-checkout, single cp312-abi3 wheel reused/tested for cp312/cp313/cp314; no cp314t since pyo3 abi3 cannot build Py_GIL_DISABLED and upstream ships no free-threaded wheel either. Full test suite (17 tests) verified locally against a built wheel, all passed. PR #1175 opened, CI running.'
- pkg: frisky
  version: 0.7.2
  home: https://getfrisky.dev/
  repo: ''
  status: parked
  pr: null
  notes: 'Not feasible: closed-source, no source ever published (gotcha 431/557,
    feasibility-and-triage.md). The narrow "2 Linux wheels, abi cp311" queue note undersold it
    -- it is actually cp311-abi3 (works cp311-3.14, a normal maturin/PyO3 stable-ABI tag, not a
    narrow first release) plus a macosx_arm64 wheel PyPI''s Linux-only view doesn''t show.
    Real blocker: PyPI''s own metadata is blank (home_page/project_urls both null, which is why
    the queue''s home/repo fell back to the PyPI page itself) and frisky has 43 releases across
    its whole history with **zero** sdists ever (confirmed via the release JSON, every release
    0.1.0-0.7.2, not just this version) -- no upstream checkout exists to build from at all.
    getfrisky.dev (found by web search, "Dask scheduler in Rust" by mrocklin/Matthew Rocklin --
    a genuine, real, actively-developed project, not abandoned) itself links a
    github.com/mrocklin/frisky "source" repo that 404s / is inaccessible to a GitHub
    credential -- ambiguous alone (private vs nonexistent vs just not enabled for this
    session) -- but the site''s own /history/ page removes the ambiguity in the maintainer''s
    own words: "Frisky is free and available to use (see license), but it''s not open source
    today," with reasons given (avoiding community/PR management, preferring private
    development) and only a maybe-later if it "goes big." Independently confirmed by
    downloading the real 0.7.2 manylinux_x86_64 wheel: frisky/frisky.abi3.so is 96.6% of the
    wheel (a genuine, large compiled Rust core wrapped by ~14 small .py files -- not a
    py3-none-style facade, gotcha 27/35 doesn''t apply), but the bundled
    dist-info/licenses/LICENSE is a bespoke "Binary Use and Redistribution License" stating
    verbatim "This license does not grant access to, or any rights in, the source code for the
    Software" and forbidding reverse engineering/decompilation outright -- so even gotcha 77''s
    build-from-what''s-installable escape hatch is foreclosed by the license itself, on top of
    there being no source to point cibuildwheel at in the first place. Two independent
    first-party sources (maintainer''s own blog, wheel''s own bundled LICENSE) agree: this is
    strictly a wait-on-upstream case, same shape as hdbcli/azure-cognitiveservices-speech. No
    worktree/branch/PR created -- diagnosed read-only (PyPI JSON across all releases,
    getfrisky.dev, ci_scripts/wheel_contents.py against the real 0.7.2 wheel). Re-check only if
    frisky is ever open-sourced.'
- pkg: cedarpy
  version: 4.8.7
  home: https://github.com/k9securityio/cedar-py
  repo: https://github.com/k9securityio/cedar-py
  status: publishing
  pr: 2190
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL, and PyPI/Cargo.toml metadata point nowhere obvious either -- found via PKG-INFO''s long-description badge, which links k9securityio/cedar-py (not an official cedar-policy/awslabs repo): a third-party PyO3/maturin wrapper around AWS''s cedar-policy crate. Confirmed maturin build-backend and no-abi3 pyo3 (plain `extension-module` feature) from the sdist''s pyproject.toml/Cargo.toml. Dependency graph (168 crates via `cargo metadata --filter-platform riscv64gc-unknown-linux-gnu`) is pure Rust: only -sys crates are libc/linux-raw-sys (no C libs, no TLS/crypto). `cargo check --target riscv64gc-unknown-linux-gnu` passed cleanly in a rust:trixie container with the cross gcc/libssl-dev:riscv64 packages (gotcha 186); psm (pulled in transitively via stacker) ships a native `src/arch/riscv64.s` in its build.rs arch table, so no scalar-fallback patch is needed. Per-interpreter build (cp39-cp314 + cp314t, mirroring valkey-glide''s identical abi shape); pp311 dropped since PyPy has no riscv64 port. PR #2190 opened, CI running.'
- pkg: taplo
  version: 0.9.3
  home: https://taplo.tamasfe.dev
  repo: https://github.com/tamasfe/taplo
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1173
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Maturin bindings = "bin" CLI (crates/taplo-cli), same shape as typos/zizmor; manifest-path = crates/taplo-cli/Cargo.toml, tag release-taplo-cli-0.9.3. Cargo.lock resolves cleanly for riscv64gc-unknown-linux-gnu (cargo metadata --filter-platform, no target_arch dead-ends); ring 0.17.7 has riscv64gc support. No wheel-level test suite ships in the sdist; test step drives real taplo lint/format against known-bad TOML fixtures, validated end-to-end against a local native build before pushing. First CI run: build succeeded but all 4 test jobs failed - the git checkout has no pyproject.toml anywhere (only the released sdist does, with no in-repo release job producing it), so maturin fell back to the Cargo package name and shipped taplo_cli-0.9.3-*.whl instead of taplo-0.9.3-*.whl; uv pip install taplo then found no match. Filed as gotcha 312 (rust-maturin-and-pyo3.md), committed to main. Fix: write the sdist''s real pyproject.toml at the checkout root via a run: heredoc before the maturin-action step (verified against maturin''s own project_layout.rs resolver that this placement is discovered via the --manifest-path ancestor walk). Pushed, second CI run in progress. Worktree/branch at .claude/worktrees/taplo (branch taplo). PR #1173.'
- pkg: google-antigravity
  version: 0.1.15
  home: https://github.com/Google-Antigravity/antigravity-sdk-python
  repo: https://github.com/Google-Antigravity/antigravity-sdk-python
  status: parked
  pr: null
  notes: 'Verified legitimate (Google-Antigravity org, real commit history, Apache-2.0, PyPI metadata matches the repo exactly). The py3-none tag is not a false alarm: unzip on the two Linux wheels shows a real per-platform payload, a 119.7 MB google/antigravity/bin/localharness (statically linked, stripped ELF, matching arch) beside ~150 KB of actual .py sources, plus 3 more platform wheels on PyPI (macosx_11_0_arm64, win_amd64, win_arm64) - one binary per platform, playwright''s vendored-binary shape (gotcha 35/183). It is load-bearing, not optional: local_connection.py spawns it as the subprocess every LocalAgentConfig session talks to over a protobuf stdio wire ("the Go localharness ingress layer"); the SDK cannot run locally without it. Unlike playwright/claude-agent-sdk (gotcha 35/157) there is no public source, fetch script, or downstream artifact index anywhere: pyproject.toml says "the release script places the platform-appropriate binary...before building each wheel" but that release script and the Go source are not in the repo (Copybara export leaves only the Python side world-readable), and the README states cloning the repo alone is not sufficient to run the SDK. No sibling repo (unlike mosek/gotcha 534), no artifact bucket, no unofficial community build to check - nothing public to probe at any layer. A riscv64 localharness can only be produced and shipped by Google itself. Filed as gotcha 556 (feasibility-and-triage.md). parked as vendored-binary: real arch-specific content (not gotcha 24/27''s cosmetic-tag case) but zero public build surface this repo could ever act on. No worktree/branch/PR created.'
- pkg: hip-python
  version: 7.2.2.562.43
  home: https://github.com/ROCmSoftwarePlatform/hip-python
  repo: https://github.com/ROCmSoftwarePlatform/hip-python
  status: parked
  pr: null
  notes: 'Not feasible: hip-python is AMD ROCm''s Cython/C bindings for the HIP GPU runtime
    (rocm.docs.amd.com/projects/hip-python), and the 7.2.2.562.43 wheels are real
    per-interpreter compiled extensions (six wheels, cp39-cp314, manylinux2014_x86_64 only,
    60-67MB each, no aarch64) -- verified via readelf, not inferred. Every compiled
    module''s source (hip.pyx, chip.pyx, chipblas.pyx, chipsolver.pyx, ...) does `cdef
    extern from "hip/hip_runtime.h"` (and the matching
    hipblas.h/hipsolver.h/hipsparse.h/hipfft.h/hiprand.h/rccl.h/roctx.h for the other
    modules), so the build itself needs the ROCm HIP SDK''s dev headers on the build host,
    even though the compiled .so only dlopens libamdhip64.so (and its siblings) lazily at
    first call rather than linking it: readelf -d on hip.cpython-*.so and chip.cpython-*.so
    shows only libc.so.6 NEEDED (new gotcha 560, feasibility-and-triage.md, generalizing
    gotcha 284''s dlopen rescue -- it defers the missing *library*, not the missing
    *headers*). AMD''s own compatibility matrix
    (rocm.docs.amd.com/.../compatibility-matrix.html) lists supported Linux distros and GPU
    families (CDNA/RDNA) and names no CPU architecture beyond the historical x86_64 default;
    the only riscv64 HIP/ROCm port that exists anywhere is a third-party downstream
    (ISCAS''s port, shipped by Fedora 42 / T2 SDE 25.4, not by AMD), and ROCm/ROCm#5629 --
    the open ask to upstream it -- closed with no AMD commitment or timeline. There is no
    official ROCm apt/yum repo, container image, or header package for riscv64 to provision
    a build host from, so even the from-source build (before any GPU execution is ever
    reached) has no supported path. Parked like gurobipy/mosek (gotcha 453/534):
    vendor-locked to a toolkit/hardware stack this repo cannot provide for riscv64, not a
    code-level blocker.'
- pkg: fast-diff-match-patch
  version: 2.1.0
  home: https://github.com/JoshData/fast_diff_match_patch
  repo: https://github.com/JoshData/fast_diff_match_patch
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1496
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp36,cp37,cp38,cp39,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev. Single C++ extension (interface.cpp) wrapping Google diff-match-patch via a vendored git submodule (diff-match-patch-cpp-stl, header-only, Apache-2.0, same licence as top-level); no external dependencies. Mirrors upstream''s own wheels.yml cibuildwheel job and test invocation (python -m unittest discover {project}/tests). Verified locally (macOS, cp314): pip wheel + install, 9/9 tests passed. PR #1496, branch/worktree fast-diff-match-patch.'
- pkg: polars-lts-cpu
  version: 1.33.1
  home: https://www.pola.rs/
  repo: https://github.com/pola-rs/polars
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2265
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. This repo already ships polars-runtime-32/polars-runtime-64 for riscv64 (build-polars-runtime.yml, versions 1.39.3-1.44.2), which post-split carry polars'' actual compiled backend; 1.33.1 predates that split, so py-polars/pyproject.toml is still named "polars" with everything compiled into one wheel - polars-lts-cpu is a legitimate separate, older-style port. Checked upstream''s own release-python.yml at tag py-1.33.1: the "lts-cpu" distinction is x86-64-only - its CPU-feature RUSTFLAGS step (dropping avx/avx2/fma/bmi in favor of an sse4.2 baseline) is gated `if: matrix.architecture == x86-64`, so on any non-x86 architecture (aarch64 upstream, riscv64 here) polars-lts-cpu compiles byte-for-byte the same as plain polars, just renamed via the same tomlq/sed steps upstream uses. py-polars/Cargo.toml default features (`full` + `nightly`, the latter gating polars-core''s SIMD kernels) are unchanged from what polars-runtime-32/64 already builds successfully on the riscv64 self-hosted runners at newer versions, so no new SIMD/native-library gap is expected; `nightly`''s SIMD path is std::simd (portable_simd), architecture-generic, not x86 intrinsics. Workflow (build-polars-lts-cpu.yml) mirrors build-polars-runtime.yml directly: PyO3/maturin-action against py-polars/Cargo.toml with `--profile dist-release`, LTO/codegen-units overridden off via CARGO_PROFILE_DIST_RELEASE_* env (upstream''s dist-release profile is fat-LTO/codegen-units=1, unaffordable on these runners per gotcha 141), CARGO_BUILD_JOBS=2, manylinux 2_39, and the same minimal `import polars; polars.show_versions()` smoke test (not the full pytest suite) that build-polars-runtime.yml already settled on for this same upstream crate graph. Timeout set to 1440 min: build-polars-runtime.yml took ~4.5h at this same opt-level/job-count on the 4-core riscv64 runners, deltalake''s comparably sized graph took 8.1h. PR #2265 opened; pr-checks and setup green, build_wheels job queued on the riscv64 pool (run 35948524861), multi-hour compile in progress, not yet confirmed green.'
- pkg: pymeshlab
  version: 2025.7.post1
  home: https://github.com/cnr-isti-vclab/PyMeshLab
  repo: https://github.com/cnr-isti-vclab/PyMeshLab
  status: parked
  pr: null
  notes: 'Not feasible within this repo''s build image (park): pymeshlab is a pybind11 wrapper over MeshLab/VCGlib with a Python API that has nothing GUI-shaped about it, but src/CMakeLists.txt (the CMake tree pymeshlab itself drives, not just MeshLab''s optional GUI app) does `find_package(OpenGL REQUIRED)` and `find_package(Qt5 COMPONENTS OpenGL Xml Network REQUIRED)` unconditionally - MESHLAB_BUILD_ONLY_LIBRARIES only drops the standalone meshlab executable, Qt5 stays linked into meshlab-common and the ~60 plugins pymeshlab depends on. Every other heavy dependency in this tree (Boost, CGAL, Embree, libigl, qhull, muparser, tinygltf, lib3ds/lib3mf/u3d, xerces/e57, nexus) is genuinely optional and degrades cleanly at configure time (src/meshlab/src/external/*.cmake, gated by MESHLAB_ALLOW_OPTIONAL_EXTERNAL_LIBRARIES, each with an else()-skip branch) - the same shape this repo already disables Embree with for libigl/point-cloud-utils - so unlike ifcopenshell, most of this dependency tree is not the blocker. Qt5 is the one hold-out with no fallback, and Qt5 is exactly where the pyqt5-qt5 park already leaves this repo: confirmed directly against Rocky Linux 10''s real riscv64 repodata (dl.rockylinux.org, AppStream/BaseOS/CRB) that it ships qt6-qtbase* (what let shiboken6 build, PR #1696) but zero qt5-* packages, and EPEL (gotcha 51) has no riscv64 architecture at all (dl.fedoraproject.org/pub/epel/10/Everything/ lists aarch64/ppc64le/s390x/source/x86_64 only) - so there is no Qt5 SDK to dnf install inside quay.io/pypa/manylinux_2_39_riscv64, the Rocky-10-based image every riscv64 native compile in this repo actually runs in (confirmed across build-quantlib.yml/build-decord.yml/build-eckitlib.yml/build-pyinstaller.yml/build-c2pa-python.yml/build-austin-dist.yml: apt-get on the bare ubuntu-24.04-riscv runner is only ever used for host-side tooling - python3-venv, gdb, locales - never for the library the wheel links against). Ubuntu 24.04''s own riscv64 port does carry a prebuilt qtbase5-dev in universe (ports.ubuntu.com), but that is irrelevant: no build in this repo links a manylinux-container-compiled extension against a different distro''s Qt5 ABI. Building Qt5 (qtbase, with the OpenGL/Xml/Network/Gui/Widgets modules meshlab needs) from source inside the manylinux image would be the pyqt5-qt5 park''s already-documented full Qt5 SDK bring-up - an order of magnitude past pygame/SDL2 or libclang, the largest from-source builds this repo carries - and it would only be the base layer underneath MeshLab''s own VCGlib + meshlab-common + plugin codebase on top, comparable in scale to the OCCT-alone 17h/dependency precedent that parked ifcopenshell. Filed as gotcha 561 (feasibility-and-triage.md); re-queue only alongside pyqt5-qt5 behind a separately scoped in-repo Qt5-from-source project. No worktree/branch/PR created.'
- pkg: cysignals
  version: 1.12.6
  home: https://github.com/sagemath/cysignals
  repo: https://github.com/sagemath/cysignals
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1222
  notes: 'Pure-POSIX Cython/C extension (sigaltstack/sigprocmask/setjmp); the only arch-conditioned code is an x86-only FPU-reset instruction (feature-probed, no-op elsewhere) and a Cygwin+x86_64-only Windows shim, neither required on Linux. Debian builds and installs it on riscv64 already. Build-from-checkout with meson-python, matrix cp312/cp313/cp314/cp314t x manylinux/musllinux. Only divergence: CIBW_BUILD_FRONTEND=build (upstreams build[uv] frontend crashes cibuildwheels audit step with no host uv on the self-hosted runner). PR #1222 open, CI running.'
- pkg: cocotb
  version: 2.0.1
  home: https://www.cocotb.org
  repo: https://github.com/cocotb/cocotb
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2189
  notes: 'Pure POSIX C++ (dlopen-based GPI/VPI/VHPI/FLI shim libs + cocotb.simulator extension), no x86-specific code; verified with a local sdist->wheel build plus a gotcha-25/52-style dry run of the staged nosim pytest suite (98 passed). setup.py hard-errors above Python 3.13 for this release (matches upstreams own cp36-cp313 wheel set), so the matrix is cp312/cp313 only. No HDL simulator is available in the manylinux image, so CI runs the same simulator-agnostic subset upstreams noxfile release_test_nosim runs before publishing; simulator_required tests are deselected. Build-from-checkout, mirrors upstreams build-test-release.yml. PR #2189 open, CI not yet run.'
- pkg: daqp
  version: 0.9.1
  home: http://github.com/darnstrom/daqp
  repo: http://github.com/darnstrom/daqp
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2188
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Pure setuptools+Cython build, no SIMD/asm, no external BLAS/LAPACK - clean port, PR open.'
- pkg: mxnet
  version: 1.9.1
  home: https://github.com/apache/incubator-mxnet
  repo: https://github.com/apache/incubator-mxnet
  status: parked
  pr: null
  notes: 'Bitrotten, retired upstream - not a riscv64-specific blocker. Repo formally moved to the Apache Attic and archived read-only 2023-11-17; zero maintainer response since. mxnet==1.9.1 ships NO sdist on PyPI, only 3 prebuilt py3-none-<platform> bdist wheels (macosx_x86_64, manylinux2014_{x86_64,aarch64}), so a port means reconstructing upstream''s own from-scratch pipeline: the legacy tools/staticbuild/build.sh + make/staticbuild/*.mk + tools/dependencies/make_shared_dependencies.sh Makefile system, which vendor-builds OpenBLAS/OpenCV/libpng/libjpeg-turbo/zlib/protobuf/dmlc-core/nnvm-tvm from source before compiling mxnet''s own C++ core, then hand-packages via tools/pip/setup.py - no CMake/python -m build path is what actually produces the published wheel. Upstream''s real CI was Jenkins (ci/Jenkinsfile*, ci/docker/Dockerfile.build.*), targeting CentOS7/GCC8 (ci/docker/Dockerfile.build.centos7_aarch64_cpu, ci/docker/install/ubuntu_gcc8.sh); the only two .github/workflows are license_check.yml and os_x_staticbuild.yml, so there is no current Linux GH Actions recipe of any kind to narrow to riscv64, let alone one exercised since 2023. On record, unresolved, upstream-unaddressed build breakage against ordinary modern-ish toolchains confirms this is not just staleness: apache/mxnet#21085 (Jul 2022, Ubuntu + GCC 9.4.0, CPU/oneDNN/OpenCV build) fails compiling src/operator/mshadow_op.h (\"expected a (\" in a half2_t template specialization), still open with zero response; apache/mxnet#21170 (Apple Silicon) hits a broken/missing 3rdparty/googletest checkout, also unresolved. riscv64 is absent from every arch-detection point actually read in the build (make/staticbuild/*.mk''s `ARCH` filter list armv6l/armv7l/powerpc64le/ppc64le/aarch64 never includes it, so USE_SSE silently defaults to 1 on riscv64 instead of the kPlain/portable-C mshadow path aarch64 already proves works; tools/staticbuild/build.sh hardcodes the x86-only -mno-avx flag for every non-aarch64 arch) - individually patchable (mirroring the existing aarch64/ppc64le branches) but one more layer on an already-dead, multi-hour, multi-vendored-C++-project legacy build with no working upstream CI left to validate against. Parking rather than sinking a multi-day from-scratch build effort into unmaintained, already-broken-on-x86_64 software; no worktree opened.'
- pkg: gpiod
  version: 2.5.0
  home: https://git.kernel.org/pub/scm/libs/libgpiod/libgpiod.git
  repo: https://git.kernel.org/pub/scm/libs/libgpiod/libgpiod.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1400
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: runai-model-streamer
  version: 0.16.1
  home: https://github.com/run-ai/runai-model-streamer
  repo: https://github.com/run-ai/runai-model-streamer
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2267
  notes: 'PyPI has no project URL (home_page/project_urls both null); found via web search
    -- github.com/run-ai/runai-model-streamer, Run:ai (NVIDIA-owned) Model Streamer, tag
    0.16.1 matches the PyPI version exactly. Pure ctypes wrapper: setup.py bdist_wheel
    --plat-name bundles a Bazel-built libstreamer.so (py3-none-manylinux2014_<arch> on
    PyPI, same scheme this port reuses), no CUDA/GPU dependency at all -- it is a CPU-side
    local-file reader; the S3/GCS/Azure backends (needing AWS/Azure/GCP C++ SDKs) are
    separate optional PyPI packages this port does not touch. cpp/toolchain/configure.bzl
    hardcodes ARCHITECTURES = ["x86_64", "aarch64"]; one two-line patch adds riscv64 (the
    toolchain macros are already fully generic over the arch string). bazel has no riscv64
    release binary, so it is bootstrapped from source (gotcha 47), same recipe as
    build-ray.yml. torch/numpy/humanize/safetensors (test-only) are all already on the
    registry for riscv64.'
- pkg: tgcrypto
  version: 1.2.5
  home: https://github.com/pyrogram
  repo: https://github.com/pyrogram/tgcrypto
  status: published
  pr: 1170
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1170 open, CI running.'
- pkg: typeid-python
  version: 0.3.10
  home: https://github.com/akhundMurad/typeid-python
  repo: https://github.com/akhundMurad/typeid-python
  status: published
  pr: 1398
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #1398 open, CI green (cp312/cp313/cp314/cp314t).'
- pkg: c2pa-python
  version: 0.37.10
  home: https://contentauthenticity.org
  repo: https://github.com/contentauth/c2pa-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1818
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Bundles a prebuilt libc2pa_c.so fetched from contentauth/c2pa-rs releases (c2pa-native-version.txt pins the tag); c2pa-rs publishes no riscv64 asset, so building it from source (gotcha 77) at the pinned tag with the rust_native_crypto feature (avoids OpenSSL). PR #1818 open, CI running.'
- pkg: ada-url
  version: 4.0.0
  home: https://www.ada-url.com/
  repo: https://github.com/ada-url/ada-python
  status: published
  pr: 1221
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cmarkgfm
  version: 2025.10.22
  home: https://github.com/theacodes/cmarkgfm
  repo: https://github.com/theacodes/cmarkgfm
  status: published
  pr: 1177
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1177 opened, CI running.'
- pkg: sphn
  version: 0.2.1
  home: https://github.com/kyutai-labs/sphn
  repo: https://github.com/kyutai-labs/sphn
  status: published
  pr: 1399
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Upstream is kyutai-labs/sphn (PyO3/maturin, symphonia+opus audio I/O). PR #1399 opened, CI running.'
- pkg: onnxsim
  version: 0.7.3
  home: https://github.com/onnxsim/onnxsim
  repo: https://github.com/onnxsim/onnxsim
  status: ci-running
  pr: 2266
  notes: 'PR #2266: mirrors build-onnxoptimizer.yml (onnxsim vendors onnx-optimizer, which vendors onnx, both linked statically); cp310 dropped, no riscv64 onnx wheel for it.'
- pkg: multimark
  version: 0.3.2
  home: https://github.com/posit-dev/multimark
  repo: https://github.com/posit-dev/multimark
  status: in-review
  pr: 2268
  notes: 'CFFI bindings statically compiling vendored cmark-gfm C sources; abi3 (cp39 floor). PR #2268: mirrors build-cmarkgfm.yml, single cp39 build (upstream itself restricts to cp39-abi3), manylinux+musllinux riscv64 both green, publish dry-run clean. No riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xprof
  version: 2.23.1
  home: https://github.com/openxla/xprof
  repo: https://github.com/openxla/xprof
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2270
  notes: 'The "abi: py3" tag is not a pure-Python build: unzipping the released 2.23.1 wheel shows a compiled profiler_plugin_c_api.so (arch-specific, ~100MB), built via Bazel (bazel run plugin:build_pip_package, per the README and plugin/build_pip_package.sh) against XLA/TSL - the same family as jaxlib (PR #526, parked) and tensorflow-io-gcs-filesystem (gotcha 343). No libpywrap_events_db_c_api.so at this version - that Arrow/Parquet events-database target was added to xprof after 2.23.1 (CI first caught this: "no such package xprof/convert/events_db/python" against the xprof-v2.23.1 tag, since the earlier note had been checked against main instead of the tag). Not the jaxlib wall though: jaxlib dies in xla/codegen/intrinsic/cpp (embed_bitcode, no RISC-V LLVM backend); xprof''s native target (xprof/pywrap) depends only on xla/pjrt, xla/tsl/* and xla/hlo/ir, which XLA''s own BUILD files never route through xla/codegen/intrinsic (that genrule is used exclusively by xla/backends/cpu/codegen, xla/service/cpu and xla/service/gpu - confirmed by grepping every reverse dependent in the pinned XLA revision, and re-confirmed against the BUILD file at the actual xprof-v2.23.1 tag). The embedded/llo_analysis path that would pull in more of XLA sits behind enable_embedded_features, off by default in the OSS build. Frontend (Angular/rollup, trace_viewer_v2 wasm) is architecture-independent (confirmed byte-identical across upstream''s macOS/Linux/Windows wheels) and built once on ubuntu-24.04 since node.js has no riscv64 release; only the native .so is built on the riscv64 runner. Wheel is py3-none (ctypes/C-API loaded, not linked against a specific CPython) so one build serves every interpreter - no matrix needed. Bazel-bootstrap-from-source hit a Javac persistent-worker/glibc crash on the manylinux_riscv64 image (Fatal glibc error: pthread_mutex_lock.c, assertion failed: mutex->__data.__owner == 0), fixed with --strategy=Javac=standalone. PR #2270 opened; CI running.'
- pkg: ahocorasick-rs
  version: 1.0.3
  home: https://github.com/G-Research/ahocorasick_rs
  repo: https://github.com/G-Research/ahocorasick_rs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1172
  notes: Distinct from pyahocorasick (already ported, C++-based) - this is the Rust/pyo3 implementation. No [tool.cibuildwheel] upstream; pyo3 0.26 with no abi3 feature, so per-interpreter wheels (matches PyPI cp3xx-cp3xx tags), matrix cp312/cp313/cp314/cp314t. Small pure-Rust dep tree (aho-corasick, itertools, pyo3 + build-only proc-macro crates), Cargo.lock committed upstream, cargo metadata --filter-platform riscv64gc-unknown-linux-gnu confirms no arch-gated deps. Apache-2.0, LICENSE at repo root picked up by maturin default glob. Workflow based on build-regress.yml (rustup installed in CIBW_BEFORE_ALL_LINUX, musllinux dropped).
- pkg: quadprog
  version: 0.1.13
  home: https://github.com/quadprog/quadprog
  repo: https://github.com/quadprog/quadprog
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1176
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Despite the classic Goldfarb/Idnani algorithm''s Fortran heritage, the current quadprog/quadprog v0.1.13 sdist/repo ships hand-ported linear-algebra.c/qr-update.c/solve.QP.c plus a plain Cython wrapper (typed memoryviews, no cimport numpy) - no Fortran toolchain needed, filed as gotcha 310. Build-from-checkout mirrors upstream release.yaml verbatim (cibuildwheel, default cp312/cp313/cp314/cp314t matrix); numpy (runtime) and scipy (test-only, via requirements_test.txt) both have riscv64 wheels on pypi.riseproject.dev for all four interpreters. GPLv2+, single LICENSE at repo root picked up by setuptools'' default license_files glob, no vendored deps so no gpl_sources job needed. Verified end-to-end under QEMU against the real quay.io/pypa/manylinux_2_39_riscv64 image on cp312: builds, installs numpy/scipy from our registry, imports the compiled quadprog.so, 9/9 upstream tests pass. PR #1176, branch quadprog, worktree at .claude/worktrees/quadprog. CI running.'
- pkg: chalk-viztracer
  version: 1.1.2
  home: https://github.com/chalk-ai/viztracer
  repo: https://github.com/chalk-ai/viztracer
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2269
  notes: 'PR #2269 opened. Fork of gaogaotiantian/viztracer; plain C99 extension (src/viztracer/modules), no vendored/third-party C, no SIMD. Upstream already builds Linux wheels via cibuildwheel (manylinux2014) for auto+aarch64 archs, and ships CIBW_ENABLE=cpython-freethreading for cp313t/cp314t, so this is a strong-portability precedent. Only arch-conditional code is one x86 TSC read in modules/quicktime.h guarded by __i386__/__x86_64__/__amd64__; every other arch (riscv64 included) falls back to clock_gettime(CLOCK_MONOTONIC), the same branch already compiled by upstreams own aarch64 leg. attach_process/ ships prebuilt process-injector binaries only for x86/amd64/x86_64/dylib -- no aarch64 or riscv64 build is attempted for that feature either upstream or here, consistent with upstream never functionally testing attach on non-x86 Linux (python-package.yml only runs ubuntu-latest/macos/windows). Workflow mirrors wheels.ymls Linux job: quay.io/pypa/manylinux_2_39_riscv64 (manylinux2014 predates riscv64), matrix cp312/cp313/cp314/cp314t, no musllinux (upstream itself sets CIBW_SKIP=*musllinux*). Validated locally on x86_64: built the wheel with pip wheel --no-build-isolation (clean build under upstreams -Werror -std=c99), installed it, and ran the real test suite (tests/ + example/) with pytest+psutil+loky installed -- 241 passed, 8 skipped, 0 failed, excluding test_torch.py (needs a torch wheel per interpreter our registry doesnt fully cover) and test_remote.py (needs the injector .so above). Worktree .claude/worktrees/chalk-viztracer, branch chalk-viztracer. All 4 legs (cp312/13/14/14t) initially timed out at 90min: pytest tests/test_tracer.py showed 2 F among 17 items, then went silent forever right after test_util.py. Root-caused both with no riscv64 hardware needed, by reproducing locally on x86_64: (1) the hang is tests/test_viewer.py::test_use_external_processor (alphabetically next after test_util.py) -- same bug already on file for upstream viztracer (patches/viztracer/1.1.1/0002-*, gotcha 297): Perfetto trace_processors prebuilt-binary manifest has no riscv64 entry, and the tests own readline-until-marker wait never notices the exited childs closed pipe, spinning forever; fixed with -k "not test_use_external_processor" (deselect, not a path patch, per gotcha 14/283 -- {project}-relative --deselect silently no-ops). (2) the two test_tracer.py failures (TestTracerFilter::test_exclude_files/test_include_files) reproduced deterministically on x86_64 by faithfully replicating cibuildwheels own test-cwd staging: our CIBW_TEST_COMMAND collected via the absolute {project}/tests path while these tests build their expectations around os.path.abspath(./), which resolves to cibuildwheels isolated test cwd -- exactly gotcha 25s documented anti-pattern (use paths relative to that cwd, not {project}/{package}). Fixed by switching to `pytest tests --ignore=tests/test_torch.py --ignore=tests/test_remote.py -k "not test_use_external_processor"` (paths relative to the staged cwd); local repro of the full corrected suite gave 234 passed, 6 skipped, 1 deselected, 0 failed. Neither issue was a riscv64-specific or chalk-viztracer C-extension bug. PR #2269 fully green after the fix (all 4 build legs + publish); .queue.yml marked in-review.'
- pkg: rawpy
  version: 0.27.0
  home: https://github.com/letmaik/rawpy
  repo: https://github.com/letmaik/rawpy.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2193
  notes: 'PR #2193 opened. 12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Ported: Cython wrapper around LibRaw (vendored git submodule), built via cibuildwheel with CMake, same recipe as upstream setup.py on every platform. No architectural blocker: this LibRaw release (0.22.1) has no x86 SIMD intrinsics anywhere, and every optional codec dep (LCMS2/JPEG/Jasper) degrades gracefully via CMake find_package when absent. Rocky 10 riscv64 ships lcms2-devel/jasper-devel directly; libjpeg-turbo is still built from source with -DWITH_JPEG8=ON (matching upstreams build-linux.sh) since Rockys package lacks the JPEG8 ABI the DNG lossy codec needs -- WITH_SIMD=0 since libjpeg-turbo has no riscv64 SIMD kernels either way. Found and fixed a genuine trap doing this: jasper-devel pulls in Rockys own libjpeg-turbo-devel as a dependency, which drops a stale multilib jconfig-64.h that CMakes FindJPEG glob reads before the from-source jconfig.h (ASCII - sorts before . ), silently reporting JPEG_LIB_VERSION 62 instead of 80 with no error -- fixed by rm -f jconfig-{32,64}.h after the custom install; recorded as new gotcha 543. One patch moves 16 OpenMP loops in the vendored LibRaw copy from schedule(dynamic)/schedule(guided) to schedule(static) (gotcha 166s riscv64 libgomp defect) -- raw2image.cpp two of them run on every single postprocess() call regardless of camera or demosaic algorithm, so this is reachable by ordinary decoding, not an edge case. Validated locally end-to-end on x86_64 (quay.io/pypa/manylinux_2_28_x86_64, matching upstreams own manylinux job): built the wheel with the full riscv64-equivalent recipe, installed it in a clean venv, and ran upstreams real test suite against the vendored sample RAW files (NEF/CR2/X3F) with CI=true so the CI-only feature-flag assertion also ran -- 51 passed, 2 skipped (mypy/stubtest self-skip on a non-editable install), 0 failed. License check asserts all five dist-info/licenses files (LICENSE, LICENSE.LibRaw, LICENSE.lcms2, LICENSE.libjpeg-turbo, LICENSE.jasper) land in the wheel. gpl_sources job publishes gcc sources since auditwheel bundles the images libgomp (GPL-3.0-or-later with the GCC Runtime Library Exception) into the wheel. Worktree .claude/worktrees/rawpy, branch rawpy. Next step: watch PR #2193s riscv64 CI run.'
- pkg: drake
  version: 1.56.0
  home: https://drake.mit.edu
  repo: https://drake.mit.edu
  status: parked
  pr: null
  notes: 'Not feasible within practical CI time (park): 6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Real portability signal checked first, and it is positive, not the blocker: drake already ships manylinux_2_34_aarch64 wheels alongside x86_64 (plus macosx_arm64), proving its Bazel 8.6+/CMake/GCC 14 toolchain and ~80 vendored tools/workspace/ deps already compile clean on a non-x86_64 target -- unlike ifcopenshell/pymeshlab/mxnet, this is not a zero-precedent case. The three commercial solvers are all cleanly avoidable without patching drake itself: Gurobi is off by default, Mosek is already turned off for Linux aarch64 in upstreams own tools/wheel/image/build-drake.sh (`[ "$(arch)" == "aarch64" ] && WITH_MOSEK=OFF`), and SNOPT (WITH_SNOPT=ON unconditionally in every official wheel) is fetched from an access-controlled private repo (git@github.com:RobotLocomotion/snopt.git) this repo has no credentials for, but WITH_SNOPT=OFF needs no source at all per tools/workspace/snopt/repository.bzls own deferred-failure design -- none of the three is what blocks this port. What does: (1) upstreams own supported-platform table (doc/_pages/installation.md) lists only x86-64/x86-64-v3/arm64 for every OS row -- riscv64 is explicitly scoped out, not merely unbuilt; (2) there is no public upstream CI workflow to narrow to riscv64 (the drake repos .github/workflows/ has only stale.yml; the real wheel-release pipeline is undocumented internal buildbot infrastructure per tools/wheel/README.rsts pointer to drake.mit.edu/release_playbook.html), so this would be a drive-the-container-yourself port from tools/wheel/wheel_builders Docker/CMake recipe with no CI-file precedent to lean on; (3) scale: drakes own C++ core (excluding third-party, bindings, examples and tests) is 814k+ lines across just multibody/solvers/geometry/systems/math/common/planning, on top of a from-source stack of 15+ substantial native libraries -- VTK (a trimmed internal build via tools/workspace/vtk_internal, but this repos own standalone VTK port already needed "up to 24h/job" for a full VTK build on the 4-core riscv64 self-hosted runners), SuiteSparse, IPOPT, NLopt, OSQP, SCS, oneTBB, Highway, FCL, CCD, gz_math/gz_utils, sdformat, tinygltf, qhull, meshcat, and a Rust toolchain + crate_universe for Clarabel -- not abi3 (pydrake links Python.h directly like VTK), so a full relink of drakes own huge core plus its bindings layer repeats per interpreter (cp312/cp313/cp314) even with Bazels disk-cache sharing the third-party+core compile across them; and (4) upstreams own troubleshooting doc (doc/_pages/troubleshooting.md #build-oom) documents that a default full build commonly runs out of memory even on ordinary x86_64 desktops ("the Drake build will try to use all available CPU and RAM... together those compilation jobs consumed more RAM than was available"), mapping directly onto this repos known-constrained 4-core riscv64 runners. The combination -- an 800k+-line own-codebase, 15+ from-source native deps (one alone needing a 24h budget standalone), three non-abi3 CPython rebuilds, a documented OOM-prone default build, and no CI file to narrow -- is the same sum-exceeds-any-single-precedent reasoning that parked ifcopenshell and pymeshlab, not a park-by-analogy call from size alone. Filed as gotcha 562 (feasibility-and-triage.md). No worktree/branch/PR created.'
- pkg: adbc-driver-flightsql
  version: 1.12.0
  home: https://arrow.apache.org/adbc/
  repo: https://github.com/apache/arrow-adbc
  status: in-review
  pr: 2271
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. Not a pure-Python ctypes wrapper over a plain C/C++ lib like adbc-driver-postgresql/adbc-driver-sqlite: c/driver/flightsql wraps go/adbc/pkg/flightsql, a Go module built via CMakes add_go_lib as `go build -buildmode=c-shared` (CGO_ENABLED=1). Go has shipped linux-riscv64 tier-2 toolchains since 1.19; go.dev serves a go1.26.1.linux-riscv64.tar.gz for the version upstreams own .env pins. Same drive-the-manylinux-image-directly shape as adbc-driver-postgresql/adbc-driver-sqlite (py3-none-any retagged + auditwheel-repaired), with a Go toolchain added to the container (same go.dev-tarball pattern already proven by build-certbot-dns-multi.yml/build-wandb.yml) since the manylinux image ships none; no vcpkg/dnf-devel dependency needed, only Go + the gcc/g++ the image already carries for the other two drivers C++ framework code. Testing mirrors upstreams own native-unix.yml "Test Python Driver Flight SQL" job: build go/adbc/driver/flightsql/cmd/testserver (a self-contained in-memory Flight SQL server, no TLS/auth by default) and run the wheel test suite against it on localhost:41414 - no Dremio/GizmoSQL account needed, those fixtures self-skip via their own env-var checks. Three real fixes during CI: (1) add_go_lib always passes -buildvcs=true to go build, which fails with "detected dubious ownership" against the bind-mounted checkout inside the container - fixed with `git config --global --add safe.directory "*"` (gotcha 117s pattern); (2) the testserver binary is written by the container running as root, so the following host-side step (unprivileged runner user) cannot chmod it - fixed by setting the executable bit inside the container instead; (3) upstreams own pyproject.toml [test] extra is pandas/protobuf/pyarrow/pytest and protobuf was missing from the install line, causing test_errors.py/test_incremental.py to fail collection on `import google.protobuf` - added protobuf (already has real riscv64 wheels on PyPI). CI fully green: build+package, install+pytest (17 passed, 15 skipped - the 15 skips are Dremio/GizmoSQL-only fixtures with no service to point at, self-skipping via their own env-var checks), and the publish jobs dry run logged "[dry-run] Not on main branch" cleanly. All 9 PR checks green (build job, pr-checks.yml x3, docs preview x3, setup). PR #2271 un-drafted, ready for review/merge.'
- pkg: hdrhistogram
  version: 0.10.7
  home: https://github.com/HdrHistogram/HdrHistogram_py
  repo: https://github.com/HdrHistogram/HdrHistogram_py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1397
  notes: Compiles pyhdrh (src/python-codec.c). Mirrors upstream cibuildwheel matrix cp310-cp314, manylinux+musllinux. CI green on all 10 jobs; carries one patch relaxing test_hist_encode's exact zlib-compressed-length assertion (zlib-ng vs zlib version artifact, not riscv64-specific - verified on aarch64 too) and adjusting the wheel license-file check (checkout build has no AUTHORS, unlike the sdist-built PyPI wheel).
- pkg: vegafusion
  version: 2.0.3
  home: https://vegafusion.io
  repo: https://github.com/hex-inc/vegafusion
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2275
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.
    FEASIBLE: PyO3/maturin bindings (pyo3 abi3-py39 in vegafusion-python/Cargo.toml)
    around vegafusion-core/runtime, which pull in arrow 55.1.0, a datafusion 48.0.0
    fork (patched to an apache/datafusion git rev), and tonic/reqwest with rustls
    (no OpenSSL). Strong precedent: datafusion and pylance are already published
    riscv64 ports with the same arrow/datafusion/rustls dependency shape, and
    arro3-core (vegafusion''s own hard runtime dependency) already has a riscv64
    wheel (cp311-abi3) on pypi.riseproject.dev. Uses the vegafusion-core
    protobuf-src feature to vendor+compile protoc, the same mechanism
    build-datafusion.yml''s protoc feature already proves on riscv64. Local
    cargo check --target riscv64gc-unknown-linux-gnu confirms the crate graph
    resolves and that ring''s build script emits correct riscv64gc flags
    (-march=rv64gc -mabi=lp64d); it cannot fully build past ring''s C sources on
    this x86_64 host with no riscv64 cross-gcc available (installing one is
    against the rules), so the C portion is unverified locally. Matrix built on
    cp312 (our registry''s floor for pyarrow/duckdb, both hard test
    dependencies), retested on cp313/cp314, matching build-datafusion.yml''s
    shape. Worktree at .claude/worktrees/vegafusion, branch vegafusion. PR #2275
    opened, CI running.'
- pkg: fuzzyset2
  version: 0.2.5
  home: https://github.com/alpae/fuzzyset/
  repo: https://github.com/alpae/fuzzyset/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1220
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tokie
  version: 0.1.4
  home: https://github.com/chonkie-inc/tokie
  repo: https://github.com/chonkie-inc/tokie
  status: in-review
  pr: 2272
  notes: 'Ordinary maturin/pyo3 build (crates/tokie-python, no abi3): rustls resolves ring not aws-lc-sys (gotcha 541), no onig/vendored C in the enabled feature set. All 5 riscv64 wheels (cp310-cp314) built and tested green; publish dry-run verified. PR #2272.'
- pkg: rapids-logger
  version: 0.3.0
  home: https://github.com/rapidsai/rapids-logger
  repo: https://github.com/rapidsai/rapids-logger
  status: publishing
  pr: 2273
  notes: 'CPU-only C++20 shim around a statically-linked spdlog (scikit-build-core+CMake); no CUDA/GPU code. CI green (build + publish dry-run); 2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hyperscan
  version: 0.8.2
  home: https://github.com/darvid/python-hyperscan
  repo: https://github.com/darvid/python-hyperscan
  status: parked
  pr: null
  notes: 'Parked: python-hyperscan''s CMakeLists.txt (v0.8.2) only uses upstream Intel hyperscan on Windows (USE_VECTORSCAN FALSE there); everywhere else, including all Linux cibuildwheel targets, it hardcodes `set(HYPERSCAN_TAG vectorscan/5.4.12)` / `HYPERSCAN_REPO https://github.com/VectorCamp/vectorscan.git` and builds that fork from source via ExternalProject_Add. So the real dependency is VectorCamp/vectorscan 5.4.12, not Intel/hyperscan -- but that fork has no riscv64 port either: its cmake/platform.cmake only probes for __x86_64__/_M_X64, __i386__/_M_IX86, __ARM_ARCH_ISA_A64, __ARM_ARCH_ISA_ARM and the ppc64el VSX check, with every unmatched architecture (riscv64 included) silently mis-set as ARCH_32_BIT; CMakeLists.txt then does `elseif (ARCH_PPC64EL) ... else() message(FATAL_ERROR "Unsupported platform")` for the arch-specific cflags/SIMD-backend include, so configure aborts outright on riscv64. Even patching past that fatal has nothing to link against: src/util/supervector/arch/ (the NFA/vermicelli SIMD execution backend selected per-arch in the top-level CMakeLists.txt) ships only x86/, arm/ and ppc64el/ implementations -- confirmed with a live clone of both the vectorscan/5.4.12 tag and current upstream master (2026-09-24), zero riscv64/rvv references anywhere in the tree. This is not a CI-coverage gap or an unset SIMDe fallback; it is a missing architecture branch plus a missing SIMD backend in the vendored C++ engine itself, so there is no portable build path today (unlike the SIMDe-generic path vectorscan already offers for ARM/ppc64le). Revisit if VectorCamp/vectorscan ever lands a riscv64 arch branch and supervector backend upstream.'
- pkg: cupy-cuda13x
  version: 14.2.0
  home: https://cupy.dev/
  repo: https://github.com/cupy/cupy
  status: parked
  pr: null
  notes: 'Parked: CuPy built against CUDA 13.x is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: pgserver
  version: 0.1.4
  home: https://github.com/orm011/pgserver
  repo: https://github.com/orm011/pgserver
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2276
  notes: 'Compiles PostgreSQL 16.2 and pgvector 0.6.2 from source via its own Makefile (cibuildwheel before-all = "make"), not a vendored prebuilt binary; neither PostgreSQL''s spinlock/atomics fallback (generic gcc __sync builtins) nor pgvector 0.6.2 (no SIMD dispatch at this version) gate on architecture. Runtime deps fasteners/platformdirs are pure Python; psutil already published to pypi.riseproject.dev for riscv64. Test deps psycopg2-binary/sqlalchemy/sqlalchemy-utils all resolve on riscv64 from PyPI directly. Patch 0001 adds the PostgreSQL/pgvector license texts upstream''s own wheel omits from dist-info/licenses/. Build-from-checkout cibuildwheel workflow mirroring upstream''s build-and-test.yml, tag v0.1.4. PR #2276 opened.'
- pkg: posix-ipc
  version: 1.3.2
  home: https://github.com/osvenskan/posix_ipc/
  repo: https://github.com/osvenskan/posix_ipc/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1171
  notes: '57 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp37,cp38,cp39,pp310,pp311,pp37,pp38); no riscv64 on PyPI or pypi.riseproject.dev. Single C extension (semaphores, shared memory, message queues); build-from-checkout cibuildwheel workflow mirroring upstream build-wheels.yaml, tag rel1.3.2. No runtime deps, so manylinux + musllinux riscv64 both included (gotcha 221). Validated locally via aarch64 rehearsal (manylinux_2_39 + musllinux_1_2, cp312): 93 tests passed on each. PR #1171 CI green: all 8 matrix jobs (cp312/cp313/cp314/cp314t x manylinux/musllinux) passed, publish dry-ran clean. Awaiting review/merge. Worktree/branch at .claude/worktrees/posix-ipc.'
- pkg: lefthook
  version: 2.1.11
  home: https://github.com/evilmartians/lefthook
  repo: https://github.com/evilmartians/lefthook
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2277
  notes: '6 platform wheels upstream (abi: py3; linux x86_64/aarch64, macOS, Windows); no riscv64 on PyPI or pypi.riseproject.dev. hatchling build hook repackages a prebuilt, statically-linked Go binary per platform, no sdist. Go cross-compiles cleanly for riscv64 (CGO_ENABLED=0, no asm, no purego/cgo_import_dynamic, so gotcha 124''s ld-linux issue does not apply - static binary, no PT_INTERP). Workflow mirrors upstream release.yml''s publish-pypi job/PyPI.rakumod: builds the same "no_self_update" goreleaser build id, stages it under lefthook/bin/lefthook-linux-riscv64/, runs `uv build --wheel` with the same LEFTHOOK_TARGET_PLATFORM/ARCH env vars. patches/lefthook/2.1.11/ adds a riscv64 entry to hatch_build.py''s PEP425_TAGS table (py3-none-manylinux_2_39_riscv64), otherwise it falls back to a universal py3-none-any tag. Validated locally: cross-built riscv64 binary + hand-built wheel via uv build reproduce the exact same file layout/tag as the real PyPI wheels; ran the full install/pre-commit-hook/git-commit smoke test against the real x86_64 PyPI binary. PR #2277 opened, CI running. Worktree/branch at .claude/worktrees/lefthook.'
- pkg: pylibmc
  version: 1.6.3
  home: https://sendapatch.se/projects/pylibmc/
  repo: https://github.com/lericson/pylibmc
  status: published
  pr: 1504
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. C extension binding libmemcached; Rocky 10 riscv64 has no libmemcached-devel (no EPEL for this arch, and it is absent from baseos/appstream/CRB too), so build-pylibmc.yml builds libmemcached-awesome (header/API/SONAME-compatible maintained fork) from source in CIBW_BEFORE_ALL instead of upstream''s `yum install -y libmemcached-devel`. SASL left off (CMake default; untested by pylibmc''s own suite, avoids a large transitive Kerberos/GSSAPI dependency). Tests run against a real memcached server started in CIBW_BEFORE_TEST. cp312/cp313/cp314/cp314t x manylinux only (matches upstream''s own CIBW_SKIP of musllinux/pp). Worktree/branch at .claude/worktrees/pylibmc. PR #1504 opened, CI running.'
- pkg: asv
  version: 0.6.6
  home: https://asv.readthedocs.io/en/stable/
  repo: https://github.com/airspeed-velocity/asv
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1820
  notes: '4 Linux wheels upstream (abi: cp314,cp36); no riscv64 on PyPI or pypi.riseproject.dev. Single C++ extension (asv/_rangemedian.cpp, plain STL, no arch-specific code) built via plain setuptools; setup.py hardcodes options={"bdist_wheel": {"py_limited_api": "cp36"}} guarded by Py_GIL_DISABLED (gotcha 34 fixed abi3 floor, upstream''s not ours), so one cp312 build covers cp312/cp313/cp314 plus a separate cp314t free-threaded build. Validated locally (macOS): sdist builds from a full git clone at v0.6.6 (setuptools_scm needs full history/tags, not the GitHub tarball) and `pip wheel --no-deps` produces a real cp36-abi3 wheel with the compiled extension. Testing scoped to test/test_step_detect.py + test/test_statistics.py (exercises _rangemedian directly, including a free-threaded-only concurrency test); full upstream suite needs git/hg/conda/a browser, out of scope. Repo-root `asv/` checkout shadows the installed wheel under pytest''s default rootdir import (gotcha 25), fixed with CIBW_TEST_SOURCES: test pyproject.toml. numpy/scipy/pyyaml are on our registry but numpy (2.5.2 vs PyPI 2.5.3) and pyyaml (6.0.2 vs PyPI 6.0.3) are behind latest PyPI, so PIP_ONLY_BINARY scopes to those three. PR #1820 opened, CI dispatched on the PR branch.'
- pkg: aerospike
  version: 19.2.2
  home: https://aerospike.com
  repo: https://aerospike.com
  status: in-review
  pr: 2310
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Python client wraps the aerospike-client-c C SDK (Apache-2.0, https://github.com/aerospike/aerospike-client-c), bundled as a git submodule, not vendored as a prebuilt binary. Only riscv64-hostile spot found: as_arch.h''s as_arch_pause() spin hint is `#if defined __x86_64__ / #elif __aarch64__` with no fallback, and is used unconditionally from always-compiled spinlock/rwlock inline functions in as_atomic_gcc.h, so riscv64 hits an implicit-declaration -Werror failure; fixed with a one-line portable no-op patch (patches/aerospike/19.2.2/). The patch lands inside a nested submodule (aerospike-client-c/modules/common) whose own .gitmodules already sets `ignore = dirty` on that path, so it does not poison the versioningit-computed wheel version (verified empirically with a throwaway nested-submodule repo: `git describe --dirty` stays clean after the same kind of edit). libyaml-devel (CRB) and openssl-devel (AppStream) both resolve on the riscv64 manylinux image via plain `dnf install`, matching upstream''s own yum install line; no custom OpenSSL-baked image needed. Verified locally on x86_64: sdist->wheel build succeeds cleanly (no cibuildwheel), the compiled .so imports, and `aerospike.client()` against a closed port raises the expected exception.ConnectionError, exercising the same C cluster-init path the patch touches. No live Aerospike server in CI; build-aerospike.yml''s CIBW_TEST_COMMAND is a smoke test instead of upstream''s server-backed integration suite. Default cp312/cp313/cp314/cp314t matrix (upstream is unpinned per-interpreter, no abi3). PR #2310 opened, initial CI (setup/check_patches/check_packages_yaml/check_commit_messages) green, riscv64 build matrix running.'
- pkg: nagisa
  version: 0.3.0
  home: https://github.com/taishi-i/nagisa
  repo: https://github.com/taishi-i/nagisa
  status: parked
  pr: null
  notes: 'Not feasible (blocked-on-dependency, gotcha 492 pattern). nagisa''s own extension (nagisa/nagisa_utils.pyx, plain Cython, no arch-specific code) builds cleanly: `pip wheel` on the real 0.3.0 sdist with current setuptools/Cython/numpy succeeds with no patch, producing a real compiled cp3xx wheel. The blocker is pyproject.toml''s unconditional dependency on DyNet38 (python_version >= "3.8", i.e. every interpreter our matrix would build). DyNet38 -- taishi-i''s own PyPI fork of CMU''s DyNet toolkit, publishing prebuilt wheels for py>=3.8 -- has never shipped an sdist on any of its releases (2.1, 2.2): bdist_wheel only, for x86_64/aarch64/win/macOS, none for riscv64, and none on pypi.riseproject.dev. DyNet itself (github.com/clab/dynet) is a large standalone C++ neural-network toolkit needing CMake, SWIG bindings and a bespoke pinned Eigen fork fetched from a github.com/clab/dynet release asset -- porting it from source is its own multi-week project, well beyond this package''s scope. Traced the actual runtime import graph: `import nagisa` -> nagisa/tagger.py -> nagisa/np_model.py, which loads the bundled pretrained model through a pure-numpy DyNet-text-format parser (nagisa/dynet_loader.py) and never imports `dynet`; only the undocumented nagisa.fit() training path (nagisa/model.py) touches it. So the dependency is nominal for the documented tagging use case (confirmed against the current README, which never mentions DyNet), but pip enforces Requires-Dist at install time regardless of what the code imports -- same shape as the cmeel-assimp/cmeel-zlib case in gotcha 492 -- so patching the dependency out of pyproject.toml to route around it would both diverge from every other platform''s published wheel and is explicitly the wrong fix per that gotcha. `pip install nagisa` therefore cannot resolve on riscv64 for any interpreter we''d build. Parked until DyNet38 (or DyNet, for py<3.8, which does publish sdists but is out of our matrix) ships a riscv64 wheel or sdist. Research done in a throwaway .claude/worktrees/nagisa branch, discarded after confirming; no PR opened.'
- pkg: headroom-ai
  version: 0.37.0
  home: https://headroom-docs.vercel.app
  repo: https://github.com/chopratejas/headroom
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2307
  notes: 'The cp310 ABI tag is real, not a mislabeled pure-Python wheel: crates/headroom-py
    is a genuine PyO3/maturin extension (headroom._core) over a large Rust workspace
    (headroom-core), and pyo3''s abi3-py310 feature is baked unconditionally into
    Cargo.toml''s workspace.dependencies, which is why upstream ships one abi3 wheel
    per platform rather than a per-interpreter matrix. Confirmed via the real sdist
    (pip download + tar tzf) and a local pip wheel/install/import against the
    published x86_64 wheel (headroom._core.hello() == "headroom-core"). Builds
    cleanly on riscv64: aws-lc-sys ships prebuilt riscv64gc bindings (gotcha 138),
    rusqlite (bundled sqlite3.c), tokenizers/onig and the tree-sitter grammars all
    vendor portable C, and the optional ML path (fastembed/ort/magika, gated behind
    the default "ml" Cargo feature) uses ort''s load-dynamic feature - the ONNX
    Runtime shared library is dlopen''d at first use, not linked or downloaded at
    build time, so it is a runtime concern only (and this repo already publishes
    onnxruntime for riscv64 if that path is ever exercised). No musllinux (rustup.rs
    ships no riscv64 musl toolchain) and no free-threaded cp314t wheel: the same
    unconditional abi3-py310 feature has no non-abi3 build path to switch to for
    it, and upstream''s own release.yml builds no cp314t wheel either. Wheel built
    on cp310 (the floor the abi3 tag claims) and re-tested on cp311-cp314 via
    find_compatible_wheel. Test command mirrors upstream''s own per-wheel
    smoke-import gate (release.yml''s smoke-import-wheels job), not the 175k-line
    source-tree pytest suite, which needs the proxy/ml/evals extras and a live
    checkout and belongs to upstream''s ci.yml rather than its wheel matrix. PR
    #2307 open; fast checks (setup, pr-checks lint/commit-message/patches jobs)
    green, riscv64 build_wheels matrix job still running.'
- pkg: tsdownsample
  version: 0.1.5.1
  home: https://github.com/predict-idlab/tsdownsample
  repo: https://github.com/predict-idlab/tsdownsample
  status: published
  pr: 1502
  notes: '56 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mjml-python
  version: 1.4.1
  home: https://github.com/mgd020/mjml-python
  repo: https://github.com/mgd020/mjml-python
  status: in-review
  pr: 2308
  notes: 'PyO3/maturin abi3 wrapper around MRML (Rust MJML port); Cargo.toml hardcodes pyo3 abi3-py38 unconditionally, patched to abi3-py39 since manylinux_2_39_riscv64 ships no cp38.'
- pkg: nixl-cu12
  version: 1.4.0
  home: https://github.com/ai-dynamo/nixl
  repo: https://github.com/ai-dynamo/nixl
  status: in-review
  pr: 2314
  notes: 'CPU-only build (gotcha 411 shape): meson.build treats CUDA as optional ("CUDA not found. UCX backend will be built without CUDA support") and defaults the package dir to nixl_cu12; core libnixl/_bindings have no CUDA DT_NEEDED (only nixl_ep needs libcuda/libtorch_cuda, and it is off by default). Builds UCX v1.22.0 from source (upstream flags minus --with-cuda; Rocky ucx 1.19 lacks --enable-mt and fails "UCX library does not support multi-threading"). Ships UCX+POSIX plugins; patch adds ATTRIBUTIONS-CPP.md to license-files; libaio (LGPL) bundled -> gpl_sources. x86_64 CPU-only rehearsal on Rocky 10: 24 passed, 2 skipped. Latest is 1.4.1. nixl-cu13 is the same tree renamed by nvcc major; a CPU build has no cu13 identity.'
- pkg: siphash24
  version: '1.8'
  home: https://dnicolodi.github.io/python-siphash24/
  repo: https://github.com/dnicolodi/python-siphash24/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1174
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1174 open, CI running.'
- pkg: multi-storage-client
  version: 1.0.1
  home: https://nvidia.github.io/multi-storage-client/
  repo: https://github.com/NVIDIA/multi-storage-client
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2312
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev. PR #2312 open, CI running.'
- pkg: playa-pdf
  version: 1.1.0
  home: https://dhdaines.github.io/playa
  repo: https://dhdaines.github.io/playa
  status: in-review
  pr: 2309
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dynet38
  version: '2.2'
  home: https://github.com/taishi-i/dynet
  repo: https://github.com/taishi-i/dynet
  status: in-review
  pr: 2316
  notes: 'Built from taishi-i/dynet@3c9f35de (build_macos_wheels tip; no 2.2 tag) via setup.py (downloads pinned Eigen b2e267dc99d4, CMake-builds libdynet, Cython _dynet; no SWIG/MKL in the Python build). Three patches: skip -march=native (riscv64 GCC rejects it), posix_memalign fallback for x86-only <mm_malloc.h>, fix license_files (upstream wheels ship no licence). cp312 wheel built and upstream tests/python/test.py passed (60 tests) under QEMU in the manylinux_2_39_riscv64 image (~55 min). Prerequisite for nagisa (parked on this dependency).'
- pkg: chia-rs
  version: 0.48.0
  home: https://github.com/Chia-Network/chia_rs
  repo: https://github.com/Chia-Network/chia_rs
  status: in-review
  pr: 2311
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: annotatedyaml
  version: 1.0.2
  home: https://annotatedyaml.readthedocs.io
  repo: https://github.com/home-assistant-libs/annotatedyaml
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2313
  notes: 'Real Cython extension (constructors, reference); cp313/cp314 only because requires-python >=3.13. PR #2313 open, CI running.'
- pkg: triangle
  version: '20250106'
  home: https://rufat.be/triangle
  repo: https://github.com/drufat/triangle
  status: parked
  pr: null
  notes: 'Parked on licensing: the wrapper is LGPL-3.0, but it statically compiles Jonathan Shewchuk''s vendored triangle.c (github.com/drufat/triangle-c submodule) under his own non-OSI licence, whose text (c/README) reads "Distribution of this code as part of a commercial system is permissible ONLY BY DIRECT ARRANGEMENT WITH THE AUTHOR" and "no compensation is received" for redistribution. This is the exact clause that keeps Debian from carrying Triangle in main (non-free only), and meshpy''s own FAQ confirms commercial redistribution needs Shewchuk''s explicit permission. Publishing riscv64 wheels to pypi.riseproject.dev is exactly this kind of redistribution, so shipping without contacting Shewchuk first is legally questionable; not attempted. Also has no sdist on PyPI since 20200424 (would need building from the v20250106 git tag + submodule) - moot given the licence blocker.'
- pkg: ngrok
  version: 1.7.0
  home: https://ngrok.com
  repo: https://github.com/ngrok/ngrok-python
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2317
  notes: 'Not a vendored-binary case: the wheel ships only ngrok/ngrok.abi3.so (PyO3 0.22 abi3-py310 over the ngrok-rust crate 0.18), no bundled Go agent. No sdist on PyPI, built from the v1.7.0 tag via cibuildwheel (one cp310-abi3 wheel built on cp312, re-tested on cp313/cp314; no cp314t, pyo3 0.22 has no free-threaded support). Only native dep is aws-lc-sys =0.28.0 with the bindgen feature (CMake builder + libclang, so clang-devel in before-all); local cargo check --target riscv64gc-unknown-linux-gnu passed, aws-lc objects compiled for RISC-V. Tests: upstream make mypy on examples, test_main.py, and the token-free test.py tests (test_import, test_pipe_name, test_ca_cert, test_invalid_authtoken); the rest need an NGROK_AUTHTOKEN secret. PR #2317 opened, CI running.'
- pkg: tesserocr
  version: 2.11.0
  home: https://github.com/sirfz/tesserocr
  repo: https://github.com/sirfz/tesserocr
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1821
  notes: '21 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1821 open, CI confirmed green (8/8 legs).'
- pkg: pdf-inspector
  version: 1.17.0
  home: https://github.com/firecrawl/pdf-inspector
  repo: https://github.com/firecrawl/pdf-inspector
  status: in-review
  pr: 2319
  notes: 'The queue''s "abi: cp38" is cp38-abi3, not an old one-interpreter build: a PyO3 0.25 abi3-py38 Rust extension (maturin), actively maintained by Firecrawl (releases every few days; 1.24.0 is latest as of 2026-09-25, so this entry is behind). Upstream pushed no v1.16.0/v1.17.0 tags, so the workflow pins release commit 32555d2 (#450), whose tree matches the PyPI sdist except maturin''s readme path rewrite. The python feature pulls the optional OCR stack (ort load-dynamic, firecrawl-pdfium loaded at runtime, ureq/rustls/ring 0.17.14) but links no native runtime. x86_64 rehearsal of the full build: 7.5 min, 82 passed on cp312/cp313/cp314 with tests/test_python.py; the encrypted-secret123.pdf fixture case fails on every arch at 1.17.0 and is deselected (fixed upstream in #489). A riscv64 cargo check could not reach ring''s C without a riscv64 sysroot on the host. PR #2319 opened; CI running.'
- pkg: py-sr25519-bindings
  version: 0.2.4
  home: https://github.com/JAMdotTech/py-sr25519
  repo: https://github.com/polkascan/py-sr25519-bindings
  status: published
  pr: 1223
  notes: '83 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. CI green on PR #1223.'
- pkg: sdbus
  version: 0.14.3
  home: https://github.com/python-sdbus/python-sdbus
  repo: https://github.com/python-sdbus/python-sdbus/
  status: published
  pr: 1503
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1503 open.'
- pkg: pyvex
  version: 9.3.3
  home: https://api.angr.io/projects/pyvex/en/latest/
  repo: https://github.com/angr/pyvex
  status: in-review
  pr: 2318
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: blosc
  version: 1.11.4
  home: https://github.com/blosc/python-blosc
  repo: https://github.com/blosc/python-blosc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1500
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bashkit
  version: 0.17.1
  home: https://github.com/everruns/bashkit
  repo: https://github.com/everruns/bashkit
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2320
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clevercsv
  version: 0.8.5
  home: https://github.com/alan-turing-institute/CleverCSV/
  repo: https://github.com/alan-turing-institute/CleverCSV/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1745
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cupti-python
  version: 13.3.1
  home: https://docs.nvidia.com/cupti-python/13.3.1/index.html
  repo: https://docs.nvidia.com/cupti-python/13.3.1/index.html
  status: parked
  pr: null
  notes: 'Not feasible: cupti-python is a closed NVIDIA binary wrapper over a closed CUDA Toolkit component, with no source and no riscv64 payload anywhere -- same family as the parked sglang/onnxruntime-gpu/cupy-cuda12x/humming-kernels entries, but reached via gotchas 157/431 (closed vendor, wheel-only) plus 452/480 (clean readelf, driver reached through dlopen). (1) NO SOURCE: all 8 releases on PyPI (12.6.0..13.4.0) are wheels only, zero sdists ever (pip download --no-binary :all: -> no matching distribution); License: ''NVIDIA SOFTWARE LICENSE AGREEMENT'' / Classifier Other/Proprietary, whose sections 2.3 forbid reverse engineering or obtaining source and 2.11 forbid translating output to a non-NVIDIA platform; the only project URLs are docs.nvidia.com pages; it is not in NVIDIA/cuda-python (tree has cuda_bindings/cuda_core/cuda_pathfinder/cuda_python only, no cupti). WHEEL Generator is stock setuptools 83.0.0 (gotcha 385: no vendor repackager recipe to chase). (2) WHEEL CONTENTS (cp312 manylinux_2_28_x86_64, 1.2 MB): seven Cython 3.2.8 extensions built from unpublished .pyx (strings name cupti/_internal/cupti_linux.pyx) -- cupti/cupti.*.so 4.5 MB, _internal/cupti.*.so, _internal/utils.*.so, cycupti, callback_wrappers, globals, utils -- plus cupti.pyi, pure-Python profiler_host.py/pm_sampling.py and cupti-python-samples/*.py (Numba/CuPy/driver vectorAdd demos). readelf -d on every .so lists only libc/libm/libstdc++/libgcc_s/libpthread: libcupti is dlopen''d at runtime via cuda-pathfinder (descriptor_catalog.py ''cupti'' -> libcupti.so.13/.12). (3) DEPENDENCY CHAIN (requires_dist, all hard): nvidia-cuda-cupti~=13.3 (the actual payload: libcupti.so.13, libnvperf_host.so 37 MB, libnvperf_target.so, libpcsamplingutil.so, libcheckpoint.so; LicenseRef-NVIDIA-Proprietary; wheels manylinux_2_25 x86_64/aarch64 + win only, no real sdist -- the 0.0.x sdists are name placeholders), cuda-bindings>=13.0.2 (Apache-2.0 source but no sdist on any of 27 releases, x86_64/aarch64/win wheels only, and its build_hooks.py raises without CUDA_HOME/CUDA_PATH toolkit headers), cuda-pathfinder (py3-none-any), numpy (on RISE), pyelftools (any). Neither nvidia-cuda-cupti nor cuda-bindings has a riscv64 file on PyPI or pypi.riseproject.dev, so per gotcha 40 a wheel could be neither installed nor smoke-tested on riscv64. NVIDIA''s redist manifests settle the payload: redistrib_13.3.1.json cuda_cupti 13.3.75 = linux-x86_64, linux-sbsa, windows-x86_64; redistrib_13.4.2.json cuda_cupti 13.4.92 adds only windows-arm64; zero ''riscv'' strings in either manifest. (4) NO NON-GPU USE CASE: import is lazy (import cupti / from cupti import cupti succeed on a GPU-less x86_64 host in a scratch venv), but every API call fails -- get_context_id/get_device_id/activity_flush_all -> NotSupportedError ''CUDA driver is not found (libcuda.so.1: cannot open shared object file)''; libcupti.so.13 itself dlopens libcuda.so.1/libnvidia-*. CUPTI''s whole purpose is tracing/profiling kernels on NVIDIA hardware (activity/callback/PM-sampling/profiler-host APIs over the CUDA driver); even the host-side metric evaluation in profiler_host.py sits on the closed libnvperf_host.so. Contrast gotcha 405 (nvtx, header-only, no CUDA linkage) and nixl-cu12 (optional CUDA): here there is no CPU path and nothing to compile but a wrapper over closed x86_64/sbsa blobs. Latest upstream is 13.4.0 (queue_triage flags 13.3.1 stale); identically blocked (13.4.0 requires nvidia-cuda-cupti~=13.4, same shape). No workflow, docs/packages yaml, patches or PR created. Revisit only if NVIDIA ships a riscv64 CUDA driver + cuda_cupti redist AND publishes cupti-python source or riscv64 wheels.'
- pkg: acsylla
  version: 1.1.0
  home: http://github.com/acsylla/acsylla
  repo: http://github.com/acsylla/acsylla
  status: in-review
  pr: 2325
  notes: 'PR #2325 opened (build-from-checkout cibuildwheel port of upstream''s bin/build_manylinux_2_28_wheel.sh). No sdist on PyPI for any release; the tag checks out scylladb/cpp-driver (eb6b70d, C++, not cpp-rust-driver) as a git submodule, built static with upstream''s Makefile install-driver target against libuv 1.52.1 built from dist.libuv.org and Rocky 10 openssl-devel (3.5.8). cpp-driver has no x86-only code: its arch #ifs (hash.hpp fnv1a, dse_serialization byte order, atomic exchange, rapidjson SSE/endianness) all have generic fallbacks. Upstream''s suite is 100% integration (class-scoped autouse session fixture against a live Cassandra via docker-compose cassandra:latest); that image has no riscv64 build, so before-test runs the same Cassandra 5.0.9 (archive.apache.org tarball, sha512 pinned) on a pinned Temurin 17.0.20.1 riscv64 JRE (Rocky 10 only ships java-21, which Cassandra 5.0 does not support), with upstream''s conf/cassandra/cassandra.yaml and bin/make_test_certs.sh; procps-ng is needed because cassandra-env.sh calls free. Cassandra''s native jars (jna 5.13, snappy-java, zstd-jni) all carry linux-riscv64; lz4-java and netty-epoll fall back to Java. Local QEMU: cp312 wheel built (~20 min for the extension at -O3 -flto), auditwheel -> manylinux_2_39_riscv64 bundling libssl/libcrypto 3.5.8, libuv static; Cassandra 5.0.9 came up under QEMU in 465s; the riscv64 wheel passed 379 of 380 on session/ssl/statement/cql_types (the one test_ssl connect timeout passed on rerun, and upstream''s own x86_64 wheel hits the same timeout against the emulated server). A full-suite x86_64 dry run against the emulated server had 10 failed / 406 errors, all server write timeouts (upstream yaml sets write_request_timeout 2000ms) from emulation latency -- if real riscv64 runners show CassErrorServerWriteTimeout, that is the knob. Wheel adds LICENSE.* for cpp-driver + third_party, libuv and OpenSSL (upstream wheels ship none; acsylla itself has no LICENSE file, classifiers say Apache-2.0 and MIT).'
- pkg: openviking
  version: 0.4.16
  home: https://github.com/volcengine/openviking
  repo: https://github.com/volcengine/openviking
  status: in-review
  pr: 2323
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: promql-parser
  version: 0.10.0
  home: https://github.com/messense/py-promql-parser
  repo: https://github.com/messense/py-promql-parser
  status: in-review
  pr: 2321
  notes: '70 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cwcwidth
  version: 0.1.12
  home: https://github.com/sebastinas/cwcwidth
  repo: https://github.com/sebastinas/cwcwidth
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1182
  notes: PR open; cp311-abi3 (reused on cp312/cp313/cp314) + cp314t free-threaded build.
- pkg: pocketsphinx
  version: 5.1.1
  home: https://github.com/cmusphinx/pocketsphinx
  repo: https://github.com/cmusphinx/pocketsphinx.git
  status: in-review
  pr: 2322
  notes: '8 Linux wheels upstream (abi: cp311,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: serpyco-rs
  version: 1.22.0
  home: https://github.com/ermakov-oleg/serpyco-rs
  repo: https://github.com/ermakov-oleg/serpyco-rs
  status: published
  pr: 1226
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libcudf-cu12
  version: 26.8.1
  home: https://github.com/rapidsai/cudf
  repo: https://github.com/rapidsai/cudf
  status: parked
  pr: null
  notes: 'Parked: CUDA-toolkit-blocked at build time and at load time, no CPU backend (same verdict as dask-cuda). No sdist in any release (26.8.1 back to 25.4.0); only py3-none-manylinux_2_28 x86_64/aarch64 wheels (~716 MB), Generator scikit-build-core via rapids-build-backend (matrix-entry cuda_suffixed=true;use_cuda_wheels=true). The x86_64 wheel''s payload is one 1.07 GB libcudf/lib64/libcudf.so plus 2849 C++ headers and CMake configs (thrust/cub/libcudacxx/cccl). Of that .so, 919 MB is .nv_fatbin (strings show sm_100/sm_120/sm_120a GPU targets) and 77 MB is .text. cudart is linked statically: its strings include "Failed to load libcuda.so.1", and it dlopens libnvrtc/libnvJitLink. DT_NEEDED: librmm.so, libkvikio.so, libnvcomp.so.5, librapids_logger.so. libcudf/load.py loads nvJitLink and nvrtc through cuda.pathfinder, then rapids_logger, librmm, libkvikio and nvidia.libnvcomp, all before libcudf.so. Upstream cpp/CMakeLists.txt at v26.08.01 has project(CUDF LANGUAGES C CXX CUDA ASM) with a hard FATAL_ERROR below nvcc 12.2 and lists 454 .cu against 113 .cpp sources. It has no CPU, OpenMP, TBB or THRUST_DEVICE_SYSTEM backend option (all 16 option()s are NVTX, tests/benchmarks, per-thread default stream, static cudart, KvikIO remote IO, large strings and lint), so there is no gotcha 411/459 device selector to use. Hard deps are all blocked for riscv64. nvidia-libnvcomp-cu12==5.3.0.16 is a closed NVIDIA SDK license with no sdist: its wheels are x86_64/aarch64/win_amd64 only, and redistrib_5.3.0.json lists linux-x86_64/linux-sbsa/windows only. nvidia-nvjitlink-cu12 is NVIDIA-proprietary, with x86_64/aarch64/win wheels only. librmm-cu12 and libkvikio-cu12 26.8.0 have no sdist, and libkvikio also requires cuda-toolkit[cufile]==12.* and nvidia-cufile-cu12. rapids-logger==0.2.* is also pinned, while RISE carries 0.3.0. NVIDIA''s CUDA redist manifests (redistrib_12.9.1.json, and 13.4.2 as the latest) list no riscv64 platform, so there is no nvcc, cudart or libcuda.so.1 to build or load against. cudf.pandas''s CPU fallback is plain pandas, which is already portable, and not a libcudf code path. Siblings (cudf-cu12, pylibcudf-cu12, librmm/rmm, libkvikio, libraft, libcuvs, libucxx, cuml) share this wall. Revisit only if NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: texterrors
  version: 1.1.8
  home: https://github.com/RuABraun/texterrors
  repo: https://github.com/RuABraun/texterrors
  status: in-review
  pr: 2324
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: econml
  version: 0.17.0
  home: https://github.com/py-why/EconML
  repo: https://github.com/py-why/EconML
  status: blocked-on-dependency
  pr: null
  notes: 'Blocked-on-dependency: numba/llvmlite, same wall as the numba, llvmlite and shap entries (gotchas 40/42/187, feasibility-and-triage.md). Own build is not the problem: the per-interpreter tags are honest, the 0.17.0 sdist ships 7 Cython-pregenerated C extensions (econml/tree/{_criterion,_splitter,_tree,_utils}.c, econml/grf/{_criterion,_utils}.c, econml/policy/_forest/_criterion.c) that setup.py builds as plain numpy-only setuptools Extensions (build requires setuptools/wheel/numpy>=2,<3/scipy/cython), so it would be a trivial cibuildwheel port with a cp39-cp314t matrix. Blocker: 0.17.0 Requires-Dist lists numba>=0.60.0 and shap>=0.46.0 unconditionally (no markers), and pip enforces metadata regardless of use (gotcha 492), so a wheel we built could not be installed on riscv64. It is also a real import-time need, not just metadata: econml/utilities.py does module-level `import sparse as sp` (sparse>=0.15.4, hard dep), and sparse 0.19.2''s default numba_backend does `import numba` in _coo/core.py; utilities is imported by 22 econml modules incl. _cate_estimator and dml, so `import econml.dml` fails without numba. shap itself is lazy-loaded in econml/_shap.py (_LazyModule), but shap is independently parked on the same numba wall. Checked 2026-09-25: numba 0.67.0, llvmlite 0.49.0 and shap 0.52.0 have no riscv64 file on PyPI and 404 on pypi.riseproject.dev/simple/; no numba/llvmlite PR exists in this repo. Other hard deps are fine: numpy, scipy, scikit-learn, pandas, statsmodels, lightgbm are on RISE; sparse, joblib, packaging are py3-none-any. Unblock order: llvmlite -> numba -> (shap, sparse usable) -> econml. Once numba is on the registry, resume with a plain build-from-checkout cibuildwheel job (tag v0.17.0), PIP_EXTRA_INDEX_URL=https://pypi.riseproject.dev/simple/, and upstream''s pytest econml/tests. No worktree/branch/PR created; diagnosed from the PyPI sdist.'
- pkg: pyagrum
  version: 3.1.1
  home: https://agrum.gitlab.io/
  repo: https://gitlab.com/agrumery/aGrUM
  status: queued
  pr: null
  notes: 'New entry (was not previously in the queue) - added after triaging the pyagrum-nightly entry, which turned out unportable (its version string encodes an arbitrary nightly-build timestamp, not a reproducible source ref) and recommended this stable release instead. C++/SWIG/CMake project (aGrUM), tag `3.1.1` = commit c8d35dae; ships 5 abi3 wheels (incl. manylinux2014 x86_64/aarch64) built via `wrappers/pyagrum/wheelhouse/build_wheels_linux.sh` in agrumery/manylinux_2_28_* images; no sdist, no riscv64 wheel. Upstream''s own `get_tags()` only maps x86_64/aarch64/i686 to manylinux2014, so a riscv64 build would come out tagged `linux_riscv64` and need `auditwheel repair`. Runtime deps (numpy, matplotlib, pydot, scikit-learn>=1.7) all resolve on riscv64 (RISE or pure-Python). Not yet build-tested locally; a quick source read found nothing riscv64-hostile, but this is a first-pass note, not a full triage - verify before committing to a port.'
- pkg: pyagrum-nightly
  version: 3.1.1.9.dev202608281787169860
  home: https://agrum.gitlab.io/
  repo: https://gitlab.com/agrumery/aGrUM
  status: parked
  pr: null
  notes: 'PARKED: not a reproducible, trackable release; port the stable `pyagrum` distribution instead (same upstream, same code). Evidence (checked 2026-09-25): (1) The version string is not source-derived. aGrUM own wheel builder (acttools/ActBuilderWheel.py, `act release nightly_wheel pyAgrum`, METADATA `Generator: act (3.0)`) stamps `{VERSION.txt}.dev{datetime.today():%Y%m%d}{HEAD committer unix time}`. 1787169860 is commit 019c3f23 ("[aGrUM] after tag 3.1.1", 2026-08-19, on master/dev); 20260828 is just the day the GitLab schedule re-ran. The same commit was republished 23 times as 3.1.1.9.dev20260820..20260912-1787169860, so this pinned version is one arbitrary rebuild; any build we run gets our own CI date and can never match the queued filename (gotcha 18 three-way match) without faking the clock or patching acttools. (2) No tag, no sdist: pyagrum-nightly has 950 releases on PyPI, every one a .dev release, and none ever shipped an sdist. ci_scripts/check_versions.py skips `is_devrelease` versions, so the upgrade tracker could never follow it, and upstream publishes a new version roughly daily. (3) The queue entry is stale (gotcha 388): latest is 3.2.0.dev202609251790021546 (commit 7b17dad4, after the 3.2.0 release commit d0052301). (4) The code is the stable release: `git diff 3.1.1 019c3f23` touches only VERSION.txt (3.1.1 -> 3.1.1.9) and the version string in generated pyagrum.py. Stable `pyagrum` (PyPI latest 3.1.1, tag 3.1.1 = c8d35dae, 5 abi3 wheels incl. manylinux2014_x86_64/aarch64, no sdist, no riscv64) is not in this queue and is the right target. Nothing arch-blocking was seen in a quick read: C++/SWIG/CMake built by `act release wheel pyAgrum` (upstream CI: wrappers/pyagrum/wheelhouse/build_wheels_linux.sh in agrumery/manylinux_2_28_* images); get_tags() hard-maps only x86_64/aarch64/i686 to manylinux2014, so riscv64 would emit linux_riscv64 and need auditwheel repair; runtime deps numpy/matplotlib/pydot (+ scikit-learn>=1.7 for stable 3.1.1 and nightly 3.2.x) all resolve on riscv64 via RISE/py3-none-any.'
- pkg: littlefs-python
  version: 0.19.0
  home: https://github.com/jrast/littlefs-python
  repo: https://github.com/jrast/littlefs-python
  status: published
  pr: 1227
  notes: '35 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cursor-sdk
  version: 1.0.30
  home: https://cursor.com
  repo: https://cursor.com
  status: parked
  pr: null
  notes: 'PARKED: closed-source vendored-binary with no source to build from (gotcha 157/183/556, feasibility-and-triage.md). This is the real SDK for Anysphere''s proprietary Cursor editor/agents (PyPI author "Cursor", `License: Proprietary`, classifier `License :: Other/Proprietary License`; docs cursor.com/docs/sdk/python), not an unrelated open-source "cursor" library. Evidence (checked 2026-09-25 against the real 1.0.30 files): (1) The 1177-byte sdist is a deliberate stub: its `__init__.py` does `raise ImportError("cursor-sdk requires a binary wheel ... there is no build-from-source path")` and its README says it "exists only as a typo-squatting and integrity guard". All 20 PyPI releases follow the same shape, so there is nothing to build. (2) The 5 wheels are `py3-none-{macosx_11_0_arm64,macosx_11_0_x86_64,manylinux2014_aarch64,manylinux2014_x86_64,win_amd64}`, `Root-Is-Purelib: true` (no extension module, gotcha 27/35); the x86_64 wheel is 182 MB uncompressed of which only 0.32 MB is Python. The rest is a staged "cursor-sdk-bridge" tree under `cursor_sdk/_vendor/bridge/` (manifest.json: os linux, arch x64, sdkPlatformPackage @cursor/sdk-linux-x64): a prebuilt Node.js v24.5.0 x86-64 ELF (129 MB), the minified/webpacked proprietary npm package @cursor/sdk 1.0.30 (LICENSE.md: "(c) Anysphere Inc. All rights reserved. Use is subject to Cursor''s Terms of Service"), and @cursor/sdk-linux-x64 holding `rg`, `cursorsandbox` (4.7 MB static-pie Rust binary, cargo paths show it was built from `repository-cache.ci.anysphere.co`, i.e. Anysphere-internal CI) and two tree-sitter `binding.node` modules. The Python client is only an httpx client of this local bridge: `_vendor.resolve_bridge_path()` takes the bundled launcher, CURSOR_SDK_BRIDGE_BIN, or `cursor-sdk-bridge` on PATH, else raises CursorSDKError; the bridge then talks to Cursor''s hosted backend (api2.cursor.sh / api.cursor.com, CURSOR_API_KEY), so every call needs both the closed bridge and a Cursor account. (3) No public source at any layer: the package.json `repository` (github.com/cursor/cursor) is a 1.6 MB public issue-tracker repo with no code; GitHub code search for `cursorsandbox` in org:cursor returns 0; `scripts/build_wheel.py` (referenced in `_vendor/__init__.py`) is not published. (4) Vendor platform table has no riscv64, read from npm: @cursor/sdk optionalDependencies (1.0.30 and latest 1.0.32) = darwin-arm64, darwin-x64, linux-arm64, linux-x64, win32-x64; registry.npmjs.org/@cursor/sdk-linux-riscv64 is 404; nodejs.org/dist/v24.5.0/SHASUMS256.txt has 0 riscv entries. Even with an unofficial riscv64 Node and rebuilt rg/tree-sitter, `cursorsandbox` is closed and x86_64/aarch64-only, and the "All rights reserved" licence forbids us repacking/modifying the bundle, so only Anysphere can ship a riscv64 build. On riscv64 today pip resolves the stub sdist, which fails at import by design. Queue entry is stale (gotcha 388): latest is 1.0.32, same shape. No worktree/branch/PR created; diagnosed read-only from the real sdist and manylinux2014_x86_64 wheel.'
- pkg: polars-hash
  version: 0.9.2
  home: https://github.com/ion-elgreco/polars-hash
  repo: https://github.com/ion-elgreco/polars-hash
  status: blocked-on-dependency
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. The Python-level dependency chain is fine: polars-hash''s only runtime dep is `polars>=1.36.1`, and modern `polars` is a pure-Python py3-none-any metapackage whose real backend (`polars-runtime-32`, pinned by exact version) is already published for riscv64 on pypi.riseproject.dev at every version polars-hash could plausibly need (confirmed 1.39.3 through 1.44.1, cp310-abi3 + cp314-cp314t) - not a blocker. The actual blocker is at the Rust/maturin layer: polars_hash/Cargo.toml (both the 0.9.1/0.9.2 tags and the unreleased main branch) pins `gxhash = { version = "=3.5.0", default-features = false }` unconditionally (no Cargo feature gate), and polars_hash/src/expressions.rs calls `gxhash::gxhash32/64/128` directly to implement the public `nchash.gxhash32/64/128` Polars expressions. gxhash 3.5.0 (the only version ever published, and still current on crates.io) is hardware-AES-only: its src/gxhash/platform/mod.rs selects an x86.rs module under `cfg(target_arch = "x86"/"x86_64")` or an arm.rs module under `cfg(target_arch = "arm"/"aarch64")`, with no third arm for any other architecture - so `platform::*` fails to resolve at all on riscv64gc-unknown-linux-gnu. Worse, x86.rs itself carries an unconditional `compile_error!` unless `target_feature = "aes"` and `"sse2"` are both set ("Gxhash requires aes and sse2 intrinsics"), confirming there is no software/portable fallback in the crate on any platform - it calls hardware intrinsics (`_mm_aesenc_si128`/`_mm_aesenclast_si128` on x86, ARM crypto extension equivalents on aarch64) directly, gated by a `deterministic`/`hybrid`/`std` feature set that does not include a scalar path. Checked upstream gxhash''s own git main branch (ogxd/gxhash) for a fix: none: last commit touching platform/ was 2025-05-18, still only arm.rs/x86.rs, no riscv issue ever filed. Also checked polars-hash''s own issue tracker (#67, #53 - the original gxhash feature requests) and its main-branch Cargo.toml: still an unconditional `=3.5.0` pin, no feature-gating planned. A riscv64 build would therefore need either (a) a from-scratch, bit-exact reimplementation of AES-based hashing for riscv64 in a crate we do not own and upstream itself has never attempted in over two years - RISC-V''s vector-crypto AES extension (Zvkned) is itself new and not reliably present on generic riscv64 CI hardware, or (b) forking polars_hash to strip the `nchash.gxhash32/64/128` public API and its ~20 hardcoded-expected-value test cases (polars_hash/tests/test_hash.py GXHASH_VECTORS etc., which assert exact hash values matching the upstream `gxhash` PyPI reference implementation, so a from-scratch riscv64 implementation would also have to be bit-identical to pass them unmodified). Both are out of proportion to a small hashing-utility plugin port and would ship a materially different, upstream-divergent wheel rather than a working precedent upstream could adopt. No worktree/branch created (research-only; would have been removed per the blocked-on-dependency convention). Re-check if/when gxhash gains riscv64 support upstream (no such issue exists as of 2026-09-07) or if polars-hash makes gxhash an optional feature.'
- pkg: sglang-router
  version: 0.3.2
  home: https://github.com/sgl-project/sglang/tree/main/sgl-model-gateway
  repo: https://github.com/sgl-project/sglang/tree/main/sgl-model-gateway
  status: in-review
  pr: https://github.com/riseproject-dev/python-wheels/pull/2328
  notes: 'Upstream is sgl-project/sglang sgl-model-gateway/ (formerly sgl-router), a CPU-only Rust HTTP/gRPC router (axum/tokio/tonic, wasmtime, tokenizers, oracle ODPI-C) with a pyo3 abi3-py38 binding; no CUDA anywhere in the crate graph, the Python package only imports sglang in the optional launch_server co-launcher. 0.3.2 is untagged: release commit 3f44268fe5 ("[smg] release 0.3.2 (#17168)"), sources identical to the PyPI sdist. Upstream commits no Cargo.lock, so the sdist lock is carried as a patch (a fresh resolve drifts 304 of ~670 crates). cargo check --target riscv64gc-unknown-linux-gnu with the release lock passed locally (all C build scripts incl. aws-lc-sys, ring, onig, esaxx, odpic, zstd, vendored OpenSSL); CRB protoc 3.19 parses the protos with PROTOC_INCLUDE=/usr/include. Built with maturin-action, --release (fat LTO, opt-level z), timeout 1440.'
- pkg: streaming-form-data
  version: 2.1.0
  home: https://github.com/siddhantgoel/streaming-form-data
  repo: https://github.com/siddhantgoel/streaming-form-data
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1181
  notes: 'Cython extension; _parser.c is committed upstream so no Cython build-time dep. build-from-checkout mirroring build-aiokafka.yml/build-fonttools.yml, cp312/cp313/cp314 (no cp314t: no upstream free-threading signal). manylinux only: moto test dep pulls cryptography, which has no musllinux riscv64 wheel. PR #1181 open, CI running.'
- pkg: sqloxide
  version: 0.61.1
  home: https://github.com/wseaton/sqloxide
  repo: https://github.com/wseaton/sqloxide
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1180
  notes: 'PyO3/maturin extension, no vendored C deps; sdist->bdist build mirroring build-fastuuid.yml. PR #1180 open, CI running.'
- pkg: pycolmap
  version: 4.1.1
  home: https://github.com/colmap/colmap
  repo: https://github.com/colmap/colmap
  status: in-review
  pr: 2330
  notes: 'PR #2330 open, first CI run in progress. No riscv64 blocker found (Debian sid builds COLMAP 4.2.0 on riscv64; CUDA is optional and auto-disabled); the cost is size: upstream vcpkg-builds Ceres/SuiteSparse(+GPL CHOLMOD/SPQR)/METIS/glog/OpenImageIO(+OCIO)/GLEW, none of which Rocky 10 riscv64 packages, so a cached deps+COLMAP SDK job builds them once and cp312/cp313/cp314 jobs build only the bindings. Latest upstream is 4.2.0.'
- pkg: pot
  version: 0.9.7.post1
  home: https://github.com/PythonOT/POT
  repo: https://github.com/PythonOT/POT
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1823
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython/C++ build (setuptools.build_meta): ot.lp.emd_wrap (network simplex), ot.partial.partial_cython, ot.bsp.bsp_wrap (needs the deps/eigen git submodule, header-only, checked out via submodules:true). OpenMP (-fopenmp) on emd_wrap/bsp_wrap. Wheel bundles Eigen (MPL2 + a BSD-licensed AlignedBox::transform()); upstream ships no licence text for it, so patches/pot/0.9.7.post1 adds LICENSE.eigen-mpl2/LICENSE.eigen-bsd at the project root (picked up by setuptools default license_files glob). PR #1823 open, CI running.'
- pkg: gmsh
  version: 4.15.2
  home: https://gmsh.info
  repo: https://gitlab.onelab.info/gmsh/gmsh
  status: published
  pr: 1822
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev. Ships without OpenCASCADE/FLTK (neither available prebuilt for riscv64) and PETSc/MUMPS (upstream default off); single py2.py3-none wheel, tested on cp312/cp313/cp314. PR #1822, CI running.'
- pkg: crosshair-tool
  version: 0.0.110
  home: https://github.com/pschanely/CrossHair
  repo: https://github.com/pschanely/CrossHair
  status: in-review
  pr: 2327
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libcuml-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/cuml
  repo: https://github.com/rapidsai/cuml
  status: parked
  pr: null
  notes: 'Parked: CUDA-toolkit-blocked at build time and at load time, with no CPU backend. The libcudf-cu12 verdict applies here, checked on this package''s own artifacts. 26.8.0 back to 26.2.0 ship no sdist, only py3-none-manylinux_2_28 x86_64/aarch64 wheels (99 MB in 26.8.0, down from ~380-470 MB because FIL moved out to the new libnvforest-cu12). The 25.2.0-25.8.0 "sdists" are 4 KB wheel-stub redirectors (build-backend wheel_stub.buildapi, index_url pypi.nvidia.com) that contain only PKG-INFO and pyproject.toml, with no source. The x86_64 wheel''s payload is one 138.5 MB libcuml/lib64/libcuml.so plus 86 headers. Of that .so, 123.7 MB is .nv_fatbin (strings show sm_100f/sm_120/sm_120a) and 11.3 MB is .text. cudart is linked statically: strings include "Failed to load CUDA driver!", libcuda.so.1 and libnvJitLink.so.12. DT_NEEDED: libcublas.so.12, libcublasLt.so.12, libcufft.so.11, libcurand.so.10, libcusolver.so.11, libcusparse.so.12, libnvJitLink.so.12, libcuvs.so, libnvforest.so, librmm.so, librapids_logger.so and a bundled libgomp. The RPATH points into nvidia/{cublas,cufft,curand,cusolver,cusparse,nvjitlink}/lib. libcuml/load.py loads rapids_logger, libcuvs, libnvforest, librmm and libraft before libcuml.so. Upstream cpp/CMakeLists.txt at v26.08.00 (265b9da) has project(CUML LANGUAGES CXX CUDA) unconditionally. Unlike libcudf it does have option(CUML_ENABLE_GPU ... ON), but that option only adds a CUML_ENABLE_GPU compile definition, which zero .cu/.cpp/.h/.hpp/.cuh files under cpp/ reference. It does not remove CUDA from project(), switch sources or drop dependencies. cpp/ has 153 .cu files against 6 .cpp files, and those 6 are examples/tests/bench only; every libcuml source is .cu. The other options (SINGLEGPU, DISABLE_OPENMP, static cuVS/treelite, per-algo selection) are GPU-build knobs. python/cuml/CMakeLists.txt also appends CUDA to its languages unconditionally, so the old cuml-cpu conda mode is gone, and its CPU path was sklearn/umap-learn delegation anyway, never libcuml. cuml.accel''s CPU fallback is plain scikit-learn, which is already portable. So there is no gotcha 411/459 device selector to use. Hard deps are all blocked for riscv64. cuda-toolkit[cublas,cufft,curand,cusolver,cusparse]==12.* is a py3-none-any facade over nvidia-cublas/cufft/curand/cusolver/cusparse-cu12, which are NVIDIA-proprietary with x86_64/aarch64/win_amd64 wheels only and 0.0.1.dev5 placeholder sdists. nvidia-nvjitlink-cu12 (LicenseRef-NVIDIA-Proprietary) is likewise x86_64/aarch64/win only. libcuvs-cu12, libraft-cu12, librmm-cu12 and libnvforest-cu12 26.8.x have no sdist and only x86_64/aarch64 wheels, and libnvforest has never shipped an sdist. rapids-logger==0.2.* is pinned, while RISE carries 0.3.0. NVIDIA''s CUDA redist manifests list no riscv64 platform (see libcudf-cu12). RAPIDS siblings still queued behind the same wall: cuml-cu12, cudf-cu12, pylibcudf-cu12, librmm-cu12/rmm-cu12 (and -cu13), libkvikio-cu12, libraft-cu12/pylibraft-cu12/raft-dask-cu12, libcuvs-cu12/cuvs-cu12, libucxx-cu12/ucxx-cu12/distributed-ucxx-cu12 and libucx-cu12. libnvforest-cu12 is not queued. Revisit only if NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: pyscipopt
  version: 6.2.1
  home: https://github.com/SCIP-Interfaces/PySCIPOpt
  repo: https://github.com/SCIP-Interfaces/PySCIPOpt
  status: in-review
  pr: 2329
  notes: 'Upstream wheels bundle a prebuilt libscip-linux*.zip from scipoptsuite-deploy v0.12.0 (x86_64/aarch64 only); that zip is built from source by its linux.bash (Boost, LAPACK, METIS, MUMPS, Ipopt, SoPlex 8.0.2, SCIP 10.0.2, all permissive licences), so build-pyscipopt.yml replays the recipe in before-all (minus GCG) and ships the licences + gpl_sources for libgfortran.'
- pkg: pysqlite3
  version: 0.6.0
  home: https://github.com/coleifer/pysqlite3
  repo: https://github.com/coleifer/pysqlite3
  status: published
  pr: 1183
  notes: PyPI wheels (since 0.6.0) are self-contained, statically linked against a downloaded SQLite amalgamation via build-scripts/fetch_source.py; only the sdist links the system libsqlite3. build-pysqlite3.yml mirrors the wheels.yaml job.
- pkg: cvc5
  version: 1.3.4
  home: https://cvc5.github.io/
  repo: https://github.com/cvc5/cvc5
  status: queued
  pr: null
  notes: '22 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp38,pp3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: azureml-dataprep-rslex
  version: 2.26.0
  home: http://aka.ms/data-prep-sdk
  repo: http://aka.ms/data-prep-sdk
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jax-cuda13-plugin
  version: 0.11.1
  home: https://github.com/jax-ml/jax
  repo: https://github.com/jax-ml/jax
  status: parked
  pr: null
  notes: 'Parked: JAX''s CUDA 13 backend plugin is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: pyopengl-accelerate
  version: 3.1.10
  home: https://mcfletch.github.io/pyopengl/
  repo: https://github.com/mcfletch/pyopengl
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1748
  notes: 'PR #1748 green: cp312/cp313/cp314/cp314t manylinux_riscv64 wheels built, 53 tests passed on each, publish dry-ran cleanly. Cython/setuptools build of PyOpenGL''s compiled acceleration package (wrapper, formathandler, arraydatatype, errorchecker, vbo, nones_formathandler, latebind, buffers_formathandler, numpy_formathandler) - dlopened GL, no link-time GL dependency. Dropped musllinux: numpy (a build-time dependency) has no riscv64 musllinux wheel anywhere. Runs the real upstream test suite (upstream itself tests nothing in CI); CIBW_BEFORE_TEST_LINUX installs mesa-libGL since OpenGL.GL binds libGL at import time. Small pyproject.toml patch (license-files) so the wheel ships license.txt - the legacy [project.license] table form suppresses setuptools'' PEP 639 default glob, same gap as the real PyPI wheel. Awaiting maintainer review/merge.'
- pkg: jax-cuda13-pjrt
  version: 0.11.1
  home: https://github.com/jax-ml/jax
  repo: https://github.com/jax-ml/jax
  status: parked
  pr: null
  notes: 'Parked: JAX''s CUDA 13 PJRT runtime is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: kuzu
  version: 0.11.3
  home: https://kuzudb.com/
  repo: https://github.com/kuzudb/kuzu
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jsonobject
  version: 2.3.1
  home: https://github.com/dimagi/jsonobject
  repo: https://github.com/dimagi/jsonobject
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1178
  notes: 'Cython-compiled (5 extension modules: api, base, base_properties, containers, properties), verified against the real PyPI wheel - not pure Python. Build-from-checkout, mirrors upstream pypi.yml. CIBW_TEST_SOURCES: test avoids the checkout jsonobject/ package shadowing the installed wheel (unittest fixtures resolve paths relative to cwd, not __file__). No vendored deps, BSD license.'
- pkg: intel-cmplr-lib-rt
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  status: parked
  pr: null
  notes: 'Not feasible (park, gotcha 506/528 pattern, same evidence rigor as the parked
    intel-cmplr-lib-ur/intel-openmp/mkl entries; NOT parked merely for being Intel-branded -
    tbb (gotcha 263) is the standing counter-example this repo does build from public source).
    Downloaded and unpacked the real manylinux_2_28_x86_64 wheel (40.2MB) from PyPI: the payload
    is 9 unstripped .so files under <pkg>.data/data/lib/ - icx-lto.so (85MB, 68% of unpacked
    size), libsvml.so (27MB), libimf.so (4.7MB), libirng.so, libqkmalloc.so, libirc.so,
    libintlc.so(.5), libxptifw.so, libze_trace_collector.so - exactly the SVML/libimf/libirc
    vectorized-math and compiler-runtime libraries the task description names, not a thin
    shim. All carry the build banner `Intel(R) oneAPI DPC++/C++ Compiler 2026.1.1
    (2026.1.1.20260724)` (gotcha 431 builder-path tell). The biggest file, icx-lto.so, is
    unstripped with debug_info and its embedded debug paths resolve to
    `/netbatch/donb73197_00/.../xmain-rel/LX/xmainefi2linux_release/ws/icsws/...` - Intel''s
    internal build farm and internal release branch, the identical tell gotcha 528 found in
    intel-cmplr-lib-ur, not a public ref. Version 2026.1.1 is a oneAPI release-train number
    with no matching public project tag (gotcha 506''s first check). Checked for public source:
    GitHub search (mcp__github__search_code) for libsvml.so across all repos returns only
    intel/oneapi-containers'' licensing/*/compiler/credist.txt manifests, which list
    libsvml.so, libqkmalloc.so, libpdbx.so.5, libistrconv.so as "credistributable" third-party
    binaries Intel bundles under license - i.e. Intel''s own repos catalog these as
    redistribute-only binaries, never as buildable source. No intel or oneapi-src GitHub org
    repo builds libirc/libimf/libsvml; intel/llvm (Apache-2.0 WITH LLVM-exception, open) is the
    DPC++/SYCL frontend and does not contain them - they are the proprietary "classic"
    Intel C/Fortran compiler runtime, distinct from the open LLVM-based toolchain, and have
    never been open-sourced. LICENSE.txt in the wheel is the Intel Developer Tools EULA
    (proprietary, no source-disclosure or redistribution-with-modification rights). All 6 real
    binaries are x86-64 machine code (SSE/AVX-dispatched vectorized math), inherently
    non-portable without source. Sibling intel-cmplr-lic-rt (same PyPI project family) is
    NOT parked on Intel-branding alone - see its own entry: its wheel has zero compiled code.'
- pkg: tensorflow-decision-forests
  version: 1.12.0
  home: https://github.com/tensorflow/decision-forests
  repo: https://github.com/tensorflow/decision-forests
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: liteparse
  version: 2.14.2
  home: https://github.com/run-llama/liteparse
  repo: https://github.com/run-llama/liteparse
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: shfmt-py
  version: 4.1.0
  home: https://github.com/MaxWinterstein/shfmt-py
  repo: https://github.com/MaxWinterstein/shfmt-py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2197
  notes: 'mvdan/sh ships no riscv64 shfmt release asset; workflow cross-compiles it from source (GOARCH=riscv64), picked up via setup.py''s own PATH fallback with no patch.'
- pkg: traits
  version: 7.1.0
  home: http://docs.enthought.com/traits
  repo: https://github.com/enthought/traits
  status: published
  pr: 1864
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zen-engine
  version: 2.0.2
  home: https://gorules.io
  repo: https://github.com/gorules/zen
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aplr
  version: 10.26.0
  home: https://github.com/ottenbreit-data-science/aplr
  repo: https://github.com/ottenbreit-data-science/aplr
  status: queued
  pr: null
  notes: '19 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onigurumacffi
  version: 1.5.0
  home: https://github.com/asottile/onigurumacffi
  repo: https://github.com/asottile/onigurumacffi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1185
  notes: 'Wraps oniguruma via cffi; setup.py links a system libonig with no vendored source. Build oniguruma 6.9.10 from its official release tarball in CIBW_BEFORE_ALL_LINUX (configure already generated, no autogen.sh needed) so auditwheel vendors libonig.so, mirroring upstream bin/build-manylinux-wheels. abi3 tag is dynamic (setup.py sets py_limited_api to the building interpreter), so build once on cp312 (our floor) + reuse/test on cp313/cp314, plus a separate cp314t job. Patch setup.cfg to widen the explicit license_files list so oniguruma''s BSD-2-Clause COPYING (staged as LICENSE.oniguruma) ships in the wheel. CI is green - both matrix jobs pass (27 tests each, cp312/cp313/cp314 via find_compatible_wheel reuse, plus cp314t), publish job dry-ran cleanly with both wheels named as expected. PR #1185 awaiting maintainer review.'
- pkg: dumb-init
  version: 1.2.5.post1
  home: https://github.com/Yelp/dumb-init/
  repo: https://github.com/Yelp/dumb-init/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1184
  notes: PR open; build-from-checkout cibuildwheel, single cp312 build (py2.py3-none tag collapses matrix); 2 patches backport a testing/ fix and replicate the .post1 version bump Yelp never pushed to the v1.2.5 tag.
- pkg: azureml-dataprep-native
  version: 43.0.0
  home: http://aka.ms/data-prep-sdk
  repo: http://aka.ms/data-prep-sdk
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ladybug
  version: 0.19.1
  home: https://ladybugdb.com/
  repo: https://github.com/LadybugDB/ladybug-python
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dracopy
  version: 2.0.0
  home: https://github.com/seung-lab/DracoPy
  repo: https://github.com/seung-lab/DracoPy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1432
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: linearmodels
  version: '7.0'
  home: https://github.com/bashtage/linearmodels
  repo: https://github.com/bashtage/linearmodels
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cuml-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/cuml
  repo: https://github.com/rapidsai/cuml
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: praat-parselmouth
  version: 0.4.7
  home: https://github.com/YannickJadoul/Parselmouth
  repo: https://github.com/YannickJadoul/Parselmouth
  status: published
  pr: 1746
  notes: '36 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp35,cp36,cp37,cp38,cp39,); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cudf-cu12
  version: 26.8.1
  home: https://github.com/rapidsai/cudf
  repo: https://github.com/rapidsai/cudf
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libtorrent
  version: 2.1.1
  home: http://libtorrent.org
  repo: http://libtorrent.org
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rmm-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/rmm
  repo: https://github.com/rapidsai/rmm
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cascadio
  version: 0.1.1
  home: https://github.com/trimesh/cascadio
  repo: https://github.com/trimesh/cascadio
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kaldialign
  version: 0.12.0
  home: https://github.com/pzelasko/kaldialign
  repo: https://github.com/pzelasko/kaldialign
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1179
  notes: pybind11/CMake extension (edit-distance/alignment methods extracted from Kaldi). No native runtime deps, no vendored code; build-from-checkout mirroring upstream build-wheels.yaml, manylinux + musllinux (upstream skips musllinux, but nothing here blocks it). Apache-2.0, no third-party licensing concerns. Worktree at .claude/worktrees/kaldialign, branch kaldialign.
- pkg: pylibcudf-cu12
  version: 26.8.1
  home: https://github.com/rapidsai/cudf
  repo: https://github.com/rapidsai/cudf
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tf-nightly
  version: 2.22.0.dev20260823
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mink
  version: 1.3.0
  home: https://kevinzakka.github.io/mink/
  repo: https://github.com/kevinzakka/mink
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scalene
  version: 2.3.0
  home: https://github.com/plasma-umass/scalene
  repo: https://github.com/plasma-umass/scalene
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: solace-pubsubplus
  version: 1.11.0
  home: https://docs.solace.com/API/Messaging-APIs/Python-API/python-home.htm
  repo: https://docs.solace.com/API/Messaging-APIs/Python-API/python-home.htm
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: py36); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chiapos
  version: 2.0.12
  home: https://github.com/Chia-Network/chiapos
  repo: https://github.com/Chia-Network/chiapos
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libkvikio-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/kvikio
  repo: https://github.com/rapidsai/kvikio
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dmpython
  version: 2.5.38
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '23 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: libraft-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/raft
  repo: https://github.com/rapidsai/raft
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: librmm-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/rmm
  repo: https://github.com/rapidsai/rmm
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chiavdf
  version: 1.1.14
  home: https://github.com/Chia-Network/chiavdf
  repo: https://github.com/Chia-Network/chiavdf
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vtracer
  version: 0.6.15
  home: http://www.visioncortex.org/vtracer
  repo: http://www.visioncortex.org/vtracer
  status: published
  pr: 1435
  notes: '112 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39,pp310,pp311,pp3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kiwipiepy
  version: 0.23.2
  home: https://github.com/bab2min/kiwipiepy
  repo: https://github.com/bab2min/kiwipiepy
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libuuu
  version: 1.5.243
  home: https://github.com/nxp-imx/mfgtools/tree/master/wrapper
  repo: https://github.com/nxp-imx/mfgtools/tree/master/wrapper
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nautilus-trader
  version: 1.231.0
  home: https://nautilustrader.io
  repo: https://github.com/nautechsystems/nautilus_trader
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zope-proxy
  version: '7.3'
  home: https://zopeproxy.readthedocs.io
  repo: https://github.com/zopefoundation/zope.proxy
  status: published
  pr: 1228
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: osmium
  version: 4.3.1
  home: https://osmcode.org/pyosmium
  repo: https://github.com/osmcode/pyosmium
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: color-operations
  version: 0.4.0
  home: https://github.com/vincentsarago/color-operations
  repo: https://github.com/vincentsarago/color-operations
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: perf-analyzer
  version: 2.60.0
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: pylibraft-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/raft
  repo: https://github.com/rapidsai/raft
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: stim
  version: 1.16.0
  home: https://github.com/quantumlib/stim
  repo: https://github.com/quantumlib/stim
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onemkl-license
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cantera
  version: 3.2.0
  home: https://cantera.org
  repo: https://github.com/Cantera/cantera
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pylzss
  version: 0.3.8
  home: https://github.com/m1stadev/pylzss
  repo: https://github.com/m1stadev/pylzss
  status: published
  pr: 1434
  notes: '39 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: primer3-py
  version: 2.3.0
  home: https://github.com/libnano/primer3-py
  repo: https://github.com/libnano/primer3-py
  status: published
  pr: 1501
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyicu-binary
  version: 2.7.4
  home: https://gitlab.pyicu.org/main/pyicu
  repo: https://gitlab.pyicu.org/main/pyicu
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1508
  notes: '10 Linux wheels upstream (abi: cp36,cp37,cp38,cp39,pp37); no riscv64 on PyPI or pypi.riseproject.dev. C++ extension linking system ICU (libicu-devel 74.2 in manylinux_2_39_riscv64''s appstream repo); auditwheel vendors it in. 2.7.4 predates CPython 3.12''s removal of PyUnicode_WCHAR_KIND; patched with upstream''s own later backport (v2.11, commit 8850cfc). Verified locally against a from-source ICU4C 74.2 build on cp312: 69 passed, 4 deselected (CLDR/tzdata string-literal tests pinned to ICU <=69).'
- pkg: pyston
  version: 2.3.5
  home: https://www.github.com/pyston/pyston
  repo: https://www.github.com/pyston/pyston
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cel-expr-python
  version: 0.1.3
  home: https://github.com/cel-expr/cel-python
  repo: https://github.com/cel-expr/cel-python
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyston-autoload
  version: 2.3.5
  home: https://www.github.com/pyston/pyston
  repo: https://www.github.com/pyston/pyston
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chiabip158
  version: 1.5.4
  home: https://github.com/Chia-Network/chiabip158
  repo: https://github.com/Chia-Network/chiabip158
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1750
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #1750 green: cp312/cp313/cp314/cp314t riscv64 wheels built, 2 upstream tests pass on every leg, publish job dry-ran cleanly.'
- pkg: symengine
  version: 0.14.1
  home: https://github.com/symengine/symengine.py
  repo: https://github.com/symengine/symengine.py
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1752
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mojimoji
  version: 0.0.13
  home: http://github.com/studio-ousia/mojimoji
  repo: http://github.com/studio-ousia/mojimoji
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1186
  notes: 'build-from-checkout cibuildwheel build, default cp312/cp313/cp314/cp314t matrix; PR #1186 green (all 4 wheels built, publish dry-run OK), awaiting review.'
- pkg: pygrib
  version: 2.1.8
  home: https://jswhit.github.io/pygrib/
  repo: https://github.com/jswhit/pygrib
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: runai-model-streamer-s3
  version: 0.16.1
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: comrak
  version: 0.0.16
  home: https://github.com/lmmx/comrak
  repo: https://github.com/lmmx/comrak.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1191
  notes: Rust/PyO3 CommonMark/GFM binding, distinct from the C-based cmarkgfm already ported. pyo3 abi3/abi3t float at the build interpreter (no fixed pyNN floor); built cp312-abi3 plus a separate cp314t job. PR open, CI running.
- pkg: mmhash3
  version: 3.0.1
  home: https://github.com/Fokko/mmhash3
  repo: https://github.com/Fokko/mmhash3
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1187
  notes: 'PR #1187 open. Fork of hajimes/mmh3 (same MurmurHash3 C++ sources, same `import mmh3` module name) - distinct PyPI distribution from the already-ported mmh3, but its own upstream has been stale since 2024 vs mmh3''s active 5.3.0. Upstream never tagged the 3.0.1 release; pinned release commit ebbdeee88 (byte-identical to the PyPI sdist, gotcha 103), no patch needed since that commit already has version=3.0.1 in setup.py. Built/tested clean locally (macOS, cp39 venv, 8/8 upstream tests pass). Worktree at .claude/worktrees/mmhash3, branch mmhash3.'
- pkg: faust-streaming
  version: 0.15.3
  home: https://faust-streaming.github.io/faust/
  repo: https://github.com/faust-streaming/faust
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: skia-pathops
  version: 0.9.2
  home: https://github.com/fonttools/skia-pathops
  repo: https://github.com/fonttools/skia-pathops
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: simplification
  version: 1.0.0
  home: https://github.com/urschrei/simplification
  repo: https://github.com/urschrei/simplification
  status: published
  pr: 1511
  notes: '4 Linux wheels upstream (abi: cp310,cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyvips-binary
  version: 8.18.6
  home: https://github.com/kleisauke/pyvips-binary
  repo: https://github.com/kleisauke/pyvips-binary
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: starlark-pyo3
  version: 2026.1.1
  home: https://github.com/inducer/starlark-pyo3
  repo: https://github.com/inducer/starlark-pyo3
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1512
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ssh2-python
  version: 1.2.0.post1
  home: https://github.com/ParallelSSH/ssh2-python
  repo: https://github.com/ParallelSSH/ssh2-python
  status: published
  pr: 1231
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. Build-from-checkout, mirrors upstream ci/build-wheels.sh which cmake-builds the vendored libssh2/ tree; skips upstream''s from-source OpenSSL build in favor of Rocky 10''s own openssl-devel (perl FindBin gap on riscv64). Default cp312/cp313/cp314/cp314t matrix. Patch widens setuptools license_files glob for LICENSE.libssh2 (BSD-3-Clause)/LICENSE.openssl (Apache-2.0). PR #1231 opened, CI running.'
- pkg: pystack
  version: 1.7.1
  home: https://github.com/bloomberg/pystack
  repo: https://github.com/bloomberg/pystack
  status: queued
  pr: null
  notes: '21 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pytest-mergify
  version: 2026.8.5.3
  home: https://pypi.org/project/pytest-mergify/
  repo: https://pypi.org/project/pytest-mergify/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clickhouse-cityhash
  version: 1.0.2.6
  home: https://github.com/xzkostyan/clickhouse-cityhash
  repo: https://github.com/xzkostyan/clickhouse-cityhash
  status: published
  pr: 1230
  notes: '54 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lilcom
  version: 1.8.2
  home: https://github.com/danpovey/lilcom
  repo: https://github.com/danpovey/lilcom
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1229
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tombi
  version: 1.4.1
  home: https://github.com/tombi-toml/tombi
  repo: https://github.com/tombi-toml/tombi
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1749
  notes: '10 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: foxglove-sdk
  version: 0.27.0
  home: https://docs.foxglove.dev/
  repo: https://github.com/foxglove/foxglove-sdk
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gnureadline
  version: 8.3.3
  home: http://github.com/ludwigschwardt/python-gnureadline
  repo: http://github.com/ludwigschwardt/python-gnureadline
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1194
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Statically links a real GNU readline the build compiles itself from a vendored tarball (rl/readline-8.3.tar.gz + official patches) rather than depending on a system readline/libedit - that vendoring is the whole point of the package. Build-from-checkout cibuildwheel workflow mirroring upstream''s own wheels.yaml (matrix cp312/cp313/cp314/cp314t, upstream already ships cp314t). Added a gpl_sources job (no existing example in the tree to copy - build-numpy.yml''s was removed in #178) that archives rl/ verbatim as GPLv3 Corresponding Source for the statically-linked readline, published alongside the wheels via the existing gpl-sources-artifact plumbing. Verified locally end-to-end in the real manylinux_riscv64 container: built, auditwheel-repaired (vendors libncurses/libtinfo, matching upstream''s own published wheel shape exactly), installed, and imported successfully. PR #1194: all jobs green (4 wheel builds, gpl_sources, publish dry-run); one cp314 job hit a transient "docker daemon not running" runner flake on first attempt, passed clean on retry. Awaiting review.'
- pkg: ripgrep
  version: 15.1.0
  home: https://github.com/BurntSushi/ripgrep
  repo: https://github.com/BurntSushi/ripgrep
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1329
  notes: 'PR #1329 opened (recovered from a stalled porting agent that never committed after 11+ hours; workflow content was already complete and correct on inspection, so committed/pushed/opened the PR directly). Maturin bindings="bin" build, single native wheel covers all interpreters, no pyproject.toml in the git tag so recreated verbatim. CI running.'
- pkg: kaldifst
  version: 1.8.0
  home: https://github.com/k2-fsa/kaldifst
  repo: https://github.com/k2-fsa/kaldifst
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-sat
  version: 1.9.dev15
  home: https://github.com/pysathq/pysat
  repo: https://github.com/pysathq/pysat
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: truss-transfer
  version: 0.0.43
  home: https://github.com/basetenlabs/truss.git
  repo: https://github.com/basetenlabs/truss.git
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp313,cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onnxoptimizer
  version: 0.4.2
  home: https://github.com/onnx/optimizer
  repo: https://github.com/onnx/optimizer
  status: publishing
  pr: 2258
  notes: 'PR #2258: cp311/cp312 built (cp310 dropped, no riscv64 onnx wheel or py3-none-any fallback for it).'
- pkg: tensorrt-cu12-bindings
  version: 11.2.1.2
  home: https://github.com/nvidia/tensorrt
  repo: https://github.com/nvidia/tensorrt
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: safe-pysha3
  version: 1.0.5
  home: https://github.com/5afe/pysha3
  repo: https://github.com/5afe/pysha3
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aspose-words
  version: 26.8.0
  home: https://products.aspose.com/words/python-net/
  repo: https://products.aspose.com/words/python-net/
  status: parked
  pr: null
  notes: 'No public source at any version: pypi.org/pypi/aspose-words/json lists 28 releases (24.6.0..26.9.0) and zero sdists ever, so per gotcha 431 the wheel is the whole evidence base; `home`/`repo` both resolve to the marketing page (gotcha 372''s tell), `License: UNKNOWN` in METADATA with classifiers `License :: Other/Proprietary License` and `License :: Free To Use But Restricted`, and no LICENSE file bundled in dist-info (only METADATA/WHEEL/RECORD/entry_points.txt/top_level.txt) - just the "Temporary License" purchase link in Project-URLs, Aspose''s well-known paid-license-key model (full functionality is gated; unlicensed use runs in evaluation mode with output limits/watermarks, matching gotcha 453/534''s licence-gated shape). Confirmed by unzipping the 26.8.0 manylinux1_x86_64 wheel (92.5 MB, x86_64-only - Aspose does not even publish a Linux aarch64 wheel, only win32/win_amd64/macos-x86_64/macos-arm64/manylinux1_x86_64, a narrower matrix than the aarch64-covering gurobipy/mosek cases): it bundles a closed 32.1 MB `aspose/assemblies/words/Aspose.Words.dll` (.NET IL assembly, arch-neutral bytecode - the actual document engine, whose C# source has never been released at any version) plus a 1.7 MB closed `WrpInterop.Aspose.Words.dll` interop layer, hosted by a private, per-CPython-ABI (cp36..cp313) x86_64-native bridge (`aspose/{pycore,pywords,pydrawing,pygc,pyreflection}.cpython-*-x86_64-linux-gnu.so`, `readelf -d` shows ordinary NEEDED on libc/libstdc++/libm only, `strings -a` has 0 riscv/rv64 hits) that Aspose ships pre-built and never as source - so unlike a plain "IL is portable" case, the Python<->.NET bridge itself is a second, independent closed native gate. That bridge loads a bundled, frozen `netcore3.1` CoreCLR runtime (`aspose/netcore/netcore3.1/{libcoreclr.so,libhostfxr.so,libhostpolicy.so,...}`, all x86_64 ELF) - .NET Core 3.1 (EOL since Dec 2022) predates any dotnet/runtime RISC-V work and was never built for riscv64 by Microsoft at any point in its lifecycle, so even open-sourcing the runtime side would not help. `github.com/aspose-words/Aspose.Words-for-Python-via-.NET` (linked from Project-URL "Examples") is confirmed to hold only usage-example scripts against the closed library, not engine source - the mosek/gurobipy "vendor repo is examples, not source" shape. No community/open-source edition exists; Aspose sells only commercial + time-limited trial licenses. Genuinely infeasible: closed engine assembly (never open, any version) + closed native interop bridge (never open, any version) + a specific closed-vendor .NET runtime build that was never produced for riscv64 by anyone.'
- pkg: lalsuite
  version: 7.26.15
  home: https://git.ligo.org/lscsoft/lalsuite
  repo: https://git.ligo.org/lscsoft/lalsuite
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tosa-tools
  version: 2026.8.0
  home: https://gitlab.arm.com/tosa/tosa-tools
  repo: https://gitlab.arm.com/tosa/tosa-tools
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: p4python
  version: 2026.1.3027817
  home: http://www.perforce.com
  repo: http://www.perforce.com
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyorc
  version: 0.11.0
  home: https://github.com/noirello/pyorc
  repo: https://github.com/noirello/pyorc.git
  status: published
  pr: 1513
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev. pybind11 binding over Apache ORC C++ core; setup.py downloads and builds ORC plus its bundled protobuf/lz4/zstd/zlib/snappy from source (matches upstream, which does this per-interpreter with no shared cache). Stages Apache ORC/lz4/zstd licence text (Apache-2.0 covers protobuf/snappy/zlib text too) since none of it ships in the wheel otherwise. tests/compare excluded: needs a debug-mode ORC tool build upstream''s own wheel job never produces either. Worktree/branch at .claude/worktrees/pyorc. PR #1513 opened, CI running.'
- pkg: databricks-zerobus-ingest-sdk
  version: 1.7.0
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: rbloom
  version: 1.5.4
  home: https://github.com/KenanHanke/rbloom
  repo: https://github.com/KenanHanke/rbloom
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1190
  notes: PyO3, abi3-py37 unconditional in Cargo.toml. Built cp37-abi3 on cp312 (image has no cp37/cp38) plus a separate cp314t job. PR open, CI running.
- pkg: scrypt
  version: 0.9.4
  home: https://github.com/holgern/py-scrypt
  repo: https://github.com/holgern/py-scrypt
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1188
  notes: '17 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Pure C extension (setuptools, build-from-checkout); setup.py links libcrypto for its software AES fallback, so build-scrypt.yml installs openssl-devel via dnf and stages LICENSE.openssl plus the vendored scrypt-1.3.3 C library''s own COPYRIGHT as LICENSE.scrypt-1.3.3 (neither covered by the top-level LICENSE). All four matrix jobs (cp312/cp313/cp314/cp314t) passed on the first CI run (55 passed, 1 skipped each), publish dry-run clean. Worktree at .claude/worktrees/scrypt, branch scrypt. PR #1188 open, awaiting review.'
- pkg: runai-model-streamer-gcs
  version: 0.16.1
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: arcticdb
  version: 6.24.0
  home: https://github.com/man-group/arcticdb
  repo: https://github.com/man-group/arcticdb
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opencc
  version: 1.4.2
  home: https://github.com/BYVoid/OpenCC
  repo: https://github.com/BYVoid/OpenCC
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1433
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: megatron-core
  version: 0.19.0
  home: https://github.com/NVIDIA/Megatron-LM
  repo: https://github.com/NVIDIA/Megatron-LM
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ansible-pylibssh
  version: 1.4.0
  home: https://github.com/ansible/pylibssh
  repo: https://github.com/ansible/pylibssh
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1514
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mercurial
  version: 7.2.4
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: pyscf
  version: 2.14.0
  home: http://www.pyscf.org
  repo: https://github.com/pyscf/pyscf
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: imgui-bundle
  version: 1.92.900
  home: https://imgui-bundle.pages.dev/
  repo: https://github.com/pthom/imgui_bundle
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: daily-python
  version: 0.32.0
  home: https://reference-python.daily.co
  repo: https://github.com/daily-co/daily-python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: persistent
  version: '6.8'
  home: https://persistent.readthedocs.io
  repo: https://github.com/zopefoundation/persistent
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1189
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. ZODB persistence C extension (setuptools + cffi_modules); mirrors build-zope-hookable.yml/build-zope-interface.yml via cibuildwheel, with PIP_EXTRA_INDEX_URL for cffi/zope.interface (registry-only on riscv64) and test_docs excluded (needs docs/api/*.rst, not shipped in the wheel). Worktree at .claude/worktrees/persistent, branch persistent. PR #1189 open, watching CI.'
- pkg: samplerate
  version: 0.2.4
  home: https://python-samplerate.readthedocs.io/
  repo: https://github.com/tuxu/python-samplerate
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1193
  notes: 'PyPI records no project URL; upstream is tuxu/python-samplerate (rewritten from CFFI to pybind11+CMake, tag v0.2.4). CMakeLists FetchContents pybind11 and libsamplerate and statically links both; upstream ships no licence for either, so the build stages both at the checkout root for the default LICEN[CS]E* glob. Matrix mirrors upstream (cp39-cp314, no cp314t). Worktree at .claude/worktrees/samplerate, branch samplerate. PR #1193 open, CI running.'
- pkg: glum
  version: 3.4.1
  home: https://github.com/Quantco/glum
  repo: https://github.com/Quantco/glum
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clvm-rs
  version: 0.19.0
  home: https://github.com/Chia-Network/clvm_rs/
  repo: https://github.com/Chia-Network/clvm_rs/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ydf
  version: 0.16.1
  home: https://github.com/google/yggdrasil-decision-forests
  repo: https://github.com/google/yggdrasil-decision-forests.git
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tabmat
  version: 4.2.1
  home: https://github.com/Quantco/tabmat
  repo: https://github.com/Quantco/tabmat
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: implicit
  version: 0.7.3
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: onnx-weekly
  version: 1.23.0.dev20260831
  home: https://onnx.ai/
  repo: https://github.com/onnx/onnx
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymunk
  version: 7.3.0
  home: https://www.pymunk.org
  repo: https://github.com/viblo/pymunk.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1751
  notes: 'PR #1751 open, CI running. Vendors Munk2D (a Chipmunk2D fork maintained by pymunk''s author) as a git submodule, compiled in-process via cffi out-of-line API mode (no system library dependency). cffi has no riscv64 wheel on PyPI, added via CIBW_ENVIRONMENT.'
- pkg: mergify-cli
  version: 2026.8.11.1
  home: https://mergify.com
  repo: https://github.com/Mergifyio/mergify-cli
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: drjit
  version: 1.5.0
  home: https://github.com/mitsuba-renderer/drjit
  repo: https://github.com/mitsuba-renderer/drjit
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pylibjpeg-libjpeg
  version: 2.4.0
  home: https://github.com/pydicom/pylibjpeg-libjpeg
  repo: https://github.com/pydicom/pylibjpeg-libjpeg
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clvm-tools-rs
  version: 0.4.0
  home: https://github.com/Chia-Network/clvm_tools_rs
  repo: https://github.com/Chia-Network/clvm_tools_rs
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: unicon
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rjieba
  version: 0.2.1
  home: https://github.com/messense/rjieba-py
  repo: https://github.com/messense/rjieba-py
  status: published
  pr: 1445
  notes: '20 Linux wheels upstream (abi: cp314,cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: icechunk
  version: 2.2.0
  home: https://icechunk.io
  repo: https://icechunk.io
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nutpie
  version: 0.16.11
  home: https://pymc-devs.github.io/nutpie/
  repo: https://github.com/pymc-devs/nutpie
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: asyncmy2
  version: 0.2.21
  home: https://github.com/vpmedia/asyncmy2
  repo: https://github.com/vpmedia/asyncmy2.git
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymupdfpro
  version: 1.28.2
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: cityhash
  version: 0.4.10
  home: https://github.com/escherba/python-cityhash
  repo: https://github.com/escherba/python-cityhash
  status: published
  pr: 1233
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1233 open, CI green.'
- pkg: editdistpy
  version: 0.4.0
  home: https://github.com/mammothb/editdistpy
  repo: https://github.com/mammothb/editdistpy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1232
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. pyo3/maturin crate; rustup installed in-container.'
- pkg: simplejpeg
  version: 1.9.0
  home: https://github.com/jfolz/simplejpeg/blob/master/README.rst
  repo: https://github.com/jfolz/simplejpeg
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1446
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython binding around vendored, statically-linked libjpeg-turbo; patched to drop REQUIRE_SIMD on riscv64 (no SIMD kernels there).'
- pkg: mitsuba
  version: 3.9.1
  home: https://github.com/mitsuba-renderer/mitsuba3
  repo: https://github.com/mitsuba-renderer/mitsuba3
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: restate-sdk
  version: 1.0.4
  home: https://restate.dev
  repo: https://github.com/restatedev/sdk-python
  status: queued
  pr: null
  notes: '31 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jdk4py
  version: 25.0.2.1
  home: https://github.com/activeviam/jdk4py
  repo: https://github.com/activeviam/jdk4py
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorflow-io
  version: 0.37.1
  home: https://github.com/tensorflow/io
  repo: https://github.com/tensorflow/io
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cheetah3
  version: 3.2.6.post1
  home: https://cheetahtemplate.org/
  repo: https://cheetahtemplate.org/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp27,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dataframely
  version: 3.0.0
  home: https://dataframely.readthedocs.io/
  repo: https://github.com/quantco/dataframely
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensordict-nightly
  version: 2026.9.2
  home: https://github.com/pytorch/tensordict
  repo: https://github.com/pytorch/tensordict
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: neo4j-rust-ext
  version: 6.2.0.0
  home: https://neo4j.com/
  repo: https://github.com/neo4j/neo4j-python-driver-rust-ext
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: btrees
  version: '6.5'
  home: https://btrees.readthedocs.io
  repo: https://github.com/zopefoundation/BTrees
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1517
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Compiles BTree/Bucket/Set/TreeSet C extensions (setuptools); mirrors build-persistent.yml/build-zope-interface.yml via cibuildwheel, with PIP_EXTRA_INDEX_URL for persistent/zope.interface (registry-only on riscv64). Matrix trimmed to cp312/cp313/cp314/cp314t - zope.interface only published for those interpreters. Worktree at .claude/worktrees/btrees, branch btrees. PR #1517 open, watching CI.'
- pkg: dask-cuda
  version: 26.8.0
  home: https://github.com/rapidsai/dask-cuda
  repo: https://github.com/rapidsai/dask-cuda
  status: parked
  pr: null
  notes: 'Parked: Dask''s RAPIDS/CUDA GPU-cluster integration is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: grpcio-observability
  version: 1.83.1
  home: https://grpc.io
  repo: https://github.com/grpc/grpc/tree/master/src/python/grpcio_observability
  status: queued
  pr: null
  notes: '35 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: astropy-healpix
  version: 2.0.1
  home: https://github.com/astropy/astropy-healpix
  repo: https://github.com/astropy/astropy-healpix
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1518
  notes: '6 Linux wheels upstream (abi: cp310,cp314); no riscv64 on PyPI or pypi.riseproject.dev. The sparse-looking upstream matrix is setup.cfg''s py_limited_api = cp310 abi3 build (one wheel covers cp310+) plus a separate cp314t free-threaded build, not a CPython-version choice. Ported the same shape, floored to our cp312: cp312-abi3 (re-tested on cp313/cp314) + cp314t. cp314t is built and shipped like upstream''s but untested -- astropy, a hard runtime+test dependency, publishes no free-threaded wheel on any index anywhere (CLAUDE.md gotcha 149). One patch (patches/astropy-healpix/2.0.1/): _core.c calls strcmp() without including <string.h>, which only hard-fails under GCC 14 (our manylinux_2_39_riscv64 image); upstream''s older manylinux2014 image lets the implicit-declaration warning slide (CLAUDE.md gotcha 226). Verified locally (macOS arm64, cp312-abi3 equivalent): patched build succeeds, 125 passed/1 skipped. PR #1518, branch/worktree astropy-healpix.'
- pkg: pyglm
  version: 2.8.3
  home: https://github.com/Zuzu-Typ/PyGLM/wiki
  repo: https://github.com/Zuzu-Typ/PyGLM/
  status: published
  pr: 1444
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ray-haproxy
  version: 2.8.25
  home: https://pypi.org/project/ray-haproxy/
  repo: https://pypi.org/project/ray-haproxy/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: valkey-glide-sync
  version: 2.5.2
  home: https://pypi.org/project/valkey-glide-sync/
  repo: https://pypi.org/project/valkey-glide-sync/
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openbricks
  version: 3.1.0
  home: https://openbricks.dev
  repo: https://github.com/1e0ng/openbricks
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: healpy
  version: 1.20.0
  home: https://github.com/healpy/healpy
  repo: https://github.com/healpy/healpy
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: executorch
  version: 1.4.1
  home: https://pytorch.org/executorch/
  repo: https://github.com/pytorch/executorch
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fatfs-ng
  version: 0.1.15
  home: https://github.com/Jason2866/pyfatfs
  repo: https://github.com/Jason2866/pyfatfs
  status: queued
  pr: null
  notes: '35 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: anastruct
  version: 1.7.0
  home: https://github.com/ritchie46/anaStruct
  repo: https://github.com/ritchie46/anaStruct
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: unitypy
  version: 1.25.3
  home: https://github.com/K0lb3/UnityPy
  repo: https://github.com/K0lb3/UnityPy
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-kleenex
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pynini
  version: 2.1.7
  home: https://pynini.opengrm.org
  repo: https://pynini.opengrm.org
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cohere-melody
  version: 0.13.4
  home: https://github.com/cohere-ai/melody
  repo: https://github.com/cohere-ai/melody
  status: queued
  pr: null
  notes: '70 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spacy-transformers
  version: 1.4.0
  home: https://spacy.io
  repo: https://spacy.io
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-easypy
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-topology
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-datastructures
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-aetest
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-async
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-connections
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-reporter
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: minijinja
  version: 2.24.0
  home: https://github.com/mitsuhiko/minijinja
  repo: https://github.com/mitsuhiko/minijinja
  status: published
  pr: 1235
  notes: '8 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev. PR #1235 opened, CI running.'
- pkg: pyats-results
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: symusic
  version: 0.6.0
  home: https://github.com/Yikai-Liao/symusic
  repo: https://github.com/Yikai-Liao/symusic.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1762
  notes: '25 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kaldi-native-fbank
  version: 1.22.3
  home: https://github.com/csukuangfj/kaldi-native-fbank
  repo: https://github.com/csukuangfj/kaldi-native-fbank
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1764
  notes: 'PR #1764 green on cp312/cp313/cp314/cp314t (manylinux only), publish dry-run OK, awaiting review. Self-contained C++ (pybind11 + kissfft, both FetchContent-downloaded at build time; no external Python deps). cp314t needed KALDI_NATIVE_FBANK_CMAKE_ARGS=-DPython_EXECUTABLE=... since setup.py''s legacy -DPYTHON_EXECUTABLE hint is ignored by pybind11 3.0''s modern find_package(Python), which otherwise resolved an unrelated system python3.15 for the free-threaded build. Added LICENSE.kissfft (BSD-3-Clause) via patch, since kissfft is statically linked but ships no licence text in the real PyPI wheel.'
- pkg: pyats-tcl
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyicu-wheels
  version: 2.15.2
  home: https://github.com/Init7/pyicu#readme
  repo: https://github.com/Init7/pyicu
  status: queued
  pr: null
  notes: '48 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sgl-deep-ep
  version: 0.1.2
  home: https://pypi.org/project/sgl-deep-ep/
  repo: https://pypi.org/project/sgl-deep-ep/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-aereport
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-utils
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyats-log
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kivy
  version: 2.3.1
  home: http://kivy.org
  repo: https://github.com/kivy/kivy
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sslpsk-pmd3
  version: 1.0.3
  home: https://github.com/doronz88/sslpsk-pmd3
  repo: https://github.com/doronz88/sslpsk-pmd3
  status: queued
  pr: null
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: qutip
  version: 5.3.1
  home: https://qutip.org
  repo: https://github.com/qutip/qutip
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openimageio
  version: 3.1.17.0
  home: https://openimageio.org/
  repo: https://github.com/AcademySoftwareFoundation/OpenImageIO
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deflate
  version: 0.9.0
  home: https://github.com/dcwatson/deflate
  repo: https://github.com/dcwatson/deflate
  status: published
  pr: 1234
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #1234 opened, CI running.'
- pkg: litert-converter
  version: 0.4.0
  home: https://www.tensorflow.org/lite/
  repo: https://www.tensorflow.org/lite/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyoxigraph
  version: 0.5.11
  home: https://pyoxigraph.readthedocs.io/
  repo: https://github.com/oxigraph/oxigraph/tree/main/python
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pywhispercpp
  version: 1.5.1
  home: https://absadiki.github.io/pywhispercpp/
  repo: https://github.com/absadiki/pywhispercpp
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rapidyaml
  version: 0.15.2
  home: https://github.com/biojppm/rapidyaml-python
  repo: https://github.com/biojppm/rapidyaml-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1761
  notes: '21 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: wreq
  version: 0.12.1
  home: https://github.com/0x676e67/wreq-python
  repo: https://github.com/0x676e67/wreq-python
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ethos-u-vela
  version: 5.2.0
  home: https://gitlab.arm.com/artificial-intelligence/ethos-u/ethos-u-vela
  repo: https://gitlab.arm.com/artificial-intelligence/ethos-u/ethos-u-vela
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: livekit-plugins-ai-coustics
  version: 0.3.2
  home: https://pypi.org/project/livekit-plugins-ai-coustics/
  repo: https://pypi.org/project/livekit-plugins-ai-coustics/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: polyscope
  version: 2.6.1
  home: https://github.com/nmwsharp/polyscope
  repo: https://github.com/nmwsharp/polyscope
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pylibjpeg-openjpeg
  version: 2.5.0
  home: https://github.com/pydicom/pylibjpeg-openjpeg
  repo: https://github.com/pydicom/pylibjpeg-openjpeg
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hierarchicalforecast
  version: 1.5.1
  home: https://github.com/Nixtla/hierarchicalforecast/
  repo: https://github.com/Nixtla/hierarchicalforecast/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: genie
  version: '26.7'
  home: https://developer.cisco.com/pyats/
  repo: https://developer.cisco.com/pyats/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: matscipy
  version: 1.2.0
  home: http://libatoms.github.io/matscipy/
  repo: https://github.com/libAtoms/matscipy
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ale-py
  version: 0.12.1
  home: https://github.com/Farama-Foundation/Arcade-Learning-Environment
  repo: https://github.com/Farama-Foundation/Arcade-Learning-Environment
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: obspy
  version: 1.5.1
  home: https://www.obspy.org
  repo: https://github.com/obspy/obspy
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sparse-dot-topn
  version: 1.2.0
  home: https://github.com/ing-bank/sparse_dot_topn
  repo: https://github.com/ing-bank/sparse_dot_topn
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: geoarrow-c
  version: 0.3.1
  home: https://geoarrow.org
  repo: https://github.com/geoarrow/geoarrow-c
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: polars-runtime-compat
  version: 1.44.1
  home: https://www.pola.rs/
  repo: https://github.com/pola-rs/polars
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymeshfix
  version: 0.18.1
  home: https://github.com/pyvista/pymeshfix
  repo: https://github.com/pyvista/pymeshfix
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyheck
  version: 0.1.5
  home: https://github.com/kevinheavey/pyheck
  repo: https://github.com/kevinheavey/pyheck
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1237
  notes: '11 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyjoulescope-driver
  version: 2.4.1
  home: https://joulescope-driver.readthedocs.io/en/latest/
  repo: https://github.com/jetperch/joulescope_driver/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymaxflow
  version: 1.3.2
  home: https://github.com/pmneila/PyMaxflow
  repo: https://github.com/pmneila/PyMaxflow
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1236
  notes: 'PR #1236 open, CI running. Cython bindings for the Boykov-Kolmogorov maxflow/min-cut C++ library; numpy is a build+runtime dep, Cython a build-only dep, both from our registry (neither has a riscv64 wheel on PyPI). cp314t dropped: Cython has no free-threaded wheel anywhere yet. Project ships no LICENSE at all despite declaring GPL; vendored core solver header states GPLv3-or-later explicitly, so a patch adds the canonical GPLv3 text plus the solvers own notice (mirrors how conda-forges feedstock fills the same gap).'
- pkg: labmaze
  version: 1.0.6
  home: https://github.com/google-deepmind/labmaze
  repo: https://github.com/google-deepmind/labmaze
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1763
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: pemja
  version: 0.6.1
  home: https://pypi.org/project/pemja/
  repo: https://pypi.org/project/pemja/
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aic-sdk
  version: 3.1.0
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: taichi
  version: 1.7.4
  home: https://github.com/taichi-dev/taichi
  repo: https://github.com/taichi-dev/taichi
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: strict-kwargs
  version: 2026.8.28.post2
  home: https://github.com/adamtheturtle/strict-kwargs
  repo: https://github.com/adamtheturtle/strict-kwargs
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyjnius
  version: 1.7.0
  home: https://github.com/kivy/pyjnius
  repo: https://github.com/kivy/pyjnius
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pylibmagic
  version: 0.5.0
  home: https://github.com/kratsg/pylibmagic
  repo: https://github.com/kratsg/pylibmagic
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: atheris
  version: 3.1.0
  home: https://github.com/google/atheris/
  repo: https://github.com/google/atheris/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ssh-python
  version: 1.2.0.post1
  home: https://github.com/ParallelSSH/ssh-python
  repo: https://github.com/ParallelSSH/ssh-python
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dockerfile
  version: 3.4.0
  home: https://github.com/asottile/dockerfile
  repo: https://github.com/asottile/dockerfile
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorrt-cu13-bindings
  version: 11.2.1.2
  home: https://github.com/nvidia/tensorrt
  repo: https://github.com/nvidia/tensorrt
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: adbc-driver-snowflake
  version: 1.11.0
  home: https://arrow.apache.org/adbc/
  repo: https://github.com/apache/arrow-adbc
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: connected-components-3d
  version: 4.1.0
  home: https://github.com/seung-lab/connected-components-3d/
  repo: https://github.com/seung-lab/connected-components-3d/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1450
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dimod
  version: 0.12.22
  home: https://github.com/dwavesystems/dimod
  repo: https://github.com/dwavesystems/dimod
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: doublemetaphone
  version: '1.2'
  home: https://github.com/dedupeio/doublemetaphone
  repo: https://github.com/dedupeio/doublemetaphone
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1447
  notes: '51 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39,pp37); no riscv64 on PyPI or pypi.riseproject.dev. Upstream is dedupeio/doublemetaphone. CI green (4/4 matrix jobs + publish dry-run).'
- pkg: gigatoken
  version: 0.10.0
  home: https://github.com/marcelroed/gigatoken
  repo: https://github.com/marcelroed/gigatoken
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gtsam
  version: 4.2.2
  home: https://gtsam.org/
  repo: https://gtsam.org/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: river
  version: 0.26.1
  home: https://riverml.xyz/
  repo: https://github.com/online-ml/river/
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: circt
  version: 1.157.0
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: apache-flink
  version: 2.3.0
  home: https://flink.apache.org
  repo: https://flink.apache.org
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cargo-zigbuild
  version: 0.23.4
  home: https://pypi.org/project/cargo-zigbuild/
  repo: https://pypi.org/project/cargo-zigbuild/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: iminuit
  version: 2.32.0
  home: https://scikit-hep.org/iminuit
  repo: http://github.com/scikit-hep/iminuit
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1771
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: img2table
  version: 2.0.0
  home: https://github.com/xavctn/img2table
  repo: https://github.com/xavctn/img2table
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libigl
  version: 2.6.3
  home: https://github.com/libigl/libigl-python-bindings
  repo: https://github.com/libigl/libigl-python-bindings
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1772
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: portforward
  version: 0.7.6
  home: https://portforward.readthedocs.io
  repo: https://github.com/pytogo/portforward.git
  status: queued
  pr: null
  notes: '104 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cryptg
  version: 0.6.0
  home: https://github.com/cher-nov/cryptg
  repo: https://github.com/cher-nov/cryptg
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1519
  notes: 'PR #1519 green (4 wheels built cp312-cp314t, publish dry-run OK), awaiting review. PyO3/Rust extension (grammers-crypto), setuptools-rust, build-from-checkout, per-interpreter (no abi3). No upstream test suite; CI smoke-tests the IGE round trip, factorize_pq_pair, and the staged grammers-crypto/pyo3 licences.'
- pkg: edlib
  version: 1.3.9.post1
  home: https://github.com/Martinsos/edlib
  repo: https://github.com/Martinsos/edlib
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1244
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython binding, per-interpreter C++ core (edlib.pyx cythonized in CIBW_BEFORE_BUILD; no pyproject.toml). First CI run failed on every job: CIBW_BEFORE_BUILD/TEST_COMMAND used a bare filename / {project}, but before-build''s cwd is {project} (the whole checkout) while package-dir is a subdir, so cython could not find edlib.pyx (gotcha 5) - fixed by prefixing with {package}. All 8 jobs (cp312/cp313/cp314/cp314t x manylinux/musllinux) passed on riscv64 CI after the fix; publish dry-run clean. PR #1244 open.'
- pkg: semantic-text-splitter
  version: 0.32.0
  home: https://github.com/benbrandt/text-splitter
  repo: https://github.com/benbrandt/text-splitter
  status: published
  pr: 1520
  notes: '15 Linux wheels upstream (abi: cp310-abi3,cp314-cp314t,cp315-abi3.abi3t); no riscv64 on PyPI or pypi.riseproject.dev. The cp315 entry is real, not stale: bindings/python/Cargo.toml carries a separate abi3t feature (pyo3/abi3t-py315) upstream builds with --no-default-features --features abi3t, targeting the free-threaded stable ABI that only exists from CPython 3.15 -- out of scope for this repo''s floor. The default abi3 feature (pyo3/abi3-py310) is on unconditionally, so one abi3 wheel covers cp310-cp314 and pyo3 disables abi3 under Py_GIL_DISABLED for a separate cp314t build; pyo3 0.29 compiles clean against cp314/cp314t already in upstream''s own CI, no gotcha-326-style ceiling. PR #1520 open, CI dispatched.'
- pkg: scikit-survival
  version: 0.28.0
  home: https://github.com/sebp/scikit-survival
  repo: https://github.com/sebp/scikit-survival
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: foundry-local-sdk
  version: 2.0.1
  home: https://github.com/microsoft/Foundry-Local
  repo: https://github.com/microsoft/Foundry-Local
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mdtraj
  version: 1.11.1.post2
  home: http://mdtraj.org
  repo: http://mdtraj.org
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastjet
  version: 3.5.1.4
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: ipex-llm
  version: 2.2.0
  home: https://github.com/intel-analytics/ipex-llm
  repo: https://github.com/intel-analytics/ipex-llm
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: syntaqlite
  version: 0.9.0
  home: https://github.com/LalitMaganti/syntaqlite
  repo: https://github.com/LalitMaganti/syntaqlite
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mdanalysis
  version: 2.10.0
  home: https://www.mdanalysis.org
  repo: https://github.com/mdanalysis/mdanalysis
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: phonopy
  version: 4.4.0
  home: https://phonopy.github.io/phonopy/
  repo: https://github.com/phonopy/phonopy
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: usd-exchange
  version: 3.0.0
  home: https://docs.omniverse.nvidia.com/usd/code-docs/usd-exchange-sdk
  repo: https://github.com/NVIDIA-Omniverse/usd-exchange
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sqlcipher3
  version: 0.6.2
  home: https://github.com/coleifer/sqlcipher3
  repo: https://github.com/coleifer/sqlcipher3
  status: queued
  pr: null
  notes: '35 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyreadr
  version: 0.5.6
  home: https://github.com/ofajardo/pyreadr
  repo: https://github.com/ofajardo/pyreadr
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: simplepyble
  version: 1.1.0
  home: https://github.com/simpleble/simpleble
  repo: https://github.com/simpleble/simpleble
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openslide-python
  version: 1.4.6
  home: https://openslide.org/
  repo: https://github.com/openslide/openslide-python
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: faster-eth-utils
  version: 5.3.29
  home: https://github.com/BobTheBuidler/faster-eth-utils
  repo: https://github.com/BobTheBuidler/faster-eth-utils
  status: queued
  pr: null
  notes: '60 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gseapy
  version: 1.3.1
  home: https://github.com/zqfang/gseapy
  repo: https://github.com/zqfang/GSEApy
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: no-defaults
  version: 2026.9.1
  home: https://github.com/adamtheturtle/no-defaults
  repo: https://github.com/adamtheturtle/no-defaults
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openmm
  version: 8.6.0
  home: https://openmm.org
  repo: https://openmm.org
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqtwebengine
  version: 5.15.7
  home: https://www.riverbankcomputing.com/software/pyqtwebengine/
  repo: https://www.riverbankcomputing.com/software/pyqtwebengine/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: toppra
  version: 0.6.10
  home: https://github.com/hungpham2511/toppra
  repo: https://github.com/hungpham2511/toppra
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zuban
  version: 0.9.3
  home: https://zubanls.com
  repo: https://github.com/zubanls/zubanls-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1769
  notes: '8 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyreqwest
  version: 0.12.4
  home: https://github.com/MarkusSintonen/pyreqwest
  repo: https://github.com/MarkusSintonen/pyreqwest
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqtwebengine-qt5
  version: 5.15.19
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tf-nightly-cpu
  version: 2.22.0.dev20260830
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastnumbers
  version: 5.2.0
  home: https://github.com/SethMMorton/fastnumbers
  repo: https://github.com/SethMMorton/fastnumbers
  status: published
  pr: 1242
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR opened, CI running.'
- pkg: tree-sitter-hcl
  version: 1.2.0
  home: https://github.com/tree-sitter-grammars/tree-sitter-hcl
  repo: https://github.com/tree-sitter-grammars/tree-sitter-hcl
  status: published
  pr: 1593
  notes: '3 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev. PR opened, CI running.'
- pkg: aggdraw
  version: 1.4.1
  home: https://github.com/pytroll/aggdraw
  repo: https://github.com/pytroll/aggdraw
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1449
  notes: '10 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dtw-python
  version: 1.7.5
  home: https://dynamictimewarping.github.io/
  repo: https://github.com/DynamicTimeWarping/dtw-python
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libscrc
  version: 1.8.1
  home: https://github.com/hex-in/libscrc
  repo: https://github.com/hex-in/libscrc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1240
  notes: 'PR #1240 opened; CI running. Pure-C CRC3-CRC82 checksum library, no build backend beyond setuptools, no abi3 (per-interpreter builds). Builds from the tagged upstream checkout rather than the PyPI sdist, which omits LICENSE (GPLv3+). Default cp312-cp314t matrix, manylinux + musllinux.'
- pkg: pyfftw
  version: 0.15.1
  home: https://github.com/pyFFTW/pyFFTW
  repo: https://github.com/pyFFTW/pyFFTW
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: speedict
  version: 0.3.12
  home: https://pypi.org/project/speedict/
  repo: https://pypi.org/project/speedict/
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bittensor-wallet
  version: 4.1.1
  home: https://github.com/opentensor/btwallet
  repo: https://github.com/opentensor/btwallet
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xpress
  version: 9.9.1
  home: https://www.fico.com/en/products/fico-xpress-optimization
  repo: https://www.fico.com/en/products/fico-xpress-optimization
  status: parked
  pr: null
  notes: 'Not feasible: xpress is FICO''s proprietary Xpress Optimizer, split at PyPI into a thin binding
    package (xpress, ~1.6 MB) and its actual engine shipped as a hard runtime dependency
    (requires_dist pins xpresslibs==9.9.1 exactly), so there is no source for the engine at any
    layer, no riscv64 build from the vendor, and FICO''s own Shrinkwrap License Agreement
    independently bars redistribution even if there were (the gurobipy/gotcha 453 shape -- per-
    arch closed math kernel -- same closed-payload family as gotchas 372/376/431/450, this time
    on a fourth vendor). Verified against the real artifacts, not inferred. PYPI: xpress 9.9.1
    publishes 24 wheels (cp310-cp314 plus a free-threaded cp314t leg, over
    macosx_11_0_arm64/manylinux1_x86_64/manylinux2014_aarch64/win_amd64) across 49 releases and
    1086 files with ZERO sdists ever (gotcha 431); its dependency xpresslibs is likewise 0
    sdists across 30 releases, and its 4 wheels-per-version are tagged
    py3-none-{macosx_14_0_arm64,manylinux1_x86_64,manylinux2014_aarch64,win_amd64} -- a
    fabricated py3-none-<platform> tag hiding real per-arch compiled binaries (gotcha
    27/81/145''s shape). home/repo/Project-URL for both are FICO''s marketing page
    https://www.fico.com/en/products/fico-xpress-optimization (gotcha 372''s hdbcli tell);
    xpress''s own METADATA carries no License: field at all, with the real terms only in the
    bundled LICENSE.txt. WHAT IS IN THE WHEELS:
    xpress-9.9.1-cp312-cp312-manylinux2014_aarch64.whl (1.6 MB) is genuinely thin -- one real
    extension, _xpress.cpython-312-aarch64-linux-gnu.so (2.9 MB uncompressed), plus Python glue
    and example .mps/.cnf/.opb data files; readelf shows it NEEDs libxprs.so.47 with RPATH /home
    /ec2-user/workspace/nux_aarch64_python_release-9.9.0/Optimizer/xprs.python/build_envs/py-
    3.12/lib:$ORIGIN/xpresslibs/lib -- a hard link into the sibling xpresslibs package (not a
    dlopen) and another closed-vendor-builder-path tell (gotcha 431), so with no riscv64
    xpresslibs wheel there is no importable xpress wheel at all (the gurobipy/saxonche shape,
    gotcha 450). The engine itself lives entirely in xpresslibs-9.9.1-py3-none-
    manylinux2014_aarch64.whl (20.8 MB, 19 files): libxprs.so.47 (21 MB, the LP/MIP/QP/MIQCQP
    simplex+barrier solver), libxpnll.so (12 MB, the nonlinear solver), libxknitro.so (12 MB,
    the bundled Knitro NLP solver), libxprscuda.so/libxprsws.so/libxprl.so.x9.9, plus vendored
    thirdparty/{libcrypto,libssl,libcurl,libwebsockets,libzip,libjansson} for its
    cloud/licensing features; no .c/.h/.pyx anywhere in either wheel. PER-ARCH CLOSED MATH
    KERNEL (gotcha 453 confirmed on a fourth vendor): the x86_64 build of libxknitro.so is 96 MB
    against 12 MB on aarch64 (8x), libxprs.so.47 is 25 MB against 21 MB, and the x86_64-only
    xpresslibs wheel additionally vendors thirdparty/libiomp5.so (Intel OpenMP) with
    libxprs.so.47 strings showing a real AVX dispatch table ("Using AVX2 support",
    icom_avx512_vectorlength, "Barrier used AVX2 support") that the stripped aarch64 build
    carries none of; the whole-wheel size gap is 50.6 MB (x86_64) vs 20.8 MB (aarch64) vs 16.1
    MB (macOS arm64). Neither libxprs.so.47 build contains a single riscv/rv64gc string (0 hits,
    both scanned in full). NO OPEN-SOURCE OR CPU-GENERIC ENGINE: FICO''s own fico-xpress GitHub
    org has 16 public repos, and the one that looks most promising by name -- mosel ("FICO
    Xpress Mosel Open Source Repository", Apache-2.0) -- is, by its own README, "extension
    libraries for the Mosel language, not the solver engine itself": its myqxprs/myxprs
    interface modules explicitly state "Requires Xpress Optimizer libraries", and its own build
    instructions say to go download "the free Xpress Community Edition" -- i.e. the same closed
    xpresslibs binary analyzed above -- to build against; every other repo in the org (python-
    notebooks, xpress-community, XpressAPI.jl, xpress-3rdparty-interfaces, insight-*, vdlx-
    datagrid, xpress-dockerfiles, ...) is a modeling-layer binding, notebook, or Insight web-app
    tool, never the LP/MIP engine (the gurobipy-pandas/gurobi-optimods shape, repeated a second
    time in this same triage session). THE LICENCE CLOSES A SECOND, INDEPENDENT DOOR, NAMING THE
    EXACT GATE THE QUEUE ENTRY FLAGGED: xpresslibs/bin/community-xpauth.xpr is FICO''s own
    Community Edition license file, plain-text and unencrypted in the wheel (license
    expiry="31-jul-2027" features="Community OptLib OptConsole ... Integer Threads=1024 ..."
    hostid="any" platform="any" sig=<hex>), and libxprs.so.47''s strings confirm the whole
    enforcement path (XPRSbeginlicensing/XPRSendlicensing/XPAUTH_PATH/"Xpress-NLP has not been
    licensed for use here"/"Knitro is not licensed to be run from Xpress"). Both xpress''s and
    xpresslibs'' dist-info/licenses/LICENSE.txt are FICO''s own "SHRINKWRAP LICENSE AGREEMENT
    FICO(R) Xpress Optimization Suite (On-Premises)" (a real bundled license file, not just a
    URL): it grants only a "non-exclusive, non-transferable, non-assignable, non-sublicenseable,
    limited license", separately bars the Licensee from making "derivative works" or attempting
    to "reverse engineer, decompile, disassemble" the product, and nowhere grants redistribution
    rights -- so publishing any xpress/xpresslibs wheel on pypi.riseproject.dev would not be
    permitted even if a riscv64 engine existed. The Community Edition''s
    "Threads=1024"/"static=uncounted" terms are a reduced-capability free SKU, not a
    differently-licensed one -- the same distinction as CPLEX''s Community Edition and MOSEK''s
    academic/trial licenses. Revisit only if FICO itself ships a riscv64 Xpress Optimizer (a
    manylinux*_riscv64 xpresslibs wheel on PyPI, or a Linux/RISC-V download on FICO''s own
    distribution channel) AND the Shrinkwrap Agreement is amended to permit redistribution;
    nothing in a build-xpress.yml can close either gap. No worktree/branch/PR created -- settled
    before any YAML was written.'
- pkg: bleak-esphome
  version: 4.1.0
  home: https://bleak-esphome.readthedocs.io
  repo: https://github.com/bluetooth-devices/bleak-esphome
  status: queued
  pr: null
  notes: '21 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: habachen
  version: 0.6.0
  home: https://github.com/Hizuru3/python-habachen
  repo: https://github.com/Hizuru3/python-habachen
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-pkcs11
  version: 0.9.5
  home: https://python-pkcs11.readthedocs.io/en/latest/
  repo: https://github.com/pyauth/python-pkcs11
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: unicode-segmentation-rs
  version: 0.3.3
  home: https://weblate.org/
  repo: https://github.com/WeblateOrg/unicode-segmentation-rs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1448
  notes: '30 Linux wheels upstream (abi: cp310,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyre-check
  version: 0.10.0
  home: https://pyre-check.org/
  repo: https://pyre-check.org/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hydraters
  version: 0.1.4
  home: https://github.com/developmentseed/hydraters
  repo: https://github.com/developmentseed/hydraters
  status: queued
  pr: null
  notes: '78 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-bip39-bindings
  version: 0.3.0
  home: https://github.com/polkascan/py-bip39-bindings
  repo: https://github.com/polkascan/py-bip39-bindings
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1239
  notes: '79 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1239 open, mirrors py-sr25519-bindings (#1223): sdist-to-bdist maturin build, per-interpreter (no abi3), rustup bootstrapped in-container, tested via upstream tests.py.'
- pkg: faster-eth-abi
  version: 5.2.31
  home: https://github.com/BobTheBuidler/faster-eth-abi
  repo: https://github.com/BobTheBuidler/faster-eth-abi
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kreuzberg
  version: 4.10.2
  home: https://kreuzberg.dev
  repo: https://github.com/kreuzberg-dev/kreuzberg-lts
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aspose-slides
  version: 26.7.0
  home: https://products.aspose.com/slides/python-net/
  repo: https://products.aspose.com/slides/python-net/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xpress9
  version: 0.3.8
  home: https://github.com/Hugoberry/xpress9-python
  repo: https://github.com/Hugoberry/xpress9-python
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cyksuid
  version: 2.1.0
  home: https://github.com/timonwong/cyksuid
  repo: https://github.com/timonwong/cyksuid
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1238
  notes: '27 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Cython/C++ KSUID binding, no runtime deps; build-from-checkout shape mirroring the wheel job of upstream wheels.yml. Cythonizes in-container via CYTHON=1 + --no-build-isolation since the tagged checkout ships no pre-generated .cpp and pyproject.toml omits Cython from build requirements. Worktree/branch cyksuid. PR #1238 open, watching CI.'
- pkg: ed25519-blake2b-fork
  version: 1.4.2
  home: https://github.com/ebellocchia/python-ed25519-blake2b
  repo: https://github.com/ebellocchia/python-ed25519-blake2b
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pynng
  version: 0.9.0
  home: https://github.com/codypiersall/pynng
  repo: https://github.com/codypiersall/pynng
  status: publishing
  pr: 1454
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-ed25519-zebra-bindings
  version: 1.3.0
  home: https://github.com/polkascan/py-ed25519-zebra-bindings
  repo: https://github.com/polkascan/py-ed25519-zebra-bindings
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1241
  notes: 'Merged (#1241) and published: 4 wheels (cp312, cp313, cp314, cp314t) under release tag py-ed25519-zebra-bindings-v1.3.0-20260907113315; pypi.riseproject.dev/simple/py-ed25519-zebra-bindings/ returns 200 and the downloaded cp313 wheel is a real riscv64 maturin build (ed25519_zebra.cpython-313-riscv64-linux-gnu.so, ELF e_machine 243) with licenses/LICENSE bundled. Issue #1313 linked, project status set. 1.3.0 is still the latest PyPI release, so docs/packages/py-ed25519-zebra-bindings.yaml has no pending version (pending_versions.py --check passes). Upstream has moved from polkascan/py-ed25519-zebra-bindings to JAMdotTech/py-ed25519, which build-py-ed25519-zebra-bindings.yml already checks out; musllinux dropped because rustup ships no riscv64 musl host toolchain.'
- pkg: texture2ddecoder
  version: 1.0.6
  home: https://github.com/K0lb3/texture2ddecoder
  repo: https://github.com/K0lb3/texture2ddecoder
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1594
  notes: '60 Linux wheels upstream (abi: cp310,cp311,cp38,cp39,pp310,pp311,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mkl-include
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: parked
  pr: null
  notes: 'HEADERS-ONLY GIVEN EXTRA SCRUTINY (per task instructions) SINCE HEADERS ARE
    ARCH-NEUTRAL IN PRINCIPLE -- VERDICT IS STILL PARK, CONFIRMED FROM THE REAL 2026.1.0
    WHEEL, not assumed from the mkl/mkl-static park. NO SOURCE, EVER: all 32 mkl-include
    releases back to 2018.0.0 have 0 sdists (checked the full PyPI JSON release history);
    every wheel ever published is manylinux1/manylinux_2_28_x86_64, win32/win_amd64 or
    macosx_x86_64 -- never any other architecture. WHEEL CONTENTS: mkl_include-2026.1.0
    -py2.py3-none-manylinux_2_28_x86_64.whl (1.3 MB compressed, 19 MB unpacked) ships C/C++
    headers (mkl.h, mkl_blas.h, mkl_lapack.h, mkl_cblas.h, mkl_lapacke.h, mkl_dfti.h,
    mkl_pardiso.h, mkl_sparse*.h, fftw compatibility headers) plus Fortran .f90/.fi includes
    and precompiled Fortran .mod binary module files (blas95.mod, lapack95.mod,
    f95_precision.mod, mkl_service.mod under intel64/{lp64,ilp64}/) -- all under
    mkl_include-2026.1.0.data/data/include/, zero .py files. NOT ACTUALLY ARCH-NEUTRAL: one
    of the shipped headers, mkl_direct_blas_kernels.h, contains inlined x86 AVX2 intrinsic
    code (`__m256i`, `_mm256_set_epi64x`) for MKL''s "direct call" small-matrix fast path --
    this is not a pure declaration header, part of it is x86-specific compiled-in code. THE
    STANDALONE-USE CASE THE TASK ASKS TO CHECK DOES NOT HOLD: a build-time-only header
    dependency is only useful if something can link against the library it declares: mkl.h
    and friends declare hundreds of extern C routines (dgemm_, dsyev_, etc.) that resolve
    only against libmkl_*.so/.a, and per the mkl-static/mkl park evidence those binaries have
    never existed for riscv64 at any oneMKL version -- oneMKL''s own system-requirements page
    lists only Intel Core/Xeon/Atom CPUs, oneMath''s (the open API-dispatch project, not an
    implementation) own README lists its mklcpu backend''s supported hardware as "x86 CPU" /
    "Intel Core/Xeon processors" with no riscv64 or ARM entry anywhere in its platform table,
    and apt.repos.intel.com/oneapi dists/all/Release says Architectures: all amd64 i386
    i686. So a riscv64 mkl-include wheel would install a headers tree that declares symbols
    no riscv64 library on earth implements -- pip install succeeds, but any code that
    `#include`s mkl.h and calls an MKL routine fails to link. This is unlike a genuine
    build-time-only header package (e.g. numpy''s C headers), where a working implementation
    exists to link against on every platform numpy itself ships; there is no such
    counterpart here on riscv64, so headers-only does not rescue this package. LICENCE:
    LICENSE.txt is the Intel Simplified Software License (Oct 2022), same as mkl-static/mkl
    -- "provided in binary form only ... without modification" and forbids "modification or
    alteration" -- Intel''s own licence frames even this text-header payload as unmodifiable
    binary-form redistribution. Requires-Dist: onemkl-license==2026.1.0, which 404s on our
    index (0-sdist Intel EULA-wrapper package, same shape as gotcha 40). NO CONSUMER: no
    build-*.yml in this repo references mkl-include (grepped every workflow); this repo''s
    MKL-adjacent packages already avoid MKL on riscv64 (build-ctranslate2.yml builds
    WITH_MKL=OFF, build-clarabel.yml swaps in pardiso-panua for the mkl-backed pardiso,
    build-scs.yml links OpenBLAS instead), and pypi.riseproject.dev 404s for mkl-include,
    mkl-static, mkl and onemkl-license. QUEUE VERSION IS CURRENT (gotcha 388): 2026.1.0 is
    still the latest release. Sibling mkl-static parked on matching evidence in the same
    commit. No worktree, branch or PR created.'
- pkg: vllm-cpu
  version: 0.28.0
  home: https://github.com/vllm-project/vllm
  repo: https://github.com/vllm-project/vllm
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: numpy-rms
  version: 0.7.0
  home: https://github.com/nomonosound/numpy-rms
  repo: https://github.com/nomonosound/numpy-rms
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1453
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: numpy-minmax
  version: 0.6.0
  home: https://github.com/nomonosound/numpy-minmax
  repo: https://github.com/nomonosound/numpy-minmax
  status: published
  pr: 1451
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymatching
  version: 2.4.0
  home: https://github.com/oscarhiggott/PyMatching
  repo: https://github.com/oscarhiggott/PyMatching
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-fcl
  version: 0.7.0.11
  home: https://github.com/berkeleyautomation/python-fcl
  repo: https://github.com/berkeleyautomation/python-fcl
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1770
  notes: 'CI green on PR #1770: cp312/cp313/cp314/cp314t riscv64 wheels built, 15 tests passed each, publish job dry-ran cleanly. Root cause of the riscv64-only build failure: setup.py''s get_libraries_dir() unconditionally splits os.environ["LD_LIBRARY_PATH"] whenever the key is merely present (our manylinux_riscv64 container exports it empty), producing a bare -L with no argument that swallows the following -lfcl as its path instead of a library flag -- libfcl was never linked, causing undefined symbol: typeinfo for fcl::CollisionGeometry<double> at import. Patched (0002-*.patch) to treat an empty value as unset. Awaiting maintainer review/merge; not yet published.'
- pkg: zttp
  version: 0.0.31
  home: https://github.com/Kludex/zttp
  repo: https://github.com/Kludex/zttp
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: emval
  version: 0.1.13
  home: https://github.com/bnkc/emval
  repo: https://github.com/bnkc/emval
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp314,cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intel-sycl-rt
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  status: parked
  pr: null
  notes: 'Not feasible (park, gotcha 506/528 pattern, verified on THIS package''s own wheel, not
    by family analogy per the explicit tbb counter-precedent). Downloaded the real
    intel_sycl_rt-2026.1.1-py2.py3-none-manylinux_2_28_x86_64.whl (134.1MB compressed, ~461MB
    unpacked, all under .data/data, empty top_level.txt, zero Python modules). METADATA hard-pins
    Requires-Dist: intel-cmplr-lib-rt(==2026.1.1), intel-cmplr-lic-rt(==2026.1.1),
    intel-cmplr-lib-ur(==2026.1.1) -- two of the three (intel-cmplr-lib-rt, intel-cmplr-lib-ur)
    are already parked in this queue on independent evidence (x86-only SVML-family runtime;
    GPU-only unified-runtime adapters with no riscv64 path, gotcha 528), so intel-sycl-rt cannot
    be installed on riscv64 regardless of its own payload. PAYLOAD BREAKDOWN: libsycl.so.9.0.0
    (11.7MB, ~2.5% of the payload) has a clean DT_NEEDED
    (libdl/libpthread/libstdc++/libm/libgcc_s/libc/ld-linux plus libur_loader.so.0) -- no
    SVML/libimf, consistent with intel/llvm''s own open sycl-runtime CMake target -- but that
    one extra NEEDED, libur_loader.so.0, only ships in the already-parked intel-cmplr-lib-ur
    wheel (GPU-only adapters, no riscv64 path), so even this cleanest component is inert on our
    arch. libsycl-jit.so is the actual biggest file in the wheel at 186MB (40% of the unpacked
    payload, bigger than libsycl.so itself) and DT_NEEDs
    libimf.so/libsvml.so/libirng.so/libintlc.so.5 -- Intel''s closed, x86-only compiler runtime,
    the same family already parked via intel-openmp (gotcha 506). Its own DWARF DW_AT_comp_dir
    strings read literally
    "/netbatch/donb73197_00/runDir/dir/workspace/NIT/xmain-rel/LX/xmainefi2linux_release/ws/icsws/builds/.../llvm/_deps/protobuf-src/..."
    -- Intel''s internal build farm and internal xmain-rel release branch, the exact tell gotcha
    528 used for intel-cmplr-lib-ur, not a checkout of the public github.com/intel/llvm repo.
    libpstloffload.so.1.0.0 also DT_NEEDs libsvml.so/libirng.so/libintlc.so.5/libimf.so. Every
    .so''s .comment section reads "Intel(R) oneAPI DPC++/C++ Compiler 2026.1.1" (Intel''s own
    closed, self-hosted toolchain). VERSION-TO-TAG CHECK (gotcha 506''s first tell): `git
    ls-remote --tags https://github.com/intel/llvm.git` lists 2494 tags -- the real public
    releases are workweek tags (2026-WW22, 2026-WW28), nightly-YYYY-MM-DD, and LLVM version tags
    (v7.1.1) -- there is no "2026.1.1" tag anywhere; that number is Intel''s internal oneAPI
    product/release-train version, not a project ref, so "build the open part instead" has no
    matching public source snapshot to build from. Unlike tbb (gotcha 263), whose PyPI version
    matched an open oneTBB tag exactly, this version maps to nothing public. CONCLUSION: not the
    same shape as intel-openmp/intel-cmplr-lib-ur (most of its own weight is not obviously
    closed), but functionally inert on riscv64 either way -- its cleanest component needs an
    already-parked GPU-only loader, its single biggest component is verified built off Intel''s
    internal branch with closed x86-only math-runtime deps, and two of its three hard
    Requires-Dist are packages this queue has already parked for riscv64 on independent evidence.
    No worktree, branch or PR created; .queue.yml only.'
- pkg: rtest
  version: 0.0.46
  home: https://github.com/hughhan1/rtest
  repo: https://github.com/hughhan1/rtest.git
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: etcpak
  version: 0.9.15
  home: https://github.com/K0lb3/etcpak
  repo: https://github.com/K0lb3/etcpak
  status: queued
  pr: null
  notes: '23 Linux wheels upstream (abi: cp37,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intel-cmplr-lic-rt
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/base-toolkit.html
  status: parked
  pr: https://github.com/riseproject-dev/python-wheels/pull/2195
  notes: 'PR #2195 was closed by the user without merging (mergeable_state clean, CI green,
    no review comment explaining why) - treating this as a deliberate reject of publishing
    the package rather than a technical blocker; do not re-attempt the same port without new
    instruction. Original triage, kept for reference: PORTABLE (do not lump in with sibling
    intel-cmplr-lib-rt just because of Intel branding). Confirmed via PyPI JSON API:
    only two files ever published, both bdist_wheel, no sdist
    (intel_cmplr_lic_rt-2026.1.1-py2.py3-none-manylinux_2_28_x86_64.whl and a win_amd64
    sibling) - so gotcha 27''s "an all-py3-none-* set with an -any fallback is not-feasible"
    escape hatch does not apply: riscv64 has no wheel and no sdist to fall back to, a real
    gap. Downloaded and unpacked the real manylinux_2_28_x86_64 wheel from PyPI: it is 18.9KB
    total and carries ZERO compiled code - no .so, no binary of any kind. The entire payload
    beyond dist-info/ is one file,
    <pkg>.data/data/licensing/compiler/"Intel Developer Tools EULA" (25224 bytes, plain text,
    byte-identical sha256 to dist-info/LICENSE.txt - it is the same EULA text staged twice).
    METADATA confirms this is by design: "This Intel oneAPI Runtime COMMON LICENSING package
    contains the Third Party program files and EULA needed to deploy executables... This
    COMMON LICENSING package should be downloaded and installed along with the COMMON
    LIBRARIES package and one or more of the Compiler-Specific runtimes" - i.e. it is a
    license-document carrier, not a runtime library, despite the "-rt" name and the
    manylinux_2_28_x86_64 platform tag it happens to ship under upstream (gotcha 27''s
    hand-set-tag shape: WHEEL confirms Root-Is-Purelib: true). The
    License :: Other/Proprietary classifier governs redistribution of the EULA *text*, not
    any code - there is no source-disclosure question because there is no compiled artifact
    to disclose source for. Intel''s build pipeline is internal/non-public, so
    build-intel-cmplr-lic-rt.yml does not reproduce it: it `pip download`s the real wheel on
    ubuntu-latest (gotcha 4, no riscv64 runner needed for an arch-independent artifact) and
    repackages it verbatim under manylinux_2_39_riscv64 with `python -m wheel tags --remove`
    (gotcha 398), then installs and verifies the EULA file on an actual riscv64 runner across
    the default interpreter matrix. docs/packages/intel-cmplr-lic-rt.yaml license: field
    carries METADATA''s own "Intel End User License Agreement for Developer Tools" string
    verbatim, matching how other proprietary/non-SPDX-license packages in this repo record
    their license (e.g. pymssql, matplotlib) rather than a misleading SPDX/OSI id. Validated
    locally: `wheel tags --platform-tag manylinux_2_39_riscv64 --remove` on the real wheel
    produces a correctly re-signed RECORD/WHEEL, and installing the (x86_64-tagged) original
    into a throwaway venv lands the EULA at <prefix>/licensing/compiler/ with no importable
    module (top_level.txt is empty) - exactly what the CI test step checks via
    importlib.metadata.files().'
- pkg: libsql
  version: 0.1.11
  home: https://pypi.org/project/libsql/
  repo: https://pypi.org/project/libsql/
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastecdsa
  version: 3.0.1
  home: https://github.com/AntonKueltz/fastecdsa
  repo: https://github.com/AntonKueltz/fastecdsa
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2109
  notes: 'PR #2109. 3.0.1 confirmed latest on PyPI; upstream ships manylinux x86_64/i686 + macOS only, no riscv64 anywhere (404 on our registry). Plain setup.py C extension: two modules (curvemath, _ecdsa) built from the bundled src/ curve math, dynamically linked against GMP, which is a system dependency (not vendored) -- gmp-devel 6.2.1-12.el10 is in Rocky 10''s riscv64 repos, so upstream''s `yum install -y gmp-devel` becomes a plain `dnf -y install`. No patch needed. auditwheel vendors libgmp into fastecdsa.libs/, and the wheel otherwise ships only fastecdsa''s own LICENSE, so before-all stages GMP''s four licence files at the project root as LICENSE.gmp.* (setuptools'' default PEP 639 glob still applies despite the legacy `license = {file = "LICENSE"}` table, verified on a real wheel, so gotcha 349 does not bite here) plus a gpl_sources job for gcc + gmp. pyproject.toml is deliberately not staged into the test cwd: its addopts demand pytest-cov and its `pythonpath = ["."]` would shadow the wheel. Validated locally under QEMU in quay.io/pypa/manylinux_2_39_riscv64: cp312 wheel built, repaired to manylinux_2_38/2_39_riscv64, 79/79 tests passed; `dnf download --source gcc gmp` also confirmed to resolve for the gpl_sources job.'
- pkg: json-tools-rs
  version: 0.9.30
  home: https://github.com/amaye15/JSON-Tools-rs
  repo: https://github.com/amaye15/JSON-Tools-rs
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/2107
  notes: 'PR #2107 open. pr-checks (check_commit_messages/check_patches/check_packages_yaml) and setup all green; the three riscv64 build_wheels jobs were still queued on the saturated self-hosted pool when the session ended, so they need watching in a later session -- but the build and the whole test suite are already validated locally under QEMU (below), so this is queue latency, not an unknown. Ordinary maturin/PyO3 port, no patches: build-from-checkout + cibuildwheel, rustup in CIBW_BEFORE_ALL_LINUX, musllinux dropped (no riscv64 musl toolchain on rustup.rs). [tool.maturin] features = ["python"], python-source = "python", module-name = "json_tools_rs" are all inherited through PEP 517, so no MATURIN_PEP517_ARGS; pyo3 0.29 carries no abi3 feature, so the build is per-interpreter, and Cargo.toml sets no [tool.maturin] profile, so maturin uses its own release profile (gotcha 141 does not bite). Matrix is cp312/cp313/cp314: cp314t is dropped because orjson -- a hard runtime dependency, imported lazily from src/python.rs via PyOnceLock -- publishes no free-threaded wheel on PyPI or pypi.riseproject.dev, so the test venv cannot be built (gotcha 84); everything else the suite touches (pandas/polars/pyarrow/pyspark) is behind importorskip/has_* guards and skips cleanly, matching upstream''s own manylinux test job of pytest + orjson only. CIBW_TEST_SOURCES stages pyproject.toml alongside python/tests because [tool.pytest.ini_options] adds tests.py to python_files -- without it pytest collects nothing. Feasibility settled before any CI cycle: sonic-rs 0.5.8 (64-bit-only dependency) has a portable src/util/arch/fallback.rs and sonic-simd 0.1.x has v128/v256/v512 fallbacks, so the x86/NEON gates are not a wall; the s390x job upstream disabled is a big-endian simd-json issue that does not apply. A full cross-compile of the 146-crate riscv64 graph with upstream''s real fat-LTO/codegen-units=1/opt-level=3 release profile (gotcha 182: --features python --features pyo3/extension-module, PYO3_CROSS_PYTHON_VERSION=3.12) linked a real riscv64 .so in 13m54s on a contended 4-core x86 host, so timeout-minutes: 360 is ample. A full QEMU rehearsal in quay.io/pypa/manylinux_2_39_riscv64 (gotcha 188 profile knockdown of lto/codegen-units only, opt-level kept at 3 because the suite has ops/sec assertions) then went green end to end: pip wheel produced json_tools_rs-0.9.30-cp312-cp312-linux_riscv64.whl in 263 min emulated, auditwheel retagged it manylinux_2_39_riscv64, the wheel layout matches PyPI''s x86 one exactly (json_tools_rs/json_tools_rs.cpython-312-riscv64-linux-gnu.so + __init__.py + .pyi + py.typed, LICENSE-MIT/LICENSE-APACHE and the cyclonedx SBOM under dist-info), orjson resolved from our registry, and upstream''s suite ran 215 passed / 129 skipped in 42s with pytest rootdir at the staged dir (so the pyproject.toml staging works and nothing shadows the wheel). The ops/sec assertions passed even at lto=off, so they are not marginal. License needs nothing: the published wheel already carries LICENSE-MIT and LICENSE-APACHE under dist-info/licenses, and nothing third-party is vendored. The PR briefly also carried ci_scripts/check_riscv64_deps.py (per-interpreter pip resolution against PyPI + our registry, which is what settled the matrix); that commit was reverted on the branch, so do not re-add it -- a port adds only .github/workflows/, docs/packages/ and patches/ files, and the matrix answer is already recorded here.'
- pkg: atomic-dict
  version: 0.5.0
  home: https://github.com/terpstra/atomic-dict
  repo: https://github.com/terpstra/atomic-dict
  status: published
  pr: 1595
  notes: '3 Linux wheels upstream (abi: cp310,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sentry-cli
  version: 3.7.0
  home: https://github.com/getsentry/sentry-cli
  repo: https://github.com/getsentry/sentry-cli
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyrage
  version: 1.4.0
  home: https://github.com/woodruffw/pyrage
  repo: https://github.com/woodruffw/pyrage
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1452
  notes: 'PR #1452 open. PyO3/maturin bindings for age/rage file encryption. pyo3''s abi3-py310 feature is unconditional, so cp310-abi3 covers cp310-cp314 and cp314t builds separately. No C dependencies (age/age-core/pyo3-file are pure Rust).'
- pkg: spacy-alignments
  version: 0.9.2
  home: https://github.com/explosion/spacy-alignments
  repo: https://github.com/explosion/spacy-alignments
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: floret
  version: 0.10.5
  home: https://github.com/explosion/floret
  repo: https://github.com/explosion/floret
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aiofastnet
  version: 1.1.0
  home: https://github.com/tarasko/aiofastnet
  repo: https://github.com/tarasko/aiofastnet
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: astc-encoder-py
  version: 0.1.12
  home: https://github.com/K0lb3/astc-encoder-py
  repo: https://github.com/K0lb3/astc-encoder-py
  status: published
  pr: 1776
  notes: '23 Linux wheels upstream (abi: cp37,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gdstk
  version: 1.0.1
  home: https://heitzmann.github.io/gdstk/
  repo: https://github.com/heitzmann/gdstk
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1773
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqt6-webengine-qt6
  version: 6.11.2
  home: https://www.riverbankcomputing.com/software/pyqt/
  repo: https://www.riverbankcomputing.com/software/pyqt/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pydemumble
  version: 0.1.3
  home: https://github.com/angr/pydemumble
  repo: https://github.com/angr/pydemumble
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chialisp
  version: 0.5.0
  home: https://pypi.org/project/chialisp/
  repo: https://pypi.org/project/chialisp/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyrealsense2
  version: 2.58.4.10922
  home: https://github.com/realsenseai/librealsense
  repo: https://github.com/realsenseai/librealsense
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: git-cliff
  version: 2.13.1
  home: https://github.com/orhun/git-cliff
  repo: https://github.com/orhun/git-cliff
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1596
  notes: 'PR #1596 green (build + cp312/cp313/cp314/cp314t tests + publish dry-run all pass), awaiting review. maturin bindings = "bin" build (mirrors ripgrep/typos), built from the pypi/ dir upstream already ships in the tag, manylinux 2_39 for riscv64.'
- pkg: common-expression-language
  version: 0.8.0
  home: https://github.com/hardbyte/python-common-expression-language
  repo: https://github.com/hardbyte/python-common-expression-language
  status: queued
  pr: null
  notes: '40 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dearpygui
  version: 2.3.1
  home: https://github.com/hoffstadt/DearPyGui
  repo: https://github.com/hoffstadt/DearPyGui
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: longbridge
  version: 4.5.0
  home: https://open.longbridge.com/en/
  repo: https://github.com/longbridge/openapi
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: reproject
  version: 0.21.0
  home: https://reproject.readthedocs.io
  repo: https://reproject.readthedocs.io
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: open-spiel
  version: 2.0.2
  home: https://github.com/deepmind/open_spiel
  repo: https://github.com/deepmind/open_spiel.git
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: depthai
  version: 3.10.0
  home: https://github.com/luxonis/depthai-core/tree/main/bindings/python
  repo: https://github.com/luxonis/depthai-core/tree/main/bindings/python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39.cp310.cp311.cp312.cp313.cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: browser-use-core
  version: 0.13.3
  home: https://pypi.org/project/browser-use-core/
  repo: https://pypi.org/project/browser-use-core/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pantab
  version: 5.3.0
  home: https://github.com/innobi/pantab
  repo: https://github.com/innobi/pantab
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: eppo-server-sdk
  version: 4.4.1
  home: https://github.com/Eppo-exp/rust-sdk/issues
  repo: https://github.com/Eppo-exp/rust-sdk/issues
  status: queued
  pr: null
  notes: '96 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: uefi-firmware
  version: '1.16'
  home: https://github.com/theopolis/uefi-firmware-parser
  repo: https://github.com/theopolis/uefi-firmware-parser
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mooncake-transfer-engine-cuda13
  version: 0.3.13.post1
  home: https://github.com/kvcache-ai/Mooncake
  repo: https://github.com/kvcache-ai/Mooncake
  status: parked
  pr: null
  notes: 'Parked: Mooncake''s CUDA 13 transfer-engine build is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: cyscale
  version: 0.8.0
  home: https://github.com/thewhaleking/cy-scale-codec
  repo: https://github.com/thewhaleking/cy-scale-codec
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: keystone-engine
  version: 0.9.2
  home: https://www.keystone-engine.org
  repo: https://github.com/keystone-engine/keystone
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1775
  notes: '2 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev. Pure ctypes wrapper around a vendored, self-contained LLVM 3.9 MC-layer/codegen fork (Arm, Arm64, Hexagon, Mips, PowerPC, Sparc, SystemZ, X86, EVM) built via CMake; no ext_modules and setup.cfg bdist_wheel universal=1 tag every wheel py2.py3-none-..., collapsing the matrix to a single cp312 build. musllinux dropped: setup.py hardcodes a manylinux1_ plat-name regardless of actual libc (fixed upstream in capstone, capstone#2445, but not here), and upstream itself never builds musllinux either. CI green on manylinux riscv64; test command exercises ks.asm() across every architecture keystone supports.'
- pkg: python-stretch
  version: 0.3.1
  home: https://github.com/gregogiudici/python-stretch
  repo: https://github.com/gregogiudici/python-stretch
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-flint
  version: 0.9.0
  home: https://github.com/flintlib/python-flint
  repo: https://github.com/flintlib/python-flint
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pypcode
  version: 4.0.0
  home: https://api.angr.io/projects/pypcode/en/latest/
  repo: https://github.com/angr/pypcode
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: beancount
  version: 3.2.3
  home: https://beancount.github.io/
  repo: https://github.com/beancount/beancount
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opendal
  version: 0.47.10
  home: https://opendal.apache.org/
  repo: https://github.com/apache/opendal
  status: ci-running
  pr: https://github.com/riseproject-dev/python-wheels/pull/2257
  notes: 'PyO3/maturin binding over the opendal Rust storage-access crate; bindings/python/Cargo.toml declares abi3 as an opt-in feature (["pyo3/abi3-py311"], not pyo3''s default), matching PyPI''s cp310 + cp311-abi3 + cp314-cp314t wheel matrix. Triaged the full services-all Cargo feature graph (the set upstream''s own release_python.yml builds with): default TLS is rustls+aws-lc-rs, and aws-lc-rs''s own platform-support table lists riscv64gc-unknown-linux-gnu with build+test CI coverage; every other services-all backend (mysql/postgresql/sqlite via sqlx, mongodb, hdfs-native, redb, etc.) is pure Rust or bundled C compiled through the cc crate -- no riscv64-blocking native dep found. foundationdb/etcd/tikv/rocksdb are already excluded from services-all upstream (need protoc or a preinstalled native lib), so nothing extra was dropped on our side beyond musllinux (no riscv64 musl Rust toolchain). No git tag exists for the 0.47.10 python release (bindings/python/Cargo.toml versions independently of the whole-repo vX.Y.Z tags, which stop at v0.47.3) -- pinned the commit HEAD carried when PyPI''s 0.47.10 was uploaded (2026-09-22), verified byte-identical to the sdist bar one unrelated docstring in python/opendal/config.py. Workflow builds cp310 and cp314t per-interpreter plus one cp311-abi3 wheel re-tested on cp312/cp313/cp314; tests run the real pytest suite against the in-process memory service (OPENDAL_TEST=memory), dropping only test_file_http_requests.py (spawns a multiprocessing HTTP server that cross-talks on a shared runner). PR #2257 open; setup job green and a riscv64 build job running at push time -- not waited out to completion.'
- pkg: aim
  version: 3.29.1
  home: https://pypi.org/project/aim/
  repo: https://pypi.org/project/aim/
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cql2
  version: 0.6.0
  home: https://github.com/developmentseed/cql2-rs
  repo: https://github.com/developmentseed/cql2-rs
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqt6-webengine
  version: 6.11.0
  home: https://www.riverbankcomputing.com/software/pyqtwebengine/
  repo: https://www.riverbankcomputing.com/software/pyqtwebengine/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zodbpickle
  version: '4.5'
  home: https://github.com/zopefoundation/zodbpickle
  repo: https://github.com/zopefoundation/zodbpickle
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lance-context
  version: 0.6.5
  home: https://pypi.org/project/lance-context/
  repo: https://pypi.org/project/lance-context/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: exactextract
  version: 0.3.0
  home: https://isciences.github.io/exactextract/
  repo: https://github.com/isciences/exactextract
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cvxpy-base
  version: 1.9.2
  home: https://github.com/cvxpy/cvxpy
  repo: https://github.com/cvxpy/cvxpy
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intel-opencl-rt
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/opencl-sdk/overview.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/opencl-sdk/overview.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-radix
  version: 1.1.0
  home: https://github.com/mjschultz/py-radix
  repo: https://github.com/mjschultz/py-radix
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1328
  notes: 'PR #1328 open, CI green (8/8 manylinux+musllinux jobs, publish dry-run OK). Small C extension (radix._radix) for IP prefix radix trees; no native deps. 28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spiceypy
  version: 8.2.0
  home: https://github.com/AndrewAnnex/SpiceyPy
  repo: https://github.com/AndrewAnnex/SpiceyPy
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: faiss-gpu-cu12
  version: 1.14.1.post1
  home: https://github.com/Di-Is/faiss-gpu-wheels
  repo: https://github.com/Di-Is/faiss-gpu-wheels
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: slixmpp
  version: 1.17.0
  home: https://pypi.org/project/slixmpp/
  repo: https://pypi.org/project/slixmpp/
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fmod-toolkit
  version: 0.1.3
  home: https://github.com/K0lb3/fmod_toolkit
  repo: https://github.com/K0lb3/fmod_toolkit
  status: parked
  pr: null
  notes: 'Not feasible: fmod_toolkit (K0lb3''s MIT-licensed community wrapper, not Firelight''s
    own repo) is a thin layer over pyfmodex (an open ctypes wrapper) that is only ever useful
    with Firelight Technologies'' proprietary FMOD Core Engine binary vendored alongside it --
    same wrapper-vs-engine split as gotcha 453''s gurobipy-pandas/gurobipy. Checked the actual
    sdist and wheel contents (fmod_toolkit-0.1.3.tar.gz and the cp38-abi3 wheel from PyPI):
    the sdist itself hard-vendors prebuilt libfmod.{so,dylib}/fmod.dll binaries at
    fmod_toolkit/libfmod/{Darwin,Linux/{x86,x86_64,arm,arm64},Windows/{x86,x64,arm}}/, 8
    files total, 1.0-2.4 MB each, checked into the repo/sdist directly (setup.py''s custom
    SDist.make_distribution walks that tree and force-includes every .so/.dylib/.dll) --
    not fetched from a URL, so there is no separate download step to swap a riscv64 build into.
    strings on the Linux x86_64 one confirms it is the genuine Firelight engine, not a
    reimplementation: FMOD_System_GetVersion, FMOD5_System_GetVersion,
    _ZN4FMOD6System10getVersionEPj (System::getVersion). The upstream README says so outright:
    the libraries shipped with the package are FMOD Engine 2.00.10, and that using a
    different FMOD library via the PYFMODEX_DLL_PATH env var is necessary on platforms this
    package does not ship FMOD libraries for -- i.e. upstream''s own escape hatch for an
    unsupported arch is go get a FMOD Engine build for it yourself, and Firelight ships FMOD
    Engine only for Windows (x86/x64/arm), Linux (x86/x86_64/arm/arm64) and macOS (universal)
    -- no riscv64 anywhere in Firelight''s own platform matrix, confirmed against the
    exhaustive vendored-binary list above, which is the same architecture set the README
    documents. FMOD Engine has no public source release at any version (Firelight distributes
    it only as signed prebuilt SDKs from fmod.com, gated behind creating a free FMOD account
    and accepting Firelight''s EULA before any download starts) -- there is no from-scratch
    reimplementation of the engine to build against, only pyfmodex''s open ctypes bindings to
    it, which is the wrapper, never the codec/mixer itself. This is also a hard, not optional,
    dependency: fmod_toolkit/__init__.py imports .fmod unconditionally, which calls
    importer.import_pyfmodex() at module-import time; that raises ValueError/
    NotImplementedError immediately for any platform.machine() not in
    {x64,x86,arm,arm64} unless PYFMODEX_DLL_PATH already points at a working FMOD .so for
    that platform (which does not exist for riscv64) -- so unlike the dlopen-optional cases
    elsewhere in this repo, the package cannot even be imported in a degraded/no-op mode
    without the closed binary, let alone perform its one job (FSB/bank extraction via the
    FMOD System/Sound API). Parking rather than a from-scratch riscv64 reimplementation of a
    proprietary audio engine, which is out of scope for this repo.'
- pkg: nassl
  version: 5.4.0
  home: https://github.com/nabla-c0d3/nassl
  repo: https://github.com/nabla-c0d3/nassl
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scikit-learn-extra
  version: 0.3.0
  home: https://github.com/scikit-learn-contrib/scikit-learn-extra
  repo: https://github.com/scikit-learn-contrib/scikit-learn-extra
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dpcpp-cpp-rt
  version: 2026.1.1
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/dpc-compiler.html
  status: parked
  pr: null
  notes: 'Not feasible (park). Downloaded the real
    dpcpp_cpp_rt-2026.1.1-py2.py3-none-manylinux_2_28_x86_64.whl -- only 28KB, and despite the
    "cpp-rt" name it ships ZERO compiled code: empty top_level.txt, no .so/.a/.o anywhere. The
    entire payload beyond dist-info/ is
    .data/data/share/doc/compiler/{credist.txt,licensing/c/LICENSE,licensing/c/third-party-programs.txt}
    -- docs plus the same EULA text also carried in dist-info/LICENSE.txt (the same
    duplicate-staging pattern already found for intel-cmplr-lic-rt). It is a pure metapackage:
    METADATA hard-pins Requires-Dist: intel-opencl-rt(==2026.1.1), intel-openmp(==2026.1.1),
    intel-sycl-rt(==2026.1.1), and its own description ("contains the compiler-specific shared
    libraries needed to deploy executables") is delivered entirely through those three deps --
    every actual byte lives downstream. intel-openmp is already parked in this queue (x86_64-only
    closed Intel OpenMP blob, gotcha 506) and intel-sycl-rt is parked in this same pass (see that
    entry: two of its three hard deps are themselves already-parked closed x86-only packages, and
    its single biggest component, libsycl-jit.so at 186MB, is verified built from Intel''s
    internal xmain-rel branch with closed x86-only math-runtime deps via its own DWARF
    DW_AT_comp_dir strings). intel-opencl-rt (separately queued, untouched by this pass) is
    Intel''s own OpenCL ICD/runtime for Intel GPUs, same oneAPI family, no evidence of any
    non-x86 distribution anywhere. Since dpcpp-cpp-rt carries no payload of its own and
    unconditionally requires packages that cannot be satisfied on riscv64, it cannot be installed
    as intended on riscv64 regardless of any single dependency''s own fate. No worktree, branch
    or PR created; .queue.yml only.'
- pkg: passagemath-homfly
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: graspologic-native
  version: 1.3.1
  home: https://github.com/graspologic-org/graspologic-native
  repo: https://github.com/graspologic-org/graspologic-native
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jenkspy
  version: 0.4.1
  home: https://github.com/mthh/jenkspy
  repo: https://github.com/mthh/jenkspy.git
  status: published
  pr: 1326
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pillow-jxl-plugin
  version: 1.3.8
  home: https://github.com/Isotr0py/pillow-jpegxl-plugin
  repo: https://github.com/Isotr0py/pillow-jpegxl-plugin
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ignore-python
  version: 0.4.1
  home: https://borsattoz.github.io/ignore-python
  repo: https://github.com/borsattoz/ignore-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/2196
  notes: 'maturin/PyO3 binding over the Rust `ignore` crate. pyo3 = "0.29.0" with no abi3 feature (verified in Cargo.toml), so the wide upstream matrix (cp39-cp315/cp315t, pp311) is genuine per-interpreter, not abi3 collapse; cp315 confirmed real on PyPI, not a queue-data artifact. Dependency tree (ignore, aho-corasick, walkdir, globset, same-file, regex-automata, crossbeam-*) is pure Rust with no links= entries; cargo check --target riscv64gc-unknown-linux-gnu (with PYO3_CROSS_PYTHON_VERSION set) type-checks clean, and a local x86 maturin build passed all 7 upstream pytest tests. Matrix: cp39/cp310/cp311/cp312/cp313/cp314/cp314t/cp315/cp315t; pp311 dropped (PyPy has no riscv64 port). PR #2196 open.'
- pkg: qdk
  version: 1.31.0
  home: https://github.com/microsoft/qdk
  repo: https://github.com/microsoft/qdk
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: treelite-runtime
  version: 3.9.1
  home: https://github.com/dmlc/treelite
  repo: https://github.com/dmlc/treelite
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deno
  version: 2.9.6
  home: https://deno.com
  repo: https://deno.com
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arrow-odbc
  version: 10.4.2
  home: https://github.com/pacman82/arrow-odbc-py
  repo: https://github.com/pacman82/arrow-odbc-py
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: faiss-gpu
  version: 1.15.0
  home: https://github.com/facebookresearch/faiss
  repo: https://github.com/facebookresearch/faiss
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tmtools
  version: 0.3.0
  home: https://github.com/jvkersch/tmtools
  repo: https://github.com/jvkersch/tmtools
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyrnnoise
  version: 0.4.3
  home: https://github.com/pengzhendong/pyrnnoise
  repo: https://github.com/pengzhendong/pyrnnoise
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pybigwig
  version: 0.3.25
  home: https://github.com/deeptools/pyBigWig
  repo: https://github.com/deeptools/pyBigWig
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1597
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1597 open, CI dispatched.'
- pkg: ruff-format
  version: 0.5.4
  home: https://github.com/reflex-dev/ruff-format
  repo: https://github.com/reflex-dev/ruff-format
  status: ci-green
  pr: https://github.com/riseproject-dev/python-wheels/pull/2198
  notes: '20 Linux wheels upstream (abi: cp314,cp38); no riscv64 on PyPI or pypi.riseproject.dev. FEASIBLE and ported: a PyO3/maturin extension (pyo3 abi3-py38 unconditional in Cargo.toml) that vendors ruff_python_parser/ruff_python_formatter from astral-sh/ruff.git as Cargo git dependencies -- it has no runtime Python dependency on the `ruff` PyPI package, so the initial concern about ruff''s own riscv64 status was a non-issue (same shape as complexipy/chalkpy-rs, already ported). Local cargo check --locked --target riscv64gc-unknown-linux-gnu passes clean. Matrix: cp38-abi3 (built on cp39, image floor) + cp314t. First CI run only produced a cp314t job and failed there: the include-only tag/build keys collapsed the matrix onto one combination (gotcha 402 shape) and the smoke test probed the maturin package shim''s __file__ instead of the compiled submodule (gotcha 308 shape, generalized in new gotcha 559 to apply unconditionally to any pure-Rust maturin project, not only on a name collision). Fixed both: tag: [cp38-abi3, cp314t] added as a real matrix dimension, and the probe rewritten as sys.modules[''ruff_format.ruff_format''].__file__. Run 35941432233 green on both build legs plus the publish dry run. PR #2198 ready for review.'
- pkg: arro3-io
  version: 0.8.2
  home: https://kylebarron.dev/arro3
  repo: https://kylebarron.dev/arro3
  status: queued
  pr: null
  notes: '50 Linux wheels upstream (abi: cp310,cp311,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spookyhash
  version: 2.1.1
  home: https://github.com/buhanec/spookyhash
  repo: https://github.com/buhanec/spookyhash
  status: published
  pr: 1325
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tfx-bsl
  version: 1.21.0
  home: https://www.tensorflow.org/tfx
  repo: https://www.tensorflow.org/tfx
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sqruff
  version: 0.40.0
  home: https://github.com/quarylabs/sqruff
  repo: https://github.com/quarylabs/sqruff
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1598
  notes: '18 Linux wheels upstream (abi: cp314,cp38); no riscv64 on PyPI or pypi.riseproject.dev. Maturin pyo3 extension-module (crates/cli-python), abi3-py38 unconditional feature; cp38-abi3 built on cp39 (riscv64 image floor) plus a separate cp314t free-threaded build. PR opened, CI running.'
- pkg: pylerc
  version: 4.2.0
  home: https://github.com/Esri/lerc
  repo: https://github.com/Esri/lerc
  status: published
  pr: 1458
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clingo
  version: 5.8.2
  home: https://github.com/potassco/clingo
  repo: https://github.com/potassco/clingo
  status: published
  pr: 2199
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev. Portable C++17 CMake/scikit-build project (clasp/gringo/libclingo) with a CFFI-generated extension; no SIMD/arch-specific code, and upstream already builds aarch64/ppc64le wheels via cibuildwheel. bison/re2c are not needed: CMake falls back to the pre-generated parser/lexer sources checked into libgringo/gen when they are absent. Two-file port (build-clingo.yml + docs/packages/clingo.yaml), no patches. Validated locally on x86_64 before pushing: pip wheel build succeeded and the full upstream libpyclingo/clingo/tests suite (72 tests) passed against the installed wheel.'
- pkg: clickhouse-toolset
  version: 0.37.dev0
  home: https://gitlab.com/tinybird/clickhouse-toolset
  repo: https://gitlab.com/tinybird/clickhouse-toolset
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310,cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: polars-ols
  version: 0.3.5
  home: https://pypi.org/project/polars-ols/
  repo: https://pypi.org/project/polars-ols/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onyx-devtools
  version: 0.13.0
  home: https://github.com/onyx-dot-app/onyx
  repo: https://github.com/onyx-dot-app/onyx
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libsql-experimental
  version: 0.0.55
  home: https://pypi.org/project/libsql-experimental/
  repo: https://pypi.org/project/libsql-experimental/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cyvcf2
  version: 0.34.0
  home: https://github.com/brentp/cyvcf2/
  repo: https://github.com/brentp/cyvcf2/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1774
  notes: '23 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PR #1774 open, CI running (cp312/cp313/cp314/cp314t manylinux_riscv64).'
- pkg: power-grid-model
  version: 1.13.158
  home: https://lfenergy.org/projects/power-grid-model/
  repo: https://github.com/PowerGridModel/power-grid-model
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mkl-static
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: parked
  pr: null
  notes: 'NOT FEASIBLE, CONFIRMED FROM THE REAL 2026.1.0 WHEEL (not assumed from the mkl
    park): NO SOURCE, EVER: all 18 mkl-static releases back to 2018.0.0 have 0 sdists
    (checked full PyPI JSON release history); every wheel ever published is
    manylinux1/manylinux_2_28_x86_64, win32/win_amd64 or macosx_x86_64 -- never any other
    architecture. WHEEL CONTENTS ARE REAL COMPILED KERNELS, NOT STUBS:
    mkl_static-2026.1.0-py2.py3-none-manylinux_2_28_x86_64.whl (247 MB compressed, 897 MB
    unpacked) contains only 19 `.a` static archives under
    mkl_static-2026.1.0.data/data/lib/ (libmkl_core.a alone is 546 MB, plus
    libmkl_{intel,gnu,tbb}_thread.a, libmkl_sequential.a, libmkl_{intel,gf}_{lp64,ilp64}.a,
    libmkl_{blas95,lapack95,scalapack}_{lp64,ilp64}.a, libmkl_blacs_{intelmpi,openmpi}_
    {lp64,ilp64}.a, libmkl_cdft_core.a) plus dist-info; top_level.txt is an empty file, 0
    Python modules. `ar t libmkl_core.a` / `libmkl_intel_thread.a` lists real BLAS/LAPACK
    kernel object files by name (dgemm, sgemm, dtrsm, zgemm, sparse SpDOTMV, etc.) with
    per-microarch ISA-tuned variants baked into the member/symbol names (`_bdz`, `_zen`
    kernel suffixes; symbols like `mkl_blas_avx512_xzgemm_*`, `.L__const.mkl_blas_avx512_*`
    throughout libmkl_core.a). Extracted a sample .o from libmkl_core.a: `file` reports
    "ELF 64-bit LSB relocatable, x86-64" (not stripped). `grep -ria riscv` across all 19 .a
    files: 0 hits -- unlike gotcha 465''s inverted tell (bundled LLVM tables), MKL has
    nothing riscv-shaped at all, matching the sibling mkl park''s libmkl_rt.so finding.
    ONEMKL HAS NEVER HAD PUBLIC KERNEL SOURCE, AT ANY VERSION, ANYWHERE ON GITHUB: checked
    both the oneapi-src and uxlfoundation orgs directly -- the only oneMKL-named open repo
    is oneMKL-interfaces, since renamed/moved to uxlfoundation/oneMath. Its own README
    describes it as an API-dispatch wrapper implementing the oneMath *specification*
    ("demonstrate how the SYCL interfaces documented in the oneMath specification can be
    implemented for any math library and work for any target hardware"), and its mklcpu
    backend section states it "is used for the Intel backends of oneMath... highly
    optimized for Intel CPU and Intel GPU hardware" while listing Intel(R) oneMKL itself as
    the required closed third-party dependency that backend links against -- i.e. oneMath
    dispatches into the exact same closed libmkl_core.a computational core inspected above,
    it does not reimplement it, and its own supported-hardware table lists only "x86 CPU
    (mklcpu)" / "Intel Core/Xeon processors" with no riscv64 or ARM entry at all. Intel''s
    own oneMKL system-requirements page and apt.repos.intel.com/oneapi (Architectures: all
    amd64 i386 i686) agree: oneMKL ships for no non-x86 architecture, and never has, back to
    the 2018.0.0 PyPI history checked above. LICENCE: LICENSE.txt is the Intel Simplified
    Software License (Oct 2022) -- "provided in binary form only ... without modification,"
    forbids "reverse engineering, decompilation, or disassembly ... nor any modification or
    alteration" -- so the only artifact this licence permits redistributing is the x86_64
    blob itself; producing a riscv64 build is precisely the modification the licence
    prohibits, and there is no riscv64 upstream binary to redistribute unmodified in the
    first place. NOT THE TBB CASE (gotcha 506/528, per package not per vendor): tbb''s PyPI
    2023.1.0 matches the Apache-2.0 oneTBB v2023.1.0 tag exactly and this repo does build it
    from that public source (build-tbb.yml); mkl-static''s 2026.1.0 is a oneAPI release-
    train number with no corresponding open tag anywhere, and unlike tbb there is no open
    project hiding inside this wheel to rebuild -- every kernel in libmkl_core.a traces back
    to the same closed oneMKL binary oneMath dispatches to, confirmed above, not to any
    Apache-2.0 or otherwise open source tree. Requires-Dist: mkl-include==2026.1.0 (parked
    alongside this entry, same evidence), tbb-devel==2023.* (only tbb==2023.* itself
    resolves on our index), intel-openmp>=2025,<2027 (already parked, x86_64/win_amd64-only,
    0-sdist). NO CONSUMER: pypi.riseproject.dev 404s for mkl-static, mkl-include, mkl,
    intel-openmp and onemkl-license; no build-*.yml in this repo references mkl-static
    (grepped every workflow), and this repo''s MKL-adjacent packages already take the non-
    Intel path on riscv64 (build-ctranslate2.yml builds WITH_MKL=OFF with
    OPENMP_RUNTIME=COMP, build-clarabel.yml swaps in pardiso-panua for the mkl-backed
    pardiso, build-scs.yml links OpenBLAS). QUEUE VERSION IS CURRENT (gotcha 388): 2026.1.0
    is still the latest release. Sibling mkl-include parked on matching evidence in the same
    commit; both join the already-parked mkl and intel-cmplr-lib-{ur,rt}/intel-cmplr-lic-rt/
    intel-sycl-rt/intel-opencl-rt/dpcpp-cpp-rt family. No worktree, branch or PR created.'
- pkg: torchtext
  version: 0.18.0
  home: https://github.com/pytorch/text
  repo: https://github.com/pytorch/text
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tbb-devel
  version: 2023.1.0
  home: https://github.com/oneapi-src/oneTBB
  repo: https://github.com/oneapi-src/oneTBB
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: voyager
  version: 2.1.0
  home: https://github.com/spotify/voyager
  repo: https://github.com/spotify/voyager
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyxdia
  version: 0.1.1
  home: https://github.com/mborgerson/xdia
  repo: https://github.com/mborgerson/xdia
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastremap
  version: 1.20.0
  home: https://github.com/seung-lab/fastremap/
  repo: https://github.com/seung-lab/fastremap/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1455
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: regopy
  version: 1.5.2
  home: https://microsoft.github.io/rego-cpp/
  repo: https://github.com/microsoft/rego-cpp
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyjpt
  version: 1.3.4
  home: https://pypi.org/project/pyjpt/
  repo: https://pypi.org/project/pyjpt/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nominal-streaming
  version: 0.9.7
  home: https://github.com/nominal-io/nominal-streaming
  repo: https://github.com/nominal-io/nominal-streaming
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymongoarrow
  version: 1.15.0
  home: https://www.mongodb.org
  repo: https://github.com/mongodb-labs/mongo-arrow/tree/main/bindings/python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1777
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. PR #1777 green: cp312/cp313/cp314/cp314t riscv64 wheels built, publish job dry-ran cleanly (not on main, no upload). cp310/cp311 dropped from the matrix - our registry has no riscv64 pyarrow/numpy/pymongo/cython for them (pyproject.toml pins pyarrow>=25.0,<25.1). Awaiting maintainer review/merge; not yet published.'
- pkg: openvino-tokenizers
  version: 2026.3.1.0
  home: https://pypi.org/project/openvino-tokenizers/
  repo: https://pypi.org/project/openvino-tokenizers/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: anki
  version: 26.8.1
  home: https://apps.ankiweb.net/
  repo: https://github.com/ankitects/anki
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: janitor-rs
  version: 0.6.1
  home: https://pypi.org/project/janitor-rs/
  repo: https://pypi.org/project/janitor-rs/
  status: queued
  pr: null
  notes: '96 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pykdtree
  version: 1.4.3
  home: https://github.com/storpipfugl/pykdtree
  repo: https://github.com/storpipfugl/pykdtree
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1327
  notes: 'PR #1327 open, CI running. Cython/C KD-tree extension with OpenMP-parallel queries; links -lgomp on Linux by default, so a gpl_sources job collects the build image''s gcc sources. numpy is a build-time (and implicit runtime) dependency, pulled from our registry via PIP_ONLY_BINARY. 21 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: protoc-gen-connect-python
  version: 0.9.0
  home: https://github.com/connectrpc/connect-python
  repo: https://github.com/connectrpc/connect-python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk-core
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: attoworld
  version: 2026.2.8
  home: https://github.com/NickKarpowicz/Attoworld
  repo: https://github.com/NickKarpowicz/Attoworld
  status: queued
  pr: null
  notes: '92 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: questdb
  version: 5.0.0
  home: https://questdb.com/
  repo: https://github.com/questdb/py-questdb-client/
  status: queued
  pr: null
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: klujax
  version: 0.5.0
  home: https://pypi.org/project/klujax/
  repo: https://pypi.org/project/klujax/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: segyio
  version: 1.9.14
  home: https://github.com/equinor/segyio
  repo: https://github.com/equinor/segyio
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1456
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp314t); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyopencl
  version: 2026.1.4
  home: https://mathema.tician.de/software/pyopencl
  repo: https://github.com/inducer/pyopencl
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk-numerics
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: panda3d
  version: 1.10.16
  home: https://www.panda3d.org/
  repo: https://github.com/panda3d/panda3d
  status: queued
  pr: null
  notes: '38 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp314,cp34,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sysv-ipc
  version: 1.2.0
  home: https://github.com/osvenskan/sysv_ipc/
  repo: https://github.com/osvenskan/sysv_ipc/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1599
  notes: '34 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. Single C extension (semaphores, shared memory, message queues), no runtime deps; build-from-checkout cibuildwheel workflow mirroring upstream build-wheels.yaml, tag rel1.2.0. Same author/shape as already-published posix-ipc. manylinux + musllinux riscv64 both included (gotcha 221). PR #1599 CI green: all 8 matrix jobs (cp312/cp313/cp314/cp314t x manylinux/musllinux) passed, publish dry-ran clean. Awaiting review/merge.'
- pkg: itk-filtering
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onemkl-sycl-blas
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onemkl-sycl-rng
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cbcbox
  version: '2.935'
  home: https://github.com/h-g-s/cbcbox
  repo: https://github.com/h-g-s/cbcbox
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pnnx
  version: '20260526'
  home: https://github.com/Tencent/ncnn/tree/master/tools/pnnx
  repo: https://github.com/Tencent/ncnn/tree/master/tools/pnnx
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vortex-data
  version: 0.85.0
  home: https://github.com/spiraldb/vortex
  repo: https://github.com/spiraldb/vortex
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onemkl-sycl-lapack
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk-io
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onemkl-sycl-dft
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pg0-embedded
  version: 0.15.1
  home: https://github.com/vectorize-io/pg0
  repo: https://github.com/vectorize-io/pg0
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bittensor-drand
  version: 2.0.0
  home: https://github.com/opentensor/bittensor-drand
  repo: https://github.com/opentensor/bittensor-drand
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: accesscontrol
  version: '7.5'
  home: https://github.com/zopefoundation/AccessControl
  repo: https://github.com/zopefoundation/AccessControl
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zope-security
  version: '8.4'
  home: https://zopesecurity.readthedocs.io
  repo: https://github.com/zopefoundation/zope.security
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1334
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Security proxy/checker C extension (optional_build_ext, same shape as zope-hookable/zope-proxy); cibuildwheel build-from-checkout, matrix cp312/cp313/cp314/cp314t to match zope.interface registry availability. PIP_EXTRA_INDEX_URL set for cffi/zope.interface (registry-only on riscv64); zope.proxy and BTrees (test-only) build from sdist in-container since not yet on the registry. Tests via zope.testrunner with/without the C extension (PURE_PYTHON gate), matching upstream. PR #1334 open, watching CI.'
- pkg: zlib-state
  version: 0.1.12
  home: https://github.com/seanmacavaney/zlib-state
  repo: https://github.com/seanmacavaney/zlib-state
  status: published
  pr: 1600
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intel-pti
  version: 1.1.0
  home: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  repo: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: torchft-nightly
  version: 2026.9.3
  home: https://docs.pytorch.org/torchft
  repo: https://github.com/pytorch/torchft
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: oboros
  version: 0.8.3
  home: https://pypi.org/project/oboros/
  repo: https://pypi.org/project/oboros/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cbitstruct
  version: 1.2.0
  home: https://github.com/qchateau/cbitstruct
  repo: https://github.com/qchateau/cbitstruct
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mooncake-transfer-engine
  version: 0.3.13.post1
  home: https://github.com/kvcache-ai/Mooncake
  repo: https://github.com/kvcache-ai/Mooncake
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: theine-core
  version: 2.1.0
  home: https://pypi.org/project/theine_core/
  repo: https://pypi.org/project/theine_core/
  status: queued
  pr: null
  notes: '80 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tinyobjloader
  version: 2.0.0rc13
  home: https://github.com/tinyobjloader/tinyobjloader
  repo: https://github.com/tinyobjloader/tinyobjloader
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1457
  notes: 'PyPI default install is the 2019 0.1/cp36 build, but the repo is actively maintained (pushes into 2026) and 2.0.0rc13 (Jan 2024) is its real current cibuildwheel-built matrix, just never promoted out of pre-release. Builds cp312/cp313/cp314/cp314t manylinux_riscv64 only: numpy (test-only dependency) has no musllinux riscv64 wheel on our registry. Upstream runs no test at this tag, so CI uses its own python/sample.py smoke script instead.'
- pkg: onemkl-sycl-sparse
  version: 2026.1.0
  home: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  repo: https://www.intel.com/content/www/us/en/developer/tools/oneapi/onemkl.html
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorlake
  version: 0.5.121
  home: https://github.com/tensorlakeai/tensorlake
  repo: https://github.com/tensorlakeai/tensorlake
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pdf-oxide
  version: 0.3.77
  home: https://github.com/yfedoseev/pdf_oxide
  repo: https://github.com/yfedoseev/pdf_oxide
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: just-bin
  version: 1.58.0
  home: https://github.com/casey/just
  repo: https://github.com/casey/just
  status: published
  pr: 1602
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PyPI''s just-bin repackages casey/just''s own binary releases via justin-yan/pybin, whose Architecture enum only covers x86_64/arm64 - no riscv64 support at all, not just unconfigured. Home/repo corrected from pybin to casey/just (the actual wrapped project) accordingly. Built from source instead: maturin bindings=bin wheel wrapping the `just` binary, same shape as build-ripgrep.yml. Synthesized pyproject.toml (casey/just ships none). Targets riscv64gc-unknown-linux-gnu/manylinux_2_39, not upstream''s own musl cross-compile - rustup ships no riscv64 musl toolchain. Local `cargo build --release --locked` from the pristine 1.58.0 tag succeeds with no patches needed (pure-Rust deps, no C/asm). PR #1602 opened, CI running.'
- pkg: qiskit-terra
  version: 0.46.3
  home: https://www.ibm.com/quantum/qiskit
  repo: https://github.com/Qiskit/qiskit
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pytetwild
  version: 0.4.2
  home: https://pypi.org/project/pytetwild/
  repo: https://pypi.org/project/pytetwild/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyamg
  version: 5.3.0
  home: https://github.com/pyamg/pyamg
  repo: https://github.com/pyamg/pyamg
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1780
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: materialyoucolor
  version: 3.0.4
  home: https://pypi.org/project/materialyoucolor/
  repo: https://pypi.org/project/materialyoucolor/
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bpy
  version: 5.2.1
  home: https://www.blender.org
  repo: https://www.blender.org
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: imgui
  version: 2.0.0
  home: https://github.com/pyimgui/pyimgui
  repo: https://github.com/pyimgui/pyimgui
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1779
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp36,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openalgo
  version: 2.0.3
  home: https://openalgo.in
  repo: https://github.com/marketcalls/openalgo-python-library
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: simple-equ
  version: 1.5.11
  home: https://pypi.org/project/simple-equ/
  repo: https://pypi.org/project/simple-equ/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: icsneopy
  version: 1.4.1
  home: https://github.com/intrepidcs/libicsneo
  repo: https://github.com/intrepidcs/libicsneo
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libucx-cu12
  version: 1.21.0
  home: https://github.com/openucx/ucx
  repo: https://github.com/openucx/ucx
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: photutils
  version: 3.0.0
  home: https://github.com/astropy/photutils
  repo: https://github.com/astropy/photutils
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: indexed-gzip
  version: 1.10.3
  home: https://github.com/pauldmccarthy/indexed_gzip
  repo: https://github.com/pauldmccarthy/indexed_gzip
  status: published
  pr: 1335
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onnxruntime-extensions
  version: 0.15.2
  home: https://github.com/microsoft/onnxruntime-extensions
  repo: https://github.com/microsoft/onnxruntime-extensions
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: netifaces-plus
  version: 0.12.5
  home: https://github.com/tsukumijima/netifaces-plus
  repo: https://github.com/tsukumijima/netifaces-plus
  status: published
  pr: 1601
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: strongtyping
  version: 3.14.5
  home: https://github.com/FelixTheC/strongtyping
  repo: https://github.com/FelixTheC/strongtyping
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: laszip
  version: 0.3.0
  home: https://github.com/tmontaigu/laszip-python
  repo: https://github.com/tmontaigu/laszip-python
  status: published
  pr: 1459
  notes: '22 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: piot-fixture-zzz-python-maturin
  version: 0.0.1788368955
  home: https://pypi.org/project/piot-fixture-zzz-python-maturin/
  repo: https://pypi.org/project/piot-fixture-zzz-python-maturin/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: acquisition
  version: '6.3'
  home: https://github.com/zopefoundation/Acquisition
  repo: https://github.com/zopefoundation/Acquisition
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: memfabric-zbal
  version: 1.2.0
  home: https://gitcode.com/Ascend/memfabric_hybrid/app/zbal
  repo: https://gitcode.com/Ascend/memfabric_hybrid/app/zbal
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vesin
  version: 0.6.1
  home: https://github.com/Luthaf/vesin/
  repo: https://github.com/Luthaf/vesin/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1778
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sxtwl
  version: 2.0.7
  home: https://github.com/yuangu/sxtwl_cpp
  repo: https://github.com/yuangu/sxtwl_cpp
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp27,cp310,cp311,cp312,cp313,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mecab-ko
  version: 1.0.2
  home: https://github.com/NoUnique/pymecab-ko
  repo: https://github.com/NoUnique/pymecab-ko
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: igwn-segments
  version: 2.1.1
  home: https://pypi.org/project/igwn-segments/
  repo: https://pypi.org/project/igwn-segments/
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: piot-fixture-zzz-python
  version: 0.0.1788368970
  home: https://pypi.org/project/piot-fixture-zzz-python/
  repo: https://pypi.org/project/piot-fixture-zzz-python/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rectangle-packer
  version: 2.1.0
  home: https://github.com/Penlect/rectangle-packer
  repo: https://github.com/Penlect/rectangle-packer
  status: published
  pr: 1333
  notes: 'CI green (cp312/cp313/cp314 manylinux_riscv64); PR #1333 open for review.'
- pkg: qoi
  version: 0.7.2
  home: https://github.com/kodonnell/qoi
  repo: https://github.com/kodonnell/qoi
  status: published
  pr: 1460
  notes: 'CI green (cp312/cp313/cp314/cp314t manylinux_riscv64); PR #1460 open for review.'
- pkg: aliyun-log-fastpb
  version: 0.3.0
  home: https://pypi.org/project/aliyun-log-fastpb/
  repo: https://pypi.org/project/aliyun-log-fastpb/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tree-sitter-dockerfile
  version: 0.2.0
  home: https://github.com/camdencheek/tree-sitter-dockerfile
  repo: https://github.com/camdencheek/tree-sitter-dockerfile
  status: published
  pr: 1603
  notes: '1 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: glean-sdk
  version: 70.0.0
  home: https://mozilla.github.io/glean/
  repo: https://github.com/mozilla/glean
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-libsbml
  version: 5.21.1
  home: http://sbml.org
  repo: http://sbml.org
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xpresslibs
  version: 9.9.1
  home: https://www.fico.com/en/products/fico-xpress-optimization
  repo: https://www.fico.com/en/products/fico-xpress-optimization
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: blurhash-python
  version: 1.2.2
  home: https://blurha.sh
  repo: https://blurha.sh
  status: published
  pr: 1462
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dprint-py
  version: 0.57.0.0
  home: https://dprint.dev/
  repo: https://github.com/dprint/dprint
  status: published
  pr: 1605
  notes: '3 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PyPI''s dprint-py (trim21/dprint-py) repackages dprint/dprint''s own binary releases, whose tooling has no riscv64 target. dprint''s own CI already cross-builds a riscv64gc-unknown-linux-gnu asset, so built from source instead: maturin bindings=bin wheel wrapping the dprint binary, same shape as build-ripgrep.yml. Synthesized pyproject.toml with a static version (0.57.0.0, matching the real PyPI package''s scheme) decoupled from the crate''s own Cargo.toml version (0.57.0). PR #1605 opened, CI running.'
- pkg: surrealdb
  version: 2.0.0
  home: https://github.com/surrealdb/surrealdb.py
  repo: https://github.com/surrealdb/surrealdb.py
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gllm-inference-binary
  version: 0.6.145
  home: https://pypi.org/project/gllm-inference-binary/
  repo: https://pypi.org/project/gllm-inference-binary/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scikit-misc
  version: 0.5.2
  home: https://has2k1.github.io/scikit-misc/stable
  repo: https://github.com/has2k1/scikit-misc
  status: published
  pr: 1606
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hyperlight-sandbox-backend-wasm
  version: 0.6.0
  home: https://pypi.org/project/hyperlight-sandbox-backend-wasm/
  repo: https://pypi.org/project/hyperlight-sandbox-backend-wasm/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scann
  version: 1.4.2
  home: https://github.com/google-research/google-research/tree/master/scann
  repo: https://github.com/google-research/google-research/tree/master/scann
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: s3torchconnectorclient
  version: 1.5.0
  home: ''
  repo: ''
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PyPI records no project URL; locate upstream during research.'
- pkg: ai-edge-litert-nightly
  version: 2.3.0.dev20260902
  home: https://www.tensorflow.org/lite/
  repo: https://www.tensorflow.org/lite/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: epicscorelibs
  version: 7.0.10.99.0.1
  home: https://github.com/mdavidsaver/epicscorelibs
  repo: https://github.com/mdavidsaver/epicscorelibs
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-planarity
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chompjs
  version: 1.4.1
  home: https://github.com/Nykakin/chompjs
  repo: https://github.com/Nykakin/chompjs
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1344
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorflow-cpu-aws
  version: 2.15.1
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mnn
  version: 3.6.1
  home: https://www.yuque.com/mnn/en/usage_in_python
  repo: https://www.yuque.com/mnn/en/usage_in_python
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pgpq
  version: 0.12.0
  home: https://github.com/adriangb/pgpq
  repo: https://github.com/adriangb/pgpq
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: snowpipe-streaming
  version: 1.8.0
  home: https://github.com/snowflake-eng/snowflake-ingest-sdk
  repo: https://github.com/snowflake-eng/snowflake-ingest-sdk
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tower
  version: 0.3.72
  home: https://pypi.org/project/tower/
  repo: https://pypi.org/project/tower/
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: daal
  version: 2026.1.0
  home: https://github.com/uxlfoundation/oneDAL
  repo: https://github.com/uxlfoundation/oneDAL
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gsw
  version: 3.6.23
  home: https://www.teos-10.org/
  repo: https://github.com/TEOS-10/GSW-python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opentimelineio
  version: 0.18.1
  home: http://opentimeline.io
  repo: https://github.com/AcademySoftwareFoundation/OpenTimelineIO
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1784
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyopenms
  version: 3.5.0
  home: https://openms.de
  repo: https://github.com/OpenMS/OpenMS/tree/develop/src/pyOpenMS
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tml-renderers
  version: 0.1.0
  home: https://pypi.org/project/tml-renderers/
  repo: https://pypi.org/project/tml-renderers/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scikit-learn-intelex
  version: 2026.1.0
  home: https://github.com/intel/scikit-learn-intelex
  repo: https://github.com/uxlfoundation/scikit-learn-intelex
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: py310,py311,py312,py313,py314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: extensionclass
  version: '6.4'
  home: https://github.com/zopefoundation/ExtensionClass
  repo: https://github.com/zopefoundation/ExtensionClass
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1338
  notes: 'PR #1338 green (4 wheels built cp312-cp314t, publish dry-run OK), awaiting review. Mirrors upstream tests.yml: cibuildwheel builds ExtensionClass, ComputedAttribute, MethodObject C extensions; zope.testrunner runs with and without PURE_PYTHON. No build-time dependencies.'
- pkg: proxsuite
  version: 0.7.3
  home: https://github.com/cmake-wheel/proxsuite
  repo: https://github.com/cmake-wheel/proxsuite.git
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hud-sdk
  version: 0.5.4
  home: https://hud.io
  repo: https://hud.io
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-ripgrep
  version: 0.0.9
  home: https://pypi.org/project/python-ripgrep/
  repo: https://pypi.org/project/python-ripgrep/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hadolint-coatl
  version: 2.15.1
  home: https://github.com/coatl-dev/hadolint-coatl
  repo: https://github.com/coatl-dev/hadolint-coatl
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: advent-of-code
  version: 2025.6.0
  home: https://github.com/fornwall/advent-of-code
  repo: https://github.com/fornwall/advent-of-code
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: raft-dask-cu12
  version: 26.8.0
  home: https://github.com/rapidsai/raft
  repo: https://github.com/rapidsai/raft
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jina
  version: 3.34.0
  home: https://github.com/jina-ai/jina/
  repo: https://github.com/jina-ai/jina/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk-segmentation
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: based58
  version: 0.1.1
  home: https://github.com/kevinheavey/based58
  repo: https://github.com/kevinheavey/based58
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1461
  notes: '11 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: oneccl
  version: 2022.1.2
  home: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  repo: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: impi-rt
  version: 2021.18.1
  home: https://software.intel.com/en-us/mpi-library
  repo: https://software.intel.com/en-us/mpi-library
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rnet
  version: 2.4.2
  home: https://github.com/0x676e67/rnet
  repo: https://github.com/0x676e67/rnet
  status: queued
  pr: null
  notes: '72 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deeplake
  version: 4.7.4
  home: https://docs.deeplake.ai/
  repo: https://github.com/activeloopai/deeplake
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gersemi
  version: 0.28.1
  home: https://github.com/BlankSpruce/gersemi
  repo: https://github.com/BlankSpruce/gersemi
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: joulescope
  version: 1.6.0
  home: https://joulescope.readthedocs.io
  repo: https://github.com/jetperch/pyjoulescope/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vllm-router
  version: 0.1.15
  home: https://pypi.org/project/vllm-router/
  repo: https://pypi.org/project/vllm-router/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tfparse
  version: 0.6.21
  home: https://github.com/cloud-custodian/tfparse
  repo: https://github.com/cloud-custodian/tfparse
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastnanoid
  version: 0.4.3
  home: https://github.com/oliverlambson/fastnanoid
  repo: https://github.com/oliverlambson/fastnanoid
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1343
  notes: '98 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: autosar-data
  version: 0.17.0
  home: https://danielt.github.io/autosar-data-py/
  repo: https://github.com/DanielT/autosar-data-py
  status: queued
  pr: null
  notes: '64 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: igwn-ligolw
  version: 2.1.1
  home: https://pypi.org/project/igwn-ligolw/
  repo: https://pypi.org/project/igwn-ligolw/
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyside2
  version: 5.15.2.1
  home: https://www.pyside.org
  repo: https://www.pyside.org
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp27,cp35.cp36.cp37.cp38.cp39.cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: netifaces2
  version: 0.0.22
  home: https://github.com/SamuelYvon/netifaces-2
  repo: https://github.com/SamuelYvon/netifaces-2
  status: published
  pr: 1604
  notes: '15 Linux wheels upstream (abi: cp36,cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ucxx-cu12
  version: 0.51.1
  home: https://github.com/rapidsai/ucxx
  repo: https://github.com/rapidsai/ucxx
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: runai-model-streamer-azure
  version: 0.16.1
  home: https://pypi.org/project/runai-model-streamer-azure/
  repo: https://pypi.org/project/runai-model-streamer-azure/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: siphashc
  version: '2.8'
  home: https://github.com/WeblateOrg/siphashc
  repo: https://github.com/WeblateOrg/siphashc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1342
  notes: 'PR #1342 open, CI green (cp310 abi3, cp314t, cp315t). No riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: betterproto-rust-codec
  version: 0.1.1
  home: https://github.com/124C41p/betterproto-rust-codec
  repo: https://github.com/124C41p/betterproto-rust-codec
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymicro-vad
  version: 2.1.0
  home: https://github.com/rhasspy/pymicro-vad
  repo: https://github.com/rhasspy/pymicro-vad
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: geode-conversion
  version: 6.7.2
  home: https://pypi.org/project/Geode-Conversion/
  repo: https://pypi.org/project/Geode-Conversion/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gdsfactoryplus
  version: 1.8.20
  home: https://pypi.org/project/gdsfactoryplus/
  repo: https://pypi.org/project/gdsfactoryplus/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: persistence
  version: '5.5'
  home: https://github.com/zopefoundation/Persistence
  repo: https://github.com/zopefoundation/Persistence
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libucxx-cu12
  version: 0.51.1
  home: https://github.com/rapidsai/ucxx
  repo: https://github.com/rapidsai/ucxx
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: itk-registration
  version: 5.4.7
  home: https://github.com/InsightSoftwareConsortium/ITK
  repo: https://github.com/InsightSoftwareConsortium/ITK
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: anchorpy-core
  version: 0.2.0
  home: https://github.com/kevinheavey/anchorpy-core
  repo: https://github.com/kevinheavey/anchorpy-core
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kolo
  version: 3.3.0
  home: https://kolo.app
  repo: https://github.com/koloai/kolo
  status: queued
  pr: null
  notes: '46 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyclothoids
  version: 0.2.0
  home: https://github.com/phillipd94/PyClothoids
  repo: https://github.com/phillipd94/PyClothoids
  status: queued
  pr: null
  notes: '38 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: oneccl-devel
  version: 2022.1.2
  home: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  repo: https://software.intel.com/en-us/articles/empowering-science-with-high-performance-python
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysha3
  version: 1.0.2
  home: https://github.com/tiran/pysha3
  repo: https://github.com/tiran/pysha3
  status: parked
  pr: null
  notes: 'Not feasible (gotcha 334, feasibility-and-triage.md): pysha3 backports hashlib.sha3_*/shake_* for Python < 3.6 by vendoring the exact C sources CPython later shipped as its own _sha3 module; sha3.py only monkey-patches hashlib if not hasattr(_hashlib, "sha3_512"), a no-op on every interpreter since 3.6, so this repo''s cp312+ floor gets zero benefit even if it built. It doesn''t build: Modules/_sha3/backport.inc does #include "pystrhex.h" for Python >= 3.5, and pystrhex.h is a CPython-internal header under Modules/, never installed under Include/ alongside Python.h -- confirmed by actually building the real 1.0.2 sdist against a Python 3.12 venv (pip wheel . --no-deps --no-build-isolation): fatal error: ''pystrhex.h'' file not found. Upstream itself settled this in November 2022, deleting the whole tree down to a one-line README.md: "pysha3 has reached its end of life ... please use SHA-3 functions from hashlib". No worktree/branch/PR created.'
- pkg: passagemath-glpk
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: diffq
  version: 0.2.4
  home: https://github.com/facebookresearch/diffq
  repo: https://github.com/facebookresearch/diffq
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: marin-finelog-server
  version: 0.2.10
  home: https://pypi.org/project/marin-finelog-server/
  repo: https://pypi.org/project/marin-finelog-server/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyjls
  version: 0.18.0
  home: https://joulescope.readthedocs.io
  repo: https://github.com/jetperch/jls/
  status: published
  pr: 1782
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: iteration-utilities
  version: 0.13.0
  home: https://github.com/MSeifert04/iteration_utilities
  repo: https://github.com/MSeifert04/iteration_utilities.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1781
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp37,cp38,cp39,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-rattler
  version: 0.25.0
  home: https://github.com/conda/rattler
  repo: https://github.com/conda/rattler
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: libcuvs-cu12
  version: 26.8.1
  home: https://github.com/rapidsai/cuvs
  repo: https://github.com/rapidsai/cuvs
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: quickjs-ng
  version: 0.16.2.1
  home: https://github.com/genotrance/quickjs-ng
  repo: https://github.com/genotrance/quickjs-ng
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1783
  notes: '12 Linux wheels upstream (abi: cp310,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vodozemac
  version: 0.10.0
  home: https://github.com/matrix-nio/vodozemac-python
  repo: https://github.com/matrix-nio/vodozemac-python
  status: published
  pr: 1607
  notes: '74 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp314t,cp315,cp315t,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intbitset
  version: 4.1.2
  home: http://github.com/inveniosoftware-contrib/intbitset/
  repo: http://github.com/inveniosoftware-contrib/intbitset/
  status: published
  pr: 1340
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-rankwidth
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: distributed-ucxx-cu12
  version: 0.51.1
  home: https://github.com/rapidsai/ucxx
  repo: https://github.com/rapidsai/ucxx
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zope-container
  version: '7.3'
  home: https://zopecontainer.readthedocs.io
  repo: https://github.com/zopefoundation/zope.container
  status: published
  pr: 1339
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: debug-mgr
  version: 0.0.2.33288736809
  home: https://github.com/mballance/debug-mgr
  repo: https://github.com/mballance/debug-mgr
  status: queued
  pr: null
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-coxeter3
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nmslib
  version: 2.1.2
  home: https://github.com/nmslib/nmslib
  repo: https://github.com/nmslib/nmslib
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ncompress
  version: 1.0.2
  home: https://github.com/valgur/ncompress
  repo: https://github.com/valgur/ncompress
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1341
  notes: 'PR #1341 open, CI running. Nanobind/scikit-build-core extension, cp312 (abi3) + cp314t x manylinux/musllinux, no runtime deps.'
- pkg: python-active-directory
  version: 2.0.1
  home: https://github.com/theatlantic/python-active-directory
  repo: https://github.com/theatlantic/python-active-directory
  status: queued
  pr: null
  notes: '45 Linux wheels upstream (abi: cp310,cp311,cp36,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arraykit
  version: 1.12.0
  home: https://github.com/static-frame/arraykit
  repo: https://github.com/static-frame/arraykit
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1787
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyedflib
  version: 0.1.42
  home: https://github.com/holgern/pyedflib
  repo: https://github.com/holgern/pyedflib
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1464
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyfixest
  version: 0.60.0
  home: https://pyfixest.com
  repo: https://github.com/py-econometrics/pyfixest
  status: queued
  pr: null
  notes: '84 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: unpdf
  version: 1.0.0
  home: https://github.com/digi-deity/unPDF
  repo: https://github.com/digi-deity/unPDF
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sqlcipher3-binary
  version: 0.6.0
  home: https://github.com/coleifer/sqlcipher3
  repo: https://github.com/coleifer/sqlcipher3
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyheif
  version: 0.8.0
  home: https://github.com/carsales/pyheif
  repo: https://github.com/carsales/pyheif
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1465
  notes: 'PR #1465 open. Builds libheif/libde265/libaom/x265 from source (none packaged for riscv64), mirroring upstream''s own Dockerfile recipe exactly (same pinned versions, --preset=release-noplugins). Stages LICENSE.<lib> for each (x265 GPL-2.0, libde265/libheif LGPL-3.0, libaom BSD-2-Clause+patent) alongside pyheif''s own Apache-2.0 LICENSE.'
- pkg: stretchable
  version: 1.1.8
  home: https://stretchable.readthedocs.io/en/latest/
  repo: https://github.com/mortencombat/stretchable
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pytket
  version: 2.18.1
  home: https://docs.quantinuum.com/tket/api-docs/
  repo: https://github.com/quantinuum/tket
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opentsne
  version: 1.0.4
  home: https://github.com/pavlin-policar/openTSNE
  repo: https://github.com/pavlin-policar/openTSNE
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1789
  notes: 'PR #1789 open, CI green: cp312/cp313/cp314/cp314t riscv64 wheels built (145 passed, 27 skipped each; hnswlib/pynndescent self-skip, no riscv64 wheels), GPL sources collected for the image''s vendored libgomp, publish dry-ran cleanly. Cython/C++ t-SNE extension (Barnes-Hut quad-tree, vendored Annoy nearest-neighbour index), OpenMP-parallel. Mirrors upstream''s azure-pipelines-release.yml Linux job. setup.py falls back to the numpy FFT implementation since FFTW3 headers are absent from the manylinux image, same as upstream''s own manylinux wheels. Patch ships Spotify annoy''s Apache-2.0 LICENSE, missing from upstream''s own wheel too. Awaiting maintainer review.'
- pkg: datafusion-query-builder
  version: 0.2.0
  home: https://github.com/pydantic/datafusion-query-builder
  repo: https://github.com/pydantic/datafusion-query-builder
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ormar-utils
  version: 0.2.1
  home: https://github.com/ormar-orm/ormar-utils
  repo: https://github.com/ormar-orm/ormar-utils
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-cddlib
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dtaidistance
  version: 2.4.0
  home: https://github.com/wannesm/dtaidistance
  repo: https://github.com/wannesm/dtaidistance
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1786
  notes: '5 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cronexpr
  version: 0.30.0
  home: https://github.com/CodyRay/pycronexpr
  repo: https://github.com/CodyRay/pycronexpr
  status: published
  pr: 1608
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev. Pybind11/scikit-build-core extension wrapping supertinycron/ccronexpr, CPM-fetched at CMake configure time; CMAKE_POLICY_VERSION_MINIMUM=3.5 works around its cmake_minimum_required(3.5) floor being unsupported by newer CMake (CLAUDE.md gotcha 257). No upstream test suite; smoke-tests the compiled extension and the documented next_fire/prev_fire example.'
- pkg: pymgclient
  version: 1.6.0
  home: https://memgraph.github.io/pymgclient
  repo: https://github.com/memgraph/pymgclient
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastobo
  version: 0.14.1
  home: https://github.com/fastobo/fastobo-py/
  repo: https://github.com/fastobo/fastobo-py/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1788
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tippecanoe
  version: 2.72.0
  home: https://github.com/felt/tippecanoe
  repo: https://github.com/felt/tippecanoe
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sgl-kernel
  version: 0.3.21
  home: https://github.com/sgl-project/sglang/tree/main/sgl-kernel
  repo: https://github.com/sgl-project/sglang/tree/main/sgl-kernel
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ai-ml-sdk-vgf-library
  version: 0.10.0
  home: https://pypi.org/project/ai-ml-sdk-vgf-library/
  repo: https://pypi.org/project/ai-ml-sdk-vgf-library/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastdigest
  version: 0.12.0
  home: https://github.com/moritzmucha/fastdigest
  repo: https://github.com/moritzmucha/fastdigest
  status: published
  pr: 1349
  notes: 'PR #1349 merged. PyO3/maturin t-digest library; upstream has no [tool.cibuildwheel], so the Rust toolchain is installed in-container via CIBW_BEFORE_ALL_LINUX. musllinux dropped (rustup ships no riscv64 musl toolchain). Publish run on main succeeded; immutable GitHub Release created with all 4 riscv64 wheels (cp312,cp313,cp314,cp314t). Docs PR #1367 auto-opened. Confirmed live on the registry (200). 126 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: geode-explicit
  version: 7.4.1
  home: https://pypi.org/project/Geode-Explicit/
  repo: https://pypi.org/project/Geode-Explicit/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: runstats
  version: 2.0.0
  home: http://www.grantjenks.com/docs/runstats/
  repo: https://github.com/grantjenks/python-runstats
  status: published
  pr: 1346
  notes: '16 Linux wheels upstream (abi: cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green.'
- pkg: cocoindex
  version: 1.0.20
  home: https://cocoindex.io/
  repo: https://cocoindex.io/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: wgpu
  version: 0.32.0
  home: https://github.com/pygfx/wgpu-py
  repo: https://github.com/pygfx/wgpu-py
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cydifflib
  version: 1.2.0
  home: https://github.com/rapidfuzz/CyDifflib
  repo: https://github.com/rapidfuzz/CyDifflib.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1348
  notes: 'PR #1348 open. sdist built from a tagged checkout (cythonized with generate.sh, upstream tools/sdist.patch applied) then cibuildwheel builds cp312-cp314t from that tarball, no CIBW_BEFORE_BUILD needed. Plain MIT, no vendored code. Verified locally: sdist + wheel build and 29/29 tests pass on native arch.'
- pkg: xtgeo
  version: 4.25.1
  home: https://github.com/equinor/xtgeo
  repo: https://github.com/equinor/xtgeo
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pygeos
  version: '0.14'
  home: https://github.com/pygeos/pygeos
  repo: https://github.com/pygeos/pygeos
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-objects
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ovstage
  version: 0.1.1.355824
  home: https://github.com/NVIDIA-Omniverse/ovstage
  repo: https://github.com/NVIDIA-Omniverse/ovstage
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: flydsl
  version: 0.3.2
  home: https://pypi.org/project/flydsl/
  repo: https://pypi.org/project/flydsl/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cuequivariance-ops-torch-cu12
  version: 0.11.1
  home: https://developer.nvidia.com/cuequivariance
  repo: https://developer.nvidia.com/cuequivariance
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openjd-model
  version: 0.11.6
  home: https://github.com/OpenJobDescription/openjd-model-for-python
  repo: https://github.com/OpenJobDescription/openjd-model-for-python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: freeze-core
  version: 0.7.2
  home: https://cx-freeze.readthedocs.io
  repo: https://github.com/marcelotduarte/freeze-core
  status: queued
  pr: null
  notes: '35 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: base2048
  version: 0.1.3
  home: https://pypi.org/project/base2048/
  repo: https://pypi.org/project/base2048/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1463
  notes: '1 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ouster-sdk
  version: 1.0.1
  home: https://github.com/ouster-lidar/ouster-sdk
  repo: https://github.com/ouster-lidar/ouster-sdk
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: monotonic-alignment-search
  version: 0.2.1
  home: https://github.com/eginhard/monotonic_alignment_search
  repo: https://github.com/eginhard/monotonic_alignment_search
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: intersystems-irispython
  version: 5.4.0
  home: https://www.intersystems.com/
  repo: https://www.intersystems.com/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39.cp310.cp311.cp312.cp313.cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pytrilogy
  version: 0.3.339
  home: https://pypi.org/project/pytrilogy/
  repo: https://pypi.org/project/pytrilogy/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pybedtools
  version: 0.12.1
  home: https://github.com/daler/pybedtools
  repo: https://github.com/daler/pybedtools
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arm-pyart
  version: 2.2.5
  home: https://github.com/ARM-DOE/pyart
  repo: https://github.com/ARM-DOE/pyart
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: omniverseclient
  version: 2.73.1
  home: https://docs.omniverse.nvidia.com/kit/docs/client_library/latest/index.html
  repo: https://docs.omniverse.nvidia.com/kit/docs/client_library/latest/index.html
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: smoldot-light
  version: 0.1.0
  home: https://github.com/JAMdotTech/py-smoldot-light
  repo: https://github.com/JAMdotTech/py-smoldot-light
  status: queued
  pr: null
  notes: '73 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zhinst-comms
  version: 4.2.1.26081241
  home: https://pypi.org/project/zhinst-comms/
  repo: https://pypi.org/project/zhinst-comms/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyjpegls
  version: 1.5.1
  home: https://github.com/pydicom/pyjpegls
  repo: https://github.com/pydicom/pyjpegls
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: anycrc
  version: 2.0.0
  home: https://github.com/marzooqy/anycrc
  repo: https://github.com/marzooqy/anycrc
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1350
  notes: 'PR #1350 open, CI running. Build-from-checkout cibuildwheel workflow mirroring upstream''s github-deploy.yml. setup.py already disables its CLMUL SIMD path off x86_64/aarch64/arm64, so riscv64 needs no patching. cp311-abi3 wheel covers cp311-cp314; cp314t built separately (free-threaded, no limited API).'
- pkg: fno
  version: 0.3.1
  home: https://github.com/bllshttng/footnote
  repo: https://github.com/bllshttng/footnote
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-sirocco
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastpdb
  version: 1.3.3
  home: https://github.com/biotite-dev/fastpdb
  repo: https://github.com/biotite-dev/fastpdb
  status: parked
  pr: null
  notes: 'Upstream repo is archived (confirmed via API, archived: true, last push 2026-06-21) with its README stating "fastpdb is integrated into biotite now and this repository has been archived" - no more releases will happen. Verified by building v1.3.3 locally (macOS arm64, cp312): the wheel builds fine (maturin, pyo3 0.24.2), but installing it alongside the only biotite compatible with our registry/PyPI (1.7.1, also the latest release upstream) breaks almost the entire test suite (1119/1144 failed) with `AttributeError: attribute lines of biotite.structure.PDBFile objects is not writable`. Root cause: biotite 1.7.1 absorbed fastpdb''s own fast Rust PDB parser natively (biotite.structure.io.pdb.PDBFile now subclasses biotite.rust.structure.io.pdb.PDBFile, biotite''s own compiled extension) and no longer exposes a writable `lines` attribute the way fastpdb 1.3.3''s Python shim (python-src/fastpdb/__init__.py) assumes when syncing state after every read()/set_structure() call. This is a real, riscv64-independent incompatibility (same break on any architecture with biotite >=1.7.1, the only version PyPI or pypi.riseproject.dev offers) with no small patch available - the fix would mean reimplementing fastpdb''s compatibility shim against biotite''s new internal architecture, and would be pointless since biotite already ships the same fast parser natively. Also noted in passing (moot given the above): pyproject.toml build-system pins `numpy >=1.26,<1.27`, which has no cp313 wheel anywhere (numpy 1.26 predates Python 3.13 support) and would need a patch to build past cp312; and Cargo.toml pins `pyo3 = "0.24"` (resolves to 0.24.2), which hard-fails to build for cp314/cp314t (CPython interpreter version newer than PyO3''s maximum supported version (3.13), gotcha 326) so those two interpreters would need dropping from the matrix regardless. No worktree/branch/PR created (a worktree was opened, used only for local build/test rehearsal against the real biotite-dev/fastpdb v1.3.3 checkout, then removed once this was confirmed).'
- pkg: spire-xls
  version: 16.8.2
  home: https://www.e-iceblue.com
  repo: https://www.e-iceblue.com
  status: parked
  pr: null
  notes: 'No public source at any version: pypi.org/pypi/spire-xls/json lists 20 releases (13.9.0..16.8.2) and zero sdists ever (gotcha 431, wheel is the whole evidence base); `home`/`repo` both resolve to the marketing page (gotcha 372''s tell), `License: UNKNOWN` in METADATA with classifiers `License :: Other/Proprietary License` and `License :: Free To Use But Restricted`, and no LICENSE file bundled in dist-info (only METADATA/WHEEL/RECORD/top_level.txt) - just a "Temporary License" link in the long-description, E-iceblue''s paid-license-key model (unlicensed use runs in evaluation mode with output limits, per the same "Free To Use But Restricted" classifier as aspose-words above). Confirmed by unzipping both 16.8.2 Linux wheels: `spire/xls/lib/Spire.Xls.Base.so` is a closed, unmanaged ELF (not IL - `strings -a` shows `CoreCLR GC` and other .NET Native-AOT runtime markers, i.e. ahead-of-time-compiled machine code, no separate arch-neutral assembly to fall back on) and is a *different* per-arch binary: 113.9 MB unstripped on manylinux_2_34_x86_64 vs 48.2 MB on manylinux2014_aarch64 - the gurobipy/mosek "closed engine, not a recompile" shape, one proprietary build per platform, never shipped as source. `readelf -d` on the x86_64 one shows ordinary NEEDED (libstdc++/libm/libz/libc) and `RPATH $ORIGIN`, so it is hard-linked into the extension the same way gurobipy/mosek hard-link their engines. Two riscv64 string hits inside it are a false lead worth recording so a future session does not reopen this on the same evidence: both sit inside the embedded .NET runtime-identifier (RID) architecture-name table (`...ppc64le / loongarch64 / riscv64 / calendar...`, i.e. `System.Runtime.InteropServices.RuntimeInformation`''s known-architecture list), which every .NET Native-AOT corelib ships regardless of which architectures were actually built - it is not evidence a riscv64 build of this engine exists anywhere. `spire/xls/license/LicenseProvider.py` and `spire/xls/common/License.py` wrap native `LicenseProvider_SetLicenseKey`/`LISetLicenseFileFullPath` calls into that same closed .so, confirming the license-key gate lives in the engine itself, not a Python-side check that could be bypassed by a rebuild. `github.com/eiceblue/Spire.XLS-for-Python` (linked from PyPI long-description as "Examples") is confirmed to hold only usage-example scripts against the closed library, not engine source - the mosek/gurobipy "vendor repo is examples, not source" shape. No community/open-source edition exists. Sibling packages spire-doc (14.8.1) and spire-presentation (11.8.1) are still `status: queued` elsewhere in this file as of this triage and were not touched (out of scope for this pass), but both ship the same E-iceblue Spire.* packaging shape (zero sdists, same license classifiers, same closed native engine pattern) and are candidates for the same park disposition in a future pass.'
- pkg: ytsaurus-yson
  version: 0.4.10
  home: https://pypi.org/project/ytsaurus-yson/
  repo: https://pypi.org/project/ytsaurus-yson/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp34); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spatial-access
  version: 1.0.2
  home: https://github.com/GeoDaCenter/spatial_access
  repo: https://github.com/GeoDaCenter/spatial_access
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp36,cp37,cp38,cp39,pp37,pp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fpylll
  version: 0.6.4
  home: https://github.com/fplll/fpylll
  repo: https://github.com/fplll/fpylll
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: triton-ascend
  version: 3.2.0
  home: https://gitcode.com/Ascend/triton-ascend/
  repo: https://gitcode.com/Ascend/triton-ascend/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spidev
  version: '3.8'
  home: http://github.com/doceme/py-spidev
  repo: http://github.com/doceme/py-spidev
  status: published
  pr: 1609
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arize-phoenix-sqlean
  version: 0.1.1
  home: https://github.com/Arize-ai/phoenix/tree/main/packages/phoenix-sqlean
  repo: https://github.com/Arize-ai/phoenix/tree/main/packages/phoenix-sqlean
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: databend-driver
  version: 0.34.2
  home: https://github.com/databendlabs/bendsql
  repo: https://github.com/databendlabs/bendsql
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rensa
  version: 0.4.1
  home: https://github.com/beowolx/rensa
  repo: https://github.com/beowolx/rensa
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1345
  notes: '92 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: renkin
  version: 1.0.0
  home: https://github.com/kent-tokyo/renkin
  repo: https://github.com/kent-tokyo/renkin
  status: queued
  pr: null
  notes: '19 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: argus-redact
  version: 0.8.16
  home: https://github.com/wan9yu/argus-redact
  repo: https://github.com/wan9yu/argus-redact
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ft4222
  version: 1.13.0
  home: https://gitlab.com/msrelectronics/python-ft4222
  repo: https://gitlab.com/msrelectronics/python-ft4222
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rdkit-pypi
  version: 2022.9.5
  home: https://github.com/kuelumbus/rdkit-pypi
  repo: https://github.com/kuelumbus/rdkit-pypi
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fckitlib
  version: 0.14.2.19
  home: https://pypi.org/project/fckitlib/
  repo: https://pypi.org/project/fckitlib/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pcodec
  version: 1.0.3
  home: https://github.com/pcodec/pcodec
  repo: https://github.com/pcodec/pcodec
  status: published
  pr: 1610
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: diptest
  version: 0.11.0
  home: https://github.com/RUrlus/diptest#readme
  repo: https://github.com/RUrlus/diptest
  status: published
  pr: 1611
  notes: 'scikit-build-core/CMake/pybind11 build, OpenMP enabled to mirror upstream''s wheels.yml (triggers a gpl_sources job for the vendored libgomp). Patch stages LICENSE.pcg-cpp since diptest''s own vendoring of the dual Apache-2.0/MIT pcg-cpp headers drops the licence text. numpy and psutil (both runtime deps) have riscv64 wheels on our registry for cp312-cp314t. cp314t needed CIBW_TEST_ENVIRONMENT: PYTHON_GIL=1 (gotcha 127): _diptest_core has no Py_mod_gil slot.'
- pkg: polars-ds
  version: 0.12.1
  home: https://pypi.org/project/polars-ds/
  repo: https://pypi.org/project/polars-ds/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pypolyline
  version: 0.5.8
  home: https://github.com/urschrei/pypolyline
  repo: https://github.com/urschrei/pypolyline
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1467
  notes: 'PR #1467 open, CI green (cp312/cp313/cp314). Builds urschrei/polyline-ffi (Rust cdylib) from source, no riscv64 release asset upstream.'
- pkg: jxlpy
  version: 0.9.5
  home: http://github.com/olokelo/jxlpy
  repo: http://github.com/olokelo/jxlpy
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clarifai-protocol
  version: 0.0.62
  home: https://clarifai.com
  repo: https://github.com/Clarifai/clarifai-protocol
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-plot
  version: 10.8.10.post1
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: typecode-libmagic
  version: 5.39.210531
  home: https://github.com/nexB/scancode-plugins
  repo: https://github.com/nexB/scancode-plugins
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-meataxe
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-cliquer
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: point-cloud-utils
  version: 0.34.0
  home: https://github.com/fwilliams/point-cloud-utils
  repo: https://github.com/fwilliams/point-cloud-utils
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1790
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aimmspy
  version: 26.1.8.1
  home: https://pypi.org/project/aimmspy/
  repo: https://pypi.org/project/aimmspy/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: swmm-toolkit
  version: 0.17.0
  home: https://github.com/pyswmm/swmm-python
  repo: https://github.com/pyswmm/swmm-python
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorflow-data-validation
  version: 1.21.0
  home: https://www.tensorflow.org/tfx/data_validation/get_started
  repo: https://www.tensorflow.org/tfx/data_validation/get_started
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ntgcalls
  version: 2.2.5
  home: https://pytgcalls.github.io/
  repo: https://github.com/pytgcalls/ntgcalls.git
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: extractcode-7z
  version: 16.5.210531
  home: https://github.com/nexB/scancode-plugins
  repo: https://github.com/nexB/scancode-plugins
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: extractcode-libarchive
  version: 3.5.1.210531
  home: https://github.com/nexB/scancode-plugins
  repo: https://github.com/nexB/scancode-plugins
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lcm
  version: 1.5.2
  home: https://pypi.org/project/lcm/
  repo: https://pypi.org/project/lcm/
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: diffq-fixed
  version: 0.2.4
  home: https://github.com/JackismyShephard/diffq
  repo: https://github.com/JackismyShephard/diffq
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: oneagent-sdk
  version: 1.5.2.20260107.153442
  home: https://github.com/Dynatrace/OneAgent-SDK-for-Python
  repo: https://github.com/Dynatrace/OneAgent-SDK-for-Python
  status: publishing
  pr: 2274
  notes: '2 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.
    Triage (2026-09-21): looks feasible, not a park candidate -- deferred only because of the
    CI backlog, not because it is closed. Checked the actual sdist (oneagent_sdk-1.5.2...tar.gz)
    and wheel (the py2.py3-none-manylinux1_x86_64 one) from PyPI: the wheel does bundle a
    prebuilt libonesdk_shared.so (237 KB, unstripped, x86_64-only, symbols like
    onesdk_tracer_get_outgoing_dynatrace_string_tag confirm it is Dynatrace''s real native
    stub), but that .so is not built from this repo -- setup.py only copies it in from a path
    given via the DT_PYSDK_CSDK_PATH env var, which is meant to point at a separately
    downloaded OneAgent SDK for C/C++ (a different, closed Dynatrace repo/download). Critically,
    setup.py explicitly supports building WITHOUT that env var set: get_dll_input_path() logs
    a warning and returns None instead of failing, so bdist_wheel simply produces a wheel with
    no native .so at all (confirmed by reading PostBuildExtCommand.run/get_inputs, which no-ops
    the copy when the DLL path lookup raises/returns nothing). At runtime, oneagent/__init__.py
    (initialize()/_try_init_noref()) wraps the whole nativeagent.initialize()/loadsdk() call in
    a broad except and falls back to SDKNullInterface (a pure-Python stub in
    _impl/native/sdknulliface.py) on ANY failure to load the native library -- this is
    documented behaviour, not a workaround: initialize()''s own docstring says "Even if
    initialization fails, a dummy SDK will be available so that SDK functions can be called
    but will do nothing." So a riscv64 wheel built without DT_PYSDK_CSDK_PATH set would install
    and import cleanly and run in permanent no-op mode (InitResult.STATUS_STUB_LOAD_ERROR) until
    a user supplies their own OneAgent SDK for C/C++ build via DT_PYSDK_CSDK_PATH/sdklibname --
    same "pure Python + optional runtime dlopen against a component the user''s own agent
    installation provides" shape as other already-handled packages in this repo. The wheel
    itself carries License: Apache License 2.0 with no separate EULA in dist-info, and the repo
    (github.com/Dynatrace/OneAgent-SDK-for-Python) is genuinely Apache-2.0 source, not a stub
    over a closed repo -- only the optional native accelerator is closed, and it degrades
    gracefully. Recommend a real port once CI capacity frees up: build with
    DT_PYSDK_CSDK_PATH unset/omitted so no closed binary is required, verify the resulting
    wheel installs and `import oneagent; oneagent.initialize()` returns
    STATUS_STUB_LOAD_ERROR/no-op rather than crashing.'
- pkg: amplpy
  version: 0.18.0
  home: http://ampl.com/
  repo: http://ampl.com/
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dust-riven
  version: 0.1.11
  home: https://github.com/Minh1billion/dust-riven
  repo: https://github.com/Minh1billion/dust-riven
  status: queued
  pr: null
  notes: '92 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-libecm
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fastcluster
  version: 1.3.0
  home: https://danifold.net
  repo: https://danifold.net
  status: published
  pr: 1347
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-neo-lzf
  version: 0.3.5
  home: https://github.com/FledgeXu/python-neo-lzf
  repo: https://github.com/FledgeXu/python-neo-lzf
  status: queued
  pr: null
  notes: '34 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zhinst-core
  version: 26.7.2.5
  home: https://docs.zhinst.com/labone_api_user_manual
  repo: https://docs.zhinst.com/labone_api_user_manual
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mapfile-parser
  version: 2.13.2
  home: https://github.com/Decompollaborate/mapfile_parser
  repo: https://github.com/Decompollaborate/mapfile_parser
  status: queued
  pr: null
  notes: '50 Linux wheels upstream (abi: cp314,cp38,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ruckig
  version: 0.19.4
  home: https://ruckig.com
  repo: https://github.com/pantor/ruckig.git
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1468
  notes: Draft, green. scikit-build-core + nanobind build, no runtime deps; matrix covers manylinux and musllinux riscv64.
- pkg: pyvrp
  version: 0.14.0
  home: https://pyvrp.org/
  repo: https://github.com/PyVRP/PyVRP
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: epam-indigo
  version: 1.46.0
  home: https://lifescience.opensource.epam.com/indigo/index.html
  repo: https://github.com/epam/indigo/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: biom-format
  version: 2.1.17
  home: http://www.biom-format.org
  repo: https://github.com/biocore/biom-format
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1791
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sentencex
  version: 1.0.30
  home: https://github.com/wikimedia/sentencex
  repo: https://github.com/wikimedia/sentencex
  status: queued
  pr: null
  notes: '27 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorflow-aarch64
  version: 2.16.1
  home: https://www.tensorflow.org/
  repo: https://www.tensorflow.org/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pupil-apriltags
  version: 1.0.4.post11
  home: https://github.com/pupil-labs/apriltags
  repo: https://github.com/pupil-labs/apriltags
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyboolector
  version: 3.2.4.19342042739
  home: https://github.com/boolector/boolector
  repo: https://github.com/boolector/boolector
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: greenify
  version: 0.5.0
  home: https://github.com/douban/greenify
  repo: https://github.com/douban/greenify
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rhino3dm
  version: 8.32.1
  home: https://github.com/mcneel/rhino3dm
  repo: https://github.com/mcneel/rhino3dm
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1794
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: genicam
  version: 1.6.0
  home: https://www.emva.org
  repo: https://www.emva.org
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cityseer
  version: 5.8.0
  home: https://cityseer.benchmarkurbanism.com/
  repo: https://github.com/benchmark-urbanism/cityseer-api
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: shazamio-core
  version: 1.2.0
  home: https://pypi.org/project/shazamio-core/
  repo: https://pypi.org/project/shazamio-core/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cuequivariance-ops-cu12
  version: 0.11.1
  home: https://developer.nvidia.com/cuequivariance
  repo: https://developer.nvidia.com/cuequivariance
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: djade
  version: 1.9.0
  home: https://github.com/adamchainz/djade
  repo: https://github.com/adamchainz/djade
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1466
  notes: 'PR #1466 green (build + cp312/cp313/cp314/cp314t + publish dry-run all pass), awaiting review. maturin bindings = "bin" CLI (Django template formatter), no pyo3, single riscv64gc-unknown-linux-gnu build covers every interpreter. No wheel-level test suite in the sdist; tested against README-documented formatting rules instead.'
- pkg: isaacteleop
  version: 1.4.142
  home: https://pypi.org/project/isaacteleop/
  repo: https://pypi.org/project/isaacteleop/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: breez-sdk-spark
  version: 0.24.1
  home: https://github.com/breez/spark-sdk
  repo: https://github.com/breez/spark-sdk
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lmcache
  version: 0.5.4
  home: https://docs.lmcache.ai
  repo: https://github.com/LMCache/LMCache
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: daal4py
  version: 2024.7.0
  home: https://github.com/IntelPython/daal4py
  repo: https://github.com/IntelPython/daal4py
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py310,py311,py312,py39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cppyy-cling
  version: 6.32.8
  home: https://root.cern.ch/cling
  repo: https://root.cern.ch/cling
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: eth-brownie
  version: 1.22.2
  home: https://github.com/eth-brownie/brownie
  repo: https://github.com/eth-brownie/brownie
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: isystem-connect
  version: 9.21.417.2
  home: http://www.isystem.com/downloads/winIDEA/SDK/iSYSTEM.Python.SDK/documentation/isystem-connect-api/index.html
  repo: http://www.isystem.com/downloads/winIDEA/SDK/iSYSTEM.Python.SDK/documentation/isystem-connect-api/index.html
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-mcqd
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aimrocks
  version: 0.5.2
  home: https://pypi.org/project/aimrocks/
  repo: https://pypi.org/project/aimrocks/
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openbabel-wheel
  version: 3.1.1.23
  home: https://github.com/njzjz/openbabel-wheel
  repo: https://github.com/njzjz/openbabel-wheel
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kingfisher-bin
  version: 2.1.0
  home: https://github.com/mongodb/kingfisher
  repo: https://github.com/mongodb/kingfisher
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pvxslibs
  version: 1.5.2
  home: https://epics-base.github.io/pvxs
  repo: https://epics-base.github.io/pvxs
  status: queued
  pr: null
  notes: '7 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: minorminer
  version: 0.2.22
  home: https://github.com/dwavesystems/minorminer
  repo: https://github.com/dwavesystems/minorminer
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1793
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: basemap
  version: 2.0.0
  home: https://github.com/matplotlib/basemap
  repo: https://github.com/matplotlib/basemap.git
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ai-parrot
  version: 0.28.1
  home: https://github.com/phenobarbital/ai-parrot
  repo: https://github.com/phenobarbital/ai-parrot
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-categories
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: islpy
  version: 2026.2.1
  home: https://documen.tician.de/islpy
  repo: https://github.com/inducer/islpy
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1792
  notes: '23 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysentry-rs
  version: 0.5.0
  home: https://github.com/nyudenkov/pysentry
  repo: https://github.com/nyudenkov/pysentry
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sase-core-rs
  version: 0.32.16
  home: https://github.com/sase-org/sase-core
  repo: https://github.com/sase-org/sase-core
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hpp-fcl
  version: 2.4.4
  home: https://github.com/cmake-wheel/hpp-fcl
  repo: https://github.com/cmake-wheel/hpp-fcl
  status: queued
  pr: null
  notes: '45 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-rtmidi
  version: 1.5.8
  home: https://github.com/SpotlightKid/python-rtmidi
  repo: https://gitlab.com/SpotlightKid/python-rtmidi/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1374
  notes: 'PR #1374 green (cp312-manylinux_riscv64 built, 8 passed/2 skipped, publish dry-run OK), awaiting review. Cython/C++ binding over RtMidi; links ALSA (excluded from the wheel via auditwheel --exclude, matching upstream) and JACK (built from jack2 1.9.22 source in before-all and bundled, matching upstream - libjack.so.0 confirmed in the wheel). Single cp312 job: upstream 1.5.8 predates 3.13/3.14. Patches pyproject.toml to declare license-files for jack2''s LGPL notice (meson-python has no PEP 639 default glob) - confirmed LICENSE.jack2 and LICENSE.md both land in dist-info/licenses/.'
- pkg: bitwarden-sdk
  version: 2.1.0
  home: https://bitwarden.com
  repo: https://bitwarden.com
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cuequivariance-ops-cu13
  version: 0.11.1
  home: https://developer.nvidia.com/cuequivariance
  repo: https://developer.nvidia.com/cuequivariance
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: infini-gram
  version: 2.6.0
  home: https://pypi.org/project/infini-gram/
  repo: https://pypi.org/project/infini-gram/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cargo-lambda
  version: 1.9.2
  home: https://github.com/cargo-lambda/cargo-lambda
  repo: https://github.com/cargo-lambda/cargo-lambda
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zuvloop
  version: 0.0.13
  home: https://github.com/Kludex/zuvloop
  repo: https://github.com/Kludex/zuvloop
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xmhuffman
  version: 0.3.0
  home: https://github.com/Hugoberry/xmhuffman-cython
  repo: https://github.com/Hugoberry/xmhuffman-cython
  status: published
  pr: 1469
  notes: 'PR #1469 open, CI running. Builds from checkout (not sdist): upstream sdist ships cythonized .c but not .pyx, so it cannot rebuild a wheel.'
- pkg: pyvista-zstd
  version: 0.4.1
  home: https://github.com/pyvista/pyvista-zstd
  repo: https://github.com/pyvista/pyvista-zstd
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: infisical-python
  version: 2.3.6
  home: https://github.com/Infisical/sdk/tree/main/crates/infisical-py#readme
  repo: https://github.com/infisical/sdk
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: torchrl
  version: 0.13.3
  home: https://github.com/pytorch/rl
  repo: https://github.com/pytorch/rl
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arro3-compute
  version: 0.8.2
  home: https://kylebarron.dev/arro3
  repo: https://kylebarron.dev/arro3
  status: queued
  pr: null
  notes: '50 Linux wheels upstream (abi: cp310,cp311,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-m4ri-m4rie
  version: 10.8.10.post1
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cuvs-cu12
  version: 26.8.1
  home: https://github.com/rapidsai/cuvs
  repo: https://github.com/rapidsai/cuvs
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deepsearch-glm
  version: 1.0.0
  home: https://pypi.org/project/deepsearch-glm/
  repo: https://pypi.org/project/deepsearch-glm/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sqlcipher3-wheels
  version: 0.5.7
  home: https://github.com/laggykiller/sqlcipher3
  repo: https://github.com/laggykiller/sqlcipher3
  status: queued
  pr: null
  notes: '80 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scikit-bio
  version: 0.7.3
  home: https://scikit.bio
  repo: https://github.com/scikit-bio/scikit-bio
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: p4p
  version: 4.2.2
  home: https://epics-base.github.io/p4p
  repo: https://epics-base.github.io/p4p
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xpress8
  version: 0.1.0
  home: https://github.com/Hugoberry/xpress8-python
  repo: https://github.com/Hugoberry/xpress8-python
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bx-python
  version: 0.15.1
  home: https://github.com/bxlab/bx-python
  repo: https://github.com/bxlab/bx-python
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1369
  notes: '34 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev. PR #1369 CI green: builds cp310-cp314+cp314t (numpy has no riscv64 wheel for cp315/cp315t/pp311 on our registry).'
- pkg: passagemath-bliss
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nono-py
  version: 0.16.0
  home: https://github.com/nolabs-ai/nono-py
  repo: https://github.com/nolabs-ai/nono-py
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fable-library
  version: 5.15.0
  home: https://fable.io
  repo: https://fable.io
  status: queued
  pr: null
  notes: '74 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: shiboken2
  version: 5.15.2.1
  home: https://www.pyside.org
  repo: https://www.pyside.org
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp27,cp35.cp36.cp37.cp38.cp39.cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fill-voids
  version: 2.1.2
  home: https://github.com/seung-lab/fill_voids/
  repo: https://github.com/seung-lab/fill_voids/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1472
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: accelerator-toolbox
  version: 0.8.0
  home: https://github.com/atcollab/at.git
  repo: https://github.com/atcollab/at.git
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tts
  version: 0.22.0
  home: https://github.com/coqui-ai/TTS
  repo: https://github.com/coqui-ai/TTS
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyxdameraulevenshtein
  version: 1.10.0
  home: https://github.com/lanl/pyxDamerauLevenshtein
  repo: https://github.com/lanl/pyxDamerauLevenshtein
  status: published
  pr: 1368
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cosmos-xenna
  version: 0.5.11
  home: https://pypi.org/project/cosmos-xenna/
  repo: https://pypi.org/project/cosmos-xenna/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: convex
  version: 0.7.0
  home: https://convex.dev
  repo: https://github.com/get-convex/convex-py
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: nrel-pysam
  version: 7.1.1.post1
  home: https://nrel-pysam.readthedocs.io
  repo: https://nrel-pysam.readthedocs.io
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fabio
  version: 2026.6.0
  home: http://www.silx.org
  repo: https://github.com/silx-kit/fabio
  status: published
  pr: 1372
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: whylogs-sketching
  version: 3.4.1.dev3
  home: https://github.com/whylabs/whylogs-sketching
  repo: https://github.com/whylabs/whylogs-sketching
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-libbraiding
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cached-ipaddress
  version: 1.1.2
  home: https://github.com/bluetooth-devices/cached-ipaddress
  repo: https://github.com/bluetooth-devices/cached-ipaddress
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openvino-genai
  version: 2026.3.1.0
  home: https://pypi.org/project/openvino-genai/
  repo: https://pypi.org/project/openvino-genai/
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: querysource
  version: 4.5.13
  home: https://github.com/phenobarbital/querysource/
  repo: https://github.com/phenobarbital/querysource/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pypylon
  version: '26.7'
  home: https://github.com/basler/pypylon
  repo: https://github.com/basler/pypylon
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: shioaji
  version: 1.7.4
  home: https://sinotrade.github.io/
  repo: https://github.com/Sinotrade/Shioaji
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp314,cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: php2json
  version: '0.2'
  home: https://github.com/mbachry/php2json
  repo: https://github.com/mbachry/php2json
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp27,cp36); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: piqp
  version: 0.6.4
  home: https://predict-epfl.github.io/piqp/
  repo: https://predict-epfl.github.io/piqp/
  status: queued
  pr: null
  notes: '42 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bittensor-core
  version: 0.1.3
  home: https://github.com/RaoFoundation/subtensor
  repo: https://github.com/RaoFoundation/subtensor
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rebrowser-playwright
  version: 1.52.0
  home: https://github.com/rebrowser/rebrowser-playwright-python
  repo: https://github.com/rebrowser/rebrowser-playwright-python
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tetgen
  version: 0.8.4
  home: https://github.com/pyvista/tetgen
  repo: https://github.com/pyvista/tetgen
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xbbg
  version: 1.4.11
  home: https://xbbg.org/
  repo: https://github.com/xbbg-org/xbbg
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pybammsolvers
  version: 0.9.1
  home: https://pypi.org/project/pybammsolvers/
  repo: https://pypi.org/project/pybammsolvers/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dwave-samplers
  version: 1.8.0
  home: https://docs.dwavequantum.com
  repo: https://github.com/dwavesystems/dwave-samplers
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lets-plot
  version: 4.11.0
  home: https://lets-plot.org
  repo: https://github.com/JetBrains/lets-plot
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysoem
  version: 1.1.13
  home: https://github.com/bnjmnp/pysoem
  repo: https://github.com/bnjmnp/pysoem
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scipp
  version: 26.8.0
  home: https://scipp.github.io/
  repo: https://github.com/scipp/scipp
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cassetter
  version: 0.10.0
  home: https://kludex.github.io/cassetter/
  repo: https://github.com/Kludex/cassetter
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-tdlib
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cmeel-tinyxml
  version: 2.6.2.3
  home: https://github.com/cmake-wheel/cmeel-tinyxml
  repo: https://github.com/cmake-wheel/cmeel-tinyxml
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: convertbng
  version: 1.0.0
  home: https://github.com/urschrei/convertbng
  repo: https://github.com/urschrei/convertbng
  status: published
  pr: 1373
  notes: '4 Linux wheels upstream (abi: cp310,cp311); no riscv64 on PyPI or pypi.riseproject.dev. PR #1373 open, CI green.'
- pkg: blackfire
  version: 2026.8.0
  home: https://blackfire.io
  repo: https://blackfire.io
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-modules
  version: 10.8.10.post1
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-buckygen
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onnxruntime-genai
  version: 0.15.2
  home: https://pypi.org/project/onnxruntime-genai/
  repo: https://pypi.org/project/onnxruntime-genai/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: miniupnpc
  version: 2.3.3
  home: https://miniupnp.tuxfamily.org/
  repo: https://miniupnp.tuxfamily.org/
  status: published
  pr: 1370
  notes: 'PR #1370 open, CI green (8/8 manylinux+musllinux riscv64 wheels, cp312-cp314t); awaiting review.'
- pkg: marin-iris-native
  version: 0.1.3
  home: https://github.com/marin-community/marin
  repo: https://github.com/marin-community/marin
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-benzene
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: networkit
  version: 11.2.1
  home: https://networkit.github.io/
  repo: https://github.com/networkit/networkit
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: geode-implicit
  version: 5.3.0
  home: https://pypi.org/project/Geode-Implicit/
  repo: https://pypi.org/project/Geode-Implicit/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spconv-cu126
  version: 2.3.8
  home: https://github.com/traveller59/spconv
  repo: https://github.com/traveller59/spconv
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyspeex-noise
  version: 2.0.1
  home: https://github.com/rhasspy/pyspeex-noise
  repo: https://github.com/rhasspy/pyspeex-noise
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1470
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ldpc
  version: 2.4.1
  home: https://software.roffe.eu/ldpc
  repo: https://software.roffe.eu/ldpc
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-lrslib
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mongo-ninja-python
  version: 1.11.1.7
  home: https://pypi.org/project/mongo-ninja-python/
  repo: https://pypi.org/project/mongo-ninja-python/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: clangd
  version: 22.1.1
  home: https://clangd.llvm.org/
  repo: https://github.com/jmpfar/clangd-wheel
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyqir
  version: 0.12.5
  home: https://github.com/qir-alliance/pyqir
  repo: https://github.com/qir-alliance/pyqir
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: openslide-bin
  version: 4.0.1.2
  home: https://openslide.org/
  repo: https://github.com/openslide/openslide-bin
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bitarray-hardbyte
  version: 2.3.8
  home: https://github.com/ilanschnell/bitarray
  repo: https://github.com/ilanschnell/bitarray
  status: published
  pr: 1371
  notes: 'PR #1371 open, CI running. No git tag for 2.3.8 upstream (untagged one-off hardbyte redistribution); pinned to commit 50b62ede.'
- pkg: histomicstk
  version: 1.4.0
  home: https://github.com/DigitalSlideArchive/HistomicsTK
  repo: https://github.com/DigitalSlideArchive/HistomicsTK
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opencolorio
  version: 2.5.2
  home: https://opencolorio.org/
  repo: https://opencolorio.org/
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rigour
  version: 2.4.0
  home: https://rigour.followthemoney.tech/
  repo: https://github.com/opensanctions/rigour.git
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1473
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-cmr
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bermuda
  version: 0.1.7
  home: https://github.com/napari/bermuda/blob/main/README.md
  repo: https://github.com/napari/bermuda
  status: queued
  pr: null
  notes: '34 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-svg-hush
  version: 0.3.0
  home: https://github.com/jams2/py-svg-hush
  repo: https://github.com/jams2/py-svg-hush
  status: queued
  pr: null
  notes: '76 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: secantusdb
  version: 0.6.0b16
  home: https://secantusdb.com/
  repo: https://github.com/jdrumgoole/SecantusDB
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-plantri
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: migrate-to-uv
  version: 0.12.0
  home: https://osprey-oss.github.io/migrate-to-uv/
  repo: https://github.com/osprey-oss/migrate-to-uv
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: vesin-torch
  version: 0.6.1
  home: https://github.com/Luthaf/vesin/
  repo: https://github.com/Luthaf/vesin/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: real-ladybug
  version: 0.15.3
  home: https://ladybugdb.com/
  repo: https://github.com/lbugdb/lbug
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: silx
  version: 3.1.1
  home: http://www.silx.org
  repo: https://github.com/silx-kit/silx
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyexiv2
  version: 2.16.0
  home: https://github.com/LeoHsiao1/pyexiv2
  repo: https://github.com/LeoHsiao1/pyexiv2
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sqlfluffrs
  version: 4.3.0
  home: https://www.sqlfluff.com
  repo: https://github.com/sqlfluff/sqlfluff
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pythran-openblas
  version: 0.3.6
  home: https://github.com/serge-sans-paille/pythran-openblas
  repo: https://github.com/serge-sans-paille/pythran-openblas
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp27,cp36,cp37,py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dnaio
  version: 1.2.4
  home: https://dnaio.readthedocs.io/
  repo: https://github.com/marcelm/dnaio/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1375
  notes: 'PR #1375 green (6 wheels cp310-cp314/cp314t, publish dry-run OK), awaiting review. Single-extension Cython package (dnaio._core); only runtime dep is xopen (pure Python). Matrix cp310-cp314t, matching upstream''s own published tags.'
- pkg: pixeltable-pgserver
  version: 0.6.0
  home: https://pixeltable.com/
  repo: https://github.com/pixeltable/pixeltable-pgserver
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mysql-mimic
  version: 3.0.4
  home: https://github.com/kelsin/mysql-mimic
  repo: https://github.com/kelsin/mysql-mimic
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: causalml
  version: 0.17.0
  home: https://github.com/uber/causalml
  repo: https://github.com/uber/causalml
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gstools-cython
  version: 1.2.0
  home: https://geostat-framework.org/#gstools
  repo: https://github.com/GeoStat-Framework/GSTools-Cython
  status: queued
  pr: null
  notes: '26 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tsv2py
  version: 0.8.0
  home: https://github.com/hunyadi/tsv2py
  repo: https://github.com/hunyadi/tsv2py
  status: published
  pr: 1471
  notes: '8 Linux wheels upstream (abi: cp310,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: formualizer
  version: 0.8.4
  home: https://github.com/PSU3D0/formualizer
  repo: https://github.com/PSU3D0/formualizer
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: open3d-cpu
  version: 0.19.0
  home: https://www.open3d.org
  repo: https://github.com/isl-org/Open3D
  status: parked
  pr: null
  notes: 'Not feasible, and not even producible under this name on riscv64 (gotcha 426, plus 436/366 via the base entry). Same upstream tree and version as the open3d entry, which is parked for a hard, unavoidable Embree blocker -- read that entry for the full evidence; this sibling inherits it unchanged, since on an arch with no CUDA the base package build already is the CPU-only build. On top of that the -cpu name is x86_64-only by construction, gotcha 426 shape exactly: python/setup.py appends "-cpu" to @PYPI_PACKAGE_NAME@ only when sys.platform starts with "linux" AND platform.machine() is in ("i386", "x86_64", "AMD64") AND @BUILD_CUDA_MODULE@ == OFF AND @BUILD_SYCL_MODULE@ == OFF, so a riscv64 build of the CPU-only config publishes as plain "open3d", exactly as aarch64 and macOS do. PyPI agrees: open3d-cpu 0.19.0 is 5x manylinux_2_31_x86_64 at 99.6 MB and nothing else, against 447.7 MB for the CUDA-bearing open3d x86_64 wheel and ~103 MB for its macOS wheels. Publishing a manylinux_riscv64 wheel named open3d-cpu would mean diverging setup.py from upstream in order to invent a name upstream uses on exactly one architecture -- gotcha 50 divergence with no gap closed. If open3d is ever unparked, port that entry and leave this one parked. No worktree/branch/PR created.'
- pkg: ciris-verify
  version: 14.1.0
  home: https://github.com/CIRISAI/CIRISVerify
  repo: https://github.com/CIRISAI/CIRISVerify
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-glucose
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: redislite
  version: 6.2.912183
  home: https://github.com/yahoo/redislite
  repo: https://github.com/yahoo/redislite
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sapien
  version: 3.0.3
  home: https://sapien.ucsd.edu
  repo: https://sapien.ucsd.edu
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: scikit-surprise
  version: 1.1.5
  home: https://surpriselib.com
  repo: https://github.com/NicolasHug/Surprise
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: edt
  version: 3.1.2
  home: https://github.com/seung-lab/euclidean-distance-transform-3d/
  repo: https://github.com/seung-lab/euclidean-distance-transform-3d/
  status: published
  pr: 1377
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. PR #1377 CI green.'
- pkg: convertertools
  version: 1.1.0
  home: https://convertertools.readthedocs.io
  repo: https://github.com/bluetooth-devices/convertertools
  status: queued
  pr: null
  notes: '31 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: riskcal
  version: 1.5.1
  home: https://github.com/interpretable-dp/riskcal.git
  repo: https://github.com/interpretable-dp/riskcal.git
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: multimodars
  version: 0.7.0
  home: https://github.com/yungselm/multimoda-rs
  repo: https://github.com/yungselm/multimoda-rs
  status: queued
  pr: null
  notes: '44 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tfa-nightly
  version: 0.23.0.dev20240415222534
  home: https://pypi.org/project/tfa-nightly/
  repo: https://pypi.org/project/tfa-nightly/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: essentia
  version: 2.1b6.dev1438
  home: http://essentia.upf.edu
  repo: https://github.com/MTG/essentia
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: riskfolio-lib
  version: 7.3.0
  home: https://github.com/dcajasn/Riskfolio-Lib
  repo: https://github.com/dcajasn/Riskfolio-Lib
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: slangtorch
  version: 1.3.22
  home: https://github.com/shader-slang/slang
  repo: https://github.com/shader-slang/slang
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: finlab
  version: 2.0.18
  home: https://finlab.finance
  repo: https://finlab.finance
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-kissat
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensor-grep
  version: 1.118.0
  home: https://pypi.org/project/tensor-grep/
  repo: https://pypi.org/project/tensor-grep/
  status: published
  pr: 1476
  notes: '1 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev. PyO3/maturin-action build, single cp311-abi3 wheel (matches upstream, which ships no free-threaded build either).'
- pkg: passagemath-rubiks
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysequoia
  version: 0.1.35
  home: https://github.com/wiktor-k/pysequoia
  repo: https://github.com/wiktor-k/pysequoia
  status: queued
  pr: null
  notes: '92 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: phonors
  version: 0.3.0
  home: https://github.com/phonopy/phonors
  repo: https://github.com/phonopy/phonors
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-groups
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: typedunits
  version: 0.0.2
  home: https://github.com/quantumlib/TypedUnits
  repo: https://github.com/quantumlib/TypedUnits
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1376
  notes: 'PR #1376 open, CI green (890 tests passed on cp312). Build-from-checkout; writes version/version.txt like upstream''s own release workflow.'
- pkg: pickle5
  version: 0.0.12
  home: https://github.com/pitrou/pickle5-backport
  repo: https://github.com/pitrou/pickle5-backport
  status: parked
  pr: null
  notes: 'Parked: pickle5 backports PEP 574 (pickle protocol 5) for Python versions that predate it. setup.py hard-pins python_requires=">=3.5, <3.8" and every release (0.0.1-0.0.12) has only ever published cp36/cp37 wheels, never cp38+ - not staleness, an enforced ceiling. Protocol 5 and pickle.PickleBuffer have been in the stdlib pickle module since Python 3.8, confirmed present on this host''s Python 3.9. This repo''s default interpreter matrix is cp312/cp313/cp314/cp314t (numpy''s own floor is 3.12), which has zero overlap with cp36/cp37, and the manylinux riscv64 image/runners (ubuntu-24.04-riscv) do not carry EOL Python 3.6/3.7 toolchains to build against in the first place. Building only cp36/cp37 wheels would produce artifacts unusable by anything else in this registry and unneeded since the stdlib already provides protocol 5 for every supported interpreter. Not worth porting.'
- pkg: quadrants
  version: 1.3.0
  home: https://github.com/Genesis-Embodied-AI/quadrants
  repo: https://github.com/Genesis-Embodied-AI/quadrants
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dipy
  version: 1.12.1
  home: https://dipy.org
  repo: https://github.com/dipy/dipy
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tiledbsoma
  version: 2.3.0
  home: https://github.com/single-cell-data/TileDB-SOMA/tree/main/apis/python
  repo: https://github.com/single-cell-data/TileDB-SOMA/tree/main/apis/python
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pycauset
  version: 0.6.1
  home: https://github.com/BrorH/pycauset
  repo: https://github.com/BrorH/pycauset
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: onnxruntime-openvino
  version: 1.24.1
  home: https://onnxruntime.ai
  repo: https://onnxruntime.ai
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spire-presentation
  version: 11.8.1
  home: https://www.e-iceblue.com
  repo: https://www.e-iceblue.com
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PARKED: same E-iceblue Spire.* family as spire-xls (parked this session with confirmed evidence of a closed, license-gated native engine). Zero sdist across all 20 PyPI releases; classifiers License :: Free To Use But Restricted / License :: Other/Proprietary License, no LICENSE file in dist-info, only a Temporary License purchase link - the standard E-iceblue paid-license-key model. manylinux2014_aarch64 (24.0 MB) and manylinux_2_34_x86_64 (25.3 MB) wheels are differently sized per arch, confirming a real per-arch compiled native engine rather than an arch-neutral payload - same shape spire-xls''s Spire.Xls.Base.so showed (Native-AOT .NET, license calls wired straight into the closed .so). github.com/eiceblue/Spire.Presentation-for-Python (their own examples link) holds only usage-example scripts, not engine source. No community/open-source edition. Genuinely infeasible.'
- pkg: passagemath-sympow
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: soldr
  version: 0.9.11
  home: https://github.com/zackees/soldr
  repo: https://github.com/zackees/soldr
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dotnetcore2
  version: 3.1.23
  home: https://github.com/dotnet/core
  repo: https://github.com/dotnet/core
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rqdatac
  version: 3.6.4
  home: https://www.ricequant.com/doc/rqdata-institutional#research-version
  repo: https://www.ricequant.com/doc/rqdata-institutional#research-version
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: agent-doc
  version: 0.35.323
  home: https://github.com/btakita/agent-doc
  repo: https://github.com/btakita/agent-doc
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dwave-optimization
  version: 0.7.3
  home: https://docs.dwavequantum.com
  repo: https://github.com/dwavesystems/dwave-optimization
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aspose-cells-python
  version: 26.8.0
  home: https://products.aspose.com/cells/
  repo: https://products.aspose.com/cells/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: managed-deepagents
  version: 0.6.1
  home: https://github.com/langchain-ai/managed-deepagents-sdk
  repo: https://github.com/langchain-ai/managed-deepagents-sdk
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: regions
  version: '0.12'
  home: https://github.com/astropy/regions
  repo: https://github.com/astropy/regions
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: elixcee
  version: 1.0.0
  home: https://github.com/kent-tokyo/elixcee
  repo: https://github.com/kent-tokyo/elixcee
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dscribe
  version: 2.1.2
  home: https://singroup.github.io/dscribe/latest/
  repo: https://singroup.github.io/dscribe
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cloudcheck
  version: 11.1.0
  home: https://github.com/blacklanternsecurity/cloudcheck
  repo: https://github.com/blacklanternsecurity/cloudcheck
  status: queued
  pr: null
  notes: '64 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kiss-icp
  version: 1.3.0
  home: https://github.com/PRBonn/kiss-icp
  repo: https://github.com/PRBonn/kiss-icp
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rook
  version: 0.1.209
  home: http://rookout.com/
  repo: http://rookout.com/
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp27,cp310,cp311,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-flirt
  version: 0.10.0
  home: https://github.com/williballenthin/lancelot/tree/master/pyflirt
  repo: https://github.com/williballenthin/lancelot/tree/master/pyflirt
  status: queued
  pr: null
  notes: '23 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cartoboost
  version: 0.3.11
  home: https://github.com/theculliganman/cartoboost
  repo: https://github.com/theculliganman/cartoboost
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: spire-doc
  version: 14.8.1
  home: https://www.e-iceblue.com
  repo: https://www.e-iceblue.com
  status: parked
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev. PARKED: same E-iceblue Spire.* family as spire-xls (parked this session with confirmed evidence of a closed, license-gated native engine). Zero sdist across all 24 PyPI releases; classifiers License :: Free To Use But Restricted / License :: Other/Proprietary License, no LICENSE file in dist-info, only a Temporary License purchase link - the standard E-iceblue paid-license-key model. manylinux2014_aarch64 (25.6 MB) and manylinux_2_34_x86_64 (45.0 MB) wheels are genuinely differently sized per arch, confirming a real per-arch compiled native engine rather than an arch-neutral payload - same shape spire-xls''s Spire.Xls.Base.so showed (113.9 MB x86_64 vs 48.2 MB aarch64, Native-AOT .NET, license calls wired straight into the closed .so). github.com/eiceblue/Spire.Doc-for-Python (their own examples link) holds only usage-example scripts, not engine source. No community/open-source edition. Genuinely infeasible.'
- pkg: saxoncee
  version: 13.0.0
  home: https://www.saxonica.com/saxon-c/index.xml
  repo: https://www.saxonica.com/saxon-c/index.xml
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: symbolic
  version: 13.9.0
  home: https://pypi.org/project/symbolic/
  repo: https://pypi.org/project/symbolic/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ai-dynamo-runtime
  version: 1.4.2
  home: https://github.com/ai-dynamo/dynamo
  repo: https://github.com/ai-dynamo/dynamo
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deebot-client
  version: 18.5.1
  home: https://deebot.readthedocs.io/
  repo: https://github.com/DeebotUniverse/client.py
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pygel3d
  version: 0.8.2
  home: https://github.com/janba/GEL
  repo: https://github.com/janba/GEL
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: transformer-engine-cu13
  version: 2.18.0
  home: https://pypi.org/project/transformer-engine-cu13/
  repo: https://pypi.org/project/transformer-engine-cu13/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-sugiyama
  version: 0.5.3
  home: https://pypi.org/project/fast-sugiyama/
  repo: https://pypi.org/project/fast-sugiyama/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyresample
  version: 1.35.0
  home: https://github.com/pytroll/pyresample
  repo: https://github.com/pytroll/pyresample
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tinybird-toolset
  version: 2.6.0
  home: https://gitlab.com/tinybird/clickhouse-toolset
  repo: https://gitlab.com/tinybird/clickhouse-toolset
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: swiglpk
  version: 5.0.13
  home: https://github.com/biosustain/swiglpk
  repo: https://github.com/biosustain/swiglpk
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1379
  notes: 'SWIG bindings for GLPK (GPLv3); GLPK has no riscv64 distro package, so build-swiglpk.yml builds it from the pinned upstream tarball (ftp.gnu.org) in CIBW_BEFORE_ALL_LINUX, with swig/gmp-devel from Rocky 10''s repos. gpl_sources job collects gcc/gmp RPM sources plus the pinned GLPK tarball. PR #1379 open, watching CI.'
- pkg: dolphin-memory-engine
  version: 1.3.1
  home: https://github.com/henriquegemignani/py-dolphin-memory-engine
  repo: https://github.com/henriquegemignani/py-dolphin-memory-engine
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tenseal
  version: 0.3.17
  home: https://github.com/OpenMined/TenSEAL
  repo: https://github.com/OpenMined/TenSEAL
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-mecab-ko
  version: 1.3.7
  home: https://github.com/jonghwanhyeon/python-mecab-ko
  repo: https://github.com/jonghwanhyeon/python-mecab-ko
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1486
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev. pybind11 binding linking libmecab (mecab-ko fork) via mecab-config; upstream''s own pyproject.toml before-all (scripts/cibw_before_all.sh) builds mecab-ko, unchanged. Patch 0001 adds LICENSE.mecab-ko (mecab-ko''s own BSD text) since auditwheel vendors libmecab into the wheel with no licence text otherwise, same tri-license gap already resolved for mecab/mecab-python3. Patch 0002 swaps the upstream script''s config.guess/config.sub download (savannah.gnu.org 404s from our riscv64 runners) for the manylinux image''s own automake copies. PR #1486 green (4 wheels built cp312-cp314t, 7 tests passed, publish dry-run OK), awaiting review.'
- pkg: matrix-synapse
  version: 1.160.0
  home: https://element-hq.github.io/synapse/latest
  repo: https://github.com/element-hq/synapse
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opengeode-inspector
  version: 6.12.1
  home: https://pypi.org/project/OpenGeode-Inspector/
  repo: https://pypi.org/project/OpenGeode-Inspector/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: protoc-wheel-0
  version: '30.2'
  home: https://gitlab.com/dfritzsche/create-protoc-wheel
  repo: https://gitlab.com/dfritzsche/create-protoc-wheel
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py2.py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hugr
  version: 0.18.5
  home: https://github.com/quantinuum/hugr/tree/main/hugr-py
  repo: https://github.com/quantinuum/hugr/tree/main/hugr-py
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: eventio
  version: 2.1.1
  home: https://github.com/cta-observatory/pyeventio
  repo: https://github.com/cta-observatory/pyeventio
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: poselib
  version: 2.0.5
  home: https://pypi.org/project/poselib/
  repo: https://pypi.org/project/poselib/
  status: published
  pr: 1487
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: chematic
  version: 1.0.1
  home: https://kent-tokyo.github.io/chematic/
  repo: https://github.com/kent-tokyo/chematic
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: python-kadmin-rs
  version: 0.7.4
  home: https://github.com/authentik-labs/kadmin-rs
  repo: https://github.com/authentik-labs/kadmin-rs.git
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: viennarna
  version: 2.7.2
  home: https://www.tbi.univie.ac.at/RNA
  repo: https://www.tbi.univie.ac.at/RNA
  status: queued
  pr: null
  notes: '18 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: memory-allocator
  version: 0.2.0
  home: https://github.com/sagemath/memory_allocator
  repo: https://github.com/sagemath/memory_allocator
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1378
  notes: '16 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: anise
  version: 0.10.6
  home: https://nyxspace.com/
  repo: https://github.com/nyx-space/anise
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rustpy-xlsxwriter
  version: 0.6.1
  home: https://github.com/rahmadafandi/rustpy-xlsxwriter
  repo: https://github.com/rahmadafandi/rustpy-xlsxwriter
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1475
  notes: '92 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev. build-from-checkout, cp312-cp314+cp314t (no abi3 feature, each interpreter its own build). pandas+polars (via polars-runtime-32/64) both on our registry; cp314t test leg skipped (no pandas riscv64 wheel there yet). Needed pyarrow added to CIBW_TEST_REQUIRES: without it pandas 3''s __arrow_c_stream__ falls back to a path that serializes NaT as 0001-01-01 instead of empty (reproduced locally off the real PyPI wheel, arch-independent). CI green, PR #1475 open for review.'
- pkg: kasa-crypt
  version: 1.1.0
  home: https://github.com/python-kasa/kasa-crypt
  repo: https://github.com/python-kasa/kasa-crypt
  status: queued
  pr: null
  notes: '37 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyfaup-rs
  version: 0.4.18
  home: https://github.com/ail-project/faup-rs
  repo: https://github.com/ail-project/faup-rs
  status: publishing
  pr: https://github.com/riseproject-dev/python-wheels/pull/1474
  notes: '104 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green: cp312/cp313/cp314/cp314t on manylinux_riscv64, publish dry-run OK.'
- pkg: cyseq
  version: 0.1.3
  home: https://github.com/aboutcode-org/cyseq
  repo: https://github.com/aboutcode-org/cyseq
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: k-means-constrained
  version: 0.9.1
  home: https://github.com/joshlk/k-means-constrained
  repo: https://github.com/joshlk/k-means-constrained
  status: blocked-on-dependency
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev. Own build would be trivial (setuptools + Cython>=3.1 + numpy>=2.0,<3, three small .pyx extensions, no C++ complexity, confirmed against the real v0.9.1 sdist/checkout) but the package hard-depends on ortools at runtime: k_means_constrained/__init__.py imports k_means_constrained_.py, which does an unconditional module-level `from ortools.graph.python.min_cost_flow import SimpleMinCostFlow` (line 30) -- so `import k_means_constrained` itself hard-fails without ortools installed, not an optional accelerator. ortools has zero riscv64 wheels on PyPI or pypi.riseproject.dev (registry returns 404 for the whole project), and this repo already assessed porting ortools itself and parked it as disproportionate in scope (see the ortools entry above and gotcha 214, feasibility-and-triage.md): BUILD_DEPS=ON FetchContents abseil/protobuf/Eigen3/re2/Boost/SoPlex/SCIP/the COIN-OR suite/HiGHS/PDLP/BOP on top of ortools'' own CP-SAT/routing core and SWIG bindings, read as a multi-day effort with zero of those sub-builds proven on riscv64 here except abseil/protobuf/HiGHS. Note this corrects an initial premise that scikit-learn was a hard dependency here: scikit-learn is only a requirements-dev.txt/test dependency (tests/test_kmeans_constrained_from_sklearn.py uses make_blobs/v_measure_score/load_iris) -- the package''s own sklearn_import/ subpackage is a vendored copy, not a real scikit-learn import, and requirements.txt''s actual runtime deps are ortools, scipy, numpy, six, joblib. k-means-constrained cannot be sequenced ahead of ortools; revisit once ortools (or a lighter alternative min-cost-flow solver it could switch to) lands on the registry. No PR created; a worktree/branch was opened for research, then removed once the ortools blocker was confirmed against the real upstream checkout -- no CI cycle burned.'
- pkg: cargo-xwin
  version: 0.23.1
  home: https://pypi.org/project/cargo-xwin/
  repo: https://pypi.org/project/cargo-xwin/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bauplan
  version: 0.2.0
  home: https://www.bauplanlabs.com/
  repo: https://github.com/BauplanLabs/bauplan/
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dclab
  version: 0.71.10
  home: https://dclab.readthedocs.io/en/stable/
  repo: https://github.com/DC-Analysis/dclab
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pytango
  version: 10.3.1
  home: https://www.tango-controls.org/
  repo: https://gitlab.com/tango-controls/pytango.git
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-histogram
  version: '0.14'
  home: https://github.com/astrofrog/fast-histogram
  repo: https://github.com/astrofrog/fast-histogram
  status: published
  pr: 1381
  notes: '4 Linux wheels upstream (abi: cp39,pp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green.'
- pkg: rsloop
  version: 0.1.46
  home: https://github.com/RustedBytes/rsloop
  repo: https://github.com/RustedBytes/rsloop
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: earshot
  version: 1.2.2
  home: https://github.com/pykeio/earshot/tree/main/bindings/py
  repo: https://github.com/pykeio/earshot
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pybgpkit-parser
  version: 0.18.0
  home: https://pypi.org/project/pybgpkit-parser/
  repo: https://pypi.org/project/pybgpkit-parser/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyxcp
  version: 0.29.18
  home: https://github.com/christoph2/pyxcp
  repo: https://github.com/christoph2/pyxcp
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-walk
  version: 0.2.2
  home: https://pypi.org/project/fast-walk/
  repo: https://pypi.org/project/fast-walk/
  status: published
  pr: 1485
  notes: '34 Linux wheels upstream (abi: cp313,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: librmm-cu13
  version: 26.8.0
  home: https://github.com/rapidsai/rmm
  repo: https://github.com/rapidsai/rmm
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-standard-no-symbolics
  version: 10.8.10.post1
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dbmate-bin
  version: 2.35.1
  home: https://github.com/justin-yan/pybin
  repo: https://github.com/justin-yan/pybin
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysplashsurf
  version: 0.14.1.0
  home: https://splashsurf.physics-simulation.org/
  repo: https://github.com/InteractiveComputerGraphics/splashsurf.git
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: strix-agent
  version: 1.6.1
  home: https://pypi.org/project/strix-agent/
  repo: https://pypi.org/project/strix-agent/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cylp
  version: 0.94.0
  home: https://coin-or.github.io/CyLP
  repo: https://github.com/coin-or/CyLP
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ur-rtde
  version: 1.6.5
  home: https://gitlab.com/sdurobotics/ur_rtde
  repo: https://gitlab.com/sdurobotics/ur_rtde.git
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp37,cp38,cp39,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dcc-mcp-server
  version: 0.20.22
  home: https://github.com/dcc-mcp/dcc-mcp-core
  repo: https://github.com/dcc-mcp/dcc-mcp-core
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pycrc32
  version: 0.3.0
  home: https://github.com/code-inflation/pycrc32
  repo: https://github.com/code-inflation/pycrc32
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1380
  notes: '86 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39,pp310,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: robotpy-wpiutil
  version: 2026.2.2
  home: https://github.com/robotpy/mostrobotpy
  repo: https://github.com/robotpy/mostrobotpy
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rmm-cu13
  version: 26.8.0
  home: https://github.com/rapidsai/rmm
  repo: https://github.com/rapidsai/rmm
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymonocypher
  version: 4.0.3.3
  home: https://github.com/jetperch/pymonocypher
  repo: https://github.com/jetperch/pymonocypher/
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1477
  notes: 'PR #1477 green (cp312/cp313/cp314/cp314t built and tested on riscv64, 29 passed on cp312, publish dry-run OK), awaiting review. sdist->bdist shape mirroring upstream packaging.yml; Cython-generated C extension binding the bundled Monocypher library, no external native deps. numpy is test-only (test_monocypher.py cross-checks against hashlib), pulled from our registry via PIP_EXTRA_INDEX_URL/PIP_ONLY_BINARY. 6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: codebuddy-agent-sdk
  version: 0.3.250
  home: https://pypi.org/project/codebuddy-agent-sdk/
  repo: https://pypi.org/project/codebuddy-agent-sdk/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: emcache
  version: 1.3.3
  home: http://github.com/emcache/emcache
  repo: http://github.com/emcache/emcache
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-plaid
  version: 1.7.0.2110
  home: https://github.com/lightonai/fast-plaid
  repo: https://github.com/lightonai/fast-plaid
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: planegcs
  version: 0.8.0
  home: https://github.com/spookylukey/planegcs
  repo: https://github.com/spookylukey/planegcs
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: guardpycfn
  version: 0.1.0
  home: https://pypi.org/project/guardpycfn/
  repo: https://pypi.org/project/guardpycfn/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyfastx
  version: 2.3.1
  home: https://github.com/lmdu/pyfastx
  repo: https://github.com/lmdu/pyfastx
  status: published
  pr: 1488
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lzallright
  version: 0.2.6
  home: https://vlaci.github.io/lzallright
  repo: https://github.com/vlaci/lzallright
  status: published
  pr: 1384
  notes: '10 Linux wheels upstream (abi: cp313,cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pysjef
  version: 1.45.31
  home: https://pypi.org/project/pysjef/
  repo: https://pypi.org/project/pysjef/
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: rpi-gpio
  version: 0.7.1
  home: http://sourceforge.net/projects/raspberry-gpio-python/
  repo: http://sourceforge.net/projects/raspberry-gpio-python/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp27,cp310,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opengeode-io
  version: 7.8.2
  home: https://pypi.org/project/OpenGeode-IO/
  repo: https://pypi.org/project/OpenGeode-IO/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bigdl-nano
  version: 2.4.0
  home: https://github.com/intel-analytics/BigDL
  repo: https://github.com/intel-analytics/BigDL
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: private-attribute-cpp
  version: 2.1.12
  home: https://github.com/Locked-chess-official/private_attribute_cpp
  repo: https://github.com/Locked-chess-official/private_attribute_cpp
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: verovio
  version: 6.3.0
  home: https://www.verovio.org
  repo: https://github.com/rism-digital/verovio
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-combinat
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hops-deltalake
  version: 1.4.0.post121
  home: https://github.com/logicalclocks/delta-rs
  repo: https://github.com/delta-io/delta-rs/tree/main/python/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: browser-act-cli
  version: 1.4.2
  home: https://www.browseract.com
  repo: https://www.browseract.com
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cgal
  version: 6.0.1.post202410241521
  home: https://github.com/CGAL/cgal-swig-bindings
  repo: https://github.com/CGAL/cgal-swig-bindings
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xy
  version: 0.0.7
  home: https://github.com/reflex-dev/xy
  repo: https://github.com/reflex-dev/xy
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tensorflow-io-nightly
  version: 0.31.0.dev20230309180344
  home: https://github.com/tensorflow/io
  repo: https://github.com/tensorflow/io
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: blasthttp
  version: 0.10.0
  home: https://pypi.org/project/blasthttp/
  repo: https://pypi.org/project/blasthttp/
  status: queued
  pr: null
  notes: '50 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyrodigal
  version: 3.7.1
  home: https://pyrodigal.readthedocs.io/en/stable/
  repo: https://github.com/althonos/pyrodigal/issues
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1383
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: dedupe
  version: 3.0.3
  home: https://github.com/dedupeio/dedupe
  repo: https://github.com/dedupeio/dedupe
  status: queued
  pr: null
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39,pp310,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyinstrument-cext
  version: 0.2.4
  home: https://github.com/joerick/pyinstrument_cext
  repo: https://github.com/joerick/pyinstrument_cext
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp27,cp35,cp36,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ukkonen
  version: 1.1.0
  home: https://www.github.com/asottile/ukkonen
  repo: https://www.github.com/asottile/ukkonen
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: puhu
  version: 0.4.1
  home: https://github.com/bgunebakan/puhu
  repo: https://github.com/bgunebakan/puhu
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: markdown-it-pyrs
  version: 0.4.0
  home: https://github.com/chrisjsewell/markdown-it-pyrs
  repo: https://github.com/chrisjsewell/markdown-it-pyrs
  status: published
  pr: 1382
  notes: '30 Linux wheels upstream (abi: cp310,cp311,cp312,cp39,pp310,pp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green: cp312/cp313 built and tested, publish dry-ran cleanly. cp314 dropped: pyo3 0.19.2 segfaults against CPython 3.14 (gotcha 322); cp314t dropped: pyo3 0.19.2 predates Py_GIL_DISABLED.'
- pkg: geode-background
  version: 9.14.1
  home: https://pypi.org/project/Geode-Background/
  repo: https://pypi.org/project/Geode-Background/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyroomacoustics
  version: 0.10.1
  home: https://github.com/LCAV/pyroomacoustics
  repo: https://github.com/LCAV/pyroomacoustics.git
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: epaper-dithering
  version: 6.0.0
  home: https://opendisplay.org
  repo: https://github.com/OpenDisplay-org/epaper-dithering
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: filprofiler
  version: 2024.11.2
  home: https://pythonspeed.com/fil/
  repo: https://pythonspeed.com/fil/
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: instanttensor
  version: 0.1.9
  home: https://pypi.org/project/instanttensor/
  repo: https://pypi.org/project/instanttensor/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: torch-xla
  version: 2.9.0
  home: https://github.com/pytorch/xla
  repo: https://github.com/pytorch/xla
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: loudness
  version: 0.2.0
  home: https://github.com/iver56/loudness
  repo: https://github.com/iver56/loudness
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aspose-diagram-python
  version: '26.8'
  home: https://products.aspose.com/diagram/
  repo: https://products.aspose.com/diagram/
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-json-repair
  version: 0.2.3
  home: https://github.com/Rusty-Ports/fast_json_repair
  repo: https://github.com/Rusty-Ports/fast_json_repair
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: asyncdb
  version: 2.15.10
  home: https://github.com/phenobarbital/asyncdb
  repo: https://github.com/phenobarbital/asyncdb
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aspose-cad
  version: 26.7.0
  home: https://products.aspose.com/cad/python
  repo: https://products.aspose.com/cad/python
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: coal-library
  version: 3.0.1
  home: https://github.com/cmake-wheel/coal
  repo: https://github.com/cmake-wheel/coal
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ciris-server
  version: 0.5.196
  home: https://github.com/CIRISAI/CIRISServer
  repo: https://github.com/CIRISAI/CIRISServer
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: marin-dupekit-native
  version: 0.1.7.dev32497398902
  home: https://github.com/marin-community/marin
  repo: https://github.com/marin-community/marin
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyside6-qtads
  version: 5.0.0.2
  home: https://github.com/mborgerson/pyside6_qtads/
  repo: https://github.com/mborgerson/pyside6_qtads/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-lib3mf
  version: 2.5.0
  home: https://pypi.org/project/py-lib3mf/
  repo: https://pypi.org/project/py-lib3mf/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: harmonypy
  version: 2.0.0
  home: https://github.com/slowkow/harmonypy
  repo: https://github.com/slowkow/harmonypy
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyoxipng
  version: 9.1.1
  home: https://github.com/nfrasser/pyoxipng
  repo: https://github.com/nfrasser/pyoxipng
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1478
  notes: 'PR #1478 open. PyO3 binding, no abi3 (per-interpreter cp312/cp313/cp314/cp314t matrix); Cargo.lock is committed so cibuildwheel builds straight from the git checkout, no separate sdist job. 30 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: robotpy-wpimath
  version: 2026.2.2
  home: https://github.com/robotpy/mostrobotpy
  repo: https://github.com/robotpy/mostrobotpy
  status: queued
  pr: null
  notes: '9 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pkscreener
  version: 0.46.20260615.927
  home: https://github.com/pkjmesra/pkscreener
  repo: https://github.com/pkjmesra/pkscreener
  status: queued
  pr: null
  notes: '1 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: torch-directml
  version: 0.2.5.dev240914
  home: https://github.com/microsoft/directml
  repo: https://github.com/microsoft/directml
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: evtx
  version: 0.12.1
  home: https://pypi.org/project/evtx/
  repo: https://pypi.org/project/evtx/
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pycolmap-cuda12
  version: 4.2.0
  home: https://github.com/colmap/colmap
  repo: https://github.com/colmap/colmap
  status: parked
  pr: null
  notes: 'Parked: pycolmap''s CUDA 12 GPU-accelerated build is fundamentally NVIDIA-GPU-only (links the CUDA 12/13 toolkit''s runtime/driver libraries at build and/or import time). NVIDIA does not ship a CUDA toolkit for riscv64 at all today (see the onnxruntime-gpu/cupy-cuda12x/cuda-tile entries for the same finding), so there is no toolchain to build this against and no driver for a resulting wheel to load on riscv64 hardware even if it did build. Revisit if/when NVIDIA ships a riscv64 CUDA toolkit.'
- pkg: passagemath-tachyon
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp311,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: duckdb-extension-httpfs
  version: 1.5.5
  home: https://github.com/santosh-d3vpl3x/duckdb_extensions#readme
  repo: https://github.com/santosh-d3vpl3x/duckdb_extensions
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: efel
  version: 5.7.34
  home: https://github.com/openbraininstitute/eFEL
  repo: https://github.com/openbraininstitute/eFEL
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: auto-gptq
  version: 0.7.1
  home: https://github.com/PanQiWei/AutoGPTQ
  repo: https://github.com/PanQiWei/AutoGPTQ
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: faster-fifo
  version: 1.5.2
  home: https://github.com/alex-petrenko/faster-fifo
  repo: https://github.com/alex-petrenko/faster-fifo
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1479
  notes: '20 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: torchmonarch
  version: 0.6.0
  home: https://meta-pytorch.org/monarch/
  repo: https://github.com/meta-pytorch/monarch
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tiledb
  version: 0.36.1
  home: https://github.com/TileDB-Inc/TileDB-Py
  repo: https://github.com/TileDB-Inc/TileDB-Py
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xtcocotools
  version: 1.14.3
  home: https://github.com/jin-s13/xtcocoapi
  repo: https://github.com/jin-s13/xtcocoapi
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cysystemd
  version: 2.0.5
  home: https://github.com/mosquito/cysystemd
  repo: https://github.com/mosquito/cysystemd
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1480
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pybigtools
  version: 0.3.0
  home: https://github.com/jackh726/bigtools
  repo: https://github.com/jackh726/bigtools
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: home-assistant-chip-core
  version: 2025.7.0
  home: https://github.com/project-chip/connectedhomeip
  repo: https://github.com/project-chip/connectedhomeip
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp37); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: akquant
  version: 0.3.55
  home: https://github.com/akfamily/akquant
  repo: https://github.com/akfamily/akquant
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: aiotieba
  version: 4.7.1
  home: https://aiotieba.cc/
  repo: https://github.com/lumina37/aiotieba/
  status: queued
  pr: null
  notes: '14 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,pp310,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: box2d
  version: 2.3.10
  home: http://github.com/pybox2d/pybox2d
  repo: http://github.com/pybox2d/pybox2d
  status: queued
  pr: null
  notes: '48 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp35,cp36,cp37,cp38,cp39,pp310,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: flexivrdk
  version: 2.1.0
  home: https://pypi.org/project/flexivrdk/
  repo: https://pypi.org/project/flexivrdk/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp312,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyrush
  version: 1.0.13
  home: https://github.com/jianlins/PyRuSH
  repo: https://github.com/jianlins/PyRuSH
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: iree-base-compiler
  version: 3.11.0
  home: https://iree.dev/
  repo: https://github.com/iree-org/iree
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: demoparser2
  version: 0.42.0
  home: https://pypi.org/project/demoparser2/
  repo: https://pypi.org/project/demoparser2/
  status: published
  pr: 1483
  notes: '16 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp37,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tdewolff-minify
  version: 2.24.12
  home: https://github.com/tdewolff/minify
  repo: https://github.com/tdewolff/minify
  status: queued
  pr: null
  notes: '32 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lgpio
  version: 0.2.2.0
  home: http://abyz.me.uk/lg/py_lgpio.html
  repo: http://abyz.me.uk/lg/py_lgpio.html
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: moviepilot-rust
  version: 0.3.3
  home: https://pypi.org/project/moviepilot-rust/
  repo: https://pypi.org/project/moviepilot-rust/
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bomiot-asgi
  version: 0.6.20
  home: https://pypi.org/project/bomiot-asgi/
  repo: https://pypi.org/project/bomiot-asgi/
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: xa11y
  version: 0.13.0
  home: https://github.com/xa11y/xa11y
  repo: https://github.com/xa11y/xa11y
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: sciqlopplots
  version: 0.35.1
  home: https://github.com/SciQLop/SciQLopPlots
  repo: https://github.com/SciQLop/SciQLopPlots
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: endec
  version: 0.5.6
  home: https://github.com/fluxth/endec
  repo: https://github.com/fluxth/endec
  status: queued
  pr: null
  notes: '80 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pykrige
  version: 1.7.3
  home: https://github.com/GeoStat-Framework/PyKrige
  repo: https://github.com/GeoStat-Framework/PyKrige
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: opengeode-geosciences
  version: 9.7.2
  home: https://pypi.org/project/OpenGeode-Geosciences/
  repo: https://pypi.org/project/OpenGeode-Geosciences/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cffsubr
  version: 0.4.0
  home: https://github.com/adobe-type-tools/cffsubr
  repo: https://github.com/adobe-type-tools/cffsubr
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tblite
  version: 0.7.0
  home: https://tblite.readthedocs.io
  repo: https://github.com/tblite/tblite
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39,pp310,pp311,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pcapy-ng
  version: 2.1.0
  home: https://github.com/stamparm/pcapy-ng/
  repo: https://github.com/stamparm/pcapy-ng/
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pqcrypto
  version: 1.0.0
  home: https://github.com/backbone-hq/pqcrypto
  repo: https://github.com/backbone-hq/pqcrypto
  status: published
  pr: 1484
  notes: '4 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: louvain
  version: 0.8.2
  home: https://github.com/vtraag/louvain
  repo: https://github.com/vtraag/louvain
  status: queued
  pr: null
  notes: '34 Linux wheels upstream (abi: cp310,cp311,cp37,cp38,cp39,pp37,pp38,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymetis
  version: 2025.2.2
  home: https://documen.tician.de/pymetis
  repo: https://github.com/inducer/pymetis
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyavd-utils
  version: 0.0.7
  home: https://avd.arista.com
  repo: https://github.com/aristanetworks/avd
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fast-query-parsers
  version: 1.0.3
  home: https://github.com/starlite-api/fast-query-parsers
  repo: https://github.com/starlite-api/fast-query-parsers
  status: queued
  pr: null
  notes: '11 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pkgconf
  version: 3.0.1.post0
  home: https://pkgconf-pypi.readthedocs.io/en/latest/
  repo: https://github.com/pypackaging-native/pkgconf-pypi
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gtsam-develop
  version: 4.3a2.dev202609030306
  home: https://gtsam.org/
  repo: https://gtsam.org/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hydride
  version: 1.2.3
  home: https://hydride.biotite-python.org
  repo: https://github.com/biotite-dev/hydride
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp310,cp311,cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: transformations
  version: 2026.8.8
  home: https://www.cgohlke.com
  repo: https://github.com/cgohlke/transformations
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp314,cp315); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: biscuit-python
  version: 0.4.0
  home: https://biscuitsec.org/usage/python
  repo: https://github.com/biscuit-auth/biscuit-python
  status: queued
  pr: null
  notes: '102 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp37,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cdshealpix
  version: 0.8.1
  home: https://github.com/cds-astro/cds-healpix-python
  repo: https://github.com/cds-astro/cds-healpix-python
  status: queued
  pr: null
  notes: '36 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: cplex
  version: 22.2.0.1
  home: https://www.ibm.com/products/ilog-cplex-optimization-studio
  repo: https://www.ibm.com/products/ilog-cplex-optimization-studio
  status: parked
  pr: null
  notes: 'Not feasible: cplex is IBM''s proprietary CPLEX Callable Library engine (the Community Edition
    of ILOG CPLEX Optimization Studio), so there is no source to build from, no riscv64 build
    from the vendor at any layer, and its own License Information terms independently bar
    redistribution even if there were (the gurobipy/gotcha 453 shape -- per-arch closed math
    kernel -- same closed-payload family as gotchas 372/376/431/450, this time on a third
    vendor). Verified against the real artifacts, not inferred. PYPI: 22.2.0.1 publishes 25
    wheels (cp310-cp314 x macosx_11_0_arm64/manylinux2014_aarch64/manylinux2014_ppc64le/manylinu
    x2014_x86_64/win_amd64), and the project has published 273 files across 22 releases with
    ZERO sdists ever, so per gotcha 431 the wheel is the only evidence; home/repo/Project-URL
    are all IBM''s marketing page https://www.ibm.com/products/ilog-cplex-optimization-studio
    (gotcha 372''s hdbcli tell) and METADATA says License: Proprietary. WHAT IS IN THE WHEEL:
    unzipping cplex-22.2.0.1-cp312-cp312-manylinux2014_x86_64.whl (52.2 MB) gives 41 files, no
    .c/.h/.pyx anywhere, and three real closed payloads: libcplex2220.so (duplicated at 27 MB
    each under both cplex-22.2.0.1.data/scripts/ and .data/purelib/cplex/_internal/, ELF x86-64,
    not stripped), .data/scripts/cpoptimizer (55 MB, the CP Optimizer constraint-programming
    engine) and .data/scripts/oplcpolpgen (45 MB, the OPL/CPO code generator), plus a 956 KB
    per-interpreter py312_cplex2220.so Cython glue module and a 284 KB standalone cpxchecklic
    license-checking executable. cplex/_internal/_pycplex.py imports the Cython extension ->
    libcplex2220.so at import time, so with no riscv64 engine there is no importable wheel at
    all (the gurobipy/saxonche shape, gotcha 450). PER-ARCH CLOSED MATH KERNEL (gotcha 453
    confirmed on a third vendor): the same libcplex2220.so is 27 MB on x86_64 against 18 MB on
    aarch64 (both cp312) -- strings show why: the x86_64 build statically links Intel oneMKL (an
    AVX/AVX512_EH/AVX2_E1 dispatch table, "Intel oneMKL INTERNAL ERROR: Insufficient workspace")
    while the aarch64 build carries no MKL strings at all and instead an arm64-suffixed path
    (/cplex/bin/arm64_linux); the x86_64 build path is itself a closed-vendor-Jenkins tell
    (gotcha 431): /SoftwareFactory/hudson_slave_8080/builds/workspace/FP222.cplex_li-
    distrib.x86-64_linux/.... Neither .so contains a single riscv/rv64gc string (0 hits, both
    scanned in full). NO OPEN-SOURCE OR CPU-GENERIC ENGINE: IBM''s own IBMDecisionOptimization
    GitHub org has 52 public repos (docplex, docplex-doc, tutorials, DO-Samples, cpofzn, docker-
    install, cplex_code_examples, ...) and every one is a Python/Java/C#/R modeling-layer
    binding, a Jupyter/notebook sample, or a Dockerfile that installs the closed CPLEX binary
    into a container -- none contains the CPLEX or CP Optimizer solving engine itself (the
    gurobipy-pandas/gurobi-optimods shape, repeated on a third vendor). THE LICENCE CLOSES A
    SECOND, INDEPENDENT DOOR: unlike mosek, the wheel ships a real bundled licenses/LI_en.txt
    (not just a URL) -- IBM''s own International License Agreement for Non-Warranted Programs
    plus the CPLEX Community Edition License Information; clause 3(e) states the Licensee "does
    not 1) use, copy, modify, or distribute the Program except as expressly permitted in this
    Agreement ... or 4) sublicense, rent, or lease the Program", and nothing elsewhere in the
    748-line agreement grants redistribution rights -- the Community Edition is a free-of-charge
    SKU with a reduced Authorized Use (problem-size limits on unregistered use), not a
    differently-licensed one. cpxchecklic (strings: download_license/write_license/"failed to
    parse license") and a docloud.ibm.com API-key endpoint baked into the x86_64 build show
    IBM''s own license-key mechanism gates the full (non-Community) tiers -- a second closed-
    vendor gate on top of the source gap, the same shape as MOSEK''s FlexNet and Gurobi''s PIP
    license. Revisit only if IBM itself ships a riscv64 CPLEX Callable Library (a
    manylinux*_riscv64 wheel on PyPI, or a Linux/RISC-V download on IBM''s own distribution
    channel) AND the Community Edition license is amended to permit redistribution; nothing in a
    build-cplex.yml can close either gap. No worktree/branch/PR created -- settled before any
    YAML was written.'
- pkg: ansys-mapdl-reader
  version: 0.56.0
  home: https://github.com/pyansys/pymapdl-reader
  repo: https://github.com/pyansys/pymapdl-reader
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: topk-sdk
  version: 0.15.0
  home: https://topk.io
  repo: https://github.com/topk-io/topk
  status: queued
  pr: null
  notes: '24 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zope-index
  version: '8.2'
  home: https://zopeindex.readthedocs.io
  repo: https://github.com/zopefoundation/zope.index
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: zccache
  version: 1.13.21
  home: https://pypi.org/project/zccache/
  repo: https://pypi.org/project/zccache/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: kfnetlist
  version: 0.2.2
  home: https://pypi.org/project/kfnetlist/
  repo: https://pypi.org/project/kfnetlist/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp312); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: ligo-skymap
  version: 2.5.4
  home: https://lscsoft.docs.ligo.org/ligo.skymap/
  repo: https://git.ligo.org/lscsoft/ligo.skymap
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: quantile-forest
  version: 1.4.2
  home: https://zillow.github.io/quantile-forest
  repo: https://github.com/zillow/quantile-forest
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: lmdeploy
  version: 0.17.0
  home: https://pypi.org/project/lmdeploy/
  repo: https://pypi.org/project/lmdeploy/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: gpudb
  version: 7.2.3.12
  home: https://www.kinetica.com
  repo: https://www.kinetica.com
  status: queued
  pr: null
  notes: '15 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: falkordblite
  version: 0.10.0
  home: https://www.falkordb.com/
  repo: https://github.com/FalkorDB/falkordblite
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: geode-hybrid-geosciences
  version: 1.18.4
  home: https://pypi.org/project/Geode-Hybrid-Geosciences/
  repo: https://pypi.org/project/Geode-Hybrid-Geosciences/
  status: queued
  pr: null
  notes: '5 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fbuild
  version: 2.5.21
  home: https://pypi.org/project/fbuild/
  repo: https://pypi.org/project/fbuild/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: tesseract-decoder
  version: 0.1.1.dev20260901151234
  home: https://github.com/quantumlib/tesseract-decoder
  repo: https://github.com/quantumlib/tesseract-decoder
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: py-ballisticcalc-exts
  version: 2.3.1
  home: https://github.com/o-murphy/py-ballisticcalc
  repo: https://github.com/o-murphy/py-ballisticcalc
  status: queued
  pr: null
  notes: '16 Linux wheels upstream (abi: cp311,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: resdata
  version: 6.3.3
  home: https://github.com/equinor/resdata
  repo: https://github.com/equinor/resdata
  status: queued
  pr: null
  notes: '4 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: mocpy
  version: 0.20.0
  home: https://cds-astro.github.io/mocpy/
  repo: https://github.com/cds-astro/mocpy
  status: queued
  pr: null
  notes: '19 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: flowtask
  version: 5.12.14
  home: https://github.com/phenobarbital/flowtask
  repo: https://github.com/phenobarbital/flowtask
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: oxapy
  version: 0.11.3
  home: https://pypi.org/project/oxapy/
  repo: https://pypi.org/project/oxapy/
  status: queued
  pr: null
  notes: '74 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyramids-gis
  version: 0.59.0
  home: https://github.com/serapeum-org/pyramids
  repo: https://github.com/serapeum-org/pyramids
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: bio-grumpy
  version: 1.1.4
  home: https://github.com/oxfordmmm/grumpy
  repo: https://github.com/oxfordmmm/grumpy
  status: queued
  pr: null
  notes: '76 Linux wheels upstream (abi: cp310,cp311,cp312,cp38,cp39,pp310,pp311,pp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: orange3
  version: 3.40.0
  home: https://orangedatamining.com/
  repo: https://github.com/biolab/orange3
  status: queued
  pr: null
  notes: '8 Linux wheels upstream (abi: cp310,cp311,cp312,cp313); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fa3-fwd
  version: 0.0.3
  home: https://pypi.org/project/fa3-fwd/
  repo: https://pypi.org/project/fa3-fwd/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: modern-colorthief
  version: 0.3.0
  home: https://github.com/baseplate-admin/modern_colorthief
  repo: https://github.com/baseplate-admin/modern_colorthief
  status: queued
  pr: null
  notes: '68 Linux wheels upstream (abi: cp311,cp312,cp313,cp314,cp315,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: hifitime
  version: 4.3.1
  home: https://nyxspace.com/
  repo: https://nyxspace.com/
  status: queued
  pr: null
  notes: '78 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp315,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pymcubes
  version: 0.1.6
  home: https://github.com/pmneila/PyMCubes
  repo: https://github.com/pmneila/PyMCubes
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1482
  notes: '4 Linux wheels upstream (abi: cp310,cp311,cp312,cp39); no riscv64 on PyPI or pypi.riseproject.dev. CI green (cp312/cp313/cp314/cp314t).'
- pkg: madoka
  version: 0.7.2.1
  home: https://github.com/ikegami-yukino/madoka-python
  repo: https://github.com/ikegami-yukino/madoka-python
  status: queued
  pr: null
  notes: '64 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp38,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: jijmodeling
  version: 2.8.0
  home: https://www.jijzept.com
  repo: https://www.jijzept.com
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: blastdns
  version: 1.9.1
  home: https://pypi.org/project/blastdns/
  repo: https://pypi.org/project/blastdns/
  status: queued
  pr: null
  notes: '86 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-gsl
  version: 10.8.10.post1
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: picows
  version: 2.1.3
  home: https://github.com/tarasko/picows
  repo: https://github.com/tarasko/picows
  status: queued
  pr: null
  notes: '28 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: arc4
  version: 0.5.0
  home: https://github.com/manicmaniac/arc4
  repo: https://github.com/manicmaniac/arc4
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1481
  notes: '6 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp39,pp311); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: polars-cloud
  version: 0.11.0
  home: https://cloud.pola.rs/
  repo: https://www.pola.rs/
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: cp310); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: fbgemm-gpu-nightly-cpu
  version: 2026.9.2
  home: https://github.com/pytorch/fbgemm
  repo: https://github.com/pytorch/fbgemm
  status: queued
  pr: null
  notes: '10 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: doroutes
  version: 1.4.0
  home: https://pypi.org/project/doroutes/
  repo: https://pypi.org/project/doroutes/
  status: queued
  pr: null
  notes: '6 Linux wheels upstream (abi: cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: pyvirtualcam
  version: 0.15.0
  home: https://github.com/letmaik/pyvirtualcam
  repo: https://github.com/letmaik/pyvirtualcam
  status: queued
  pr: null
  notes: '12 Linux wheels upstream (abi: cp310,cp311,cp312,cp313,cp314,cp39); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: passagemath-graphs
  version: 10.8.10
  home: https://passagemath.org/docs/latest
  repo: https://github.com/passagemath/passagemath/releases
  status: queued
  pr: null
  notes: '20 Linux wheels upstream (abi: cp311,cp312,cp313,cp314); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: keeper-pam-webrtc-rs
  version: 2.2.2
  home: https://pypi.org/project/keeper-pam-webrtc-rs/
  repo: https://pypi.org/project/keeper-pam-webrtc-rs/
  status: queued
  pr: null
  notes: '3 Linux wheels upstream (abi: cp38); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: deepseek-harness-runtime-bin
  version: 0.1.2a3
  home: https://github.com/deepseek-ai/deepseek-harness
  repo: https://github.com/deepseek-ai/deepseek-harness
  status: queued
  pr: null
  notes: '2 Linux wheels upstream (abi: py3); no riscv64 on PyPI or pypi.riseproject.dev.'
- pkg: citiespy
  version: 0.6.13
  home: https://github.com/ringsaturn/go-cities.json
  repo: https://github.com/ringsaturn/go-cities.json
  status: published
  pr: null
  notes: Merged and serving on pypi.riseproject.dev.
- pkg: apache-beam
  version: 2.75.0
  home: https://beam.apache.org
  repo: https://github.com/apache/beam
  status: published
  pr: https://github.com/riseproject-dev/python-wheels/pull/1726
  notes: '10 Linux wheels upstream (abi cp310-cp314, x86_64+aarch64), sdist present; no riscv64 anywhere. sdks/python/setup.py confirms a genuinely small native surface: 6 .pyx files plus a handful of setup.py-cythonized .py modules under coders/metrics/runners/transforms/utils, producing 17 .so files total - no Bazel/large-C++ build, a normal cibuildwheel candidate. Validated end-to-end locally (macOS arm64, ~2.5min): `pip wheel <sdist> --no-deps --no-build-isolation` with only cython>=3.2.5,<4 + numpy preinstalled succeeds and produces a real 17-.so wheel. pyproject.toml''s build-system.requires pins grpcio-tools==1.62.1 (py<=3.12) / 1.71.0 (py>=3.13), which has no riscv64 wheel, but it''s unused for the riscv64 bdist: the released sdist ships pre-generated apache_beam/portability/api/*_pb2.py with zero .proto sources alongside them (confirmed against the real PyPI sdist), so gen_protos.py''s "no proto files, reuse existing output" branch never imports grpc_tools - fixed with --no-build-isolation (gotcha 76) instead of patching the pin. Matrix cp312/cp313/cp314 (cp314t dropped: upstream''s own build_wheels.yml matrix never builds a free-threaded wheel for Beam); numpy<2.5.0 and cython riscv64 wheels both cover this range on our registry. Upstream''s own released sdist ships with no LICENSE at all (verified against the real PyPI tarball) even though sdks/python/MANIFEST.in already lists NOTICE/LICENSE/LICENSE.python - they live at the repo root, not sdks/python - so the sdist-build step copies them in first, same gap/fix shape as build-ctranslate2.yml. Testing mirrors upstream exactly (no CIBW_TEST_COMMAND): upstream''s own build_wheels.yml runs zero wheel tests itself (real Beam test suites live in separate massive PostCommit/PreCommit workflows, out of scope). Known caveat for future maintainers: Beam''s mandatory (non-extra) install_requires caps pyarrow<24.0.0 and protobuf<7.0.0.dev0, but pypi.riseproject.dev currently hosts exactly one riscv64 version of each (pyarrow 25.0.1, protobuf 7.35.1), both incompatible with those bounds - so `pip install apache-beam` will not fully resolve on riscv64 today regardless of this port; that''s a downstream ecosystem gap (would need an additional pyarrow<24/protobuf<7 riscv64 build), not a defect in this workflow. PR #1726 all checks green: sdist build, cp312/cp313/cp314 riscv64 bdist builds, check_commit_messages, check_patches, and publish dry-run all passed - dry-run log confirms the three expected wheels (apache_beam-2.75.0-cp31{2,3,4}-cp31{2,3,4}-manylinux_2_31_riscv64.manylinux_2_39_riscv64.whl). Awaiting maintainer review.'
